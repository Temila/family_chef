/**
 * BottomBar Re-export (Phase 11)
 * 实际组件已迁移至 ./composites/BottomBar.jsx；本文件保留作为稳定 import 路径。
 * 消费方仍通过 `import BottomBar from './components/BottomBar'` 引用。
 */
export { default } from './composites/BottomBar';
