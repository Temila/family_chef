/**
 * Sidebar Re-export (Phase 11)
 * 实际组件已迁移至 ./composites/Sidebar.jsx；本文件保留作为稳定 import 路径。
 * App.jsx 与其它消费方仍通过 `import Sidebar from './components/Sidebar'` 引用。
 */
export { default } from './composites/Sidebar';
