---
phase: 01-data-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/guest_invitation.py
  - backend/app/models/order.py
  - backend/app/models/__init__.py
  - backend/alembic/env.py
  - backend/alembic/versions/   # new migration file (revision hash generated by alembic)
  - backend/app/initial_data.py
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03, DATA-05, INV-03]

must_haves:
  truths:
    - "guest_invitations 表存在，包含 token(UUID4)、inviter_id、chef_id、status(String20)、expires_at 字段"
    - "虚拟 guest 用户存在于 users 表中 (is_active=False)，auth_service.py:39 已有检查阻止其登录"
    - "orders 表有 guest_invitation_id 外键列 (nullable)，指向 guest_invitations.id"
    - "Alembic 迁移可正向执行 (upgrade) 且可回滚 (downgrade)"
    - "通过 orders.guest_invitation_id IS NOT NULL 可识别访客订单 (per D-05)"
    - "guest_invitations 表有 4 个索引: token(UNIQUE)、inviter_id、expires_at、(status,expires_at) (per D-09)"
  artifacts:
    - path: "backend/app/models/guest_invitation.py"
      provides: "GuestInvitation SQLAlchemy model"
      contains: "class GuestInvitation"
    - path: "backend/app/models/order.py"
      provides: "guest_invitation_id FK + relationship on Order model"
      contains: "guest_invitation_id"
    - path: "backend/app/models/__init__.py"
      provides: "GuestInvitation export"
      contains: "GuestInvitation"
    - path: "backend/alembic/env.py"
      provides: "guest_invitation model registration for autogenerate"
      contains: "guest_invitation"
    - path: "backend/app/initial_data.py"
      provides: "Virtual guest user seed (create_if_not_exists)"
      contains: "__guest__"
  key_links:
    - from: "backend/app/models/order.py"
      to: "backend/app/models/guest_invitation.py"
      via: "ForeignKey('guest_invitations.id') + relationship"
      pattern: "guest_invitation_id.*ForeignKey.*guest_invitations"
    - from: "backend/app/alembic/env.py"
      to: "backend/app/models/guest_invitation.py"
      via: "import statement registering model with Base.metadata"
      pattern: "from app.models import.*guest_invitation"
    - from: "backend/app/models/__init__.py"
      to: "backend/app/models/guest_invitation.py"
      via: "import + __all__ export"
      pattern: "GuestInvitation"
---

<objective>
创建 GuestInvitation SQLAlchemy 模型、修改 Order 模型添加 guest_invitation_id FK、注册新模型、创建单次 Alembic 迁移（含虚拟 guest 用户插入）、更新 initial_data.py 种子脚本。

Purpose: 建立数据库结构支持访客邀请和访客订单，为 Phase 2 后端服务开发解除阻塞 (per D-01 through D-09)。
Output: GuestInvitation 模型文件、修改后的 Order 模型、可执行的 Alembic 迁移、updated initial_data.py。
</objective>

<execution_context>
@/home/temila/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/temila/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-foundation/01-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create GuestInvitation model + modify Order model + register exports</name>
  <files>
    backend/app/models/guest_invitation.py
    backend/app/models/order.py
    backend/app/models/__init__.py
    backend/alembic/env.py
  </files>
  <read_first>
    backend/app/models/dish.py        # DishChef pattern: status=Column(String(20)), FK, relationship
    backend/app/models/order.py        # Current Order structure: user_id FK, chef_id FK, relationships
    backend/app/models/user.py         # User model: is_active field, role field
    backend/app/models/__init__.py     # Export registration pattern: import + __all__
    backend/alembic/env.py             # Model import line 11: current import list
  </read_first>
  <acceptance_criteria>
    - `backend/app/models/guest_invitation.py` exists and defines class `GuestInvitation(Base)` with `__tablename__ = "guest_invitations"`
    - GuestInvitation has columns: id (Integer PK autoincrement), token (String(36) UNIQUE NOT NULL), inviter_id (Integer FK users.id NOT NULL), chef_id (Integer FK users.id NOT NULL), status (String(20) NOT NULL default="active"), expires_at (DateTime NOT NULL), created_at (DateTime server_default=func.now()), updated_at (DateTime server_default=func.now() onupdate=func.now())
    - GuestInvitation has relationships: inviter → User (foreign_keys=[inviter_id]), chef → User (foreign_keys=[chef_id])
    - GuestInvitation has __table_args__ with 4 indexes per D-09: token UNIQUE, inviter_id plain, expires_at plain, (status, expires_at) composite
    - NO guest_name column exists (per D-03)
    - Order model has new column: guest_invitation_id = Column(Integer, ForeignKey("guest_invitations.id"), nullable=True) (per D-05, D-08)
    - Order model has new relationship: guest_invitation → GuestInvitation (per D-08)
    - GuestInvitation model has back_populates relationship: orders → Order
    - `backend/app/models/__init__.py` imports `GuestInvitation` from `app.models.guest_invitation` and adds `"GuestInvitation"` to `__all__` list
    - `backend/alembic/env.py` line 11 adds `guest_invitation` to the import list
    - File starts with Chinese module docstring per conventions
    - All imports follow: stdlib → third-party → app pattern per conventions
  </acceptance_criteria>
  <action>
    **1. Create `backend/app/models/guest_invitation.py`:**

    按现有模型惯例创建文件。开头中文模块 docstring（参考 dish.py 格式）。

    导入: sqlalchemy 的 Column, Integer, String, DateTime, ForeignKey, Index; sqlalchemy.sql.func; sqlalchemy.orm.relationship; app.database.Base。

    定义 `class GuestInvitation(Base)`, `__tablename__ = "guest_invitations"`。

    列定义（per D-01, D-02, D-08, D-09）:
    - `id` = Column(Integer, primary_key=True, autoincrement=True)
    - `token` = Column(String(36), nullable=False, unique=True) — UUID4 字符串，36 字符
    - `inviter_id` = Column(Integer, ForeignKey("users.id"), nullable=False) — 创建者 (per D-01)
    - `chef_id` = Column(Integer, ForeignKey("users.id"), nullable=False) — 负责厨师 (per D-01)
    - `status` = Column(String(20), nullable=False, default="active") — active/used/expired/revoked (per D-02)
    - `expires_at` = Column(DateTime, nullable=False) — 过期时间 (per D-02)
    - `created_at` = Column(DateTime, nullable=False, server_default=func.now()) — 按现有模型惯例
    - `updated_at` = Column(DateTime, nullable=False, server_default=func.now(), onupdate=func.now()) — 按现有模型惯例

    注意：不加 guest_name 列 (per D-03)。

    Relationships:
    - `inviter` = relationship("User", foreign_keys=[inviter_id])
    - `chef` = relationship("User", foreign_keys=[chef_id])
    - `orders` = relationship("Order", back_populates="guest_invitation")

    `__table_args__` 包含 4 个索引 (per D-09):
    - Index("uq_guest_invitations_token", "token", unique=True)
    - Index("ix_guest_invitations_inviter_id", "inviter_id")
    - Index("ix_guest_invitations_expires_at", "expires_at")
    - Index("ix_guest_invitations_status_expires_at", "status", "expires_at")

    **2. Modify `backend/app/models/order.py`:**

    在 Order 类中，在 `completed_at` 列之后添加:
    - `guest_invitation_id` = Column(Integer, ForeignKey("guest_invitations.id"), nullable=True)

    在 imports 部分无需额外导入（ForeignKey 已存在）。

    在 relationships 部分，在 `chef` relationship 之后添加:
    - `guest_invitation` = relationship("GuestInvitation", back_populates="orders")

    不修改 user_id 列 — 保持 nullable=False (per D-04)。

    **3. Modify `backend/app/models/__init__.py`:**

    在导入行添加: `from app.models.guest_invitation import GuestInvitation`
    在 `__all__` 列表中添加 `"GuestInvitation"` (按字母序放在 "DishSemifinishedIngredient" 之后、"Order" 之前)。

    **4. Modify `backend/alembic/env.py`:**

    修改第 11 行的 import 语句，在 `log` 之后添加 `, guest_invitation`:
    `from app.models import user, ingredient, category, dish, order, preference, favorite, schedule, log, guest_invitation`
  </action>
  <verify>
    <automated>cd /home/temila/family_chef/backend && python -c "
from app.models.guest_invitation import GuestInvitation
from app.models.order import Order
from app.models import GuestInvitation as GI2
assert hasattr(GuestInvitation, 'token')
assert hasattr(GuestInvitation, 'inviter_id')
assert hasattr(GuestInvitation, 'chef_id')
assert hasattr(GuestInvitation, 'status')
assert hasattr(GuestInvitation, 'expires_at')
assert hasattr(GuestInvitation, 'created_at')
assert hasattr(GuestInvitation, 'updated_at')
assert hasattr(GuestInvitation, 'inviter')
assert hasattr(GuestInvitation, 'chef')
assert hasattr(GuestInvitation, 'orders')
assert not hasattr(GuestInvitation, 'guest_name'), 'D-03: guest_name must NOT exist'
assert hasattr(Order, 'guest_invitation_id')
assert hasattr(Order, 'guest_invitation')
assert Order.guest_invitation_id.property.columns[0].nullable == True
assert GI2 is GuestInvitation
print('All model assertions passed')
"</automated>
  </verify>
  <done>
    - GuestInvitation 模型文件存在，包含 token/inviter_id/chef_id/status/expires_at 列 + 4 个索引
    - Order 模型有 guest_invitation_id FK (nullable) + relationship
    - __init__.py 导出 GuestInvitation
    - alembic/env.py 导入 guest_invitation 模块
    - Python import 验证通过
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create Alembic migration + update initial_data.py + verify round-trip</name>
  <files>
    backend/alembic/versions/   # new migration file
    backend/app/initial_data.py
  </files>
  <read_first>
    backend/alembic/versions/8a258d50ee87_phase_6_add_system_logs_indexes.py  # Current HEAD, migration pattern
    backend/alembic/versions/52a06862ef2d_add_meal_date_and_meal_type_to_orders.py  # add_column pattern
    backend/app/initial_data.py        # Seed data pattern: select → check → add → commit
    backend/app/models/guest_invitation.py  # Just created in Task 1 — migration must match
    backend/app/models/order.py        # Just modified — verify guest_invitation_id column
    backend/app/utils/security.py      # hash_password import for guest user seed
  </read_first>
  <acceptance_criteria>
    - 新 Alembic 迁移文件存在于 `backend/alembic/versions/` 目录
    - 迁移的 `down_revision` 指向当前 HEAD `8a258d50ee87`
    - upgrade() 创建 guest_invitations 表（含全部列和 4 个索引 per D-09）
    - upgrade() 在 users 表插入虚拟 guest 用户: username="__guest__", is_active=False (per D-04, D-06)
    - upgrade() 在 orders 表添加 guest_invitation_id 列 (nullable, FK 指向 guest_invitations.id) (per D-05, D-08)
    - downgrade() 按逆序撤销: 删除 orders.guest_invitation_id 列 → 删除 guest_invitations 表 → 删除虚拟 guest 用户
    - `alembic upgrade head` 执行成功（无报错）
    - `alembic downgrade -1` 执行成功（无报错）
    - `alembic upgrade head` 再次执行成功（round-trip 验证）
    - `backend/app/initial_data.py` 中 `create_initial_data()` 函数在 admin 创建后也创建虚拟 guest 用户（username="__guest__", is_active=False, display_name="访客"）
    - 初始数据的 guest 用户使用 `hash_password()` 生成 password_hash（不可用空密码）
    - 迁移中 guest 用户的 password_hash 也通过 `hash_password()` 生成（import from app.utils.security）
  </acceptance_criteria>
  <action>
    **1. 创建 Alembic 迁移文件:**

    使用 `alembic revision` 生成迁移骨架（或手动创建），revision ID 由 alembic 自动生成。

    迁移描述: "add guest_invitations table and guest_invitation_id to orders"

    设置 `down_revision = '8a258d50ee87'`（当前 HEAD）。

    **upgrade() 函数执行 3 步操作（per D-07 单次迁移完成全部）:**

    步骤 A — 创建 guest_invitations 表:
    使用 `op.create_table('guest_invitations', ...)` 创建表，包含所有列:
    - sa.Column('id', sa.Integer(), autoincrement=True, nullable=False)
    - sa.Column('token', sa.String(36), nullable=False)
    - sa.Column('inviter_id', sa.Integer(), nullable=False)
    - sa.Column('chef_id', sa.Integer(), nullable=False)
    - sa.Column('status', sa.String(20), nullable=False, server_default='active')
    - sa.Column('expires_at', sa.DateTime(), nullable=False)
    - sa.Column('created_at', sa.DateTime(), nullable=False, server_default=sa.text('CURRENT_TIMESTAMP'))
    - sa.Column('updated_at', sa.DateTime(), nullable=False, server_default=sa.text('CURRENT_TIMESTAMP'))
    - sa.PrimaryKeyConstraint('id')
    - sa.ForeignKeyConstraint(['inviter_id'], ['users.id'])
    - sa.ForeignKeyConstraint(['chef_id'], ['users.id'])

    然后创建 4 个索引（per D-09）:
    - op.create_index('uq_guest_invitations_token', 'guest_invitations', ['token'], unique=True)
    - op.create_index('ix_guest_invitations_inviter_id', 'guest_invitations', ['inviter_id'])
    - op.create_index('ix_guest_invitations_expires_at', 'guest_invitations', ['expires_at'])
    - op.create_index('ix_guest_invitations_status_expires_at', 'guest_invitations', ['status', 'expires_at'])

    步骤 B — 插入虚拟 guest 用户（per D-04, D-06）:
    从 `app.utils.security` 导入 `hash_password`。
    使用 `op.bulk_insert` 向 users 表插入:
    - username: '__guest__'
    - password_hash: hash_password 生成的不可猜测哈希值（传入一个长随机字符串如 'never-used-guest-account-placeholder-password-!@#$%'）
    - display_name: '访客'
    - role: 'user'
    - is_active: False (per D-06)
    - force_pwd_change: False

    步骤 C — 给 orders 表添加 guest_invitation_id 列（per D-05, D-08）:
    使用 `op.add_column('orders', sa.Column('guest_invitation_id', sa.Integer(), nullable=True))`
    然后 `op.create_foreign_key('fk_orders_guest_invitation_id', 'orders', 'guest_invitations', ['guest_invitation_id'], ['id'])`

    **downgrade() 函数按逆序撤销:**

    步骤 C 反向 — 删除 FK 和列:
    - op.drop_constraint('fk_orders_guest_invitation_id', 'orders', type_='foreignkey')
    - op.drop_column('orders', 'guest_invitation_id')

    步骤 B 反向 — 删除虚拟 guest 用户:
    - op.execute(sa.text("DELETE FROM users WHERE username = '__guest__'"))

    步骤 A 反向 — 删除表（级联删除索引）:
    - op.drop_table('guest_invitations')

    **2. 修改 `backend/app/initial_data.py`:**

    在 `create_initial_data()` 函数中，在 admin 创建逻辑之后（session.commit() 之后），添加创建虚拟 guest 用户的逻辑。

    模式与 admin 创建相同:
    1. select(User).where(User.username == "__guest__") 查询是否已存在
    2. 如果已存在，print("✅ 虚拟访客用户已存在，跳过创建") 并跳过
    3. 如果不存在，创建 User:
       - username: "__guest__"
       - password_hash: hash_password("never-used-guest-account-placeholder-password-!@#$%") — 与迁移中一致的不可猜测密码
       - display_name: "访客"
       - role: "user"
       - is_active: False (per D-06)
       - force_pwd_change: False
    4. session.add(guest_user) + session.commit()
    5. print("✅ 虚拟访客用户创建成功 (__guest__, is_active=False)")

    **3. 验证迁移 round-trip:**

    执行 `alembic upgrade head` 确认正向迁移成功。
    执行 `alembic downgrade -1` 确认回滚成功。
    执行 `alembic upgrade head` 再次确认 round-trip。
  </action>
  <verify>
    <automated>cd /home/temila/family_chef/backend && alembic upgrade head 2>&1 && echo "--- UPGRADE OK ---" && alembic downgrade -1 2>&1 && echo "--- DOWNGRADE OK ---" && alembic upgrade head 2>&1 && echo "--- ROUND-TRIP OK ---"</automated>
  </verify>
  <done>
    - Alembic 迁移文件存在，down_revision='8a258d50ee87'
    - upgrade() 创建 guest_invitations 表 + 插入虚拟 guest 用户 + 添加 orders.guest_invitation_id FK
    - downgrade() 按逆序完整撤销所有变更
    - 迁移 round-trip 验证通过 (upgrade → downgrade → upgrade)
    - initial_data.py 在 admin 创建后也创建虚拟 guest 用户
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → users table | 迁移插入虚拟 guest 用户，password_hash 不可为空或可猜测值 |
| migration → orders table | 添加 FK 列，nullable=True 不影响现有数据 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Spoofing | 虚拟 guest 用户登录 | mitigate | is_active=False 阻止登录 (auth_service.py:39)，密码 hash 使用不可猜测的占位字符串 |
| T-01-02 | Tampering | token 枚举攻击 | accept | UUID4 ~122 bits 熵值，实际不可猜测，低风险无需额外缓解 |
| T-01-03 | Tampering | 迁移 SQL 注入 | accept | 使用 Alembic op.* 抽象层，不使用原始 SQL（除 DELETE __guest__ 使用参数化 sa.text） |
| T-01-04 | Elevation | guest_invitation_id FK 约束绕过 | mitigate | FK 使用 RESTRICT（默认 ondelete），防止删除邀请时产生孤立订单 |
| T-01-SC | Tampering | pip install 依赖 | accept | 本阶段不引入新依赖，使用现有 SQLAlchemy + Alembic |
</threat_model>

<verification>
1. Python import 验证: GuestInvitation 和 Order.guest_invitation_id 可正常导入和访问
2. Alembic round-trip: upgrade head → downgrade -1 → upgrade head 全部成功
3. 迁移后 orders 表结构包含 guest_invitation_id 列
4. 迁移后 guest_invitations 表存在且包含全部 4 个索引
5. 迁移后 users 表包含 username='__guest__', is_active=False 的记录
6. initial_data.py 包含 '__guest__' 用户创建逻辑
</verification>

<success_criteria>
- GuestInvitation 模型包含 token(inviter_id/chef_id/status/expires_at + 4 索引) per D-01, D-02, D-09
- Order.guest_invitation_id FK (nullable) 存在 per D-05, D-08
- 虚拟 guest 用户 (is_active=False) 可通过迁移和 initial_data 两种途径创建 per D-04, D-06, D-07
- Alembic 迁移可正向执行且可回滚
- 不存在 guest_name 列 per D-03
- 所有文件遵循现有中文 docstring + snake_case + PascalCase 惯例
</success_criteria>

<output>
Create `.planning/phases/01-data-foundation/01-01-SUMMARY.md` when done
</output>
