---
phase: 14-ui-bugfix-filter-popup
plan: 07
type: execute
wave: 2
depends_on:
  - 14-05
gap_closure: true
files_modified:
  - frontend/src/pages/ChefDishesPage.jsx
  - frontend/src/pages/AdminIngredientsPage.jsx
autonomous: true
requirements:
  - BUG-04

must_haves:
  truths:
    - "ChefDishesPage ingredient dropdown escapes modal body overflow (BUG-04 partially closed for chef users)"
    - "ChefDishesPage semifinished dropdown escapes modal body overflow"
    - "When chef clicks an ingredient/semifinished item, item is selected (NOT prematurely closed by mousedown)"
    - "AdminIngredientsPage dropdown trigger button has a small visible borderRadius (6px) instead of fully frameless"
    - "ChefDishesPage dropdown click-outside close fires on click (not mousedown) to avoid race with Portal'd menu onClick"
  artifacts:
    - path: "frontend/src/pages/ChefDishesPage.jsx"
      provides: "createPortal-rendered ingredient dropdown (data-ing-dropdown) + semifinished dropdown (data-sf-dropdown)"
      contains: "createPortal"
    - path: "frontend/src/pages/ChefDishesPage.jsx"
      provides: "useEffect close handlers switched from mousedown to click + closest guards + import createPortal"
      contains: "closest.*data-(ing|sf)-dropdown"
    - path: "frontend/src/pages/AdminIngredientsPage.jsx"
      provides: "Inline borderRadius: 6px on 2 trigger buttons (table row + mobile card)"
      contains: "borderRadius: '6px'"
  key_links:
    - from: "frontend/src/pages/ChefDishesPage.jsx"
      to: "react-dom"
      via: "createPortal import"
      pattern: "import \\{ createPortal \\} from 'react-dom'"
    - from: "frontend/src/pages/ChefDishesPage.jsx"
      to: "document.body"
      via: "createPortal(<div data-...>, document.body)"
      pattern: "createPortal\\("
    - from: "frontend/src/pages/ChefDishesPage.jsx"
      to: "closest('[data-ing-dropdown]')"
      via: "useEffect click handler skips menu-internal clicks"
      pattern: "closest\\('\\[data-ing-dropdown\\]'\\)"
    - from: "frontend/src/pages/AdminIngredientsPage.jsx"
      to: "trigger button inline style"
      via: "borderRadius: '6px' inline restores small visible border-radius per BUG-04 SC4"
      pattern: "borderRadius: '6px'"
---

<objective>
关闭 VERIFICATION Gap 2（BUG-04 SC4 "圆角 1/4"）+ 关闭 REVIEW WR-05（ChefDishesPage BUG-04 incomplete migration）。

**WR-05 根因**：14-03 修复 BUG-04 时，把 AdminDishesPage 的 2 个 dropdown 改成 `createPortal`（z-index 1000 逃出 Modal scrim 遮挡），但 ChefDishesPage 仍保留 `position: absolute, z-index: 100` 的 inline 块（line 783-837 + 855-911）。当 chef 编辑菜品且 modal body 滚动时，dropdown 视觉上被 modal 边界裁剪——BUG-04 缺陷对 chef 用户仍存在。ChefDishesPage 也仍用 `mousedown` 监听（line 88, 99），与 AdminDishesPage 已改 `click` 不一致。

**Gap 2 根因**：14-03 完全删掉了 AdminIngredientsPage 触发按钮的 24×24px borderRadius/border，改为 inline-flex transparent + 12dp min target，无可见圆角框。SC4 字面"圆角 1/4"未达成。Plan 决策：廉价实现 `(a) 恢复小可见 borderRadius`——inline 加 `borderRadius: '6px'`（约原 24px 框的 1/4，单边 6px 半径，符合 MD3 small button 视觉尺寸）。

**Purpose:** 关闭 REVIEW WR-05（ChefDishesPage BUG-04 完整迁移）+ VERIFICATION Gap 2（AdminIngredientsPage 触发按钮小可见圆角）。

**Output:** ChefDishesPage 的 2 个 dropdown 与 AdminDishesPage 一致地用 Portal 渲染 + click 守卫，chef 用户编辑菜品时下拉不被遮挡；AdminIngredientsPage 触发按钮保留小可见圆角。
</objective>

<execution_context>
@/home/temila/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/temila/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-ui-bugfix-filter-popup/14-VERIFICATION.md
@.planning/phases/14-ui-bugfix-filter-popup/14-UAT.md
@.planning/phases/14-ui-bugfix-filter-popup/14-REVIEW.md
@.planning/phases/14-ui-bugfix-filter-popup/14-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ChefDishesPage.jsx — 引入 createPortal + 改 2 个 click-outside useEffect 为 click + closest 守卫</name>
  <files>frontend/src/pages/ChefDishesPage.jsx</files>
  <read_first>
    - frontend/src/pages/ChefDishesPage.jsx line 1-30 (imports)
    - frontend/src/pages/ChefDishesPage.jsx line 81-101 (current useEffect for ingDropdown / sfDropdown — mousedown)
    - frontend/src/pages/ChefDishesPage.jsx line 783-837 (current ingredient dropdown inline div)
    - frontend/src/pages/ChefDishesPage.jsx line 855-911 (current semifinished dropdown inline div)
    - frontend/src/pages/AdminDishesPage.jsx line 776-825 (reference — already-Portal'd trigger pattern)
    - frontend/src/pages/AdminDishesPage.jsx line 84-108 (reference — click + closest useEffect)
    - frontend/src/pages/AdminDishesPage.jsx line 1087-1183 (reference — Portal menu render)
  </read_first>
  <action>
    1. **import**：在 ChefDishesPage.jsx 顶部 import 区追加 `import { createPortal } from 'react-dom';`（与 AdminDishesPage.jsx:2 同一位置惯例）。

    2. **替换 line 81-90 (ingDropdown useEffect)**：
       改前：
       ```jsx
       useEffect(() => {
         if (!showIngDropdown) return;
         const handler = (e) => {
           if (ingDropdownRef.current && !ingDropdownRef.current.contains(e.target)) {
             setShowIngDropdown(false);
           }
         };
         document.addEventListener('mousedown', handler);
         return () => document.removeEventListener('mousedown', handler);
       }, [showIngDropdown]);
       ```
       改后（参考 AdminDishesPage.jsx:84-95）：
       ```jsx
       useEffect(() => {
         if (!showIngDropdown) return;
         const handler = (e) => {
           if (ingDropdownRef.current && !ingDropdownRef.current.contains(e.target)) {
             if (e.target.closest('[data-ing-dropdown]')) return;
             setShowIngDropdown(false);
           }
         };
         document.addEventListener('click', handler);
         return () => document.removeEventListener('click', handler);
       }, [showIngDropdown]);
       ```

    3. **替换 line 92-101 (sfDropdown useEffect)**：与 ingDropdown 同样替换为 click + `closest('[data-sf-dropdown]')` 守卫。

    4. **trigger onClick 当前状态**：当前 line 776 / 812 附近两个 trigger onClick **不包含** `getBoundingClientRect()` 捕获 coords 的逻辑——只看 `ingDropdownRef.current` / `sfDropdownRef.current` 是不够的，因为 portal 渲染条件 line 783-837 / 855-911 用的是 `position: absolute, z-index: 100` 内联块（尚非 portal），不依赖 coords state。**Task 2 step 1 会检查并按需补 `*DropdownCoords` state + 在 trigger onClick 中追加 `setIngDropdownCoords({ ... })` 调用**；本 Task 不修改 trigger 部分。

    5. **不要修改**：line 76-78 loadIngredients effect、line 92-101 useEffect 之外的代码、line 781-783 / 854-855 trigger 容器（仅 onClick 保留）。
  </action>
  <accept_criteria>
    - ChefDishesPage.jsx 包含 `import { createPortal } from 'react-dom';`（grep 命中 1 次）
    - ChefDishesPage.jsx 包含 `document.addEventListener('click', handler)` 2 次（ing + sf，grep 命中 2 次）
    - ChefDishesPage.jsx 0 命中 `addEventListener('mousedown', handler)`（已改为 click）
    - ChefDishesPage.jsx 包含 `closest('[data-ing-dropdown]')` 和 `closest('[data-sf-dropdown]')` 各 1 次
    - `npm run build` exit 0
  </accept_criteria>
  <verify>
    <automated>rg -n 'import.*createPortal.*react-dom' frontend/src/pages/ChefDishesPage.jsx; rg -c "addEventListener\\('click', handler\\)" frontend/src/pages/ChefDishesPage.jsx; rg -n "addEventListener\\('mousedown', handler\\)" frontend/src/pages/ChefDishesPage.jsx; rg -n "closest\\('\\[data-(ing|sf)-dropdown\\]'\\)" frontend/src/pages/ChefDishesPage.jsx; cd frontend && npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>
    ChefDishesPage.jsx 已引入 createPortal import；2 个 useEffect 从 mousedown 改为 click + closest 守卫，与 AdminDishesPage:84-108 保持一致。Portal 渲染将在 Task 2 完成。
  </done>
</task>

<task type="auto">
  <name>Task 2: ChefDishesPage.jsx — 2 个 dropdown inline div 改 createPortal (data-ing-dropdown + data-sf-dropdown)</name>
  <files>frontend/src/pages/ChefDishesPage.jsx</files>
  <read_first>
    - frontend/src/pages/ChefDishesPage.jsx line 783-837 (current ingredient dropdown inline div — POSITION ABSOLUTE)
    - frontend/src/pages/ChefDishesPage.jsx line 855-911 (current semifinished dropdown inline div — POSITION ABSOLUTE)
    - frontend/src/pages/AdminDishesPage.jsx line 1087-1139 (reference — Portal ingredient menu)
    - frontend/src/pages/AdminDishesPage.jsx line 1141-1183 (reference — Portal semifinished menu)
  </read_first>
  <action>
    1. **state 检查（先 grep）**：
       ```bash
       rg -n 'ingDropdownCoords|sfDropdownCoords' frontend/src/pages/ChefDishesPage.jsx
       ```
       如果 `ingDropdownCoords` / `sfDropdownCoords` state 缺失，需在 state 定义区（line 35 附近）添加：
       ```jsx
       const [ingDropdownCoords, setIngDropdownCoords] = useState(null);
       const [sfDropdownCoords, setSfDropdownCoords] = useState(null);
       ```
       并在 trigger onClick 中（line 779 附近）追加 `setIngDropdownCoords({ top: rect.bottom + 4, left: rect.left, width: rect.width })`（与 AdminDishesPage.jsx:780 模式一致）。

    2. **替换 line 783-837 (ingredient dropdown)**：将 `<div ...>` 包裹的 inline 内容用 `createPortal(<div data-ing-dropdown ...>, document.body)` 渲染。
       改前（line 783-837 包裹）：
       ```jsx
       {showIngDropdown && (
         <div style={{ position: 'absolute', left: 0, right: 0, top: '100%', zIndex: 100, ... }}>
           <Input ... />
           <Chip ... />
           {filteredIngForDropdown.slice(0, 50).map(ing => (
             <div key={ing.id} className="preference-search-item" onClick={() => { toggleIngredient(ing.id); }} ...>
             ...
           ))}
         </div>
       )}
       ```
       改后（参考 AdminDishesPage.jsx:1087-1138 形状，关键是改为 `position: 'fixed' + top: ingDropdownCoords.top + left: ingDropdownCoords.left + zIndex: 1000`）：
       ```jsx
       {showIngDropdown && ingDropdownCoords && createPortal(
         <div data-ing-dropdown style={{
           position: 'fixed', top: ingDropdownCoords.top, left: ingDropdownCoords.left, width: ingDropdownCoords.width, zIndex: 1000,
           background: 'var(--md-color-surface-container-lowest)', border: '1px solid var(--md-color-outline-variant)',
           borderRadius: 'var(--md-radius-md)', boxShadow: 'var(--md-elevation-2)',
           maxHeight: 280, overflowY: 'auto', padding: 'var(--md-spacing-1)',
         }}>
           <div style={{ padding: 'var(--md-spacing-2) var(--md-spacing-2) var(--md-spacing-2)'}}>
             <Input ... />
             <Chip ... />
           </div>
           <div style={{ overflowY: 'auto' }}>
             {filteredIngForDropdown.length === 0 ? (
               <div style={{ padding: 'var(--md-spacing-3)', textAlign: 'center', color: 'var(--md-color-on-surface-variant)', fontSize: '0.85rem' }}>无匹配食材</div>
             ) : (
               filteredIngForDropdown.slice(0, 50).map(ing => (
                 <div key={ing.id} className="preference-search-item" onClick={() => { toggleIngredient(ing.id); }} style={{ cursor: 'pointer' }}>
                   <span>{ing.name}</span>
                   <span style={{ fontSize: '0.7rem', color: 'var(--md-color-on-surface-variant)', marginLeft: 'auto'}}>
                     {ingredientCategories.find(c => c.name === ing.category)?.name || ''}
                   </span>
                 </div>
               ))
             )}
           </div>
         </div>,
         document.body
       )}
       ```

    3. **替换 line 855-911 (semifinished dropdown)**：同样包成 `createPortal(<div data-sf-dropdown ...>, document.body)`，结构与 AdminDishesPage.jsx:1141-1183 形状一致。

    4. **保留**：trigger 父级容器 `position: relative` 不变（line 793 附近与 line 855 附近），仍由其 `getBoundingClientRect()` 提供 trigger rect。

    5. **不要修改**：line 778-781 已有 trigger onClick 逻辑（仅当 state 缺失时补 `setIngDropdownCoords`）；其他无关样式、Card 组件、Button 列表等。
  </action>
  <accept_criteria>
    - ChefDishesPage.jsx 包含 `createPortal(` 2 次（ing + sf，grep 命中 2 次）
    - ChefDishesPage.jsx 包含 `data-ing-dropdown` 1 次（菜单根 div 属性）
    - ChefDishesPage.jsx 包含 `data-sf-dropdown` 1 次
    - 2 个 dropdown 根 div 均使用 `position: 'fixed', zIndex: 1000`（grep 命中 `position: 'fixed', top: ingDropdownCoords.top` 和 `position: 'fixed', top: sfDropdownCoords.top` 至少 1 次每 portal）
    - 若 state 缺失，`ingDropdownCoords` 和 `sfDropdownCoords` 已添加到 useState 区
    - `npm run build` exit 0
  </accept_criteria>
  <verify>
    <automated>rg -c 'createPortal\\(' frontend/src/pages/ChefDishesPage.jsx; rg -n 'data-ing-dropdown|data-sf-dropdown' frontend/src/pages/ChefDishesPage.jsx; rg -n "position: 'fixed', top: (ing|sf)DropdownCoords" frontend/src/pages/ChefDishesPage.jsx; cd frontend && npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>
    ChefDishesPage 2 个 dropdown 已用 createPortal 渲染到 document.body，z-index 1000 逃出 modal body overflow 遮挡。trigger state 已设置 `*DropdownCoords`（若原本缺失则补上）。ChefDishesPage 菜品编辑 modal 中下拉菜单与 AdminDishesPage 视觉行为一致。WR-05 关闭。
  </done>
</task>

<task type="auto">
  <name>Task 3: AdminIngredientsPage.jsx — 2 个 trigger 按钮 inline `borderRadius: '6px'` 恢复小可见圆角 + 总验证</name>
  <files>frontend/src/pages/AdminIngredientsPage.jsx</files>
  <read_first>
    - frontend/src/pages/AdminIngredientsPage.jsx line 332-357 (current table-row trigger button inline style — transparent + no borderRadius)
    - frontend/src/pages/AdminIngredientsPage.jsx line 419-444 (current mobile-card trigger button inline style — transparent + no borderRadius)
    - .planning/phases/14-ui-bugfix-filter-popup/14-VERIFICATION.md Gap 2 (圆角 1/4 字面未实现)
    - .planning/phases/14-ui-bugfix-filter-popup/14-04-SUMMARY.md (Decisions: dropdown 24×24 → 12dp min target + transparent)
  </read_first>
  <action>
    1. **修改 line 332-357 (table-row trigger button inline style)**：在 `style={{ ... }}` 块内、在 `padding: 'var(--md-spacing-1)'` 之后追加 `borderRadius: '6px',`。

    2. **修改 line 419-444 (mobile-card trigger button inline style)**：同样在 `padding: 'var(--md-spacing-1)'` 之后追加 `borderRadius: '6px',`。

    3. **不要修改**：其他 60 多个 `compact-interactive-target` 消费者（chef-select-item / wish-picker-item / menu-item / guest-add-btn / theme-toggle / qty-stepper / preference-tag / quick-action / field-trigger 等）的样式——本 Task 只针对 AdminIngredientsPage 的 2 个 ▾ 触发按钮添加可见小圆角。

    4. **保留 12dp min target**：不修改 `.compact-interactive-target` 规则（line 533-544 styles.css），按钮 hit area 仍 ≥ 12px，视觉上加 6px 圆角边框使 SC4 字面"圆角 1/4"得到视觉表达。

    5. **最终验证 + 提交**：
       ```bash
       # 静态 grep 验证
       rg -n 'import.*createPortal.*react-dom' frontend/src/pages/ChefDishesPage.jsx
       rg -c 'createPortal\(' frontend/src/pages/ChefDishesPage.jsx
       rg -n 'data-ing-dropdown|data-sf-dropdown' frontend/src/pages/ChefDishesPage.jsx
       rg -n "addEventListener\\('click', handler\\)" frontend/src/pages/ChefDishesPage.jsx
       rg -n "addEventListener\\('mousedown', handler\\)" frontend/src/pages/ChefDishesPage.jsx  # 应 0 命中
       rg -n "closest\\('\\[data-(ing|sf)-dropdown\\]'\\)" frontend/src/pages/ChefDishesPage.jsx
       rg -c "borderRadius: '6px'" frontend/src/pages/AdminIngredientsPage.jsx
       rg -n 'data-dropdown-id' frontend/src/pages/AdminIngredientsPage.jsx
       # build 验证
       cd frontend && npm run build  # exit 0
       # dist 验证
       rg -c "data-ing-dropdown" dist/assets/*.js
       rg -c "data-sf-dropdown" dist/assets/*.js
       ```

    6. **人类视觉验证清单**（在 build 通过后验证）：
       - 登录 chef 账号 → ChefDishesPage → 编辑任一菜品 → modal 中点击 "点击选择食材..." 触发器 → 下拉菜单应在 modal 之上正常弹出（不被 modal 边界裁剪）
       - 在搜索输入框中输入"西" → 列表过滤正常 → 点击 "西红柿" 食材项 → 食材被加入菜品 list（菜单不提前关闭）
       - 同样流程测试半成品菜单
       - 登录 admin 账号 → AdminIngredientsPage → 桌面表格视图 → 任一食材行触发器 ▾ 按钮 → 视觉上有 6px 圆角（hover 时背景从透明变为 var(--md-color-surface-container-high)，圆角视觉明显）
       - 移动端卡片视图 → 同样 ▾ 按钮有 6px 圆角

    7. 单一 commit `fix(14-07): ChefDishesPage Portal 迁移 + 食材触发按钮 6px 圆角 (WR-05 + Gap 2)` 提交。
  </action>
  <accept_criteria>
    - ChefDishesPage.jsx 已 createPortal + 2x data-* attributes + click (not mousedown) + closest guards
    - AdminIngredientsPage.jsx 包含 `borderRadius: '6px'` 2 次（table-row + mobile-card trigger 按钮）
    - 2 个按钮仍保留 `className="compact-interactive-target"` + `data-dropdown-id` + `aria-label` 不变
    - `npm run build` exit 0
    - dist JS 包含 `data-ing-dropdown` 和 `data-sf-dropdown` 字符串
    - 5 项人类视觉验证清单全部通过
    - 单一 commit
  </accept_criteria>
  <verify>
    <automated>bash -c 'cd /home/temila/family_chef; echo "=== ChefDishesPage Portal migration ==="; rg -n "import.*createPortal.*react-dom" frontend/src/pages/ChefDishesPage.jsx; rg -c "createPortal\\(" frontend/src/pages/ChefDishesPage.jsx; rg -n "data-ing-dropdown|data-sf-dropdown" frontend/src/pages/ChefDishesPage.jsx; rg -n "addEventListener\\(.click., handler.\\)" frontend/src/pages/ChefDishesPage.jsx; echo "=== mousedown listener must be gone (expect 0 hits) ==="; (rg -n "addEventListener\\(.mousedown., handler.\\)" frontend/src/pages/ChefDishesPage.jsx || echo "as expected: 0 mousedown handlers remain"); rg -n "closest\\(.\\[data-(ing|sf)-dropdown\\].\\)" frontend/src/pages/ChefDishesPage.jsx; echo "=== AdminIngredientsPage borderRadius ==="; rg -c "borderRadius: .6px." frontend/src/pages/AdminIngredientsPage.jsx; cd frontend && npm run build 2>&1 | tail -3; cd /home/temila/family_chef; echo "=== dist JS contains data-ing-dropdown ==="; (rg -c "data-ing-dropdown" frontend/dist/assets/*.js || echo "FAIL: data-ing-dropdown missing from dist"); echo "=== dist JS contains data-sf-dropdown ==="; (rg -c "data-sf-dropdown" frontend/dist/assets/*.js || echo "FAIL: data-sf-dropdown missing from dist")'</automated>
  </verify>
  <done>
    REVIEW WR-05 关闭：ChefDishesPage 2 个 dropdown 已迁移到 createPortal + click + closest 守卫，与 AdminDishesPage 视觉行为一致；chef 用户编辑菜品时下拉不被遮挡。VERIFICATION Gap 2 关闭：AdminIngredientsPage 2 个 trigger 按钮 inline borderRadius: '6px' 恢复小可见圆角。5 项视觉验证清单全部通过。单一 commit 提交。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User keyboard/mouse → trigger | trigger onClick / onKeyDown stopPropagation + capture coords; document click handler skips menu-internal clicks via closest guard |
| Modal body scroll → Portaled menu | Portaled menu lives in document.body; modal body overflow-y:auto does not clip fixed-position menu; position: fixed + z-index 1000 escapes visual stacking |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-07-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependencies; only modifications to ChefDishesPage.jsx + AdminIngredientsPage.jsx |
| T-14-07-01 | Spoofing | Portal-rendered menu not waiting for click before close | mitigate | click handler (not mousedown) + closest('[data-ing-dropdown]') guard ensures React onClick on menu item fires before any close; same pattern as AdminDishesPage 14-04 fix |
| T-14-07-02 | Information Disclosure | ChefDishesPage missing ingDropdownCoords state | mitigate | Task 2 accepts the file may lack state; if state missing, plan adds `useState` + setter in trigger onClick; agent verifies via grep before declaring done |
| T-14-07-03 | Denial of Service | Inline borderRadius rules bloat | accept | 6px string literal · 2 inline edits; negligible impact |
| T-14-07-04 | Elevation of Privilege | Other compact-interactive-target consumers get unintended borderRadius | mitigate | Plan restricts edit to AdminIngredientsPage.jsx 2 trigger buttons; .compact-interactive-target rule in styles.css unchanged; grep verifies only 2 occurrences in target file |
| T-14-07-05 | Repudiation | Portal mount lifecycle | mitigate | React unmounts Portaled content when `showIngDropdown && coords` becomes false; existing useEffect cleanup removes click + scroll listeners; no leaks |
| T-14-07-06 | Tampering | Outdated trigger after coords change | accept | When portal closes, trigger retain its original DOM; reopen re-captures rect; behavior identical to AdminDishesPage |
</threat_model>

<verification>
- `npm run build` exits 0
- ChefDishesPage has createPortal + 2x data-* attributes + click (not mousedown) + closest guards
- AdminIngredientsPage has 2x borderRadius: '6px' inline
- dist JS bundle includes `data-ing-dropdown` and `data-sf-dropdown` strings
- 5-item visual checklist passes (chef dropdown escapes modal + admin trigger has visible 6px radius)
- Single atomic commit on feature/ui-rebuild branch
</verification>

<success_criteria>
- [ ] REVIEW WR-05 closed (ChefDishesPage dropdown uses Portal + click + closest, matching AdminDishesPage)
- [ ] VERIFICATION Gap 2 closed (AdminIngredientsPage trigger button has visible 6px borderRadius)
- [ ] `npm run build` exit 0
- [ ] Visual checklist 5 items passed
- [ ] Single commit
- [ ] verifier 重新核查：WR-05 + Gap 2 关闭
</success_criteria>

<output>
Create `.planning/phases/14-ui-bugfix-filter-popup/14-07-SUMMARY.md` when done
</output>
