---
phase: 14-ui-bugfix-filter-popup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/composites/BottomBar.css
  - frontend/src/css/styles.css
  - frontend/src/components/WishCard.jsx
  - frontend/src/components/DishCard.jsx
autonomous: true
requirements:
  - BUG-01
  - BUG-02
  - BUG-03
  - BUG-04

must_haves:
  truths:
    - "Bottom-bar fills viewport width on every mobile/tablet resolution (≤1023px) with no left/right gap"
    - "All <table> elements across admin pages use the .pc-data-table class (audit passes)"
    - "Table headers align with first content column via th::before (≥32px, currently 48px)"
    - "Mobile wish list cards render single-column with uniform row height; footer/actions pinned to bottom of each card"
    - "Dish cards (admin dish cards on mobile) likewise stretch uniformly with footer pinned to bottom"
  artifacts:
    - path: "frontend/src/components/composites/BottomBar.css"
      provides: "BUG-01 fix — width:100%, no max-width cap, no translateX centering"
      contains: "width: 100%;"
    - path: "frontend/src/css/styles.css"
      provides: "D-08 compact-interactive-target selector + BUG-02 th::before width 48px"
      contains: "compact-interactive-target"
    - path: "frontend/src/components/WishCard.jsx"
      provides: "BUG-03 — cardStyle declares flex column + footer marginTop:auto"
      contains: "marginTop: 'auto'"
    - path: "frontend/src/components/DishCard.jsx"
      provides: "BUG-03 cross-card consistency — same flex column + footer marginTop:auto"
      contains: "marginTop: 'auto'"
  key_links:
    - from: "BottomBar.css"
      to: "tokens.css"
      via: "var(--md-nav-height) preserved"
      pattern: "md-nav-height"
    - from: "styles.css"
      to: "tokens.css"
      via: "48dp touch-target rule stays; 12dp override appended after"
      pattern: "min-width: 48px"
    - from: "WishCard.jsx"
      to: "Card primitive"
      via: "cardStyle passed via style prop; spread into .md-card via Card.jsx ...rest"
      pattern: "flexDirection: 'column'"
---

<objective>
Knock out BUG-01 (bottom-bar width), BUG-02 (table th::before width), and BUG-03 (mobile wish/dish card uniform height with footer pinned to bottom) as a parallel file cluster. Establish the D-08 CSS reset class in styles.css so Wave 3 Plan 03 can apply it to the AdminIngredientsPage + AdminDishesPage dropdowns.

Purpose: These four independent CSS/component fixes have no functional dependency on each other or on Wave 1 (Plan 01). They share no files with Plan 01 (Sheet.jsx, Sheet.css, Modal.css). Lands ahead of Wave 3 so the Sheet migration can immediately bind to the new CSS pattern.

Output: 4 files modified; no new files; no new tokens; no new behavior beyond visual layout fix.
</objective>

<execution_context>
@/home/temila/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/temila/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-ui-bugfix-filter-popup/14-CONTEXT.md
@.planning/phases/14-ui-bugfix-filter-popup/14-PATTERNS.md

# Source files to read first (per deep_work_rules)
@frontend/src/components/composites/BottomBar.css
@frontend/src/css/styles.css
@frontend/src/components/WishCard.jsx
@frontend/src/components/DishCard.jsx
@frontend/src/components/primitives/Card.css
@frontend/src/components/primitives/Card.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix BUG-01 BottomBar width</name>
  <files>
    frontend/src/components/composites/BottomBar.css
  </files>
  <read_first>
    - frontend/src/components/composites/BottomBar.css (full file — the broken max-width 420px / centering at lines 12-13, 25-27; desktop hide ≥1024px at lines 28-30)
    - frontend/src/css/styles.css (lines 225, 230, 235 — .order-bar, .cart-bar, .cart-detail-panel — these ALSO use left:50%/translateX(-50%) and must NOT be changed; risk-mitigation per PATTERNS.md Risk 1)
    - .planning/phases/14-ui-bugfix-filter-popup/14-CONTEXT.md D-12 (verbatim spec for the change)
  </read_first>
  <action>
    Apply D-12 to `frontend/src/components/composites/BottomBar.css`. The change is contained to the `.md-bottom-bar` selector block only.

    In the `.md-bottom-bar { ... }` rule (lines 9-24): remove `left: 50%` (line 12) and `transform: translateX(-50%)` (line 13). Keep `width: 100%`. Remove `max-width: 420px` (line 15).

    Then delete the entire `@media (min-width: 640px) { .md-bottom-bar { max-width: 768px; } }` block (lines 25-27) — this was the tablet-capping rule that contributed to the gap.

    Preserve every other rule in the file unchanged:
    - `.md-tab`, `.md-tab--active`, `.md-tab--active::before`, active-pill hover, child layering, `.md-tab__icon`, `.md-tab__label`, and the `@media (min-width: 1024px) { .md-bottom-bar { display: none; } }` rule.

    The desktop-hiding rule stays exactly as-is — BottomBar must remain `display: none` at ≥1024px per the existing media query (PATTERNS.md Risk 7).

    Do NOT touch any other CSS file. Do NOT modify `.order-bar` / `.cart-bar` / `.cart-detail-panel` — those still legitimately use left:50%/translateX(-50%) for their own layout (different visual contract per PATTERNS.md Risk 1).
  </action>
  <behavior>
    - On viewports 360px..1023px: bottom-bar spans the full viewport width with no left/right gap
    - On viewports ≥1024px: bottom-bar is display:none (desktop unchanged)
  </behavior>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('frontend/src/components/composites/BottomBar.css','utf8');if(c.includes('max-width: 420px'))throw new Error('residual max-width: 420px');if(c.includes('max-width: 768px'))throw new Error('residual max-width: 768px');if(c.includes('translateX(-50%)'))throw new Error('residual translateX(-50%)');if(c.includes('left: 50%'))throw new Error('residual left: 50%');if(!c.includes('@media (min-width: 1024px)'))throw new Error('desktop hide media query missing');if(!c.includes('display: none'))throw new Error('desktop hide rule missing');console.log('BottomBar.css BUG-01 fix: OK')"</automated>
  </verify>
  <done>
    - BottomBar.css contains no `max-width` rule on `.md-bottom-bar`
    - BottomBar.css contains no `translateX(-50%)` rule on `.md-bottom-bar`
    - BottomBar.css contains no `left: 50%` rule on `.md-bottom-bar`
    - BottomBar.css still has the `width: 100%` declaration
    - BottomBar.css still has the `@media (min-width: 1024px) { .md-bottom-bar { display: none; } }` block
    - .md-tab, .md-tab--active, active pill, hover state, and safe-area-inset-bottom all unchanged
  </done>
</task>

<task type="auto">
  <name>Task 2: styles.css — D-08 reset + BUG-02 th::before width bump</name>
  <files>
    frontend/src/css/styles.css
  </files>
  <read_first>
    - frontend/src/css/styles.css lines 348-354 (existing `.pc-data-table th::before` 32px — will bump to 48px per D-13)
    - frontend/src/css/styles.css lines 519-527 (the 48dp global touch-target rule that D-08 *overrides* — the override class MUST come AFTER this rule to win the cascade per CONTEXT D-08)
    - .planning/phases/14-ui-bugfix-filter-popup/14-CONTEXT.md D-08 (compact-interactive-target selector list, exactly: button, a, input, select, textarea, [role="button"], [tabindex]:not([tabindex="-1"]), .theme-toggle, .qty-stepper button, .menu-item, .chef-select-item, .wish-picker-item, .guest-add-btn, .preference-tag button, .quick-action)
  </read_first>
  <action>
    Two additive edits to `frontend/src/css/styles.css`. Both are non-breaking inserts; do not modify any other rule.

    Edit A — D-08 reset class (insert AFTER line 527, the closing brace of the 48dp global rule, so cascade order is "later wins"):
    Add a Chinese comment block introducing the rule. Then add:
    ```
    .compact-interactive-target,
    .compact-interactive-target.chef-select-item,
    .compact-interactive-target.wish-picker-item,
    .compact-interactive-target.menu-item,
    .compact-interactive-target.guest-add-btn,
    .compact-interactive-target.theme-toggle,
    .compact-interactive-target .qty-stepper button,
    .compact-interactive-target .preference-tag button,
    .compact-interactive-target .quick-action {
      min-width: 12px;
      min-height: 12px;
    }
    ```
    Per D-08's locked spec, this class is applied only via explicit `className` prop in JSX (Wave 3 Plan 03), never blanket-applied via element selectors. The above selector list is the AUTHORITATIVE variant from CONTEXT.md D-08 — do not extend, contract, or rename the list. The selectors `.compact-interactive-target.chef-select-item`, `.compact-interactive-target.wish-picker-item`, etc. require the dual-class presence (e.g., both `compact-interactive-target` AND one of the consumer classes). The base `.compact-interactive-target` selector alone catches plain toggle buttons that don't also carry a consumer class.

    Edit B — BUG-02 th::before (modify line 351 within the existing `.pc-data-table th::before` rule):
    Change `width: 32px;` to `width: 48px;`. This bumps the placeholder per D-13 fallback ("如 ::before 32px 与某些首列内容仍不对齐，可调宽到 48px"). Keep `content: ''`, `display: inline-block;`, `margin-right: var(--md-spacing-2);`, `vertical-align: middle;` unchanged.

    Audit (verification step, no edit needed): ensure all 7 <table> usages in `frontend/src/pages/*.jsx` carry `className="pc-data-table"`. The grep source pattern is `<table className`. Existing files (AdminDishesPage:532, AdminIngredientsPage:307, AdminCategoriesPage:137, AdminChefsPage:99, AdminLogsPage:82, ChefDishesPage:552, AdminUsersPage:170) all already pass per PATTERNS.md Wave 2 audit.
  </action>
  <behavior>
    - An element with className `compact-interactive-target` (alone or combined with any of the listed consumer classes) has min-size 12px regardless of the 48dp global rule
    - An element without that class retains 48dp min-size from the existing global rule
    - Table th cells get a 48px-wide ::before placeholder inline-block before the label text, pushing the visible label to the right and aligning with first content column
  </behavior>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('frontend/src/css/styles.css','utf8');if(!c.match(/\.compact-interactive-target,/m))throw new Error('D-08 selector base missing');if(!c.includes('.compact-interactive-target.theme-toggle'))throw new Error('D-08 theme-toggle dual selector missing');if(!c.includes('min-width: 12px'))throw new Error('D-08 min-width 12px missing');if(!c.match(/\.pc-data-table th::before\s*\{[^}]*width:\s*48px/m))throw new Error('th::before width 48px missing');if(!c.includes('width: 32px'))console.log('OK — 32px removed');else if(c.match(/\.pc-data-table th::before\s*\{[^}]*width:\s*32px/m))throw new Error('th::before still 32px — not bumped');else{console.log('32px not in th::before rule (other usages ok)');} const tables=require('fs').readdirSync('frontend/src/pages').filter(f=>f.endsWith('.jsx')).reduce((acc,f)=>{const t=fs.readFileSync('frontend/src/pages/'+f,'utf8');return acc+(t.match(/<table className=[^>]+pc-data-table/g)||[]).length;},0);console.log('pc-data-table table count: '+tables);if(tables<5)throw new Error('fewer than 5 tables with pc-data-table');console.log('styles.css D-08 + BUG-02 fix: OK');"</automated>
  </verify>
  <done>
    - styles.css contains the `.compact-interactive-target, ...` selector block with EXACTLY the 9 listed selectors (no more, no less) per D-08
    - styles.css `.pc-data-table th::before` rule declares `width: 48px;`
    - styles.css still preserves the 48dp global rule at lines 519-527 (no removal of the existing touch-target contract)
    - At least 5 `.jsx` files in `frontend/src/pages/` contain `<table className="pc-data-table"` (audit passes — current count is 7)
  </done>
</task>

<task type="auto">
  <name>Task 3: BUG-03 wish/dish card uniform-height with footer pinned to bottom</name>
  <files>
    frontend/src/components/WishCard.jsx
    frontend/src/components/DishCard.jsx
  </files>
  <read_first>
    - frontend/src/components/WishCard.jsx lines 146-159 (cardStyle — must add flex/flexDirection); lines 199-213 (footer slot — must bump marginTop to 'auto')
    - frontend/src/components/WishCard.jsx lines 193-258 (verify card body structure does not break when card root becomes flex column)
    - frontend/src/components/DishCard.jsx full file (lines 51-77 — Card primitive usage with footer slot; need analogous flex column + marginTop auto)
    - frontend/src/components/primitives/Card.css lines 9-16 (`.md-card` root — already has `display: flex; flex-direction: column;` — the root is fine; only the consumer's inline `style` needs to allow grid stretch by NOT setting height)
    - frontend/src/components/primitives/Card.jsx lines 17-63 (forwards `...rest` to root div → consumer's `style` prop lands on `.md-card` — confirms the inline style approach)
    - .planning/phases/14-ui-bugfix-filter-popup/14-PATTERNS.md "WishCard.jsx (MODIFY — BUG-03)" section
    - .planning/phases/14-ui-bugfix-filter-popup/14-CONTEXT.md D-05/D-06 (3-card consistency: WishCard / DishCard / AdminIngredientsPage mobile — D-05 says uniform layout strategy; Plan 03 applies to AdminIngredientsPage, this task covers WishCard + DishCard)
  </read_first>
  <action>
    Two independent edits across the two card files. The CSS-grid container (`.mobile-card-list--grid`) already uses `display: grid` with default `align-items: stretch` (styles.css:290-308 area per PATTERNS.md Risk 6) so cards stretch to row height automatically. The missing piece is per-card flex-column root + margin-top:auto on the footer slot, so footer pushes to the bottom regardless of content height.

    Edit A — WishCard.jsx:
    1. Locate the `cardStyle` object (lines 146-159). Add two new properties at the END of the object literal (after `...(highlight ? {...} : {})` spread, so they always apply):
       ```
       display: 'flex',
       flexDirection: 'column',
       ```
       This makes the `.md-card` root a flex column. The existing `minHeight: '120px'` stays as a floor.
    2. Locate the footer slot JSX (lines 199-213). Inside the inline `style` object, change `marginTop: 'var(--md-spacing-2)'` to `marginTop: 'auto'`. Keep `paddingTop`, `borderTop`, `display: 'flex'`, `gap`, `flexWrap` unchanged.

    Edit B — DishCard.jsx (cross-card consistency per D-05):
    1. The DishCard.jsx file does NOT currently declare a local cardStyle — it relies on Card primitive defaults. To match WishCard, add a `cardStyle` const above the return and pass it via the `style` prop. Specifically:
       - Declare `const cardStyle = { display: 'flex', flexDirection: 'column' };` near the top of the function (before the return statement).
       - Pass `style={cardStyle}` to the `<Card>` component (currently no `style` prop is set; the line that reads `<Card\n      variant="elevated"\n      image={image}\n      ...` needs a `style={cardStyle}` line added).
    2. Locate the existing footer slot (lines 56-65) inside `<Card ... footer={!simple && (...)}>`. Add `marginTop: 'auto'` to the outer footer div's inline style (the div with `display: 'flex', gap, justifyContent: 'space-between'`). The footer does not have a top border or paddingTop currently; leave both unset. The only insertion needed is `marginTop: 'auto'` in the style object.

    Do not change other behavior in either file — only the 3 specific insertions listed.
  </action>
  <behavior>
    - WishCard: when 3 wish cards render in a grid row, all 3 cards stretch to the height of the tallest one; the action footer stays pinned to the bottom of each card
    - DishCard: when rendered in a grid (e.g., `.dish-grid` on admin pages), the footer (badge + availability) stays pinned to the bottom of each card
    - Visual: no change for static non-grid contexts (existing minHeight 120px + flex column has no observable effect when card is content-height-sized)
  </behavior>
  <verify>
    <automated>node -e "const fs=require('fs');const wish=fs.readFileSync('frontend/src/components/WishCard.jsx','utf8');if(!wish.includes(\"display: 'flex'\"))throw new Error('WishCard missing display flex');if(!wish.includes(\"flexDirection: 'column'\"))throw new Error('WishCard missing flexDirection column');if(!wish.match(/footer=\\{[^}]*marginTop:\\s*'auto'\\s*\\}/m))throw new Error('WishCard footer missing marginTop auto');const dish=fs.readFileSync('frontend/src/components/DishCard.jsx','utf8');if(!dish.includes(\"flexDirection: 'column'\"))throw new Error('DishCard missing flexDirection column');if(!dish.includes(\"marginTop: 'auto'\"))throw new Error('DishCard missing marginTop auto');console.log('WishCard + DishCard BUG-03 fix: OK');"</automated>
  </verify>
  <done>
    - WishCard.jsx cardStyle contains `display: 'flex'` AND `flexDirection: 'column'` properties
    - WishCard.jsx footer slot inline style contains `marginTop: 'auto'` (was `var(--md-spacing-2)`)
    - DishCard.jsx declares a `cardStyle` const with `display: 'flex'` + `flexDirection: 'column'`, passed to `<Card style={cardStyle} ...>`
    - DishCard.jsx footer outer div inline style contains `marginTop: 'auto'`
    - No other behavior (Ripple binding, click handlers, readonly check) modified
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- None new. Plan 02 is pure-CSS + JSX-style-attribute change. No new HTTP, no new endpoints, no auth surface.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04-S | Spoofing | Card style changes | accept | No identity surface touched |
| T-14-04-T | Tampering | D-08 selector + th::before width | accept | Static CSS via existing tokens |
| T-14-04-I | Information Disclosure | — | n/a | No data flow changes |
| T-14-04-D | Denial of Service | BottomBar.css change | accept | Mobile layout regression risk monitored via the desktop-hide media query preserved at ≥1024px |
| T-14-04-E | Elevation of Privilege | WishCard/DishCard change | accept | No role-check bypass |

D-08 is documented in CSS comment as INTENTIONAL compact-target override (12dp < MD3 48dp). Accessibility audit by Phase 15 UAT to confirm consumer classes remain semantically correct.
</threat_model>

<verification>
- BottomBar.css: `node -e` script proves no `max-width` / `translateX` / `left: 50%` survives on `.md-bottom-bar`, and the desktop-hide rule remains intact
- styles.css: source-grep proves the D-08 selector list contains exactly the 9 selectors per CONTEXT D-08, no more; `.pc-data-table th::before` declares `width: 48px`; existing 48dp global rule at lines 519-527 unchanged
- 7 `<table className="pc-data-table"` occurrences across pages/ — audit PASS (4 are admin pages, the 3 the user listed in the bug report)
- WishCard.jsx + DishCard.jsx: source-grep proves both files have the new `display: 'flex', flexDirection: 'column'` on the cardStyle (or inline) and `marginTop: 'auto'` on the footer style
</verification>

<success_criteria>
- SC1 (BUG-01): BottomBar fills viewport width on mobile (360px–1023px) with no left/right gap; hidden ≥1024px per existing media query
- SC2 (BUG-02): Table headers align with first content column on all 7 admin pages
- SC3 (BUG-03): Mobile wish list cards render single-column with uniform row height; action footer pinned to bottom of each card
- SC4-precursor (BUG-04): D-08 selector class is available in styles.css so Wave 3 can apply `className="compact-interactive-target"` to dropdown triggers
</success_criteria>

<output>
Create `.planning/phases/14-ui-bugfix-filter-popup/14-02-SUMMARY.md` when done
</output>
