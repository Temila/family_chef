---
phase: 14-ui-bugfix-filter-popup
plan: 05
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - frontend/src/css/styles.css
  - frontend/src/pages/AdminDishesPage.jsx
  - frontend/src/pages/ChefDishesPage.jsx
  - frontend/src/pages/AdminUsersPage.jsx
  - frontend/src/pages/AdminChefsPage.jsx
autonomous: true
requirements:
  - BUG-02

must_haves:
  truths:
    - "Operator reports '表头错位' as resolved across all 7 pages using .pc-data-table"
    - "First-column header text aligns vertically with first-column body text (NOT with avatar) on every page"
    - "Pages with avatar leading content (4 pages) get additional 36px + gap padding-left on both th and td"
    - "Pages without leading content (3 pages) keep baseline padding-left — first column reads flush"
    - "Table is not shifted horizontally relative to its container — both header and body share the same first-column offset"
  artifacts:
    - path: "frontend/src/css/styles.css"
      provides: "Diff'd .pc-data-table th:first-child + .pc-data-table--with-leading modifier (removes universal 48px hack)"
      contains: "pc-data-table--with-leading"
    - path: "frontend/src/pages/AdminDishesPage.jsx"
      provides: "table className adds pc-data-table--with-leading (line 550)"
      contains: "pc-data-table--with-leading"
    - path: "frontend/src/pages/ChefDishesPage.jsx"
      provides: "table className adds pc-data-table--with-leading (line 552)"
      contains: "pc-data-table--with-leading"
    - path: "frontend/src/pages/AdminUsersPage.jsx"
      provides: "table className adds pc-data-table--with-leading (line 170)"
      contains: "pc-data-table--with-leading"
    - path: "frontend/src/pages/AdminChefsPage.jsx"
      provides: "table className adds pc-data-table--with-leading (line 99)"
      contains: "pc-data-table--with-leading"
  key_links:
    - from: "frontend/src/css/styles.css"
      to: ".pc-data-table--with-leading selector"
      via: "CSS cascade — modifier class adds extra padding-left to th:first-child + td:first-child"
      pattern: "pc-data-table--with-leading th:first-child"
    - from: "frontend/src/pages/AdminDishesPage.jsx"
      to: "frontend/src/css/styles.css"
      via: "table className adds the modifier class"
      pattern: "pc-data-table pc-data-table--with-leading"
---

<objective>
修复 BUG-02 表头对齐缺陷，覆盖全部 7 个 `.pc-data-table` 页面（之前由 Plan 14-04 实现的 `th:first-child { padding-left: calc(... + 48px + ...) }` 仅对带头像的 4 个页面正确，对 AdminIngredientsPage / AdminCategoriesPage / AdminLogsPage 反而过度偏移——表整体被推右、左侧多出一段空白带，operator 的"表头错位"反馈实质未消除）。

**核心改动**：放弃"所有 `.pc-data-table` 通用偏移"的 hack，改为：
1. CSS 拆分为 `.pc-data-table`（基线 padding-left = `var(--md-spacing-3)`） + `.pc-data-table--with-leading`（额外 `+ 36px + var(--md-spacing-2)` 偏移，对齐到头像后正文）
2. 给 4 个带头像的页面（AdminDishesPage / ChefDishesPage / AdminUsersPage / AdminChefsPage）的 `<table>` element 添加 `pc-data-table pc-data-table--with-leading` 类
3. 3 个无头像的页面（AdminIngredientsPage / AdminCategoriesPage / AdminLogsPage）保持 `pc-data-table` 基线

**Purpose:** 关闭 VERIFICATION Gap 1（BUG-02 功能性对齐失败，非语义达成）——operator 原文："表头错位的问题依然没有解决"。

**Output:** 全部 7 个 `.pc-data-table` 页面在任意 ≥1024px 桌面分辨率下，第一列表头文字与第一列表体文字（不算头像）严格在同一垂直对齐线；表整体不偏移、左右边界对称。
</objective>

<execution_context>
@/home/temila/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/temila/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-ui-bugfix-filter-popup/14-VERIFICATION.md
@.planning/phases/14-ui-bugfix-filter-popup/14-UAT.md
@.planning/phases/14-ui-bugfix-filter-popup/14-REVIEW.md
@.planning/phases/14-ui-bugfix-filter-popup/14-CONTEXT.md
@.planning/phases/14-ui-bugfix-filter-popup/14-PATTERNS.md
@.planning/phases/14-ui-bugfix-filter-popup/14-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 重构 styles.css 表格首列偏移规则——拆分为基线 + --with-leading 修饰类</name>
  <files>frontend/src/css/styles.css</files>
  <read_first>
    - frontend/src/css/styles.css (lines 344-357 — current .pc-data-table th/td/tr/tbody::before rules)
    - frontend/src/css/styles.css (lines 316-318 — .pc-user-cell flex container)
    - frontend/src/pages/AdminDishesPage.jsx (lines 562-577 — first td uses .pc-user-cell with 36×36 avatar)
    - frontend/src/pages/AdminUsersPage.jsx (lines 180-192 — first td uses .pc-user-cell with .avatar avatar-sm)
    - frontend/src/pages/AdminChefsPage.jsx (lines 110-118 — first td uses .pc-user-cell with .avatar avatar-sm)
    - frontend/src/pages/ChefDishesPage.jsx (lines 563-579 — first td uses .pc-user-cell with 36×36 avatar)
    - frontend/src/pages/AdminIngredientsPage.jsx (lines 321-323 — first td is plain text fontWeight:600, no leading)
    - frontend/src/pages/AdminCategoriesPage.jsx (lines 148-153 — first td is plain text fontWeight:600, no leading)
    - frontend/src/pages/AdminLogsPage.jsx (lines 92-96 — first td is plain text, no leading)
  </read_first>
  <action>
    1. 在 `frontend/src/css/styles.css` 删掉原 line 348-351 的规则块：
       ```
       /* BUG-02: 第一列表头 padding-left 偏移，与含编辑按钮的表体内容对齐 */
       .pc-data-table th:first-child {
         padding-left: calc(var(--md-spacing-3) + 48px + var(--md-spacing-2));
       }
       ```
       替换为以下三个规则块（见下），并保留原 line 344-347 `.pc-data-table th` 和 line 352-357 `.pc-data-table td` 不变。

    2. 在原 position 插入：
       ```css
       /* BUG-02 (Phase 14-05): 首列基线 padding-left — 对齐到首列 cell 文本，无头像偏移 */
       .pc-data-table th:first-child,
       .pc-data-table td:first-child {
         padding-left: var(--md-spacing-3);
       }

       /* BUG-02 带头像修饰类：4 个页面 (AdminDishesPage / ChefDishesPage / AdminUsersPage / AdminChefsPage)
          首列含 36px 头像 + 8px gap，th/td 同步偏移 36px + gap 剩余 padding-left
          = 12px + 36px + 8px = 56px */
       .pc-data-table--with-leading th:first-child,
       .pc-data-table--with-leading td:first-child {
         padding-left: calc(var(--md-spacing-3) + 36px + var(--md-spacing-2));
       }
       ```

    3. token 引用验证：
       - `var(--md-spacing-3)` = 12px (verified in styles.css:430 `padding-left: var(--md-spacing-5)` 引用同系列 token)
       - `var(--md-spacing-2)` = 8px (verified in styles.css:387 `.pc-user-cell { gap: var(--md-spacing-2) }`)
       - 36px = 头像尺寸，hardcoded（与 AdminDishesPage.jsx:567 / ChefDishesPage.jsx:568 的 inline `width: 36, height: 36` 一致）

    4. 不要修改 `.pc-data-table tr { position: relative; overflow: hidden; z-index: 0; }` 和 `.pc-data-table tbody tr::before` 状态层规则（line 355-357）。

    5. 不要修改 `.pc-data-table` 基类（line 341-343）、`.pc-data-table th` 基础规则（line 344-347）、`.pc-data-table td` 基础规则（line 352-354）—— 仅替换 line 348-351 的 4 行块。
  </action>
  <accept_criteria>
    - styles.css line 348-351 旧规则 `.pc-data-table th:first-child { padding-left: calc(var(--md-spacing-3) + 48px + var(--md-spacing-2)); }` 已删除（grep 0 命中）
    - styles.css 包含 `.pc-data-table th:first-child, .pc-data-table td:first-child { padding-left: var(--md-spacing-3); }`（grep 命中）
    - styles.css 包含 `.pc-data-table--with-leading th:first-child, .pc-data-table--with-leading td:first-child { padding-left: calc(var(--md-spacing-3) + 36px + var(--md-spacing-2)); }`（grep 命中）
    - `npm run build` exit 0
  </accept_criteria>
  <verify>
    <automated>rg -n 'pc-data-table th:first-child' frontend/src/css/styles.css; rg -n 'pc-data-table--with-leading' frontend/src/css/styles.css; cd frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    styles.css 偏移规则已重构为基线 + 修饰类双层结构。基线 `.pc-data-table th:first-child, .pc-data-table td:first-child { padding-left: var(--md-spacing-3); }` 适用于无头像页面；修饰类 `.pc-data-table--with-leading th:first-child, .pc-data-table--with-leading td:first-child { padding-left: calc(var(--md-spacing-3) + 36px + var(--md-spacing-2)); }` 适用于带头像页面。`npm run build` 通过，无 CSS 解析错误。
  </done>
</task>

<task type="auto">
  <name>Task 2: 给 4 个带头像页面添加 `pc-data-table--with-leading` 修饰类</name>
  <files>frontend/src/pages/AdminDishesPage.jsx, frontend/src/pages/ChefDishesPage.jsx, frontend/src/pages/AdminUsersPage.jsx, frontend/src/pages/AdminChefsPage.jsx</files>
  <read_first>
    - frontend/src/pages/AdminDishesPage.jsx line 550 (current `<table className="pc-data-table">`)
    - frontend/src/pages/ChefDishesPage.jsx line 552 (current `<table className="pc-data-table">`)
    - frontend/src/pages/AdminUsersPage.jsx line 170 (current `<table className="pc-data-table">`)
    - frontend/src/pages/AdminChefsPage.jsx line 99 (current `<table className="pc-data-table">`)
    - styles.css 任务 1 后的最新 .pc-data-table--with-leading 规则
  </read_first>
  <action>
    1. **AdminDishesPage.jsx line 550**: 将 `<table className="pc-data-table">` 改为 `<table className="pc-data-table pc-data-table--with-leading">`

    2. **ChefDishesPage.jsx line 552**: 同样改为 `<table className="pc-data-table pc-data-table--with-leading">`

    3. **AdminUsersPage.jsx line 170**: 同样改为 `<table className="pc-data-table pc-data-table--with-leading">`

    4. **AdminChefsPage.jsx line 99**: 同样改为 `<table className="pc-data-table pc-data-table--with-leading">`

    5. **不要修改** 3 个无头像页面的 className：
       - AdminIngredientsPage.jsx line 311 保持 `<table className="pc-data-table">`
       - AdminCategoriesPage.jsx line 137 保持 `<table className="pc-data-table">`
       - AdminLogsPage.jsx line 82 保持 `<table className="pc-data-table">`

    6. 每个文件仅做一处字符串替换（className）；不要触碰其他业务逻辑、行号、或缩进。
  </action>
  <accept_criteria>
    - 4 个带头像页面（AdminDishesPage / ChefDishesPage / AdminUsersPage / AdminChefsPage）的 `<table className>` 已包含 `pc-data-table--with-leading`（grep 命中各 1 次）
    - 3 个无头像页面（AdminIngredientsPage / AdminCategoriesPage / AdminLogsPage）的 `<table className>` 仍为 `pc-data-table`（无 `with-leading`，grep 0 命中）
    - 7 个 `.pc-data-table` 引用计数仍为 7（rg -l pc-data-table frontend/src/pages 仍返回 7 个文件）
    - `npm run build` exit 0
  </accept_criteria>
  <verify>
    <automated>rg -n 'pc-data-table--with-leading' frontend/src/pages/; cd frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    4 个带头像页面已添加 `pc-data-table--with-leading` 修饰类；3 个无头像页面保持基线。CSS 重构 + JSX 类挂载组合完成。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 7 页面对齐行为枚举——per-page 视觉验证矩阵 + 回归 grep</name>
  <files>frontend/src/css/styles.css, frontend/src/pages/AdminDishesPage.jsx, frontend/src/pages/ChefDishesPage.jsx, frontend/src/pages/AdminUsersPage.jsx, frontend/src/pages/AdminChefsPage.jsx, frontend/src/pages/AdminIngredientsPage.jsx, frontend/src/pages/AdminCategoriesPage.jsx, frontend/src/pages/AdminLogsPage.jsx</files>
  <read_first>
    - 已完成 14-05 Task 1 + Task 2 的 styles.css + 4 个页面 JSX 修改
    - frontend/src/css/styles.css line 344-360 (改后的偏移规则)
    - 4 个带头像页面第一列定义: AdminDishesPage:563-577, ChefDishesPage:564-579, AdminUsersPage:182-192, AdminChefsPage:111-118
    - 3 个无头像页面第一列定义: AdminIngredientsPage:322-324, AdminCategoriesPage:151-152, AdminLogsPage:93-96
  </read_first>
  <action>
    1. **Per-page 行为矩阵验证**（build 完成后由人类视觉验证；agent 在 verify 中枚举静态预期）：
       - **AdminDishesPage** (line 550 表 `<table className="pc-data-table pc-data-table--with-leading">`)：第一列 36px 头像 + 8px gap + 菜名 + 描述。预期：表头"菜品"字左缘与菜名"红烧肉"左缘在同一垂直对齐线（距离 td 左缘 56px = 12+36+8）。
       - **ChefDishesPage** (line 552)：与 AdminDishesPage 同结构。预期：表头"菜品"字左缘与菜名左缘对齐（56px）。
       - **AdminUsersPage** (line 170)：第一列含 `.avatar avatar-sm` (~36px) + 用户名 + @username。预期：表头"用户信息"字左缘与用户名左缘对齐（56px）。
       - **AdminChefsPage** (line 99)：第一列含 `.avatar avatar-sm` (~36px) + 厨师名。预期：表头"厨师"字左缘与厨师名左缘对齐（56px）。
       - **AdminIngredientsPage** (line 311 `<table className="pc-data-table">` 不含 with-leading)：第一列仅纯文本 `fontWeight: 600` 食材名。预期：表头"名称"字左缘与食材名"西红柿"左缘对齐（距离 td 左缘 12px = 仅 md-spacing-3）。
       - **AdminCategoriesPage** (line 137)：第一列纯文本 `fontWeight: 600` 分类名。预期：表头"名称"字左缘与分类名左缘对齐（12px）。
       - **AdminLogsPage** (line 82)：第一列纯文本时间戳。预期：表头"时间"字左缘与时间戳左缘对齐（12px）。

    2. **静态回归 grep**（agent 执行）：
       ```bash
       # 0 命中：旧 universal 48px hack 已删除
       rg -n 'pc-data-table th:first-child \{ padding-left: calc\(var\(--md-spacing-3\) \+ 48px' frontend/src/css/styles.css
       # 1+ 命中：基线规则存在
       rg -n 'pc-data-table th:first-child, .pc-data-table td:first-child \{ padding-left: var\(--md-spacing-3\)' frontend/src/css/styles.css
       # 1+ 命中：with-leading 修饰类存在
       rg -n 'pc-data-table--with-leading th:first-child, .pc-data-table--with-leading td:first-child' frontend/src/css/styles.css
       # 4 命中：4 个带头像页面已挂修饰类
       rg -c 'pc-data-table--with-leading' frontend/src/pages/AdminDishesPage.jsx frontend/src/pages/ChefDishesPage.jsx frontend/src/pages/AdminUsersPage.jsx frontend/src/pages/AdminChefsPage.jsx
       # 3 命中：3 个无头像页面仍仅 pc-data-table
       rg -c '<table className="pc-data-table"' frontend/src/pages/AdminIngredientsPage.jsx frontend/src/pages/AdminCategoriesPage.jsx frontend/src/pages/AdminLogsPage.jsx
       # 仍 7 个文件用 .pc-data-table
       rg -l 'pc-data-table' frontend/src/pages/
       ```

    3. **dist 验证**（build 后）：
       ```bash
       # dist CSS 文件应包含 with-leading 修饰类
       rg 'pc-data-table--with-leading' frontend/dist/assets/*.css
       ```

    4. 提交 commit: `fix(14-05): BUG-02 表头对齐覆盖全 7 页 — 拆分基线 + --with-leading 修饰类`
  </action>
  <accept_criteria>
    - 7 个 `.pc-data-table` 页面 per-page 行为符合上述矩阵（4 个 56px + 3 个 12px）
    - 静态 grep：旧 universal 48px rule 0 命中；基线规则 1 命中；with-leading 规则 1 命中；4 个带头像页面各 1 命中 with-leading；3 个无头像页面 0 命中 with-leading；7 个文件用 .pc-data-table
    - dist CSS 包含 `pc-data-table--with-leading` 选择器
    - `npm run build` exit 0；dist 大小变化 < 1KB
    - 单一 commit 提交，含描述"BUG-02 表头对齐覆盖全 7 页"
  </accept_criteria>
  <verify>
    <automated>bash -c 'cd /home/temila/family_chef; echo "=== old universal rule must be gone (expect 0 hits) ==="; (rg -n "pc-data-table th:first-child \\{ padding-left: calc\\(var\\(--md-spacing-3\\) \\+ 48px" frontend/src/css/styles.css && echo "FAIL: old universal rule still present" || echo "OK: old universal rule removed"); echo "=== baseline rule ==="; rg -n "pc-data-table th:first-child, .pc-data-table td:first-child" frontend/src/css/styles.css; echo "=== with-leading rule ==="; rg -n "pc-data-table--with-leading" frontend/src/css/styles.css; echo "=== 4 avatar pages must have with-leading (expect count=1 each) ==="; rg -c "pc-data-table--with-leading" frontend/src/pages/AdminDishesPage.jsx frontend/src/pages/ChefDishesPage.jsx frontend/src/pages/AdminUsersPage.jsx frontend/src/pages/AdminChefsPage.jsx; echo "=== 3 non-avatar pages must NOT have with-leading (expect count=0 each) ==="; (rg -c "pc-data-table--with-leading" frontend/src/pages/AdminIngredientsPage.jsx frontend/src/pages/AdminCategoriesPage.jsx frontend/src/pages/AdminLogsPage.jsx || echo "as expected: 0 hits on non-avatar pages"); echo "=== 7 files use pc-data-table ==="; rg -l "pc-data-table" frontend/src/pages/ | wc -l; cd frontend && npm run build 2>&1 | tail -3; cd /home/temila/family_chef; echo "=== dist CSS contains with-leading ==="; (rg "pc-data-table--with-leading" frontend/dist/assets/*.css || echo "FAIL: with-leading missing from dist CSS")'</automated>
  </verify>
  <done>
    BUG-02 表头对齐覆盖全 7 个 `.pc-data-table` 页面：4 个带头像页面（AdminDishesPage / ChefDishesPage / AdminUsersPage / AdminChefsPage）通过 `pc-data-table--with-leading` 修饰类获得 56px 偏移；3 个无头像页面（AdminIngredientsPage / AdminCategoriesPage / AdminLogsPage）保持 12px 基线。Operator 反馈"表头错位的问题依然没有解决"被关闭。所有 7 页面 + 视觉验证矩阵 + 静态 grep + dist CSS 全检通过。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Author CSS → user agent | CSS rewrite touches padding-left values, no injection risk; all changes use existing tokens |
| Page JSX → CSS class lookup | Modifier class pc-data-table--with-leading is added/not added by page author; CSS cascade handles non-applied pages |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-05-SC | Tampering | npm/pip/cargo installs | mitigate | This plan introduces NO new dependencies; modifications only to existing files; no install commands |
| T-14-05-01 | Repudiation | Agent mis-applies modifier class | mitigate | Per-page 7-row behavior matrix in Task 3 acceptance_criteria explicitly enumerates each page's expected className + offset value; 4-page className verification grep ensures no omission |
| T-14-05-02 | Information Disclosure | Visual regression on mobile | accept | .pc-data-table is hidden <1024px via .pc-data-table-wrap { display: none } (verified at styles.css:289); mobile view uses .mobile-card-list regardless; no mobile regression risk |
| T-14-05-03 | Denial of Service | N/A | accept | Static CSS only; no runtime cost beyond current rules |
| T-14-05-04 | Elevation of Privilege | N/A | accept | CSS-only change; no privilege boundary crossed |
| T-14-05-05 | Spoofing | ClassName typo | mitigate | Task 3 acceptance_criteria uses precise grep patterns `pc-data-table pc-data-table--with-leading` to verify exact className; build will fail if class contains typo (CSS rule won't apply) |
| T-14-05-06 | Tampering | Forgotten page misses modifier | mitigate | Per-page matrix in Task 3 + rg -c 'pc-data-table--with-leading' returns count per file; 4 expected for avatar pages + 0 for non-avatar pages |
</threat_model>

<verification>
- `npm run build` exits 0
- All 7 `.pc-data-table` pages per-page behavior as enumerated in Task 3 acceptance_criteria
- Visual verification at 1280px viewport: 第一列表头文字与第一列表体文字（不算头像）严格对齐
- Table horizontal position relative to container unchanged (no left/right shift)
- dist CSS contains `pc-data-table--with-leading` selector
- Single atomic commit on feature/ui-rebuild branch
</verification>

<success_criteria>
- [ ] Operator feedback "表头错位的问题依然没有解决" closed
- [ ] BUG-02 SC2 全部 7 页面功能性对齐达成
- [ ] styles.css 偏移规则从 universal 48px hack 拆分为基线 + --with-leading 修饰类
- [ ] 4 个带头像页面挂修饰类，3 个无头像页面保持基线
- [ ] `npm run build` exit 0
- [ ] PER-PAGE 行为矩阵全部 7 项达成
- [ ] 单一 commit 提交
- [ ] verifier 重新核查：VERIFICATION Gap 1 关闭
</success_criteria>

<output>
Create `.planning/phases/14-ui-bugfix-filter-popup/14-05-SUMMARY.md` when done
</output>
