---
phase: 07-wish-list-frontend
plan: 03
type: execute
wave: 3
depends_on:
  - 07-02
files_modified:
  - frontend/src/App.jsx
  - frontend/src/components/Sidebar.jsx
  - frontend/src/components/BottomBar.jsx
  - frontend/src/pages/UserWishesPage.jsx
  - frontend/src/pages/ChefWishesPage.jsx
autonomous: true
requirements:
  - UX-01
  - UX-02

must_haves:
  truths:
    - "All three wish pages are reachable from both desktop (Sidebar) and mobile (BottomBar) navigation, with the wish nav items placed exactly as D-02 specifies per role"
    - "Deep links of the form /wishes/:id (Phase 6) resolve to the user's role-appropriate wish page (UserWishesPage / ChefWishesPage / AdminWishesPage) with `?wish=:id` highlight directive preserved"
    - "When `?wish=:id` is present in the URL, the matching WishCard receives a `wish-card-highlight` class for 4 seconds and the page scrolls to it; if not found within 5 loaded pages, a toast appears and the query param is cleared"
    - "Active nav state on Sidebar and BottomBar correctly highlights the wish entries when on /my-wishes, /chef/wishes, or /admin/wishes (and does not falsely activate on /admin which is a prefix of /admin/wishes)"
    - "After Wave 3, `npm run build` exits 0 and targeted ESLint over the 13 Phase-7 files exits 0 (full lint baseline remains red but is documented as known-pre-existing)"
  artifacts:
    - path: "frontend/src/App.jsx"
      provides: "Three wish routes + /wishes/:id compatibility redirect"
      contains: "path=\"/my-wishes\""
      contains: "path=\"/chef/wishes\""
      contains: "path=\"/admin/wishes\""
      contains: "path=\"/wishes/:id\""
    - path: "frontend/src/components/Sidebar.jsx"
      provides: "Three new role-branch nav entries (admin/chef/user)"
      contains: "'💡', label: '我的愿望', path: '/my-wishes'"
      contains: "'💡', label: '愿望管理', path: '/chef/wishes'"
      contains: "'💡', label: '愿望总览', path: '/admin/wishes'"
    - path: "frontend/src/components/BottomBar.jsx"
      provides: "Three new role-branch mobile tabs"
      contains: "'💡', label: '愿望', path: '/my-wishes'"
      contains: "'💡', label: '愿望', path: '/chef/wishes'"
      contains: "'💡', label: '愿望', path: '/admin/wishes'"
    - path: "frontend/src/pages/UserWishesPage.jsx"
      provides: "?wish=:id highlight + scroll-to + missing-card toast"
      contains: "searchParams.get('wish')"
    - path: "frontend/src/pages/ChefWishesPage.jsx"
      provides: "?wish=:id highlight + scroll-to + missing-card toast (also covers admin via wrapper)"
      contains: "searchParams.get('wish')"
  key_links:
    - from: "frontend/src/App.jsx"
      to: "frontend/src/pages/UserWishesPage.jsx"
      via: "ProtectedRoute wraps element"
      pattern: "element=\\{<UserWishesPage /\\>\\}"
    - from: "frontend/src/App.jsx"
      to: "frontend/src/pages/ChefWishesPage.jsx"
      via: "ProtectedRoute wraps element"
      pattern: "element=\\{<ChefWishesPage /\\>\\}"
    - from: "frontend/src/App.jsx"
      to: "WishDeepLinkRedirect inline component"
      via: "useAuth + Navigate"
      pattern: "WishDeepLinkRedirect"
    - from: "frontend/src/pages/UserWishesPage.jsx"
      to: "document.querySelector('[data-wish-id]')"
      via: "scrollIntoView on highlight match"
      pattern: "data-wish-id"
---

<objective>
Wire the wish-list feature into the navigation layer and integrate end-to-end:

1. Register `/my-wishes`, `/chef/wishes`, `/admin/wishes` routes in `App.jsx` with appropriate `ProtectedRoute` role gates.
2. Add the Phase-6 `/wishes/:id` deep-link compatibility redirect that routes to the viewer's role-appropriate page with `?wish=:id` highlight directive.
3. Append the three locked nav entries (D-02) to `Sidebar.jsx` for desktop (≥1024px) and to `BottomBar.jsx` for mobile (≤1023px).
4. Implement `?wish=:id` highlight + scroll-to behavior in `UserWishesPage` and `ChefWishesPage` (covers admin via the wrapper).
5. Run the verification gate: `npm run build` + targeted ESLint over all 13 Phase-7 files.

Purpose: Wave 1 built the components; Wave 2 built the pages; Wave 3 makes the feature discoverable, reachable on mobile, and integrated with Phase-6 deep links.

Output: 5 modified files (App.jsx, Sidebar.jsx, BottomBar.jsx, UserWishesPage.jsx, ChefWishesPage.jsx). Build green; targeted ESLint zero-warning.
</objective>

<execution_context>
@/home/temila/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/temila/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-wish-list-frontend/07-CONTEXT.md
@.planning/phases/07-wish-list-frontend/07-RESEARCH.md
@.planning/phases/07-wish-list-frontend/07-PATTERNS.md
@.planning/phases/07-wish-list-frontend/07-UI-SPEC.md

# Wave 1 + Wave 2 outputs (now exist)
@frontend/src/api/client.js
@frontend/src/utils/index.js
@frontend/src/css/styles.css
@frontend/src/components/WishCard.jsx
@frontend/src/components/WishFormModal.jsx
@frontend/src/components/WishRejectModal.jsx
@frontend/src/components/WishAdvanceModal.jsx
@frontend/src/components/ConfirmModal.jsx
@frontend/src/pages/UserWishesPage.jsx
@frontend/src/pages/ChefWishesPage.jsx
@frontend/src/pages/AdminWishesPage.jsx

# Files to MODIFY in this plan
@frontend/src/App.jsx
@frontend/src/components/Sidebar.jsx
@frontend/src/components/BottomBar.jsx

# Phase 6 deep-link origin
@.planning/phases/06-notifications-integration/06-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: App.jsx — register 3 wish routes + /wishes/:id deep-link redirect</name>
  <files>frontend/src/App.jsx</files>
  <read_first>
    - frontend/src/App.jsx (whole file — existing flat Route shape inside PcLayout)
    - .planning/phases/06-notifications-integration/06-CONTEXT.md lines 46-63 (deep-link origin and expected URL pattern)
    - .planning/phases/07-wish-list-frontend/07-PATTERNS.md lines 689-760 (App.jsx route registration pattern + WishDeepLinkRedirect component)
    - .planning/phases/07-wish-list-frontend/07-UI-SPEC.md §11 (Routing and URL Contract — locked requiredRoles, URL params, deep-link behavior)
  </read_first>
  <action>
    Modify `frontend/src/App.jsx`:

    1. Update the existing import on line 1 to include `useParams`: change `import { BrowserRouter, Routes, Route, Navigate, Outlet, useLocation } from 'react-router-dom';` to `import { BrowserRouter, Routes, Route, Navigate, Outlet, useLocation, useParams } from 'react-router-dom';`.

    2. After the existing imports on lines 6-26, add three new imports (alphabetical with other page imports):
       - `import UserWishesPage from './pages/UserWishesPage';`
       - `import ChefWishesPage from './pages/ChefWishesPage';`
       - `import AdminWishesPage from './pages/AdminWishesPage';`

    3. Add an inline functional component `WishDeepLinkRedirect` somewhere before the App export (e.g., after the existing `PcLayout` component definition around line 81):
       - Read `useAuth()` to obtain `user`. If no user, return `<Navigate to="/login" replace />`.
       - Read `useParams()` to obtain the `:id` parameter.
       - Build a target path based on `user.role`: admin → `/admin/wishes?wish=${id}`; chef → `/chef/wishes?wish=${id}`; otherwise → `/my-wishes?wish=${id}`.
       - Return `<Navigate to={target} replace />`.

    4. Inside the `<PcLayout>` route block (currently lines 95-240), add three new `<Route>` entries. Place them right after the existing `/admin/logs` route (after line 239, before line 240's closing `</Route>`):
       - `<Route path="/my-wishes" element={<ProtectedRoute><UserWishesPage /></ProtectedRoute>} />` — user view (any authenticated role can browse own wishes).
       - `<Route path="/chef/wishes" element={<ProtectedRoute requiredRoles={['chef', 'admin']}><ChefWishesPage /></ProtectedRoute>} />` — chef queue.
       - `<Route path="/admin/wishes" element={<ProtectedRoute requiredRoles={['admin']}><AdminWishesPage /></ProtectedRoute>} />` — admin overview.

    5. Register the deep-link compatibility route. Place it OUTSIDE the `<PcLayout>` block but INSIDE the main `<Routes>` (e.g., after the existing `/force-change-password` route around line 92):
       - `<Route path="/wishes/:id" element={<ProtectedRoute><WishDeepLinkRedirect /></ProtectedRoute>} />`

       This must come BEFORE the `<Route path="*" element={<Navigate to="/" replace />} />` wildcard (line 242) so it is matched first.

    Constraints:
    - Do NOT add a nested wish-detail page (D-05 forbids new top-level routes for forms).
    - Do NOT modify any existing route. Only ADD new routes.
    - Do NOT introduce new dependencies or context providers.
  </action>
  <verify>
    <automated>cd /home/temila/family_chef/frontend && grep -q 'path="/my-wishes"' src/App.jsx && grep -q 'path="/chef/wishes"' src/App.jsx && grep -q 'path="/admin/wishes"' src/App.jsx && grep -q 'path="/wishes/:id"' src/App.jsx && grep -q "WishDeepLinkRedirect" src/App.jsx && grep -q "useParams" src/App.jsx && grep -q "import UserWishesPage" src/App.jsx && grep -q "import ChefWishesPage" src/App.jsx && grep -q "import AdminWishesPage" src/App.jsx && echo OK && npx eslint src/App.jsx --max-warnings=0 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `App.jsx` imports include `useParams` (added to the react-router-dom line) and three new page imports (UserWishesPage, ChefWishesPage, AdminWishesPage).
    - `App.jsx` defines `WishDeepLinkRedirect` inline component that uses `useAuth` and `useParams` to redirect to `/my-wishes?wish=:id` / `/chef/wishes?wish=:id` / `/admin/wishes?wish=:id` based on role.
    - Three new routes registered inside `<PcLayout>`: `/my-wishes` (no role restriction), `/chef/wishes` (`requiredRoles={['chef', 'admin']}`), `/admin/wishes` (`requiredRoles={['admin']}`).
    - One new route registered outside `<PcLayout>`: `/wishes/:id` rendering `<WishDeepLinkRedirect />` wrapped in `<ProtectedRoute>`.
    - The `/wishes/:id` route is placed BEFORE the `*` wildcard.
    - No existing route is modified or removed.
    - Targeted ESLint exits 0 with `--max-warnings=0`.
    - `cd frontend && npm run build` exits 0.
  </acceptance_criteria>
  <done>
    All four new routes are live. Navigating to /my-wishes, /chef/wishes, /admin/wishes renders the correct page; navigating to /wishes/:id from a Phase-6 notification deep-links to the role's wish page with highlight directive preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Sidebar.jsx + BottomBar.jsx — desktop and mobile nav entries</name>
  <files>frontend/src/components/Sidebar.jsx, frontend/src/components/BottomBar.jsx</files>
  <read_first>
    - frontend/src/components/Sidebar.jsx (whole file — three role-branch navItems arrays)
    - frontend/src/components/BottomBar.jsx (whole file — three role-branch tabs arrays)
    - .planning/phases/07-wish-list-frontend/07-PATTERNS.md lines 575-633 (Sidebar + BottomBar insertion patterns)
    - .planning/phases/07-wish-list-frontend/07-UI-SPEC.md §5.5 (nav entry placement — append-after-口味偏好 for user/chef; admin appends in admin section)
  </read_first>
  <action>
    Modify `frontend/src/components/Sidebar.jsx`:

    Append one new nav entry to each role's `navItems` array. Use the existing emoji + label + path format:

    - User branch (current lines 46-51 — array of 4 items, after `口味偏好` line 49 and before `我的` line 50): insert `{ icon: '💡', label: '我的愿望', path: '/my-wishes' }`. Final user order: 首页 / 点菜 / 口味偏好 / 我的愿望 / 我的 (5 items).

    - Chef branch (current lines 36-44 — array of 7 items, after `口味偏好` line 42 and before `我的` line 43): insert `{ icon: '💡', label: '愿望管理', path: '/chef/wishes' }`. Final chef order: 首页 / 订单管理 / 菜品管理 / 食材管理 / 点菜 / 口味偏好 / 愿望管理 / 我的 (8 items).

    - Admin branch (current lines 24-34 — array of 9 items): append `{ icon: '💡', label: '愿望总览', path: '/admin/wishes' }` at the end of the array (after `点菜预览` line 33). Final admin order: 管理后台 / 菜品管理 / 食材管理 / 分类管理 / 厨师管理 / 用户管理 / 数据统计 / 系统日志 / 点菜预览 / 愿望总览 (10 items).

    Do NOT modify the active-state matching logic (`location.pathname === item.path`) — exact-path match is correct for wish pages because `/chef/wishes` does not need to also match `/chef/wishes/:anything` (there is no sub-route).

    Do NOT add a badge count to the wish entries — per CONTEXT `agent's discretion` deferred.

    Modify `frontend/src/components/BottomBar.jsx`:

    Append one new tab to each role's `tabs` array. Use a shorter label `愿望` to fit the 5-tab mobile layout:

    - User branch (current lines 37-41 — array of 3 tabs): insert `{ id: 'user-wishes', icon: '💡', label: '愿望', path: '/my-wishes' }` BETWEEN the `order-dish` (line 39) and `user-profile` (line 40) entries. Final user order: 首页 / 点菜 / 愿望 / 我的 (4 tabs).

    - Chef branch (current lines 30-35 — array of 4 tabs): insert `{ id: 'chef-wishes', icon: '💡', label: '愿望', path: '/chef/wishes' }` AFTER the `order-dish` (line 32) entry. Final chef order: 订单 / 点菜 / 愿望 / 首页 / 我的 (5 tabs).

    - Admin branch (current lines 23-28 — array of 4 tabs): insert `{ id: 'admin-wishes', icon: '💡', label: '愿望', path: '/admin/wishes' }` BETWEEN the `admin-dishes` (line 25) and `admin-users` (line 26) entries. Final admin order: 后台 / 菜品 / 愿望 / 用户 / 退出 (5 tabs).

    The existing `isActive(path)` function (lines 44-47) uses `startsWith(path + '/')` for prefix matching. Verify that `/admin/wishes` does NOT falsely activate on `/admin` — the current `startsWith('/admin/')` check would match `/admin/wishes`, which is correct. But `/admin` itself has `path === '/admin'` exact match — also correct. No change needed to `isActive`.

    Do NOT modify the existing badge logic on `chef-orders` (uses `usePendingOrderCount` for order queue, not wish queue).

    Stability: do not introduce new state, hooks, or imports.
  </action>
  <verify>
    <automated>cd /home/temila/family_chef/frontend && grep -q "'💡', label: '我的愿望', path: '/my-wishes'" src/components/Sidebar.jsx && grep -q "'💡', label: '愿望管理', path: '/chef/wishes'" src/components/Sidebar.jsx && grep -q "'💡', label: '愿望总览', path: '/admin/wishes'" src/components/Sidebar.jsx && grep -q "'💡', label: '愿望', path: '/my-wishes'" src/components/BottomBar.jsx && grep -q "'💡', label: '愿望', path: '/chef/wishes'" src/components/BottomBar.jsx && grep -q "'💡', label: '愿望', path: '/admin/wishes'" src/components/BottomBar.jsx && echo OK && npx eslint src/components/Sidebar.jsx src/components/BottomBar.jsx --max-warnings=0 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `Sidebar.jsx` contains exactly three new nav items with the locked copy: `💡 我的愿望 → /my-wishes` (user branch), `💡 愿望管理 → /chef/wishes` (chef branch), `💡 愿望总览 → /admin/wishes` (admin branch).
    - User items count is now 5; chef items count is now 8; admin items count is now 10.
    - `BottomBar.jsx` contains exactly three new mobile tabs with the locked label `愿望` (not `我的愿望` etc.).
    - User tabs count is now 4; chef tabs count is now 5; admin tabs count is now 5.
    - No existing nav item is removed or reordered (only additions).
    - No new imports; no new state; no new hooks.
    - Targeted ESLint exits 0.
    - `cd frontend && npm run build` exits 0.
  </acceptance_criteria>
  <done>
    Wish pages are discoverable from both desktop sidebar (≥1024px) and mobile bottom bar (≤1023px) for all three roles. UX-01 and UX-02 mobile-discoverability concerns are fully resolved.
  </done>
</task>

<task type="auto">
  <name>Task 3: Deep-link highlight + scroll-to in UserWishesPage and ChefWishesPage + verification gate</name>
  <files>frontend/src/pages/UserWishesPage.jsx, frontend/src/pages/ChefWishesPage.jsx</files>
  <read_first>
    - frontend/src/pages/UserWishesPage.jsx (existing state and effects from Wave 2 Task 1)
    - frontend/src/pages/ChefWishesPage.jsx (existing state and effects from Wave 2 Task 2)
    - .planning/phases/07-wish-list-frontend/07-UI-SPEC.md §11 (deep-link highlight directive: 4s outline + scroll-to, 5-page missing-card fallback, repeated navigation restarts timer)
    - .planning/phases/07-wish-list-frontend/07-PATTERNS.md lines 432-460 (ChefWishesPage pattern with searchParams)
  </read_first>
  <action>
    Modify `frontend/src/pages/UserWishesPage.jsx`:

    Add the deep-link highlight behavior to the existing UserWishesPage:

    1. Add `import { useSearchParams } from 'react-router-dom';` to the existing react-router-dom import line (currently `import { Link } from 'react-router-dom';` — extend to `import { Link, useSearchParams } from 'react-router-dom';`).

    2. After the existing `useToast()` call, add:
       - `const [searchParams, setSearchParams] = useSearchParams();`
       - `const highlightId = searchParams.get('wish');`

    3. Add a `highlightedId` state variable: `const [highlightedId, setHighlightedId] = useState(null);`

    4. Add a `useEffect` (deps: `[wishes, highlightId]`) that:
       - If `highlightId` is null or empty, return early.
       - Look up `const targetWish = wishes.find(w => String(w.id) === String(highlightId));`
       - If `targetWish` is found: `setHighlightedId(String(highlightId));`
         - Use `setTimeout(() => { setHighlightedId(null); setSearchParams((cur) => { const next = new URLSearchParams(cur); next.delete('wish'); return next; }, { replace: true }); }, 4000);` — clears highlight + URL param after 4 seconds.
         - After `setHighlightedId` call, schedule a microtask (or use `requestAnimationFrame`) to call `document.querySelector('[data-wish-id="' + highlightId + '"]')?.scrollIntoView({ behavior: 'smooth', block: 'center' });`.
       - If `targetWish` is NOT found: show toast `未找到该愿望，可能已撤销或需要切换标签` and call `setSearchParams((cur) => { const next = new URLSearchParams(cur); next.delete('wish'); return next; }, { replace: true });`.

    5. In the list-render section, pass a new prop to each `<WishCard>`: `highlighted={highlightedId === String(wish.id)}` and modify WishCard to accept this prop and conditionally apply class `wish-card-highlight`.

    Note: WishCard.jsx (from Wave 1) already accepts a `highlighted` prop or uses external class application — verify by reading the file. If WishCard does not yet support a `highlighted` prop, modify it inline in this task:
    - Add `highlighted` prop to WishCard's parameter list (default false).
    - On root `<div className="wish-card">`, conditionally append ` ${highlighted ? 'wish-card-highlight' : ''}` to the className.

    Modify `frontend/src/pages/ChefWishesPage.jsx`:

    Add the same deep-link highlight behavior:
    1. Extend the existing react-router-dom import to include `useSearchParams`: change `import { useSearchParams } from 'react-router-dom';` (already present in Wave 2) — no change needed since Wave 2 already imports it for tab state.
    2. After the existing `activeTab` derivation, add: `const highlightId = searchParams.get('wish');`
    3. Add `const [highlightedId, setHighlightedId] = useState(null);` state.
    4. Add a `useEffect` (deps: `[wishes, highlightId]`) with the same highlight logic as UserWishesPage (look up, set state, setTimeout 4s to clear, scrollIntoView, missing-card toast).
    5. Pass `highlighted={highlightedId === String(wish.id)}` to each `<WishCard>` in the list render.

    Modify `frontend/src/components/WishCard.jsx` (if not already done in Wave 1):

    Check if the Wave 1 implementation already accepts a `highlighted` prop. If not, add it: in the function parameter list add `highlighted = false`. On the root `<div className="wish-card" data-wish-id={wish.id}>`, modify to use a computed className: `className={`wish-card${highlighted ? ' wish-card-highlight' : ''}`}`.

    Verification gate (must pass before claiming plan complete):

    Run from the project root:
    - `cd /home/temila/family_chef/frontend && npm run build` — must exit 0.
    - `cd /home/temila/family_chef/frontend && npx eslint src/api/client.js src/components/ConfirmModal.jsx src/components/Sidebar.jsx src/components/BottomBar.jsx src/components/WishCard.jsx src/components/WishFormModal.jsx src/components/WishRejectModal.jsx src/components/WishAdvanceModal.jsx src/utils/index.js src/pages/UserWishesPage.jsx src/pages/ChefWishesPage.jsx src/pages/AdminWishesPage.jsx src/App.jsx --max-warnings=0` — must exit 0.

    Document the known-baseline full-lint failure in the SUMMARY: `npm run lint` is expected to remain red (95 errors + 21 warnings across pre-existing files unrelated to Phase 7) — this is acceptable per RESEARCH §Verification Gate.

    No new state for pagination; no new API calls; the highlight effect uses pure DOM (querySelector + scrollIntoView).
  </action>
  <verify>
    <automated>cd /home/temila/family_chef/frontend && grep -q "searchParams.get('wish')" src/pages/UserWishesPage.jsx && grep -q "searchParams.get('wish')" src/pages/ChefWishesPage.jsx && grep -q "scrollIntoView" src/pages/UserWishesPage.jsx && grep -q "scrollIntoView" src/pages/ChefWishesPage.jsx && grep -q "未找到该愿望" src/pages/UserWishesPage.jsx && grep -q "未找到该愿望" src/pages/ChefWishesPage.jsx && echo OK_PHASE && cd /home/temila/family_chef/frontend && npm run build 2>&1 | tail -5 && npx eslint src/api/client.js src/components/ConfirmModal.jsx src/components/Sidebar.jsx src/components/BottomBar.jsx src/components/WishCard.jsx src/components/WishFormModal.jsx src/components/WishRejectModal.jsx src/components/WishAdvanceModal.jsx src/utils/index.js src/pages/UserWishesPage.jsx src/pages/ChefWishesPage.jsx src/pages/AdminWishesPage.jsx src/App.jsx --max-warnings=0 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `UserWishesPage.jsx` reads `searchParams.get('wish')`, declares a `highlightedId` state, runs a `useEffect` that locates the matching wish in `wishes`, applies `wish-card-highlight` for 4 seconds, scrolls the card into view, and clears the URL param on timeout.
    - `ChefWishesPage.jsx` (which is the engine for both `/chef/wishes` and `/admin/wishes`) has the same deep-link highlight behavior.
    - Both pages show toast `未找到该愿望，可能已撤销或需要切换标签` when the `?wish=:id` ID is not found in the currently loaded list, and clear the URL param.
    - `WishCard.jsx` accepts a `highlighted` prop and conditionally appends the `wish-card-highlight` class to the root element.
    - `cd frontend && npm run build` exits 0.
    - `cd frontend && npx eslint src/api/client.js src/components/ConfirmModal.jsx src/components/Sidebar.jsx src/components/BottomBar.jsx src/components/WishCard.jsx src/components/WishFormModal.jsx src/components/WishRejectModal.jsx src/components/WishAdvanceModal.jsx src/utils/index.js src/pages/UserWishesPage.jsx src/pages/ChefWishesPage.jsx src/pages/AdminWishesPage.jsx src/App.jsx --max-warnings=0` exits 0.
    - Full `npm run lint` baseline remains red and is documented as a known pre-existing issue (acceptable per RESEARCH §Verification Gate).
  </acceptance_criteria>
  <done>
    Deep-link highlight behavior is complete on both user and chef/admin pages. Phase 6 `/wishes/:id` links now resolve to the role's wish page with the matching card highlighted and scrolled into view. The verification gate passes for build + targeted lint over all 13 Phase-7 files.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL → route | `/wishes/:id` and `?wish=:id` come from user-controlled sources (Phase 6 notifications, shared links) but are validated server-side |
| URL → Nav highlight | `?wish=:id` ID is matched against loaded wish list (no server query) |
| DOM manipulation | scrollIntoView on user-controlled selector is safe — `data-wish-id` is set from server-returned ID, never raw user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-N01 | Spoofing / Deep-link route resolution | App.jsx WishDeepLinkRedirect | mitigate | Component reads `useAuth().user.role` server-side-via-JWT and uses `useParams()` to extract `:id`. Redirect target is built from a hard-coded role→path mapping (`/my-wishes`, `/chef/wishes`, `/admin/wishes`) — no user-controlled path segments beyond the wish ID itself, which is URL-encoded via template literal. |
| T-07-N02 | Information Disclosure / URL enumeration | Deep-link redirect | accept | If wish is not visible to the current user (e.g., admin links to a wish they cannot see), the backend 404s on subsequent GET /api/wishes calls; the page-level missing-card toast handles the UX. No enumeration of hidden wishes via URL — backend authorization is authoritative. |
| T-07-N03 | Elevation of Privilege / Route gates | App.jsx wish routes | mitigate | `/my-wishes` is wrapped in `<ProtectedRoute>` (any authenticated role) — backend filters to current user's wishes only. `/chef/wishes` requires `['chef', 'admin']`. `/admin/wishes` requires `['admin']`. ProtectedRoute already handles role mismatch by redirecting to role home. |
| T-07-N04 | Denial of Service / DOM injection via URL | Deep-link highlight effect | mitigate | `data-wish-id` attribute is rendered by WishCard as `String(wish.id)` from backend response (numeric ID). The selector `document.querySelector('[data-wish-id="' + id + '"]')` uses string concatenation; the ID is server-validated to be numeric (integer column), so no XSS injection vector. |
| T-07-N05 | Tampering / Mobile nav discoverability | BottomBar.jsx + Sidebar.jsx | mitigate | Both files add wish entries per role branch. Sidebar is CSS-hidden below 1024px (`styles.css:380`) but BottomBar (≤1023px) now contains wish tabs — UX-01/02 mobile discoverability requirement is satisfied. |
| T-07-N06 | Tampering / Active-state false-positive | BottomBar isActive + Sidebar pathname | accept | BottomBar's `isActive(path)` uses `path === pathname OR pathname.startsWith(path + '/')`. For `/admin/wishes`, `startsWith('/admin/wishes/')` matches only deeper paths (which do not exist). The `/admin` route uses exact `path === '/admin'` so `/admin/wishes` does NOT falsely activate the `/admin` tab. Verified by reading the existing logic. |
| T-07-N07 | Repudiation / Verification gate | npm run build + targeted ESLint | mitigate | Phase 7 ships only when `npm run build` exits 0 and targeted ESLint over the 13 Phase-7 files exits 0. Full `npm run lint` baseline failure is documented as known-pre-existing (95 errors + 21 warnings) and not a blocker — per RESEARCH §Risk 11 + §Verification Gate. |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] packages — NOT TRIGGERED this phase (RESEARCH §Package Legitimacy Audit: "this phase installs no external package"). |
</threat_model>

<verification>
- [ ] `cd frontend && npm run build` exits 0
- [ ] `cd frontend && npx eslint src/api/client.js src/components/ConfirmModal.jsx src/components/Sidebar.jsx src/components/BottomBar.jsx src/components/WishCard.jsx src/components/WishFormModal.jsx src/components/WishRejectModal.jsx src/components/WishAdvanceModal.jsx src/utils/index.js src/pages/UserWishesPage.jsx src/pages/ChefWishesPage.jsx src/pages/AdminWishesPage.jsx src/App.jsx --max-warnings=0` exits 0
- [ ] grep checks: App.jsx has the 4 new routes + WishDeepLinkRedirect; Sidebar.jsx has the 3 wish entries; BottomBar.jsx has the 3 wish tabs; both wish pages use searchParams.get('wish'); wish-card-highlight class applied to WishCard root when highlighted prop is true.
- [ ] No `console.log` / `debugger` / `TODO` comments in any of the 5 modified files.
- [ ] Full `npm run lint` baseline remains red and is documented in SUMMARY as a known pre-existing condition.
- [ ] Navigation active-state verified: navigating to /chef/wishes highlights only the `愿望管理` Sidebar entry; navigating to /admin/wishes highlights only the `愿望总览` Sidebar entry; navigating to /my-wishes highlights only the `我的愿望` Sidebar entry. No false-positive activation.
</verification>

<success_criteria>
1. UX-01 + UX-02 satisfied end-to-end: wish pages reachable from both desktop (Sidebar ≥1024px) and mobile (BottomBar ≤1023px) navigation; the wish nav items appear in the correct position per role.
2. Phase 6 deep-link compatibility: `/wishes/:id` URLs resolve to the viewer's role-appropriate wish page with `?wish=:id` highlight directive preserved; the matching WishCard is highlighted with `wish-card-highlight` for 4 seconds and scrolled into view; missing-card toast appears when the wish is not in the loaded list.
3. Active-state matching on Sidebar and BottomBar correctly highlights the wish entries when on /my-wishes, /chef/wishes, or /admin/wishes.
4. All Phase-7 files are listed in `npm run build` and `npx eslint` verification gates, with both exiting 0.
5. Full `npm run lint` baseline failure is documented as known-pre-existing (not in scope per RESEARCH §Risk 11).
6. Threat mitigations applied: route-level role gates (V4), deep-link redirect to role-appropriate page (V4), DOM querySelector safe due to numeric ID (XSS), no new packages installed.
</success_criteria>

<output>
Create `.planning/phases/07-wish-list-frontend/07-03-SUMMARY.md` when done
</output>