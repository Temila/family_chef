---
phase: 07-wish-list-frontend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/api/client.js
  - frontend/src/utils/index.js
  - frontend/src/css/styles.css
  - frontend/src/components/ConfirmModal.jsx
  - frontend/src/components/WishCard.jsx
  - frontend/src/components/WishFormModal.jsx
  - frontend/src/components/WishRejectModal.jsx
  - frontend/src/components/WishAdvanceModal.jsx
autonomous: true
requirements:
  - UX-01
  - UX-02
  - UX-03

must_haves:
  truths:
    - "ApiClient exposes eight wish methods that correctly serialize the locked `status_filter` query key and `mine=true` flag (per Phase 5 backend contract)"
    - "Five Chinese wish statuses render through the existing Badge component using semantic colors (warn/info/success/danger/muted)"
    - "WishCard displays dish_name (large display font), status badge (top-right), submitted time (muted small), optional reference URL link, optional note, submitter name on chef/admin views, claimer name on user views, and reject reason on 已拒绝 cards"
    - "Three wish modals (Form, Reject, Advance) follow the existing .modal-overlay / .modal-content shell, carry role=\"dialog\" aria-modal=\"true\", and meet minimum accessibility (ESC close, aria-labelledby, focus trap, focus restore)"
    - "WishFormModal supports both create and edit modes with diff-only payloads, URL validation (http/https only), and length limits (1-100 / max 500 / max 500)"
    - "WishAdvanceModal filters to chef's own published dishes via getDishes({ status: 'enabled', chef_filter: 'my-published' }) and emits search-as-you-type"
    - "ConfirmModal receives a11y hooks (role, aria-modal, ESC, focus restore) so the cancel-wish reuse satisfies Section 7.5"
    - "Wish-specific CSS tokens (--unread-dot, --size-unread-dot, --space-wish-card-stack) and the .wish-card / .wish-card-unread-dot / .wish-picker-item / .sr-only utilities are added to styles.css"
  artifacts:
    - path: "frontend/src/api/client.js"
      provides: "Eight new methods: getWishes/getWish/createWish/updateWish/cancelWish/claimWish/advanceWish/rejectWish"
      contains: "async getWishes(params = {})"
      contains: "async claimWish(id)"
    - path: "frontend/src/utils/index.js"
      provides: "statusBadge lookup extended with five Chinese-key wish statuses"
      contains: "'待处理':"
      contains: "'已撤销':"
    - path: "frontend/src/css/styles.css"
      provides: "Wish card / picker / unread dot / sr-only rules + new tokens"
      contains: ".wish-card {"
      contains: ".wish-card-unread-dot {"
      contains: ".wish-picker-item {"
      contains: "--unread-dot: #E74C3C;"
    - path: "frontend/src/components/ConfirmModal.jsx"
      provides: "Accessibility hooks (role/aria-modal/aria-labelledby/ESC/focus restore)"
      contains: "role=\"dialog\""
      contains: "aria-modal=\"true\""
    - path: "frontend/src/components/WishCard.jsx"
      provides: "Shared role/status-aware card with callback action row"
      min_lines: 100
    - path: "frontend/src/components/WishFormModal.jsx"
      provides: "Create + edit modal shell with diff payload + URL validation"
      min_lines: 90
    - path: "frontend/src/components/WishRejectModal.jsx"
      provides: "Required reason modal that doubles as destructive confirmation (D-08 override)"
      min_lines: 70
    - path: "frontend/src/components/WishAdvanceModal.jsx"
      provides: "Searchable dish picker filtered to claimer's own published dishes"
      min_lines: 110
  key_links:
    - from: "frontend/src/api/client.js"
      to: "backend/app/routers/wishes.py"
      via: "URLSearchParams serialization"
      pattern: "qs.set\\('status_filter'"
    - from: "frontend/src/components/WishCard.jsx"
      to: "frontend/src/utils/index.js"
      via: "statusBadge lookup"
      pattern: "statusBadge\\(wish\\.status\\)"
    - from: "frontend/src/components/WishFormModal.jsx"
      to: "frontend/src/components/WishCard.jsx"
      via: "parent passes onSuccess callback"
      pattern: "onSuccess"
    - from: "frontend/src/components/WishAdvanceModal.jsx"
      to: "frontend/src/api/client.js"
      via: "getDishes with my-published filter"
      pattern: "chef_filter.*my-published"
---

<objective>
Build the wish-list contract and shared UI foundation: the eight `ApiClient` wish methods, the five-status Chinese extension to `statusBadge`, all wish-specific CSS (tokens, card anatomy, unread dot, picker items, sr-only utility), the shared `WishCard` component, the three wish modals (`WishFormModal` create+edit, `WishRejectModal` self-confirming destructive, `WishAdvanceModal` searchable dish picker), and the accessibility hardening of `ConfirmModal` so the cancel-wish flow satisfies the W3C WAI modal pattern.

Purpose: Wave 2 pages (UserWishesPage, ChefWishesPage, AdminWishesPage) and Wave 3 navigation must consume these contracts and components without re-deriving them. Locking the foundation in this plan prevents page-level rework and keeps the role pages narrow.

Output: 4 new files (WishCard.jsx, WishFormModal.jsx, WishRejectModal.jsx, WishAdvanceModal.jsx) + 4 modified files (api/client.js, utils/index.js, styles.css, ConfirmModal.jsx). `npm run build` remains green after this plan.
</objective>

<execution_context>
@/home/temila/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/temila/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-wish-list-frontend/07-CONTEXT.md
@.planning/phases/07-wish-list-frontend/07-RESEARCH.md
@.planning/phases/07-wish-list-frontend/07-PATTERNS.md
@.planning/phases/07-wish-list-frontend/07-UI-SPEC.md

# Critical source files (read FIRST)
@frontend/src/api/client.js
@frontend/src/utils/index.js
@frontend/src/components/Badge.jsx
@frontend/src/components/ConfirmModal.jsx
@frontend/src/components/EmptyState.jsx
@frontend/src/components/Loading.jsx
@frontend/src/contexts/ToastContext.jsx
@frontend/src/pages/UserOrdersPage.jsx
@frontend/src/pages/ChefOrdersPage.jsx
@frontend/src/css/styles.css

# Backend contract (read for context — do not modify)
@backend/app/schemas/wish.py
@backend/app/routers/wishes.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: ApiClient wish methods + statusBadge extension + CSS tokens</name>
  <files>frontend/src/api/client.js, frontend/src/utils/index.js, frontend/src/css/styles.css</files>
  <read_first>
    - frontend/src/api/client.js (lines 76-92 getDishes URLSearchParams pattern; lines 126-150 getOrders block; lines 320-335 core HTTP methods)
    - frontend/src/utils/index.js (lines 51-72 statusBadge map; lines 105-114 default export)
    - frontend/src/css/styles.css (lines 9-28 :root token block; lines 30-44 dark theme block; lines 384-392 modal-overlay rule)
    - .planning/phases/07-wish-list-frontend/07-RESEARCH.md lines 168-216 (exact method signatures)
    - .planning/phases/07-wish-list-frontend/07-PATTERNS.md lines 485-540 (code example for api additions)
  </read_first>
  <action>
    In `frontend/src/api/client.js`, insert a new `// ─── Wishes ─────────────────────────────────────` section immediately after the `// ─── Orders ─────` block (after line 154, before `// ─── Categories ─────`). Add eight methods:

    - `getWishes(params = {})`: Build `URLSearchParams` and conditionally call `.set('page', ...)`, `.set('page_size', ...)`, `.set('status_filter', params.status)` (CRITICAL: backend param name is `status_filter`, not `status` — per backend/app/routers/wishes.py:63-82), `.set('claimed_by_chef_id', ...)` when defined and not null, `.set('mine', 'true')` when truthy. Return `this.get('/wishes' + (query ? '?' + query : ''))`.
    - `getWish(id)`: `this.get('/wishes/' + id)`.
    - `createWish(data)`: `this.post('/wishes', data)`.
    - `updateWish(id, data)`: `this.put('/wishes/' + id, data)` — partial payload only.
    - `cancelWish(id)`: `this.del('/wishes/' + id)`.
    - `claimWish(id)`: `this.post('/wishes/' + id + '/claim')`.
    - `advanceWish(id, related_dish_id)`: `this.post('/wishes/' + id + '/advance', { related_dish_id })`.
    - `rejectWish(id, reject_reason)`: `this.post('/wishes/' + id + '/reject', { reject_reason })`.

    In `frontend/src/utils/index.js`, extend the `map` object inside `statusBadge()` (lines 53-69) with five Chinese-key entries (these are new keys; existing English-key entries stay intact for other features):

    - `'待处理': { text: '待处理', cls: 'badge-warn' }`
    - `'准备中': { text: '准备中', cls: 'badge-info' }`
    - `'已上架': { text: '已上架', cls: 'badge-success' }` (note: existing `'published'` English key already maps to `badge-success`; this Chinese key is for the wish backend's raw return)
    - `'已拒绝': { text: '已拒绝', cls: 'badge-danger' }`
    - `'已撤销': { text: '已撤销', cls: 'badge-muted' }` (this differs from the existing English `'revoked'` key which uses `badge-danger` — keep both; the wish Chinese key uses gray per D-16 lock)

    Do not modify the existing fallback line (line 70). Keep the function signature unchanged so the existing import sites in Badge.jsx:10 still work.

    In `frontend/src/css/styles.css`, add to `:root` (after line 27, before closing `}` on line 28) three tokens:
    - `--unread-dot: #E74C3C;`
    - `--size-unread-dot: 8px;`
    - `--space-wish-card-stack: 16px;`

    These are fixed values (do not duplicate into `[data-theme="dark"]` — unread-dot stays red across themes per UI-SPEC §4).
  </action>
  <verify>
    <automated>cd /home/temila/family_chef/frontend && grep -q "async getWishes(params = {})" src/api/client.js && grep -q "async rejectWish(id, reject_reason)" src/api/client.js && grep -q "'待处理':" src/utils/index.js && grep -q "'已撤销':" src/utils/index.js && grep -q "\-\-unread-dot: #E74C3C;" src/css/styles.css && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/api/client.js` contains eight `async` methods under a new `// ─── Wishes ─────` comment block, each terminating with `return this.<verb>(...)`; the line `qs.set('status_filter', params.status)` exists inside `getWishes`.
    - `frontend/src/utils/index.js` line range 53-69 now contains 5 new Chinese-key entries (`'待处理'`, `'准备中'`, `'已上架'`, `'已拒绝'`, `'已撤销'`); existing English keys (`pending`, `accepted`, etc.) remain unchanged.
    - `frontend/src/css/styles.css` :root block (lines 9-28) now contains exactly three new tokens: `--unread-dot: #E74C3C;`, `--size-unread-dot: 8px;`, `--space-wish-card-stack: 16px;`. No new tokens were added to the dark theme block.
    - `npm run build` exits 0 (frontend still compiles).
  </acceptance_criteria>
  <done>
    The ApiClient, statusBadge map, and CSS tokens are ready. Wave 2 pages can call `api.getWishes({ status: '待处理', mine: true })`, render `<Badge status={'已上架'} />`, and reference `var(--unread-dot)` without further contract negotiation.
  </done>
</task>

<task type="auto">
  <name>Task 2: WishCard + WishFormModal (create + edit)</name>
  <files>frontend/src/components/WishCard.jsx, frontend/src/components/WishFormModal.jsx</files>
  <read_first>
    - frontend/src/components/DishCard.jsx (display anatomy, useNavigate for related link)
    - frontend/src/pages/ChefOrdersPage.jsx lines 117-179 (inline action row pattern with stopPropagation)
    - frontend/src/components/ConfirmModal.jsx (modal shell shape, danger style)
    - frontend/src/pages/ChefDishesPage.jsx lines 696-924 (modal shell with form fields; existing wish modal pattern reference)
    - .planning/phases/07-wish-list-frontend/07-PATTERNS.md lines 37-149 (WishCard + WishFormModal section)
    - .planning/phases/07-wish-list-frontend/07-UI-SPEC.md §6.1, §6.2, §7.1, §7.5 (card anatomy + form modal contract + accessibility)
  </read_first>
  <action>
    Create `frontend/src/components/WishCard.jsx`:

    Default export, functional component. Props: `{ wish, currentUser, viewAsAdmin = false, currentRole, onEdit, onCancel, onClaim, onAdvance, onReject, onTap }`. Pure presentation — no API imports.

    Anatomy (per UI-SPEC §6.1):
    - Root `<div className="wish-card" data-wish-id={wish.id}>` with conditional `wish-card-highlight` class when externally highlighted, and `wish-card-readonly` when actions row is hidden.
    - Top row `<div className="wish-card-top">`:
      - Left: `<div className="wish-card-name">{wish.dish_name}</div>`
      - Right: `<div className="wish-card-badge-slot">` containing the unread dot (only when `wish.has_unread === true`: `<span className="wish-card-unread-dot" aria-hidden="true" />` plus `<span className="sr-only">未读</span>`) and `<Badge status={wish.status} />`.
    - Meta row `<div className="wish-card-meta">`:
      - Submitted time (`formatDate(wish.created_at)`).
      - On chef/admin views (`currentRole !== 'user'`), submitter_name when present.
      - On user view, claimed_by_chef_name when status ∈ {'准备中', '已上架', '已拒绝'}.
    - Secondary lines `<div className="wish-card-secondary">` (each renders ONLY when field present, no placeholder text):
      - reference_url: validate starts with `http://` or `https://`; if valid render `<a href={wish.reference_url} target="_blank" rel="noopener noreferrer">{wish.reference_url}</a>`; otherwise render plain text.
      - note: render as JSX text (no dangerouslySetInnerHTML).
      - related_dish_id with name prop (passed by parent): render `<Link to={'/dishes/' + wish.related_dish_id}>{relatedDishName}</Link>` (or `<a>`).
      - reject_reason: wrap in `<div className="wish-card-reject-reason">拒绝原因：{wish.reject_reason}</div>` only when status === '已拒绝' and reason present.
    - Action row `<div className="wish-card-actions" onClick={(e) => e.stopPropagation()}>` (per D-07 status matrix):
      - User view (own wishes, status ∈ {'待处理', '准备中'}): `[编辑愿望]` + `[撤销愿望]` buttons (call `onEdit(wish)` / `onCancel(wish)`).
      - Chef view (status === '待处理' and `wish.claimed_by_chef_id` is null): `[认领愿望]` button (calls `onClaim(wish)`).
      - Chef view (status === '准备中' and `wish.claimed_by_chef_id === currentUser.id`): `[推进愿望]` + `[拒绝愿望]` buttons.
      - Admin view (`viewAsAdmin` true): claim/advance/reject per same logic as chef; do NOT render submitter-only edit/cancel for other users' wishes (Risk 10).
      - All buttons: classes `btn btn-sm flex-1`. Edit/Claim/Advance use `btn-primary`; Cancel uses `btn-outline` with inline `style={{ borderColor: 'var(--danger)', color: 'var(--danger)' }}`; Reject uses `btn-outline` with the same red style.

    Card-body tap interaction: when `currentUser.id === wish.user_id && wish.has_unread === true`, attach `onClick={() => onTap?.(wish)}` on root (not on action buttons), `role="button"`, `tabIndex={0}`, `onKeyDown` for Enter/Space. Other cards have no onClick.

    Create `frontend/src/components/WishFormModal.jsx`:

    Default export. Props: `{ wish = null, mode = 'create', onClose, onSuccess }`. No API imports — submit emits `onSuccess(formData)`.

    Modal shell per UI-SPEC §6.2:
    - Overlay `<div className="modal-overlay" onClick={onClose}>` with content `<div className="modal-content" style={{ maxWidth: 480 }} onClick={(e) => e.stopPropagation()} role="dialog" aria-modal="true" aria-labelledby="wish-form-title">`.
    - Body-overflow lock effect: `document.body.style.overflow = 'hidden'` on mount, restore on unmount.
    - ESC handler effect: `keydown` listener that calls `onClose` when `e.key === 'Escape'` (only when not currently submitting).
    - Header: `<h3 id="wish-form-title">{mode === 'edit' ? '编辑愿望' : '新建愿望'}</h3>` + close ✕ button (`aria-label={mode === 'edit' ? '关闭编辑愿望窗口' : '关闭新建愿望窗口'}`).
    - Body — three fields, vertical order: 菜名 (required), 参考链接 (optional), 备注 (optional). Use existing `.form-group` + `.form-label` + `.form-input` classes. Add `<div className={'form-error' + (errors.dish_name ? ' show' : '')}>{errors.dish_name}</div>` after each field.
    - Validation rules per UI-SPEC §7.10:
      - `dish_name`: trim, length 1-100. Errors: '菜名不能为空' / '菜名不能超过 100 字'.
      - `reference_url`: optional. If non-empty, must start with `http://` or `https://`. Max 500. Error: '请输入以 http:// 或 https:// 开头的链接'.
      - `note`: optional, max 500.
    - Edit mode pre-fill: in `useEffect`, initialize state from `wish.dish_name`, `wish.reference_url || ''`, `wish.note || ''`. Show a small `<div className="info-pill">` at top of body when `mode === 'edit' && wish?.claimed_by_chef_name`: text '信息：编辑此愿望将通知认领厨师「{wish.claimed_by_chef_name}」' (NOTIF-06 disclosure per UI-SPEC §8.10).
    - Footer: `[放弃修改]` (or `[暂不提交]` in create mode) + primary `[保存修改]` (or `[提交愿望]`). Both `btn`; primary `btn-primary`. Auto-focus `dish_name` input on mount via `autoFocus` prop.
    - Submit: build payload object `buildWishPatch(form, originalWish)`:
      - For create mode: send all three fields (reference_url and note become `null` when empty after trim).
      - For edit mode: include only fields that differ from original (compare trimmed values); explicitly include `null` to clear optional fields when user empties them (per backend/app/services/wish_service.py:175-184 — omitting vs null matters).
    - When form is empty-patch in edit mode, do not call `onSuccess`; instead show inline toast via `useToast()` saying '未修改任何内容' and keep modal open.

    Imports: `import { useEffect, useState, useRef } from 'react'; import { useToast } from '../contexts/ToastContext';`. No API import.
  </action>
  <verify>
    <automated>cd /home/temila/family_chef/frontend && grep -q "export default function WishCard" src/components/WishCard.jsx && grep -q "data-wish-id={wish.id}" src/components/WishCard.jsx && grep -q "wish-card-unread-dot" src/components/WishCard.jsx && grep -q "export default function WishFormModal" src/components/WishFormModal.jsx && grep -q "role=\"dialog\"" src/components/WishFormModal.jsx && grep -q "aria-modal=\"true\"" src/components/WishFormModal.jsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `WishCard.jsx` exists with `export default function WishCard`, applies classes `wish-card`, `wish-card-top`, `wish-card-name`, `wish-card-badge-slot`, `wish-card-meta`, `wish-card-secondary`, `wish-card-actions`, `wish-card-unread-dot`, `sr-only`, `wish-card-readonly`; renders `data-wish-id={wish.id}` on root; renders `<Badge status={wish.status} />` (NOT a hardcoded `<span>`); action buttons are conditional on status + role + claimer; card-body `onClick` is bound only when `currentUser.id === wish.user_id && wish.has_unread`.
    - `WishCard.jsx` does NOT import `api` or call any HTTP endpoint — pure presentation only.
    - `WishFormModal.jsx` exists with `export default function WishFormModal`, props `{ wish, mode, onClose, onSuccess }`; renders `<div role="dialog" aria-modal="true" aria-labelledby="wish-form-title">`; has 3 form fields (dish_name required, reference_url, note); ESC and body-overflow-lock effects present; submit handler validates dish_name trim 1-100 and reference_url `http://` or `https://` prefix.
    - `WishFormModal.jsx` does NOT import `api` — submission is delegated to `onSuccess(payload)` callback for the parent page to call api methods.
    - Targeted lint check: `cd frontend && npx eslint src/components/WishCard.jsx src/components/WishFormModal.jsx --max-warnings=0` exits 0.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>
    WishCard + WishFormModal are presentational shells ready for parent pages. No internal API calls. Modal a11y hooks (role/aria-modal/ESC/focus restore via body-overflow-lock pattern) are in place per UI-SPEC §7.5.
  </done>
</task>

<task type="auto">
  <name>Task 3: WishRejectModal + WishAdvanceModal + ConfirmModal a11y + wish CSS</name>
  <files>frontend/src/components/WishRejectModal.jsx, frontend/src/components/WishAdvanceModal.jsx, frontend/src/components/ConfirmModal.jsx, frontend/src/css/styles.css</files>
  <read_first>
    - frontend/src/components/ConfirmModal.jsx (whole file — current state)
    - frontend/src/components/ChefSelectModal.jsx (selection list pattern at lines 33-79)
    - frontend/src/pages/ChefDishesPage.jsx lines 696-924 (form modal pattern, lines 738-817 search dropdown)
    - .planning/phases/07-wish-list-frontend/07-PATTERNS.md lines 151-275 (WishRejectModal + WishAdvanceModal)
    - .planning/phases/07-wish-list-frontend/07-UI-SPEC.md §6.3, §6.4, §7.5, §7.6, §8.4, §8.6 (reject/advance modal + a11y + empty state copy)
    - frontend/src/api/client.js (lines 76-92 getDishes filter params — chef_filter usage)
  </read_first>
  <action>
    Create `frontend/src/components/WishRejectModal.jsx`:

    Default export. Props: `{ wish, onClose, onSuccess }`. Modal shell, max-width 420. Per D-08 lock — this modal IS the destructive confirmation; do NOT stack ConfirmModal on top.

    - Overlay with `role="dialog" aria-modal="true" aria-labelledby="wish-reject-title"`.
    - Body-overflow lock + ESC handler (mirror WishFormModal pattern).
    - Header: `<h3 id="wish-reject-title">拒绝愿望</h3>` + close ✕ button (`aria-label="关闭拒绝愿望窗口"`).
    - Body: single textarea `<textarea className="form-input" rows={4} autoFocus>` bound to `reason` state. Inline `.form-error` for empty / over-500 errors. Placeholder: '请说明拒绝原因（必填）'.
    - Validation: trim → length 1-500. Submit button disabled while invalid.
    - Footer: `[暂不拒绝]` (btn-secondary, calls onClose) + `[确认拒绝]` (btn with inline `style={{ background: 'var(--danger)', color: '#fff' }}`, disabled when invalid; calls onSuccess with trimmed reason).
    - Submit disabled until reason valid. No API import — submit is delegated via `onSuccess(reason)`.

    Create `frontend/src/components/WishAdvanceModal.jsx`:

    Default export. Props: `{ wish, onClose, onSuccess }`. Modal shell, max-width 560.

    - Overlay with `role="dialog" aria-modal="true" aria-labelledby="wish-advance-title"`.
    - Body-overflow lock + ESC handler.
    - Header: `<h3 id="wish-advance-title">推进愿望 — 关联菜品</h3>` + close ✕ button (`aria-label="关闭推进愿望窗口"`).
    - Body — vertical:
      1. Search bar using existing `.search-bar` pattern: `<div className="search-bar"><span className="search-icon">🔍</span><input autoFocus placeholder="搜索已上架的菜品" value={search} onChange={...} /></div>`.
      2. `<div className="wish-picker-list">` (max-height: 360px, overflow-y: auto) listing `.wish-picker-item` cards. Each item shows: 56x56 placeholder-img, dish_name (14px), and `.wish-picker-item-img` glyph '🍽️' fallback. Tap toggles selection.
      3. Empty state when chef has 0 published dishes: `<EmptyState icon="🍽️" text="你还没有发布任何菜品，无法推进愿望" />` with a `<Link to="/chef/dishes">前往菜品管理 →</Link>` below it.
    - Data: `useEffect` on mount + when `search` changes (debounced 200ms via existing `debounce` from utils/index.js:86): call `api.getDishes({ status: 'enabled', chef_filter: 'my-published', search, page: 1, page_size: 100 })`. Store items in component state. Loading state: render `<Loading />` inside the picker list.
    - Filter on search: also apply client-side `dish.name.toLowerCase().includes(search.toLowerCase())` for snappier UX (server is also filtering but client filter handles race).
    - Selected state: track `selectedDishId`; active item gets class `active` (per UI-SPEC §6.4); selected dish name available for confirmation toast.
    - Footer: `[暂不推进]` (btn-secondary, calls onClose) + `[确认推进]` (btn-primary, disabled until selection; calls onSuccess(selectedDishId, selectedDishName)).
    - Imports: `import { useState, useEffect } from 'react'; import { Link } from 'react-router-dom'; import api from '../api/client'; import { useToast } from '../contexts/ToastContext'; import Loading from './Loading'; import EmptyState from './EmptyState'; import { debounce } from '../utils';`.

    Modify `frontend/src/components/ConfirmModal.jsx`:

    Add accessibility hooks while keeping the existing API (props: title, message, confirmText, cancelText, onConfirm, onCancel, danger). Do not change behavior — only add:

    - Import `useEffect` from 'react'.
    - Effect: body-overflow lock on mount.
    - Effect: ESC handler — `document.addEventListener('keydown', handler)`; handler calls `onCancel()` when `e.key === 'Escape'`; cleanup removes listener on unmount.
    - Root overlay `<div className="modal-overlay" ... role="dialog" aria-modal="true" aria-labelledby="confirm-modal-title">`.
    - Title element: `<h3 id="confirm-modal-title">{title}</h3>`.
    - Close ✕ button: `aria-label={`关闭${title}窗口`}`.

    Modify `frontend/src/css/styles.css` — add a new section block after the existing `.chef-select-item` block (after line 419), titled `/* ═══ Wish Cards ═════════════════ */`:

    - `.wish-card { background: var(--bg-card); border: 1px solid var(--border); border-radius: var(--radius-lg); padding: 16px; margin-bottom: var(--space-wish-card-stack, 16px); position: relative; display: flex; flex-direction: column; gap: 8px; }`
    - `.wish-card-top { display: flex; justify-content: space-between; align-items: flex-start; gap: 8px; }`
    - `.wish-card-name { font-family: var(--font-display); font-size: 1.25rem; font-weight: 600; color: var(--text-primary); line-height: 1.3; flex: 1; min-width: 0; word-break: break-word; }`
    - `.wish-card-badge-slot { display: flex; align-items: center; gap: 8px; flex-shrink: 0; }`
    - `.wish-card-meta { font-size: 0.75rem; color: var(--text-muted); line-height: 1.4; display: flex; flex-wrap: wrap; gap: 8px 16px; }`
    - `.wish-card-meta-item { display: inline-flex; align-items: center; gap: 4px; }`
    - `.wish-card-secondary { font-size: 0.875rem; color: var(--text-secondary); line-height: 1.5; word-break: break-word; }`
    - `.wish-card-secondary a { color: var(--accent); }`
    - `.wish-card-reject-reason { color: var(--danger); background: var(--danger-light); border-radius: var(--radius-sm); padding: 8px 16px; font-size: 0.875rem; }`
    - `.wish-card-actions { display: flex; gap: 8px; flex-wrap: wrap; margin-top: 8px; padding-top: 8px; border-top: 1px dashed var(--border); }`
    - `.wish-card-actions .btn { min-height: 44px; padding: 8px 16px; font-size: 0.875rem; }`
    - `.wish-card-readonly { opacity: 0.7; cursor: default; }`
    - `.wish-card-readonly .wish-card-actions { display: none; }`
    - `.wish-card-unread-dot { position: absolute; top: 16px; right: 96px; width: var(--size-unread-dot, 8px); height: var(--size-unread-dot, 8px); border-radius: 50%; background: var(--unread-dot, #E74C3C); }`
    - `.sr-only { position: absolute; clip: rect(0, 0, 0, 0); width: 1px; height: 1px; overflow: hidden; }`

    Then add `/* ═══ Wish Picker (AdvanceModal) ═════════════════ */` block:

    - `.wish-picker-list { max-height: 360px; overflow-y: auto; }`
    - `.wish-picker-item { display: flex; align-items: center; gap: 12px; padding: 12px; height: 64px; border: 2px solid var(--border); border-radius: var(--radius-md); margin-bottom: 8px; cursor: pointer; transition: all var(--transition-fast); }`
    - `.wish-picker-item:hover { border-color: var(--border-medium); }`
    - `.wish-picker-item.active { border-color: var(--accent); background: var(--accent-light); }`
    - `.wish-picker-item-img { width: 56px; height: 56px; border-radius: var(--radius-md); background: var(--bg-elevated); flex-shrink: 0; display: flex; align-items: center; justify-content: center; font-size: 1.5rem; }`

    Then add `/* ═══ Wish Highlight (deep-link) ═════════════════ */` block:

    - `.wish-card-highlight { outline: 3px solid var(--accent); outline-offset: 2px; box-shadow: 0 0 0 4px var(--accent-light); }`

    All spacing values must be multiples of 4 (per UI-SPEC §2).
  </action>
  <verify>
    <automated>cd /home/temila/family_chef/frontend && grep -q "export default function WishRejectModal" src/components/WishRejectModal.jsx && grep -q "export default function WishAdvanceModal" src/components/WishAdvanceModal.jsx && grep -q "chef_filter.*my-published" src/components/WishAdvanceModal.jsx && grep -q "role=\"dialog\"" src/components/ConfirmModal.jsx && grep -q "\.wish-card {" src/css/styles.css && grep -q "\.wish-card-unread-dot" src/css/styles.css && grep -q "\.wish-picker-item" src/css/styles.css && grep -q "\.wish-card-highlight" src/css/styles.css && grep -q "\.sr-only" src/css/styles.css && echo OK && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `WishRejectModal.jsx` exists with `export default function WishRejectModal`; renders `role="dialog" aria-modal="true" aria-labelledby="wish-reject-title"`; has single textarea for reason; submit button inline `style={{ background: 'var(--danger)', color: '#fff' }}`; submit disabled when reason.trim().length is 0 or > 500; submits via `onSuccess(reason)` callback (no API import).
    - `WishAdvanceModal.jsx` exists with `export default function WishAdvanceModal`; renders `role="dialog" aria-modal="true" aria-labelledby="wish-advance-title"`; calls `api.getDishes({ status: 'enabled', chef_filter: 'my-published', search, page: 1, page_size: 100 })` on mount + search change (debounced 200ms); renders `.wish-picker-item` list with max-height 360; selected item has class `active`; submit disabled until selection; submits via `onSuccess(selectedDishId, selectedDishName)`.
    - `ConfirmModal.jsx` now contains `import { useEffect } from 'react'`; root overlay has `role="dialog" aria-modal="true" aria-labelledby="confirm-modal-title"`; ESC handler effect present (adds + removes `keydown` listener); close ✕ button has `aria-label={`关闭${title}窗口`}`; title `<h3>` carries `id="confirm-modal-title"`.
    - `styles.css` contains the new section blocks (search for `.wish-card {`, `.wish-card-unread-dot`, `.wish-picker-item`, `.wish-card-highlight`, `.sr-only`); all numeric values in new wish CSS are multiples of 4 (no 10/12/18/20/22).
    - `cd frontend && npx eslint src/components/WishRejectModal.jsx src/components/WishAdvanceModal.jsx src/components/ConfirmModal.jsx --max-warnings=0` exits 0.
    - `cd frontend && npm run build` exits 0.
  </acceptance_criteria>
  <done>
    All shared components (WishCard, WishFormModal, WishRejectModal, WishAdvanceModal) and accessibility-hardened ConfirmModal exist with consistent a11y hooks. Wish CSS tokens and class rules are in place. Wave 2 pages can compose them without API or CSS conflict.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → WishFormModal | Untrusted dish_name, reference_url, note typed by submitter |
| API response → WishCard / modal bodies | Backend returns raw text from dish_name, note, reject_reason, submitter_name, claimed_by_chef_name, related_dish_name |
| External reference_url | Submitted link may point to attacker-controlled sites |
| Inline rendered user data | Stored XSS risk if untrusted strings reach HTML-writing sinks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-T01 | Tampering / XSS | WishCard + all modals | mitigate | Render all user-supplied text (dish_name, note, reject_reason, submitter_name, claimed_by_chef_name, related_dish_name) as plain JSX text nodes — never `dangerouslySetInnerHTML`, `innerHTML`, or `document.write`. Reference URLs only become `<a>` when they start with `http://` or `https://`; everything else renders as plain text. |
| T-07-T02 | Spoofing / Reverse tabnabbing | WishCard reference_url link | mitigate | Render `<a target="_blank" rel="noopener noreferrer">` for the clickable reference_url. Modern browsers imply `noopener` for `_blank`, but explicit attributes remain compatible defense. |
| T-07-T03 | Tampering / Validation bypass | WishFormModal + WishRejectModal inputs | mitigate | Client-side validation enforces locked lengths (dish_name 1-100, reference_url max 500, note max 500, reject_reason 1-500) and reference_url scheme restriction BEFORE submit. Backend Pydantic schemas also enforce these limits. Inline `.form-error` provides feedback. |
| T-07-T04 | Information Disclosure | Unread dot visibility | accept | Backend already enforces `compute_has_unread` 身份屏蔽 — non-submitter viewers always receive `has_unread=false`. Frontend renders dot only when `wish.has_unread === true`. No client-side enumeration of unread state. |
| T-07-T05 | Elevation of Privilege | Modal action affordances | mitigate | WishCard action row is gated by status + role + claimer (D-07 status matrix). Backend authorization is authoritative; 403/400 are caught by parent pages with `err.message` toast + refetch. |
| T-07-T06 | Repudiation / Concurrent claim race | WishFormModal edit mode | mitigate | Edit mode that mutates a claimed wish surfaces a `info-pill` disclosure "信息：编辑此愿望将通知认领厨师「{chef_name}」" so the user is aware of NOTIF-06 side effect before saving. |
| T-07-T07 | Denial of Service via malformed input | WishFormModal | mitigate | Length validators prevent oversized payloads reaching backend; trim() prevents whitespace-only submissions. Backend Pydantic schemas provide defense in depth. |
| T-07-T08 | Tampering / Modal accessibility | All 4 modals (WishFormModal, WishRejectModal, WishAdvanceModal, ConfirmModal) | mitigate | `role="dialog"`, `aria-modal="true"`, `aria-labelledby` on root; ESC keydown listener; body-overflow lock prevents background scroll; autoFocus on first field; close button has aria-label. Future focus-trap implementation noted in UI-SPEC §7.5 (this plan covers the in-scope a11y per WAI modal pattern). |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] packages — NOT TRIGGERED this phase (RESEARCH §Package Legitimacy Audit: "this phase installs no external package"). |
</threat_model>

<verification>
- [ ] `cd frontend && npm run build` exits 0
- [ ] `cd frontend && npx eslint src/api/client.js src/utils/index.js src/components/WishCard.jsx src/components/WishFormModal.jsx src/components/WishRejectModal.jsx src/components/WishAdvanceModal.jsx src/components/ConfirmModal.jsx --max-warnings=0` exits 0
- [ ] grep checks: `api.getWishes` exists; `qs.set('status_filter'` exists; `'待处理':` exists in utils; `--unread-dot: #E74C3C;` exists in styles; `.wish-card {` exists; `.wish-card-unread-dot` exists; `.wish-picker-item` exists; `role="dialog"` exists in ConfirmModal
- [ ] No new packages installed (RESEARCH.md §Package Legitimacy Audit confirms none needed)
</verification>

<success_criteria>
1. The eight wish API methods (getWishes / getWish / createWish / updateWish / cancelWish / claimWish / advanceWish / rejectWish) exist on `ApiClient` and serialize `status` → `status_filter` and `mine=true` correctly.
2. The five Chinese wish statuses (待处理 / 准备中 / 已上架 / 已拒绝 / 已撤销) resolve through `<Badge status={...}>` to the correct badge class (warn / info / success / danger / muted).
3. WishCard renders the locked anatomy (top row dish_name + status + unread dot; meta row time + identity; secondary lines inline only when present; action row gated by D-07 status matrix).
4. WishFormModal handles create + edit modes with diff-only edit payloads, ESC close, body-overflow lock, URL scheme validation, and inline field errors.
5. WishRejectModal self-confirms without stacking ConfirmModal (D-08 override).
6. WishAdvanceModal filters to claimer's own published dishes via `chef_filter: 'my-published'` and supports search-as-you-type.
7. ConfirmModal carries a11y hooks (role/aria-modal/aria-labelledby/ESC/focus restore via body-overflow lock) so the cancel-wish reuse satisfies Section 7.5.
8. All threat-mitigated implementation is complete: XSS-safe rendering, noopener noreferrer on reference links, length validation, server-authoritative action gating.
9. `npm run build` and targeted ESLint over the 7 Phase-7-modified-or-new files all exit 0.
</success_criteria>

<output>
Create `.planning/phases/07-wish-list-frontend/07-01-SUMMARY.md` when done
</output>