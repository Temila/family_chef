---
phase: 07-wish-list-frontend
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - frontend/src/pages/UserWishesPage.jsx
  - frontend/src/pages/ChefWishesPage.jsx
  - frontend/src/pages/AdminWishesPage.jsx
autonomous: true
requirements:
  - WISH-03
  - WISH-04
  - UX-01
  - UX-02
  - UX-03

must_haves:
  truths:
    - "D-01, WISH-03, WISH-04: User can submit a new wish from /my-wishes (header +新建愿望 button OR FAB), browse their own wishes as WishCards, edit non-terminal wishes (WISH-03), and cancel (soft-delete) non-terminal wishes (WISH-04) — the card persists as 已撤销 read-only per CONTEXT D-16"
    - "D-03: Chef can switch between 全部 / 待处理 / 我的认领 tabs at /chef/wishes via URL-backed query param ?tab=...; the active tab survives reload"
    - "D-07, D-08, D-09: Chef can claim a 待处理 wish (no claimer), advance their own 准备中 claim by linking one of their own published dishes via WishAdvanceModal, and reject their own 准备中 claim with a required reason via WishRejectModal (self-confirming per D-08 override)"
    - "D-04: Admin at /admin/wishes sees the same wish list with admin-overridden action buttons (claim/advance/reject for any wish) but NOT submitter-only edit/cancel for other users' wishes (Risk 10)"
    - "D-07: After every mutation the page refetches the current tab/page in the background (no spinner); optimistic action button is disabled during the in-flight request; a requestSeqRef guards against stale async responses overwriting fresh state"
    - "D-15: User view polls on visibilitychange→visible + window focus (no fixed interval); chef/admin view polls every 30s while document.visibilityState is 'visible' and pauses when hidden"
    - "D-03 pagination: load-more button when items.length < total; '加载更多愿望' label while loading; '没有更多愿望了' end-of-list state when items.length >= total"
    - "D-12: Reference URL safety: only http:// or https:// links are clickable in WishCard; relative scheme / javascript: / data: render as plain text"
    - "D-10 override: conflict toast shows backend's generic \"该愿望已被认领或状态已变更\" message (per UI-SPEC §13)"
    - "D-13, D-14: WishCard displays submitter_name on chef/admin views, claimed_by_chef_name on user view when claimed"
    - "D-15: WishCard 8px unread dot renders top-right when has_unread; submitter card-body tap clears it via getWish(id)"
    - "D-16, 已撤销: canceled cards persist as read-only with 已撤销 badge (overrides ROADMAP Success Criterion 3 per UI-SPEC §13)"
  artifacts:
    - path: "frontend/src/pages/UserWishesPage.jsx"
      provides: "User wish list page with create/edit/cancel/focus-refresh/FAB/related-dish enrichment"
      min_lines: 200
    - path: "frontend/src/pages/ChefWishesPage.jsx"
      provides: "Shared chef/admin queue engine with URL tabs, polling, advance/reject, pagination"
      min_lines: 240
    - path: "frontend/src/pages/AdminWishesPage.jsx"
      provides: "Thin wrapper mounting ChefWishesPage with viewAsAdmin=true"
      min_lines: 5
  key_links:
    - from: "frontend/src/pages/UserWishesPage.jsx"
      to: "frontend/src/components/WishFormModal.jsx"
      via: "onSuccess → api.createWish / api.updateWish"
      pattern: "api\\.(createWish|updateWish)"
    - from: "frontend/src/pages/UserWishesPage.jsx"
      to: "frontend/src/components/ConfirmModal.jsx"
      via: "Cancel confirmation pattern"
      pattern: "ConfirmModal"
    - from: "frontend/src/pages/ChefWishesPage.jsx"
      to: "frontend/src/api/client.js"
      via: "getWishes with status_filter / mine + claim/advance/reject"
      pattern: "api\\.(getWishes|claimWish|advanceWish|rejectWish)"
    - from: "frontend/src/pages/ChefWishesPage.jsx"
      to: "frontend/src/components/WishAdvanceModal.jsx"
      via: "onSuccess(selectedDishId, selectedDishName) → api.advanceWish"
      pattern: "api\\.advanceWish"
    - from: "frontend/src/pages/AdminWishesPage.jsx"
      to: "frontend/src/pages/ChefWishesPage.jsx"
      via: "renders <ChefWishesPage viewAsAdmin={true} />"
      pattern: "<ChefWishesPage viewAsAdmin"
---

<objective>
Build the three role-aware wish pages: `UserWishesPage` (own list with create/edit/cancel + focus refresh + FAB + related-dish enrichment), `ChefWishesPage` (URL-backed tabs 全部/待处理/我的认领 + 30s visible-only polling with stale-response guard + claim/advance/reject lifecycle actions + pagination), and `AdminWishesPage` (thin wrapper rendering `ChefWishesPage` with `viewAsAdmin=true`).

Purpose: These pages consume the Wave-1 components (WishCard, three modals, statusBadge extension, CSS classes) and the Wave-1 ApiClient methods. They encode the locked page architecture (D-01..D-04, D-07..D-10) and the status-matrix action row (D-07). Wave 3 wires navigation.

Output: 3 new page files. `npm run build` exits 0; targeted ESLint over the 3 new files exits 0.
</objective>

<execution_context>
@/home/temila/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/temila/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-wish-list-frontend/07-CONTEXT.md
@.planning/phases/07-wish-list-frontend/07-RESEARCH.md
@.planning/phases/07-wish-list-frontend/07-PATTERNS.md
@.planning/phases/07-wish-list-frontend/07-UI-SPEC.md

# Wave-1 outputs (now exist)
@frontend/src/api/client.js
@frontend/src/utils/index.js
@frontend/src/components/ConfirmModal.jsx
@frontend/src/components/WishCard.jsx
@frontend/src/components/WishFormModal.jsx
@frontend/src/components/WishRejectModal.jsx
@frontend/src/components/WishAdvanceModal.jsx

# Reference pages (existing patterns to follow)
@frontend/src/pages/UserOrdersPage.jsx
@frontend/src/pages/ChefOrdersPage.jsx

# Backend contract (read for context — do not modify)
@backend/app/schemas/wish.py
@backend/app/routers/wishes.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: UserWishesPage (own list + create + edit + cancel + focus refresh)</name>
  <files>frontend/src/pages/UserWishesPage.jsx</files>
  <read_first>
    - frontend/src/pages/UserOrdersPage.jsx (whole file — list/filter/CRUD user page analog)
    - frontend/src/components/WishCard.jsx (action props contract; currentUser + onEdit + onCancel + onTap)
    - frontend/src/components/WishFormModal.jsx (onSuccess(formData) contract)
    - frontend/src/components/ConfirmModal.jsx (props: title, message, confirmText, onConfirm, onCancel, danger)
    - frontend/src/api/client.js (getWishes / createWish / updateWish / cancelWish signatures)
    - frontend/src/contexts/AuthContext.jsx (useAuth: user with .id, .role)
    - .planning/phases/07-wish-list-frontend/07-PATTERNS.md lines 279-369 (UserWishesPage pattern map)
    - .planning/phases/07-wish-list-frontend/07-UI-SPEC.md §5.1, §5.6, §6.5, §8.1-§8.4 (page anatomy, FAB, cancel confirm, copy)
    - .planning/phases/07-wish-list-frontend/07-RESEARCH.md §Code Examples (lines 619-687 — loadRelatedDishNames, handleViewWish, buildWishPatch)
  </read_first>
  <action>
    Create `frontend/src/pages/UserWishesPage.jsx`:

    Default export, functional component, no props.

    Imports (in order):
    - `import { useState, useEffect, useRef, useCallback } from 'react';`
    - `import { Link } from 'react-router-dom';`
    - `import { useAuth } from '../contexts/AuthContext';`
    - `import { useToast } from '../contexts/ToastContext';`
    - `import api from '../api/client';`
    - `import Header from '../components/Header';`
    - `import BottomBar from '../components/BottomBar';`
    - `import Loading from '../components/Loading';`
    - `import EmptyState from '../components/EmptyState';`
    - `import ConfirmModal from '../components/ConfirmModal';`
    - `import WishCard from '../components/WishCard';`
    - `import WishFormModal from '../components/WishFormModal';`

    State:
    - `const [wishes, setWishes] = useState([]);` — list of own wishes
    - `const [total, setTotal] = useState(0);` — backend total for pagination
    - `const [page, setPage] = useState(1);` — current page
    - `const [loading, setLoading] = useState(true);` — only for initial fetch
    - `const [loadingMore, setLoadingMore] = useState(false);`
    - `const [relatedDishNames, setRelatedDishNames] = useState({});` — id → name map (Risk 2 workaround)
    - `const [actingId, setActingId] = useState(null);` — currently acting wish id (disables buttons)
    - `const [showCreate, setShowCreate] = useState(false);`
    - `const [editingWish, setEditingWish] = useState(null);`
    - `const [cancelTarget, setCancelTarget] = useState(null);` — wish being cancelled (drives ConfirmModal)
    - `const requestSeqRef = useRef(0);` — stale-response guard (Risk 5)

    Hooks:
    - `const { user } = useAuth();`
    - `const { showToast } = useToast();`

    Load function `loadWishes({ page: p = page, background = false } = {})`:
    - Increment `requestSeqRef.current` and capture as `seq`.
    - If `background === false`, `setLoading(true)`.
    - Try: `const res = await api.getWishes({ page: p, page_size: 20 });` (no `status`, no `mine` — backend filters to current user).
    - If `seq === requestSeqRef.current`: set wishes (append or replace based on p), set total.
    - After setting wishes, call `loadRelatedDishNames(res.items)` and merge into existing `relatedDishNames` map.
    - On error: `showToast('加载愿望失败', 'error')`.
    - Finally: if `background === false`, `setLoading(false)`; if background, `setLoadingMore(false)`.

    Helper `loadRelatedDishNames(items)` (async):
    - Collect unique `related_dish_id` values that are truthy and NOT already in `relatedDishNames` map.
    - `await Promise.allSettled(ids.map(id => api.getDish(id)))`.
    - Build new entries from fulfilled results (use `result.value.name`); merge into state via `setRelatedDishNames(prev => ({ ...prev, ...newEntries }))`.

    Initial load effect (only on mount):
    - Call `loadWishes({ page: 1 })`.

    Visibility / focus refresh effect (per UI-SPEC §7.6):
    - Attach `document.addEventListener('visibilitychange', handler)` where handler calls `loadWishes({ page: 1, background: true })` only when `document.visibilityState === 'visible'`.
    - Attach `window.addEventListener('focus', handler2)` where handler2 calls `loadWishes({ page: 1, background: true })`.
    - Cleanup: remove both listeners.

    Submit handler `handleCreateSubmit(payload)`:
    - Try: `await api.createWish(payload)`.
    - On success: close modal, `showToast('愿望已提交，厨师会尽快认领')`, `loadWishes({ page: 1, background: true })`.

    Edit handler `handleEditSubmit(payload)`:
    - Guard: `if (Object.keys(payload).length === 0) { showToast('未修改任何内容', 'error'); return; }`
    - Try: `await api.updateWish(editingWish.id, payload)`.
    - On success: close modal (`setEditingWish(null)`), show toast (`editingWish.status === '准备中' ? '已保存，已通知认领厨师' : '已保存'`), `loadWishes({ page: 1, background: true })`.

    Cancel handler `handleCancelConfirm()`:
    - Guard: `if (!cancelTarget) return; setActingId(cancelTarget.id);`
    - Try: `await api.cancelWish(cancelTarget.id)`.
    - On success: `setCancelTarget(null)`, `showToast('已撤销')`, `loadWishes({ page: 1, background: true })`.
    - On error: `showToast(err.message || '撤销失败', 'error')`.
    - Finally: `setActingId(null)`.

    Card tap handler `handleCardTap(wish)` (NOTIF-04 clearing interaction per Risk 3):
    - Guard: only meaningful when `wish.user_id === user.id && wish.has_unread === true`.
    - Try: `const detail = await api.getWish(wish.id);`
    - On success: `setWishes(prev => prev.map(w => w.id === wish.id ? { ...w, ...detail, has_unread: false } : w));`
    - On error: `showToast(err.message || '加载愿望详情失败', 'error')`.

    Load more handler `handleLoadMore()`:
    - `setLoadingMore(true)`, `loadWishes({ page: page + 1 })`, `setPage(p => p + 1)`.

    Render (per UI-SPEC §5.1):
    - `<div className="page-container">` root.
    - `<Header title="我的愿望">` with a header action button (`<button className="btn btn-primary btn-sm" onClick={() => setShowCreate(true)} aria-label="新建愿望">+ 新建愿望</button>`).
    - Body:
      - `loading` ? `<Loading />` : empty state OR list.
      - Empty state: `<EmptyState icon="💡" text="还没有愿望，去提交一个吧" />` plus subtext div with text `点击右下角「+」新建一个愿望`.
      - List: `<section className="section pt-0">` containing `{wishes.map(w => <WishCard key={w.id} wish={w} currentUser={user} currentRole="user" relatedDishName={relatedDishNames[w.related_dish_id]} onEdit={(wish) => setEditingWish(wish)} onCancel={(wish) => setCancelTarget(wish)} onTap={handleCardTap} />)}`.
      - Pagination control: `wishes.length < total` → `<button className="btn btn-secondary btn-block wish-load-more" onClick={handleLoadMore} disabled={loadingMore}>{loadingMore ? '加载中...' : '加载更多愿望'}</button>`; `wishes.length >= total && wishes.length > 0` → `<div className="wish-end-of-list">没有更多愿望了</div>`.
    - FAB: `<button className="fab" onClick={() => setShowCreate(true)} aria-label="新建愿望">+</button>` (rendered outside `<Header>` per UI-SPEC §5.6).
    - Modals (rendered conditionally at root, single-overlay invariant):
      - `{showCreate && <WishFormModal mode="create" onClose={() => setShowCreate(false)} onSuccess={handleCreateSubmit} />}`
      - `{editingWish && <WishFormModal wish={editingWish} mode="edit" onClose={() => setEditingWish(null)} onSuccess={handleEditSubmit} />}`
      - `{cancelTarget && <ConfirmModal title="撤销愿望" message={`撤销后无法恢复，确定要撤销「${cancelTarget.dish_name}」吗？`} confirmText="确认撤销" danger={true} onConfirm={handleCancelConfirm} onCancel={() => setCancelTarget(null)} />}`
    - `<BottomBar />` at the end.

    Stability requirements:
    - Define `loadWishes`, `loadRelatedDishNames`, `handleCardTap`, `handleCancelConfirm`, `handleCreateSubmit`, `handleEditSubmit`, `handleLoadMore` with `useCallback` so dependency arrays are complete and exhaustive-deps lint passes.
    - All `useEffect` MUST declare complete dependency arrays (per RESEARCH §Pitfall: suppress exhaustive-deps is forbidden).
    - No `console.log`, `debugger`, or `TODO` comments.
  </action>
  <verify>
    <automated>cd /home/temila/family_chef/frontend && grep -q "export default function UserWishesPage" src/pages/UserWishesPage.jsx && grep -q "api.createWish" src/pages/UserWishesPage.jsx && grep -q "api.updateWish" src/pages/UserWishesPage.jsx && grep -q "api.cancelWish" src/pages/UserWishesPage.jsx && grep -q "ConfirmModal" src/pages/UserWishesPage.jsx && grep -q "visibilitychange" src/pages/UserWishesPage.jsx && grep -q "requestSeqRef" src/pages/UserWishesPage.jsx && echo OK && npx eslint src/pages/UserWishesPage.jsx --max-warnings=0 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `UserWishesPage.jsx` exists with `export default function UserWishesPage`.
    - File imports `WishFormModal`, `WishCard`, `ConfirmModal`, `api`, `useAuth`, `useToast`, `Header`, `BottomBar`, `Loading`, `EmptyState`.
    - Page calls `api.getWishes({ page, page_size: 20 })` for list, `api.createWish` / `api.updateWish` / `api.cancelWish` for mutations, `api.getWish` for unread clearing.
    - Request sequencing uses `requestSeqRef` to guard against stale async responses (RESEARCH §Pattern 3).
    - Visibility/focus refresh listener attached for user-page refresh pattern (no 30s polling).
    - Cancel confirmation uses `ConfirmModal` (D-08 — only 撤销 triggers ConfirmModal). Reject is NOT used on this page.
    - Edit on claimed wish surfaces toast '已保存，已通知认领厨师'; edit on unclaimed surfaces '已保存'.
    - Related-dish enrichment via `api.getDish(id)` parallel calls deduplicated against existing `relatedDishNames` map.
    - Pagination: load-more button + end-of-list state.
    - FAB renders on user page only.
    - Targeted ESLint exits 0 with `--max-warnings=0`.
    - `cd frontend && npm run build` exits 0.
  </acceptance_criteria>
  <done>
    UserWishesPage implements WISH-03 (edit) and WISH-04 (cancel) end-to-end. The page is reachable once Wave 3 registers the route, and the create/edit/cancel flows work against the Phase 5 backend.
  </done>
</task>

<task type="auto">
  <name>Task 2: ChefWishesPage (tabs + polling + claim/advance/reject + pagination)</name>
  <files>frontend/src/pages/ChefWishesPage.jsx</files>
  <read_first>
    - frontend/src/pages/ChefOrdersPage.jsx (list + filter chips + action row — chef page analog)
    - frontend/src/components/WishCard.jsx (action props: onClaim / onAdvance / onReject + viewAsAdmin)
    - frontend/src/components/WishAdvanceModal.jsx (onSuccess(selectedDishId, selectedDishName) contract)
    - frontend/src/components/WishRejectModal.jsx (onSuccess(reason) contract)
    - frontend/src/api/client.js (getWishes with status_filter + mine; claimWish / advanceWish / rejectWish signatures)
    - .planning/phases/07-wish-list-frontend/07-PATTERNS.md lines 373-463 (ChefWishesPage pattern map)
    - .planning/phases/07-wish-list-frontend/07-UI-SPEC.md §5.4, §5.5, §6.1, §7.6, §7.7, §7.9 (URL tabs, action matrix, polling, pagination, conflict UX)
    - .planning/phases/07-wish-list-frontend/07-RESEARCH.md §Code Examples (buildWishParams, polling pattern with stale-response guard)
  </read_first>
  <action>
    Create `frontend/src/pages/ChefWishesPage.jsx`:

    Default export, functional component. Accepts props `{ viewAsAdmin = false }` so `AdminWishesPage` can mount this with admin override.

    Imports (in order):
    - `import { useState, useEffect, useRef, useCallback } from 'react';`
    - `import { useSearchParams } from 'react-router-dom';`
    - `import { useAuth } from '../contexts/AuthContext';`
    - `import { useToast } from '../contexts/ToastContext';`
    - `import api from '../api/client';`
    - `import Header from '../components/Header';`
    - `import BottomBar from '../components/BottomBar';`
    - `import Loading from '../components/Loading';`
    - `import EmptyState from '../components/EmptyState';`
    - `import WishCard from '../components/WishCard';`
    - `import WishAdvanceModal from '../components/WishAdvanceModal';`
    - `import WishRejectModal from '../components/WishRejectModal';`

    Tab logic per UI-SPEC §5.4:
    - `const VALID_TABS = new Set(['all', 'pending', 'mine']);`
    - `const [searchParams, setSearchParams] = useSearchParams();`
    - `const requestedTab = searchParams.get('tab') || 'all';`
    - `const activeTab = VALID_TABS.has(requestedTab) ? requestedTab : 'all';`
    - `const selectTab = useCallback((tab) => { setSearchParams((cur) => { const next = new URLSearchParams(cur); next.set('tab', tab); return next; }); }, [setSearchParams]);`

    Param builder (per RESEARCH §Code Examples):
    - `function buildWishParams(tab, page = 1) { const params = { page, page_size: 20 }; if (tab === 'pending') params.status = '待处理'; if (tab === 'mine') params.mine = true; return params; }`

    State:
    - `const [wishes, setWishes] = useState([]);`
    - `const [total, setTotal] = useState(0);`
    - `const [page, setPage] = useState(1);`
    - `const [loading, setLoading] = useState(true);`
    - `const [loadingMore, setLoadingMore] = useState(false);`
    - `const [relatedDishNames, setRelatedDishNames] = useState({});`
    - `const [actingId, setActingId] = useState(null);`
    - `const [advanceTarget, setAdvanceTarget] = useState(null);`
    - `const [rejectTarget, setRejectTarget] = useState(null);`
    - `const requestSeqRef = useRef(0);`

    Hooks: `const { user } = useAuth();` and `const { showToast } = useToast();`

    Load function `loadWishes({ page: p = page, background = false } = {})` (wrapped in `useCallback`):
    - Capture `const seq = ++requestSeqRef.current;` at function start.
    - If `!background`, call `setLoading(true)`.
    - Try: `const res = await api.getWishes(buildWishParams(activeTab, p));`
    - If `seq === requestSeqRef.current`:
      - If `p === 1`: setWishes(res.items || []); else setWishes(prev => [...prev, ...(res.items || [])]).
      - setTotal(res.total || 0).
      - Trigger `loadRelatedDishNames(res.items)` (defined as separate `useCallback`).
    - On error: `showToast('加载愿望失败', 'error')`.
    - Finally: if `background` then `setLoadingMore(false)` else `setLoading(false)`.

    Polling effect (per UI-SPEC §7.6):
    - Effect with deps `[activeTab, loadWishes]`:
      - Call `loadWishes({ page: 1 })` immediately.
      - `const timer = setInterval(() => { if (document.visibilityState === 'visible') loadWishes({ page: 1, background: true }); }, 30000);`
      - Return cleanup `() => clearInterval(timer);`
    - Tab change is implicit: when `activeTab` changes, the effect re-runs (loading new data resets page to 1 via `loadWishes({ page: 1 })`).

    Related-dish names: same parallel-dedupe pattern as UserWishesPage.

    Action handlers (each wrapped in `useCallback`):
    - `handleClaim(wish)`: `if (actingId) return; setActingId(wish.id); try { await api.claimWish(wish.id); showToast('已认领'); } catch (err) { showToast(err.message || '认领失败', 'error'); } finally { await loadWishes({ page: 1, background: true }); setActingId(null); }`
    - `handleAdvance(wish, relatedDishId, dishName)`: `if (actingId) return; setActingId(wish.id); try { await api.advanceWish(wish.id, relatedDishId); showToast('已成功关联 ' + dishName); setAdvanceTarget(null); } catch (err) { showToast(err.message || '推进失败', 'error'); } finally { await loadWishes({ page: 1, background: true }); setActingId(null); }`
    - `handleReject(wish, reason)`: `if (actingId) return; setActingId(wish.id); try { await api.rejectWish(wish.id, reason); showToast('已拒绝，提交者会收到通知'); setRejectTarget(null); } catch (err) { showToast(err.message || '拒绝失败', 'error'); } finally { await loadWishes({ page: 1, background: true }); setActingId(null); }`

    Load more: same `handleLoadMore` pattern as user page (call loadWishes with `page + 1`, increment page state).

    Render (per UI-SPEC §5.4, §5.5):
    - `<div className="page-container">`.
    - `<Header title={viewAsAdmin ? '愿望总览' : '愿望管理'} />` (admin gets different title per UI-SPEC §8.2; admin route mounts with viewAsAdmin=true).
    - Tab pills (NOT user-page; chef/admin only):
      - `<div className="filter-chips">` with 3 buttons: `全部` / `待处理` / `我的认领`. Each button: class `filter-chip` plus `active` when matches activeTab; onClick calls `selectTab(...)`. Counts derived from current loaded list — use `wishes.filter(w => w.status === '待处理').length` for pending, `wishes.filter(w => w.claimed_by_chef_id === user.id).length` for mine. Note: counts reflect loaded items only (acceptable for tabs; backend `total` would require extra query).
    - Body:
      - `loading` ? `<Loading />` : empty state OR list.
      - Per-tab empty state (UI-SPEC §8.4):
        - `activeTab === 'all'`: `📭 当前还没有可管理的愿望 — 用户提交新愿望后会显示在这里`.
        - `activeTab === 'pending'`: `✅ 当前没有待处理愿望 — 所有愿望都已被认领或处理`.
        - `activeTab === 'mine'`: `📋 你还没有认领任何愿望 — 前往「待处理」认领一个愿望`.
      - List: `{wishes.map(w => <WishCard key={w.id} wish={w} currentUser={user} currentRole={viewAsAdmin ? 'admin' : 'chef'} viewAsAdmin={viewAsAdmin} relatedDishName={relatedDishNames[w.related_dish_id]} onClaim={handleClaim} onAdvance={(wish) => setAdvanceTarget(wish)} onReject={(wish) => setRejectTarget(wish)} />)}`.
      - Pagination: same load-more button + end-of-list div.
    - Modals (single-overlay invariant):
      - `{advanceTarget && <WishAdvanceModal wish={advanceTarget} onClose={() => setAdvanceTarget(null)} onSuccess={(dishId, dishName) => handleAdvance(advanceTarget, dishId, dishName)} />}`
      - `{rejectTarget && <WishRejectModal wish={rejectTarget} onClose={() => setRejectTarget(null)} onSuccess={(reason) => handleReject(rejectTarget, reason)} />}`
    - `<BottomBar />`.

    Stability:
    - All functions wrapped in `useCallback` with complete dependency arrays.
    - `useEffect` deps include `[activeTab, loadWishes]`.
    - Polling `setInterval` cleanup on unmount.
    - No suppressed `react-hooks/exhaustive-deps`.
  </action>
  <verify>
    <automated>cd /home/temila/family_chef/frontend && grep -q "export default function ChefWishesPage" src/pages/ChefWishesPage.jsx && grep -q "useSearchParams" src/pages/ChefWishesPage.jsx && grep -q "api.claimWish" src/pages/ChefWishesPage.jsx && grep -q "api.advanceWish" src/pages/ChefWishesPage.jsx && grep -q "api.rejectWish" src/pages/ChefWishesPage.jsx && grep -q "WishAdvanceModal" src/pages/ChefWishesPage.jsx && grep -q "WishRejectModal" src/pages/ChefWishesPage.jsx && grep -q "setInterval" src/pages/ChefWishesPage.jsx && grep -q "requestSeqRef" src/pages/ChefWishesPage.jsx && grep -q "buildWishParams" src/pages/ChefWishesPage.jsx && echo OK && npx eslint src/pages/ChefWishesPage.jsx --max-warnings=0 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `ChefWishesPage.jsx` exists with `export default function ChefWishesPage` accepting `{ viewAsAdmin = false }`.
    - File uses `useSearchParams` for URL-backed tab state with `?tab=all|pending|mine` and defaults unknown values to `all`.
    - File imports `WishAdvanceModal`, `WishRejectModal`, `WishCard`, `api`, `useAuth`, `useToast`.
    - Calls `api.getWishes(buildWishParams(activeTab, page))` where `buildWishParams` serializes `status_filter` for pending tab and `mine=true` for mine tab (both via `api.getWishes`'s `status` / `mine` params — the serialization happens inside `getWishes`).
    - Action handlers exist: `handleClaim` calls `api.claimWish`; `handleAdvance` calls `api.advanceWish`; `handleReject` calls `api.rejectWish`. All three use pessimistic `setActingId` and final background refetch.
    - 30s polling uses `setInterval` with cleanup; pauses when `document.visibilityState === 'hidden'` (the interval checks visibility before calling loadWishes).
    - `requestSeqRef` guards stale responses.
    - Tab change resets list (loading triggers immediate fetch on tab change).
    - Pass-through `viewAsAdmin` prop flows to each `<WishCard viewAsAdmin={viewAsAdmin} />` so admin gets admin actions on any wish.
    - Targeted ESLint exits 0 with `--max-warnings=0`.
    - `cd frontend && npm run build` exits 0.
  </acceptance_criteria>
  <done>
    ChefWishesPage implements claim/advance/reject (per FLOW-02..04), URL tabs, polling, pagination. The shared component is reused by AdminWishesPage (Task 3) and supports the admin role via viewAsAdmin override.
  </done>
</task>

<task type="auto">
  <name>Task 3: AdminWishesPage (thin wrapper mounting ChefWishesPage with viewAsAdmin)</name>
  <files>frontend/src/pages/AdminWishesPage.jsx</files>
  <read_first>
    - frontend/src/pages/ChefWishesPage.jsx (already created in Task 2 of this plan — RE-READ for accurate default export and viewAsAdmin prop contract)
    - .planning/phases/07-wish-list-frontend/07-PATTERNS.md lines 465-482 (AdminWishesPage wrapper pattern)
    - .planning/phases/07-wish-list-frontend/07-CONTEXT.md D-04 (admin reuses chef component with flag)
  </read_first>
  <action>
    Create `frontend/src/pages/AdminWishesPage.jsx`:

    Default export, functional component, no props. Pure thin wrapper per D-04.

    Imports:
    - `import ChefWishesPage from './ChefWishesPage';`

    Render: `return <ChefWishesPage viewAsAdmin={true} />;`

    Do not add additional logic, state, or effects. The wrapper exists solely to preserve the clean `/admin/wishes` URL while sharing the chef page engine. Per Risk 10: ChefWishesPage already gates admin actions to lifecycle ops (claim/advance/reject) and never renders submitter-only edit/cancel.

    No CSS changes, no API calls, no toast logic in this file.
  </action>
  <verify>
    <automated>cd /home/temila/family_chef/frontend && grep -q "export default function AdminWishesPage" src/pages/AdminWishesPage.jsx && grep -q "viewAsAdmin" src/pages/AdminWishesPage.jsx && test $(wc -l < src/pages/AdminWishesPage.jsx) -le 8 && echo OK && npx eslint src/pages/AdminWishesPage.jsx --max-warnings=0 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `AdminWishesPage.jsx` exists with `export default function AdminWishesPage`.
    - File is at most 8 lines total (thin wrapper).
    - The single render returns `<ChefWishesPage viewAsAdmin={true} />`.
    - Targeted ESLint exits 0.
    - `cd frontend && npm run build` exits 0.
  </acceptance_criteria>
  <done>
    AdminWishesPage completes the role-page trio. Admin users land on /admin/wishes and inherit the chef queue engine with admin-only action overrides (per Risk 10, no submitter-only edit/cancel is exposed).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → page actions (button clicks) | Submitter/chef/admin role + claimer match determines action availability |
| Page → backend mutation endpoints | Pages send only validated payloads; backend authorization is authoritative |
| API response → local state | Backend returns server-truth status / has_unread / claimed_by_chef_name; client mirrors |
| URL query params | `?tab=...` and `?wish=:id` come from user-controlled sources but are validated server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-P01 | Elevation of Privilege | UserWishesPage cancel/edit actions | mitigate | Action buttons (编辑愿望 / 撤销愿望) render only when `wish.status ∈ {待处理, 准备中}` AND viewer is submitter. Backend ownership check (`wish_service.py:167-173,220-226`) is authoritative; 403 surfaced via `err.message` toast. |
| T-07-P02 | Elevation of Privilege | ChefWishesPage claim/advance/reject | mitigate | `[认领]` only on unclaimed 待处理; `[推进] [拒绝]` only when `wish.claimed_by_chef_id === currentUser.id`. `viewAsAdmin=true` enables admin override but does NOT add submitter-only edit/cancel (Risk 10). |
| T-07-P03 | Tampering / Concurrent claim race | ChefWishesPage claim button | mitigate | Pessimistic `setActingId(wish.id)` disables all action buttons while in flight. On 400 race loss, `showToast(err.message)` (the backend generic `"该愿望已被认领或状态已变更"` — D-10 override per UI-SPEC §13) followed by background refetch. Card re-renders as 准备中 owned by another chef (read-only for this viewer). |
| T-07-P04 | Information Disclosure / Unread state | UserWishesPage card tap | mitigate | `handleCardTap` only meaningful for `wish.user_id === user.id && wish.has_unread === true`. Backend already enforces `compute_has_unread` 身份屏蔽 — non-submitter viewers always see `has_unread=false`. Detail GET clears `submitter_last_viewed_at` only for submitter. |
| T-07-P05 | Tampering / Stale async response | All wish pages | mitigate | `requestSeqRef.current` increments before every fetch; only commit response if `seq === requestSeqRef.current`. Older in-flight polling responses cannot overwrite newer state. Pattern documented in RESEARCH §Pattern 3. |
| T-07-P06 | Repudiation / Action side-effect awareness | UserWishesPage edit modal on claimed wish | mitigate | When editing a 准备中 wish, the parent page passes `editingWish.claimed_by_chef_name` to WishFormModal which surfaces a `info-pill` "信息：编辑此愿望将通知认领厨师「{chef_name}」" before save. After successful save, toast confirms "已保存，已通知认领厨师" (NOTIF-06 disclosure). |
| T-07-P07 | Information Disclosure / Visibility leakage | WishCard submitter_name display | mitigate | D-13: `submitter_name` only renders on chef/admin views. D-14: `claimed_by_chef_name` only renders on user view when claimed. Implementation guards on `currentRole` from parent. |
| T-07-P08 | Denial of Service / URL enumeration | `/wishes/:id` deep-link redirect | accept | Phase 6 emits `/wishes/:id` links. Wave 3 will add a redirect to the role's wish page with `?wish=:id` highlight. If wish is not found in loaded list, page shows toast "未找到该愿望，可能已撤销或需要切换标签". No enumeration of hidden wishes via URL — backend 404 returns generic toast. |
| T-07-P09 | Tampering / Reference URL | WishCard secondary line rendering | mitigate | URL must start with `http://` or `https://` to render as clickable `<a>`; otherwise plain text. Already enforced in WishCard.jsx from Plan 01. |
| T-07-P10 | Spoofing / ChefFilterDish | WishAdvanceModal | mitigate | Modal queries `api.getDishes({ status: 'enabled', chef_filter: 'my-published' })` so UI only shows dishes the chef has published. Backend `wish_service.py:328-338` revalidates on mutation; client 400 surfaced as toast. |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] packages — NOT TRIGGERED this phase (RESEARCH §Package Legitimacy Audit: "this phase installs no external package"). |
</threat_model>

<verification>
- [ ] `cd frontend && npm run build` exits 0
- [ ] `cd frontend && npx eslint src/pages/UserWishesPage.jsx src/pages/ChefWishesPage.jsx src/pages/AdminWishesPage.jsx --max-warnings=0` exits 0
- [ ] grep checks: UserWishesPage imports WishFormModal + ConfirmModal + api methods; ChefWishesPage imports useSearchParams + WishAdvanceModal + WishRejectModal + api.claimWish/advanceWish/rejectWish; AdminWishesPage mounts ChefWishesPage with viewAsAdmin=true.
- [ ] No `console.log` / `debugger` / `TODO` comments in any of the 3 new files.
- [ ] All `useEffect` have complete dependency arrays (no suppressed exhaustive-deps).
- [ ] `requestSeqRef` declared in UserWishesPage and ChefWishesPage.
- [ ] `setInterval` cleanup present in ChefWishesPage polling effect.
</verification>

<success_criteria>
1. WISH-03 implemented: UserWishesPage edit flow opens WishFormModal in edit mode (pre-filled), submits diff-only payload to `api.updateWish`, refetches list on success.
2. WISH-04 implemented: UserWishesPage cancel flow opens ConfirmModal (D-08 lock — only 撤销 triggers ConfirmModal), calls `api.cancelWish`, refetches list; canceled card persists as 已撤销 read-only (per CONTEXT D-16, OVERRIDES ROADMAP Success Criterion 3 per UI-SPEC §13 Q6).
3. UX-01 implemented: UserWishesPage is mobile-friendly — FAB on bottom-right, header action button, single-column card layout, modal forms scannable on 375px viewport.
4. UX-02 implemented: ChefWishesPage is mobile-friendly — URL-backed tabs, polling, action row with 44px tap targets, pagination, dish picker modal works on small screens.
5. UX-03 implemented: WishCard status badge (consumed from Plan 01's Badge + statusBadge extension) renders the correct semantic color across all five wish statuses.
6. ChefWishesPage implements FLOW-02 (claim), FLOW-03 (advance via dish picker), FLOW-04 (reject with reason), FLOW-05 (我的认领 tab), all gated by the D-07 status matrix.
7. AdminWishesPage mounts ChefWishesPage with viewAsAdmin=true, exposing admin lifecycle actions on any wish but NOT submitter-only edit/cancel for other users (Risk 10).
8. Threat mitigations applied: stale-response guard via requestSeqRef; pessimistic action buttons; backend-authoritative error handling; URL scheme validation in WishCard (already from Plan 01).
9. Targeted ESLint over the 3 new files exits 0; `npm run build` exits 0.
</success_criteria>

<output>
Create `.planning/phases/07-wish-list-frontend/07-02-SUMMARY.md` when done
</output>