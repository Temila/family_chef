---
phase: 07-wish-list-frontend
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/main.py
# NOTE: backend/data/family_chef.db* are runtime data, gitignored (`.gitignore: data/`).
# The migration applies a local schema change; the migration SCRIPT was committed in Phase 6.
autonomous: true
requirements:
  - WISH-03
  - WISH-04
  - UX-01
  - UX-02
  - UX-03
gap_closure: true
must_haves:
  truths:
    - "GET /api/wishes with an authenticated user token returns HTTP 200 (not 500)"
    - "wishes table has both last_status_change_at and submitter_last_viewed_at columns"
    - "alembic_version points to the head revision 3a41e4977098"
    - "No existing wishes rows were lost or modified by the migration"
    - "backend/app/main.py carries a NOTE(07-04) comment flagging the absence of an automatic alembic upgrade on startup"
  artifacts:
    - path: "backend/data/family_chef.db"
      provides: "Local SQLite DB state with the two notification timestamp columns present on wishes (runtime data, gitignored — NOT committed)"
      contains: "last_status_change_at"
    - path: "backend/alembic/versions/3a41e4977098_add_wish_notification_timestamps.py"
      provides: "Migration that adds last_status_change_at + submitter_last_viewed_at via batch_alter_table (recreate=always) — committed in Phase 6"
    - path: "backend/app/main.py"
      provides: "Startup hook carries NOTE(07-04) comment above await init_db() documenting the missing auto-migration step"
      contains: "NOTE(07-04)"
  key_links:
    - from: "backend/alembic.ini"
      to: "backend/data/family_chef.db"
      via: "sqlalchemy.url = sqlite+aiosqlite:///./data/family_chef.db (relative to cwd backend/)"
      pattern: "alembic upgrade head"
    - from: "backend/app/models/wish.py"
      to: "backend/data/family_chef.db#wishes"
      via: "Wish.last_status_change_at + Wish.submitter_last_viewed_at columns"
      pattern: "last_status_change_at|submitter_last_viewed_at"
    - from: "backend/app/routers/wishes.py#GET /"
      to: "WishListResponse.has_unread"
      via: "compute_has_unread reads both new columns on every list query"
      pattern: "compute_has_unread"
---

<objective>
Apply the pending Phase-6 Alembic migration `3a41e4977098_add_wish_notification_timestamps.py` to the running SQLite database so the `wishes` table gains the `last_status_change_at` and `submitter_last_viewed_at` columns that the SQLAlchemy `Wish` model already declares. Once applied, `GET /api/wishes` returns 200 instead of 500, and the user-facing wish list loads normally. Also annotate `backend/app/main.py` with a NOTE flagging the absence of an automatic alembic upgrade on startup so future regressions of this class are visible.

Purpose: This is the single blocker diagnosed in `07-HUMAN-UAT.md` H-1 ("打开我的愿望时持续报错加载愿望单失败"). Every other Phase-7 UAT item depends on the wish list loading. The migration file already exists and is non-destructive (only ADDS two nullable columns), so no application code logic needs to change — only the schema state and a documentation marker.

Output: `backend/data/family_chef.db` brought into sync with the migration chain; `alembic current` reports head revision; live HTTP probe of `GET /api/wishes` with the seeded `admin/admin` user returns 200; `backend/app/main.py` carries a `NOTE(07-04)` comment block.
</objective>

<execution_context>
@/home/temila/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/temila/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-wish-list-frontend/07-HUMAN-UAT.md
@.planning/debug/resolved/wish-list-load-failure.md
@backend/alembic/versions/3a41e4977098_add_wish_notification_timestamps.py
@backend/alembic.ini
@backend/alembic/env.py
@backend/app/main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply the pending Alembic migration to the running SQLite database</name>
  <files>backend/data/family_chef.db, backend/data/family_chef.db-wal, backend/data/family_chef.db-shm</files>
  <action>
    From the repository root, apply the pending Phase-6 migration to the running database. The migration uses `batch_alter_table("wishes", recreate="always")` to add two nullable columns (`last_status_change_at` with `CURRENT_TIMESTAMP` server default, `submitter_last_viewed_at` with no default); this is non-destructive — existing rows are preserved.

    Step-by-step:

    1. Sanity-check the pre-state. Run the SQLite CLI against the existing DB and capture the current `alembic_version` and the column list of the `wishes` table. Use `sqlite3 backend/data/family_chef.db "PRAGMA table_info(wishes);"` and `sqlite3 backend/data/family_chef.db "SELECT * FROM alembic_version;"` so the output lands in the executor's log. Expect `alembic_version` to be `72b56533bb6d` and the wishes column list to NOT contain `last_status_change_at` or `submitter_last_viewed_at`. If the column list ALREADY contains them, stop and report that the migration was already applied (the actual root cause must be elsewhere).

    2. Stop the backend if it is running. Any process holding the SQLite file open could conflict with the migration's `batch_alter_table` recreation. The standard dev script `scripts/run-dev.sh` starts uvicorn on port 8000; if `lsof -i :8000` or `pgrep -f "uvicorn app.main:app"` shows a live process, kill it before continuing. Do not modify the script or any systemd unit — just stop the local dev process for this plan.

    3. Run the migration. Working directory MUST be `backend/` because `alembic.ini` line 89 sets `sqlalchemy.url = sqlite+aiosqlite:///./data/family_chef.db` as a path relative to the alembic.ini location, and `prepend_sys_path = .` requires `app.*` imports to resolve from cwd. Use `cd backend && uv run alembic upgrade head` (preferred — uv is the project's Python package manager per STACK.md). Fallback: `cd backend && alembic upgrade head` if `uv` is not on PATH in this environment. Do NOT pass `--sql` or any offline-mode flag — this is an online migration against the live DB.

    4. Confirm the upgrade. From `backend/`, run `uv run alembic current` and expect output containing the head revision `3a41e4977098`. Also run `uv run alembic history` and confirm the head of the linear chain is the same revision.

    5. Verify the columns were added. Run `sqlite3 backend/data/family_chef.db "PRAGMA table_info(wishes);"` and confirm two new rows appear with `name = last_status_change_at` (type `DATETIME`, default `CURRENT_TIMESTAMP`) and `name = submitter_last_viewed_at` (type `DATETIME`, no default).

    6. Verify no data loss. Run `sqlite3 backend/data/family_chef.db "SELECT COUNT(*) FROM wishes;"` BEFORE the migration in step 1 (capture the count), then again AFTER the upgrade in step 5. The counts MUST match — `batch_alter_table(recreate="always")` recreates the table and copies rows, so a count mismatch indicates something unexpected and the executor must roll back to revision `72b56533bb6d` (`alembic downgrade -1`) and stop.

    7. Confirm the DB files are NOT committed. `backend/data/` is gitignored (see `.gitignore` line 43: `data/`), so the SQLite file is a **local runtime state change** — it must NOT be staged or committed. Run `git check-ignore backend/data/family_chef.db` and confirm it prints the path (proving the ignore is active). If `git status` shows the DB files as modified, the gitignore is broken and the executor must STOP and report it. The migration SCRIPT was already committed in Phase 6; only the DB *state* changed locally.
  </action>
  <verify>
    <automated>cd backend && (uv run alembic current 2>/dev/null || alembic current) 2>&1 | grep -q "3a41e4977098" && (sqlite3 data/family_chef.db "PRAGMA table_info(wishes);" 2>/dev/null || python3 -c "import sqlite3; [print(r[1]) for r in sqlite3.connect('data/family_chef.db').execute('PRAGMA table_info(wishes)').fetchall()]") | grep -E "last_status_change_at|submitter_last_viewed_at" | wc -l | grep -q "^2$"</automated>
  </verify>
  <done>
    - `cd backend && uv run alembic current` reports head revision `3a41e4977098` (or the fallback `alembic current` does).
    - `PRAGMA table_info(wishes)` lists both `last_status_change_at` and `submitter_last_viewed_at`.
    - `SELECT COUNT(*) FROM wishes` returns the same value as before the upgrade.
    - `git check-ignore backend/data/family_chef.db` prints the path (confirms DB is NOT staged for commit — runtime data only).
    - No DB files are committed (the migration SCRIPT was already committed in Phase 6).
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify GET /api/wishes returns 200 against a live backend and annotate main.py</name>
  <files>backend/app/main.py</files>
  <action>
    Prove the user-facing symptom is gone by exercising the actual HTTP endpoint against a live uvicorn instance, then capture a brief observation about `backend/app/main.py`'s startup hook for follow-up planning.

    Step-by-step:

    1. Start the backend in the background on a non-default port (avoid 8000 collisions with any other running instance). From the repo root run: `cd backend && uv run uvicorn app.main:app --host 127.0.0.1 --port 18000 --log-level warning` in the background, redirect stdout+stderr to `/tmp/uvicorn-18000.log`, and wait for the process to print "启动成功" (the `_log` line emitted by `main.py` near line 242). Poll the log file for up to 15 seconds; if the banner doesn't appear, fail and surface the log.

    2. Acquire an admin JWT. The seeded default admin per `backend/app/initial_data.py:25-32` is `admin` / `admin`. POST to `/api/auth/login` with form-encoded body `username=admin&password=admin` (the existing `/api/auth/login` endpoint accepts `application/x-www-form-urlencoded` per FastAPI's `OAuth2PasswordRequestForm`). Capture the `access_token` field. Use `curl -s -X POST http://127.0.0.1:18000/api/auth/login -H 'Content-Type: application/x-www-form-urlencoded' -d 'username=admin&password=admin'`.

    3. Probe the previously-failing endpoint. GET `http://127.0.0.1:18000/api/wishes` with the bearer token. Expect HTTP 200, body shape `{"total": N, "page": 1, "page_size": 20, "items": [...]}` where each item has fields including `last_status_change_at`, `submitter_last_viewed_at`, and `has_unread`. Use `curl -s -o /tmp/wishes.json -w '%{http_code}' http://127.0.0.1:18000/api/wishes -H "Authorization: Bearer ${TOKEN}"` and assert the status code is `200`. Then run `python3 -c "import json; d=json.load(open('/tmp/wishes.json')); assert 'items' in d and 'total' in d; assert all('last_status_change_at' in i and 'submitter_last_viewed_at' in i and 'has_unread' in i for i in d['items']); print('OK')"` — this confirms the schema and the migration's columns are visible at the HTTP boundary.

    4. Probe one positive control. GET `/api/health` and assert 200 — proves the server is genuinely running and the test is not silently passing because of a network error.

    5. Shut down the test server. Send SIGTERM to the uvicorn PID captured in step 1. Wait up to 5 seconds for graceful exit, then SIGKILL if needed. Clean up `/tmp/uvicorn-18000.log` and `/tmp/wishes.json`.

    6. Capture the missing-hook observation. Open `backend/app/main.py` near the `startup` function (around lines 227-244) and confirm the `startup` hook calls `init_db()` (which creates tables IF NOT EXIST but never runs Alembic upgrade), `create_initial_data()`, `create_preset_categories()`, `create_preset_ingredients()`, and nothing else. Add a comment block IMMEDIATELY ABOVE the `await init_db()` call reading exactly:

       ```
       # NOTE(07-04): No automatic `alembic upgrade head` runs at startup.
       # New migrations must be applied manually via `cd backend && uv run alembic upgrade head`.
       # See .planning/phases/07-wish-list-frontend/07-04-PLAN.md for context.
       ```

       Do NOT add the actual auto-migration call — that decision (whether to make the server self-heal migrations at startup) is a deliberate architectural choice with deployment, concurrency, and rollback implications and belongs in a separate planning cycle.

    7. Commit the annotation. Stage `backend/app/main.py` and commit with message `fix(07-04): note absence of startup alembic hook in main.py`. This is the ONLY commit on the branch for this plan — Task 1's migration fix is a local DB state change (gitignored runtime data), so there is no DB commit to fold in.
  </action>
  <verify>
    <automated>cd backend && (uv run uvicorn app.main:app --host 127.0.0.1 --port 18000 --log-level warning >/tmp/uvicorn-18000.log 2>&1 &) ; for i in $(seq 1 15); do curl -sf http://127.0.0.1:18000/api/health >/dev/null 2>&1 && break; sleep 1; done ; TOKEN=$(curl -s -X POST http://127.0.0.1:18000/api/auth/login -H 'Content-Type: application/x-www-form-urlencoded' -d 'username=admin&password=admin' | python3 -c 'import json,sys; print(json.load(sys.stdin)["access_token"])') ; STATUS=$(curl -s -o /tmp/wishes.json -w '%{http_code}' http://127.0.0.1:18000/api/wishes -H "Authorization: Bearer ${TOKEN}") ; pkill -f 'uvicorn app.main:app --host 127.0.0.1 --port 18000' ; [ "$STATUS" = "200" ] && python3 -c "import json; d=json.load(open('/tmp/wishes.json')); assert 'items' in d and 'total' in d and all('has_unread' in i and 'last_status_change_at' in i and 'submitter_last_viewed_at' in i for i in d['items']); print('PASS')" && grep -q "NOTE(07-04)" app/main.py && echo "ALL CHECKS PASSED"</automated>
  </verify>
  <done>
    - `GET /api/wishes` with a valid bearer token returns HTTP 200 (not 500), and the response JSON contains the new columns `last_status_change_at`, `submitter_last_viewed_at`, and `has_unread`.
    - `GET /api/health` returns 200.
    - The background uvicorn process is terminated; `/tmp/uvicorn-18000.log` and `/tmp/wishes.json` are removed.
    - `backend/app/main.py` contains the `NOTE(07-04)` comment block above `await init_db()` documenting the absence of an automatic alembic upgrade.
    - Exactly ONE commit lands on the branch: `fix(07-04): note absence of startup alembic hook in main.py` (the only source artifact; DB state change is local-only/gitignored).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer-host → SQLite file | The agent runs `alembic upgrade head` against `backend/data/family_chef.db` on the user's local dev machine. |
| local uvicorn → SQLite file | The Task-2 verification spins up a local uvicorn on port 18000 and reads/writes the same DB file. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-04-01 | Tampering | alembic upgrade execution | accept | Migration script is committed in repo (`backend/alembic/versions/3a41e4977098_add_wish_notification_timestamps.py`); Alembic only applies scripts whose revision ID matches the chain head. Local dev machine only — not a remote attacker surface. |
| T-7-04-02 | Information Disclosure | HTTP probe of `/api/wishes` with admin token | accept | Admin/admin credentials are the seeded default per `initial_data.py:25-32`; the token never leaves the executor's local filesystem (`/tmp/wishes.json`), no network egress. |
| T-7-04-03 | Denial of Service | `batch_alter_table(recreate="always")` locks wishes table during copy | accept | SQLite single-file, local dev only, table has < 1000 wishes rows; copy completes in < 1 second. Verified via COUNT pre/post check in Task 1. |
| T-7-04-04 | Elevation of Privilege | Local uvicorn background process left running | mitigate | Task 2 step 5 sends SIGTERM (then SIGKILL if needed) and verifies shutdown; `pkill -f` in the verify script is a defense-in-depth fallback. |
| T-7-04-05 | Repudiation | Migration applied without audit trail | mitigate | DB state captured in alembic_version table; the migration SCRIPT was committed in Phase 6 (git history records the schema change); debug session `.planning/debug/resolved/wish-list-load-failure.md` records the diagnosis; the `NOTE(07-04)` comment + plan 07-04 commit document the local DB state fix. |
| T-7-04-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan — migration runs against existing `uv`-managed environment only. Threat does not apply. |
</threat_model>

<verification>
1. `cd backend && uv run alembic current` reports head revision `3a41e4977098`.
2. `sqlite3 data/family_chef.db "PRAGMA table_info(wishes);"` (from `backend/`) lists both new columns.
3. `SELECT COUNT(*) FROM wishes` matches the pre-migration count.
4. `curl -H "Authorization: Bearer ${TOKEN}" http://127.0.0.1:18000/api/wishes` returns HTTP 200 with the expected `PageResponse<WishListResponse>` body containing `has_unread`, `last_status_change_at`, and `submitter_last_viewed_at`.
5. `curl http://127.0.0.1:18000/api/health` returns 200.
6. `backend/app/main.py` contains the `NOTE(07-04)` comment above `await init_db()`.
7. `git check-ignore backend/data/family_chef.db` prints the path (confirms DB NOT staged); `git status` shows only `backend/app/main.py` modified.
8. The UAT test H-1 ("打开我的愿望时持续报错加载愿望单失败") can be re-run by the human and now passes — the wish list loads.
</verification>

<success_criteria>
- Phase 7's blocker (UAT H-1) is resolved: `GET /api/wishes` returns 200 for any authenticated user.
- The `wishes` table schema matches the SQLAlchemy `Wish` model definition (lines 33-34 of `backend/app/models/wish.py`).
- Alembic version state is at head; no migration drift remains in the chain.
- No existing wishes data is lost (count unchanged, status values preserved).
- The fix ships as ONE commit (`backend/app/main.py` annotation only); the DB schema change is a local state fix (gitignored runtime data).
- Future regressions of this class are observable: the `NOTE(07-04)` comment in `main.py` flags the missing startup hook for follow-up planning without making a unilateral architectural decision.
</success_criteria>

<output>
Create `.planning/phases/07-wish-list-frontend/07-04-SUMMARY.md` when done.
</output>