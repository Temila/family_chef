---
phase: 15-navigation-restructure-test-data
plan: 06
type: execute
wave: 5
depends_on:
  - "15-02"
  - "15-03"
  - "15-04"
  - "15-05"
files_modified:
  - backend/app/initial_data.py
  - backend/app/main.py
  - frontend/tests/phase15-navigation.spec.js
  - backend/tests/test_initial_data.py
autonomous: true
requirements:
  - NAV-04
  - NAV-05
  - BUG-06
  - DATA-01
  - UI-01

must_haves:
  truths:
    - "backend/app/initial_data.py contains async def create_seed_test_dishes()"
    - "create_seed_test_dishes() guards on ENVIRONMENT=development OR AUTO_SEED_DEMO_DISHES=1 (returns early otherwise)"
    - "create_seed_test_dishes() inserts exactly 8 Dish records with all 2^3 combinations of recipe / description / image presence"
    - "create_seed_test_dishes() is idempotent (second call inserts 0 records if all 8 [1]..[8] names already exist)"
    - "All 8 seed dishes have created_by = default admin user id"
    - "Each seed dish is linked to at least one region Category via DishCategory"
    - "All 8 seed dishes have name prefix 测试菜品 (so name LIKE '测试菜品 %' matches)"
    - "backend/app/main.py imports create_seed_test_dishes and awaits it after create_preset_ingredients()"
    - "backend/tests/test_initial_data.py contains pytest cases covering: 8-row creation, idempotency, admin link, category link, env guard, missing-admin guard"
    - "frontend/tests/phase15-navigation.spec.js contains Playwright tests covering Header action-bar, avatar menu (2 items), Sidebar footer version, BottomBar tab counts/orders per role, UserHomePage quick actions per role, OrderPage filter Sheet"
    - "All 4 modified files pass syntax validation (python -m py_compile / node --check)"
  artifacts:
    - path: "backend/app/initial_data.py"
      provides: "create_seed_test_dishes() async function with env guard + idempotency + 8 seed rows"
      exports: ["create_seed_test_dishes"]
      contains: "def create_seed_test_dishes"
    - path: "backend/app/main.py"
      provides: "Startup hook awaits create_seed_test_dishes after create_preset_ingredients"
      contains: "create_seed_test_dishes"
    - path: "frontend/tests/phase15-navigation.spec.js"
      provides: "Playwright tests for NAV-01..05, UI-01, BUG-06 verification targets"
      exports: ["test"]
      contains: "phase15-navigation"
    - path: "backend/tests/test_initial_data.py"
      provides: "pytest cases for seed 8-row creation, idempotency, env guard, admin link, category link"
      exports: ["test_seed_test_dishes_creates_eight_combinations"]
      contains: "test_seed_test_dishes"
  key_links:
    - from: "main.py"
      to: "initial_data.create_seed_test_dishes"
      via: "await create_seed_test_dishes() called after await create_preset_ingredients() during startup"
      pattern: "await create_seed_test_dishes"
    - from: "initial_data.create_seed_test_dishes"
      to: "Dish + DishCategory models"
      via: "async with async_session_factory() inserts 8 Dish + 8 DishCategory rows"
      pattern: "session.add\\(Dish\\("
    - from: "test_initial_data.py"
      to: "initial_data.create_seed_test_dishes"
      via: "await create_seed_test_dishes() invoked inside async test functions with monkeypatched env"
      pattern: "await create_seed_test_dishes"
    - from: "phase15-navigation.spec.js"
      to: "Header.jsx + Sidebar.jsx + BottomBar.jsx + UserHomePage.jsx + OrderPage.jsx"
      via: "page.locator() assertions verify Phase 15 contracts (action-bar, version text, tab counts, Sheet)"
      pattern: "md-header__theme-toggle|md-sidebar__version|md-tab|quick-action|md-modal--bottom-sheet"
---

<objective>
Add the Phase 15 dev seed data fixture (DATA-01) and the regression test suite that locks the Phase 15 navigation contracts (NAV-01..05, UI-01, BUG-06 visual verification target).

This plan covers 4 files in 3 logical groups:

1. **Backend seed (DATA-01)**: Add `create_seed_test_dishes()` to backend/app/initial_data.py. Function is environment-guarded (only fires when ENVIRONMENT=development OR AUTO_SEED_DEMO_DISHES=1), idempotent (detects existing [1]..[8] prefix-named rows), and inserts 8 Dish + 8 DishCategory records covering all 2^3 combinations of recipe/description/image presence. Wire it into main.py startup after create_preset_ingredients().

2. **Backend tests (DATA-01 verification)**: Create backend/tests/test_initial_data.py with pytest cases covering: 8-row creation, idempotency on re-call, admin link, category link, env guard (skipped in prod), missing-admin guard (no throw).

3. **Frontend tests (NAV-01..05 + UI-01 + BUG-06)**: Create frontend/tests/phase15-navigation.spec.js with Playwright tests covering: Header action-bar below main row, avatar menu has exactly 2 items + Divider, Sidebar footer has version text + 0 buttons, BottomBar tab counts per role (chef 7 / admin 7 / user 4), tab order (首页 first, 我的 last), no logout, UserHomePage chef sees 菜品管理 + 食材管理, user does not see them, OrderPage 高级筛选 opens Sheet.

Output: 4 files (2 backend source/test + 2 test files). No new packages, no new tokens.
</objective>

<execution_context>
@/home/temila/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/temila/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/15-navigation-restructure-test-data/15-CONTEXT.md
@.planning/phases/15-navigation-restructure-test-data/15-RESEARCH.md
@.planning/phases/15-navigation-restructure-test-data/15-PATTERNS.md
@.planning/phases/15-navigation-restructure-test-data/15-UI-SPEC.md
@backend/app/initial_data.py
@backend/app/main.py
@backend/tests/conftest.py
@backend/tests/test_guest.py
@frontend/playwright.config.js
@frontend/tests/phase12-bugfix.spec.js
@frontend/tests/md3-compliance.spec.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add create_seed_test_dishes() to backend/app/initial_data.py and wire into main.py</name>
  <files>
    backend/app/initial_data.py
    backend/app/main.py
  </files>
  <read_first>
    - backend/app/initial_data.py (full file - module imports at lines 1-10; create_initial_data pattern at lines 11-57; create_preset_categories at lines 60-171; create_preset_ingredients at lines 401-422 - the canonical seed function analog)
    - backend/app/main.py (lines 1-50 - existing startup import + 3 await calls at lines 235-245)
    - backend/app/models/dish.py (lines 1-75 - Dish fields: name, description, recipe, image_url, status, is_popular, is_semifinished, created_by; DishChef model at lines 65-75)
    - backend/app/models/category.py (Category.type field for region/cuisine classification)
    - backend/app/models/user.py (User model - admin user lookup)
    - .planning/phases/15-navigation-restructure-test-data/15-PATTERNS.md initial_data.py section
  </read_first>
  <action>
    In backend/app/initial_data.py:

    1. ADD module-level imports at the top (after existing imports at lines 1-10):
       `import os`
       `import random`
       Do NOT add Dish/DishCategory/Category imports here - keep them inside the function body (mirrors the existing create_preset_categories pattern at line 61).

    2. APPEND a new async function after create_preset_ingredients (after line 422):
       ```
       async def create_seed_test_dishes():
           """
           注入 8 道测试菜品（食谱×介绍×图片 的 2³ = 8 种组合）。
           仅在开发环境生效（通过 ENVIRONMENT=development 或 AUTO_SEED_DEMO_DISHES=1 触发）。
           production 默认跳过。幂等：检测已存在的 [1]..[8] 前缀名称后跳过。
           """
           # D-DATA01-01: 环境变量守卫 - production 默认不注入
           if not (
               os.environ.get("ENVIRONMENT") == "development"
               or os.environ.get("AUTO_SEED_DEMO_DISHES") == "1"
           ):
               return

           # D-DATA01-03: 固定种子 - 保证 dev 环境每次 8 道菜品状态一致
           rng = random.Random(42)

           from app.models.dish import Dish, DishCategory
           from app.models.category import Category
           from app.models.user import User
           from sqlalchemy import select

           async with async_session_factory() as session:
               # 找到默认 admin (D-DATA01-02)
               admin_res = await session.execute(
                   select(User).where(User.username == "admin")
               )
               admin = admin_res.scalar_one_or_none()
               if not admin:
                   print("⚠️  默认 admin 不存在，跳过 seed 测试菜品")
                   return

               # 找一个 region 分类作为默认分类 (D-DATA01-04)
               region_res = await session.execute(
                   select(Category).where(Category.type == "region")
               )
               regions = region_res.scalars().all()
               if not regions:
                   print("⚠️  region 分类不存在，跳过 seed 测试菜品")
                   return

               # D-DATA01-02: 8 个组合定义（recipe × description × image 的 2³ 矩阵）
               combinations = [
                   {"recipe": True,  "description": True,  "image": True},
                   {"recipe": True,  "description": True,  "image": False},
                   {"recipe": True,  "description": False, "image": True},
                   {"recipe": True,  "description": False, "image": False},
                   {"recipe": False, "description": True,  "image": True},
                   {"recipe": False, "description": True,  "image": False},
                   {"recipe": False, "description": False, "image": True},
                   {"recipe": False, "description": False, "image": False},
               ]

               # 幂等：检测已存在的 seed 行（按 name 前缀匹配）
               existing_res = await session.execute(
                   select(Dish.name).where(Dish.name.like("测试菜品 %"))
               )
               existing_names = {row[0] for row in existing_res.all()}
               if len(existing_names) >= 8:
                   print(f"✅ 测试菜品已存在 ({len(existing_names)} 道)，跳过 seed")
                   return

               flags = ["有食谱", "无食谱"]
               descs = ["有介绍", "无介绍"]
               imgs = ["有图", "无图"]

               for idx, combo in enumerate(combinations, start=1):
                   label = f"测试菜品 {idx} · {flags[0 if combo['recipe'] else 1]}{descs[0 if combo['description'] else 1]}{imgs[0 if combo['image'] else 1]}"
                   if label in existing_names:
                       continue  # 单条已存在则跳过（partial-failure 兼容）

                   dish = Dish(
                       name=label,
                       description=f"测试菜品 {idx} 的介绍文字" if combo["description"] else None,
                       recipe=f"# 食谱 {idx}\n食材：...\n步骤：..." if combo["recipe"] else None,
                       image_url="https://via.placeholder.com/400x300?text=Test+Dish+" + str(idx) if combo["image"] else None,
                       status=rng.choice(["published", "draft"]),
                       is_popular=rng.choice([True, False]),
                       is_semifinished=rng.choice([True, False]),
                       created_by=admin.id,
                   )
                   session.add(dish)
                   await session.flush()  # flush to get dish.id

                   # 关联随机一个 region 分类
                   cat = rng.choice(regions)
                   session.add(DishCategory(dish_id=dish.id, category_id=cat.id))

               await session.commit()
               print(f"✅ Seed 测试菜品注入完成（{len(combinations)} 道）")
       ```

    3. Do NOT modify create_initial_data, create_preset_categories, or create_preset_ingredients.

    In backend/app/main.py:

    1. UPDATE the existing import at lines ~15-20 from:
       `from app.initial_data import create_initial_data, create_preset_categories, create_preset_ingredients`
       TO:
       `from app.initial_data import create_initial_data, create_preset_categories, create_preset_ingredients, create_seed_test_dishes`

    2. ADD a new await call AFTER `await create_preset_ingredients()` at line ~241:
       `await create_seed_test_dishes()`

    3. Do NOT change any other code in main.py (keep init_db, os.makedirs, _log, smart_settings block all unchanged).
  </action>
  <acceptance_criteria>
    - backend/app/initial_data.py contains the function signature `async def create_seed_test_dishes():`
    - Function body contains the env-guard `if not (os.environ.get("ENVIRONMENT") == "development" or os.environ.get("AUTO_SEED_DEMO_DISHES") == "1"):`
    - Function body contains the 8-combinations list (8 dict entries with recipe/description/image keys)
    - Function body contains `rng = random.Random(42)` for fixed seed
    - Function body contains `name.like("测试菜品 %")` for idempotency check
    - Function body inserts Dish rows with fields: name, description, recipe, image_url, status, is_popular, is_semifinished, created_by
    - Function body inserts DishCategory rows linking each dish to a region Category
    - backend/app/main.py imports create_seed_test_dishes AND awaits it after create_preset_ingredients
    - Both files pass `python -m py_compile`
  </acceptance_criteria>
  <verify>
    <automated>python -m py_compile backend/app/initial_data.py && python -m py_compile backend/app/main.py && (grep -c 'async def create_seed_test_dishes' backend/app/initial_data.py | awk '{ if ($1 >= 1) print "function-defined PASS"; else { print "function-defined FAIL"; exit 1 } }') && (grep -c 'random.Random(42)' backend/app/initial_data.py | awk '{ if ($1 >= 1) print "fixed-seed PASS"; else { print "fixed-seed FAIL"; exit 1 } }') && (grep -c 'name.like."测试菜品 %".' backend/app/initial_data.py | awk '{ if ($1 >= 1) print "idempotency-check PASS"; else { print "idempotency-check FAIL"; exit 1 } }') && (grep -c 'create_seed_test_dishes' backend/app/main.py | awk '{ if ($1 >= 2) print "mainpy-wires PASS"; else { print "mainpy-wires FAIL"; exit 1 } }')</automated>
  </verify>
  <done>
    - create_seed_test_dishes() added to backend/app/initial_data.py with env guard + idempotency + 8 seed rows
    - main.py wires it into startup after create_preset_ingredients()
    - Both files syntactically valid
  </done>
</task>

<task type="auto">
  <name>Task 2: Create backend/tests/test_initial_data.py with seed verification cases</name>
  <files>backend/tests/test_initial_data.py</files>
  <read_first>
    - backend/tests/conftest.py (full file - db fixture at lines 116-121; clean_db autouse fixture at lines 107-113; test_session_factory at lines 24-28)
    - backend/tests/test_guest.py (lines 24-54 - admin_token / setup pattern)
    - .planning/phases/15-navigation-restructure-test-data/15-PATTERNS.md backend/tests/test_initial_data.py section
  </read_first>
  <action>
    CREATE backend/tests/test_initial_data.py with the following pytest cases:

    1. `test_seed_test_dishes_creates_eight_combinations`: monkeypatch ENVIRONMENT=development; call create_initial_data() + create_preset_categories() + create_seed_test_dishes(); query Dish WHERE name LIKE '测试菜品 %'; assert len == 8; assert set of (recipe is not None, description is not None, image_url is not None) tuples has length 8 (all 2^3 combinations covered).

    2. `test_seed_test_dishes_is_idempotent`: monkeypatch ENVIRONMENT=development; call create_initial_data + create_preset_categories + create_seed_test_dishes twice; assert still 8 dishes.

    3. `test_seed_test_dishes_attaches_to_admin`: monkeypatch ENVIRONMENT=development; seed; query admin User; assert every seed dish.created_by == admin.id.

    4. `test_seed_test_dishes_attaches_to_category`: monkeypatch ENVIRONMENT=development; seed; assert every seed dish has len(dish.categories) > 0.

    5. `test_seed_test_dishes_skipped_when_env_not_set`: monkeypatch.delenv ENVIRONMENT and AUTO_SEED_DEMO_DISHES; call create_initial_data + create_preset_categories + create_seed_test_dishes; assert 0 测试菜品 rows.

    6. `test_seed_test_dishes_skipped_without_admin`: create_preset_categories() (NOT create_initial_data so admin is absent); call create_seed_test_dishes; assert 0 测试菜品 rows (no exception thrown).

    Use autouse fixture to set ENVIRONMENT=development for all tests except test_seed_test_dishes_skipped_when_env_not_set (which deletes the env var).
  </action>
  <acceptance_criteria>
    - backend/tests/test_initial_data.py file exists
    - File contains 6 test functions covering: 8-row creation, idempotency, admin link, category link, env guard, missing-admin guard
    - Each test function is decorated with @pytest.mark.asyncio
    - File passes `python -m py_compile`
  </acceptance_criteria>
  <verify>
    <automated>python -m py_compile backend/tests/test_initial_data.py && (grep -c 'async def test_seed_test_dishes' backend/tests/test_initial_data.py | awk '{ if ($1 >= 6) print "test-count PASS (>=6)"; else { print "test-count FAIL"; exit 1 } }') && (grep -c '@pytest.mark.asyncio' backend/tests/test_initial_data.py | awk '{ if ($1 >= 6) print "asyncio-deco PASS"; else { print "asyncio-deco FAIL"; exit 1 } }')</automated>
  </verify>
  <done>
    - 6 pytest cases cover seed creation, idempotency, admin link, category link, env guard, missing-admin guard
    - File syntactically valid (py_compile passes)
  </done>
</task>

<task type="auto">
  <name>Task 3: Create frontend/tests/phase15-navigation.spec.js with NAV-01..05, UI-01, BUG-06 tests</name>
  <files>frontend/tests/phase15-navigation.spec.js</files>
  <read_first>
    - frontend/tests/phase12-bugfix.spec.js (full file - loginAs fixture pattern, page.goto + expect conventions)
    - frontend/playwright.config.js (test setup; dev server command)
    - frontend/src/auth/index.js (loginAs helper for tests)
    - .planning/phases/15-navigation-restructure-test-data/15-PATTERNS.md phase15-navigation.spec.js section
  </read_first>
  <action>
    CREATE frontend/tests/phase15-navigation.spec.js with the following test.describe blocks:

    1. `describe('Header restructure', ...)`: 
       - test 'actions prop renders below main row in .header-action-bar': loginAs admin; goto /admin/dishes; expect .header-action-bar visible; expect .md-header__theme-toggle visible in .md-header__right.
       - test 'avatar menu contains only Edit Profile + Divider + Logout': loginAs admin; goto /home; click .md-header__avatar; expect .md-header__menu-item count == 2; expect .md-divider visible; expect text '切换主题' count == 0.

    2. `describe('Sidebar footer', ...)`:
       - test 'shows version text and no buttons': loginAs admin; goto /admin; expect .md-sidebar__version visible; expect .md-sidebar__footer button count == 0.

    3. `describe('BottomBar role tabs', ...)`:
       - test 'chef: 7 tabs in order 首页 / 订单 / 菜品 / 食材 / 愿望 / 点菜 / 我的': loginAs chef; goto /home; assert .md-tab count == 7; assert first tab label is 首页; assert last tab label is 我的.
       - test 'admin: 7 tabs in order 后台 / 菜品 / 食材 / 愿望 / 用户 / 点菜 / 我的': loginAs admin; goto /admin; assert .md-tab count == 7; assert first tab label is 后台; assert last tab label is 我的.
       - test 'user: 4 tabs in order 首页 / 点菜 / 愿望 / 我的 (no 菜品/食材)': loginAs user; goto /home; assert .md-tab count == 4; assert no tab with label containing 菜品 or 食材.
       - test 'all roles: no logout tab': for each role, loginAs + goto /; assert .md-tab__label count for text 退出 == 0.

    4. `describe('UserHomePage quick actions', ...)`:
       - test 'chef sees 菜品管理 + 食材管理 quick actions on mobile viewport (420px)': setViewportSize 420x800; loginAs chef; goto /home; expect .quick-action:has-text('菜品管理') visible; expect .quick-action:has-text('食材管理') visible.
       - test 'user does NOT see 菜品管理 or 食材管理': setViewportSize 420x800; loginAs user; goto /home; expect .quick-action:has-text('菜品管理') count == 0; expect .quick-action:has-text('食材管理') count == 0.

    5. `describe('OrderPage filter Sheet', ...)`:
       - test 'clicking 高级筛选 opens Sheet': setViewportSize 420x800; loginAs user; goto /order; click button:has-text('高级筛选'); expect .md-modal--bottom-sheet visible; expect text 清空 visible; expect text 应用 visible.

    Import @playwright/test, use the loginAs fixture from existing test files (or replicate its body inline if not exported).

    Use test.beforeEach to set viewport and login per role as needed. Use test.afterEach to logout / clear storage.
  </action>
  <acceptance_criteria>
    - frontend/tests/phase15-navigation.spec.js file exists
    - File contains 5 describe blocks covering: Header restructure, Sidebar footer, BottomBar role tabs, UserHomePage quick actions, OrderPage filter Sheet
    - File contains at least 9 test cases total
    - File passes `node --check`
  </acceptance_criteria>
  <verify>
    <automated>node --check frontend/tests/phase15-navigation.spec.js && (grep -c 'test.describe' frontend/tests/phase15-navigation.spec.js | awk '{ if ($1 >= 5) print "describe-count PASS (>=5)"; else { print "describe-count FAIL"; exit 1 } }') && (grep -cE 'test\(' frontend/tests/phase15-navigation.spec.js | awk '{ if ($1 >= 9) print "test-count PASS (>=9)"; else { print "test-count FAIL"; exit 1 } }')</automated>
  </verify>
  <done>
    - 5 describe blocks cover Header / Sidebar / BottomBar / UserHomePage / OrderPage contracts
    - At least 9 test cases (2 + 1 + 4 + 2 + 1 = 10 minimum)
    - File syntactically valid (node --check passes)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- This plan introduces a new dev-only seed function that creates Dish + DishCategory records when ENVIRONMENT=development or AUTO_SEED_DEMO_DISHES=1. The seed code is gated by environment variables; production startups skip it entirely.
- The seed uses a fixed random seed (random.Random(42)) so dev environments get consistent fixture state for screenshot tests. The seed inserts deterministic data - no randomness that affects security.
- No new HTTP endpoints are introduced. The seed runs during startup, gated by env var.
- The new Playwright tests assert UI selectors - no security boundary changes.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-06-S | Spoofing | — | n/a | Seed uses real admin user lookup (User.username == 'admin'), no identity fabrication |
| T-15-06-T | Tampering | create_seed_test_dishes | accept | Environment guard + idempotency check; fixed RNG seed; only fires in dev with explicit env var |
| T-15-06-R | Repudiation | — | n/a | Seed runs once at startup; no per-request events |
| T-15-06-I | Information Disclosure | test_initial_data.py | accept | Tests use in-memory fixtures (per conftest.py); no production data |
| T-15-06-D | Denial of Service | create_seed_test_dishes | mitigate | Idempotency check (>= 8 existing names) prevents repeated inserts across restarts; partial-failure per-row skip |
| T-15-06-E | Elevation of Privilege | — | n/a | Seed assigns dishes to existing admin user only (no privilege escalation); admin user lookup uses existing User table |
| T-15-06-SC | Supply Chain | npm/pip/cargo installs | n/a | Plan 06 introduces zero new dependencies (uses existing pytest, playwright, sqlalchemy) |

Net: low-risk plan. The env guard is the primary mitigation - production deployments cannot accidentally seed test data because the function returns early when neither env var is set.
</threat_model>

<verification>
- backend/app/initial_data.py contains `async def create_seed_test_dishes():`
- create_seed_test_dishes guards on ENVIRONMENT=development OR AUTO_SEED_DEMO_DISHES=1
- create_seed_test_dishes is idempotent via name LIKE '测试菜品 %' check
- create_seed_test_dishes inserts 8 Dish + 8 DishCategory records
- backend/app/main.py awaits create_seed_test_dishes after create_preset_ingredients
- backend/tests/test_initial_data.py contains 6 pytest cases
- frontend/tests/phase15-navigation.spec.js contains 5 describe blocks with 9+ test cases
- All 4 files pass syntax validation
</verification>

<success_criteria>
- SC for DATA-01: 8 dev seed dishes (recipe × description × image combinations) inserted on startup when env guard active; production default skips
- SC for BUG-06 (test side): mobile AdminDishes cards can be visually verified against 8 fixture combinations
- SC for NAV-04 (test side): UserHomePage chef quick actions verified
- SC for NAV-05 (test side): BottomBar tab counts/orders per role verified
- SC for UI-01 (test side): OrderPage Sheet verified
- SC for NAV-01..03 (test side): Header action-bar + avatar menu + Sidebar footer verified
- All 4 files commit-ready
</success_criteria>

<output>
Create .planning/phases/15-navigation-restructure-test-data/15-06-SUMMARY.md when done
</output>