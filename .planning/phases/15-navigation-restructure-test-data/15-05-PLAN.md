---
phase: 15-navigation-restructure-test-data
plan: 05
type: execute
wave: 4
depends_on: []
files_modified:
  - frontend/src/pages/OrderPage.jsx
autonomous: true
requirements:
  - UI-01

must_haves:
  truths:
    - "OrderPage replaces inline '展开筛选 ▼' Chip with a tonal Button labeled '高级筛选'"
    - "Clicking 高级筛选 opens a Sheet containing the existing region / cuisine / filterType chip block"
    - "Sheet footer has 清空 (clears selectedRegion/Cuisine/Filters) + 应用 (closes Sheet) buttons"
    - "Sheet preserves immediate-apply semantics: chip clicks inside the Sheet still trigger loadDishes effect (existing behavior, no deferred-apply)"
    - "Sheet uses <Icon name='filter' /> icon (NOT 'tune' which is not in Icon.jsx mapping)"
    - "favoritesOnly chip + sortBy select remain inline next to the 高级筛选 trigger (independent top-level controls)"
    - "OrderPage filter state (selectedRegion, selectedCuisine, selectedFilters, favoritesOnly, sortBy, searchQuery) stays in OrderPage - Sheet is pure UI container"
    - "OrderPage chef picker Modal + order confirmation Modal still work (not migrated to Sheet)"
    - "File passes node --check"
  artifacts:
    - path: "frontend/src/pages/OrderPage.jsx"
      provides: "Tonal 高级筛选 trigger Button + Sheet wrapping the existing filter chip block"
      contains: "高级筛选"
      exports: ["default OrderPage"]
  key_links:
    - from: "OrderPage.jsx (trigger)"
      to: "Sheet composite"
      via: "tonal Button onClick={() => setShowFilters(true)} + Sheet onClose={() => setShowFilters(false)}"
      pattern: "import Sheet from"
    - from: "OrderPage.jsx (Sheet body)"
      to: "OrderPage.jsx state"
      via: "selectedRegion / selectedCuisine / selectedFilters setters called directly inside Sheet (no deferred-apply)"
      pattern: "setSelectedRegion|setSelectedCuisine|setSelectedFilters"
    - from: "OrderPage.jsx (Sheet footer)"
      to: "handleClearFilters"
      via: "清空 button onClick calls handleClearFilters which resets region/cuisine/filters (NOT favoritesOnly or sortBy)"
      pattern: "handleClearFilters"
---

<objective>
Migrate OrderPage's inline "展开筛选 ▼" Chip + filter expansion block to a tonal Button trigger + Sheet overlay. This is the only remaining UserRole browser "advanced filter" that needs Sheet migration (Phase 14 already completed AdminDishesPage and AdminIngredientsPage).

The Sheet follows the proven Phase 14 shape from AdminDishesPage.jsx:553-608 (tonal Button trigger + Sheet with title="高级筛选" + footer with 清空/应用 + children = existing chip block moved verbatim). Filter state stays in OrderPage (locked decision per UI-SPEC.md) — no state migration. The 应用 button is close/confirm only, NOT deferred-apply (chip clicks continue to trigger loadDishes immediately).

Output: 1 modified page file. No new components, no new dependencies.
</objective>

<execution_context>
@/home/temila/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/temila/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/15-navigation-restructure-test-data/15-CONTEXT.md
@.planning/phases/15-navigation-restructure-test-data/15-PATTERNS.md
@.planning/phases/15-navigation-restructure-test-data/15-UI-SPEC.md

# Source files to read first
@frontend/src/pages/OrderPage.jsx
@frontend/src/pages/AdminDishesPage.jsx
@frontend/src/components/composites/Sheet.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace OrderPage filter trigger with tonal Button and add Sheet import</name>
  <files>frontend/src/pages/OrderPage.jsx</files>
  <read_first>
    - frontend/src/pages/OrderPage.jsx (full file - existing imports at lines 1-19; filter state useState at lines 44-53; filter block at lines 364-441; 展开筛选 Chip at lines 336-342)
    - frontend/src/pages/AdminDishesPage.jsx (lines 553-608 - reference Sheet invocation shape)
    - frontend/src/components/composites/Sheet.jsx (full file - public API: open, onClose, title, footer, children)
    - frontend/src/components/primitives/Icon.jsx (verify 'filter' icon name maps to FilterList Material Symbol at line 32, 103; 'tune' is NOT in the mapping per PATTERNS.md Finding 1)
    - .planning/phases/15-navigation-restructure-test-data/15-PATTERNS.md OrderPage.jsx section
  </read_first>
  <action>
    In frontend/src/pages/OrderPage.jsx:

    1. ADD a new import after the existing Modal import (around line 18):
       `import Sheet from '../components/composites/Sheet';`

    2. LOCATE the existing inline 展开筛选 Chip element at lines ~336-342. REPLACE it with a tonal Button:
       ```
       <Button
         variant="tonal"
         size="sm"
         onClick={() => setShowFilters(true)}
         startIcon={<Icon name="filter" size={18} />}
       >
         高级筛选
       </Button>
       ```

       Do NOT add a `selected` prop; the trigger button is not a toggle. When `showFilters` is true, the Sheet overlay itself provides the visual cue.

       Use `name="filter"` (NOT `name="tune"`) per PATTERNS.md Finding 1 — `tune` is not in Icon.jsx mapping.

    3. Do NOT change anything else in this task. Task 2 wraps the existing filter chip block in a Sheet.
  </action>
  <acceptance_criteria>
    - OrderPage.jsx imports Sheet from '../components/composites/Sheet'
    - OrderPage.jsx contains `高级筛选` Button label (replacing 展开筛选 ▼)
    - OrderPage.jsx contains `<Icon name="filter" size={18} />` (NOT `<Icon name="tune"`)
    - OrderPage.jsx NO longer contains `展开筛选 ▼` or `收起筛选 ▲` strings
    - File passes node --check
  </acceptance_criteria>
  <verify>
    <automated>node --check frontend/src/pages/OrderPage.jsx && (grep -c "import Sheet from" frontend/src/pages/OrderPage.jsx | awk '{ if ($1 >= 1) print "sheet-import PASS"; else { print "sheet-import FAIL"; exit 1 } }') && (grep -c '高级筛选' frontend/src/pages/OrderPage.jsx | awk '{ if ($1 >= 1) print "button-label PASS"; else { print "button-label FAIL"; exit 1 } }') && (grep -c 'name="tune"' frontend/src/pages/OrderPage.jsx | awk '{ if ($1 == 0) print "tune-icon REMOVED (use filter)"; else { print "tune-icon PRESENT (regression)"; exit 1 } }') && (grep -c '展开筛选' frontend/src/pages/OrderPage.jsx | awk '{ if ($1 == 0) print "old-chip-label REMOVED"; else { print "old-chip-label STILL PRESENT"; exit 1 } }')</automated>
  </verify>
  <done>
    - OrderPage imports Sheet composite
    - 高级筛选 tonal Button (with filter icon) replaces 展开筛选 Chip
    - File syntactically valid
  </done>
</task>

<task type="auto">
  <name>Task 2: Wrap OrderPage filter chip block in Sheet with 清空/应用 footer</name>
  <files>frontend/src/pages/OrderPage.jsx</files>
  <read_first>
    - frontend/src/pages/OrderPage.jsx (existing filter block at lines 364-441 - region / filteredCuisines / filterTypes sections)
    - frontend/src/pages/AdminDishesPage.jsx (lines 553-608 - reference Sheet invocation shape)
    - .planning/phases/15-navigation-restructure-test-data/15-PATTERNS.md OrderPage.jsx section
  </read_first>
  <action>
    In frontend/src/pages/OrderPage.jsx:

    1. ADD a handleClearFilters function in the OrderPage component body (near other handlers like loadDishes):
       ```
       const handleClearFilters = () => {
         setSelectedRegion(null);
         setSelectedCuisine(null);
         setSelectedFilters({});
         // favoritesOnly and sortBy NOT reset (per UI-SPEC.md - they are top-level controls, not advanced filters)
       };
       ```

    2. LOCATE the existing inline filter block at lines ~364-441 (the region / cuisine / filterTypes chip sections that rendered when showFilters was true). WRAP this entire block in a Sheet:
       ```
       {showFilters && (
         <Sheet
           open
           onClose={() => setShowFilters(false)}
           title="高级筛选"
           footer={
             <div style={{ display: 'flex', gap: 'var(--md-spacing-2)', width: '100%' }}>
               <Button variant="tonal" className="flex-1" onClick={handleClearFilters}>清空</Button>
               <Button variant="filled" className="flex-1" onClick={() => setShowFilters(false)}>应用</Button>
             </div>
           }
         >
           {/* MOVED VERBATIM from lines 364-441 - the entire filter chip block */}
         </Sheet>
       )}
       ```

       The chip block content (region Chip row + filteredCuisines Chip row + filterTypes.map Chip row) is moved verbatim - do not change any chip logic, useState calls, or chip click handlers.

    3. DELETE the now-orphaned inline filter block at lines ~364-441 (its content has been moved into the Sheet above).

    4. KEEP untouched:
       - selectedRegion / selectedCuisine / selectedFilters / favoritesOnly / sortBy / searchQuery useState (lines 44-53)
       - loadDishes effect (lines 75-77) - chip clicks continue to trigger immediate API reload
       - favoritesOnly Chip + sortBy select (lines 343-358) - remain inline next to 高级筛选 Button
       - chef picker Modal + order confirmation Modal - unchanged

    5. Do NOT introduce deferred-apply semantics. The 应用 button is close/confirm only; the loadDishes effect already ran when chips were clicked.
  </action>
  <acceptance_criteria>
    - OrderPage.jsx contains handleClearFilters function
    - OrderPage.jsx contains a Sheet component with title="高级筛选" wrapping the filter chip block
    - Sheet footer contains 清空 button (calls handleClearFilters) + 应用 button (closes Sheet)
    - handleClearFilters resets selectedRegion, selectedCuisine, selectedFilters (NOT favoritesOnly or sortBy)
    - Inline filter block (lines ~364-441) is REMOVED from page top - chips live inside Sheet now
    - favoritesOnly Chip + sortBy select remain inline (NOT in Sheet)
    - chef picker Modal + order confirmation Modal still work (unchanged)
    - File passes node --check
  </acceptance_criteria>
  <verify>
    <automated>node --check frontend/src/pages/OrderPage.jsx && (grep -c 'handleClearFilters' frontend/src/pages/OrderPage.jsx | awk '{ if ($1 >= 2) print "handler DEFINED+USED PASS"; else { print "handler DEFINED+USED FAIL"; exit 1 } }') && (grep -c 'title="高级筛选"' frontend/src/pages/OrderPage.jsx | awk '{ if ($1 >= 1) print "sheet-title PASS"; else { print "sheet-title FAIL"; exit 1 } }') && (grep -c '清空' frontend/src/pages/OrderPage.jsx | awk '{ if ($1 >= 1) print "clear-button PASS"; else { print "clear-button FAIL"; exit 1 } }') && (grep -c '应用' frontend/src/pages/OrderPage.jsx | awk '{ if ($1 >= 1) print "apply-button PASS"; else { print "apply-button FAIL"; exit 1 } }')</automated>
  </verify>
  <done>
    - Sheet wraps existing filter chip block with 清空/应用 footer
    - handleClearFilters resets region/cuisine/filters only (preserves favoritesOnly + sortBy)
    - Inline filter block removed from page top
    - File syntactically valid
    - All other OrderPage state and Modals (chef picker, confirmation) preserved
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- This plan modifies only the filter UI presentation in OrderPage. No new HTTP endpoints, no new data shape, no auth/session interaction.
- The Sheet composite delegates to Modal (Phase 14) which already handles focus trap, ESC, scroll lock. No new injection vectors introduced.
- Filter state (selectedRegion / selectedCuisine / selectedFilters) continues to flow through existing useState → loadDishes effect → GET /api/dishes with status_filter.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-05-S | Spoofing | — | n/a | No identity surface in filter UI |
| T-15-05-T | Tampering | OrderPage.jsx JSX | accept | Static className strings; no DOM injection path |
| T-15-05-R | Repudiation | — | n/a | No audit-relevant events |
| T-15-05-I | Information Disclosure | OrderPage filter API params | accept | Filter values are integer IDs / enum strings; no PII |
| T-15-05-D | Denial of Service | — | n/a | No backend surface change |
| T-15-05-E | Elevation of Privilege | — | n/a | Filter UI is read-only; auth unchanged |
| T-15-05-SC | Supply Chain | npm/pip/cargo installs | n/a | Plan 05 introduces zero new dependencies (Sheet composite reused from Phase 14) |

Net: zero new threat surface. UI-01 is a pure UX refinement.
</threat_model>

<verification>
- frontend/src/pages/OrderPage.jsx imports Sheet from '../components/composites/Sheet'
- 高级筛选 tonal Button replaces 展开筛选 ▼ Chip
- Sheet wraps existing region / cuisine / filterTypes chip block
- Sheet footer has 清空 + 应用 buttons
- File passes node --check
- favoritesOnly Chip + sortBy select remain inline (not in Sheet)
- All other OrderPage Modals (chef picker, confirmation) unchanged
</verification>

<success_criteria>
- SC for UI-01: OrderPage 高级筛选 trigger opens Sheet containing the filter chips; 清空 resets region/cuisine/filters; 应用 closes; chip clicks continue to trigger immediate API reload
- File commit-ready
</success_criteria>

<output>
Create .planning/phases/15-navigation-restructure-test-data/15-05-SUMMARY.md when done
</output>