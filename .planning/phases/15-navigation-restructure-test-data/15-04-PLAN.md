---
phase: 15-navigation-restructure-test-data
plan: 04
type: execute
wave: 3
depends_on:
  - "15-02"
files_modified:
  - frontend/src/pages/AdminUsersPage.jsx
  - frontend/src/pages/AdminCategoriesPage.jsx
  - frontend/src/pages/OrderDetailPage.jsx
  - frontend/src/pages/DishDetailPage.jsx
  - frontend/src/components/composites/BottomBar.jsx
  - frontend/src/pages/UserHomePage.jsx
autonomous: true
requirements:
  - NAV-01
  - NAV-04
  - NAV-05

must_haves:
  truths:
    - "AdminUsersPage / AdminCategoriesPage / OrderDetailPage / DishDetailPage Header actions are wrapped in <div className=\"header-action-bar\">"
    - "BottomBar has 3 role-specific tab arrays: chef 7 tabs, admin 7 tabs, user 4 tabs"
    - "BottomBar chef array order: 首页 / 订单 / 菜品 / 食材 / 愿望 / 点菜 / 我的"
    - "BottomBar admin array order: 后台 / 菜品 / 食材 / 愿望 / 用户 / 点菜 / 我的"
    - "BottomBar user array order: 首页 / 点菜 / 愿望 / 我的"
    - "BottomBar has NO logout tab in any role array"
    - "BottomBar chef tab array preserves id 'chef-orders'"
    - "UserHomePage chef menuEntries contains 菜品管理 + 食材管理 entries (4 total)"
    - "UserHomePage user menuEntries still contains only 开始点菜 + 口味偏好 (2 entries)"
    - "All 6 modified files pass node --check"
  artifacts:
    - path: "frontend/src/pages/AdminUsersPage.jsx"
      provides: "Header actions wrapped in .header-action-bar"
      contains: "header-action-bar"
    - path: "frontend/src/pages/AdminCategoriesPage.jsx"
      provides: "Header actions wrapped in .header-action-bar"
      contains: "header-action-bar"
    - path: "frontend/src/pages/OrderDetailPage.jsx"
      provides: "Header back-button actions wrapped in .header-action-bar"
      contains: "header-action-bar"
    - path: "frontend/src/pages/DishDetailPage.jsx"
      provides: "Header favorite IconButton actions wrapped in .header-action-bar"
      contains: "header-action-bar"
    - path: "frontend/src/components/composites/BottomBar.jsx"
      provides: "3 role tab arrays per NAV-05 + no logout branch"
      contains: "user-home"
    - path: "frontend/src/pages/UserHomePage.jsx"
      provides: "chef menuEntries extended with 菜品管理 + 食材管理 entries"
      contains: "菜品管理"
  key_links:
    - from: "BottomBar.jsx"
      to: "App.jsx ProtectedRoute"
      via: "tab.path values match existing route role authorizations"
      pattern: "path: '/(home|chef/dishes|admin/dishes|admin/users|chef/orders|ingredients|admin/wishes|chef/wishes|order|profile|admin|my-wishes)'"
    - from: "UserHomePage.jsx"
      to: "App.jsx"
      via: "navigate calls match route definitions"
      pattern: "navigate\\('/(chef/dishes|admin/dishes|ingredients)'\\)"
---

<objective>
Complete three Phase 15 navigation refinements in one plan:

1. NAV-01 finish: Wrap the 4 remaining Header actions callsites with bare single Button or IconButton in div.header-action-bar.
2. NAV-05: Replace the 3 BottomBar role tab arrays with locked sequences (chef 7, admin 7, user 4). Remove logout branch.
3. NAV-04: Extend UserHomePage chef menuEntries with 菜品管理 + 食材管理.

Output: 6 modified files. No new components, no new dependencies.
</objective>

<execution_context>
@/home/temila/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/temila/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/15-navigation-restructure-test-data/15-CONTEXT.md
@.planning/phases/15-navigation-restructure-test-data/15-PATTERNS.md
@.planning/phases/15-navigation-restructure-test-data/15-UI-SPEC.md
@frontend/src/pages/AdminUsersPage.jsx
@frontend/src/pages/AdminCategoriesPage.jsx
@frontend/src/pages/OrderDetailPage.jsx
@frontend/src/pages/DishDetailPage.jsx
@frontend/src/components/composites/BottomBar.jsx
@frontend/src/pages/UserHomePage.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wrap 4 single-button Header callers in .header-action-bar</name>
  <files>frontend/src/pages/AdminUsersPage.jsx
frontend/src/pages/AdminCategoriesPage.jsx
frontend/src/pages/OrderDetailPage.jsx
frontend/src/pages/DishDetailPage.jsx</files>
  <read_first>
    - frontend/src/pages/AdminUsersPage.jsx (lines 130-140 - Header actions prop with bare Button; analog: AdminDishesPage.jsx ~line 524 from Phase 15 PATTERNS — see "AdminDishesPage.jsx (page, modify — wrap actions only)" section)
    - frontend/src/pages/AdminCategoriesPage.jsx (lines 110-120 - Header actions prop with bare Button; analog: AdminDishesPage.jsx wrap pattern)
    - frontend/src/pages/OrderDetailPage.jsx (lines 55-65 - Header actions prop with bare back Button; analog: single-Button Header caller — wrap with same div.header-action-bar)
    - frontend/src/pages/DishDetailPage.jsx (lines 125-145 - Header actions prop with bare favorite IconButton; analog: single-IconButton Header caller — wrap with same div.header-action-bar)
    - .planning/phases/15-navigation-restructure-test-data/15-PATTERNS.md Header action-bar wrapper section (closest analog: AdminDishesPage.jsx wrap pattern lines 524-536)
  </read_first>
  <action>
    For each of the 4 page files, locate the Header actions block and wrap the bare Button/IconButton in a div.header-action-bar container with inline style { display: flex, gap: var(--md-spacing-2) }.

    Targets:
    - AdminUsersPage.jsx ~line 135: bare Button + 添加
    - AdminCategoriesPage.jsx ~line 115: bare Button + 添加
    - OrderDetailPage.jsx ~line 60: bare back Button
    - DishDetailPage.jsx ~line 131: bare favorite IconButton

    The analog for all 4 is AdminDishesPage.jsx (Phase 15 Task 1 reference) — single wrapper div with className="header-action-bar" + inline style preserved.

    Do NOT modify any other code. Do NOT rename handlers.
  </action>
  <acceptance_criteria>
    - All 4 page files contain className=header-action-bar on actions wrapper div
    - All 4 files preserve original Button/IconButton and onClick handlers
    - All 4 files pass node --check
  </acceptance_criteria>
  <verify>
    <automated>node --check frontend/src/pages/AdminUsersPage.jsx && node --check frontend/src/pages/AdminCategoriesPage.jsx && node --check frontend/src/pages/OrderDetailPage.jsx && node --check frontend/src/pages/DishDetailPage.jsx && (grep -c 'className="header-action-bar"' frontend/src/pages/AdminUsersPage.jsx | awk '{ if ($1 >= 1) print "wrapper1 PASS"; else { print "wrapper1 FAIL"; exit 1 } }') && (grep -c 'className="header-action-bar"' frontend/src/pages/AdminCategoriesPage.jsx | awk '{ if ($1 >= 1) print "wrapper2 PASS"; else { print "wrapper2 FAIL"; exit 1 } }') && (grep -c 'className="header-action-bar"' frontend/src/pages/OrderDetailPage.jsx | awk '{ if ($1 >= 1) print "wrapper3 PASS"; else { print "wrapper3 FAIL"; exit 1 } }') && (grep -c 'className="header-action-bar"' frontend/src/pages/DishDetailPage.jsx | awk '{ if ($1 >= 1) print "wrapper4 PASS"; else { print "wrapper4 FAIL"; exit 1 } }')</automated>
  </verify>
  <done>
    - 4 single-button Header callers wrapped in .header-action-bar
    - All original handlers preserved
    - All 4 files syntactically valid
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace BottomBar role tab arrays and remove logout branch</name>
  <files>frontend/src/components/composites/BottomBar.jsx</files>
  <read_first>
    - frontend/src/components/composites/BottomBar.jsx (full file - current tab arrays at lines 30-58 with admin 7 tabs including logout at end; chef 5 tabs; user 4 tabs already; onClick logout branch at lines 75-77; chef-orders Badge at line 87 — analog: existing role-branch if/else-if/else at lines 33-58 is the canonical "role-branch tabs" pattern)
    - frontend/src/pages/UserHomePage.jsx (lines 27-34 - role-branch push pattern; analogs: BottomBar.jsx role-branch tabs + UserHomePage.jsx role-branch menuEntries both push role-specific items)
    - .planning/phases/15-navigation-restructure-test-data/15-PATTERNS.md "BottomBar.jsx (composite, request-response)" section
    - .planning/phases/15-navigation-restructure-test-data/15-PATTERNS.md "UserHomePage.jsx (page, modify — extend chef/admin quickActions)" section for the role-branch menuEntries pattern
  </read_first>
  <action>
    In frontend/src/components/composites/BottomBar.jsx:

    1. CHANGE `const { user, logout } = useAuth();` to `const { user } = useAuth();` (line ~25) - logout is no longer invoked from BottomBar.

    2. REPLACE the entire if/else-if/else block that builds `tabs` at lines ~30-58 with:

       Chef array (7 tabs, order: 首页 / 订单 / 菜品 / 食材 / 愿望 / 点菜 / 我的):
       - id: 'user-home', icon: 'home', label: '首页', path: '/home'
       - id: 'chef-orders', icon: 'chef', label: '订单', path: '/chef/orders'
       - id: 'chef-dishes', icon: 'set-meal', label: '菜品', path: '/chef/dishes'
       - id: 'admin-ingredients', icon: 'eco', label: '食材', path: '/ingredients'
       - id: 'chef-wishes', icon: 'lightbulb', label: '愿望', path: '/chef/wishes'
       - id: 'order-dish', icon: 'ramen-dining', label: '点菜', path: '/order'
       - id: 'user-profile', icon: 'person', label: '我的', path: '/profile'

       Admin array (7 tabs, order: 后台 / 菜品 / 食材 / 愿望 / 用户 / 点菜 / 我的):
       - id: 'admin-home', icon: 'dashboard', label: '后台', path: '/admin'
       - id: 'admin-dishes', icon: 'set-meal', label: '菜品', path: '/admin/dishes'
       - id: 'admin-ingredients', icon: 'eco', label: '食材', path: '/ingredients'
       - id: 'admin-wishes', icon: 'lightbulb', label: '愿望', path: '/admin/wishes'
       - id: 'admin-users', icon: 'group', label: '用户', path: '/admin/users'
       - id: 'order-dish', icon: 'ramen-dining', label: '点菜', path: '/order'
       - id: 'user-profile', icon: 'person', label: '我的', path: '/profile'

       User array (4 tabs, order: 首页 / 点菜 / 愿望 / 我的):
       - id: 'user-home', icon: 'home', label: '首页', path: '/home'
       - id: 'order-dish', icon: 'ramen-dining', label: '点菜', path: '/order'
       - id: 'user-wishes', icon: 'lightbulb', label: '愿望', path: '/my-wishes'
       - id: 'user-profile', icon: 'person', label: '我的', path: '/profile'

       IMPORTANT: Preserve id 'chef-orders' exactly so the Badge logic at line ~87 continues to work.

    3. REPLACE the entire onClick handler branch at lines ~73-77 (the if/else that checks `tab.action === 'logout'`) with:
       `onClick={() => navigate(tab.path)}`

    4. KEEP untouched: isActive() prefix matching (lines ~60-63), the chef-orders Badge rendering on chef tab only, the Ripple wrapper around tabs, the CSS file (BottomBar.css).
  </action>
  <acceptance_criteria>
    - BottomBar.jsx chef array has exactly 7 tabs in the order 首页 / 订单 / 菜品 / 食材 / 愿望 / 点菜 / 我的
    - BottomBar.jsx admin array has exactly 7 tabs in the order 后台 / 菜品 / 食材 / 愿望 / 用户 / 点菜 / 我的
    - BottomBar.jsx user array has exactly 4 tabs in the order 首页 / 点菜 / 愿望 / 我的
    - BottomBar.jsx contains id 'chef-orders' (Badge integration preserved)
    - BottomBar.jsx contains NO string 'logout' as a tab action
    - BottomBar.jsx onClick handler does NOT contain if (tab.action === 'logout') branch
    - File passes node --check
  </acceptance_criteria>
  <verify>
    <automated>node --check frontend/src/components/composites/BottomBar.jsx && (grep -c 'chef-orders' frontend/src/components/composites/BottomBar.jsx | awk '{ if ($1 >= 1) print "chef-orders-id KEPT"; else { print "chef-orders-id REMOVED (regression)"; exit 1 } }') && (grep -c "tab.action === 'logout'" frontend/src/components/composites/BottomBar.jsx | awk '{ if ($1 == 0) print "logout-branch REMOVED"; else { print "logout-branch STILL PRESENT"; exit 1 } }') && (grep -c 'label: '\''后台'\''' frontend/src/components/composites/BottomBar.jsx | awk '{ if ($1 >= 1) print "admin-backend-tab PASS"; else { print "admin-backend-tab FAIL"; exit 1 } }')</automated>
  </verify>
  <done>
    - BottomBar role tab arrays updated per NAV-05 (chef 7, admin 7, user 4)
    - Logout branch removed from onClick
    - chef-orders Badge preserved
    - File syntactically valid
  </done>
</task>

<task type="auto">
  <name>Task 3: Extend UserHomePage chef menuEntries with 菜品管理 + 食材管理 + Audit AdminHomePage.jsx for same entries</name>
  <files>frontend/src/pages/UserHomePage.jsx</files>
  <read_first>
    - frontend/src/pages/UserHomePage.jsx (full file - current menuEntries init at lines 12-25, role-branch push at lines 27-34 with chef/admin both getting 订单管理 entry)
    - frontend/src/pages/AdminHomePage.jsx (lines 61-104 - quickActions array already contains 菜品管理 + 食材管理 entries; NO code change needed - this is a VERIFICATION task)
    - frontend/src/components/composites/BottomBar.jsx (lines 30-58 - existing role-branch tabs pattern with admin/chef/user arrays)
    - .planning/phases/15-navigation-restructure-test-data/15-PATTERNS.md UserHomePage.jsx section
  </read_first>
  <action>
    In frontend/src/pages/UserHomePage.jsx:

    1. After the existing 订单管理 menuEntries.push block (chef/admin branch at lines ~30-33), ADD two more push calls for chef and admin:

       菜品管理 entry: icon 'set-meal', title '菜品管理', desc '管理菜品信息与食谱', onClick navigate('/chef/dishes') for chef OR '/admin/dishes' for admin (use a ternary on user.role).

       食材管理 entry: icon 'eco', title '食材管理', desc '管理食材与库存', onClick navigate('/ingredients') (same path for both roles).

    2. KEEP untouched: the initial 开始点菜 + 口味偏好 entries (lines 12-25), the grid container (line ~40), the Header invocation, the quick-action class.

    3. Resulting chef menuEntries: 4 entries (开始点菜, 口味偏好, 菜品管理, 食材管理) plus the 订单管理 entry makes 5 if 订单管理 is also pushed. Per CONTEXT D-NAV04-04, chef gets 4 entries (开始点菜 / 口味偏好 / 菜品管理 / 食材管理) which implies 订单管理 should NOT be in chef's UserHomePage. Re-read CONTEXT D-NAV04-04 carefully: chef 4 entries = 开始点菜 / 口味偏好 / 菜品管理 / 食材管理. So chef does NOT see 订单管理 on UserHomePage. The chef sees 订单 management via BottomBar. Remove chef from the existing if (user?.role === 'chef' || user?.role === 'admin') block — only admin should get 订单管理.

       Revised: change the existing `if (user?.role === 'chef' || user?.role === 'admin')` to `if (user?.role === 'admin')` for the 订单管理 entry. Then add a separate `if (user?.role === 'chef' || user?.role === 'admin')` block for 菜品管理 + 食材管理 entries.

       Final chef menuEntries: 开始点菜 / 口味偏好 / 菜品管理 / 食材管理 = 4 entries (matches CONTEXT D-NAV04-04).
       Final admin menuEntries: 开始点菜 / 口味偏好 / 订单管理 / 菜品管理 / 食材管理 = 5 entries (matches CONTEXT D-NAV04-04).
       Final user menuEntries: 开始点菜 / 口味偏好 = 2 entries (matches CONTEXT D-NAV04-04).

    4. AUDIT (no code change) — admin home contract verification:
       frontend/src/pages/AdminHomePage.jsx already contains both `菜品管理` (line 64, icon 'set-meal', navigate('/admin/dishes')) and `食材管理` (line 70, icon 'eco', navigate('/ingredients')) entries inside its `quickActions` array (lines 61-104). This satisfies the SC for admin mobile home criterion 4 (Phase 15 success criterion "Both chef AND admin mobile home pages expose 菜品管理 + 食材管理 entries").
       NO modification to AdminHomePage.jsx is required. The acceptance test for admin home goes into Plan 15-06 (frontend/tests/phase15-navigation.spec.js) and asserts via Playwright that on `/admin` route, `.quick-action:has-text('菜品管理')` and `.quick-action:has-text('食材管理')` are both visible at mobile viewport.
  </action>
  <acceptance_criteria>
    - UserHomePage.jsx chef branch menuEntries has 4 entries: 开始点菜 / 口味偏好 / 菜品管理 / 食材管理 (no 订单管理 for chef)
    - UserHomePage.jsx admin branch menuEntries has 5 entries: 开始点菜 / 口味偏好 / 订单管理 / 菜品管理 / 食材管理
    - UserHomePage.jsx user branch menuEntries unchanged at 2 entries
    - UserHomePage.jsx contains 菜品管理 title in menuEntries
    - UserHomePage.jsx contains 食材管理 title in menuEntries
    - File passes node --check
    - AUDIT (no code change): frontend/src/pages/AdminHomePage.jsx quickActions array (lines 61-104) ALREADY contains entries for `菜品管理` and `食材管理`; no modification required from this plan. Verification of these entries is delegated to frontend/tests/phase15-navigation.spec.js (Plan 15-06 Task 3).
  </acceptance_criteria>
  <verify>
    <automated>node --check frontend/src/pages/UserHomePage.jsx && (grep -c '菜品管理' frontend/src/pages/UserHomePage.jsx | awk '{ if ($1 >= 1) print "dish-mgmt-entry PASS"; else { print "dish-mgmt-entry FAIL"; exit 1 } }') && (grep -c '食材管理' frontend/src/pages/UserHomePage.jsx | awk '{ if ($1 >= 1) print "ingredient-mgmt-entry PASS"; else { print "ingredient-mgmt-entry FAIL"; exit 1 } }') && (grep -c "navigate('/ingredients')" frontend/src/pages/UserHomePage.jsx | awk '{ if ($1 >= 1) print "ingredient-route PASS"; else { print "ingredient-route FAIL"; exit 1 } }') && (grep -c '菜品管理' frontend/src/pages/AdminHomePage.jsx | awk '{ if ($1 >= 1) print "admin-home-has-dish-mgmt PASS (audit)"; else { print "admin-home-has-dish-mgmt FAIL (audit regression)"; exit 1 } }') && (grep -c '食材管理' frontend/src/pages/AdminHomePage.jsx | awk '{ if ($1 >= 1) print "admin-home-has-ingredient-mgmt PASS (audit)"; else { print "admin-home-has-ingredient-mgmt FAIL (audit regression)"; exit 1 } }')</automated>
  <done>
    - UserHomePage chef menuEntries contains 菜品管理 + 食材管理 (4 total)
    - UserHomePage user menuEntries unchanged (2 entries)
    - File syntactically valid
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- This plan modifies navigation rendering (BottomBar tabs) and Home page menu entries. No new HTTP endpoints, no new data shape.
- BottomBar tab paths are presentation only; App.jsx ProtectedRoute still enforces role checks. A user navigating to /admin/dishes via BottomBar would be blocked by ProtectedRoute if their role is not chef/admin.
- UserHomePage menuEntries use navigate() to existing route paths; no new routes introduced.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04-S | Spoofing | — | n/a | No identity surface in nav arrays |
| T-15-04-T | Tampering | BottomBar.jsx tab.path | accept | Static strings; no DOM injection path |
| T-15-04-R | Repudiation | — | n/a | No audit-relevant events |
| T-15-04-I | Information Disclosure | — | n/a | No data flow changes |
| T-15-04-D | Denial of Service | — | n/a | No backend surface |
| T-15-04-E | Elevation of Privilege | BottomBar.jsx (visible tabs) | accept | Tab visibility is presentation; ProtectedRoute in App.jsx remains the security boundary. A user who manually types /admin in URL still gets 403 from ProtectedRoute |
| T-15-04-SC | Supply Chain | npm/pip/cargo installs | n/a | Plan 04 introduces zero new dependencies |

Net: zero new threat surface. NAV-04/05 are UI refinements; auth and authorization boundaries unchanged.
</threat_model>

<verification>
- frontend/src/pages/AdminUsersPage.jsx contains className="header-action-bar"
- frontend/src/pages/AdminCategoriesPage.jsx contains className="header-action-bar"
- frontend/src/pages/OrderDetailPage.jsx contains className="header-action-bar"
- frontend/src/pages/DishDetailPage.jsx contains className="header-action-bar"
- frontend/src/components/composites/BottomBar.jsx has 3 role tab arrays per NAV-05, no logout branch, chef-orders Badge preserved
- frontend/src/pages/UserHomePage.jsx chef branch contains 菜品管理 + 食材管理; user branch unchanged
- All 6 files pass node --check
</verification>

<success_criteria>
  - SC for NAV-01 complete: All 7 Header actions callsites wrap their payload in .header-action-bar (this plan + 15-03)
  - SC for NAV-04 (chef side): chef sees 菜品管理 + 食材管理 on mobile home (UserHomePage.jsx modified)
  - SC for NAV-04 (admin side): admin already had 菜品管理 + 食材管理 on mobile home (AdminHomePage.jsx audited unchanged; runtime verification deferred to Plan 15-06 Phase 15 Playwright spec)
  - SC for NAV-05: BottomBar has chef 7 / admin 7 / user 4 tabs in correct order, no logout
  - All 6 files commit-ready
</success_criteria>

<output>
Create .planning/phases/15-navigation-restructure-test-data/15-04-SUMMARY.md when done
</output>