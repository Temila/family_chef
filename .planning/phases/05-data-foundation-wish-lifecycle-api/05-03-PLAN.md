---
phase: 05-data-foundation-wish-lifecycle-api
plan: 03
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - backend/app/routers/wishes.py
  - backend/app/main.py
  - backend/tests/conftest.py
  - backend/tests/test_wishes.py
autonomous: true
requirements:
  - WISH-01
  - WISH-02
  - FLOW-01
  - FLOW-02
  - FLOW-03
  - FLOW-04
  - FLOW-05
must_haves:
  truths:
    - "POST /api/wishes accepts a registered user's wish submission and returns 201 with the created wish"
    - "GET /api/wishes returns role-aware lists (admin all, chef queue+mine, user own) per D-05"
    - "GET /api/wishes/{id} returns 404 (not 403) for unauthorized reads per D-03"
    - "POST /api/wishes/{id}/claim is atomic — concurrent calls produce exactly one 200 + one 400 per D-01/D-02"
    - "POST /api/wishes/{id}/advance validates DishChef published state per D-09"
    - "POST /api/wishes/{id}/reject requires reject_reason per FLOW-04"
    - "PUT /api/wishes/{id} and DELETE /api/wishes/{id} only succeed for the submitter in non-terminal states per PERM-02/D-06/D-07"
    - "Wishes router is registered under /api/wishes in main.py"
  artifacts:
    - path: backend/app/routers/wishes.py
      provides: "FastAPI router with 7 endpoints for wish lifecycle"
      contains: "router = APIRouter()"
      min_lines: 150
    - path: backend/app/main.py
      provides: "Wishes router registration"
      contains: "wishes_router"
    - path: backend/tests/test_wishes.py
      provides: "Full endpoint coverage + 8 security tests"
      contains: "test_submit_wish"
      min_lines: 200
    - path: backend/tests/conftest.py
      provides: "wishes table added to clean_all_tables list + chef2_token/user2_token fixtures"
      contains: "wishes"
  key_links:
    - from: backend/app/routers/wishes.py
      to: backend/app/services/wish_service.py
      via: "wish_service singleton + WishPermissionError import"
      pattern: "from app\\.services\\.wish_service import wish_service, WishPermissionError"
    - from: backend/app/main.py
      to: backend/app/routers/wishes.py
      via: "app.include_router"
      pattern: "app\\.include_router\\(wishes_router\\.router"
    - from: backend/tests/test_wishes.py
      to: backend/tests/conftest.py
      via: "client + token fixtures"
      pattern: "user_token|chef_token|admin_token"
---

<objective>
Expose the WishService as a complete REST API under `/api/wishes` with 7 endpoints, register the router in `main.py`, and write comprehensive tests covering happy paths + the 8 security test cases enumerated in 05-RESEARCH.md § Security Domain.

Purpose: This plan delivers the user-facing HTTP contract that Phase 7 (frontend) consumes. Every WISH-01/02 and FLOW-01..05 requirement is realized as an HTTP endpoint here. The 8 security tests close the loop on PERM-* enforcement and D-01/D-03/D-04 disambiguation.

Output: 2 new files (`routers/wishes.py`, `tests/test_wishes.py`), 2 edits (`main.py` registration, `conftest.py` test isolation + extra fixtures).
</objective>

<execution_context>
@/home/temila/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/temila/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-data-foundation-wish-lifecycle-api/05-CONTEXT.md
@.planning/phases/05-data-foundation-wish-lifecycle-api/05-RESEARCH.md
@.planning/phases/05-data-foundation-wish-lifecycle-api/05-PATTERNS.md
@.planning/phases/05-data-foundation-wish-lifecycle-api/05-01-SUMMARY.md
@.planning/phases/05-data-foundation-wish-lifecycle-api/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create wishes router + register in main.py + extend conftest</name>
  <files>backend/app/routers/wishes.py, backend/app/main.py, backend/tests/conftest.py</files>
  <read_first>
    - backend/app/routers/orders.py (PRIMARY analog — full CRUD pattern, dependency injection, error conversion, commit placement; lines 91-110, 121-127, 201-260)
    - backend/app/routers/guest.py (NEWER analog — `Depends(require_role(...))` style + flatten response pattern lines 60-77)
    - backend/app/routers/auth.py (lines 123-132 — require_role factory signature)
    - backend/app/services/wish_service.py (created in Plan 02 — confirm `wish_service` and `WishPermissionError` exports)
    - backend/app/schemas/wish.py (created in Plan 01 — confirm WishCreate/Update/Advance/Reject/Response/ListResponse/DetailResponse exports)
    - backend/app/schemas/common.py (PageResponse generic — confirm import path and generic syntax)
    - backend/app/utils/pagination.py (PaginationParams — confirm constructor signature `PaginationParams(page=..., page_size=...)` and `.offset` / `.limit` properties)
    - backend/app/main.py (lines 262-292 — current router registration block; insert wishes after guest per coding style)
    - backend/tests/conftest.py (full file — confirm clean_all_tables table list + admin_token/user_token/chef_token fixtures; identify insertion point for wishes table + chef2_token/user2_token fixtures)
    - .planning/phases/05-data-foundation-wish-lifecycle-api/05-RESEARCH.md (§ "Router Template" lines 766-943 — full skeleton with all 7 endpoints)
  </read_first>
  <behavior>
    - 7 endpoints exist with exact paths: POST "", GET "", GET "/{wish_id}", PUT "/{wish_id}", DELETE "/{wish_id}", POST "/{wish_id}/claim", POST "/{wish_id}/advance", POST "/{wish_id}/reject"
    - Mutate endpoints (claim/advance/reject) use Depends(require_role("chef", "admin"))
    - Read/submit/update/cancel use Depends(get_current_user_from_token) — any authenticated user
    - Every except block catches WishPermissionError BEFORE generic ValueError (Python first-match wins)
    - Every mutate route ends with await db.commit() + await db.refresh(wish) before serialization
    - List/detail endpoints inject submitter_name and claimed_by_chef_name via the guest.py flatten pattern
    - Router registered in main.py under prefix /api/wishes
    - conftest.py adds wishes to clean_all_tables list (test isolation)
  </behavior>
  <action>
**Create `backend/app/routers/wishes.py`** with module docstring `"""家味 · Family Chef - 愿望单路由"""`.

**Imports** (per 05-PATTERNS.md § wishes.py lines 304-323 and 05-RESEARCH.md § Router Template lines 766-786):
```python
from typing import Optional
from fastapi import APIRouter, Depends, HTTPException, status, Query
from sqlalchemy.ext.asyncio import AsyncSession

from app.database import get_db
from app.routers.auth import get_current_user_from_token, require_role
from app.models.user import User
from app.services.wish_service import wish_service, WishPermissionError
from app.schemas.wish import (
    WishCreate, WishUpdate, WishAdvance, WishReject,
    WishResponse, WishListResponse, WishDetailResponse,
)
from app.schemas.common import PageResponse
from app.utils.pagination import PaginationParams

router = APIRouter()
```

**Endpoints (exact path + handler signature per 05-RESEARCH.md § Router Template lines 788-943):**

1. `@router.post("", status_code=status.HTTP_201_CREATED, response_model=WishResponse)` `async def submit_wish(wish_data: WishCreate, current_user: User = Depends(get_current_user_from_token), db: AsyncSession = Depends(get_db))` — calls `wish_service.submit_wish(db, current_user, wish_data)`, then `await db.commit()`, `await db.refresh(wish)`, `return WishResponse.model_validate(wish)`.

2. `@router.get("", response_model=PageResponse[WishListResponse])` `async def list_wishes(page: int = Query(1, ge=1), page_size: int = Query(20, ge=1, le=100), status: Optional[str] = Query(None, description="状态筛选"), claimed_by_chef_id: Optional[int] = Query(None, description="认领厨师筛选 (admin)"), mine: bool = Query(False, description="仅看我认领的 (FLOW-05)"), current_user: User = Depends(get_current_user_from_token), db: AsyncSession = Depends(get_db))` — builds `PaginationParams(page=page, page_size=page_size)`, calls `wish_service.list_wishes(db, params, current_user, status=status, claimed_by_chef_id=claimed_by_chef_id, mine=mine)`, applies the **flatten pattern from guest.py lines 66-77**: for each wish, `WishListResponse.model_validate(w)`, then set `item.submitter_name = w.submitter.display_name if w.submitter else None` and `item.claimed_by_chef_name = w.claimer.display_name if w.claimer else None`. Returns `PageResponse[WishListResponse](total=total, page=page, page_size=page_size, items=items)`.

3. `@router.get("/{wish_id}", response_model=WishDetailResponse)` `async def get_wish(wish_id: int, current_user: User = Depends(get_current_user_from_token), db: AsyncSession = Depends(get_db))` — calls `wish = await wish_service.get_wish_by_id(db, wish_id, current_user)`. If `not wish: raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="愿望不存在")` (D-03 — service returns None for both not-found and no-permission). Build `resp = WishDetailResponse.model_validate(wish)`, set `resp.submitter_name`, `resp.claimed_by_chef_name`. Return resp. **DO NOT call db.commit()** — read-only endpoint.

4. `@router.put("/{wish_id}", response_model=WishResponse)` `async def update_wish(wish_id: int, update_data: WishUpdate, current_user: User = Depends(get_current_user_from_token), db: AsyncSession = Depends(get_db))` — error conversion block:
```python
try:
    wish = await wish_service.update_wish(db, wish_id, current_user, update_data)
except WishPermissionError as e:                              # MUST come before ValueError
    raise HTTPException(status_code=status.HTTP_403_FORBIDDEN, detail=str(e))
except ValueError as e:
    raise HTTPException(status_code=status.HTTP_400_BAD_REQUEST, detail=str(e))
if not wish:
    raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="愿望不存在")
await db.commit()
await db.refresh(wish)
return WishResponse.model_validate(wish)
```

5. `@router.delete("/{wish_id}", response_model=WishResponse)` `async def cancel_wish(wish_id: int, current_user: User = Depends(get_current_user_from_token), db: AsyncSession = Depends(get_db))` — same error conversion pattern as update. Returns `WishResponse.model_validate(wish)` (status will be `"已撤销"` per D-07).

6. `@router.post("/{wish_id}/claim", response_model=WishResponse)` `async def claim_wish(wish_id: int, current_user: User = Depends(require_role("chef", "admin")), db: AsyncSession = Depends(get_db))` — error conversion:
```python
try:
    wish = await wish_service.claim_wish(db, wish_id, current_user)
except ValueError as e:                                       # D-02 race → 400
    raise HTTPException(status_code=status.HTTP_400_BAD_REQUEST, detail=str(e))
if not wish:                                                   # D-03 not-found → 404
    raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="愿望不存在")
await db.commit()
await db.refresh(wish)
return WishResponse.model_validate(wish)
```
Note: no `WishPermissionError` catch here — claim_wish doesn't raise it (role is gated by `require_role` which raises 403 itself if user lacks chef/admin role).

7. `@router.post("/{wish_id}/advance", response_model=WishResponse)` `async def advance_wish(wish_id: int, advance_data: WishAdvance, current_user: User = Depends(require_role("chef", "admin")), db: AsyncSession = Depends(get_db))` — same try/except structure as update_wish: WishPermissionError → 403, ValueError → 400, None → 404.

8. `@router.post("/{wish_id}/reject", response_model=WishResponse)` `async def reject_wish(wish_id: int, reject_data: WishReject, current_user: User = Depends(require_role("chef", "admin")), db: AsyncSession = Depends(get_db))` — same try/except structure. `WishReject` validation (reject_reason required) happens at FastAPI level — empty/missing reject_reason returns 422 before service is called.

**Edit `backend/app/main.py`** — add wishes to the router import block (line 262-277) and registration block (line 279-292):
- Insert `wishes,` into the `from app.routers import (...)` tuple (alphabetical placement: after `users,` or as a new entry — match the existing style).
- Insert `app.include_router(wishes.router, prefix="/api/wishes", tags=["愿望单"])` after the `guest.router` line (line 283) to group with other v1.1 features, OR at the end of the include_router block. Match existing alphabetical/order convention.

**Edit `backend/tests/conftest.py`** — two changes for test isolation and concurrent-claim testing:
1. In `clean_all_tables()` function (lines 61-102), add `"wishes",` to the `tables = [...]` list — place it BEFORE `"orders"` (wishes FKs reference users and dishes, so deletion order matters: wishes first, then orders, then the rest). The `except Exception: pass` catch-all means even if the table doesn't exist yet, the call is safe.
2. In `setup_database()` fixture (line 39), add `from app.models.wish import Wish` to the model imports list (line 46-55) — this ensures `Base.metadata.create_all` picks up the Wish table for the in-memory test DB.
3. Add two new fixtures `chef2_token` and `user2_token` modeled EXACTLY on the existing `chef_token`/`user_token` fixtures (lines 176-225) but with distinct usernames/passwords/display_names. Required for the concurrent-claim test (two different chefs) and the unauthorized-read test (two different regular users). Use credentials:
   - `chef2_token`: username `"chef2"`, password `"chef123"`, display_name `"厨师二"`, role `"chef"`, is_active=True, force_pwd_change=False.
   - `user2_token`: username `"testuser2"`, password `"user123"`, display_name `"测试用户二"`, role `"user"`, is_active=True, force_pwd_change=False.

**CRITICAL — error-conversion block ordering:** `except WishPermissionError` MUST appear BEFORE `except ValueError` in EVERY endpoint that calls a service method capable of raising WishPermissionError. Python's first-match-wins means a generic `except ValueError` would intercept the subclass and incorrectly return 400 instead of 403. Affected endpoints: update_wish, cancel_wish, advance_wish, reject_wish. (submit_wish, list_wishes, get_wish, claim_wish do not raise WishPermissionError but the order convention is harmless if applied uniformly.)
  </action>
  <verify>
    <automated>cd backend && uv run python -c "
from app.routers.wishes import router
# All 7+ endpoints registered
paths = {(r.methods.pop() if hasattr(r, 'methods') and r.methods else 'X'): r.path for r in router.routes}
# Verify key endpoints exist
expected = [('POST',''), ('GET',''), ('GET','/{wish_id}'), ('PUT','/{wish_id}'), ('DELETE','/{wish_id}'), ('POST','/{wish_id}/claim'), ('POST','/{wish_id}/advance'), ('POST','/{wish_id}/reject')]
for method, path in expected:
    found = any(method in (r.methods or set()) and r.path == f'/api/wishes{path}' or r.path == path for r in router.routes)
    if not found:
        # Try with prefix not applied yet (router-level path)
        found = any(method in (r.methods or set()) and r.path == path for r in router.routes)
    assert found, f'missing {method} {path}'
print('router endpoints OK')
" && uv run python -c "
# Verify main.py has wishes registered
import ast
with open('app/main.py') as f: tree = ast.parse(f.read())
src = open('app/main.py').read()
assert 'wishes' in src, 'wishes not referenced in main.py'
assert 'app.include_router(wishes.router' in src or 'wishes_router.router' in src, 'wishes router not registered'
print('main.py registration OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/routers/wishes.py` contains `router = APIRouter()` and 7 endpoint decorators (`@router.post("")`, `@router.get("")`, `@router.get(\"/{wish_id}\")`, `@router.put(\"/{wish_id}\")`, `@router.delete(\"/{wish_id}\")`, `@router.post(\"/{wish_id}/claim\")`, `@router.post(\"/{wish_id}/advance\")`, `@router.post(\"/{wish_id}/reject\")`)
    - File imports both `wish_service` and `WishPermissionError` from `app.services.wish_service`
    - Mutate endpoints (claim/advance/reject) use `Depends(require_role(\"chef\", \"admin\"))` for `current_user`
    - Read endpoints (list/get) and submit/update/cancel use `Depends(get_current_user_from_token)`
    - `update_wish`, `cancel_wish`, `advance_wish`, `reject_wish` handlers each contain `except WishPermissionError as e:` BEFORE `except ValueError as e:` (D-04 — first-match-wins)
    - Every mutate handler contains `await db.commit()` AFTER service returns and BEFORE response build (Pitfall 6)
    - `list_wishes` handler applies the flatten pattern: contains `item.submitter_name =` and `item.claimed_by_chef_name =` assignments (guest.py style)
    - `get_wish` handler raises `HTTPException(status_code=status.HTTP_404_NOT_FOUND` when service returns None (D-03)
    - `backend/app/main.py` contains `wishes` in the routers import tuple AND `app.include_router(wishes.router, prefix=\"/api/wishes\"`
    - `backend/tests/conftest.py` contains `\"wishes\",` in the clean_all_tables list AND `from app.models.wish import Wish` in setup_database
    - `backend/tests/conftest.py` defines `chef2_token` and `user2_token` async fixtures (each `User(...)` instance with `force_pwd_change=False`)
    - `cd backend && uv run python -c "from app.main import app; routes = [r.path for r in app.routes]; assert any('/api/wishes' in r for r in routes)"` exits 0 (router mounted)
  </acceptance_criteria>
  <done>
    wishes.py router with 7 endpoints live; main.py registers it under /api/wishes; conftest.py extended with wishes cleanup + chef2/user2 fixtures. All permission boundaries (WishPermissionError→403, ValueError→400, None→404, require_role→403 for vertical escalation) wired correctly.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Happy-path tests — submit/list/get/update/cancel + claim/advance/reject basics</name>
  <files>backend/tests/test_wishes.py</files>
  <read_first>
    - backend/tests/test_orders.py (PRIMARY analog — pytest-asyncio pattern, AsyncClient usage, token-based auth headers, assertion style)
    - backend/tests/conftest.py (created in Task 1 — confirm `client`, `admin_token`, `user_token`, `chef_token`, `chef2_token`, `user2_token` fixtures all available)
    - backend/app/routers/wishes.py (created in Task 1 — exact endpoint paths + request body shapes)
    - backend/app/schemas/wish.py (created in Plan 01 — field names for assertions)
    - .planning/phases/05-data-foundation-wish-lifecycle-api/05-RESEARCH.md (§ "Test File Skeleton" lines 951-1005)
  </read_first>
  <behavior>
    - test_submit_wish: regular user POST /api/wishes with dish_name → 201 + status="待处理"
    - test_list_own_wishes: user sees only their own wishes
    - test_list_as_admin: admin sees all wishes
    - test_list_as_chef_queue: chef sees 待处理 + own-claimed; does NOT see other chef's claimed wishes
    - test_list_mine_filter: chef with mine=True sees only own claimed
    - test_get_wish_detail: submitter retrieves detail with submitter_name flattened
    - test_update_wish: submitter edits dish_name during 待处理 → 200 + updated field
    - test_update_wish_during_zhunbeizhong: submitter can edit during 准备中 (D-06)
    - test_update_wish_blocked_in_terminal_state: submitter cannot edit 已上架/已拒绝/已撤销 → 400
    - test_cancel_wish: submitter DELETE soft-deletes to status="已撤销" (D-07)
    - test_claim_wish: chef claims 待处理 wish → status="准备中" + claimed_by_chef_id set
    - test_advance_wish: claimer advances with related_dish_id of own published dish → status="已上架"
    - test_reject_wish: claimer rejects with reject_reason → status="已拒绝" + reject_reason stored
    - test_reject_requires_reason: POST reject without body or with empty reason → 422
  </behavior>
  <action>
Create `backend/tests/test_wishes.py` with module docstring `"""家味 · Family Chef - 愿望单模块测试"""`.

**Imports:**
```python
import pytest
from httpx import AsyncClient
```

**Helper fixture** — `user_wish` async fixture that creates a wish as the regular user and returns its id:
```python
@pytest.fixture
async def user_wish(client: AsyncClient, user_token: str) -> int:
    response = await client.post(
        "/api/wishes",
        headers={"Authorization": f"Bearer {user_token}"},
        json={"dish_name": "宫保鸡丁", "note": "微辣"},
    )
    assert response.status_code == 201
    return response.json()["id"]
```

**Additional fixture** — `published_dish_for_chef` that creates a Dish + a published DishChef row for `chef_token`'s user, returns the dish_id. Needed for advance_wish tests (D-09 requires published DishChef). Inspect `backend/app/models/dish.py` and `backend/tests/test_dishes.py` (or test_orders.py if it creates dishes) for the exact creation pattern; the dish must have `Dish.status='enabled'` (admin-level) and a `DishChef` row with `chef_id=<chef's user_id>` and `status='published'`. To get the chef's user_id from `chef_token`, either: (a) call `GET /api/auth/me` after login and parse the response, or (b) re-create the chef user inside the test using the test_session_factory and capture the id before login. Prefer (a) for simplicity — call `client.get("/api/auth/me", headers=...)` and read `response.json()["id"]`. If `GET /api/auth/me` does not exist, fall back to creating the dish via admin token and publishing it via the chef (look at how test_orders.py or test_dishes.py creates the chef-published dish).

**Tests to write** (one `@pytest.mark.asyncio` per test — pytest-asyncio mode=auto means the marker is implicit but include it for clarity):

1. `test_submit_wish(client, user_token)` — POST `/api/wishes` with `{"dish_name": "麻婆豆腐", "reference_url": "https://example.com/recipe"}`. Assert `status_code == 201`, `data["status"] == "待处理"`, `data["dish_name"] == "麻婆豆腐"`, `data["reference_url"] == "https://example.com/recipe"`, `data["claimed_by_chef_id"] is None`.

2. `test_submit_wish_validates_dish_name(client, user_token)` — POST with `{"dish_name": ""}` → assert `status_code == 422` (Pydantic validation).

3. `test_list_own_wishes(client, user_token, admin_token)` — user submits 2 wishes; admin submits 1. GET `/api/wishes` as user → assert `total == 2` and all items have `user_id` matching the user. GET as admin → assert `total == 3`.

4. `test_list_chef_visibility(client, user_token, chef_token, chef2_token)` — user submits wish W1; chef claims W1; user submits W2. GET `/api/wishes` as chef → assert chef sees W1 (own claim) + W2 (待处理 queue); does NOT see any other chef's claimed wish. To verify the cross-chef invisibility: have chef2 claim W2, then GET as chef1 → assert total is just 1 (W1 only, since W2 is now claimed by chef2 and no longer 待处理). This validates D-05.

5. `test_list_mine_filter(client, user_token, chef_token)` — user submits 2 wishes; chef claims one of them. GET `/api/wishes?mine=true` as chef → assert `total == 1` and that item is the claimed one (FLOW-05).

6. `test_get_wish_detail(client, user_token)` — submit a wish, then GET `/api/wishes/{id}` as same user. Assert `status_code == 200`, `data["submitter_name"]` is present (not None — should be `"测试用户"` per conftest user_token fixture).

7. `test_update_wish(client, user_token, user_wish)` — PUT `/api/wishes/{user_wish}` with `{"dish_name": "宫保鸡丁改", "note": "更辣"}`. Assert `status_code == 200`, `data["dish_name"] == "宫保鸡丁改"`, `data["note"] == "更辣"`.

8. `test_update_wish_during_preparing(client, user_token, chef_token, user_wish)` — chef claims user_wish (status → 准备中); user then PUTs `/api/wishes/{user_wish}` with new note → assert `status_code == 200` (D-06 allows edit during 准备中).

9. `test_update_wish_blocked_when_published(client, user_token, chef_token, user_wish, published_dish_for_chef)` — chef claims + advances to 已上架; user PUTs `/api/wishes/{user_wish}` → assert `status_code == 400` (D-06 lock).

10. `test_cancel_wish_soft_delete(client, user_token, user_wish)` — DELETE `/api/wishes/{user_wish}` as user. Assert `status_code == 200`, `data["status"] == "已撤销"` (D-07 soft delete — row still exists).

11. `test_claim_wish(client, user_token, chef_token, user_wish)` — POST `/api/wishes/{user_wish}/claim` as chef. Assert `status_code == 200`, `data["status"] == "准备中"`, `data["claimed_by_chef_id"]` matches chef's user id (fetch via `GET /api/auth/me` or compare to chef's known id).

12. `test_advance_wish(client, user_token, chef_token, user_wish, published_dish_for_chef)` — chef claims, then POST `/api/wishes/{user_wish}/advance` with `{"related_dish_id": <dish_id>}`. Assert `status_code == 200`, `data["status"] == "已上架"`, `data["related_dish_id"] == <dish_id>` (D-10 stores the link; never auto-clears).

13. `test_reject_wish(client, user_token, chef_token, user_wish)` — chef claims, then POST `/api/wishes/{user_wish}/reject` with `{"reject_reason": "食材季节不对"}`. Assert `status_code == 200`, `data["status"] == "已拒绝"`, `data["reject_reason"] == "食材季节不对"`.

14. `test_reject_wish_requires_reason(client, user_token, chef_token, user_wish)` — chef claims, then POST `/api/wishes/{user_wish}/reject` with `{}` (no reject_reason) → assert `status_code == 422` (Pydantic validation, FLOW-04). Also test empty string: `{"reject_reason": ""}` → 422 (min_length=1).

**All tests use** `headers={"Authorization": f"Bearer {token}"}` for authenticated requests. Use the `client` fixture (which already overrides `get_db` to use the test session). Clean state between tests is guaranteed by the `clean_db` autouse fixture in conftest.py (extended in Task 1 to include the wishes table).

**Run command at end of task:** `cd backend && uv run pytest tests/test_wishes.py -v` — all tests must pass.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_wishes.py -v --tb=short 2>&1 | tail -50</automated>
  </verify>
  <acceptance_criteria>
    - `backend/tests/test_wishes.py` contains at least 14 test functions matching the behavior list above
    - `cd backend && uv run pytest tests/test_wishes.py -v` exits 0 with ALL tests passing
    - The test file imports `pytest` and `AsyncClient`
    - At least one test asserts `status_code == 201` for submit
    - At least one test asserts `data["status"] == "待处理"` (initial state on submit — D-11)
    - At least one test asserts `data["status"] == "已撤销"` after DELETE (D-07 soft delete)
    - At least one test asserts `status_code == 422` for missing reject_reason (FLOW-04)
    - At least one test verifies chef visibility (D-05): chef sees 待处理 + own claims, NOT other chef's claims
    - At least one test verifies `mine=True` returns only chef's claimed wishes (FLOW-05)
    - At least one test asserts edit works during 准备中 (D-06) and is blocked during 已上架 (D-06 lock)
    - At least one test asserts advance succeeds with a published dish (D-09 happy path) and stores `related_dish_id` (D-10)
    - All assertions use exact status codes (200/201/400/422) — no `assert response.ok` loose checks
  </acceptance_criteria>
  <done>
    test_wishes.py covers happy paths for all 7 endpoints: submit (WISH-01), list (WISH-02, FLOW-01, FLOW-05, D-05), detail, update (D-06), cancel (D-07), claim (FLOW-02), advance (FLOW-03, D-09, D-10), reject (FLOW-04). All 14+ tests pass via `uv run pytest tests/test_wishes.py -v`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Security tests — 8 STRIDE-mapped cases from RESEARCH § Security Domain</name>
  <files>backend/tests/test_wishes.py</files>
  <read_first>
    - backend/tests/test_wishes.py (the file created in Task 2 — APPEND security tests to it, do not rewrite)
    - backend/tests/conftest.py (confirm chef2_token and user2_token fixtures available from Task 1)
    - .planning/phases/05-data-foundation-wish-lifecycle-api/05-RESEARCH.md (§ Security Domain lines 1073-1113 — the 8 enumerated security tests)
    - .planning/phases/05-data-foundation-wish-lifecycle-api/05-CONTEXT.md (D-03 404-not-403 rationale, D-04 403 with claimer name)
  </read_first>
  <behavior>
    - All 8 RESEARCH-enumerated security tests pass:
      1. Regular user cannot POST /api/wishes/{id}/claim → 403 (vertical escalation blocked by require_role)
      2. User A reads user B's wish by id → 404 NOT 403 (D-03 ID enumeration prevention)
      3. Chef A advances chef B's claimed wish → 403 with chef B's name in detail (D-04, PERM-03)
      4. Two concurrent claims on same wish → exactly one 200 + one 400 (D-01 atomic, D-02 race failure)
      5. Advance with related_dish_id of unpublished/non-owned dish → 400 (D-09)
      6. Reject without reject_reason → 422 (Pydantic)
      7. Submit with dish_name="" → 422 (Pydantic)
      8. Mass-assignment: POST /api/wishes with {"dish_name":"X","status":"已上架"} → 201 + status="待处理" (extra field dropped)
  </behavior>
  <action>
**Append** the following 8 security tests to `backend/tests/test_wishes.py` (the file from Task 2). Do NOT modify existing tests.

**Test 1 — `test_regular_user_cannot_claim(client, user_token, user_wish)`:** Verify vertical escalation blocked at router. POST `/api/wishes/{user_wish}/claim` as regular user (`Bearer {user_token}`). Assert `status_code == 403` (the `require_role("chef", "admin")` dependency raises 403 before service is called). Security: STRIDE Elevation of Privilege.

**Test 2 — `test_unauthorized_read_returns_404_not_403(client, user_token, user2_token)`:** Verify D-03 (no ID enumeration). User A (`user_token`) submits a wish; capture its id. User B (`user2_token`) GETs `/api/wishes/{id}`. Assert `status_code == 404` (NOT 403). Security: STRIDE Information Disclosure. **This test is critical** — it confirms the D-03 design choice that returns 404 for both not-found and no-permission.

**Test 3 — `test_chef_cannot_advance_other_chef_claim(client, user_token, chef_token, chef2_token, user_wish)`:** Verify D-04 horizontal escalation block. chef_token claims `user_wish` (status → 准备中, claimed_by=chef1). chef2_token then POSTs `/api/wishes/{user_wish}/advance` with a valid `related_dish_id` (use chef2's own published dish if available, or any int — the permission check runs BEFORE D-09 validation so dish_id doesn't matter here). Assert `status_code == 403` AND `response.json()["detail"]` contains the chef1's display_name `"厨师"` (the claimer name appears in the error message per D-04). Security: STRIDE Elevation of Privilege + the claimer-name-in-error contract.

**Test 4 — `test_concurrent_claim_only_one_wins(client, user_token, chef_token, chef2_token, user_wish)`:** Verify D-01 atomic UPDATE + D-02 race failure. Use `asyncio.gather` to fire two POST `/api/wishes/{user_wish}/claim` requests in parallel — one as chef_token, one as chef2_token. Assert that exactly ONE response has `status_code == 200` and the other has `status_code == 400` (D-02 — race failure returns 400 with Chinese message, NOT 409 Conflict). Assert the wish's final `claimed_by_chef_id` matches the winning chef only (not the loser). Security: STRIDE Tampering. **Implementation note:** the in-memory SQLite test DB shares a single connection (`check_same_thread=False`), so true parallelism is serialized at the DB layer — but the atomic UPDATE still guarantees correctness because the second UPDATE's WHERE clause `status='待处理'` no longer matches after the first commits. Use `asyncio.gather(client.post(...), client.post(...))` and inspect both responses. If both somehow return 200, the test fails (atomic UPDATE is broken).

**Test 5 — `test_advance_rejects_unpublished_dish(client, user_token, chef_token, user_wish)`:** Verify D-09 business rule. chef_token claims `user_wish`. Then POST `/api/wishes/{user_wish}/advance` with `{"related_dish_id": 999999}` (a dish_id that does not exist or has no published DishChef row for this chef). Assert `status_code == 400` AND `response.json()["detail"] == "你未发布此菜品或菜品不可用"`. Security: STRIDE Tampering (business rule bypass).

**Test 6 — `test_reject_without_reason_returns_422(client, user_token, chef_token, user_wish)`:** Verify FLOW-04. Chef claims; POST `/api/wishes/{user_wish}/reject` with `{}` body. Assert `status_code == 422` (FastAPI Pydantic validation — `WishReject` requires `reject_reason: str = Field(..., min_length=1)`). Also test empty string: `{"reject_reason": "   "}` — service strips and rejects with 400 (defensive check). Security: STRIDE Repudiation.

**Test 7 — `test_submit_empty_dish_name_returns_422(client, user_token)`:** Verify V5 Input Validation. POST `/api/wishes` with `{"dish_name": ""}`. Assert `status_code == 422`. Security: input validation (V5 ASVS).

**Test 8 — `test_mass_assignment_status_ignored(client, user_token)`:** Verify mass-assignment protection. POST `/api/wishes` with `{"dish_name": "鱼香肉丝", "status": "已上架", "claimed_by_chef_id": 999}`. Assert `status_code == 201` AND `data["status"] == "待处理"` (NOT "已上架" — the extra `status` field was dropped by Pydantic because `WishCreate` doesn't declare it) AND `data["claimed_by_chef_id"] is None` (NOT 999). Security: STRIDE Tampering (mass assignment).

**Run command at end of task:** `cd backend && uv run pytest tests/test_wishes.py -v` — all tests (happy path from Task 2 + these 8 security tests) must pass.

**Optional but recommended:** Run the entire backend test suite to confirm no regressions: `cd backend && uv run pytest -x` — must exit 0.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_wishes.py -v -k "cannot_claim or returns_404 or cannot_advance or concurrent_claim or rejects_unpublished or without_reason or empty_dish_name or mass_assignment" 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `backend/tests/test_wishes.py` contains all 8 security test functions matching the names above
    - `cd backend && uv run pytest tests/test_wishes.py -v` exits 0 with ALL tests passing (Task 2 happy paths + Task 3 security)
    - `test_unauthorized_read_returns_404_not_403` asserts `status_code == 404` (NOT 403) — D-03 ID enumeration prevention
    - `test_concurrent_claim_only_one_wins` asserts exactly one 200 + one 400 (D-01 atomic + D-02 race failure); test FAILS if both return 200
    - `test_chef_cannot_advance_other_chef_claim` asserts `status_code == 403` AND `detail` contains the claimer's display_name `"厨师"` (D-04 contract: claimer name in error)
    - `test_advance_rejects_unpublished_dish` asserts `status_code == 400` AND `detail == "你未发布此菜品或菜品不可用"` (exact Chinese message per D-09)
    - `test_mass_assignment_status_ignored` asserts `data["status"] == "待处理"` (NOT "已上架") AND `data["claimed_by_chef_id"] is None` — extra fields dropped
    - `cd backend && uv run pytest -x` exits 0 (no regressions across the whole suite)
  </acceptance_criteria>
  <done>
    All 8 RESEARCH-enumerated security tests in test_wishes.py pass. The full test_wishes.py suite (happy paths + security) exits 0. The entire backend test suite (`uv run pytest -x`) exits 0 with no regressions. Every STRIDE threat in Plan 02's threat model has a passing test proving the mitigation works.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → Router | Untrusted requests cross here — body validation, role gating, error conversion all at router |
| Router → Service | Router enforces role at HTTP level (require_role); service re-validates ownership at logic level |
| Test client → App | Tests prove mitigations work end-to-end through the real ASGI stack |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-14 | Elevation of Privilege | Vertical: regular user calls `/claim` | mitigate | Router uses `Depends(require_role("chef","admin"))` on claim/advance/reject endpoints. `require_role` raises HTTPException(403) before service runs. Verified by `test_regular_user_cannot_claim` → 403. |
| T-5-15 | Elevation of Privilege | Horizontal: chef A advances chef B's wish | mitigate | Service-level ownership check raises `WishPermissionError(f"该愿望已被厨师 {name} 认领")` → router catches first → 403 with claimer name. Verified by `test_chef_cannot_advance_other_chef_claim`. |
| T-5-16 | Information Disclosure | Wish ID enumeration via 403 vs 404 distinction | mitigate | D-03: `get_wish_by_id` returns None for both not-found and no-permission; router returns 404 in both cases. Verified by `test_unauthorized_read_returns_404_not_403`. |
| T-5-17 | Tampering | Concurrent claim race (DATA-08) | mitigate | D-01 atomic conditional UPDATE + `result.rowcount==0` disambiguation. Exactly one chef wins; the other receives 400 per D-02. Verified by `test_concurrent_claim_only_one_wins`. |
| T-5-18 | Tampering | Advance with unpublished dish (D-09 bypass) | mitigate | Service validates `DishChef.status == 'published'` for the claiming chef + dish_id. Failure raises `ValueError("你未发布此菜品或菜品不可用")` → 400. Verified by `test_advance_rejects_unpublished_dish`. |
| T-5-19 | Repudiation | Reject without reason (FLOW-04) | mitigate | `WishReject` Pydantic schema: `reject_reason: str = Field(..., min_length=1)`. FastAPI returns 422 on missing/empty reason before service runs. Verified by `test_reject_without_reason_returns_422`. |
| T-5-20 | Tampering | Mass assignment via WishCreate extra fields | mitigate | `WishCreate` declares only `dish_name/reference_url/note`. Pydantic v2 silently drops unknown fields. Verified by `test_mass_assignment_status_ignored` — POST with `status="已上架"` returns `status="待处理"`. |
| T-5-21 | Tampering | SQL injection via query params or path params | mitigate | SQLAlchemy 2.0 parameterized queries throughout — never string-interpolate user input. Inherited from codebase convention (no new SQL constructors in this phase). |
| T-5-22 | Information Disclosure | Error messages leak internals (stack traces, SQL fragments, internal IDs) | mitigate | All error details are hardcoded Chinese strings (`"愿望不存在"`, `"你未发布此菜品或菜品不可用"`, `"该愿望已被厨师 X 认领"`). No `str(exception)` from SQLAlchemy or internal modules reaches the client. FastAPI's default 500 handler is the only leak vector — not in scope for this phase (no unhandled exceptions expected). |
| T-5-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages introduced in Plan 03 — pure reuse of existing FastAPI/pytest/httpx stack. No legitimacy checkpoint needed. |
</threat_model>

<verification>
**Phase 5 final acceptance gate:**
- `cd backend && uv run pytest tests/test_wishes.py -v` exits 0 — all happy-path + security tests pass
- `cd backend && uv run pytest -x` exits 0 — full backend suite, no regressions
- `cd backend && uv run python -c "from app.main import app; assert any('/api/wishes' in str(r.path) for r in app.routes)"` — router mounted
- `cd backend && curl -s -o /dev/null -w "%{http_code}" http://localhost:8000/api/health` returns 200 (smoke check after app boot)
- Manual smoke (optional): start the backend, log in as testuser, POST a wish, GET the list — confirm JSON shape matches WishResponse
</verification>

<success_criteria>
- All 7 wish endpoints reachable at `/api/wishes` with correct status codes for happy paths:
  - POST "" → 201 (WISH-01)
  - GET "" → 200 with role-aware visibility (WISH-02, FLOW-01, FLOW-05, D-05)
  - GET "/{id}" → 200 or 404 (PERM-01, D-03)
  - PUT "/{id}" → 200/400/403/404 (PERM-02, D-06)
  - DELETE "/{id}" → 200 with status="已撤销" (WISH-04, D-07)
  - POST "/{id}/claim" → 200/400/403/404 (FLOW-02, D-01, D-02)
  - POST "/{id}/advance" → 200/400/403/404 (FLOW-03, D-09, D-10, D-12)
  - POST "/{id}/reject" → 200/400/403/404/422 (FLOW-04)
- Router registered in main.py under `/api/wishes`
- 8 STRIDE-mapped security tests all pass — every Plan 02 threat has end-to-end proof
- conftest.py extended for test isolation (wishes table cleanup + chef2/user2 fixtures)
- No regressions in existing v1.0 tests
- Phase 5 ready for Phase 6 (notifications hook points in place) and Phase 7 (frontend can consume the API)
</success_criteria>

<output>
Create `.planning/phases/05-data-foundation-wish-lifecycle-api/05-03-SUMMARY.md` when done
</output>
