---
phase: 16
plan: 16-01
title: "Quick fixes: TD-01 sign-off, TD-03 CORS verify, TD-04 app.url, TD-05 migration batch, TD-07 deep-link encoding"
status: pending
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/archived/v1.3/14-ui-bugfix-filter-popup/14-VERIFICATION.md
  - config.yaml
  - config.example.yaml
  - backend/app/config.py
  - backend/tests/test_datetime_utils.py
  - backend/alembic/env.py
  - backend/alembic/versions/8a258d50ee87_phase_6_add_system_logs_indexes.py
  - frontend/src/App.jsx
autonomous: false
requirements:
  - TD-01
  - TD-03
  - TD-04
  - TD-05
  - TD-07

must_haves:
  truths:
    - "14-VERIFICATION.md frontmatter overrides[0] has non-empty accepted_by and accepted_at (operator signed the TD-01 override) OR plan stopped with explicit operator rejection"
    - "config.yaml and config.example.yaml both contain `url: \"https://family-chef.app\"` under the app: section"
    - "backend/app/config.py CORS_ORIGINS fallback default is no longer [\"*\"] (hardened to localhost dev origins)"
    - "backend/alembic/env.py has render_as_batch=True in BOTH context.configure calls (offline + online)"
    - "Migration 8a258d50ee87 wraps uq_user_ingredient_pref constraint ALTER in op.batch_alter_table (upgrade AND downgrade)"
    - "alembic upgrade head from a fresh SQLite DB succeeds (verified with backup/restore of dev DB)"
    - "frontend/src/App.jsx WishDeepLinkRedirect uses encodeURIComponent(id) in the Navigate target"
    - "frontend/src/App.jsx still passes `npx eslint` with 0 errors (file was lint-clean before this change)"
  artifacts:
    - path: ".planning/archived/v1.3/14-ui-bugfix-filter-popup/14-VERIFICATION.md"
      provides: "Signed TD-01 override record (accepted_by/accepted_at filled)"
      contains: "accepted_by"
    - path: "config.yaml"
      provides: "Explicit app.url for Feishu deep links"
      contains: "url: \"https://family-chef.app\""
    - path: "config.example.yaml"
      provides: "Config parity for new deployments"
      contains: "url: \"https://family-chef.app\""
    - path: "backend/app/config.py"
      provides: "Hardened CORS fallback default"
      contains: "cors.get(\"origins\""
    - path: "backend/alembic/env.py"
      provides: "render_as_batch=True in both configure sites"
      contains: "render_as_batch=True"
    - path: "backend/alembic/versions/8a258d50ee87_phase_6_add_system_logs_indexes.py"
      provides: "Batch-mode unique constraint creation (SQLite-compatible)"
      contains: "batch_alter_table"
    - path: "frontend/src/App.jsx"
      provides: "Encoded wish deep-link redirect"
      contains: "encodeURIComponent(id)"
  key_links:
    - from: "alembic/env.py"
      to: "migration 8a258d50ee87"
      via: "render_as_batch affects autogenerate; batch_alter_table in the migration script is the runtime fix — both required (research-verified)"
      pattern: "batch_alter_table"
    - from: "config.yaml app.url"
      to: "backend/app/config.py APP_URL getter"
      via: "app.get(\"url\", \"https://family-chef.app\") reads the new key; feishu.py deep links consume settings.APP_URL"
      pattern: "APP_URL"
---

<objective>
Close 5 accumulated tech-debt items with the smallest safe diffs: the TD-01 operator sign-off (docs-only, human gate), TD-03 CORS verification + fallback hardening, TD-04 explicit `app.url` in both YAML configs, TD-05 SQLite batch-mode migration repair (the empirically-verified fix: `op.batch_alter_table` in migration `8a258d50ee87` + `render_as_batch=True` in `env.py`), and TD-07 one-line `encodeURIComponent(id)` in WishDeepLinkRedirect.

Purpose: These are the "quick" items — each is a config/docs/one-line change that unblocks the phase's larger remediation plans (TD-06 startup migrations depend on TD-05; the lint baseline is preserved by keeping App.jsx clean).
Output: 8 files modified, 1 signed verification record.
</objective>

<execution_context>
@/home/temila/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/temila/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16-tech-debt-cleanup/16-RESEARCH.md
@.planning/phases/16-tech-debt-cleanup/16-PATTERNS.md
@.planning/archived/v1.3/14-ui-bugfix-filter-popup/14-VERIFICATION.md
@backend/app/config.py
@backend/alembic/env.py
@backend/alembic/versions/8a258d50ee87_phase_6_add_system_logs_indexes.py
@config.yaml
@config.example.yaml
@frontend/src/App.jsx
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: TD-01 — Present the pre-drafted ::before override for operator sign-off</name>
  <files>.planning/archived/v1.3/14-ui-bugfix-filter-popup/14-VERIFICATION.md</files>
  <what-built>
    Nothing code-wise. The override record already exists in the archived 14-VERIFICATION.md frontmatter (lines 19-28): SC2's literal "th 有 ::before 占位符" was replaced by `th:first-child padding-left` (baseline 12px + `.pc-data-table--with-leading` modifier 56px on the 4 avatar pages) per Phase 14-05. Functional alignment is verified across all 7 pages; only the literal `::before` wording remains unmet. The `accepted_by` / `accepted_at` fields are empty, awaiting the operator's decision.
  </what-built>
  <how-to-verify>
    1. Read the `overrides:` block in `.planning/archived/v1.3/14-ui-bugfix-filter-popup/14-VERIFICATION.md` frontmatter (the pre-drafted suggestion with empty `accepted_by`/`accepted_at`).
    2. Confirm the ground-truth evidence independently: `grep -c 'pc-data-table--with-leading' frontend/src/pages/AdminDishesPage.jsx frontend/src/pages/ChefDishesPage.jsx frontend/src/pages/AdminUsersPage.jsx frontend/src/pages/AdminChefsPage.jsx` (expect 1 each) and `grep -c 'pc-data-table th::before' frontend/src/css/styles.css` (expect 0 — the literal pseudo-element is deliberately gone).
    3. Decide: ACCEPT the override (the padding-left substitute satisfies the intent) or REJECT it (a real `th::before` implementation is required).
  </how-to-verify>
  <resume-signal>Type "accepted" (planner fills `accepted_by` + `accepted_at`, clears the pending wording, and the task closes), or describe the rejection / required alternative CSS work.</resume-signal>
  <action>
    Present the pre-drafted overrides block (14-VERIFICATION.md frontmatter lines 19-28) to the operator exactly as written, together with the ground-truth evidence from how-to-verify step 2. No code changes are made in this task — it exists solely to obtain the operator's sign-off (accept) or rejection description. Do NOT fill `accepted_by`/`accepted_at` yourself; wait for the operator's decision.
  </action>
  <verify>
    <human-check>Operator decision received via resume-signal: "accepted" (or a rejection description). The ground-truth greps from how-to-verify step 2 must have been run and shared before asking.</human-check>
  </verify>
  <done>
    Operator has decided. On accept: `accepted_by` + `accepted_at` filled and pending wording cleared in the archived 14-VERIFICATION.md. On reject: the operator's required alternative CSS work is described and routed to a follow-up task.
  </done>
</task>

<task type="auto">
  <name>Task 2: TD-04 app.url in both YAML configs + TD-03 CORS verification & fallback hardening</name>
  <files>
    config.yaml
    config.example.yaml
    backend/app/config.py
    backend/tests/test_datetime_utils.py
  </files>
  <read_first>
    - config.yaml (app: section at lines 8-12 — insert url: after version: line 10, same indentation style)
    - config.example.yaml (app: section at lines 8-12 — same insert; keep secret_key: CHANGE-ME placeholder untouched)
    - backend/app/config.py (Settings class lines 29-58 — CORS_ORIGINS fallback at line 58; APP_URL getter at line 38 already reads app.url)
    - backend/tests/test_datetime_utils.py (Settings test section lines 38-55 — the established home for Settings behavior tests; mirror the test_app_url_* shape)
  </read_first>
  <action>
    Per RESEARCH.md TD-04 (zero-code task, config only) and TD-03 (verify + optional hardening):

    1. In config.yaml AND config.example.yaml, ADD `url: "https://family-chef.app"` inside the `app:` section, directly after the `version:` line, with a Chinese inline comment `# 飞书深链基址（部署时改为实际公网地址）`. Do NOT change any other key. Do NOT touch `secret_key` values (config.yaml keeps the real dev secret; example keeps its CHANGE-ME placeholder).

    2. In backend/app/config.py line 58, HARDEN the CORS fallback default: change `cors.get("origins", ["*"])` to `cors.get("origins", ["http://localhost:5173", "http://localhost:3000"])` — matching the explicit origins already in config.yaml. Rationale: `["*"]` combined with `allow_credentials=True` is an invalid Starlette combination (assertion crash) and an insecure default for deployments missing the `cors:` section. Add a Chinese inline comment explaining the fallback mirrors the dev config. Do NOT touch `main.py` CORS middleware (it already consumes `settings.CORS_ORIGINS` — verified no `["*"]` leak in code paths).

    3. In backend/tests/test_datetime_utils.py, ADD one test in the Settings section: `test_cors_origins_defaults_to_dev_origins` — `s = Settings({})` then assert `s.CORS_ORIGINS == ["http://localhost:5173", "http://localhost:3000"]` (locks the hardened fallback). Do NOT add a test asserting `["*"]`.
  </action>
  <verify>
    <automated>cd backend && .venv/bin/python -c "import yaml; d=yaml.safe_load(open('../config.yaml')); e=yaml.safe_load(open('../config.example.yaml')); assert d['app']['url']=='https://family-chef.app', 'config.yaml missing url'; assert e['app']['url']=='https://family-chef.app', 'example missing url'; print('YAML-PASS')" && .venv/bin/pytest tests/test_datetime_utils.py -q 2>&1 | tail -2 && (grep -c '"origins", \["\*"\]' app/config.py | awk '{if ($1==0) print "CORS-FALLBACK-PASS"; else exit 1}')</automated>
  </verify>
  <done>
    - Both YAML files expose `app.url`; example keeps parity with real config
    - CORS fallback default is dev origins, never `["*"]`
    - Settings CORS test passes; no other test regressions in test_datetime_utils.py
  </done>
</task>

<task type="auto">
  <name>Task 3: TD-05 — Convert migration 8a258d50ee87 to batch mode + render_as_batch in env.py</name>
  <files>
    backend/alembic/versions/8a258d50ee87_phase_6_add_system_logs_indexes.py
    backend/alembic/env.py
  </files>
  <read_first>
    - backend/alembic/versions/8a258d50ee87_phase_6_add_system_logs_indexes.py (full file — upgrade() line 21-28, downgrade() line 31-37)
    - backend/alembic/env.py (full file — offline configure at lines 24-35, online do_run_migrations at lines 38-42)
    - 16-RESEARCH.md "TD-05: Batch-mode constraint fix (verified working)" code example (lines 194-204)
  </read_first>
  <action>
    Per RESEARCH.md TD-05 (the mis-diagnosis correction — empirically verified in-session):

    1. In 8a258d50ee87_phase_6_add_system_logs_indexes.py, REPLACE the raw constraint calls with batch mode:
       - upgrade(): replace line 27 `op.create_unique_constraint('uq_user_ingredient_pref', 'taste_preferences', ['user_id', 'ingredient_id', 'preference_type'])` with:
         `with op.batch_alter_table('taste_preferences', schema=None) as batch_op:` followed by `batch_op.create_unique_constraint('uq_user_ingredient_pref', ['user_id', 'ingredient_id', 'preference_type'])` (indented under the with block).
       - downgrade(): replace line 34 `op.drop_constraint('uq_user_ingredient_pref', 'taste_preferences', type_='unique')` with the mirrored `with op.batch_alter_table('taste_preferences', schema=None) as batch_op:` + `batch_op.drop_constraint('uq_user_ingredient_pref', type_='unique')`.
       - Leave the three create_index / drop_index calls untouched. Do NOT regenerate or rewrite any other migration — touching more increases regression surface for already-applied DBs.

    2. In backend/alembic/env.py, ADD `render_as_batch=True,` to BOTH `context.configure(...)` calls: the offline one (lines 27-32, after `dialect_opts={...}`) and the online one in do_run_migrations (line 39, after `target_metadata=target_metadata`). Add a Chinese comment on the online site: `# SQLite batch 模式：autogenerate 将 alter 渲染为表重建`. This flag only affects future autogenerate output — the migration script edit above is the actual runtime repair (research-verified).

    3. VERIFY from-base upgrade with backup/restore (never destroy the dev DB):
       - `cd backend && cp data/family_chef.db /tmp/opencode/family_chef.db.bak` (if the file exists; if not, skip the backup line)
       - `rm -f data/family_chef.db && uv run alembic upgrade head` — MUST complete with no NotImplementedError
       - `cp /tmp/opencode/family_chef.db.bak data/family_chef.db` (restore the original dev DB — final step, always run)
  </action>
  <verify>
    <automated>cd backend && python -m py_compile alembic/versions/8a258d50ee87_phase_6_add_system_logs_indexes.py && python -m py_compile alembic/env.py && (grep -c 'with op.batch_alter_table' alembic/versions/8a258d50ee87_phase_6_add_system_logs_indexes.py | awk '{if ($1 >= 2) print "BATCH-PASS"; else exit 1}') && (grep -c 'render_as_batch=True' alembic/env.py | awk '{if ($1 >= 2) print "RENDER-AS-BATCH-PASS (both sites)"; else exit 1}')</automated>
  </verify>
  <done>
    - Fresh-DB `alembic upgrade head` succeeds from base through 8a258d50ee87 (backup/restore procedure completed)
    - Both env.py configure sites carry render_as_batch=True
    - Existing dev DB restored untouched
  </done>
</task>

<task type="auto">
  <name>Task 4: TD-07 — encodeURIComponent(id) in WishDeepLinkRedirect</name>
  <files>frontend/src/App.jsx</files>
  <read_first>
    - frontend/src/App.jsx (WishDeepLinkRedirect at lines 86-99 — line 98 is the target)
    - 16-RESEARCH.md "TD-07: Deep-link encoding (one line)" (lines 206-213)
  </read_first>
  <action>
    Per RESEARCH.md TD-07: in frontend/src/App.jsx line 98, change the Navigate target from `base + '?wish=' + id` to `base + '?wish=' + encodeURIComponent(id)`. Do NOT touch the consumers (ChefWishesPage.jsx:48 / UserWishesPage.jsx:29 use `searchParams.get('wish')` which auto-decodes — no consumer changes needed). Do NOT add encoding at the querySelector highlight sites (they receive the already-decoded value). Preserve the existing Chinese comments and all other lines exactly. Note: this file is currently lint-clean — the change must not introduce lint errors (encodeURIComponent is a browser global, covered by the existing globals.browser config).
  </action>
  <verify>
    <automated>cd frontend && (grep -c "encodeURIComponent(id)" src/App.jsx | awk '{if ($1 >= 1) print "ENCODE-PASS"; else exit 1}') && npx eslint src/App.jsx 2>&1 | tail -1</automated>
  </verify>
  <done>
    - WishDeepLinkRedirect builds the deep-link query with encodeURIComponent(id)
    - `npx eslint src/App.jsx` reports 0 errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.yaml → Settings | YAML is a trusted repo artifact; the added app.url key feeds Feishu deep-link URLs (outbound HTTP construction) |
| CORS middleware | Browser origin allow-list read from settings.CORS_ORIGINS; the fallback default is a trust decision when `cors:` section is absent |
| alembic migration | Migration scripts execute schema DDL on the SQLite DB; the TD-05 edit changes how the constraint ALTER is performed (table rebuild vs raw ALTER) |
| WishDeepLinkRedirect → URL | `id` flows from the route param into a query string; unencoded values could inject `&`-separated parameters |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01-I | Information Disclosure | backend/app/config.py CORS fallback | mitigate | Change `["*"]` fallback to explicit dev origins — removes wildcard default for deployments missing the `cors:` section; combined with allow_credentials=True the wildcard would also crash startup |
| T-16-01-T | Tampering | config.yaml app.url | accept | Repo-owned config with placeholder URL; operator replaces with real public URL at deploy time (comment instructs this); no secret data |
| T-16-01-T | Tampering | migration 8a258d50ee87 batch rewrite | mitigate | Batch mode is SQLite copy-and-move; upgrade AND downgrade mirrored; verified via fresh-DB upgrade with backup/restore of dev DB |
| T-16-01-T | Tampering | WishDeepLinkRedirect encodeURIComponent | mitigate | Prevents wish-id query-param injection (id can't smuggle `&`-separated params); consumers' searchParams.get auto-decodes — no double-encoding |
| T-16-01-E | Elevation of Privilege | — | n/a | No auth/role code touched |
| T-16-01-SC | Supply Chain | npm/pip installs | n/a | Zero new packages (research Package Legitimacy Audit: all tools already declared in lockfiles) |
</threat_model>

<verification>
- 14-VERIFICATION.md overrides[0].accepted_by and accepted_at non-empty (operator signed TD-01)
- `grep 'url: "https://family-chef.app"' config.yaml config.example.yaml` → 2 hits
- `grep '"origins", \["\*"\]' backend/app/config.py` → 0 hits (hardened fallback)
- `grep -c 'render_as_batch=True' backend/alembic/env.py` → 2 (offline + online)
- `grep -c 'with op.batch_alter_table' backend/alembic/versions/8a258d50ee87_phase_6_add_system_logs_indexes.py` → 2 (upgrade + downgrade)
- `alembic upgrade head` from fresh DB succeeds (backup/restore procedure)
- `grep 'encodeURIComponent(id)' frontend/src/App.jsx` → 1 hit; `npx eslint src/App.jsx` → 0 errors
</verification>

<success_criteria>
- TD-01: Operator decision recorded in 14-VERIFICATION.md frontmatter (accepted_by/accepted_at filled) or rejection escalated with required CSS work specified
- TD-03: CORS verified tightened (config.yaml explicit origins, no `["*"]` in any code path) + fallback hardened + regression test added
- TD-04: `app.url` explicit in both config.yaml and config.example.yaml; backend APP_URL getter unchanged (already correct)
- TD-05: Fresh-DB `alembic upgrade head` succeeds; render_as_batch=True in both env.py sites — unblocks TD-06 (Plan 2)
- TD-07: Deep-link id encoded; App.jsx remains lint-clean
- All 8 files commit-ready
</success_criteria>

<output>
Create .planning/phases/16-tech-debt-cleanup/16-01-SUMMARY.md when done
</output>
