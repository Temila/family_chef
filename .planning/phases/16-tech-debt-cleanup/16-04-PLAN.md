---
phase: 16
plan: 16-04
title: "Frontend lint baseline: TD-10 (101 errors → 0, warnings reduced)"
status: pending
type: execute
wave: 2
depends_on:
  - "16-02"
files_modified:
  - frontend/eslint.config.js
  - frontend/tests/phase15-navigation.spec.js
  - frontend/tests/fixtures/list-item.jsx
  - frontend/tests/fixtures/phase12-bugfix.jsx
  - frontend/tests/fixtures/snackbar.jsx
  - frontend/src/contexts/AuthContext.jsx
  - frontend/src/contexts/CategoriesContext.jsx
  - frontend/src/contexts/ToastContext.jsx
  - frontend/src/hooks/usePendingOrderCount.js
  - frontend/src/components/InvitationsSection.jsx
  - frontend/src/components/ThemeToggle.jsx
  - frontend/src/pages/AdminDishesPage.jsx
  - frontend/src/pages/AdminCategoriesPage.jsx
  - frontend/src/pages/AdminChefsPage.jsx
  - frontend/src/pages/AdminHomePage.jsx
  - frontend/src/pages/AdminIngredientsPage.jsx
  - frontend/src/pages/AdminLogsPage.jsx
  - frontend/src/pages/AdminStatsPage.jsx
  - frontend/src/pages/AdminUsersPage.jsx
  - frontend/src/pages/ChefDishesPage.jsx
  - frontend/src/pages/ChefOrdersPage.jsx
  - frontend/src/pages/DishDetailPage.jsx
  - frontend/src/pages/ForceChangePasswordPage.jsx
  - frontend/src/pages/OrderDetailPage.jsx
  - frontend/src/pages/OrderPage.jsx
  - frontend/src/pages/PreferencesPage.jsx
  - frontend/src/pages/UserFavoritesPage.jsx
  - frontend/src/pages/UserOrdersPage.jsx
  - frontend/src/pages/UserProfilePage.jsx
autonomous: true
requirements:
  - TD-10

must_haves:
  truths:
    - "eslint.config.js has a node-globals override for vite.config.js + tests/**/*.spec.js (fixes remaining process no-undef)"
    - "eslint.config.js has a test-only override disabling react-refresh/only-export-components for tests/** (fixture files)"
    - "All react-hooks/immutability errors fixed by loader hoisting (useCallback-before-effect per ChefWishesPage.jsx:115-157 pattern) or file-scoped disables with justification — never blanket rule disables"
    - "All set-state-in-effect errors fixed with queueMicrotask/setTimeout deferral (codebase-established pattern)"
    - "All no-unused-vars errors fixed per-case (catch{} without binding, deleted unused imports/state)"
    - "All exhaustive-deps warnings resolved via complete dependency arrays"
    - "npx eslint . exits 0 with 0 errors"
    - "Warnings reduced from 22 to 0 (each remaining warning, if any, must be rule-justified with a comment)"
  artifacts:
    - path: "frontend/eslint.config.js"
      provides: "Node globals for node-context files + test-only react-refresh override"
      contains: "globals.node"
    - path: "frontend/src/pages/ChefOrdersPage.jsx"
      provides: "Loader hoisted above the effect that calls it (immutability fix template for the 9 pages)"
      contains: "useCallback"
  key_links:
    - from: "page effects"
      to: "page loaders"
      via: "useCallback declared BEFORE the useEffect that calls it, with complete deps — kills both react-hooks/immutability and exhaustive-deps (ChefWishesPage.jsx:115-157 is the verified-clean template)"
      pattern: "const load\\w+ = useCallback"
---

<objective>
Bring the frontend lint baseline from 101 errors + 22 warnings to 0 errors (TD-10), fixing code per-case rather than blanket-disabling. The work splits by rule family: (1) eslint config remediation (node globals for vite.config.js + Playwright specs; test-only react-refresh override for fixtures) plus trivial fixes (no-empty, unused disable directive); (2) the react-hooks v7 rule drift (immutability 24 + set-state-in-effect 5 + exhaustive-deps 21-22) fixed by the ChefWishesPage loader-hoisting pattern; (3) the no-unused-vars sweep (59) with per-case cleanup.

Purpose: The lint baseline red predates the eslint-plugin-react-hooks 7.x upgrade (new compiler-era rules) plus accumulated dead code. Green lint is the gate that keeps future phases from shipping regressions.
Output: ~29 files touched; `npx eslint .` → 0 errors, warnings reduced.
</objective>

<execution_context>
@/home/temila/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/temila/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16-tech-debt-cleanup/16-RESEARCH.md
@.planning/phases/16-tech-debt-cleanup/16-PATTERNS.md
@frontend/eslint.config.js
@frontend/src/pages/ChefWishesPage.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: ESLint config remediation + trivial fixes (no-undef, react-refresh, no-empty, unused disable)</name>
  <files>
    frontend/eslint.config.js
    frontend/tests/phase15-navigation.spec.js
    frontend/tests/fixtures/list-item.jsx
    frontend/tests/fixtures/phase12-bugfix.jsx
    frontend/tests/fixtures/snackbar.jsx
  </files>
  <read_first>
    - frontend/eslint.config.js (full file — single config block; globals.browser at line 17; reactRefresh.configs.vite at line 14)
    - 16-RESEARCH.md TD-10 (lines 29-30, 388-393) and 16-PATTERNS.md "no-undef" + "react-refresh" per-file fixes (lines 409-416)
  </read_first>
  <action>
    Per RESEARCH.md TD-10:

    1. frontend/eslint.config.js — ADD TWO override blocks after the main config object:
       - Node-context globals: `{ files: ['vite.config.js', 'tests/**/*.spec.js'], languageOptions: { globals: globals.node } }` — fixes the `process` no-undef in tests/phase15-navigation.spec.js (lines 23-25). NOTE: the vite.config.js `process` error at line 8 was already removed by Plan 16-02's define-block deletion — do not re-add it.
       - Test-only react-refresh override: `{ files: ['tests/**'], rules: { 'react-refresh/only-export-components': 'off' } }` — the 3 fixture files (list-item.jsx, phase12-bugfix.jsx, snackbar.jsx) export test data, not components; a project-rule override for the tests directory is the sanctioned mechanism (research: "move to a test-only eslint override block").

    2. frontend/tests/phase15-navigation.spec.js — no code change needed for no-undef once (1) lands (globals.node covers it); leave the file's logic untouched.

    3. fixtures: no per-file disables needed after the override — do NOT add eslint-disable comments to fixture files.

    4. While this task's files are open, also close any `no-empty` (2) and the unused-disable-directive (1) errors wherever they appear (grep eslint output for `no-empty` / `--report-unused-disable-directives` findings across the frontend) — fix by adding real content or removing the stale directive, per-case.

    5. After edits, run `npx eslint . -f json` and re-baseline: expect no-undef = 0, react-refresh = 0, no-empty = 0; remaining errors concentrate in no-unused-vars (59) and react-hooks/immutability (24), warnings in exhaustive-deps — all handled by Tasks 2-3.
  </action>
  <verify>
    <automated>cd frontend && npx eslint . -f json 2>/dev/null | python3 -c "
import json,sys
data = json.load(sys.stdin)
errs = [m for f in data for m in f['messages'] if m['severity']==2]
warns = [m for f in data for m in f['messages'] if m['severity']==1]
undef = [m for m in errs if m['ruleId']=='no-undef']
rr = [m for m in errs+warns if m['ruleId']=='react-refresh/only-export-components']
assert not undef, undef
assert not rr, rr
print(f'CONFIG-PASS remaining: {len(errs)} err / {len(warns)} warn')
"
  </verify>
  <done>
    - eslint.config.js carries node globals + test-only react-refresh override
    - no-undef / react-refresh / no-empty / unused-disable clusters = 0
    - Re-baseline recorded for Tasks 2-3
  </done>
</task>

<task type="auto">
  <name>Task 2: react-hooks v7 rule drift — immutability + set-state-in-effect + exhaustive-deps (loader hoisting)</name>
  <files>
    frontend/src/pages/UserOrdersPage.jsx
    frontend/src/pages/ChefOrdersPage.jsx
    frontend/src/pages/DishDetailPage.jsx
    frontend/src/pages/OrderDetailPage.jsx
    frontend/src/pages/OrderPage.jsx
    frontend/src/pages/PreferencesPage.jsx
    frontend/src/pages/UserFavoritesPage.jsx
    frontend/src/pages/UserProfilePage.jsx
    frontend/src/pages/AdminCategoriesPage.jsx
    frontend/src/pages/AdminChefsPage.jsx
    frontend/src/pages/AdminHomePage.jsx
    frontend/src/pages/AdminIngredientsPage.jsx
    frontend/src/pages/AdminLogsPage.jsx
    frontend/src/pages/AdminStatsPage.jsx
    frontend/src/pages/AdminUsersPage.jsx
    frontend/src/pages/ForceChangePasswordPage.jsx
    frontend/src/contexts/AuthContext.jsx
    frontend/src/contexts/CategoriesContext.jsx
    frontend/src/contexts/ToastContext.jsx
    frontend/src/hooks/usePendingOrderCount.js
  </files>
  <read_first>
    - frontend/src/pages/ChefWishesPage.jsx lines 115-157 — THE lint-clean template: `const loadWishes = useCallback(...)` declared BEFORE the effect that calls it; effects use `queueMicrotask` for synchronous setState; `catch { ... }` without binding; deps complete.
    - frontend/src/pages/AdminDishesPage.jsx lines 59/388-396 — the boolean-in-flight state pattern (extracting) for pages with action-state effects.
    - 16-RESEARCH.md "Pitfall 4: New React Hooks lint rules flagging legacy code" (lines 162-166) and 16-PATTERNS.md "The lint-clean pattern to copy" (lines 395-406) + per-file immutability fix list (line 408).
  </read_first>
  <action>
    Per RESEARCH.md/16-PATTERNS.md — the react-hooks/immutability errors ("Cannot access variable before it is declared") are effects calling loaders declared LATER as plain `const` functions; the error count also drags exhaustive-deps warnings with it. Fix pattern (NOT moving effects below plain declarations — those still change identity per render):

    1. For each file listed by `npx eslint . -f json` with `react-hooks/immutability` errors (research enumerates 9 pages: UserOrdersPage:32, ChefOrdersPage:29, DishDetailPage:38, OrderDetailPage:28, OrderPage:73/77/81, PreferencesPage:25, UserFavoritesPage:21, UserProfilePage:32 + others): convert the page's loader function(s) to `useCallback` with COMPLETE dependency arrays, declare them BEFORE the effect(s) that call them, and ensure the effect's dependency array lists the loader (kills immutability + exhaustive-deps together). Follow ChefWishesPage.jsx:115-157 exactly: loader signature with a params object + background flag, refs for cross-callback state (requestSeqRef/pageRef pattern) where the page has sequential-race concerns, and `catch { ... }` (no err binding).

    2. For each `react-hooks/set-state-in-effect` error (5): wrap the synchronous setState call at effect top-level in `queueMicrotask(() => { ... })` — the codebase-established pattern (ChefWishesPage.jsx:139-157 comment: "内联 fetch 以规避 set-state-in-effect"; also setTimeout precedent at line 195). Do NOT convert the whole effect to .then chains if the page already uses a different working shape — only defer the offending synchronous setState.

    3. For remaining `exhaustive-deps` warnings (22): complete the dependency arrays. Where a dep is a stable ref or a setState function, follow the established convention (refs accessed via .current are not deps; setState functions are stable). Where adding a dep would change behavior (e.g., a loader re-created per render), the useCallback conversion from (1) is the fix — never add eslint-disable for exhaustive-deps.

    4. Contexts/hooks files with errors (AuthContext.jsx, CategoriesContext.jsx, ToastContext.jsx, usePendingOrderCount.js — 2+2+1+1 errors): apply the same rules; these are small files, resolve each error per-case.

    5. Disputable cases (research open question 3 — e.g. immutability errors on ref-mutation patterns that are genuinely false positives): add a FILE-SCOPED `/* eslint-disable react-hooks/immutability -- <Chinese justification> */` at the top of the specific file ONLY after attempting the hoisting fix; NEVER disable the rule project-wide. Every disable needs a justification comment naming the reason (research: "never blanket-disable the rule").
  </action>
  <verify>
    <automated>cd frontend && npx eslint . -f json 2>/dev/null | python3 -c "
import json,sys
data = json.load(sys.stdin)
errs = [m for f in data for m in f['messages'] if m['severity']==2]
warns = [m for f in data for m in f['messages'] if m['severity']==1]
imm = [m for m in errs if m['ruleId']=='react-hooks/immutability']
sse = [m for m in errs if m['ruleId']=='react-hooks/set-state-in-effect']
ed = [m for m in warns if m['ruleId']=='react-hooks/exhaustive-deps']
assert not imm, imm
assert not sse, sse
assert not ed, ed
print(f'HOOKS-PASS remaining: {len(errs)} err / {len(warns)} warn')
"
  </verify>
  <done>
    - immutability + set-state-in-effect + exhaustive-deps clusters = 0 (or each remaining case has a file-scoped justified disable)
    - All pages follow the ChefWishesPage loader-hoisting shape
  </done>
</task>

<task type="auto">
  <name>Task 3: no-unused-vars sweep (59) + final 0-error gate</name>
  <files>
    frontend/src/pages/AdminDishesPage.jsx
    frontend/src/pages/ChefDishesPage.jsx
    frontend/src/pages/PreferencesPage.jsx
    frontend/src/pages/OrderPage.jsx
    frontend/src/pages/ChefOrdersPage.jsx
    frontend/src/pages/DishDetailPage.jsx
    frontend/src/pages/OrderDetailPage.jsx
    frontend/src/pages/UserOrdersPage.jsx
    frontend/src/pages/AdminCategoriesPage.jsx
    frontend/src/pages/AdminChefsPage.jsx
    frontend/src/pages/AdminHomePage.jsx
    frontend/src/pages/UserFavoritesPage.jsx
    frontend/src/pages/AdminIngredientsPage.jsx
    frontend/src/pages/AdminLogsPage.jsx
    frontend/src/pages/AdminStatsPage.jsx
    frontend/src/pages/AdminUsersPage.jsx
    frontend/src/pages/UserProfilePage.jsx
    frontend/src/pages/ForceChangePasswordPage.jsx
    frontend/src/contexts/AuthContext.jsx
    frontend/src/contexts/CategoriesContext.jsx
    frontend/src/contexts/ToastContext.jsx
    frontend/src/components/InvitationsSection.jsx
    frontend/src/components/ThemeToggle.jsx
    frontend/src/hooks/usePendingOrderCount.js
  </files>
  <read_first>
    - The current full error list: `cd frontend && npx eslint . -f json` — filter ruleId == no-unused-vars; each message names the exact unused variable and line.
    - 16-PATTERNS.md no-unused-vars per-file notes (lines 389, 407-409): `catch (err)` with unused err → `catch {` (majority); unused imports/state (navigate, user, Input, EmptyState, useMemo, statusOptions, handleDelete, pageNum, addingTo/setAddingTo, getTypeMeta, loadOrders/loadDish...) → delete; OrderPage.jsx:98 `loadDishes(pageNum)` unused param → drop param.
  </read_first>
  <action>
    Per RESEARCH.md/16-PATTERNS.md — mechanical sweep, no blanket disables:

    1. Generate the exact list: `cd frontend && npx eslint . -f json` filtered to `no-unused-vars` (59 occurrences). Process each occurrence per-case:
       - `catch (err)` / `catch (error)` with an unused binding → `catch { ... }` (keep the block; the binding was never read). EXCEPTION: do NOT touch the ChefWishesPage handlers (err.message IS used) and do not mechanically rewrite handlers where the binding is read.
       - Unused imports → delete the import line. Unused state/const declarations → delete the declaration (and its setter if also unused — e.g. `addingTo`/`setAddingTo` pair).
       - Assigned-but-never-read function params → drop the param from the signature and the call sites in that file (e.g. OrderPage.jsx `loadDishes(pageNum)` — pageNum is unused).
       - Do NOT add `void x` or underscore-prefix hacks; do NOT add eslint-disable for unused vars.

    2. Also close any residual `no-empty` blocks and the unused-disable-directive flagged in Task 1 but found in these files.

    3. After the sweep, run the FINAL GATE: `cd frontend && npx eslint .` — exit code 0, 0 errors. For warnings: run `npx eslint . -f json`; target 0 warnings — each remaining warning (if any) must be rule-justified: either it is an exhaustive-deps case that Task 2 could not resolve with useCallback (add file-scoped disable with justification) or a react-refresh warning outside tests (already handled). Then re-run `npx eslint .` to confirm exit 0.

    4. Sanity: `npm run build` still succeeds (lint fixes must not break the build).
  </action>
  <verify>
    <automated>cd frontend && npx eslint . -f json 2>/dev/null | python3 -c "
import json,sys
data = json.load(sys.stdin)
errs = [m for f in data for m in f['messages'] if m['severity']==2]
warns = [m for f in data for m in f['messages'] if m['severity']==1]
assert not errs, errs
print(f'LINT-CLEAN-PASS: 0 errors, {len(warns)} warnings')
" && npm run build 2>&1 | tail -3
  </verify>
  <done>
    - npx eslint . → 0 errors, exit 0
    - Warnings 0 (or each remaining one rule-justified with a file-scoped comment)
    - npm run build succeeds (no behavioral regression from the sweep)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eslint.config.js → lint scope | Config changes alter which globals/rules apply to which files — dev-time only, no runtime boundary |
| page loaders (useCallback hoisting) | Refactoring state-loading order changes render-time behavior; must preserve fetch/race semantics |
| vite.config.js / Playwright specs | Node-context files gain node globals — dev/test tooling only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04-T | Tampering | useCallback loader hoisting | mitigate | Reorder declarations, do NOT change handler logic or dep semantics; deps completed per ChefWishesPage template; verified by lint + npm run build + no test-file changes |
| T-16-04-T | Tampering | eslint.config.js overrides | accept | Dev-tooling only; overrides scoped to tests/** and node-context files — production bundle unaffected (vite build passes) |
| T-16-04-I | Information Disclosure | — | n/a | No data paths touched |
| T-16-04-SC | Supply Chain | npm installs | n/a | Zero new packages; eslint/plugin versions already pinned in package-lock.json |
</threat_model>

<verification>
- `cd frontend && npx eslint .` → exit 0, 0 errors (was 101 errors / 22 warnings)
- Rule-family zeroing: no-unused-vars 0, react-hooks/immutability 0, react-hooks/set-state-in-effect 0, react-hooks/exhaustive-deps 0 warnings, no-undef 0, no-empty 0, react-refresh/only-export-components 0, unused-disable-directives 0
- `npm run build` succeeds
- No eslint-disable comments added without a Chinese justification comment (file-scoped only)
</verification>

<success_criteria>
- TD-10: 101 errors → 0 errors; warnings 22 → 0 (each remaining, if any, documented with justification)
- All fixes are per-case code changes or scoped config overrides — no blanket rule disables, no behavior changes (build + existing behavior preserved)
- ChefWishesPage remains the reference shape for all touched pages
</success_criteria>

<output>
Create .planning/phases/16-tech-debt-cleanup/16-04-SUMMARY.md when done
</output>