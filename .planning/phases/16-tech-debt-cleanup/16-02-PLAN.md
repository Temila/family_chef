---
phase: 16
plan: 16-02
title: "Medium fixes: TD-02 version from config.yaml, TD-06 auto-migrate on startup, TD-08 actingId residue"
status: pending
type: execute
wave: 1
depends_on:
  - 16-01
files_modified:
  - backend/app/main.py
  - backend/tests/test_version.py
  - frontend/src/api/client.js
  - frontend/src/components/composites/Sidebar.jsx
  - frontend/vite.config.js
  - frontend/src/pages/ChefWishesPage.jsx
autonomous: true
requirements:
  - TD-02
  - TD-06
  - TD-08

must_haves:
  truths:
    - "GET /api/version returns {\"version\": settings.APP_VERSION, \"name\": settings.APP_NAME} (public, like /api/health)"
    - "Sidebar renders the version fetched from /api/version with a '0.0.0' fallback before/without the API response"
    - "vite.config.js no longer contains a define block for VITE_APP_VERSION (package.json version chain removed)"
    - "main.py startup runs alembic upgrade head via asyncio.to_thread BEFORE await init_db(), replacing the NOTE(07-04) block"
    - "The startup migration is env-gated: AUTO_MIGRATE default-on (\"1\"), skipped when PYTEST_CURRENT_TEST is set"
    - "The alembic Config in startup is pinned to settings.DATABASE_URL so migrations hit the same DB the app uses regardless of CWD"
    - "ChefWishesPage handleAdvance/handleReject clear advanceTarget/rejectTarget in finally; all three handlers reset setActingId(null) BEFORE await loadWishes(...)"
    - "ChefWishesPage.jsx still passes `npx eslint` with 0 errors (it is the lint-clean reference page)"
  artifacts:
    - path: "backend/app/main.py"
      provides: "GET /api/version endpoint + AUTO_MIGRATE-gated startup migration hook"
      exports: ["_run_migrations"]
      contains: "AUTO_MIGRATE"
    - path: "backend/tests/test_version.py"
      provides: "pytest coverage for the version endpoint"
      exports: ["test_version_endpoint"]
      contains: "/api/version"
    - path: "frontend/src/api/client.js"
      provides: "getVersion() on ApiClient"
      contains: "getVersion"
    - path: "frontend/src/components/composites/Sidebar.jsx"
      provides: "Runtime version fetch with fallback (replaces build-time VITE_APP_VERSION const)"
      contains: "getVersion"
    - path: "frontend/vite.config.js"
      provides: "define block removed (kills process no-undef — TD-10 overlap)"
      contains: "plugins: [react()]"
    - path: "frontend/src/pages/ChefWishesPage.jsx"
      provides: "Modal targets cleared in finally; actingId reset before reload await"
      contains: "setActingId(null)"
  key_links:
    - from: "main.py startup()"
      to: "alembic.command.upgrade"
      via: "asyncio.to_thread(_run_migrations) before await init_db(); Config resolved via Path(__file__).parent.parent / 'alembic.ini' with sqlalchemy.url overridden to settings.DATABASE_URL"
      pattern: "command.upgrade"
    - from: "Sidebar.jsx"
      to: "/api/version"
      via: "api.getVersion() fetched in useEffect (lint-clean shape: .then/.catch {}, fallback '0.0.0')"
      pattern: "getVersion"
    - from: "ChefWishesPage.jsx finally blocks"
      to: "setActingId(null) / setAdvanceTarget(null) / setRejectTarget(null)"
      via: "state resets execute before the await loadWishes(...) reload so a reject never leaves actingId/modal targets stuck"
      pattern: "setActingId\\(null\\)"
---

<objective>
Fix the three medium-weight debt items: TD-02 (make config.yaml the single version source — backend `GET /api/version` endpoint + ApiClient method + Sidebar runtime fetch, removing the stale package.json build-time injection), TD-06 (programmatic `alembic upgrade head` in the startup hook, env-gated so tests are never affected, replacing the NOTE(07-04) manual-migration comment), and TD-08 (ChefWishesPage modal-target/actingId stale-state residue — clear modal targets in `finally` and reset actingId before the reload await so a blocked or failing action can never leave a stale modal or a permanently-locked card grid).

Purpose: Version truth moves server-side (config.yaml), deployments stop requiring manual migration steps, and the wish-card cross-card residue bug is closed at its mechanism.
Output: 6 files (2 backend incl. 1 new test, 4 frontend).
</objective>

<execution_context>
@/home/temila/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/temila/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16-tech-debt-cleanup/16-RESEARCH.md
@.planning/phases/16-tech-debt-cleanup/16-PATTERNS.md
@backend/app/main.py
@backend/app/config.py
@backend/tests/conftest.py
@frontend/src/api/client.js
@frontend/vite.config.js
@frontend/src/components/composites/Sidebar.jsx
@frontend/src/pages/ChefWishesPage.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: TD-02 — Version endpoint + ApiClient.getVersion + Sidebar runtime fetch + remove Vite define</name>
  <files>
    backend/app/main.py
    backend/tests/test_version.py
    frontend/src/api/client.js
    frontend/src/components/composites/Sidebar.jsx
    frontend/vite.config.js
  </files>
  <read_first>
    - backend/app/main.py (health endpoint at lines 260-263 — the analog; app FastAPI() at lines 212-216 already consumes settings.APP_VERSION)
    - backend/app/config.py (APP_VERSION getter at line 35 — single source of truth, config.yaml app.version = "0.1.0")
    - frontend/src/api/client.js (ApiClient class — add getVersion near the Auth section; baseURL is '/api' so the path is '/version')
    - frontend/src/components/composites/Sidebar.jsx (line 22 APP_VERSION const; line 100-102 render site)
    - frontend/vite.config.js (define block lines 7-9 — the only consumer of VITE_APP_VERSION is Sidebar.jsx, grep-verified)
    - 16-RESEARCH.md "TD-02: Version endpoint + frontend consumption" (lines 215-234)
    - 16-PATTERNS.md "Lint-clean loader-in-effect pattern" (ChefWishesPage.jsx:115-157 shape for the fetch effect)
  </read_first>
  <action>
    Per RESEARCH.md TD-02:

    1. backend/app/main.py: ADD next to the /api/health endpoint (after line 263):
       ```
       @app.get("/api/version")
       async def version_check():
           """应用版本"""
           return {"version": settings.APP_VERSION, "name": settings.APP_NAME}
       ```
       Public (no auth) — consistent with /api/health; exposes only name/version, no sensitive data.

    2. CREATE backend/tests/test_version.py: one async test `test_version_endpoint` using the existing `client` fixture (conftest.py:124-148, no auth needed): `GET /api/version` → status 200 and `data["version"] == settings.APP_VERSION`. Mirror the style of test_tools.py (Chinese docstring, @pytest.mark.asyncio, httpx AsyncClient typing).

    3. frontend/src/api/client.js: ADD to the ApiClient class (near the Auth section, after refreshToken):
       ```
       async getVersion() {
         return this.get('/version');
       }
       ```
       Method naming follows the existing camelCase-with-entity-prefix convention.

    4. frontend/src/components/composites/Sidebar.jsx: REPLACE line 21-22 (`// D-NAV03-01: ...` comment + `const APP_VERSION = import.meta.env.VITE_APP_VERSION || '0.0.0';`) with a runtime fetch following the lint-clean shape:
       - `const [appVersion, setAppVersion] = useState('0.0.0');`
       - `useEffect(() => { api.getVersion().then((d) => setAppVersion(d.version)).catch(() => {}); }, []);` — `catch {}`-style with no err binding (unused-vars rule), empty deps (no external bindings), and keep the `'0.0.0'` fallback so the sidebar renders before/without the API response.
       - Add imports: `useState, useEffect` from react and `api` from '../../api/client'. Update the D-NAV03-01 comment to record the new source: version now comes from backend config.yaml via GET /api/version (D-NAV03-01 amended by TD-02).
       - Update the render site (line 100-102) to `v{appVersion}`.

    5. frontend/vite.config.js: DELETE the `define: { ... }` block (lines 7-9) entirely. Nothing else consumes VITE_APP_VERSION (grep-verified — Sidebar was the only consumer). This also removes one TD-10 `no-undef` error (`process`). Do NOT touch the proxy or build sections. Leave package.json version "0.0.0" alone (npm metadata, now unused).

    6. Do NOT add a hardcoded version anywhere; config.yaml `app.version` remains the single source of truth.
  </action>
  <verify>
    <automated>cd backend && python -m py_compile app/main.py && .venv/bin/pytest tests/test_version.py -q 2>&1 | tail -2 && cd ../frontend && (grep -c "define" vite.config.js | awk '{if ($1==0) print "DEFINE-REMOVED-PASS"; else exit 1}') && npx eslint src/api/client.js src/components/composites/Sidebar.jsx 2>&1 | tail -1</automated>
  </verify>
  <done>
    - GET /api/version returns {"version": "0.1.0", "name": "家味·Family Chef"} (matches config.yaml)
    - test_version.py passes; Sidebar renders v0.1.0 fetched at runtime with 0.0.0 fallback
    - vite.config.js has no define block; client.js + Sidebar.jsx remain lint-clean
  </done>
</task>

<task type="auto">
  <name>Task 2: TD-06 — AUTO_MIGRATE-gated alembic upgrade on startup (before init_db)</name>
  <files>backend/app/main.py</files>
  <read_first>
    - backend/app/main.py (startup handler at lines 227-251 — the NOTE(07-04) block at lines 232-234 is the replacement target; existing warn-and-continue pattern at lines 202-206; loop.run_in_executor precedent at lines 208-209)
    - backend/alembic.ini (script_location = %(here)s/alembic; sqlalchemy.url default)
    - backend/app/config.py (DATABASE_URL getter at line 41)
    - 16-RESEARCH.md "TD-06: Programmatic Alembic upgrade in startup" (lines 177-192) and "Startup migration hook" architecture (lines 74-91)
  </read_first>
  <action>
    Per RESEARCH.md TD-06 (programmatic API, NOT subprocess):

    1. Add module-level imports to main.py: `from pathlib import Path`, `from alembic import command`, `from alembic.config import Config`.

    2. ADD a module-level sync function (near _print_startup_info):
       ```
       def _run_migrations() -> None:
           """启动时自动应用数据库迁移（TD-06；SQLite batch 见 TD-05）"""
           ini_path = Path(__file__).resolve().parent.parent / "alembic.ini"
           cfg = Config(str(ini_path))
           # 锁定与应用相同的数据库 URL（忽略 CWD 差异）
           cfg.set_main_option("sqlalchemy.url", settings.DATABASE_URL)
           command.upgrade(cfg, "head")
       ```
       The `set_main_option` override is REQUIRED — it guarantees the migration targets the same DB the app configured (settings.DATABASE_URL), independent of the working directory.

    3. In the startup() handler, REPLACE the NOTE(07-04) comment block (lines 232-234) with the env-gated call, placed BEFORE `await init_db()`:
       ```
       # TD-06: 启动时自动迁移（AUTO_MIGRATE=0 关闭；pytest 下自动跳过）
       if os.environ.get("AUTO_MIGRATE", "1") == "1" and "PYTEST_CURRENT_TEST" not in os.environ:
           try:
               await asyncio.to_thread(_run_migrations)
           except Exception as e:
               _log(f"  ⚠ 自动迁移失败: {e}（可手动执行 cd backend && uv run alembic upgrade head）")
       ```
       Error handling is warn-and-continue — consistent with the model-download failure pattern (main.py:202-206); init_db() still runs (create_all covers missing tables). Env gate satisfies the MUST requirement: pytest's ASGITransport/TestClient never trigger migrations (AUTO_MIGRATE defaults ON for production, skipped under pytest).

    4. Do NOT migrate the app to lifespan handlers in this task (on_event stays; flagged in research as out of scope). Do NOT reorder init_db() relative to the seed calls.
  </action>
  <verify>
    <automated>cd backend && python -m py_compile app/main.py && (grep -c 'AUTO_MIGRATE' app/main.py | awk '{if ($1 >= 2) print "ENV-GATE-PASS"; else exit 1}') && (grep -c 'command.upgrade' app/main.py | awk '{if ($1 >= 1) print "UPGRADE-CALL-PASS"; else exit 1}') && (grep -c 'asyncio.to_thread(_run_migrations)' app/main.py | awk '{if ($1 >= 1) print "TO-THREAD-PASS"; else exit 1}') && .venv/bin/python -c "from app.main import _run_migrations; _run_migrations(); print('MIGRATIONS-IDEMPOTENT-PASS')"</automated>
  </verify>
  <done>
    - Startup runs alembic upgrade head via asyncio.to_thread before init_db() when AUTO_MIGRATE enabled
    - Env gate verified: AUTO_MIGRATE default-on, skipped under pytest; no subprocess usage
    - `_run_migrations()` executes idempotently against the existing dev DB (no-op at head; forward-only otherwise — TD-05 plan verified the fresh-DB path)
  </done>
</task>

<task type="auto">
  <name>Task 3: TD-08 — Clear modal targets in finally + reset actingId before the reload await (ChefWishesPage)</name>
  <files>frontend/src/pages/ChefWishesPage.jsx</files>
  <read_first>
    - frontend/src/pages/ChefWishesPage.jsx (handlers at lines 219-272: handleClaim 219-234, handleAdvance 237-253, handleReject 256-272 — the three finally blocks at 228-231 / 247-250 / 266-269)
    - 16-RESEARCH.md "TD-08: Modal-target cleanup in finally" (lines 116-123) and 16-PATTERNS.md TD-08 section (lines 298-335)
  </read_first>
  <action>
    Per RESEARCH.md TD-08 minimal fix (item 1 — modal cleanup; do NOT add WishCard prop changes):

    The residue mechanism: (a) `setAdvanceTarget(null)`/`setRejectTarget(null)` sit INSIDE the try block after the API call — when the `if (viewAsAdmin || actingId) return;` guard blocks a second card's action, the modal target set by the earlier open survives and fires an unintended action once actingId clears; (b) `setActingId(null)` sits AFTER `await loadWishes(...)` in finally — if loadWishes ever rejects, actingId stays stuck forever, blocking ALL card actions.

    In all three handlers (handleClaim, handleAdvance, handleReject):

    1. MOVE the modal-target clears into the `finally` block:
       - handleAdvance: move `setAdvanceTarget(null)` from inside try (line 244) to the finally, placed FIRST.
       - handleReject: move `setRejectTarget(null)` from inside try (line 263) to the finally, placed FIRST.
       - handleClaim: no modal target to move.

    2. REORDER each finally so state resets execute BEFORE the reload await:
       ```
       finally {
         setActingId(null);
         setAdvanceTarget(null);   // handleAdvance only
         setRejectTarget(null);    // handleReject only
         await loadWishes({ page: 1, background: true });
       }
       ```
       The `await loadWishes(...)` refresh is deliberate (list refresh after mutation) — keep it, but it can no longer strand the state resets.

    3. Do NOT change the `catch (err)` blocks (err.message IS used — they are not TD-10 targets), the guard conditions, the useCallback dep arrays (actingId/loadWishes/showToast/viewAsAdmin remain correct), or anything else in the file. Preserve the lint-clean status.
  </action>
  <verify>
    <automated>cd frontend && npx eslint src/pages/ChefWishesPage.jsx 2>&1 | tail -1 && .venv/../node_modules/.bin/eslint src/pages/ChefWishesPage.jsx >/dev/null 2>&1; (awk 'BEGIN{ok=1} /finally \{/{in_f=1} in_f && /setActingId\(null\)/{a=1} in_f && /setAdvanceTarget\(null\)/{b=1} in_f && /setRejectTarget\(null\)/{c=1} in_f && /await loadWishes/{if(!(a)) ok=0; if(FNR>270){exit}} END{print (ok && a && b && c) ? "FINALLY-RESET-PASS" : "FINALLY-RESET-FAIL"; exit !(ok&&a&&b&&c)}' src/pages/ChefWishesPage.jsx)</automated>
  </verify>
  <done>
    - All three handlers reset actingId before the loadWishes await in finally
    - advanceTarget/rejectTarget cleared in finally (never survive a guard-blocked or failed action)
    - ChefWishesPage.jsx remains lint-clean (0 errors)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/version | Public unauthenticated endpoint; returns app name/version only |
| startup → DB migrations | Startup hook executes DDL on the production SQLite DB before serving; gated by AUTO_MIGRATE + pytest detection |
| Sidebar.jsx → /api/version | Browser fetch on mount; response value rendered as text (React escapes, no injection) |
| ChefWishesPage state | Client-side UI state; stale modal targets can fire unintended API actions (advance/reject) — a state-management integrity issue, not a security boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-02-I | Information Disclosure | GET /api/version | accept | Public by design (like /api/health — needed pre-login for the SPA); exposes only APP_NAME/APP_VERSION, no PII, no internals |
| T-16-02-T | Tampering | startup migration hook | mitigate | Env-gated (AUTO_MIGRATE default on, skipped under pytest); URL pinned via set_main_option to settings.DATABASE_URL so a malicious/foreign alembic.ini can't redirect the target; warn-and-continue keeps startup observable |
| T-16-02-D | Denial of Service | alembic upgrade in async startup | mitigate | asyncio.to_thread keeps the event loop free during batch table rebuilds (research Pitfall 5); single-process uvicorn deployment constraint documented — race between workers is accepted (upgrade is idempotent) |
| T-16-02-E | Elevation of Privilege | /api/version | n/a | No auth/role code touched |
| T-16-02-T | Tampering | ChefWishesPage finally resets | mitigate | State resets ordered before the reload await; stale advance/reject targets can no longer fire unintended wish actions after a guard-block |
| T-16-02-SC | Supply Chain | npm/pip installs | n/a | Zero new packages (alembic/fastapi already in pyproject.toml lockfile) |
</threat_model>

<verification>
- `pytest tests/test_version.py -q` passes (version endpoint contract)
- `npx eslint src/api/client.js src/components/composites/Sidebar.jsx src/pages/ChefWishesPage.jsx` → 0 errors (all three were lint-clean before)
- `grep AUTO_MIGRATE backend/app/main.py` → gate present; `python -c "from app.main import _run_migrations; _run_migrations()"` succeeds idempotently
- `grep -n 'setActingId(null)' frontend/src/pages/ChefWishesPage.jsx` → 3 occurrences, each in finally before the loadWishes await; setAdvanceTarget(null)/setRejectTarget(null) each 1 occurrence in finally
- vite.config.js define block gone (grep define → 0)
</verification>

<success_criteria>
- TD-02: Sidebar shows the config.yaml version (0.1.0) fetched from GET /api/version; Vite build-time injection removed; new backend test locks the contract
- TD-06: Fresh deployments auto-migrate at startup (idempotent on existing DBs; TD-05 in Plan 1 guarantees the fresh-DB path); tests unaffected by the gate
- TD-08: Stale modal targets and stuck actingId eliminated; per-card actions reset even when a concurrent action was blocked
- All 6 files commit-ready; frontend files lint-clean
</success_criteria>

<output>
Create .planning/phases/16-tech-debt-cleanup/16-02-SUMMARY.md when done
</output>
