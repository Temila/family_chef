---
phase: 16
plan: 16-03
title: "Test suite remediation: TD-09 (108 failures → 0)"
status: pending
type: execute
wave: 2
depends_on:
  - "16-02"
files_modified:
  - backend/app/main.py
  - backend/app/services/smart_ingredient_extractor.py
  - backend/tests/test_misc_extra.py
  - backend/tests/test_services_extra.py
  - backend/tests/test_favorites.py
  - backend/tests/test_routes_extra.py
  - backend/tests/test_routes_final.py
  - backend/tests/test_feishu.py
autonomous: true
requirements:
  - TD-09

must_haves:
  truths:
    - "uv sync --extra dev installs pytest/pytest-asyncio/pytest-cov from the existing lockfile (pytest 9.0.3 verified)"
    - "backend/app/main.py contains the normalize_api_trailing_slash middleware for /api/* paths"
    - "GET /api/categories/ returns the same status as GET /api/categories (no more 405 from the SPA catch-all shadow)"
    - "smart_ingredient_extractor.extract_ingredients falls back to the basic pypinyin extractor when llama_cpp is missing (no ImportError to the caller)"
    - "No test file constructs models with a pinyin= kwarg (model field drift closed)"
    - "Full pytest run: 0 failed, 0 errors (4 skipped allowed — pre-existing, out of scope)"
  artifacts:
    - path: "backend/app/main.py"
      provides: "Trailing-slash normalization middleware (10 lines, before the SPA catch-all route)"
      contains: "normalize_api_trailing_slash"
    - path: "backend/app/services/smart_ingredient_extractor.py"
      provides: "Graceful fallback to ingredient_extractor when llama_cpp import/load fails"
      contains: "except ImportError"
    - path: "backend/tests/test_misc_extra.py"
      provides: "pinyin= kwargs removed from Ingredient/Dish constructions"
      contains: "Ingredient("
    - path: "backend/tests/test_services_extra.py"
      provides: "pinyin= kwargs and dead pinyin assertions removed"
      contains: "Ingredient("
    - path: "backend/tests/test_feishu.py"
      provides: "Assertions aligned with current 422-validation behavior"
      contains: "test_send_notify_missing_params"
  key_links:
    - from: "main.py middleware"
      to: "all /api routers"
      via: "request.scope['path'] rewritten before routing — tests calling /api/x/ now hit the no-slash router paths like the frontend"
      pattern: "startswith(\"/api/\")"
    - from: "smart_ingredient_extractor.py"
      to: "ingredient_extractor.py"
      via: "except ImportError → await ingredient_extractor.extract_ingredients(db, text) — matches the 'smart features are optional' philosophy (config.yaml smart.enabled)"
      pattern: "ingredient_extractor"
---

<objective>
Remediate the backend test suite from 108 failed / 8 errors to 0 failures / 0 errors (TD-09), addressing all six root-cause clusters from RESEARCH.md: (1) the trailing-slash 405 shadow — fixed by one ASGI middleware in main.py (NOT by rewriting ~140 test URLs, NOT by changing router paths); (2) missing dev extras (`uv sync --extra dev` as wave-0 bootstrap); (3) llama_cpp ImportError — graceful fallback to the basic extractor; (4) model-field drift (`pinyin=` kwargs in fixtures); (5) response-shape drift (assertions updated to the shipped response envelopes — the app is the source of truth, shipped through 15 phases); (6) behavior drift (ValueError message / validation-status changes reflected in tests).

Purpose: Restore the test suite as the project's regression net. The middleware is the single-point fix verified by direct probe — it keeps the frontend contract untouched and makes the ~80 cascade failures collapse in one diff.
Output: 2 backend source files + up to 8 test files aligned; 0 failures.
</objective>

<execution_context>
@/home/temila/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/temila/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16-tech-debt-cleanup/16-RESEARCH.md
@.planning/phases/16-tech-debt-cleanup/16-PATTERNS.md
@backend/app/main.py
@backend/app/routers/tools.py
@backend/app/services/smart_ingredient_extractor.py
@backend/app/services/ingredient_extractor.py
@backend/tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave-0 env bootstrap + trailing-slash middleware + full-suite baseline</name>
  <files>backend/app/main.py</files>
  <read_first>
    - backend/app/main.py (CORS middleware block at lines 218-224 — register the new middleware next to it; SPA catch-all at lines 300-315 — the shadowing route)
    - 16-RESEARCH.md "TD-09: Trailing-slash normalization middleware (primary fix)" (lines 93-110) — the verified code
  </read_first>
  <action>
    1. Wave-0 bootstrap (run first, it is the environment root cause): `cd backend && uv sync --extra dev`. This materializes the already-pinned dev extras (pytest 9.0.3, pytest-asyncio 1.3.0, pytest-cov 7.1.0) from uv.lock. Do NOT install llama-cpp-python (research anti-pattern: multi-hundred-MB native build for 3 tests; the code fix in Task 2 is the right trade).

    2. In backend/app/main.py, ADD the trailing-slash normalization middleware (register next to the CORSMiddleware block, before the include_router calls — middleware runs before routing regardless):
       ```
       @app.middleware("http")
       async def normalize_api_trailing_slash(request, call_next):
           """TD-09: 归一化 /api/* 尾斜杠（SPA catch-all 遮蔽 redirect_slashes，405 而非 307）"""
           path = request.url.path
           if path.startswith("/api/") and path != "/api/" and path.endswith("/"):
               request.scope["path"] = path.rstrip("/")
               request.scope["raw_path"] = request.scope["raw_path"].rstrip(b"/")
           return await call_next(request)
       ```
       This fixes the ~80 failures: tests calling `/api/dishes/` etc. now route to the canonical no-slash router paths — the SPA catch-all `GET /{full_path:path}` shadowed Starlette's redirect_slashes (405/HTML instead of redirect), and ASGITransport never follows redirects. Do NOT edit routers to add "/" paths (would break frontend client.js) and do NOT rewrite test URLs (Task 3 aligns only the genuinely-drifted assertions).

    3. Confirm the collapse: run the full suite and record the new baseline: `.venv/bin/pytest tests/ -q --no-header 2>&1 | tail -3`. Expect the ~80 slash-cascade failures (test_ingredients/test_orders/test_users/test_routes_*/test_favorites/test_preferences/test_categories/test_chefs/test_comprehensive/test_advanced/test_auth/test_dishes/test_tools) to disappear, leaving only the drift clusters enumerated in Task 3.
  </action>
  <verify>
    <automated>cd backend && .venv/bin/python -c "
from httpx import AsyncClient, ASGITransport
from app.main import app
import asyncio
async def probe():
    async with AsyncClient(transport=ASGITransport(app=app), base_url='http://test') as c:
        a = await c.get('/api/categories/')
        b = await c.get('/api/categories')
        assert a.status_code == b.status_code, f'slash mismatch: {a.status_code} vs {b.status_code}'
        print(f'SLASH-NORMALIZE-PASS ({a.status_code} == {b.status_code})')
asyncio.run(probe())
" && .venv/bin/pytest tests/ -q --no-header 2>&1 | tail -2</automated>
  </verify>
  <done>
    - Dev extras installed; pytest runs from the venv
    - Middleware in place; `/api/categories/` no longer 405s
    - Full-suite baseline recorded — slash-cascade cluster collapsed
  </done>
</task>

<task type="auto">
  <name>Task 2: Smart-extractor graceful fallback + pinyin model-field drift cleanup</name>
  <files>
    backend/app/services/smart_ingredient_extractor.py
    backend/tests/test_misc_extra.py
    backend/tests/test_services_extra.py
  </files>
  <read_first>
    - backend/app/services/smart_ingredient_extractor.py (extract_ingredients at lines 98-145; _ensure_model_loaded at lines 86-96 — raises ImportError at line 93 when llama_cpp is missing)
    - backend/app/services/ingredient_extractor.py (basic extractor — returns {"matched": [...], "unmatched": []} at lines 62-65)
    - backend/app/routers/tools.py (_get_extractor at lines 26-33 already catches import-time ImportError but NOT load-time; post-processing at lines 52-55 tolerates the basic shape)
    - backend/tests/test_tools.py (asserts "matched"/"unmatched" in response — the basic shape satisfies it)
    - 16-RESEARCH.md "TD-09: Smart extractor graceful fallback" (lines 112-114)
  </read_first>
  <action>
    1. backend/app/services/smart_ingredient_extractor.py — in `extract_ingredients` (line 98), wrap the model-load step:
       - BEFORE `await self._ensure_model_loaded()` (line 99), change the flow to:
         ```
         try:
             await self._ensure_model_loaded()
         except ImportError:
             # TD-09: llama-cpp-python 未安装时优雅降级到基础抽取器（智能功能可选哲学）
             logger.warning("llama-cpp-python 未安装，降级到基础食材抽取器")
             from app.services.ingredient_extractor import ingredient_extractor
             return await ingredient_extractor.extract_ingredients(db, text)
         ```
       - Keep the rest of extract_ingredients unchanged (the LLM path still runs when the model loads). The basic return shape {"matched", "unmatched"} is compatible with tools.py post-processing (lines 52-55 add "ingredients" when absent) and with test_tools.py assertions.
       - Do NOT add llama-cpp-python to any dependency list; do NOT touch _load_model/_ensure_model_loaded internals.

    2. backend/tests/test_misc_extra.py — REMOVE the `pinyin=` kwarg from the Ingredient/Dish constructions at lines ~102 and ~137-138 (column removed in commit 13864e6). Delete the kwarg; keep name/category/is_active and any other current model fields.

    3. backend/tests/test_services_extra.py — REMOVE `pinyin=name` from `_create_ingredient` (line 41) and REMOVE the dead assertion `assert dish.pinyin is not None` (line 95 area). Also remove the `pinyin=` kwarg at line 60 and line 432 if present (grep the file for `pinyin` and clean every construction — the model has no such column).

    4. Re-run the affected test files: `.venv/bin/pytest tests/test_tools.py tests/test_misc_extra.py tests/test_services_extra.py -q`.
  </action>
  <verify>
    <automated>cd backend && (grep -c 'except ImportError' app/services/smart_ingredient_extractor.py | awk '{if ($1 >= 1) print "FALLBACK-PASS"; else exit 1}') && (grep -c 'pinyin' tests/test_misc_extra.py tests/test_services_extra.py | awk -F: '{if (($1 ~ /:/ && $2 == 0)) ok=1} END {print "PINYIN-GONE-PASS"}' && rg -c 'pinyin' tests/test_misc_extra.py tests/test_services_extra.py | awk '{if ($0 ~ /:0/) print "PINYIN-CLEAN"; else exit 1}') && .venv/bin/pytest tests/test_tools.py tests/test_misc_extra.py tests/test_services_extra.py -q 2>&1 | tail -2</automated>
  </verify>
  <done>
    - Smart extractor degrades gracefully without llama-cpp-python; test_tools.py passes (3 former ImportError failures closed)
    - Zero `pinyin=` kwargs / assertions remain in the test suite
  </done>
</task>

<task type="auto">
  <name>Task 3: Response-shape & behavior-drift alignment — app is the source of truth, target 0 failures</name>
  <files>
    backend/tests/test_feishu.py
    backend/tests/test_favorites.py
    backend/tests/test_routes_extra.py
    backend/tests/test_routes_final.py
  </files>
  <read_first>
    - 16-RESEARCH.md TD-09 root causes 4-6 (lines 29, 364-370) — the enumerated drift clusters
    - backend/app/schemas/ (response models are the source of truth for envelopes)
    - backend/tests/test_feishu.py (failing: send_notify_missing_params / send_notify_success / send_notify_failure / bind — 422 vs expected 400/403/200/500)
    - backend/tests/test_favorites.py (test_favorites_filter_dishes — KeyError 'id')
    - backend/tests/test_routes_extra.py (test_dish_update_and_delete / _update_status / test_user_get_not_found / test_dish_list_with_search)
    - backend/tests/test_routes_final.py (5-6 tests: dish update w/ relations, status missing field, order get-others, order cancel, category delete/update, favorite list)
    - backend/app/routers/feishu.py + backend/app/schemas/feishu request models (the 422 source)
  </read_first>
  <action>
    Run the full suite first: `.venv/bin/pytest tests/ -q --no-header 2>&1 | tail -5`. Then close EVERY remaining failure by aligning the TEST with the shipped app behavior (the app shipped through 15 phases — do NOT regress the app):

    1. test_feishu.py (4 fails — 422 validation drift): the request schema now rejects missing params earlier (422) than the old handler checks (400/403/500). Update assertions to the CURRENT contract: missing-params requests → 422 (assert response.status_code == 422, or assert the actual status the route returns — probe each failing case); forbidden/success/failure cases → assert the actual current status codes from the router. Do NOT loosen app validation.

    2. test_favorites.py::test_favorites_filter_dishes (KeyError 'id'): compare the actual envelope with backend/app/schemas/favorite.py (or the route's response_model) and update the assertion to index the field the API actually returns (e.g. item["dish_id"] or the nested dish object).

    3. test_routes_extra.py: 
       - test_dish_update_and_delete / _update_status: align with the actual dish response envelope (schemas/dish.py).
       - test_user_get_not_found: the auth dependency runs BEFORE existence checks — 401 for unauthenticated/unauthorized vs 404; update the expected status per the current auth-before-existence order.
       - test_dish_list_with_search / order cascade tests: re-run after Tasks 1-2 — most order tests collapse with the fixture fix; any remaining ones (search semantics, order detail/cancel/forbidden) get assertions aligned with dish_service/order_service behavior.

    4. test_routes_final.py (5-6 fails): align each with the shipped envelope (dish update with relations, status missing field, order get-others, order cancel, category delete/update, favorite list) — read the failing assertion, check the response_model in the corresponding router/schema, update the test.

    5. test_services_extra.py (if still failing): test_dish_list_with_filters (seed-data dependency — assert against the actual seeded/fixture state, e.g. len(dishes) == 0 is legitimate when no dishes match filters after clean_db), test_dish_sort_by_safety, test_order_* ValueError Chinese message drift — update expected messages to the CURRENT service strings (e.g. "以下菜品…" wording in order_service.py).

    6. Any other file still failing after Tasks 1-2: same rule — read the assertion, read the shipped router/service/schema, update the test. Do NOT disable tests, do NOT add blanket xfail/skip (except the pre-existing 4 skips), do NOT weaken the app.

    7. Final gate: `.venv/bin/pytest tests/ -q --no-header 2>&1 | tail -3` → 0 failed, 0 errors, 4 skipped (pre-existing).
  </action>
  <verify>
    <automated>cd backend && .venv/bin/pytest tests/ -q --no-header 2>&1 | tail -3</automated>
  </verify>
  <done>
    - Full suite: 0 failed, 0 errors (4 pre-existing skips acceptable)
    - No router/service/model regressed; no tests skipped/disabled to force green
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → middleware → routers | Untrusted HTTP paths cross the new normalization middleware — path rewriting must be strictly scoped to /api/* trailing slashes |
| tools extractor | User text flows into the smart extractor; the fallback path must return the same contract shape |
| test suite | Tests run against an in-memory SQLite DB via dependency_overrides — no production data touched |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03-T | Tampering | normalize_api_trailing_slash middleware | mitigate | Strict scope: only `startswith("/api/")`, excludes the bare `/api/`, only strips trailing slashes; raw_path rstrip limited to b"/"; non-API paths untouched (SPA catch-all behavior preserved); verified by the probe test (slash == no-slash status) |
| T-16-03-T | Tampering | smart extractor fallback | accept | Fallback returns the basic extractor's shape; input validation and DB queries unchanged — same trust level as the non-smart path |
| T-16-03-D | Denial of Service | test suite | accept | Suite runs locally against :memory: SQLite; no external services |
| T-16-03-SC | Supply Chain | uv sync --extra dev | mitigate | Installs only already-pinned lockfile entries (pytest 9.0.3 etc. resolved from uv.lock in-session; research Package Legitimacy Audit §"no new packages"); llama-cpp-python deliberately NOT installed |
</threat_model>

<verification>
- Probe: `/api/categories/` and `/api/categories` return identical status codes
- `cd backend && .venv/bin/pytest tests/ -q --no-header 2>&1 | tail -3` → 0 failed, 0 errors, 4 skipped
- `grep -c pinyin backend/tests/*.py` → 0 across the suite
- smart_ingredient_extractor.py has a fallback branch; `uv sync --extra dev` documented as the test-env bootstrap
</verification>

<success_criteria>
- TD-09: 108 failures → 0 failures / 0 errors across the full backend suite
- Middleware diff is the single-point fix (~10 lines); zero router path changes; zero frontend contract changes
- All test updates reflect shipped behavior; the app code changed only where research proved a real defect (extractor fallback)
</success_criteria>

<output>
Create .planning/phases/16-tech-debt-cleanup/16-03-SUMMARY.md when done
</output>
