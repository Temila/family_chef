---
phase: 10-primitive-components
plan: 03
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - frontend/src/css/styles.css
  - frontend/src/components/Badge.jsx
  - frontend/src/components/Sidebar.jsx
  - frontend/src/components/BottomBar.jsx
  - frontend/src/components/DishCard.jsx
  - frontend/src/components/WishCard.jsx
  - frontend/src/components/GuestDishCard.jsx
  - frontend/src/components/WishFormModal.jsx
  - frontend/src/components/WishRejectModal.jsx
  - frontend/src/components/WishAdvanceModal.jsx
  - frontend/src/components/InvitationsModal.jsx
  - frontend/src/components/InvitationsSection.jsx
  - frontend/src/components/CreateLinkModal.jsx
  - frontend/src/components/ConfirmModal.jsx
  - frontend/src/components/ChefSelectModal.jsx
  - frontend/src/pages/UserOrdersPage.jsx
  - frontend/src/pages/UserWishesPage.jsx
  - frontend/src/pages/UserFavoritesPage.jsx
  - frontend/src/pages/UserHomePage.jsx
  - frontend/src/pages/ChefDishesPage.jsx
  - frontend/src/pages/ChefOrdersPage.jsx
  - frontend/src/pages/ChefWishesPage.jsx
  - frontend/src/pages/AdminDishesPage.jsx
  - frontend/src/pages/AdminIngredientsPage.jsx
  - frontend/src/pages/AdminCategoriesPage.jsx
  - frontend/src/pages/AdminUsersPage.jsx
  - frontend/src/pages/AdminChefsPage.jsx
  - frontend/src/pages/AdminWishesPage.jsx
  - frontend/src/pages/AdminHomePage.jsx
  - frontend/src/pages/AdminStatsPage.jsx
  - frontend/src/pages/AdminLogsPage.jsx
  - frontend/src/pages/OrderPage.jsx
  - frontend/src/pages/OrderDetailPage.jsx
  - frontend/src/pages/DishDetailPage.jsx
  - frontend/src/pages/GuestOrderPage.jsx
  - frontend/src/pages/PreferencesPage.jsx
  - frontend/src/components/primitives/Badge.jsx
  - frontend/src/components/primitives/Badge.css
  - frontend/src/components/primitives/Chip.jsx
  - frontend/src/components/primitives/Chip.css
autonomous: true
requirements: [COMPO-06, COMPO-07, LOGIC-01, LOGIC-02, LOGIC-03]

must_haves:
  truths:
    - "Badge 组件支持 variant=assist|filter|state + tone=primary|secondary|tertiary|error|success|warn|info|muted；tone 映射 (warn→tertiary, danger→error, success→primary 等) 保留 statusBadge() 工具（D-15）— Badge.jsx 删除并重建为 primitives/Badge.jsx"
    - "Chip 组件支持 4 变体（assist/filter/input/suggestion）— filter 用 native button + aria-pressed，input 含独立 close action，assist/suggestion 静态（COMPO-07 / D-14）"
    - "40+ <Badge status={...}> 旧调用方迁移到新 Badge；向后兼容 adapter 保留旧 API (D-15 锁定)"
    - "10+ .filter-chip 调用方迁移到 <Chip variant='filter' selected={...} onClick={...} aria-pressed={...}>"
    - "styles.css 中 .badge / .badge-warn / .badge-danger / .badge-success / .badge-info / .badge-accent / .badge-gold / .badge-muted / .badge-count / .filter-chip 旧选择器全量删除（含 10-02 保留的 .form-input）"
    - "Sidebar / BottomBar count badge 迁移到新 Badge（state 变体 + error tone）"
    - "Chip filter 选中色用 secondary-container + on-secondary-container（MD3 spec），border transparent；保留 18dp check slot 防止 width jump（D-14）"
  artifacts:
    - path: "frontend/src/components/primitives/Badge.jsx"
      provides: "Badge variant × tone matrix + backward-compat <Badge status> / <Badge text type> adapter + optional leadingIcon"
      exports: ["default"]
    - path: "frontend/src/components/primitives/Badge.css"
      provides: ".md-badge 24dp pill + 8 色调色板 + 3 variant 尺寸 + count 紧凑变体"
      contains: ".md-badge--state"
    - path: "frontend/src/components/primitives/Chip.jsx"
      provides: "Chip 4 variants (assist/filter/input/suggestion) + filter aria-pressed + input onRemove + optional leading icon"
      exports: ["default"]
    - path: "frontend/src/components/primitives/Chip.css"
      provides: ".md-chip 32dp pill (48dp hit) + 4 variant 颜色 + check slot 18dp reserved"
      contains: ".md-chip--filter"
  key_links:
    - from: "frontend/src/components/primitives/Badge.jsx"
      to: "frontend/src/utils/index.js"
      via: "import { statusBadge }; statusBadge() cls→tone mapping 在 Badge 内部 (D-15)"
    - from: "frontend/src/components/primitives/Badge.jsx"
      to: "frontend/src/components/Badge.jsx (旧)"
      via: "旧 Badge.jsx 整个替换为 primitives/Badge.jsx 的 re-export shim 或直接删除旧文件 (10-01 之后的 Phase 10 Wave 2 期间 DishCard/WishCard 等 import path 待更新)"
    - from: "frontend/src/components/primitives/Chip.jsx"
      to: "frontend/src/components/primitives/Icon.jsx"
      via: "<Icon name={leadingIcon} size={18} />"
    - from: "frontend/src/components/Sidebar.jsx"
      to: "frontend/src/components/primitives/Badge.jsx"
      via: "<Badge tone='error'>{count}</Badge> 替换 span.badge-count"
    - from: "frontend/src/pages/ChefOrdersPage.jsx"
      to: "frontend/src/components/primitives/Chip.jsx"
      via: "import Chip; <Chip variant='filter' selected={...} onClick={...}>全部</Chip>"
---

<objective>
**Phase 10, Plan 03 — Wave 2 (parallel to 10-02): Badge primitive (3 variants × 8 tones) + Chip primitive (4 variants) + 40+ Badge 调用迁移 + 10+ filter-chip 迁移 + 旧 .badge-* / .filter-chip CSS 删除**

为前端落地 2 个 Phase 10 primitive（Badge 视觉-only + tone 调色板、Chip 4 变体可交互），同时完成业务语义保留下的全量换皮：保留 `statusBadge()` 工具不变（D-15），将旧 `<Badge status={...}>` 调用迁移到新 `<Badge variant="state" tone={...}>`（提供向后兼容 adapter 兜底），将 10+ `.filter-chip` 调用迁移到 `<Chip variant="filter" selected={...}>`，最后删除 styles.css 中已无消费者依赖的 `.badge / .badge-warn / .badge-danger / .badge-success / .badge-info / .badge-accent / .badge-gold / .badge-muted / .badge-count / .filter-chip` 旧选择器。**.form-input 保留**（Phase 11 Select primitive 上线时处理 select 兜底，与 10-02 SC-10 一致）。

**Purpose:** 这是 Phase 10 primitive 库**最后两个组件**的换皮。Badge 与 Chip 是 MD3 中语义最微妙的两个 primitive——Badge 是视觉-only 不可交互，Chip 是 4 种交互模式（assist 默认静态、filter 切换、input 可删除、suggestion 静态带 icon），Chip filter 选中色采用 secondary-container（不是 primary），count badge 强制 24dp 高度最小值。完成这两个 primitive 后，前端 primitive 库就具备 Phase 11 复合组件（Modal/Snackbar/List Item/Navigation）所需的全部基础。

**Output:** 2 个新 primitive (Badge + Chip) + 2 个新 primitive CSS + 旧 Badge.jsx 整体替换为 primitives/Badge.jsx + 40+ Badge 调用迁移 + 10+ filter-chip 迁移 + 旧 CSS 选择器清理。`npm run lint` 与 `npm run build` 0 error，零业务逻辑回归（LOGIC-01/02/03）。

**Sentinel-marker note (per BLOCKER 1):** 本 Plan 与 10-02 同属 Wave 2 并行执行，两者修改同一组共享文件（styles.css + 20+ page 文件 + DishCard/WishCard + 多个 modal）。为防止合并冲突，本 Plan 在所有共享文件中的所有编辑都封装在 sentinel 标记块内：
- CSS 文件：`/* === 10-03-MIGRATION:START === */` ... `/* === 10-03-MIGRATION:END === */`
- JSX 文件：`{/* === 10-03-MIGRATION:START === */}` ... `{/* === 10-03-MIGRATION:END === */}`

执行器仅在 10-03 sentinel 块内编辑；10-02 拥有的 `/* === 10-02-MIGRATION === */` 块（用于 Card/Input 迁移）由 10-02 plan 单独编辑，两者完全不相交。

</objective>

<execution_context>
@/home/temila/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/temila/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md (COMPO-06, COMPO-07)
@.planning/ROADMAP.md (Phase 10 success criteria 5)
@.planning/STATE.md
@.planning/phases/10-primitive-components/10-CONTEXT.md (D-14 Chip 4 变体 / D-15 Badge tone 保留 statusBadge() / D-13 wish-card-actions 保留)
@.planning/phases/10-primitive-components/10-UI-SPEC.md (§11 Badge 契约 / §12 Chip 契约)
@.planning/phases/10-primitive-components/10-PATTERNS.md:296-360 (Badge/Chip 模拟模式 + 偏差列表)
@.planning/phases/10-primitive-components/10-01-PLAN.md (Wave 1 完成的 primitives 基础 + Icon tree-shaking)
@.planning/phases/10-primitive-components/10-02-PLAN.md (Wave 2 Card + Input 已完成)
@frontend/src/css/tokens.css
@frontend/src/css/styles.css
@frontend/src/components/Badge.jsx (旧 Badge — Task 1 整体替换)
@frontend/src/utils/index.js (statusBadge() 工具 — D-15 不动)
@frontend/src/components/DishCard.jsx
@frontend/src/components/WishCard.jsx
@frontend/src/components/GuestDishCard.jsx
@frontend/src/components/Sidebar.jsx (badge-count 内联使用)
@frontend/src/components/BottomBar.jsx (badge-count 内联使用)
@frontend/src/components/primitives/Button.jsx (10-01 — DishCard footer 内 Badge 调用迁移后, Badge 内部不再依赖 Button)
@frontend/src/components/primitives/Card.jsx (10-02)
@frontend/src/components/primitives/Icon.jsx (10-01)
@frontend/src/pages/ChefOrdersPage.jsx (典型 filter-chip 模式)
@frontend/src/pages/ChefWishesPage.jsx (典型 filter-chip 模式)
@frontend/src/pages/AdminDishesPage.jsx (典型 filter-chip 模式)
@frontend/src/pages/AdminIngredientsPage.jsx (典型 filter-chip 模式)
@frontend/src/pages/AdminUsersPage.jsx (典型 filter-chip 模式)
@frontend/src/pages/UserOrdersPage.jsx (典型 filter-chip 模式)
@frontend/src/pages/GuestOrderPage.jsx (filter-chip 模式 + 复杂 select 区域)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Badge primitive (3 variants × 8 tones + backward-compat) + 旧 Badge.jsx 替换为 primitives/Badge.jsx + 40+ 调用迁移 + 旧 .badge-* / .form-input CSS 删除</name>

  <files>
    frontend/src/components/primitives/Badge.jsx,
    frontend/src/components/primitives/Badge.css,
    frontend/src/components/Badge.jsx (旧 — 删除),
    frontend/src/components/Sidebar.jsx,
    frontend/src/components/BottomBar.jsx,
    frontend/src/components/DishCard.jsx,
    frontend/src/components/WishCard.jsx,
    frontend/src/components/GuestDishCard.jsx,
    frontend/src/components/WishFormModal.jsx,
    frontend/src/components/WishRejectModal.jsx,
    frontend/src/components/WishAdvanceModal.jsx,
    frontend/src/components/InvitationsModal.jsx,
    frontend/src/components/InvitationsSection.jsx,
    frontend/src/components/CreateLinkModal.jsx,
    frontend/src/components/ConfirmModal.jsx,
    frontend/src/components/ChefSelectModal.jsx,
    frontend/src/pages/UserOrdersPage.jsx,
    frontend/src/pages/UserWishesPage.jsx,
    frontend/src/pages/UserFavoritesPage.jsx,
    frontend/src/pages/UserHomePage.jsx,
    frontend/src/pages/ChefDishesPage.jsx,
    frontend/src/pages/ChefOrdersPage.jsx,
    frontend/src/pages/ChefWishesPage.jsx,
    frontend/src/pages/AdminDishesPage.jsx,
    frontend/src/pages/AdminIngredientsPage.jsx,
    frontend/src/pages/AdminCategoriesPage.jsx,
    frontend/src/pages/AdminUsersPage.jsx,
    frontend/src/pages/AdminChefsPage.jsx,
    frontend/src/pages/AdminWishesPage.jsx,
    frontend/src/pages/AdminHomePage.jsx,
    frontend/src/pages/AdminStatsPage.jsx,
    frontend/src/pages/AdminLogsPage.jsx,
    frontend/src/pages/OrderPage.jsx,
    frontend/src/pages/OrderDetailPage.jsx,
    frontend/src/pages/DishDetailPage.jsx,
    frontend/src/pages/GuestOrderPage.jsx,
    frontend/src/pages/PreferencesPage.jsx,
    frontend/src/css/styles.css
  </files>

  <read_first>
    .planning/phases/10-primitive-components/10-UI-SPEC.md §11 (Badge API + 8 tones + variant matrix + count 紧凑变体)
    .planning/phases/10-primitive-components/10-PATTERNS.md:296-322 (Badge.jsx + Badge.css 模拟模式 + D-15 backward-compat)
    .planning/phases/10-primitive-components/10-CONTEXT.md D-15 (Badge 视觉 + statusBadge 保留 + 向后兼容 adapter)
    frontend/src/css/tokens.css:16-50 (--md-color-primary-container / on-primary-container / secondary-container / tertiary-container / error-container / surface-container 完整调色板)
    frontend/src/components/Badge.jsx:1-17 (旧 Badge — 整体替换目标)
    frontend/src/utils/index.js:52-78 (statusBadge() 工具 — 完整保留)
    frontend/src/css/styles.css:131-150 (.badge + .badge-* + .badge-count + .filter-chips + .filter-chip 完整源 — 删除目标)
    frontend/src/components/DishCard.jsx (10-02 重构后已使用 inline className="badge badge-info" 等 — 替换为新 Badge)
    frontend/src/components/WishCard.jsx (10-02 重构后已使用 `<Badge status={...}>` 旧 import — 替换为新 Badge primitives)
    frontend/src/components/Sidebar.jsx:6-10 (内联 Badge 函数 — 替换为新 Badge)
    frontend/src/components/BottomBar.jsx:5-9 (内联 Badge 函数 — 替换为新 Badge)
    frontend/src/pages/AdminUsersPage.jsx:190, 234 (典型 inline className="badge badge-success" / "badge-danger" — 替换为新 Badge)
  </read_first>

  <action>
    === A. 新建 frontend/src/components/primitives/Badge.jsx ===
    - docstring: "MD3 Badge Primitive (Phase 10 — COMPO-06) — visual-only (no role/button/tabIndex); 3 variants (assist/filter/state) × 8 tones (primary/secondary/tertiary/error/success/warn/info/muted); backward-compat <Badge status> / <Badge text type> adapter 保留 (D-15)"
    - imports: `import { statusBadge } from '../../utils'; import Icon from './Icon'; import './Badge.css';`
    - **Tone 映射 (D-15)**: `const CLS_TO_TONE = { 'badge-warn': 'warn', 'badge-danger': 'error', 'badge-success': 'success', 'badge-info': 'info', 'badge-accent': 'primary', 'badge-gold': 'tertiary', 'badge-muted': 'muted' };`
    - 主组件: `function Badge({ variant = 'state', tone, status, text, type, leadingIcon, children, className = '', ...rest }) { ... }`
    - 优先级逻辑 (D-15 backward-compat):
      1. 若传 `status` prop → 调用 `statusBadge(status)` 取 `text + cls` → 内部 `tone = CLS_TO_TONE[cls] || 'info'`；render children 为 `text`
      2. 否则若传 `text` prop → 内部 `tone = CLS_TO_TONE[\`badge-\${type || 'info'}\`] || 'info'`；render children 为 `text`
      3. 否则使用显式 `tone` prop + `children` (新 API 模式)
    - classes: `['md-badge', \`md-badge--\${variant}\`, \`md-badge--\${tone}\`, className].filter(Boolean).join(' ')`
    - 渲染: `<span className={classes} {...rest}>{leadingIcon && <Icon name={leadingIcon} size={14} className="md-badge__icon" />}{resolvedText || children}</span>`
    - count 变体: 接受 `count` prop (number) → 自动渲染紧凑 count badge (mini pill, 单字符优先) `<span className="md-badge md-badge--count md-badge--error">{count > 99 ? '99+' : count}</span>` (count 与 tone 互斥；count 强制 error tone + count variant)
    - **禁止**: role/button/tabIndex/onClick — Badge 视觉-only；如需交互应使用 Chip variant="filter"
    - 默认 export `Badge`

    === B. 新建 frontend/src/components/primitives/Badge.css ===
    - 顶部 `@import './base.css';`
    - 根 `.md-badge`: `display: inline-flex; align-items: center; gap: 4px; padding: 4px 8px; min-block-size: 24px; border-radius: var(--md-radius-full); font-family: var(--md-font-body); font-size: 12px; font-weight: 500; line-height: 16px; white-space: nowrap; vertical-align: middle;`
    - `.md-badge__icon { font-size: 14px; line-height: 1; }`
    - 变体尺寸: `.md-badge--assist { padding: 0 8px; min-block-size: 20px; font-size: 11px; }` `.md-badge--filter { padding: 4px 12px; min-block-size: 24px; }` `.md-badge--state { /* default sizes */ }`
    - 8 tones (按 UI-SPEC §11.2 tone family):
      * `.md-badge--primary { background: var(--md-color-primary-container); color: var(--md-color-on-primary-container); }`
      * `.md-badge--secondary { background: var(--md-color-secondary-container); color: var(--md-color-on-secondary-container); }`
      * `.md-badge--tertiary { background: var(--md-color-tertiary-container); color: var(--md-color-on-tertiary-container); }`
      * `.md-badge--error { background: var(--md-color-error-container); color: var(--md-color-on-error-container); }`
      * `.md-badge--success { background: var(--md-color-primary-container); color: var(--md-color-on-primary-container); }`（per UI-SPEC §11.2 success 复用 primary-container；语义上 OK）
      * `.md-badge--warn { background: var(--md-color-tertiary-container); color: var(--md-color-on-tertiary-container); }`（复用 tertiary-container 同 tertiary tone）
      * `.md-badge--info { background: var(--md-color-secondary-container); color: var(--md-color-on-secondary-container); }`（复用 secondary-container 同 secondary tone）
      * `.md-badge--muted { background: var(--md-color-surface-container-high); color: var(--md-color-on-surface-variant); }`
    - count 紧凑变体: `.md-badge--count { min-width: 24dp → min-inline-size: 24px; min-block-size: 24px; padding: 0 4px; font-size: 11px; font-weight: 700; background: var(--md-color-error); color: var(--md-color-on-error); }` 注: 不使用 18px 旧值（UI-SPEC §11.2 末段 "单字符仍 minimum inline 24dp"）

    === C. 删除旧 frontend/src/components/Badge.jsx ===
    - `rm frontend/src/components/Badge.jsx`
    - 现有 `<Badge status={...}>` / `<Badge text type>` 调用方的 import 路径需同步更新:
      1. `import Badge from './Badge'` (components 同目录) → `import Badge from './primitives/Badge'`
      2. `import Badge from '../components/Badge'` (pages) → `import Badge from '../components/primitives/Badge'`
    - **不修改** 旧 API 调用形式（如 `<Badge status={wish.status} />` 仍 work — D-15 backward-compat）

    === D. 40+ Badge 调用迁移 ===
    **Sentinel scope**: 本步骤的所有 Badge 迁移编辑位于 `{/* === 10-03-MIGRATION:START === */}` 与 `{/* === 10-03-MIGRATION:END === */}` 标记对内部；不要触碰 10-02 plan 拥有的 `{/* === 10-02-MIGRATION === */}` 块（用于 Card/Input 迁移）。DishCard/WishCard 等 10-02 已重构的文件中，本 Plan 只在 10-03 sentinel 块内编辑（替换 inline `className="badge badge-info"` 等为 `<Badge tone="info">`，或更新 `<Badge status>` 的 import path 为 primitives/Badge）。
    执行模板（每个 page/component 文件）：在原 Badge 调用所在 JSX 节点上方插入 `{/* === 10-03-MIGRATION:START === */}`，在最后一个 Badge 调用闭合下方插入 `{/* === 10-03-MIGRATION:END === */}`。
    **策略**: 利用 D-15 backward-compat 能力，**绝大多数调用方零改动**——只需把 import path 改为 `'./primitives/Badge'` 或 `'../components/primitives/Badge'`，旧 `<Badge status={...}>` 形式继续工作；新调用方（如 Sidebar/BottomBar count badge）显式使用 `tone="error"` 新 API。

    迁移清单（按文件，仅列 import path 变更与显著新 API 调用）:
    1. `frontend/src/components/DishCard.jsx` — `import Badge from './primitives/Badge'`（10-02 重构时未 import — 旧 inline className 需改为 `<Badge tone="tertiary">推荐</Badge>` / `<Badge tone="info">...</Badge>` / `<Badge tone={dish.is_available ? 'success' : 'error'}>{...}</Badge>`）
    2. `frontend/src/components/WishCard.jsx` — `import Badge from './primitives/Badge'`；`<Badge status={wish.status} />` 保留（D-15 自动 tone 映射）
    3. `frontend/src/components/GuestDishCard.jsx` — 无 Badge 调用
    4. `frontend/src/components/WishFormModal.jsx` — import path 更新
    5. `frontend/src/components/WishRejectModal.jsx` — import path 更新
    6. `frontend/src/components/WishAdvanceModal.jsx` — import path 更新
    7. `frontend/src/components/InvitationsModal.jsx` — import path 更新
    8. `frontend/src/components/InvitationsSection.jsx` — import path 更新
    9. `frontend/src/components/CreateLinkModal.jsx` — import path 更新
    10. `frontend/src/components/ConfirmModal.jsx` — 视情况（无 Badge 调用则跳过）
    11. `frontend/src/components/ChefSelectModal.jsx` — 视情况
    12. `frontend/src/components/Sidebar.jsx:6-10` — 内联 `function Badge({ count })` 删除，import `import Badge from './primitives/Badge';`；使用 `<Badge count={pendingCount} />` 新 API（count 自动渲染紧凑变体）
    13. `frontend/src/components/BottomBar.jsx:5-9` — 同 Sidebar
    14. `frontend/src/pages/AdminUsersPage.jsx:190, 234` — `<span className={\`badge \${u.is_active ? 'badge-success' : 'badge-danger'}\`}>` → `<Badge tone={u.is_active ? 'success' : 'error'}>{u.is_active ? '活跃' : '停用'}</Badge>` (新 API)
    15. `frontend/src/pages/AdminDishesPage.jsx` — 多个 inline className="badge badge-*" → 新 API（featured gold → `tone="tertiary"`）
    16. `frontend/src/pages/AdminChefsPage.jsx` / `AdminCategoriesPage.jsx` / `AdminWishesPage.jsx` / `AdminIngredientsPage.jsx` / `AdminStatsPage.jsx` / `AdminHomePage.jsx` / `AdminLogsPage.jsx` — 视情况迁移
    17. `frontend/src/pages/OrderPage.jsx` — 订单状态 badge → 新 API
    18. `frontend/src/pages/OrderDetailPage.jsx` — 订单状态 badge → 新 API
    19. `frontend/src/pages/DishDetailPage.jsx` — 菜品状态 badge（草稿/已上架/已下架）→ 新 API
    20. `frontend/src/pages/ChefDishesPage.jsx` / `ChefOrdersPage.jsx` / `ChefWishesPage.jsx` — 订单/菜品状态 badge
    21. `frontend/src/pages/UserOrdersPage.jsx` / `UserWishesPage.jsx` / `UserFavoritesPage.jsx` / `UserHomePage.jsx` — 状态 badge
    22. `frontend/src/pages/GuestOrderPage.jsx` — 订单/菜品状态 badge
    23. `frontend/src/pages/PreferencesPage.jsx` — 偏好类别 (dislike / allergy) 标签 — **不在 10-03 范围**（Phase 12 决定是否迁移为 `<Chip variant="filter">` 模式）

    === E. 删除 styles.css 旧 .badge-* 选择器 ===
    **Sentinel scope**: 本步骤的所有 CSS 编辑位于 `/* === 10-03-MIGRATION:START === */` 与 `/* === 10-03-MIGRATION:END === */` 标记对内部；不要触碰 10-02 plan 拥有的 `/* === 10-02-MIGRATION === */` 块（用于 .card / .dish-card / .wish-card / .form-label 等删除）。两者在 styles.css 中位于不相交的行号范围。
    **执行顺序**: 在执行本步骤 E 之前，先在 styles.css 中定位以下旧选择器的行号范围，然后用 sentinel 标记包裹本 Plan 要删除的整段 CSS 块：
    - 删 line 131-141 (.badge + .badge-warn/danger/success/info/accent/gold/muted + .badge-count 完整块) — 由 .md-badge + 8 tone className 接管
    - 删 line 144 (.filter-chips 容器) — 由 Chip primitive 内部 flex 接管；调用方在父容器自定 flex/gap
    - **特别处理**: line 145-150 (.filter-chip 完整块) — **本 Task 末尾删除**（Task 2 迁移完所有 .filter-chip 后才删）
    - 同步删除 line 569 (.filter-chip:focus-visible) — 由 .md-chip 自带
    - 同步删除 line 577 (.filter-chip.active:focus-visible) — 由 .md-chip--filter--selected 自带
    - **删除 .form-input 兜底选择器**（10-02 末尾保留的兼容性）:
      * `grep -rE 'className=.*\bform-input\b' src/ --include='*.jsx' | grep -vE '\bselect\b'` 期望 = 0
      * `grep -rE 'className=.*\bform-input\b' src/ --include='*.jsx' | grep 'select'` 期望 = 0（select 已在 AdminIngredientsPage 等迁移至 native `<select className="md-select">` 临时 className — **不强制迁移到 Input primitive**；Phase 11 Select 组件处理）
      * **方案 B (更安全)**: 实际 AdminIngredients 1 select 仍未迁移到任何 primitive（Input primitive 不支持 select），且其他文件 select 模式零星（如 ForceChangePasswordPage 可能 select 角色）— **保留 .form-input 兜底至 Phase 11**；本 Task 末尾**不删 .form-input**
      * **最终**: Task 1 末尾**仅删 .badge-* + .badge-count 选择器**；**.form-input 与 .filter-chip 留至 Task 2 末尾**
    - 同步更新全局 min-size 规则 (line 622 区域) 移除 `.filter-chip,` 与 `.btn-icon,` (10-01 已删) 引用

    === F. 验证迁移完整性 ===
    `grep -rE 'className=.*\b(badge-warn|badge-danger|badge-success|badge-info|badge-accent|badge-gold|badge-muted|badge-count)\b' frontend/src/ --include='*.jsx'` 应 0 命中；`grep "badge-count" frontend/src/components/Sidebar.jsx frontend/src/components/BottomBar.jsx` 0 命中（已迁移为新 `<Badge count>`）；`test ! -f frontend/src/components/Badge.jsx` 通过（旧文件已删除）；`grep -r "from '../components/Badge'" frontend/src/ --include='*.jsx' | wc -l` 0 命中（旧 import path 已全部更新到 primitives/Badge）

  </action>

<verify>
  <automated>cd frontend && npm run lint 2>&1 | tail -20; npm run build 2>&1 | tail -10; grep -rE 'className=.*\b(badge-warn|badge-danger|badge-success|badge-info|badge-accent|badge-gold|badge-muted|badge-count)\b' src/ --include='*.jsx' | wc -l; test ! -f src/components/Badge.jsx && echo "OK old Badge removed"; grep -cE "^\.(badge|badge-warn|badge-danger|badge-success|badge-info|badge-accent|badge-gold|badge-muted|badge-count)\s*\{" src/css/styles.css; echo '---Sentinel marker check---'; grep -c "10-03-MIGRATION" src/css/styles.css; grep -c "10-02-MIGRATION" src/css/styles.css</automated>
</verify>

  <acceptance_criteria>
    1. `frontend/src/components/primitives/Badge.jsx` 存在；`grep -E "md-badge--(assist|filter|state|count)" frontend/src/components/primitives/Badge.jsx` 命中 4 个 variant 字符串；`grep -E "md-badge--(primary|secondary|tertiary|error|success|warn|info|muted)" frontend/src/components/primitives/Badge.jsx` 命中 ≥ 8 个 tone 字符串；`grep "CLS_TO_TONE" frontend/src/components/primitives/Badge.jsx` 命中 tone 映射定义；`grep "statusBadge" frontend/src/components/primitives/Badge.jsx` 命中 D-15 backward-compat 调用。
    2. `frontend/src/components/primitives/Badge.css` 存在；`grep "var(--md-radius-full)" frontend/src/components/primitives/Badge.css` 命中 pill 圆角；`grep "var(--md-color-primary-container)" frontend/src/components/primitives/Badge.css` 命中 primary tone；`grep -E "^\.md-badge" frontend/src/components/primitives/Badge.css` 命中 ≥ 12 个 class (根 + 4 variant + 8 tone + count + icon)。
    3. 旧 `frontend/src/components/Badge.jsx` 已删除（`test ! -f` 通过）；所有 `<Badge>` 调用的 import path 已更新到 `primitives/Badge`（`grep -r "from '../components/Badge'\|from './Badge'" src/ --include='*.jsx' | grep -v primitives | wc -l` = 0）。
    4. Sidebar / BottomBar 内联 `function Badge` 组件删除，替换为 `<Badge count={pendingCount} />` 新 API（count 自动紧凑变体）；`grep -E "function Badge" src/components/Sidebar.jsx src/components/BottomBar.jsx` 0 命中。
    5. AdminUsersPage inline `<span className="badge badge-success">` / `badge-danger` 替换为新 API（`tone="success"` / `tone="error"`）；DishCard 推荐/可点/已售罄 Badge 替换为新 API（`tone="tertiary"` / `tone="success"` / `tone="error"`）。
    6. WishCard `<Badge status={wish.status} />` 保留旧 API（D-15 backward-compat 仍工作）；WishCard 视觉无变化（自动 tone 映射到 tertiary/info/success/error/muted）。
    7. styles.css 删除 `.badge / .badge-warn / .badge-danger / .badge-success / .badge-info / .badge-accent / .badge-gold / .badge-muted / .badge-count` 旧选择器（`grep -cE "^\.(badge|badge-warn|badge-danger|badge-success|badge-info|badge-accent|badge-gold|badge-muted|badge-count)\s*\{" src/css/styles.css` = 0）。
    8. styles.css 删除 `.filter-chips` 容器选择器（`grep -c "^\.filter-chips" src/css/styles.css` = 0）；**.filter-chip 仍保留**（Task 2 末尾删除）。
    9. `cd frontend && npm run lint` 0 新增 error。
    10. `cd frontend && npm run build` 0 error。
  </acceptance_criteria>

  <done>Badge primitive 3 variants × 8 tones 就位；D-15 backward-compat adapter 保留 statusBadge() 与旧 `<Badge status>` API；40+ Badge 调用迁移完成（旧调用方 import path 更新 + 新调用方使用新 API）；旧 Badge.jsx 删除；styles.css .badge-* + .badge-count 旧选择器删除；Sidebar/BottomBar count badge 迁移到新 API；lint/build 通过；零业务逻辑回归。</done>
</task>

<task type="auto">
  <name>Task 2: Chip primitive (4 variants) + 10+ filter-chip 调用迁移 + 旧 .filter-chip / .form-input CSS 删除</name>

  <files>
    frontend/src/components/primitives/Chip.jsx,
    frontend/src/components/primitives/Chip.css,
    frontend/src/pages/ChefDishesPage.jsx,
    frontend/src/pages/ChefOrdersPage.jsx,
    frontend/src/pages/ChefWishesPage.jsx,
    frontend/src/pages/AdminDishesPage.jsx,
    frontend/src/pages/AdminIngredientsPage.jsx,
    frontend/src/pages/AdminUsersPage.jsx,
    frontend/src/pages/AdminCategoriesPage.jsx,
    frontend/src/pages/GuestOrderPage.jsx,
    frontend/src/pages/UserOrdersPage.jsx,
    frontend/src/css/styles.css
  </files>

  <read_first>
    .planning/phases/10-primitive-components/10-UI-SPEC.md §12 (Chip variant semantics + geometry + checkmark + trailing micro-motion + selected colors)
    .planning/phases/10-primitive-components/10-PATTERNS.md:324-360 (Chip.jsx + Chip.css 模拟模式 + 4 variant 行为)
    .planning/phases/10-primitive-components/10-CONTEXT.md D-14 (4 变体全实现 + 静态/交互判定)
    frontend/src/css/tokens.css:147 (--md-radius-full for Chip pill)
    frontend/src/css/styles.css:144-150 (.filter-chips + .filter-chip 完整源 — 删除目标)
    frontend/src/pages/ChefOrdersPage.jsx:78-104 (典型 filter-chip 模式, 4 个状态切换)
    frontend/src/pages/ChefWishesPage.jsx:308-328 (filter-chip 模式 + 选中 active)
    frontend/src/pages/AdminIngredientsPage.jsx:275, 284 (filter-chip 模式 + 多个 category)
    frontend/src/pages/AdminUsersPage.jsx:145 (filter-chip 角色过滤)
    frontend/src/pages/GuestOrderPage.jsx:251-321 (filter-chip 区域/菜系/allergies 多种)
    frontend/src/pages/UserOrdersPage.jsx:70-88 (filter-chip 4 个状态)
    frontend/src/pages/AdminDishesPage.jsx (line 438, 490, 534, 778, 787, 984 — 6 处 filter-chip)
  </read_first>

  <action>
    === A. 新建 frontend/src/components/primitives/Chip.jsx ===
    - docstring: "MD3 Chip Primitive (Phase 10 — COMPO-07) — 4 variants (assist/filter/input/suggestion); filter 用 native button + aria-pressed; input 含独立 onRemove close action; assist/suggestion 默认 static; 静态变体传 onClick 自动升级为 interactive button (48dp hit)"
    - imports: `import Icon from './Icon'; import './Chip.css';`
    - `function Chip({ variant = 'assist', selected = false, leadingIcon, onRemove, onClick, disabled = false, className = '', children, ...rest }) { ... }`
    - classes: `['md-chip', \`md-chip--\${variant}\`, selected && variant === 'filter' && 'md-chip--selected', disabled && 'md-chip--disabled', className].filter(Boolean).join(' ')`
    - 渲染:
      1. variant === 'filter': `<button type="button" className={classes} onClick={onClick} aria-pressed={selected} disabled={disabled} {...rest}><span className="md-chip__check" aria-hidden="true"><Icon name="check" size={18} /></span>{leadingIcon && <Icon name={leadingIcon} size={18} className="md-chip__leading-icon" />}<span className="md-chip__label">{children}</span></button>`（**18dp check slot 始终保留**——selected 时 visibility:visible; 未选时 visibility:hidden 但占 18dp 防止 width jitter，per UI-SPEC §12.3 末段）
      2. variant === 'input': `<span className="md-chip__interactive-wrapper"><span className={classes}><span className="md-chip__label">{leadingIcon && <Icon name={leadingIcon} size={18} />}{children}</span><button type="button" className="md-chip__remove" onClick={onRemove} aria-label={\`移除\${children}\`}><Icon name="close" size={18} /></button></span></span>`
      3. variant === 'assist' / 'suggestion': `<span className={classes}>{leadingIcon && <Icon name={leadingIcon} size={18} className="md-chip__leading-icon" />}<span className="md-chip__label">{children}</span></span>`（静态；若 onClick 传入则升级为 `<button>` tag）
    - interactive 升级 (assist/suggestion + onClick): `if ((variant === 'assist' || variant === 'suggestion') && onClick) return <button type="button" className={classes} onClick={onClick} disabled={disabled} {...rest}>...{children}...</button>;`
    - 触控目标: filter/input 默认 48dp hit box；静态 assist/suggestion 32dp visual
    - 默认 export `Chip`

    === B. 新建 frontend/src/components/primitives/Chip.css ===
    - 顶部 `@import './base.css';`
    - 根 `.md-chip`: `display: inline-flex; align-items: center; gap: 8px; min-block-size: 32px; padding: 0 12px; border-radius: var(--md-radius-full); font-family: var(--md-font-body); font-size: 14px; font-weight: 500; line-height: 20px; white-space: nowrap; background: transparent; color: var(--md-color-on-surface-variant); border: 1px solid var(--md-color-outline-variant); cursor: default; transition: color var(--md-motion-duration-short) var(--md-motion-easing-standard), background var(--md-motion-duration-short) var(--md-motion-easing-standard), border-color var(--md-motion-duration-short) var(--md-motion-easing-standard);`
    - 触控 hit (interactive): `.md-chip--filter, .md-chip--input { min-block-size: 48px; }`（filter input 强制 48dp hit）
    - 变体颜色:
      * `.md-chip--filter:hover:not(:disabled) { color: var(--md-color-on-surface); border-color: var(--md-color-outline); background: var(--md-color-surface-container-low); }`
      * `.md-chip--filter:focus-visible { outline: var(--md-focus-ring-outer); outline-offset: 2px; }`
      * `.md-chip--filter.md-chip--selected { background: var(--md-color-secondary-container); color: var(--md-color-on-secondary-container); border-color: transparent; }`（**selected 用 secondary-container 而非 primary**——per UI-SPEC §12.2 + D-14）
      * `.md-chip--filter.md-chip--selected:focus-visible { outline: var(--md-focus-ring-inner); outline-offset: 2px; }`（container 背景用内环）
      * `.md-chip--input { padding-right: 4px; background: var(--md-color-surface-container-low); }` `.md-chip--input .md-chip__remove { background: transparent; border: none; cursor: pointer; padding: 4px; border-radius: var(--md-radius-full); display: inline-flex; align-items: center; justify-content: center; color: var(--md-color-on-surface-variant); transition: background var(--md-motion-duration-short) var(--md-motion-easing-standard); }` `.md-chip__remove:hover { background: var(--md-color-surface-container-high); color: var(--md-color-on-surface); }` `.md-chip__remove:focus-visible { outline: var(--md-focus-ring-outer); outline-offset: 2px; }`
      * `.md-chip--assist, .md-chip--suggestion { /* default 静态, 无 cursor pointer, 无 ripple */ }` `.md-chip--assist[role='button'], .md-chip--suggestion[role='button'] { cursor: pointer; }`
    - Check slot (UI-SPEC §12.3): `.md-chip__check { display: inline-flex; align-items: center; justify-content: center; min-inline-size: 18px; opacity: 0; transform: scale(0.8); transition: opacity var(--md-motion-duration-short) var(--md-motion-easing-standard), transform var(--md-motion-duration-short) var(--md-motion-easing-standard); }` `.md-chip--selected .md-chip__check { opacity: 1; transform: scale(1); }`
    - 禁止: 不应用 `.md-interactive` 类的 ripple 反馈（Chip 走 state-layer 而非 ripple——per UI-SPEC §12.3 末段）；filter 用 `:hover` 直接变色，state-layer 由 base.css 接管（filter 自身就是 interactive 元素）
    - 禁用: `.md-chip--disabled, .md-chip:disabled { opacity: 0.38; cursor: not-allowed; }`
    - reduced-motion: `@media (prefers-reduced-motion: reduce) { .md-chip__check { transition: none; } }`

    === C. 10+ filter-chip 调用迁移 ===
    **Sentinel scope**: 每个 `<Chip>` 替换必须位于 `{/* === 10-03-MIGRATION:START === */}` 与 `{/* === 10-03-MIGRATION:END === */}` 标记对内部；不要触碰 10-02 plan 拥有的 `{/* === 10-02-MIGRATION === */}` 块（用于 Card/Input 迁移）。
    模板（每个 page 文件）：在原 `<div className="filter-chips">` 或 `<button className="filter-chip">` 起始处插入 sentinel 起始标记，在 filter 区域末尾闭合下方插入 sentinel 结束标记。
    每个文件模式: `<button className={\`filter-chip \${active ? 'active' : ''}\`} onClick={fn}>label</button>` → `<Chip variant="filter" selected={active} onClick={fn} aria-label={...}>{label}</Chip>`

    清单:
    1. `frontend/src/pages/ChefOrdersPage.jsx:78-104` — 4 个 filter-chip (all/pending/cooking/completed) → 4 个 `<Chip variant="filter" selected={filterStatus === key} onClick={...}>`；外层容器 `<div className="filter-chips">` 改为 `<div style={{ display: 'flex', flexWrap: 'wrap', gap: 8, padding: '0 16px 12px' }}>`（保留视觉，inline 兜底至 Phase 12 8dp 网格）或保留 `.filter-chips` 在 styles.css 中（不删——**不删**因为 Task 1 末尾没删 .filter-chips，但**Task 2 末尾删**）—— **最终**: 全部 inline 化或保留 .filter-chips 至 Phase 12 — **本 Task 末尾** `.filter-chips` 与 `.filter-chip` 一并删除，调用方 inline `style={{ display: 'flex', flexWrap: 'wrap', gap: 8, padding: '0 16px 12px' }}` 表达
    2. `frontend/src/pages/ChefWishesPage.jsx:308-328` — 3 个 filter-chip (all/pending/mine)
    3. `frontend/src/pages/AdminIngredientsPage.jsx:275, 284` — 2 个 (全部 / category list)
    4. `frontend/src/pages/AdminUsersPage.jsx:145` — 1 个 (角色过滤, 实际是 4 个角色 map — 4 个 filter-chip)
    5. `frontend/src/pages/AdminDishesPage.jsx` — 6+ 个 (category / publish status / sf-filter / 搜索 / ing-category)
    6. `frontend/src/pages/GuestOrderPage.jsx:251-321` — 4+ 个 (region/cuisine/allergies)
    7. `frontend/src/pages/UserOrdersPage.jsx:70-88` — 4 个 (订单状态)
    8. `frontend/src/pages/AdminCategoriesPage.jsx` — 视情况（暂未 grep 到 filter-chip 实际使用 — 跳过除非发现）
    9. `frontend/src/pages/ChefDishesPage.jsx:438, 490, 534, 778, 787, 984` — 6 处
    10. 任何其他含 `className="filter-chip"` 的文件 (通过 `grep -rEln "className=.*\bfilter-chip\b" frontend/src/ --include='*.jsx'` 找出剩余)

    每个文件: `import Chip from '../components/primitives/Chip';`

    === D. 删除 styles.css 旧 .filter-chip / .form-input 选择器 ===
    **Sentinel scope**: 本步骤的 filter-chip 删除位于 `/* === 10-03-MIGRATION:START === */` 与 `/* === 10-03-MIGRATION:END === */` 标记对内部（与步骤 E 的 .badge-* 删除合并在同一 sentinel 块或单独 sentinel 块均可）；不要触碰 10-02 plan 拥有的 sentinel 块。
    - 删 line 144 (.filter-chips 容器 flex 布局) — 调用方 inline `style` 兜底
    - 删 line 145-150 (.filter-chip 完整块) — 由 .md-chip + .md-chip--filter 接管
    - 删 line 569 (.filter-chip:focus-visible) — 由 .md-chip 自带
    - 删 line 577 (.filter-chip.active:focus-visible) — 由 .md-chip--selected 自带
    - 删全局 min-size 规则 (line 622 区域) 中的 `.filter-chip,` 引用
    - **.form-input 兜底**: 此时评估 — 跑 `grep -rE 'className=.*\bform-input\b' frontend/src/ --include='*.jsx' | wc -l`；如仅剩 select 兜底且 select 仍依赖 .form-input 视觉，则**保留 .form-input 直至 Phase 11 Select primitive**；**最终决策**: 10-03 末尾**不删 .form-input**（select 仍依赖），Phase 11 Select primitive 上线时一并处理；明确写入 success_criteria
    - 注: **注: 10-02 末尾保留 .form-input 的判断与此处一致**（参见 10-02 SC-10 deviation note）——select 实际依赖此 className，Phase 11 统一处理

    === E. 验证迁移完整性 ===
    `grep -rE 'className=.*\bfilter-chip\b' frontend/src/ --include='*.jsx'` 应 0 命中；`grep -rE 'className="filter-chips"' frontend/src/ --include='*.jsx'` 应 0 命中（容器 inline 化）；`grep -cE "^\.(filter-chip|filter-chips)\s*\{" src/css/styles.css` = 0

  </action>

<verify>
  <automated>cd frontend && npm run lint 2>&1 | tail -20; npm run build 2>&1 | tail -10; grep -rE 'className=.*\bfilter-chip\b' src/ --include='*.jsx' | wc -l; grep -cE "^\.(filter-chip|filter-chips)\s*\{" src/css/styles.css; echo '---Sentinel marker check---'; grep -c "10-03-MIGRATION" src/css/styles.css; grep -c "10-02-MIGRATION" src/css/styles.css</automated>
</verify>

  <acceptance_criteria>
    1. `frontend/src/components/primitives/Chip.jsx` 存在；`grep -E "md-chip--(assist|filter|input|suggestion)" frontend/src/components/primitives/Chip.jsx` 命中 4 个 variant 字符串；`grep "aria-pressed" frontend/src/components/primitives/Chip.jsx` 命中 filter aria-pressed；`grep "onRemove" frontend/src/components/primitives/Chip.jsx` 命中 input close action；`grep "from './Icon'" frontend/src/components/primitives/Chip.jsx` 命中 Icon import。
    2. `frontend/src/components/primitives/Chip.css` 存在；`grep "var(--md-radius-full)" frontend/src/components/primitives/Chip.css` 命中 pill 圆角；`grep "var(--md-color-secondary-container)" frontend/src/components/primitives/Chip.css` 命中 selected 颜色；`grep "var(--md-color-on-secondary-container)" frontend/src/components/primitives/Chip.css` 命中 selected 文字色；`grep -E "^\.md-chip" frontend/src/components/primitives/Chip.css` 命中 ≥ 8 个 class (根 + 4 variant + selected + disabled + check + label + leading + remove)。
    3. 10+ filter-chip 迁移文件（ChefOrdersPage, ChefWishesPage, AdminDishesPage, AdminIngredientsPage, AdminUsersPage, GuestOrderPage, UserOrdersPage, ChefDishesPage 等）每个均 `import Chip from '../components/primitives/Chip';`；`grep -rL "from.*primitives/Chip" frontend/src/pages --include='*.jsx' | xargs grep -lE 'className=.*\bfilter-chip\b'` 为空。
    4. 选中色使用 secondary-container（不是 primary）— per UI-SPEC §12.2 + D-14：`<Chip variant="filter" selected>` 视觉为 secondary-container 背景 + on-secondary-container 文字 + transparent border
    5. Check slot 18dp 始终保留（per UI-SPEC §12.3 末段）— selected 时 opacity 0→1 + scale 0.8→1 150ms standard；未选时占位不可见但 width 不抖
    6. styles.css 删除 `.filter-chip`、`.filter-chips` 旧选择器（`grep -cE "^\.(filter-chip|filter-chips)\s*\{" src/css/styles.css` = 0）；line 569 + 577 focus-visible 旧规则删除；line 622 全局 min-size 移除 `.filter-chip,` 引用。
    7. **.form-input 保留**至 Phase 11（select 依赖）；明确写入 success_criteria 备注
    8. Chip filter 选中色不应用 ripple（`grep "ripple" frontend/src/components/primitives/Chip.jsx frontend/src/components/primitives/Chip.css | wc -l` = 0）；走 state-layer base.css 接管
    9. `cd frontend && npm run lint` 0 新增 error。
    10. `cd frontend && npm run build` 0 error。
  </acceptance_criteria>

  <done>Chip primitive 4 variants 就位；10+ filter-chip 调用迁移完成；styles.css .filter-chip + .filter-chips 旧选择器删除；filter 选中色使用 secondary-container（MD3 spec）；check slot 18dp 保留防 width jump；.form-input 保留至 Phase 11（select 依赖）；lint/build 通过；零业务逻辑回归。</done>
</task>

</tasks>

<verification>
**Plan-level automated verification (run after Task 2 completes):**

```bash
cd /home/temila/family_chef/frontend

# 1. 新 primitive 文件就位
test -f src/components/primitives/Badge.jsx
test -f src/components/primitives/Badge.css
test -f src/components/primitives/Chip.jsx
test -f src/components/primitives/Chip.css

# 2. 旧 Badge.jsx 已删除
test ! -f src/components/Badge.jsx

# 3. 旧 .badge-* / .filter-chip / .filter-chips 选择器清除
grep -cE "^\.(badge|badge-warn|badge-danger|badge-success|badge-info|badge-accent|badge-gold|badge-muted|badge-count|filter-chip|filter-chips)\s*\{" src/css/styles.css
# 期望: 0

# 4. 旧 className 引用已迁移
grep -rE 'className=.*\b(badge-warn|badge-danger|badge-success|badge-info|badge-accent|badge-gold|badge-muted|badge-count|filter-chip)\b' src/ --include='*.jsx' | wc -l
# 期望: 0
grep -r "from '../components/Badge'\|from './Badge'" src/ --include='*.jsx' | grep -v primitives | wc -l
# 期望: 0

# 5. 旧 Badge.css 选择器 (line 569, 577) 已删除
grep "filter-chip:focus-visible\|filter-chip.active:focus-visible" src/css/styles.css | wc -l
# 期望: 0

# 6. statusBadge() 工具未变动
grep "export const statusBadge" src/utils/index.js
# 期望: 命中（业务语义独立于视觉）

# 7. .form-input 保留（select 依赖至 Phase 11）
grep "^\.form-input" src/css/styles.css
# 期望: 命中（保留兜底）

# 8. validation gate
npm run lint  # 0 新增 error
npm run build # 0 error

# 9. Sidebar/BottomBar count badge 迁移
grep -E "function Badge\(\{ count" src/components/Sidebar.jsx src/components/BottomBar.jsx
# 期望: 0 命中（内联 Badge 函数删除）
grep "from './primitives/Badge'" src/components/Sidebar.jsx src/components/BottomBar.jsx
# 期望: 命中（新 Badge primitive import）
```

**Human verification (post-execution):**
- 浏览器访问 `/login` → 确认页面无状态 badge（如有也正常显示）
- 浏览器访问 `/home` → 确认 DishCard 推荐/可点/已售罄 Badge 视觉（tertiary/success/error tone + 24dp pill + 12sp 文字）
- 浏览器访问 `/my-wishes` → 确认 WishCard 顶部 status badge（待处理/准备中/已上架/已拒绝/已撤销 5 个不同 tone）
- 浏览器访问 `/chef/orders` → 确认顶部 filter-chip 4 状态切换视觉（unselected = surface + outline + on-surface-variant；selected = secondary-container + on-secondary-container + transparent border + check 图标 18dp）
- 浏览器访问 `/admin/dishes` → 确认分类/上架状态 filter-chip 切换正常
- 浏览器访问 `/guest/menu/{token}` → 确认 region/cuisine/allergies filter-chip 选中色一致
- 浏览器访问 Sidebar（PC 端）→ 确认订单管理红点（count badge）紧凑 24dp 视觉
- 浏览器访问 BottomBar（手机端）→ 确认订单 tab 红点
- 浏览器访问深浅色 → 确认 Badge / Chip 颜色随主题切换（tertiary container / secondary container / error container 都有深色变体）
</verification>

<success_criteria>
**Mapped to ROADMAP Phase 10 success criterion 5 + CONTEXT D-14 + D-15:**

- [ ] **SC-1 (ROADMAP SC#5, COMPO-06)**: Badge 组件支持 assist/filter/state 三种 variant；8 色调色板（primary/secondary/tertiary/error/success/warn/info/muted）；旧 `<Badge status>` / `<Badge text type>` API 通过 D-15 backward-compat adapter 继续工作；statusBadge() 工具不变 — 由 acceptance_criteria Task 1 #1, #2, #6 验证
- [ ] **SC-2 (ROADMAP SC#5, COMPO-07)**: Chip 组件支持 assist/filter/input/suggestion 四种 variant；filter 用 native button + aria-pressed；input 含独立 close action；assist/suggestion 默认 static；filter 选中色用 secondary-container — 由 acceptance_criteria Task 2 #1, #2, #4 验证
- [ ] **SC-3 (CONTEXT D-02)**: 换皮后 styles.css 中 `.badge / .badge-warn / .badge-danger / .badge-success / .badge-info / .badge-accent / .badge-gold / .badge-muted / .badge-count / .filter-chip / .filter-chips` 旧选择器**全部**删除；调用方零残留（grep 验证）— 由 verification #3, #4 验证
- [ ] **SC-4 (CONTEXT D-14)**: Chip 4 变体全实现（即使当前只用 filter）—为 Phase 11/12 预留；input 含独立 onRemove close action；assist/suggestion 传 onClick 自动升级为 interactive button 满足 48dp — 由 acceptance_criteria Task 2 #1 验证
- [ ] **SC-5 (CONTEXT D-15)**: 旧 `frontend/src/components/Badge.jsx` 删除，替换为 `primitives/Badge.jsx`；向后兼容 adapter 保留；statusBadge() 工具不变；`<Badge status={...}>` 旧调用方式继续工作（业务语义零回归）— 由 acceptance_criteria Task 1 #1, #3, #6 + verification #6 验证
- [ ] **SC-6 (CONTEXT D-04)**: `npm run lint` 0 新增 error；`npm run build` 0 error — 由 verification #8 验证
- [ ] **SC-7 (LOGIC-01/02/03)**: 40+ Badge / 10+ filter-chip 迁移后，所有 onClick / onRemove / selected / aria-pressed / 状态映射零回归；后端零改动（backend/app/* 无任何修改）— 由 verification 浏览器手测确认

**Distribution of CSS deletion (10-03 plan responsibility):**
- **10-03 删除**: `.badge`, `.badge-warn`, `.badge-danger`, `.badge-success`, `.badge-info`, `.badge-accent`, `.badge-gold`, `.badge-muted`, `.badge-count`, `.filter-chip`, `.filter-chips`
- **10-03 保留**: `.form-input` (select 依赖 — Phase 11 Select primitive 上线时处理), `.btn-search` (D-03 永久保留), `.mobile-card-list` / `.order-card` / `.login-card` / `.stat-card` / `.profile-card` / `.dietary-warning-card` / `.preference-section` / `.wish-card-actions` (domain-specific — Phase 12 决定)
- **Phase 10 全量完成后仍保留的旧类**: `.form-input` (select 兜底), `.btn-search` (D-03), `.wish-card-actions` (WishCard footer 容器), 多个 domain-specific 卡片类
</success_criteria>

<must_haves>
**Goal-backward truths — by end of 10-03 plan, the following must be TRUE for Phase 10 goal to advance:**

1. **Badge primitive exists with 3 variants and 8 tones** — `<Badge tone="success">活跃</Badge>` renders 24dp pill with primary-container fill; `<Badge variant="state" tone="warn">待处理</Badge>` uses tertiary-container; `<Badge count={5} />` renders 24dp compact error pill.
2. **Badge backward-compat is preserved (D-15)** — `<Badge status="pending" />` still works; statusBadge() utility in utils/index.js is unchanged; old call sites in WishCard, OrderPage, etc. only need import path update, no API change.
3. **Old Badge.jsx is replaced** — `frontend/src/components/Badge.jsx` deleted; new `frontend/src/components/primitives/Badge.jsx` is the single source; all imports updated to primitives/Badge.
4. **Chip primitive exists with 4 variants** — `<Chip variant="filter" selected={true} onClick={...}>全部</Chip>` renders 32dp visual in 48dp hit with secondary-container selected color + check icon; `<Chip variant="input" onRemove={...}>标签</Chip>` has close button; `<Chip variant="assist">说明</Chip>` is static span unless onClick supplied.
5. **Filter chip selected color uses secondary-container (not primary)** — per MD3 spec; border transparent; check icon 18dp appears with opacity 0→1 + scale 0.8→1 transition 150ms; check slot is reserved to prevent width jitter.
6. **40+ Badge + 10+ filter-chip consumer call sites are migrated** — all old `<span className="badge badge-info">` replaced with `<Badge tone="info">`; all old `<button className="filter-chip">` replaced with `<Chip variant="filter">`; onClick / onRemove / selected / aria-pressed behavior preserved.
7. **Old .badge-* / .filter-chip / .filter-chips CSS is removed from styles.css** — grep returns 0 matches; .form-input remains (Phase 11 Select compatibility).
8. **Sidebar / BottomBar count badges use new Badge primitive** — `<Badge count={pendingCount} />` renders compact error pill at 24dp; no more inline `function Badge({ count })`.
9. **`npm run lint` and `npm run build` pass** — no new lint errors; Vite build 0 errors.
10. **Zero business logic regressions** — all status mappings (statusBadge() returns), filter selections, chip click handlers, count displays, badge tones are identical to pre-migration; backend code is untouched.
</must_haves>

<output>
Create `/home/temila/family_chef/.planning/phases/10-primitive-components/10-03-SUMMARY.md` when done
</output>
