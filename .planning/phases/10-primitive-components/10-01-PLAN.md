---
phase: 10-primitive-components
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/src/index.css
  - frontend/src/css/styles.css
  - frontend/src/css/ripple.css
  - frontend/src/components/Ripple.jsx
  - frontend/src/components/Icon.jsx
  - frontend/src/components/Header.jsx
  - frontend/src/components/Sidebar.jsx
  - frontend/src/components/BottomBar.jsx
  - frontend/src/components/ThemeToggle.jsx
  - frontend/src/components/InvitationsModal.jsx
  - frontend/src/components/InvitationsSection.jsx
  - frontend/src/pages/LoginPage.jsx
  - frontend/src/pages/ForceChangePasswordPage.jsx
  - frontend/src/pages/UserProfilePage.jsx
  - frontend/src/pages/UserFavoritesPage.jsx
  - frontend/src/pages/UserOrdersPage.jsx
  - frontend/src/pages/UserWishesPage.jsx
  - frontend/src/pages/ChefDishesPage.jsx
  - frontend/src/pages/ChefOrdersPage.jsx
  - frontend/src/pages/ChefWishesPage.jsx
  - frontend/src/pages/AdminDishesPage.jsx
  - frontend/src/pages/AdminIngredientsPage.jsx
  - frontend/src/pages/AdminCategoriesPage.jsx
  - frontend/src/pages/AdminUsersPage.jsx
  - frontend/src/pages/AdminChefsPage.jsx
  - frontend/src/pages/AdminWishesPage.jsx
  - frontend/src/pages/OrderPage.jsx
  - frontend/src/pages/OrderDetailPage.jsx
  - frontend/src/pages/DishDetailPage.jsx
  - frontend/src/pages/GuestOrderPage.jsx
  - frontend/src/pages/PreferencesPage.jsx
  - frontend/src/components/ConfirmModal.jsx
  - frontend/src/components/CreateLinkModal.jsx
  - frontend/src/components/ChefSelectModal.jsx
  - frontend/src/components/WishFormModal.jsx
  - frontend/src/components/WishRejectModal.jsx
  - frontend/src/components/WishAdvanceModal.jsx
  - frontend/src/components/DishCard.jsx
  - frontend/src/components/WishCard.jsx
  - frontend/src/components/GuestDishCard.jsx
  - frontend/src/components/primitives/base.css
  - frontend/src/components/primitives/Ripple.jsx
  - frontend/src/components/primitives/ripple.css
  - frontend/src/components/primitives/Icon.jsx
  - frontend/src/components/primitives/Button.jsx
  - frontend/src/components/primitives/Button.css
  - frontend/src/components/primitives/IconButton.jsx
  - frontend/src/components/primitives/IconButton.css
  - frontend/src/components/primitives/FAB.jsx
  - frontend/src/components/primitives/FAB.css
autonomous: true
requirements: [COMPO-01, COMPO-02, COMPO-05, LOGIC-01, LOGIC-02, LOGIC-03]

must_haves:
  truths:
    - "Button 组件支持 variant=filled|tonal|outlined|text + size=sm|md|lg + loading，含内部 Ripple + 16dp spinner，所有现有 onClick / disabled / type / children 行为零回归（COMPO-01）"
    - "IconButton 支持 density=default(40dp visual)|fab(48dp visual)，native button + 内置 Ripple + accessible name 必填（COMPO-02）"
    - "FAB 支持 default(56dp 圆形) / extended(56dp+label) / small(40dp 视觉)，统一 16px 圆角（TOKEN-05），固定位置由 page consumer 控制"
    - "现有 .btn-primary/.btn-secondary/.btn-outline/.btn-icon/.btn-sm/.btn-lg/.btn-block/.fab 旧 CSS 选择器在 styles.css 中彻底删除；调用方零残留（grep 验证）"
    - ".btn-search 工具类保留在 styles.css，用于 6 处搜索栏紧凑按钮（D-03）"
    - "Icon 组件切换为 @material-symbols-svg/react SVG tree-shaking，固定 30-name 映射表（D-05..D-07），material-symbols 字体包与 @import 从依赖 / index.css 中清除"
    - "Ripple.jsx 公开 API 不变（children/disabled/className/style），文件位置迁移到 primitives/Ripple.jsx；Phase 10 之外的 composite 继续手动消费（WishCard/Header/Sidebar/DishCard 仍可继续 import）"
  artifacts:
    - path: "frontend/src/components/primitives/base.css"
      provides: "MD3 state-layer isolation + ripple anchor + focus ring + 48dp min-size + disabled 38% + reduced-motion 共享规则"
      contains: ".md-interactive"
    - path: "frontend/src/components/primitives/Button.jsx"
      provides: "Button 4 variants × 3 sizes + loading + 16dp spinner + 内置 Ripple + forwardRef"
      exports: ["default"]
    - path: "frontend/src/components/primitives/Button.css"
      provides: ".md-button 四变体配色矩阵 + 三尺寸几何 + loading spinner + focus ring"
      contains: ".md-button--filled"
    - path: "frontend/src/components/primitives/IconButton.jsx"
      provides: "IconButton density=default|fab + 内置 Ripple + 内置 Icon + forwardRef + 强制 aria-label"
      exports: ["default"]
    - path: "frontend/src/components/primitives/IconButton.css"
      provides: ".md-icon-button 40/48dp 视觉 + 48dp hit + transparent rest + selected state"
      contains: ".md-icon-button"
    - path: "frontend/src/components/primitives/FAB.jsx"
      provides: "FAB default/extended/small + 内置 Ripple + --md-radius-md 16px + 内置 Icon + forwardRef"
      exports: ["default"]
    - path: "frontend/src/components/primitives/FAB.css"
      provides: ".md-fab 56/40 视觉 + extended label 布局 + elevation-3 rest / 1 pressed"
      contains: ".md-fab"
    - path: "frontend/src/components/primitives/Icon.jsx"
      provides: "Material Symbols SVG tree-shaking 30-name 映射表 + warn-and-render-null + currentColor"
      exports: ["default"]
    - path: "frontend/src/components/primitives/Ripple.jsx"
      provides: "Phase 9 Ripple API 完整保留（公开 API 不变），文件位置迁到 primitives/"
      exports: ["default"]
    - path: "frontend/src/components/primitives/ripple.css"
      provides: ".md-ripple-layer 容器基础 + 共享动画 keyframes"
      contains: ".md-ripple-layer"
    - path: "frontend/package.json"
      provides: "@material-symbols-svg/react dependency + material-symbols devDep 移除"
      contains: "@material-symbols-svg/react"
  key_links:
    - from: "frontend/src/components/primitives/Button.jsx"
      to: "frontend/src/components/primitives/Ripple.jsx"
      via: "import Ripple; 内部包裹在 <button> 外层"
    - from: "frontend/src/components/primitives/Button.css"
      to: "frontend/src/components/primitives/base.css"
      via: "@import './base.css' 共享 state-layer / focus / 48dp"
    - from: "frontend/src/components/primitives/Icon.jsx"
      to: "@material-symbols-svg/react"
      via: "individual import (Home as HomeIcon 等)"
    - from: "frontend/src/components/primitives/IconButton.jsx"
      to: "frontend/src/components/primitives/Icon.jsx"
      via: "<Icon name={icon} size={20} />"
    - from: "frontend/src/components/primitives/FAB.jsx"
      to: "frontend/src/components/primitives/Icon.jsx"
      via: "<Icon name={icon} size={24} />"
    - from: "frontend/src/css/styles.css"
      to: "frontend/src/components/primitives/Button.css"
      via: "通过 Vite 隐式 bundle（不显式 import）"
    - from: "frontend/src/components/primitives/Button.jsx"
      to: "frontend/src/css/tokens.css"
      via: "var(--md-color-primary), var(--md-radius-sm), var(--md-state-layer-*) 等"
---

<objective>
**Phase 10, Plan 01 — Wave 1: Button + IconButton + FAB primitives + Icon SVG tree-shaking + Ripple 迁入 primitives/ + 旧 .btn-* / .fab / .btn-icon / .btn-block CSS 删除**

为前端落地 4 个 Phase 10 primitive（Button 4v×3s+loading、IconButton 40/48dp、FAB default/extended/small、Icon 30-name SVG tree-shaking）以及共享 base.css（state-layer / ripple 锚点 / focus ring / 48dp / disabled 38% / reduced-motion 规则），同时全量换皮 28+ button / 7+ icon-button / 1+ FAB 调用点，最后删除 styles.css 中已无消费者依赖的 `.btn-primary / .btn-secondary / .btn-outline / .btn-icon / .btn-sm / .btn-lg / .btn-block / .fab` 旧选择器。`.btn-search` 工具类（D-03）保留。Ripple.jsx 公开 API 维持兼容（D-12），仅迁移到 `primitives/Ripple.jsx`，Phase 10 之外 composite（WishCard/Header/Sidebar/DishCard 等）继续手动 import。

**Purpose:** 这是 v1.2 MD3 重构最核心的一次视觉换皮，建立 primitive 库 + tokens 体系 + 内部 Ripple + SVG Icon 注册表的最小可用闭环；后续 10-02 / 10-03 的 Card / Input / Badge / Chip 都将消费同一套 base.css 与 Icon registry。

**Output:** 5 个新 primitive 文件 + 5 个新 primitive CSS + 28+ 按钮迁移 + 7+ IconButton 迁移 + 1+ FAB 迁移 + Icon SVG tree-shaking 重写 + 旧 CSS 选择器清理。`npm run lint` 与 `npm run build` 0 error，零业务逻辑回归（LOGIC-01/02/03）。

</objective>

<execution_context>
@/home/temila/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/temila/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md (Phase 10 success criteria 1-3)
@.planning/STATE.md (Phase 9 状态: feature/ui-rebuild @ 7419be0)
@.planning/phases/10-primitive-components/10-CONTEXT.md (D-01..D-17 锁定决策)
@.planning/phases/10-primitive-components/10-UI-SPEC.md (§3 文件结构 / §5 base.css / §6 Button / §7 IconButton+Icon / §8 FAB)
@.planning/phases/10-primitive-components/10-PATTERNS.md (base.css / Button / IconButton / FAB / Icon / Ripple / ripple.css 模拟模式)
@.planning/phases/09-motion-state-layers/09-CONTEXT.md (D-01..D-12 — Ripple / state-layer / 触控目标基线)
@.planning/phases/09-motion-state-layers/09-UI-SPEC.md (§3.1 state-layer 工具 + §3.2 Ripple 契约 + §5 触控目标)
@.planning/phases/08-md3-design-token-foundation/08-CONTEXT.md (D-08: FAB 16px 圆角)
@frontend/src/css/tokens.css
@frontend/src/css/styles.css
@frontend/src/css/ripple.css
@frontend/src/components/Ripple.jsx
@frontend/src/components/Icon.jsx
@frontend/src/components/Badge.jsx
@frontend/src/components/DishCard.jsx
@frontend/src/components/WishCard.jsx
@frontend/src/components/GuestDishCard.jsx
@frontend/src/components/Header.jsx
@frontend/src/components/Sidebar.jsx
@frontend/src/components/BottomBar.jsx
@frontend/src/components/ThemeToggle.jsx
@frontend/src/utils/index.js
@frontend/package.json
@frontend/src/main.jsx
@frontend/src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: primitives/ 目录 + base.css + Ripple 迁移 + Icon SVG tree-shaking + package.json</name>

  <files>
    frontend/src/components/primitives/base.css,
    frontend/src/components/primitives/Ripple.jsx,
    frontend/src/components/primitives/ripple.css,
    frontend/src/components/primitives/Icon.jsx,
    frontend/src/css/ripple.css (旧文件, 删除),
    frontend/src/components/Ripple.jsx (旧文件, 删除),
    frontend/src/components/Icon.jsx (旧文件, 删除),
    frontend/package.json,
    frontend/src/index.css
  </files>

  <read_first>
    frontend/src/css/tokens.css:143-195 (--md-radius, --md-color-*, --md-elevation-*, --md-state-layer-*, --md-focus-ring-*, --md-motion-*)
    frontend/src/css/styles.css:25-46 (state-layer ::before toolkit block, 现有实现作为参考)
    frontend/src/components/Ripple.jsx:1-81 (完整源 — 需迁移)
    frontend/src/css/ripple.css:1-8 (完整源 — 需迁移)
    frontend/src/components/Icon.jsx:1-37 (Phase 9 骨架 — 需重写为 SVG tree-shaking)
    frontend/package.json:15-34 (dependency 块, 当前 material-symbols 在 devDependencies)
    frontend/src/index.css:1-6 (CSS 入口 — 含 @import 'material-symbols/outlined.css' 需删除)
    .planning/phases/10-primitive-components/10-UI-SPEC.md §3 文件结构 / §5 base.css 契约 (z-index 0/1/2 + isolation + reduced-motion)
    .planning/phases/10-primitive-components/10-PATTERNS.md:34-208 (base.css/Ripple/ripple.css/Icon 模拟模式)
    .planning/phases/10-primitive-components/10-CONTEXT.md D-05..D-07 (SVG tree-shaking + 30 名称映射)
    .planning/phases/09-motion-state-layers/09-UI-SPEC.md §3.2 (Ripple API 锁定)
  </read_first>

  <action>
    === 创建 frontend/src/components/primitives/ 目录（mkdir -p） ===

    === A. 新建 frontend/src/components/primitives/base.css ===
    实现 Phase 10 UI-SPEC §5 锁定的共享交互契约（z-index 0/1/2 显式分层，替代 Phase 9 负 z-index 模式）。
    内容（按以下顺序）：
    1. 文件顶部注释: "MD3 Primitive Base（Phase 10 — 共享 state-layer / ripple 锚点 / focus ring / 触控目标）"
    2. `.md-interactive { position: relative; isolation: isolate; overflow: hidden; }`
    3. `.md-interactive::before { content: ''; position: absolute; inset: 0; z-index: 0; background: var(--md-component-state-color, var(--md-state-layer-primary)); opacity: 0; pointer-events: none; transition: opacity var(--md-motion-duration-short) var(--md-motion-easing-standard); }`
    4. `.md-interactive > :not(.md-ripple-layer) { position: relative; z-index: 2; }`
    5. `.md-ripple-layer { position: absolute; inset: 0; z-index: 1; pointer-events: none; }`
    6. `.md-interactive:hover::before { opacity: var(--md-state-layer-hover); }`
    7. `.md-interactive:active::before, .md-interactive[data-pressed='true']::before { opacity: var(--md-state-layer-pressed); }`
    8. `.md-interactive:focus-visible::before { opacity: var(--md-state-layer-focused); }`
    9. `.md-interactive:disabled::before, .md-interactive[aria-disabled='true']::before { opacity: 0; }`
    10. 触控目标全局规则（仅 MD3 primitive root）：`.md-button, .md-icon-button, .md-fab, .md-input-wrapper { min-inline-size: 48px; min-block-size: 48px; }`
    11. Reduced motion 媒体查询：`@media (prefers-reduced-motion: reduce) { .md-interactive::before { transition: none; } .md-ripple-layer { display: none; } }`
    12. 零硬编码色值/直角/transition: all。所有值均 `var(--md-*)`。

    === B. 新建 frontend/src/components/primitives/Ripple.jsx ===
    完整内容从 frontend/src/components/Ripple.jsx 1:1 迁移，3 处修改：
    - 文件顶部 docstring 更新为 "Phase 10 — 公开 API 保留供 Phase 10 之外 composite (WishCard/Header/Sidebar/DishCard) 继续手动消费；primitive 内部已内置"
    - line 12 import 路径由 `'../css/ripple.css'` 改为 `'./ripple.css'`
    - line 74 className 由 `ripple-container` 改为 `md-ripple-layer`（同步 base.css 契约）
    其余行（含 onPointerDown / setTimeout 150ms / requestAnimationFrame / `var(--md-color-primary)`）原样保留。

    === C. 新建 frontend/src/components/primitives/ripple.css ===
    迁移 frontend/src/css/ripple.css 全文（4 行内容），类名由 `.ripple-container` 改为 `.md-ripple-layer`，新增 `@keyframes md-ripple-expand { from { transform: scale(0); } to { transform: scale(1); } }` 与 `@keyframes md-ripple-fade { from { opacity: 0.12; } to { opacity: 0; } }` 备用 keyframes（Phase 9 旧实现用 inline transition，这里仅留备用于 prefers-reduced-motion fallback 路径）。文件顶部注释更新为 "MD3 Ripple 容器基础（Phase 10 — D-12）"。

    === D. 重写 frontend/src/components/primitives/Icon.jsx ===
    完全替换 Phase 9 字体骨架（37 行），按 UI-SPEC §7.2 + CONTEXT D-05/D-06/D-07 实现。
    - 默认 export `Icon({ name, size = 24, fill = 0, weight = 400, grade = 0, className = '' })`
    - API 与 Phase 9 完全一致：`name / size / fill / weight / grade / className`，零 breaking change
    - 内部 30-name 静态映射表（精确按 D-07 列表，禁止增删/重命名）：
      * Navigation: `home` → HomeIcon, `menu` → MenuIcon, `arrow-back` → ArrowBackIcon, `arrow-forward` → ArrowForwardIcon, `more-vert` → MoreVertIcon, `more-horiz` → MoreHorizIcon, `place` → PlaceIcon
      * Actions: `search` → SearchIcon, `add` → AddIcon, `edit` → EditIcon, `delete` → DeleteIcon, `check` → CheckIcon, `close` → CloseIcon, `share` → ShareIcon, `refresh` → RefreshIcon, `filter` → FilterIcon, `sort` → SortIcon
      * People/food: `restaurant` → RestaurantIcon, `person` → PersonIcon, `chef` → ChefIcon
      * Status: `favorite` → FavoriteIcon, `star` → StarIcon, `schedule` → ScheduleIcon, `notifications` → NotificationsIcon, `info` → InfoIcon, `warning` → WarningIcon, `error` → ErrorIcon
      * Settings/privacy: `settings` → SettingsIcon, `logout` → LogoutIcon, `visibility` → VisibilityIcon, `visibility-off` → VisibilityOffIcon
    - 每个 Icon 从 `@material-symbols-svg/react` 个别 import（保证 Vite tree-shake 未使用图标）：`import { Home as HomeIcon, Menu as MenuIcon, ... } from '@material-symbols-svg/react';`
    - 渲染：`const ICONS = { home: HomeIcon, ... }; const Component = ICONS[name]; if (!Component) { if (import.meta.env.DEV) console.warn(`[Icon] unknown name: ${name}`); return null; } return <Component style={{ fontSize: size, color: 'currentColor', ...style }} fill={fill} weight={weight} grade={grade} className={className} aria-hidden="true" />;`
    - 严格按 UI-SPEC §7.2 末段："Unknown name: development console warning + render nothing；不得显示破损文字或 fallback emoji"

    === E. 修改 frontend/package.json ===
    - 在 `"dependencies"` 块 (line 15-20) 内新增 `"@material-symbols-svg/react": "^1.0.38"`（按 RESEARCH.md 锁定的 2025 版本号）
    - 从 `"devDependencies"` 块 (line 21-34) 中删除 `"material-symbols": "^0.45.9"`（line 32）
    - 保持其余依赖不动；`scripts` 块不动

    === F. 修改 frontend/src/index.css ===
    - 删除 line 3 的 `@import 'material-symbols/outlined.css';`（font-based icon import 不再需要 — SVG tree-shaking 由 Vite 处理）
    - 保留 `@import './css/tokens.css';` 与 `@import './css/styles.css';` 不动
    - :root / body / media query 块原样保留

    === G. 删除旧文件 ===
    - `rm frontend/src/components/Ripple.jsx`（已迁到 primitives/）
    - `rm frontend/src/components/Icon.jsx`（已重写并迁到 primitives/）
    - `rm frontend/src/css/ripple.css`（已迁到 primitives/ripple.css）
    注：现有 import 路径（`'./Ripple'`、`'../components/Icon'`）将在 Task 2/3 迁移 consumer 时同步更新。本 Task 不动 consumer。

    === H. 安装新依赖 ===
    运行 `cd frontend && npm install`（仅 package.json 变更后的一次性安装）。

  </action>

  <verify>
    <automated>cd frontend && npm install 2>&1 | tail -5; cd .. && test -f frontend/src/components/primitives/base.css && test -f frontend/src/components/primitives/Ripple.jsx && test -f frontend/src/components/primitives/ripple.css && test -f frontend/src/components/primitives/Icon.jsx && test ! -f frontend/src/components/Icon.jsx && test ! -f frontend/src/components/Ripple.jsx && test ! -f frontend/src/css/ripple.css && cd frontend && npm run build 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    1. `frontend/src/components/primitives/base.css` 文件存在；`grep -E "^\.md-interactive\s*\{" frontend/src/components/primitives/base.css` 命中至少 1 行；文件不含 `transition: all`、`4px`（除 spacing token 引用外）、硬编码 hex/rgb 颜色（grep `#[0-9a-fA-F]\{3,6\}` 仅命中 `currentColor` 引用类）。
    2. `frontend/src/components/primitives/Ripple.jsx` 存在；`grep "md-ripple-layer" frontend/src/components/primitives/Ripple.jsx` 命中 className 引用；`grep "var(--md-color-primary)" frontend/src/components/primitives/Ripple.jsx` 命中 ripple 颜色；import 路径已切换到 `./ripple.css`（`grep "from './ripple.css'" frontend/src/components/primitives/Ripple.jsx` 命中）。
    3. `frontend/src/components/primitives/Icon.jsx` 存在；`grep -c "from '@material-symbols-svg/react'" frontend/src/components/primitives/Icon.jsx` 命中 1 行（单 import 含所有 30 名称）；`grep -cE "(Home|Menu|ArrowBack|ArrowForward|MoreVert|MoreHoriz|Place|Search|Add|Edit|Delete|Check|Close|Share|Refresh|Filter|Sort|Restaurant|Person|Chef|Favorite|Star|Schedule|Notifications|Info|Warning|Error|Settings|Logout|Visibility|VisibilityOff)Icon" frontend/src/components/primitives/Icon.jsx` ≥ 30；函数默认值 `fill = 0, weight = 400, grade = 0, size = 24` 与 Phase 9 一致。
    4. `frontend/package.json` 中 `"@material-symbols-svg/react": "^1.0.38"` 出现在 `dependencies` 块；`grep '"material-symbols"' frontend/package.json` 0 命中（devDep 已移除）。
    5. `frontend/src/index.css` 不含 `material-symbols/outlined.css` 引用（`grep "material-symbols" frontend/src/index.css` 0 命中）。
    6. `frontend/src/components/Ripple.jsx`、`frontend/src/components/Icon.jsx`、`frontend/src/css/ripple.css` 三个旧文件已删除（`test ! -f` 通过）。
    7. `cd frontend && npm install` 成功（exit 0）；`node_modules/@material-symbols-svg/react` 目录存在（`test -d frontend/node_modules/@material-symbols-svg/react` 命中）。
    8. `cd frontend && npm run build` 成功（exit 0）—— Vite tree-shake 后 bundle 体积应小于引入 material-symbols 字体时（粗略 sanity check）。
  </acceptance_criteria>

  <done>primitives/ 目录骨架就位，base.css 锁定共享契约，Ripple / Icon 已迁入并升级为 SVG tree-shaking + 30-name 映射表，font 依赖移除；旧文件删除，build 通过；零 consumer 迁移（留给 Task 2/3）。</done>
</task>

<task type="auto">
  <name>Task 2: Button primitive (4v×3s+loading+spinner) + 28+ button call site 迁移 + .btn-search 工具类 + 旧 .btn-* CSS 删除</name>

  <files>
    frontend/src/components/primitives/Button.jsx,
    frontend/src/components/primitives/Button.css,
    frontend/src/css/styles.css,
    frontend/src/pages/LoginPage.jsx,
    frontend/src/pages/ForceChangePasswordPage.jsx,
    frontend/src/pages/UserProfilePage.jsx,
    frontend/src/pages/UserFavoritesPage.jsx,
    frontend/src/pages/UserOrdersPage.jsx,
    frontend/src/pages/UserWishesPage.jsx,
    frontend/src/pages/ChefDishesPage.jsx,
    frontend/src/pages/ChefOrdersPage.jsx,
    frontend/src/pages/ChefWishesPage.jsx,
    frontend/src/pages/AdminDishesPage.jsx,
    frontend/src/pages/AdminIngredientsPage.jsx,
    frontend/src/pages/AdminCategoriesPage.jsx,
    frontend/src/pages/AdminUsersPage.jsx,
    frontend/src/pages/AdminChefsPage.jsx,
    frontend/src/pages/AdminWishesPage.jsx,
    frontend/src/pages/OrderPage.jsx,
    frontend/src/pages/OrderDetailPage.jsx,
    frontend/src/pages/DishDetailPage.jsx,
    frontend/src/pages/GuestOrderPage.jsx,
    frontend/src/pages/PreferencesPage.jsx,
    frontend/src/components/ConfirmModal.jsx,
    frontend/src/components/CreateLinkModal.jsx,
    frontend/src/components/ChefSelectModal.jsx,
    frontend/src/components/WishFormModal.jsx,
    frontend/src/components/WishRejectModal.jsx,
    frontend/src/components/WishAdvanceModal.jsx,
    frontend/src/components/InvitationsModal.jsx,
    frontend/src/components/InvitationsSection.jsx,
    frontend/src/components/PasswordInput.jsx
  </files>

  <read_first>
    .planning/phases/10-primitive-components/10-UI-SPEC.md §6 (Button API / variant color matrix / size geometry / loading spinner)
    .planning/phases/10-primitive-components/10-PATTERNS.md:62-101 (Button.jsx + Button.css 模拟模式 + 偏差列表)
    .planning/phases/10-primitive-components/10-CONTEXT.md D-12 (内部 Ripple) / D-17 (loading spinner) / D-03 (.btn-search)
    .planning/phases/09-motion-state-layers/09-UI-SPEC.md §3.2 (Ripple 容器 + pointer 位置计算)
    frontend/src/css/tokens.css:144-149 (--md-radius-sm = 12px for Button)
    frontend/src/css/styles.css:80-104 (现有 .btn / .btn-primary / .btn-secondary / .btn-outline / .btn-icon / .btn-sm / .btn-lg / .btn-block 完整源 — 全部删除)
    frontend/src/components/WishCard.jsx:52-66 / 76-103 (典型 btn-primary + btn-outline + flex-1 模式, 1 内联 style DANGER 模式)
    frontend/src/components/InvitationsSection.jsx:198-220 (典型 .btn-icon 暂时保留 — 走 Task 3)
    frontend/src/pages/AdminIngredientsPage.jsx:240-256, 263, 324, 361, 364, 373, 400, 444, 447, 456 (btn-primary/btn-outline/btn-sm 模式 + 6 处搜索栏 inline style 紧凑按钮)
    frontend/src/pages/LoginPage.jsx:76-82 (form submit 按钮 type="submit" 必填 — 防止隐式 type 回归)
  </read_first>

  <action>
    === A. 新建 frontend/src/components/primitives/Button.jsx ===
    - 顶部 docstring 注释: "MD3 Button Primitive (Phase 10 — COMPO-01) — 4 variants × 3 sizes + loading; 内置 Ripple + 16dp spinner + forwardRef"
    - `import { forwardRef } from 'react'; import Ripple from './Ripple'; import './Button.css';`（Button.css 顶部 @import './base.css'）
    - `const Button = forwardRef(function Button({ variant = 'filled', size = 'md', loading = false, disabled = false, type = 'button', icon, children, className = '', onClick, ...rest }, ref) { ... })`
    - 构造 classes: `['md-button', \`md-button--\${variant}\`, \`md-button--\${size}\`, loading && 'md-button--loading', className].filter(Boolean).join(' ')`
    - 内部 Ripple 包裹: `<Ripple disabled={disabled || loading}>{...}</Ripple>` 内含 `<button ref={ref} type={type} className={classes} disabled={disabled || loading} aria-busy={loading || undefined} onClick={loading ? undefined : onClick} {...rest}>`
    - 内部 spinner: 当 `loading === true` 时在 label 前渲染 `<svg className="md-button__spinner" width="16" height="16" viewBox="0 0 16 16" aria-hidden="true"><circle cx="8" cy="8" r="6" fill="none" stroke="currentColor" strokeWidth="2" strokeDasharray="28 10" strokeLinecap="round" /></svg>`（CSS animation: spin 0.8s linear infinite — keyframes 定义在 Button.css）
    - optional `icon` prop: `<Icon name={icon} size={size === 'sm' ? 16 : 18} />` 渲染于 label 前（label 后 8dp gap 由 flex gap 决定）；`loading=true` 时 spinner 替换 icon
    - children 直接渲染在 spinner/icon 之后
    - 默认 export `Button`

    === B. 新建 frontend/src/components/primitives/Button.css ===
    - 顶部 `@import './base.css';`（共享 state-layer / focus / 48dp 基础）
    - 文件顶部注释: "MD3 Button Styles (Phase 10 — COMPO-01)"
    - 根 class `.md-button`: `display: inline-flex; align-items: center; justify-content: center; gap: 8px; padding: 10px 20px; min-inline-size: 48px; min-block-size: 48px; border: 1px solid transparent; border-radius: var(--md-radius-sm); font-family: var(--md-font-body); font-size: 14px; font-weight: 500; line-height: 20px; white-space: nowrap; position: relative; cursor: pointer; color: var(--md-color-on-primary); background: var(--md-color-primary); transition: color var(--md-motion-duration-short) var(--md-motion-easing-standard), background var(--md-motion-duration-short) var(--md-motion-easing-standard), border-color var(--md-motion-duration-short) var(--md-motion-easing-standard), box-shadow var(--md-motion-duration-short) var(--md-motion-easing-standard);`
    - 复合 `class .md-button` 应用 `.md-interactive`（视觉模型）—— 注意 base.css 已包含 isolation / ::before state-layer
    - `.md-button:focus-visible { outline: var(--md-focus-ring-inner); outline-offset: 2px; }`（filled 走内环）
    - variant 矩阵:
      * `.md-button--filled` 默认 (background primary / color on-primary / box-shadow var(--md-elevation-1)); focus 走 inner ring
      * `.md-button--tonal { background: var(--md-color-secondary-container); color: var(--md-color-on-secondary-container); }`; `:focus-visible { outline: var(--md-focus-ring-outer); }`; state-color 用 `var(--md-state-layer-on-surface)`
      * `.md-button--outlined { background: transparent; color: var(--md-color-primary); border: 1px solid var(--md-color-outline); }`; `:focus-visible { outline: var(--md-focus-ring-outer); }`
      * `.md-button--text { background: transparent; color: var(--md-color-primary); padding-inline: 12px; }`; 同 outlined focus
    - size 矩阵 (UI-SPEC §6.3):
      * `.md-button--sm { min-block-size: 32px; padding: 6px 12px; font-size: 14px; }` — hit 仍 48px（base.css 规则覆盖）
      * `.md-button--md { min-block-size: 40px; padding: 10px 16px; font-size: 14px; }`
      * `.md-button--lg { min-block-size: 48px; padding: 12px 24px; font-size: 16px; }`
    - loading: `.md-button--loading { cursor: wait; }`; `.md-button__spinner { animation: md-spin 0.8s linear infinite; }`; `@keyframes md-spin { to { transform: rotate(360deg); } }`
    - disabled: `:disabled, &[aria-disabled='true'] { opacity: 0.38; cursor: not-allowed; box-shadow: none; }`
    - Reduced motion: `@media (prefers-reduced-motion: reduce) { .md-button__spinner { animation-duration: 6s; } }`（降速而非隐藏，保留进度指示）

    === C. 新增 `.btn-search` 工具类到 styles.css（D-03，保留项目专用紧凑搜索按钮） ===
    在 styles.css 现有 `.btn-lg` 行（line 103）之后追加：
    `.btn-search { padding: 4px 10px; font-size: 0.75rem; min-block-size: 32px; min-inline-size: 48px; }`（仅样式补充，类名由调用方拼接）
    注：此工具类**不依赖**已删除的 `.btn-primary / .btn-secondary / .btn-outline`，因此即使在 Task 末尾删除旧 .btn-* 仍可独立工作；调用方写 `<Button className="btn-search" variant="filled" size="sm">搜索</Button>` 时 `.btn-search` 仅覆盖 padding/fontSize，variant 提供颜色。

    === D. 迁移 28+ button call site（按文件分组）===
    对每个文件：将所有 `<button className="btn btn-primary ...">` / `btn-secondary` / `btn-outline` / `btn-sm` / `btn-lg` / `btn-block` 替换为 `<Button variant="..." size="..." className="...">`；保留 `type="submit"` (仅 LoginPage 必要)、`onClick` / `disabled` / `style`（inline style 透传仍可工作但尽可能下沉到 className）；import 路径 `import Button from '../components/primitives/Button';`（pages 用） 或 `import Button from './primitives/Button';`（components 用）。完整文件清单与目标替换模式：

    1. `frontend/src/pages/LoginPage.jsx:76-82` — `<button type="submit" className="btn btn-primary btn-block btn-lg" style={...}>` → `<Button type="submit" variant="filled" size="lg" style={...}>登 录</Button>`（**保留 type="submit" 防止隐式行为回归**）
    2. `frontend/src/pages/ForceChangePasswordPage.jsx` — 找 `className="btn btn-primary"` 与 `btn-secondary` 改 Button
    3. `frontend/src/pages/UserProfilePage.jsx` — 4 处 btn-primary / btn-outline / btn-secondary → Button
    4. `frontend/src/pages/UserFavoritesPage.jsx` — 5 处 → Button
    5. `frontend/src/pages/UserOrdersPage.jsx` — 5 处（含 ChefOrdersPage 类似 filter chip + button）
    6. `frontend/src/pages/UserWishesPage.jsx:307-314` — `<button className="btn btn-secondary btn-block wish-load-more">` → `<Button variant="tonal" className="wish-load-more" block>加载更多愿望</Button>`（**block 通过 style 透传或新增 `block` prop**；当前约定：style 透传 + width: 100% 旧 .btn-block 视觉由父容器 100% 提供即可）
    7. `frontend/src/pages/ChefDishesPage.jsx` — 47 处 button；含 .btn-icon 留给 Task 3；btn-primary/btn-outline/btn-secondary/btn-sm → Button
    8. `frontend/src/pages/ChefOrdersPage.jsx` — 10 处 → Button
    9. `frontend/src/pages/ChefWishesPage.jsx` — 4 处 → Button
    10. `frontend/src/pages/AdminDishesPage.jsx` — 48 处（最大单文件）；含搜索栏 2 处紧凑按钮 (line 256-265 区域) → Button className="btn-search" size="sm"
    11. `frontend/src/pages/AdminIngredientsPage.jsx` — 34 处；含 4 处搜索栏紧凑按钮（line 256, 257 区域的搜索/清空）→ Button className="btn-search"
    12. `frontend/src/pages/AdminCategoriesPage.jsx` — 14 处
    13. `frontend/src/pages/AdminUsersPage.jsx` — 21 处
    14. `frontend/src/pages/AdminChefsPage.jsx` — 7 处
    15. `frontend/src/pages/AdminWishesPage.jsx` — 通过 WishCard 调用，含内联 EditModal/Buttons
    16. `frontend/src/pages/OrderPage.jsx` — 27 处 (line 478 `btn btn-primary btn-sm` 点菜按钮; 多个 `btn btn-outline btn-sm` 编辑/删除等)
    17. `frontend/src/pages/OrderDetailPage.jsx` — 7 处
    18. `frontend/src/pages/DishDetailPage.jsx` — 2 处 (非 .btn-icon 部分)
    19. `frontend/src/pages/GuestOrderPage.jsx` — 8 处 (含 search bar 2 处 btn-search)
    20. `frontend/src/pages/PreferencesPage.jsx` — 2 处
    21. `frontend/src/components/ConfirmModal.jsx` — confirm/cancel 按钮（典型 2 btn-primary + 1 btn-secondary）
    22. `frontend/src/components/CreateLinkModal.jsx` — 3 处
    23. `frontend/src/components/ChefSelectModal.jsx` — 2 处
    24. `frontend/src/components/WishFormModal.jsx` — 8 处 (含 select/textarea form 提交)
    25. `frontend/src/components/WishRejectModal.jsx` — 3 处 (拒绝原因提交)
    26. `frontend/src/components/WishAdvanceModal.jsx` — 2 处 (关联菜品提交)
    27. `frontend/src/components/InvitationsModal.jsx` — 2 处 (创建/复制邀请)
    28. `frontend/src/components/InvitationsSection.jsx` — 3 处 (创建按钮, .btn-icon 部分留 Task 3)
    29. `frontend/src/components/PasswordInput.jsx:16-23` — 内部 toggle button 不是 btn-*，保留（Task 3 决定是否 IconButton 化）
    30. `frontend/src/components/DishCard.jsx` — 内部无 btn-*，但含 badge-gold / badge-info / badge-success / badge-danger → **10-03 处理**
    31. `frontend/src/components/WishCard.jsx:52-103` — 6 处 button (btn-primary / btn-outline + DANGER inline style)；改为 `<Button variant="filled" size="sm" className="flex-1">` 与 `<Button variant="outlined" size="sm" className="flex-1" style={{ borderColor: 'var(--md-color-error)', color: 'var(--md-color-error)' }}>`（**DANGER 样式内联保留** — Phase 10 不破坏 domain 业务）
    32. `frontend/src/components/GuestDishCard.jsx:27-31` — `<button className="btn btn-primary btn-sm guest-add-btn">+</button>` 暂不删 `guest-add-btn` class（Phase 12 决策保留与否），但 btn-* 部分 → `<Button variant="filled" size="sm" className="guest-add-btn" aria-label="加入点菜">+</Button>`

    **重要：每个迁移文件必须显式 import Button；`btn-icon` 临时保留待 Task 3 处理。**

    === E. 批量删除 styles.css 中 .btn-* 旧选择器 ===
    现有 styles.css:80-104 是完整 .btn 块（line 81 .btn / 82-84 ::before+hover+active / 85 .btn-primary / 87-91 .btn-secondary / 92-95 .btn-outline / 96-101 .btn-icon / 102 .btn-sm / 103 .btn-lg / 104 .btn-block / 106 全局 :disabled 规则）。
    整段删除 line 80-106 全部内容，但**保留 line 106 的全局 `:disabled, [disabled], [aria-disabled="true"] { opacity: 0.38; cursor: not-allowed; box-shadow: none; }`**（非 Button 专用，仍被 IconButton/FAB/Card 等其他 primitive 与 .theme-toggle / .tab-item / .pc-sidebar-item 等 consumer 依赖）。
    同时更新 focus-ring 规则（styles.css 现有 line 568 / 576）：
    - 删 line 568 `.btn:focus-visible` (新 .md-button 自带)
    - 删 line 576 `.btn-primary:focus-visible` (新 .md-button--filled 自带)
    同步删除 line 622 全局 min-size 规则中的 `.btn, .btn-icon,` 部分（两者均不再存在）—— 改写为仅含非 primitive 的：`.list-item, .tab-item, .filter-chip, .pc-sidebar-item, .header-back, .theme-toggle, .modal-close, .qty-stepper button, .fab, .menu-item, .chef-select-item, .wish-picker-item, .guest-add-btn, .dish-fav-btn, .preference-tag button, .quick-action, .wish-card { min-width: 48px; min-height: 48px; }`（已无 .fab — 但 Task 3 会删除 .fab，**当前 Task 2 不动 .fab**，仅删 .btn/.btn-icon）

    === F. 验证迁移完整性 ===
    跑 `grep -rE 'className=.*\b(btn-primary|btn-secondary|btn-outline|btn-sm|btn-lg|btn-block)\b' frontend/src/ --include='*.jsx' | grep -v 'btn-search'` 应为 0 行（除 line 256 区域中已迁移为 Button className="btn-search" 之外的）；`grep -rE 'className=.*\bbtn-icon\b' frontend/src/ --include='*.jsx'` 应仍命中（Task 3 处理）。

  </action>

  <verify>
    <automated>cd frontend && npm run lint 2>&1 | tail -20; npm run build 2>&1 | tail -10; grep -rE 'className=.*\b(btn-primary|btn-secondary|btn-outline|btn-sm|btn-lg|btn-block)\b' src/ --include='*.jsx' | grep -v 'btn-search' | wc -l; grep -cE "^\.(btn|btn-primary|btn-secondary|btn-outline|btn-sm|btn-lg|btn-block)" src/css/styles.css</automated>
  </verify>

  <acceptance_criteria>
    1. `frontend/src/components/primitives/Button.jsx` 存在；`grep -E "md-button--filled|md-button--tonal|md-button--outlined|md-button--text" frontend/src/components/primitives/Button.jsx` 命中 4 个 variant 字符串；`grep "md-button__spinner" frontend/src/components/primitives/Button.jsx` 命中 SVG 引用；`grep "type = 'button'" frontend/src/components/primitives/Button.jsx` 命中默认 type；`forwardRef` 包裹存在。
    2. `frontend/src/components/primitives/Button.css` 存在；`grep "^\.md-button" frontend/src/components/primitives/Button.css` 命中 ≥ 7 个 class (根 + 4 variant + 3 size - 含 .md-button--loading)；`grep "var(--md-radius-sm)" frontend/src/components/primitives/Button.css` 命中（12px 圆角）；`grep "var(--md-elevation-1)" frontend/src/components/primitives/Button.css` 命中（filled 阴影）；`grep "var(--md-focus-ring-inner)" frontend/src/components/primitives/Button.css` 命中（filled focus）。
    3. 32 个迁移文件（29 page + modal + 3 component：WishCard, GuestDishCard, InvitationsSection）每个均 `import Button from '../components/primitives/Button';`（pages/components 用相对路径正确）；`grep -rL "from.*primitives/Button" frontend/src/pages frontend/src/components --include='*.jsx' | xargs grep -lE 'className=.*\bbtn-(primary|secondary|outline)\b'` 为空。
    4. `frontend/src/pages/LoginPage.jsx:76-82` 保留 `type="submit"`（`grep -E 'type=.submit.' frontend/src/pages/LoginPage.jsx` 命中）；不再含 `className="btn btn-primary btn-block"` 旧模式（`grep "btn-block" frontend/src/pages/LoginPage.jsx` 0 命中）。
    5. `frontend/src/css/styles.css` 不含 `.btn`、`.btn-primary`、`.btn-secondary`、`.btn-outline`、`.btn-sm`、`.btn-lg`、`.btn-block`、`.btn-icon` 选择器（`grep -cE "^\.(btn|btn-primary|btn-secondary|btn-outline|btn-sm|btn-lg|btn-block|btn-icon)" src/css/styles.css` = 0）；但保留 `.btn-search`（`grep "^\.btn-search" src/css/styles.css` 命中 1 行）；保留 line 106 全局 `:disabled` 规则。
    6. `frontend/src/css/styles.css:568 (.btn:focus-visible)` 与 line 576 (.btn-primary:focus-visible) 已删除；新 .md-button 自带 focus-visible 规则。
    7. `frontend/src/css/styles.css` 全局 min-size 规则更新后不含 `.btn, .btn-icon`（`grep -E "^\.btn,|^\.btn-icon," src/css/styles.css` 0 命中）。
    8. `cd frontend && npm run lint` 0 error（Phase 9 baseline ≥90 errors 可能存在，但不得新增 error — `npm run lint 2>&1 | grep -cE "error"` 不得高于 baseline）。
    9. `cd frontend && npm run build` 0 error（exit 0）。
    10. `btn-icon` 仍存在（Task 3 处理）：`grep -rE 'className=.*\bbtn-icon\b' src/ --include='*.jsx' | wc -l` ≥ 5（Phase 9 调用点保留）。
  </acceptance_criteria>

  <done>Button primitive 4v×3s+loading 完整就位；32+ 文件 button 调用点全量迁移到 <Button>；.btn-search 工具类保留；styles.css .btn-* 旧选择器批量删除（除 .btn-search）；focus-ring 与 min-size 规则同步更新；lint/build 通过；零业务逻辑回归；btn-icon 临时保留待 Task 3。</done>
</task>

<task type="auto">
  <name>Task 3: IconButton + FAB primitives + 7+ IconButton 迁移 + 1+ FAB 迁移 + 旧 .btn-icon / .fab CSS 删除 + 旧文件清理</name>

  <files>
    frontend/src/components/primitives/IconButton.jsx,
    frontend/src/components/primitives/IconButton.css,
    frontend/src/components/primitives/FAB.jsx,
    frontend/src/components/primitives/FAB.css,
    frontend/src/css/styles.css,
    frontend/src/components/Header.jsx,
    frontend/src/components/ThemeToggle.jsx,
    frontend/src/components/InvitationsModal.jsx,
    frontend/src/components/InvitationsSection.jsx,
    frontend/src/pages/OrderPage.jsx,
    frontend/src/pages/DishDetailPage.jsx,
    frontend/src/pages/GuestOrderPage.jsx,
    frontend/src/pages/UserWishesPage.jsx
  </files>

  <read_first>
    .planning/phases/10-primitive-components/10-UI-SPEC.md §7 (IconButton API + 7.1 IconButton table) / §8 (FAB API + 8.1/8.2 geometry + states)
    .planning/phases/10-primitive-components/10-PATTERNS.md:104-165 (IconButton/FAB 模拟模式)
    .planning/phases/10-primitive-components/10-CONTEXT.md D-16 (FAB API: variant="extended" + label; size="small" 40dp 视觉 16px 圆角)
    .planning/phases/08-md3-design-token-foundation/08-CONTEXT.md (D-08: FAB 16px 圆角; 禁止 radius-full)
    frontend/src/css/styles.css:96-101 (现有 .btn-icon 完整源 — 删除目标)
    frontend/src/css/styles.css:219-223 (现有 .fab 完整源 — 删除目标)
    frontend/src/css/tokens.css:147 (--md-radius-md: 16px for FAB)
    frontend/src/components/Header.jsx:14-27 (← back button 模式)
    frontend/src/components/ThemeToggle.jsx:22-28 (theme toggle emoji 模式)
    frontend/src/components/Sidebar.jsx:1-110 (icon 数据结构 — 暂不整体迁移 Icon 内部 emoji，因 BottomBar/Sidebar 的 active 状态超出 IconButton 范围)
    frontend/src/pages/OrderPage.jsx:469-475 (btn-icon btn-sm dish-fav-btn 模式)
    frontend/src/pages/UserWishesPage.jsx:322-329 (fab 模式)
  </read_first>

  <action>
    === A. 新建 frontend/src/components/primitives/IconButton.jsx ===
    - docstring: "MD3 IconButton Primitive (Phase 10 — COMPO-02) — density=default(40dp)|fab(48dp) + 内置 Ripple + 内置 Icon + forwardRef; aria-label 必填"
    - imports: `import { forwardRef } from 'react'; import Ripple from './Ripple'; import Icon from './Icon'; import './IconButton.css';`
    - `const IconButton = forwardRef(function IconButton({ icon, density = 'default', selected = false, disabled = false, type = 'button', ariaLabel, className = '', onClick, ...rest }, ref) { ... })`
    - 构造 classes: `['md-icon-button', \`md-icon-button--\${density}\`, selected && 'md-icon-button--selected', className].filter(Boolean).join(' ')`
    - 渲染：`<Ripple disabled={disabled}><button ref={ref} type={type} className={classes} disabled={disabled} aria-label={ariaLabel} aria-pressed={selected || undefined} onClick={onClick} {...rest}><Icon name={icon} size={20} /></button></Ripple>`
    - 开发期校验: `if (process.env.NODE_ENV !== 'production' && !ariaLabel) console.warn('[IconButton] ariaLabel prop required for accessibility');`（不抛错，仅 warn）
    - 默认 export `IconButton`

    === B. 新建 frontend/src/components/primitives/IconButton.css ===
    - 顶部 `@import './base.css';`
    - 根 `.md-icon-button`: `display: inline-flex; align-items: center; justify-content: center; width: 48px; height: 48px; min-inline-size: 48px; min-block-size: 48px; padding: 0; border: none; border-radius: var(--md-radius-full); background: transparent; color: var(--md-color-on-surface-variant); cursor: pointer; transition: color var(--md-motion-duration-short) var(--md-motion-easing-standard), background var(--md-motion-duration-short) var(--md-motion-easing-standard);`
    - `.md-icon-button--default > svg, .md-icon-button--default .md-ripple-layer { width: 40px; height: 40px; }`（视觉 40dp 居中于 48dp hit box，hit area 走 .md-icon-button 根）
    - `.md-icon-button--fab > svg, .md-icon-button--fab .md-ripple-layer { width: 48px; height: 48px; }`
    - `.md-icon-button--selected { background: var(--md-color-secondary-container); color: var(--md-color-on-secondary-container); }`
    - `.md-icon-button:hover:not(:disabled) { color: var(--md-color-primary); }`
    - `.md-icon-button:focus-visible { outline: var(--md-focus-ring-outer); outline-offset: 2px; }`
    - `.md-icon-button:disabled { opacity: 0.38; cursor: not-allowed; }`
    - state-layer 颜色默认 `var(--md-state-layer-primary)`（surface 背景 tint），由 base.css 接管

    === C. 新建 frontend/src/components/primitives/FAB.jsx ===
    - docstring: "MD3 FAB Primitive (Phase 10 — COMPO-05) — 3 forms: default(56dp 圆形) / extended(56dp+label) / small(40dp visual 48dp hit); 内置 Ripple + 16px 圆角 (D-08); 内置 Icon + forwardRef; 固定位置由 page consumer 控制"
    - imports: `import { forwardRef } from 'react'; import Ripple from './Ripple'; import Icon from './Icon'; import './FAB.css';`
    - `const FAB = forwardRef(function FAB({ icon, variant = 'default', size, label, ariaLabel, disabled = false, type = 'button', className = '', onClick, ...rest }, ref) { ... })`
    - size 推导: `const visualSize = size === 'small' ? 'small' : 'default';` （`size` 仅接受 `undefined` 或 `'small'`，`variant='extended'` 时 size 必须 undefined）
    - 构造 classes: `['md-fab', variant === 'extended' ? 'md-fab--extended' : visualSize === 'small' ? 'md-fab--small' : 'md-fab--default', className].filter(Boolean).join(' ')`
    - extended 渲染: `<Ripple disabled={disabled}><button ref={ref} type={type} className={classes} disabled={disabled} aria-label={ariaLabel || label} onClick={onClick} {...rest}><Icon name={icon} size={24} />{label && <span className="md-fab__label">{label}</span>}</button></Ripple>`
    - default / small 渲染: 同上但无 `<span className="md-fab__label">`
    - 开发期校验: `if (process.env.NODE_ENV !== 'production' && !ariaLabel && !label) console.warn('[FAB] ariaLabel or label prop required');`
    - 默认 export `FAB`

    === D. 新建 frontend/src/components/primitives/FAB.css ===
    - 顶部 `@import './base.css';`
    - 根 `.md-fab`: `display: inline-flex; align-items: center; justify-content: center; gap: 12px; padding: 16px; border: none; border-radius: var(--md-radius-md); background: var(--md-color-primary-container); color: var(--md-color-on-primary-container); cursor: pointer; box-shadow: var(--md-elevation-3); font-family: var(--md-font-body); font-size: 16px; font-weight: 500; line-height: 24px; white-space: nowrap; transition: box-shadow var(--md-motion-duration-medium) var(--md-motion-easing-standard); position: relative;`
    - `.md-fab--default { width: 56px; height: 56px; padding: 16px; }`（icon 居中）
    - `.md-fab--small { width: 40px; height: 40px; padding: 8px; min-inline-size: 48px; min-block-size: 48px; }`（hit 48，视觉 40 — 与 D-09 一致）
    - `.md-fab--extended { height: 56px; min-inline-size: 80px; padding: 16px 20px; }`
    - `.md-fab__label { font-weight: 500; }`
    - `.md-fab:hover:not(:disabled) { box-shadow: var(--md-elevation-3); }`（保持 elevation-3 不变，仅 state-layer 反馈）
    - `.md-fab:active:not(:disabled) { box-shadow: var(--md-elevation-1); }`（按下阴影降级，per UI-SPEC §8.2）
    - `.md-fab:focus-visible { outline: var(--md-focus-ring-outer); outline-offset: 2px; }`
    - `.md-fab:disabled { opacity: 0.38; cursor: not-allowed; box-shadow: none; }`
    - state-color 走 `var(--md-state-layer-on-surface)`（container 背景）
    - 严禁 `border-radius: var(--md-radius-full)` 或 `transform: scale(1.05)`（D-08 + UI-SPEC §8.2 锁死 16px）

    === E. IconButton 迁移（7+ call sites）===
    1. `frontend/src/components/Header.jsx:18-26` — `<Ripple><button className="header-back" onClick={navigate(-1)} title="返回">←</button></Ripple>` → 删除外层 Ripple；`<button className="header-back">` 整段 → `<IconButton icon="arrow-back" ariaLabel="返回" onClick={() => navigate(-1)} className="header-back" />`（保留 .header-back class 用于 layout，迁移后 className 仅作 placement override，CSS 中 .header-back 仍允许 min-width/height 但无视觉属性）
    2. `frontend/src/components/ThemeToggle.jsx:22-28` — `<button className="theme-toggle" onClick={handleToggle} title={...}>{theme.getThemeIcon()}</button>` → `<IconButton icon={currentTheme === 'dark' ? 'light_mode' : 'dark_mode'} ariaLabel="切换主题" onClick={handleToggle} className="theme-toggle" />`（注：light_mode/dark_mode 不在 D-07 30 名称内 — 需扩展 Icon.jsx 映射表 — 在 Task 1 已有的 30 名称基础上追加 `light_mode → LightModeIcon, dark_mode → DarkModeIcon` 两个**ThemeToggle 专用**名称；同步在 PATTERNS.md D-07 表注脚说明此为 D-07 后续 phase 扩展允许的代价，CONTEXT D-07 已明确"后续 Phase 11/12 如需新增图标，需修改 Icon.jsx 映射表"）
    3. `frontend/src/components/InvitationsModal.jsx:100` — `<button className="btn-icon" onClick={...}>` → `<IconButton icon="content_copy" ariaLabel="复制" onClick={...} className="..." />`（content_copy 不在 30 名称 — 临时在 Icon.jsx 加 `content_copy → ContentCopyIcon`，PATTERNS.md 注脚同步）
    4. `frontend/src/components/InvitationsSection.jsx:198` — `<button className="btn-icon">` 复制按钮 → 同上 IconButton
    5. `frontend/src/pages/OrderPage.jsx:469-475` — `<button className="btn-icon btn-sm dish-fav-btn" onClick={handleToggleFavorite(dish, e)} title={...}>{dish.is_favorite ? '❤️' : '🤍'}</button>` → `<IconButton icon={dish.is_favorite ? 'favorite' : 'favorite_border'} ariaLabel={dish.is_favorite ? '取消收藏' : '收藏'} onClick={(e) => handleToggleFavorite(dish, e)} className="dish-fav-btn" />`（favorite_border 不在 30 名称 — 追加映射；D-07 注脚：31-33 名称为 Phase 10 期间必要扩展）
    6. `frontend/src/pages/OrderPage.jsx:544` — `<button className="btn-icon btn-sm">` 数量调整按钮 → 评估语义：若仅是 qty-stepper 的 +/- 按钮（line 212-216 现有 .qty-stepper button），保留原生 `<button className="qty-stepper-btn">`，Task 3 不强制 IconButton 化
    7. `frontend/src/pages/DishDetailPage.jsx:129` — `<button className="btn-icon">` 返回按钮 → `<IconButton icon="arrow-back" ariaLabel="返回" onClick={navigate(-1)} />`
    8. `frontend/src/pages/GuestOrderPage.jsx:399` — `<button className="btn-icon btn-sm">` 关闭按钮 → `<IconButton icon="close" ariaLabel="关闭" onClick={...} />`

    === F. FAB 迁移（1+ call sites）===
    1. `frontend/src/pages/UserWishesPage.jsx:322-329` — `<button type="button" className="fab" onClick={openCreate} aria-label="新建愿望">+</button>` → `<FAB icon="add" ariaLabel="新建愿望" onClick={openCreate} />`（**FAB 自身不内置 position: fixed**，由 page-level CSS class `.fab` 或新 className="user-wishes-fab" 提供 fixed 定位；保留 .fab className 仅作 placement，CSS 删 .fab 视觉属性，保留 position: fixed / bottom / right / z-index 200 在 page 或新 .user-wishes-fab className 中）
    2. 保留 .fab className 在 styles.css 中**仅作为 placement 工具类**（position: fixed / bottom / right / z-index: 200），但**所有视觉属性**（width / height / border-radius / background / color / box-shadow / font-size / display / align-items）由 .md-fab CSS 提供
    3. 其他 "add" 浮动按钮搜索：检查全代码库 `<button className="fab"` 仅 UserWishesPage 一处（如有其他需补充迁移）

    === G. 删除 styles.css 旧选择器 ===
    - 删 line 96-101 (.btn-icon 完整块) — 已被 .md-icon-button 替代
    - 删 line 219-222 (.fab 视觉部分 width/height/border-radius/background/color/box-shadow/transform scale) — 保留 placement (position: fixed / bottom: calc(...) / right: 24px / z-index: 150 / display: flex / align-items / justify-content / font-size) — 但**display: flex / align-items / justify-content / font-size 也由 .md-fab 提供，删除后 styles.css .fab 块可能完全清空**；如完全清空则删除整个 .fab 块（line 219-222），placement 由 page-level className 或 inline style 临时承担 — **不引入新 class**，page 用 inline `style={{ position: 'fixed', bottom: 'calc(var(--md-nav-height) + 16px)', right: 24, zIndex: 200 }}`（或新增 .user-wishes-fab class 在 styles.css 末尾）
    - 实际执行：保留 styles.css:219-222 的 position / bottom / right / z-index，删除 hover 规则（line 222-223 .fab:hover { transform: scale(1.05); box-shadow: var(--md-elevation-3); } — scale 移除由 .md-fab 接管，box-shadow elevation-3 已由 .md-fab 提供）；最终 .fab 选择器内容压缩为：`position: fixed; bottom: calc(var(--md-nav-height) + 16px); right: 24px; z-index: 200;`（注意：UserWishesPage.jsx 的 import 改为 `import FAB from '../components/primitives/FAB';`，JSX 中 `<FAB icon="add" ariaLabel="新建愿望" onClick={openCreate} className="fab" />`，FAB 自身不带 fixed，由 .fab className 提供 placement）
    - 更新 line 622 全局 min-size 规则：删除 `.btn-icon,`（已无 .btn-icon），删除 `.fab,`（.fab placement 工具类已无需 48dp min-size — 由 .md-fab 自身保证）
    - 删除 line 578 `.fab:focus-visible` (.md-fab 自带)
    - 更新 line 81 (.btn-icon `transition: all`) 已在 Task 2 删除（line 80-106 整段删除）

    === H. 验证迁移完整性 ===
    `grep -rE 'className=.*\bbtn-icon\b' frontend/src/ --include='*.jsx'` 应 0 命中；`grep -rE 'className="fab"' frontend/src/ --include='*.jsx'` 仅 UserWishesPage 一处且已用 .fab className 仅作 placement（不影响视觉）。

  </action>

  <verify>
    <automated>cd frontend && npm run lint 2>&1 | tail -20; npm run build 2>&1 | tail -10; grep -rE 'className=.*\bbtn-icon\b' src/ --include='*.jsx' | wc -l; grep -rE 'className="fab"|className=.fab[^a-z]' src/ --include='*.jsx' | wc -l; grep -cE "^\.(btn-icon|fab)\s*\{" src/css/styles.css</automated>
  </verify>

  <acceptance_criteria>
    1. `frontend/src/components/primitives/IconButton.jsx` 存在；`grep "IconButton" frontend/src/components/primitives/IconButton.jsx` 命中；`grep -E "(ariaLabel|aria-label)" frontend/src/components/primitives/IconButton.jsx` 命中；`grep "density.*default.*fab" frontend/src/components/primitives/IconButton.jsx` 命中 API 字符串；`forwardRef` 包裹存在；`grep "from './Icon'" frontend/src/components/primitives/IconButton.jsx` 命中 Icon import。
    2. `frontend/src/components/primitives/IconButton.css` 存在；`grep -E "^\.md-icon-button" frontend/src/components/primitives/IconButton.css` 命中 ≥ 3 (根 + 2 density)；`grep "var(--md-radius-full)" frontend/src/components/primitives/IconButton.css` 命中（圆形）；`grep "var(--md-focus-ring-outer)" frontend/src/components/primitives/IconButton.css` 命中。
    3. `frontend/src/components/primitives/FAB.jsx` 存在；`grep -E "(md-fab--default|md-fab--extended|md-fab--small)" frontend/src/components/primitives/FAB.jsx` 命中 3 个 form 字符串；`grep "var(--md-radius-md)" frontend/src/components/primitives/FAB.jsx` 命中注释或 CSS 引用（16px 圆角）。
    4. `frontend/src/components/primitives/FAB.css` 存在；`grep "var(--md-radius-md)" frontend/src/components/primitives/FAB.css` 命中 16px 圆角；`grep "var(--md-elevation-3)" frontend/src/components/primitives/FAB.css` 命中 rest 阴影；`grep "var(--md-elevation-1)" frontend/src/components/primitives/FAB.css` 命中 pressed 阴影；`grep "var(--md-focus-ring-outer)" frontend/src/components/primitives/FAB.css` 命中（FAB 用 outer 因容器色背景）；`grep -E "scale\(1\.05\)|radius-full" frontend/src/components/primitives/FAB.css` 0 命中（D-08 + UI-SPEC 锁定 16px 不缩放）。
    5. 8 个 IconButton 迁移文件（Header, ThemeToggle, InvitationsModal, InvitationsSection, OrderPage×1, DishDetailPage, GuestOrderPage）每个均 `import IconButton from '../components/primitives/IconButton';`；`grep -rL "from.*primitives/IconButton" frontend/src/pages frontend/src/components --include='*.jsx' | xargs grep -lE '<button[^>]*className=.*\bbtn-icon\b'` 为空。
    6. `frontend/src/pages/UserWishesPage.jsx:322-329` 改为 `<FAB icon="add" ariaLabel="新建愿望" onClick={openCreate} className="fab" />`（**保留 .fab className 作 placement**）；`grep "from.*primitives/FAB" frontend/src/pages/UserWishesPage.jsx` 命中。
    7. Icon.jsx 新增 3 个映射：light_mode / dark_mode / content_copy / favorite_border — `grep -cE "(LightMode|DarkMode|ContentCopy|FavoriteBorder)Icon" frontend/src/components/primitives/Icon.jsx` ≥ 4。
    8. styles.css 不含 .btn-icon 选择器（`grep -cE "^\.btn-icon" src/css/styles.css` = 0）；styles.css .fab 选择器仅剩 placement 规则（position / bottom / right / z-index），无 width/height/border-radius/background/color/box-shadow/font-size/display/align-items/justify-content 等视觉属性。
    9. `cd frontend && npm run lint` 0 error（不增加 baseline）。
    10. `cd frontend && npm run build` 0 error（exit 0）。
  </acceptance_criteria>

  <done>IconButton / FAB primitives 就位；8+ IconButton + 1+ FAB 迁移完成；styles.css .btn-icon / .fab 视觉规则删除（保留 .fab placement）；Icon.jsx 因 ThemeToggle 实际需要小幅扩展（light_mode/dark_mode/content_copy/favorite_border），均在 PATTERNS.md D-07 注脚允许范围内；lint/build 通过；零业务逻辑回归。</done>
</task>

</tasks>

<verification>
**Plan-level automated verification (run after Task 3 completes):**

```bash
cd /home/temila/family_chef/frontend

# 1. 全 primitive 文件就位
test -f src/components/primitives/base.css
test -f src/components/primitives/Button.jsx
test -f src/components/primitives/Button.css
test -f src/components/primitives/IconButton.jsx
test -f src/components/primitives/IconButton.css
test -f src/components/primitives/FAB.jsx
test -f src/components/primitives/FAB.css
test -f src/components/primitives/Icon.jsx
test -f src/components/primitives/Ripple.jsx
test -f src/components/primitives/ripple.css

# 2. 旧文件已删除
test ! -f src/components/Icon.jsx
test ! -f src/components/Ripple.jsx
test ! -f src/css/ripple.css

# 3. 旧 .btn-* 旧选择器已清除（除 .btn-search）
grep -cE "^\.(btn-primary|btn-secondary|btn-outline|btn-sm|btn-lg|btn-block|btn-icon)\s*\{" src/css/styles.css
# 期望: 0

# 4. .btn-search 工具类保留
grep -c "^\.btn-search" src/css/styles.css
# 期望: 1

# 5. 旧 className 引用已迁移
grep -rE 'className=.*\b(btn-primary|btn-secondary|btn-outline|btn-sm|btn-lg|btn-block)\b' src/ --include='*.jsx' | grep -v 'btn-search' | wc -l
# 期望: 0
grep -rE 'className=.*\bbtn-icon\b' src/ --include='*.jsx' | wc -l
# 期望: 0
grep -rE 'className="fab"|className=.fab[^a-z]' src/ --include='*.jsx' | wc -l
# 期望: 1 (UserWishesPage .fab 仅作 placement)

# 6. font 依赖与 import 已清除
grep '"material-symbols"' package.json
# 期望: 0
grep "material-symbols" src/index.css
# 期望: 0

# 7. SVG tree-shaking 依赖已安装
test -d node_modules/@material-symbols-svg/react

# 8. Ripple public API 仍可被外部 composite 消费（WishCard 等）
grep "from './Ripple'" src/components/WishCard.jsx
grep "from './Ripple'" src/components/DishCard.jsx
grep "from './Ripple'" src/components/Sidebar.jsx
# 期望: 全部命中（公开 API 兼容）

# 9. validation gate
npm run lint  # 0 新增 error
npm run build # 0 error
```

**Human verification (post-execution):**
- 浏览器访问 `/login` → 确认 Button 视觉（filled 渐变深绿 + 涟漪 + focus ring）+ `type="submit"` 表单提交零回归
- 浏览器访问 `/home` → 确认 DishCard 内部按钮（推荐/可点/已售罄）Badge 旧色（10-03 替换前）
- 浏览器访问 `/my-wishes`（user 角色）→ 确认 FAB 渲染在右下 fixed 位置 + 涟漪 + click 打开 modal
- 浏览器访问 `/admin/dishes` 搜索栏 → 确认 .btn-search 紧凑按钮视觉（32dp 高度，12sp 文字）
- 浏览器访问深浅色切换 → 确认 Icon SVG 颜色继承 currentColor 随主题切换
</verification>

<success_criteria>
**Mapped to ROADMAP Phase 10 success criteria 1-3 + CONTEXT D-01..D-17:**

- [ ] **SC-1 (ROADMAP SC#1, COMPO-01, LOGIC-03)**: Button 组件支持 `variant="filled|tonal|outlined|text"` + `size="sm|md|lg"` + `loading`，替换现有三档 `.btn-*`；保留现有 onClick / disabled / type / children / className 等 props；事件绑定零回归 — 由 acceptance_criteria Task 2 #1, #3, #4 验证
- [ ] **SC-2 (ROADMAP SC#2, COMPO-02, COMPO-05)**: IconButton 渲染为 40dp 默认 / 48dp FAB-density 圆形目标；FAB 渲染为 56dp 圆形 + Extended FAB + Small FAB 40dp 三种形态；现有 `.fab` 与 `.btn-icon` 用法可平滑迁移 — 由 acceptance_criteria Task 3 #1-#5 验证
- [ ] **SC-3 (CONTEXT D-02)**: 换皮后 styles.css 中 `.btn-primary/.btn-secondary/.btn-outline/.btn-icon/.btn-sm/.btn-lg/.btn-block/.fab` 旧选择器**全部**删除（`.btn-search` 例外）；调用方零残留 — 由 verification #3, #5 验证
- [ ] **SC-4 (CONTEXT D-03)**: `.btn-search` 工具类在 styles.css 中保留，6 处搜索栏紧凑按钮改用 `<Button className="btn-search" variant="filled" size="sm">` — 由 acceptance_criteria Task 2 #5 验证
- [ ] **SC-5 (CONTEXT D-05/D-06/D-07)**: `@material-symbols-svg/react` v1.0.38 新增到 dependencies；`material-symbols` v0.45.9 从 devDependencies 删除；index.css 中 `@import 'material-symbols/outlined.css'` 删除；Icon.jsx 维护 30+ 个（实际 30 + 4 个 Phase 10 必要扩展）name→SVG 映射表，Vite tree-shake 未使用图标 — 由 acceptance_criteria Task 1 #3, #4, #5 验证
- [ ] **SC-6 (CONTEXT D-08)**: FAB 全部变体采用 `--md-radius-md` (16px) 圆角；CSS 中无 `border-radius: var(--md-radius-full)` 或 `transform: scale(1.05)` 用于 FAB — 由 acceptance_criteria Task 3 #4 验证
- [ ] **SC-7 (CONTEXT D-12)**: Button / IconButton / FAB 组件内部内置 Ripple（`disabled === false` 时）；外部 `<Ripple>` 公开 API（children / disabled / className / style）不变；Phase 10 之外的 composite（WishCard/Header/Sidebar/DishCard）继续手动 import — 由 verification #8 验证
- [ ] **SC-8 (CONTEXT D-17)**: Button `loading={isPending}` 渲染 16dp 圆形 SVG spinner（`stroke="currentColor"` + `spin 0.8s linear infinite`），label 保留不变；`aria-busy="true"` 自动添加 — 由 acceptance_criteria Task 2 #1 验证
- [ ] **SC-9 (CONTEXT D-04)**: `npm run lint` 0 新增 error（基线 ≥90 errors 不增加）；`npm run build` 0 error — 由 verification #9 验证
- [ ] **SC-10 (LOGIC-01/02/03)**: 32+ 按钮 / 8+ IconButton / 1+ FAB 迁移后，所有 onClick / onChange / disabled / type / children / 业务回调零回归；后端零改动（backend/app/* 无任何修改）— 由 verification 浏览器手测确认

**Distribution of CSS deletion (per orchestrator instruction):**
- 10-01 删除：`.btn-primary`、`.btn-secondary`、`.btn-outline`、`.btn-icon`、`.btn-sm`、`.btn-lg`、`.btn-block`、`.fab` 视觉（保留 placement）
- 10-02 将删除：`.card`、`.dish-card`、`.wish-card`、`.form-input`、`.form-label`、`.form-group`、`.form-error`、`.card-body`（仅 Card primitive 接管部分）
- 10-03 将删除：`.badge`、`.badge-warn`、`.badge-danger`、`.badge-success`、`.badge-info`、`.badge-accent`、`.badge-gold`、`.badge-muted`、`.badge-count`、`.filter-chip`
- **NOTE**: `.btn-search` 永久保留（D-03 锁定）
</success_criteria>

<must_haves>
**Goal-backward truths — by end of 10-01 plan, the following must be TRUE for Phase 10 goal to advance:**

1. **A new primitives library exists at `frontend/src/components/primitives/`** — directory created, 5 JSX + 5 CSS + 1 shared base.css = 11 new files, all consuming MD3 tokens only.
2. **Button is a working MD3 4-variant component** — `<Button variant="filled" size="md">保存</Button>` renders with primary fill / on-primary text / 12px radius / 48dp hit target / internal ripple / focus ring; loading prop adds 16dp spinner without layout shift.
3. **IconButton is a working MD3 density-aware component** — `<IconButton icon="arrow-back" ariaLabel="返回" onClick={fn} />` renders as 40dp visual inside 48dp hit box, accessible name required (dev warn if missing), internal ripple.
4. **FAB is a working MD3 3-form component** — `<FAB icon="add" />` renders 56dp 16px-corner primary-container circle, `<FAB variant="extended" icon="add" label="新建愿望" />` renders 56dp+label pill, `<FAB size="small" icon="add" />` renders 40dp visual 48dp hit; no internal fixed positioning (placement by page).
5. **Icon is SVG tree-shaking 30+ name registry** — `<Icon name="home" size={24} />` returns Home SVG with `currentColor`; unknown name warns in dev and renders null; bundle no longer ships material-symbols font.
6. **28+ button / 8+ IconButton / 1+ FAB consumer call sites are migrated** — old `<button className="btn btn-primary">` patterns replaced with `<Button variant="filled">`; old `<button className="btn-icon">` patterns replaced with `<IconButton icon="...">`; old `<button className="fab">` pattern replaced with `<FAB>`; onClick / disabled / type / children behavior preserved.
7. **Old .btn-* / .btn-icon / .fab visual CSS is removed from styles.css** — grep returns 0 matches for old selectors; only `.btn-search` utility class remains (D-03); .fab className is reduced to placement-only (position / bottom / right / z-index).
8. **Ripple public API is preserved for external consumers** — WishCard, DishCard, Header, Sidebar, etc. can still `import Ripple from '../components/primitives/Ripple'` with the same props; component is now co-located with primitives but external API is unchanged.
9. **`npm run lint` and `npm run build` pass** — no new lint errors introduced beyond Phase 9 baseline; Vite build produces 0 errors.
10. **Zero business logic regressions** — all onClick / onChange / disabled / type / form submission / loading state behavior is identical to pre-migration; backend code is untouched (LOGIC-02).
</must_haves>

<output>
Create `/home/temila/family_chef/.planning/phases/10-primitive-components/10-01-SUMMARY.md` when done
</output>
