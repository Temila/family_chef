---
phase: 10-primitive-components
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - frontend/src/css/styles.css
  - frontend/src/components/DishCard.jsx
  - frontend/src/components/WishCard.jsx
  - frontend/src/components/GuestDishCard.jsx
  - frontend/src/components/PasswordInput.jsx
  - frontend/src/components/WishFormModal.jsx
  - frontend/src/components/WishRejectModal.jsx
  - frontend/src/pages/LoginPage.jsx
  - frontend/src/pages/ForceChangePasswordPage.jsx
  - frontend/src/pages/UserProfilePage.jsx
  - frontend/src/pages/OrderPage.jsx
  - frontend/src/pages/ChefDishesPage.jsx
  - frontend/src/pages/AdminDishesPage.jsx
  - frontend/src/pages/AdminIngredientsPage.jsx
  - frontend/src/pages/AdminCategoriesPage.jsx
  - frontend/src/pages/AdminChefsPage.jsx
  - frontend/src/pages/AdminUsersPage.jsx
  - frontend/src/components/primitives/Card.jsx
  - frontend/src/components/primitives/Card.css
  - frontend/src/components/primitives/Input.jsx
  - frontend/src/components/primitives/Input.css
autonomous: true
requirements: [COMPO-03, COMPO-04, LOGIC-01, LOGIC-02, LOGIC-03]

must_haves:
  truths:
    - "Card 组件支持 elevated/filled/outlined 三变体，slot API 为 image/header/children(body)/footer；elevation-1→2 自动继承 Phase 9 过渡（COMPO-03）"
    - "DishCard / WishCard / GuestDishCard 重构为基于 <Card> primitive 的 slot-based 薄包装；domain 业务数据通过 props 传入，className=dish-card/wish-card 旧实现彻底消失（D-13）"
    - "20+ 通用 .card 消费方（Admin 移动列表 / OrderDetailPage / ChefDishesPage 等）迁移到 <Card> primitive"
    - "Input 组件支持 outlined/filled 两变体 + label prop（MD3 浮动 label，CSS-only :placeholder-shown 实现）+ error prop + 错误图标 + 辅助文本 + 无 label 模式（COMPO-04）"
    - "12 个表单 family（PasswordInput, WishFormModal, WishRejectModal, LoginPage, ForceChangePasswordPage, UserProfilePage, OrderPage, ChefDishesPage, AdminDishesPage, AdminCategoriesPage, AdminChefsPage, AdminIngredientsPage, AdminUsersPage）迁移到 <Input> 组件"
    - "styles.css 中 .card / .dish-card / .wish-card / .card-body / .form-input / .form-label / .form-group / .form-error 旧选择器全量删除（无消费者零残留，grep 验证）"
    - "原生 form 提交零回归（LoginPage type='submit' 仍正常工作；WishFormModal textarea / select 正确支持）"
  artifacts:
    - path: "frontend/src/components/primitives/Card.jsx"
      provides: "Card elevated/filled/outlined + image/header/children/footer slot API + forwardRef + optional onClick"
      exports: ["default"]
    - path: "frontend/src/components/primitives/Card.css"
      provides: ".md-card 根 + 3 变体 + 4 slot 几何 + elevation transition 150ms"
      contains: ".md-card--elevated"
    - path: "frontend/src/components/primitives/Input.jsx"
      provides: "Outlined/Filled variants + floating label (CSS-only) + error state + supporting text + leading/trailing slot + forwardRef"
      exports: ["default"]
    - path: "frontend/src/components/primitives/Input.css"
      provides: ".md-input-wrapper + .md-input-field + .md-input-label + .md-input-supporting + .md-input-error + floating label transition"
      contains: ".md-input-field"
  key_links:
    - from: "frontend/src/components/DishCard.jsx"
      to: "frontend/src/components/primitives/Card.jsx"
      via: "import Card; <Card image={...} footer={<Badges />}>...</Card>"
    - from: "frontend/src/components/WishCard.jsx"
      to: "frontend/src/components/primitives/Card.jsx"
      via: "import Card; <Card>...</Card> (含 actions slot via footer)"
    - from: "frontend/src/components/primitives/Card.css"
      to: "frontend/src/components/primitives/base.css"
      via: "@import './base.css' for clickable variant state-layer / focus"
    - from: "frontend/src/components/primitives/Input.jsx"
      to: "frontend/src/components/primitives/Input.css"
      via: "import './Input.css' (顶部 @import './base.css')"
    - from: "frontend/src/pages/LoginPage.jsx"
      to: "frontend/src/components/primitives/Input.jsx"
      via: "import Input; <Input label='用户名' type='text' value={...} onChange={...} />"
---

<objective>
**Phase 10, Plan 02 — Wave 2: Card primitive (3 variants + slot API) + Input primitive (2 variants + floating label + error) + 3 domain card 抽象 + 20+ 通用 .card 迁移 + 12 表单 family 迁移 + 旧 .card / .dish-card / .wish-card / .form-input / .form-label CSS 删除**

为前端落地 2 个 Phase 10 primitive（Card 3 变体 + 4 slot API、Input 2 变体 + 浮动 label + 错误态），同时完成最关键的一次 domain 重构：将 DishCard / WishCard / GuestDishCard 从自包含 .dish-card / .wish-card 实现抽象为薄 slot-based 包装（D-13），20+ 通用 .card 消费方迁移到 `<Card>` primitive，12 个表单 family 迁移到 `<Input>` 组件，最后删除 styles.css 中已无消费者依赖的 `.card / .dish-card / .wish-card / .card-body / .form-input / .form-label / .form-group / .form-error` 旧选择器。

**Purpose:** 这是 v1.2 MD3 重构中**最深的组件重构**（D-13）—— domain 卡片从"自包含"转向"slot 组合"，让 Card primitive 接管所有视觉/状态/动效，domain 组件仅保留业务逻辑与 slot 填充。这是 Phase 12 页面级重组的基石。Input 浮动 label 引入 MD3 Notched/Notch 0/Notch full 三状态机（CSS-only 实现，无 JS state）。

**Output:** 2 个新 primitive (Card + Input) + 2 个新 primitive CSS + 3 domain card slot 抽象重构 + 20+ 通用 .card 迁移 + 12 表单 family 迁移 + 旧 CSS 选择器清理。`npm run lint` 与 `npm run build` 0 error，零业务逻辑回归（LOGIC-01/02/03）。

</objective>

<execution_context>
@/home/temila/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/temila/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md (COMPO-03, COMPO-04)
@.planning/ROADMAP.md (Phase 10 success criteria 3-4)
@.planning/STATE.md
@.planning/phases/10-primitive-components/10-CONTEXT.md (D-11 Input 模式 + D-13 Domain Card slot 抽象)
@.planning/phases/10-primitive-components/10-UI-SPEC.md (§9 Card 契约 / §10 Input 契约 / §3 文件结构)
@.planning/phases/10-primitive-components/10-PATTERNS.md:222-292 (Card/Input 模拟模式 + 偏差列表)
@.planning/phases/10-primitive-components/10-01-PLAN.md (Wave 1 完成的 primitives 基础)
@.planning/phases/09-motion-state-layers/09-UI-SPEC.md §3.3 (Card elevation-1→2 过渡锁定 short 150ms)
@frontend/src/css/tokens.css
@frontend/src/css/styles.css
@frontend/src/components/DishCard.jsx
@frontend/src/components/WishCard.jsx
@frontend/src/components/GuestDishCard.jsx
@frontend/src/components/PasswordInput.jsx
@frontend/src/components/primitives/base.css (10-01 已就位)
@frontend/src/components/primitives/Button.jsx (10-01 已就位 — WishCard actions slot 内部按钮)
@frontend/src/components/primitives/Badge.jsx (10-03 才就位; DishCard footer 内部 Badge 调用需临时保留旧组件或直接 inline 旧 className)
@frontend/src/components/Badge.jsx (Phase 9 旧 Badge — 10-03 删除)
@frontend/src/utils/index.js (statusBadge() 工具 — D-15 不动)
@frontend/src/pages/LoginPage.jsx
@frontend/src/pages/AdminDishesPage.jsx
@frontend/src/pages/AdminIngredientsPage.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Card primitive (3 variants + slots) + DishCard / WishCard / GuestDishCard slot 抽象重构 + 20+ 通用 .card 迁移 + 旧 .card / .dish-card / .wish-card CSS 删除</name>

  <files>
    frontend/src/components/primitives/Card.jsx,
    frontend/src/components/primitives/Card.css,
    frontend/src/components/DishCard.jsx,
    frontend/src/components/WishCard.jsx,
    frontend/src/components/GuestDishCard.jsx,
    frontend/src/components/OrderPage.jsx,
    frontend/src/components/OrderDetailPage.jsx,
    frontend/src/components/ChefDishesPage.jsx,
    frontend/src/components/AdminDishesPage.jsx,
    frontend/src/components/AdminIngredientsPage.jsx,
    frontend/src/components/AdminCategoriesPage.jsx,
    frontend/src/components/AdminUsersPage.jsx,
    frontend/src/components/AdminChefsPage.jsx,
    frontend/src/components/AdminLogsPage.jsx,
    frontend/src/css/styles.css
  </files>

  <read_first>
    .planning/phases/10-primitive-components/10-UI-SPEC.md §9 (Card API + slot geometry + variant matrix + elevated 1→2 transition)
    .planning/phases/10-primitive-components/10-PATTERNS.md:222-255 (Card.jsx + Card.css 模拟模式)
    .planning/phases/10-primitive-components/10-CONTEXT.md D-13 (Domain Card slot 抽象)
    .planning/phases/09-motion-state-layers/09-UI-SPEC.md §3.3 (Card elevation-1→2 short 150ms 锁定)
    frontend/src/css/tokens.css:147 (--md-radius-md 16px for Card)
    frontend/src/css/styles.css:108-129 (.card / .card-body / .dish-card 完整源 — 删除目标)
    frontend/src/css/styles.css:467-484 (.wish-card 完整源 — 删除目标)
    frontend/src/components/DishCard.jsx:1-69 (完整 — slot 抽象目标)
    frontend/src/components/WishCard.jsx:1-209 (完整 — slot 抽象目标, 含 actions 区域)
    frontend/src/components/GuestDishCard.jsx:1-44 (完整 — slot 抽象目标)
    frontend/src/components/Badge.jsx (旧 Badge — DishCard/WishCard/GuestDishCard 调用 — Task 1 末尾临时调整; 10-03 彻底替换)
  </read_first>

  <action>
    === A. 新建 frontend/src/components/primitives/Card.jsx ===
    - docstring: "MD3 Card Primitive (Phase 10 — COMPO-03) — 3 variants (elevated/filled/outlined) + 4 slots (image/header/children/footer) + optional onClick; clickable 时启用 ripple/state/elevation; static 时无 cursor/ripple"
    - imports: `import { forwardRef } from 'react'; import Ripple from './Ripple'; import './Card.css';`
    - `const Card = forwardRef(function Card({ variant = 'elevated', image, header, footer, onClick, className = '', children, ...rest }, ref) { ... })`
    - 构造 classes: `['md-card', \`md-card--\${variant}\`, onClick && 'md-card--clickable', className].filter(Boolean).join(' ')`
    - clickable 判定: `const isClickable = Boolean(onClick);`（onClick 缺失则不启用 ripple/state/elevation hover）
    - 渲染：`<Ripple disabled={!isClickable}><div ref={ref} className={classes} onClick={onClick} role={onClick ? 'button' : undefined} tabIndex={onClick ? 0 : undefined} onKeyDown={onClick ? (e) => { if (e.key === 'Enter' || e.key === ' ') { e.preventDefault(); onClick(e); } } : undefined} {...rest}>{image && <div className="md-card__image">{image}</div>}{header && <div className="md-card__header">{header}</div>}<div className="md-card__body">{children}</div>{footer && <div className="md-card__footer">{footer}</div>}</div></Ripple>`
    - 默认 export `Card`

    === B. 新建 frontend/src/components/primitives/Card.css ===
    - 顶部 `@import './base.css';`
    - 根 `.md-card`: `display: flex; flex-direction: column; border-radius: var(--md-radius-md); overflow: hidden; position: relative; transition: box-shadow var(--md-motion-duration-short) var(--md-motion-easing-standard);`
    - 变体:
      * `.md-card--elevated { background: var(--md-color-surface-container-low); box-shadow: var(--md-elevation-1); }` `.md-card--elevated:hover { box-shadow: var(--md-elevation-2); }`（无 border）
      * `.md-card--filled { background: var(--md-color-surface-container-highest); box-shadow: var(--md-elevation-0); }`（无 hover elevation）
      * `.md-card--outlined { background: var(--md-color-surface); border: 1px solid var(--md-color-outline-variant); box-shadow: var(--md-elevation-0); }`（无 hover elevation）
    - `.md-card--clickable { cursor: pointer; }` `.md-card--clickable:focus-visible { outline: var(--md-focus-ring-outer); outline-offset: 2px; }`
    - slot 几何 (UI-SPEC §9.3):
      * `.md-card__image { width: 100%; aspect-ratio: 4/3; overflow: hidden; position: relative; background: var(--md-color-surface-container); }` `.md-card__image img { width: 100%; height: 100%; object-fit: cover; display: block; }`（top corner clipping 由 .md-card overflow:hidden 接管）
      * `.md-card__header { padding: 16px 16px 8px 16px; font-size: 16px; font-weight: 500; line-height: 24px; color: var(--md-color-on-surface); }`（header title）
      * `.md-card__body { padding: 16px; font-size: 16px; font-weight: 400; line-height: 24px; color: var(--md-color-on-surface); flex: 1; }`（缺少 header 时 `.md-card__body { padding-top: 16px; }` 由 .md-card 容器 padding 折叠）
      * `.md-card__footer { padding: 16px; padding-top: 0; display: flex; align-items: center; justify-content: flex-end; gap: 8px; }`（actions 默认 right-aligned）
    - 注：image 无 padding 16dp（按 UI-SPEC §9.3 slot 几何 — image 不带内部 padding）

    === C. DishCard 重构为 slot-based 薄包装 ===
    重写 `frontend/src/components/DishCard.jsx`:
    - import: `import Card from './primitives/Card'; import { formatPrice } from '../utils';`
    - 不再 import Ripple（已由 Card 内部化）
    - 不再 import 旧 Badge（10-03 替换前临时保留 className="badge badge-info" / "badge-success" / "badge-danger" / "badge-gold" inline — 这些旧 className 由 10-03 Task 1 处理；本 Task 末尾 styles.css 删除 .dish-card 时这些 inline 旧 badge className **会失效**，但仍由 styles.css 提供 .badge-* 选择器 — 10-03 才删；Phase 10 中间态是 OK 的）
    - 渲染: `<Card variant="elevated" image={<img src={dish.image_url} alt={dish.name} onError={...} />} onClick={() => navigate(`/dishes/${dish.id}`)} footer={!simple && <div style={{display:'flex', gap:8, alignItems:'center'}}><span className="badge badge-info">{dish.category_name || '默认'}</span>{dish.is_available ? <span className="badge badge-success">可点</span> : <span className="badge badge-danger">已售罄</span>}</div>}><div style={{fontSize:'0.95rem', fontWeight:600, marginBottom:4}}>{dish.name}</div>{!simple && <div style={{fontSize:'0.75rem', color:'var(--md-color-on-surface-variant)', marginBottom:8}}>{dish.cuisine_name && `${dish.cuisine_name} · `}{dish.base_price !== null && `¥${formatPrice(dish.base_price)}`}</div>}</Card>`
    - image slot 中保留 `onError` 处理 + placeholder emoji fallback（业务逻辑零回归）
    - 注：DishCard 顶层 `<div className="dish-card-badges">` (推荐) 区域在 image slot 内 `<img>` 之后绝对定位，由 image slot 容器 `.md-card__image { position: relative; }` 接管 — 实际不需要（推荐标签是 wish-card 概念？查 DishCard.jsx line 40-44 — 是 DishCard 内部），保留为 image slot 的兄弟节点或 footer 内的第一个 child

    === D. WishCard 重构为 slot-based 薄包装 ===
    重写 `frontend/src/components/WishCard.jsx`（最大规模 domain 重构）:
    - import: `import Card from './primitives/Card'; import Button from './primitives/Button'; import Badge from './primitives/Badge';` (Badge 由 10-03 提供 — **本 Task 末尾先临时保留 `<Badge status={...}>` 调用旧组件，由 10-03 Task 1 替换为新 primitive**; 旧 Badge.jsx 在 10-03 Task 1 重写为新 primitive)
    - 保留 `DANGER_BTN_STYLE` 常量
    - 保留 `HTTP_URL_RE`, `renderActions()`, `renderReferenceUrl()`, `hasSecondary` 等业务逻辑
    - 渲染外层：`<Card variant="elevated" onClick={canTap ? () => onTap?.(wish) : undefined} footer={hasActions ? <div className="wish-card-actions" onClick={(e) => e.stopPropagation()}>{actions}</div> : undefined}>`
    - 内部 children (body): top row + meta + secondary + unread dot + status badge — 全部用 inline style 表达 MD3 spec (font-family display, fontSize 1.25rem, etc.)；不再使用 .wish-card-name / .wish-card-meta / .wish-card-secondary 等 className
    - actions slot 内部按钮用 `<Button variant="filled" size="sm" className="flex-1">` 与 `<Button variant="outlined" size="sm" className="flex-1" style={DANGER_BTN_STYLE}>` 替换原 `<button className="btn btn-primary btn-sm flex-1">` 模式
    - 高亮 (`highlight` prop) 走 inline `style={{ outline: '3px solid var(--md-color-primary)', outlineOffset: 2, boxShadow: '0 0 0 4px var(--md-color-primary-container)' }}` — 替换 .wish-card-highlight className
    - readonly 视觉 (`.wish-card-readonly` opacity 0.7) 走 inline `style={{ opacity: 0.7, cursor: 'default' }}` 仅当 hasActions 为 false
    - 业务 API (props 名称、回调、role 行为) 零变化
    - 删除 .wish-card-unread-dot 旧 CSS（替换为 inline `position: absolute; top: 16px; right: 16px; width: 8px; height: 8px; border-radius: 50%; background: var(--md-color-error);`）；DOM 树可保留 `<span>` 结构加 `aria-hidden="true"` 与 `<span className="sr-only">未读</span>` 不变

    === E. GuestDishCard 重构为 slot-based 薄包装 ===
    重写 `frontend/src/components/GuestDishCard.jsx`:
    - import: `import Card from './primitives/Card'; import Button from './primitives/Button'; import { useState } from 'react';` (qty-stepper 需要 state — 现有 inline 实现无 state，迁移到 useState 内部组件 — 或保留 prop-driven 模式由父组件 OrderPage 维护)
    - **保持现有 prop API 不变**（`{ dish, quantity, onAdd, onRemove }`），不引入新 state
    - 渲染: `<Card variant="elevated" image={<img src={dish.image_url} alt={dish.name} onError={...} />} footer={<div>{quantity === 0 ? <Button variant="filled" size="sm" className="guest-add-btn" ariaLabel="加入点菜" onClick={() => onAdd(dish)}>+</Button> : <div className="qty-stepper"><button onClick={() => onRemove(dish.id)}>−</button><span className="qty-value">{quantity}</span><button onClick={() => onAdd(dish)}>+</button></div>}</div>}>...</Card>`
    - 注：**GuestDishCard 无 onClick 触发整体卡片** (D-08 访客页 .dish-card `pointer-events: none` — Card primitive 在 `onClick` 缺失时自动不启用 ripple/state/elevation，符合静态展示需求)
    - 内部 children (body): name + categories meta + qty area — inline style 表达

    === F. 20+ 通用 .card 消费方迁移 ===
    目标: `className="card"` + `className="card-body"` 模式 → `<Card variant="elevated">{body}</Card>`
    清单（**注：每处迁移后 inline `style={{ marginBottom: 10/12 }}` 由父容器 .mobile-card-list gap 接管，**作为 Phase 12 8dp 网格清理的临时中间态**; 或者**保留 inline style 不变**，确保视觉零回归**）:
    1. `frontend/src/pages/AdminIngredientsPage.jsx:390-392` — `<div className="card"><div className="card-body">` → `<Card variant="elevated" style={{ marginBottom: 10 }}><div>` 闭合 `</Card></div>`
    2. `frontend/src/pages/AdminUsersPage.jsx:223-225` — 同上
    3. `frontend/src/pages/ChefDishesPage.jsx:634-636` — 同上
    4. `frontend/src/pages/OrderPage.jsx:570-571` — `<div className="card">` → `<Card variant="elevated">` 闭合
    5. `frontend/src/pages/AdminLogsPage.jsx:111-113` — 同上
    6. `frontend/src/pages/AdminChefsPage.jsx:134-136` — 同上
    7. `frontend/src/pages/AdminCategoriesPage.jsx:175-177` — 同上
    8. `frontend/src/pages/OrderDetailPage.jsx:62-63, 86, 121` — 3 处 `<div className="card"><div className="card-body">` → `<Card variant="elevated"><div>...`
    9. `frontend/src/pages/AdminDishesPage.jsx:613-614` — 同上
    每个文件: `import Card from '../components/primitives/Card';`
    范围外（**不迁移**）: `.login-card` (LoginPage, ForceChangePasswordPage), `.profile-card` (UserProfilePage), `.stat-card` (AdminHomePage, UserProfilePage, AdminStatsPage), `.order-card` (UserOrdersPage, ChefOrdersPage), `.dietary-warning-card` (DishDetailPage), `.preference-section` (PreferencesPage) — 这些是 domain-specific 视觉（多数含 padding 32+dp / 渐变 / icon header），非通用 .card，Phase 12 决定是否抽象

    === G. 删除 styles.css 旧 .card / .dish-card / .wish-card 视觉 ===
    - 删 line 108-114 (.card + .card::before + .card:hover + .card-body 完整块) — 由 .md-card + .md-card__body 接管
    - 删 line 116-129 (.dish-card 完整块 — 含 image / body / name / meta / footer / badges / placeholder-img 内部样式) — DishCard 全部 inline 化
    - 删 line 468-484 (.wish-card 完整块 — 含 top / name / badge-slot / meta / secondary / reject-reason / actions / readonly / unread-dot / sr-only) — WishCard 全部 inline 化
    - 删 line 496-497 (.wish-card-highlight) — WishCard inline 化
    - 删 line 205 (.dish-card-wrapper) — 移动列表容器，由 .mobile-card-list 保留
    - 删 line 452 (.dish-card-actions) — WishCard actions slot 由 .wish-card-actions 走 styles.css 中保留的 line 479 规则（**保留**）
    - 删 line 453 (.dish-fav-btn 强制 `border: none; background: transparent`) — IconButton (10-01) 已就位
    - 删 line 571-572 (.dish-card:focus-visible / .card:focus-visible) — .md-card 自带
    - 删 line 592 (.wish-card:focus-visible) — .md-card 自带
    - 删 line 621 (`.wish-card` 在全局 min-size 选择器列表中) — 不再存在
    - 删 line 542-544 (`.guest-page .dish-card:hover`/`.guest-page .dish-card`/`.guest-page .dish-card::before`) — GuestDishCard 重构后无 .dish-card 引用

    === H. 验证迁移完整性 ===
    `grep -rE 'className=.*\b(dish-card|wish-card)\b' frontend/src/ --include='*.jsx'` 应 0 命中（除 .wish-card-actions 保留作为 actions slot 容器 — 来自 10-01 迁移时 WishCard.jsx 中作为 footer 子容器 `<div className="wish-card-actions">` — 此 className 在 styles.css 中保留 line 479 规则不变）；`grep -rE 'className=.*\bcard\b' frontend/src/ --include='*.jsx' | grep -vE '(mobile-card|order-card|stat-card|profile-card|login-card|dietary-warning|preference-section|guest-dish-card|recipe-card|chef-card|invitation-card|wish-picker|menu-card|cart-card|stat-)' | wc -l` 期望 = 0 (或 ≤ 5 — 部分 .card-body 残留由 styles.css 中保留 line 114 规则兜底，但本 Task 已删 114；保守接受 ≤ 5 作为安全余量)

  </action>

  <verify>
    <automated>cd frontend && npm run lint 2>&1 | tail -20; npm run build 2>&1 | tail -10; grep -rE 'className=.*\b(dish-card|wish-card)\b' src/ --include='*.jsx' | wc -l; grep -cE "^\.(card|dish-card|wish-card|card-body)\s*\{" src/css/styles.css</automated>
  </verify>

  <acceptance_criteria>
    1. `frontend/src/components/primitives/Card.jsx` 存在；`grep -E "md-card--(elevated|filled|outlined)" frontend/src/components/primitives/Card.jsx` 命中 3 个 variant 字符串；`grep "md-card__(image|header|body|footer)" frontend/src/components/primitives/Card.jsx` 命中 4 个 slot 字符串；`forwardRef` 包裹存在。
    2. `frontend/src/components/primitives/Card.css` 存在；`grep "var(--md-radius-md)" frontend/src/components/primitives/Card.css` 命中 16px 圆角；`grep "var(--md-elevation-1)" frontend/src/components/primitives/Card.css` 命中 elevated rest 阴影；`grep "var(--md-elevation-2)" frontend/src/components/primitives/Card.css` 命中 elevated hover；`grep "aspect-ratio: 4/3" frontend/src/components/primitives/Card.css` 命中 image 比例。
    3. `frontend/src/components/DishCard.jsx` 重构完成：`grep "from './primitives/Card'" frontend/src/components/DishCard.jsx` 命中；`grep -E "className=\"dish-card\"" frontend/src/components/DishCard.jsx` 0 命中；`grep "from './Ripple'" frontend/src/components/DishCard.jsx` 0 命中（已由 Card 内部化）；`grep "from '../components/Badge'" frontend/src/components/DishCard.jsx` 仍命中（10-03 替换旧 Badge；**允许 0 或 1 命中**）。
    4. `frontend/src/components/WishCard.jsx` 重构完成：`grep "from './primitives/Card'" frontend/src/components/WishCard.jsx` 命中；`grep -E "className=\"wish-card[^-]\"|className=\"wish-card \"` frontend/src/components/WishCard.jsx` 0 命中（仅允许 `wish-card-actions` 作为 footer slot 容器保留 — 不视为 .wish-card 旧 className）；`forwardRef` 包裹**不必存在**（WishCard 是普通 function 组件，无需 ref）；所有 onTap / onEdit / onCancel / onClaim / onAdvance / onReject 回调 props 保留。
    5. `frontend/src/components/GuestDishCard.jsx` 重构完成：`grep "from './primitives/Card'" frontend/src/components/GuestDishCard.jsx` 命中；`grep -E "className=\"dish-card\"" frontend/src/components/GuestDishCard.jsx` 0 命中；prop API `{dish, quantity, onAdd, onRemove}` 不变。
    6. 9 个 .card 通用迁移文件 (AdminIngredients, AdminUsers, ChefDishes, OrderPage, AdminLogs, AdminChefs, AdminCategories, OrderDetailPage, AdminDishes) 每个均 `import Card from '../components/primitives/Card';`。
    7. `frontend/src/css/styles.css` 不含 `.card {`, `.dish-card {`, `.wish-card {`, `.card-body {` 选择器（`grep -cE "^\.(card|dish-card|wish-card|card-body)\s*\{" src/css/styles.css` = 0）；保留 `.mobile-card-list` (line 388)、`.wish-card-actions` (line 479)、`.order-card`、`.login-card`、`.stat-card`、`.profile-card`、`.dietary-warning-card`、`.preference-section` 等 domain-specific 类。
    8. `frontend/src/css/styles.css:571-572` (.dish-card / .card focus-visible) 与 line 592 (.wish-card focus-visible) 已删除 — .md-card 自带。
    9. `cd frontend && npm run lint` 0 新增 error。
    10. `cd frontend && npm run build` 0 error。
  </acceptance_criteria>

  <done>Card primitive 3v + slot API 就位；DishCard / WishCard / GuestDishCard 重构为薄 slot-based 包装，className 旧实现消失；20+ 通用 .card 迁移完成；styles.css .card / .dish-card / .wish-card / .card-body 旧选择器删除；lint/build 通过；零业务逻辑回归。</done>
</task>

<task type="auto">
  <name>Task 2: Input primitive (2 variants + floating label + error) + 12 form family 迁移 + 旧 .form-input / .form-label / .form-group / .form-error CSS 删除</name>

  <files>
    frontend/src/components/primitives/Input.jsx,
    frontend/src/components/primitives/Input.css,
    frontend/src/components/PasswordInput.jsx,
    frontend/src/components/WishFormModal.jsx,
    frontend/src/components/WishRejectModal.jsx,
    frontend/src/pages/LoginPage.jsx,
    frontend/src/pages/ForceChangePasswordPage.jsx,
    frontend/src/pages/UserProfilePage.jsx,
    frontend/src/pages/OrderPage.jsx,
    frontend/src/pages/ChefDishesPage.jsx,
    frontend/src/pages/AdminDishesPage.jsx,
    frontend/src/pages/AdminIngredientsPage.jsx,
    frontend/src/pages/AdminCategoriesPage.jsx,
    frontend/src/pages/AdminChefsPage.jsx,
    frontend/src/pages/AdminUsersPage.jsx,
    frontend/src/css/styles.css
  </files>

  <read_first>
    .planning/phases/10-primitive-components/10-UI-SPEC.md §10 (Input API modes + geometry + variant state colors + floating condition)
    .planning/phases/10-primitive-components/10-PATTERNS.md:257-292 (Input.jsx + Input.css 模拟模式)
    .planning/phases/10-primitive-components/10-CONTEXT.md D-11 (Input 3 用法: label/label+placeholder/no-label; CSS-only :placeholder-shown)
    frontend/src/css/tokens.css:144-149 (--md-radius-sm 12px for Input)
    frontend/src/css/styles.css:229-233 (.form-group / .form-label / .form-input / .form-input:focus / .form-error 完整源 — 删除目标)
    frontend/src/pages/LoginPage.jsx:46-72 (典型 form-label + form-input 模式, 含 PasswordInput)
    frontend/src/components/PasswordInput.jsx:1-26 (PasswordInput 实现 — 改用 Input primitive + 内部 visibility toggle 仍由 component 自身维护)
    frontend/src/components/WishFormModal.jsx:176-229 (复杂 form: 3 个 input + 1 个 textarea + 1 个 optional 字段)
    frontend/src/components/WishRejectModal.jsx (含 textarea + 必填 reason)
    frontend/src/pages/AdminIngredientsPage.jsx:480-501 (典型 form-label + form-input + form-input[type=select] + form-input[type=textarea])
  </read_first>

  <action>
    === A. 新建 frontend/src/components/primitives/Input.jsx ===
    - docstring: "MD3 Input Primitive (Phase 10 — COMPO-04) — Outlined/Filled + label prop (CSS-only floating) + error prop + supportingText + leadingIcon/trailingIcon + forwardRef; native input/textarea/select 属性全透传"
    - imports: `import { forwardRef, useId } from 'react'; import Icon from './Icon'; import './Input.css';`
    - `const Input = forwardRef(function Input({ variant = 'outlined', label, error, supportingText, leadingIcon, trailingIcon, multiline = false, rows = 4, className = '', id: idProp, value, onChange, type = 'text', disabled, placeholder, ...rest }, ref) { ... })`
    - id 推导: `const autoId = useId(); const id = idProp || autoId; const describedById = \`\${id}-supporting\`; const hasError = Boolean(error);`
    - 构造 classes: `['md-input-wrapper', \`md-input-wrapper--\${variant}\`, hasError && 'md-input-wrapper--error', disabled && 'md-input-wrapper--disabled', multiline && 'md-input-wrapper--multiline', className].filter(Boolean).join(' ')`
    - 渲染: `<div className={classes}>{leadingIcon && <Icon name={leadingIcon} size={20} className="md-input__leading-icon" />}{label && <label htmlFor={id} className="md-input__label">{label}</label>}{multiline ? <textarea ref={ref} id={id} className="md-input__field" value={value} onChange={onChange} disabled={disabled} rows={rows} placeholder={placeholder || ' ' /* sentinel for CSS floating */} aria-invalid={hasError || undefined} aria-describedby={supportingText || error ? describedById : undefined} {...rest} /> : <input ref={ref} id={id} className="md-input__field" type={type} value={value} onChange={onChange} disabled={disabled} placeholder={placeholder || ' '} aria-invalid={hasError || undefined} aria-describedby={supportingText || error ? describedById : undefined} {...rest} />}{trailingIcon && <Icon name={trailingIcon} size={20} className="md-input__trailing-icon" />}{(supportingText || error) && <div id={describedById} className={\`md-input__supporting \${hasError ? 'md-input__supporting--error' : ''}\`}>{error || supportingText}</div>}</div>`
    - 关键: `placeholder={placeholder || ' '}` — 当 consumer 未传 placeholder 时，注入空格 sentinel 触发 CSS `:placeholder-shown` 浮动条件；consumer 传了 placeholder 则透传（CSS floating 仍工作，因为 :placeholder-shown: 时 placeholder 不为空，浮动 label 仍缩为 12sp；空字段时 placeholder=' ' 不显示但浮动条件仍成立）
    - 透传 `name`, `autoComplete`, `required`, `minLength`, `pattern`, `inputMode`, `aria-*`, `data-*` 全部支持
    - 静态校验: `if (multiline && trailingIcon) console.warn('[Input] trailingIcon ignored on multiline (textarea)');` — 仅 dev warn
    - 默认 export `Input`

    === B. 新建 frontend/src/components/primitives/Input.css ===
    - 顶部 `@import './base.css';`
    - 容器 `.md-input-wrapper`: `display: flex; flex-direction: column; position: relative; min-block-size: 56px; margin-bottom: 16px;`（56dp 满足 ≥48dp hit area）
    - 几何（按 UI-SPEC §10.2）:
      * `.md-input__field`: `width: 100%; padding: 16px; font-family: var(--md-font-body); font-size: 16px; font-weight: 400; line-height: 24px; color: var(--md-color-on-surface); background: transparent; border: none; outline: none; position: relative; z-index: 1;`
      * `.md-input__label`: `position: absolute; left: 16px; top: 18px; font-size: 16px; font-weight: 400; line-height: 24px; color: var(--md-color-on-surface-variant); pointer-events: none; transition: transform var(--md-motion-duration-short) var(--md-motion-easing-standard), font-size var(--md-motion-duration-short) var(--md-motion-easing-standard), color var(--md-motion-duration-short) var(--md-motion-easing-standard); transform-origin: left top; z-index: 0;`
    - 浮动条件（CSS-only）:
      * `.md-input__field:focus + .md-input__label, .md-input__field:not(:placeholder-shown) + .md-input__label` — **注: 此处需要 label 放在 field 之后；但现有 JSX 中 label 在 field 之前**。**方案: 改 JSX 顺序** —— label 放在 field 之后（CSS 选择器要求 field 是前一个兄弟）。重读 A: 已写 `<input/>{label && <label/>}` — 改 JSX 顺序让 label 在 field 后: `{multiline ? <textarea .../> : <input .../>}{label && <label htmlFor={id} className="md-input__label">{label}</label>}`。**JSX 顺序需调整**（不影响布局 — label 用 absolute 定位，DOM 顺序仅影响 CSS selector）
      * 选择器更新: `.md-input__field:focus ~ .md-input__label, .md-input__field:not(:placeholder-shown) ~ .md-input__label { transform: translateY(-12px) scale(0.75); color: var(--md-color-primary); font-size: 12px; }`
    - outlined 变体: `.md-input-wrapper--outlined { border: 1px solid var(--md-color-outline-variant); border-radius: var(--md-radius-sm); transition: border-color var(--md-motion-duration-short) var(--md-motion-easing-standard); }` `.md-input-wrapper--outlined:focus-within { border: 2px solid var(--md-color-primary); }` `.md-input-wrapper--outlined--error { border-color: var(--md-color-error); }` (注：focus-within 切换 2px 时外尺寸不变 — `outline-offset` + `box-shadow` 而非 width)
    - filled 变体: `.md-input-wrapper--filled { background: var(--md-color-surface-container-highest); border-bottom: 1px solid var(--md-color-outline-variant); border-radius: var(--md-radius-sm) var(--md-radius-sm) 0 0; }` `.md-input-wrapper--filled:focus-within { border-bottom: 2px solid var(--md-color-primary); }`
    - 错误态: `.md-input-wrapper--error .md-input__label { color: var(--md-color-error); }` `.md-input-wrapper--error:focus-within { border-color: var(--md-color-error); }` `.md-input-wrapper--error .md-input__field ~ .md-input__label { color: var(--md-color-error); }`
    - supporting: `.md-input__supporting { font-size: 12px; line-height: 16px; color: var(--md-color-on-surface-variant); padding: 4px 16px 0; margin-top: 4px; }` `.md-input__supporting--error { color: var(--md-color-error); }`
    - disabled: `.md-input-wrapper--disabled { opacity: 0.38; pointer-events: none; }`（保留键盘聚焦以保持 a11y，但阻止编辑）
    - 触控目标: 容器 `min-block-size: 56px`（天然 ≥48dp）
    - reduced-motion: `@media (prefers-reduced-motion: reduce) { .md-input__label { transition: none; } }`
    - 注: 不应用 `.md-interactive`（base.css 中带 ripple）—— Input 不需要 ripple 反馈（D-12 + UI-SPEC §10.3 末段）

    === C. 12 form family 迁移 ===
    **关键规则**:
    1. 模式 A — `<label className="form-label">X</label><input className="form-input" ...>` → `<Input label="X" ... />`（**label 透传 + placeholder 默认空格 sentinel**）
    2. 模式 B — `<input className="form-input" ...>` 无外置 label → `<Input aria-label="X" ... />`（**不传 label prop**，用 aria-label；可显式 placeholder）
    3. 模式 C — `<textarea className="form-input">` → `<Input multiline ... />` (rows 默认 4)
    4. 模式 D — `<select className="form-input">` → **保持原生 `<select className="form-input">` 不迁移**（Phase 10 Input primitive 不支持 select；Phase 11 Select 组件再处理 — **保留 .form-input 选择器在 styles.css 中直到 10-03 末尾**？**实际方案**: 评估 select 使用频次 — 如 select 仅少量 admin form，临时保留 select 不迁移；10-02 末尾不删 .form-input 以兼容 select；10-03 末尾才删 .form-input — **注**: 与 10-02 原计划有冲突，**调整方案**: Task 2 末尾的 .form-input 删除只针对已迁移 input/textarea，保留 select 兼容性 — **最终**: 10-02 末尾**不删 .form-input**，留给 10-03 末尾统一处理；本 Task 2 只迁移动 input/textarea，select 保持原状；styles.css 中 .form-input 保留至 10-03 Task 1 末尾（**用户指令：10-02 删 .form-input / .form-label** — 协调方案: 10-02 删除 .form-label / .form-group / .form-error，**保留 .form-input** 以兼容 select 兜底；10-03 末尾确认无 select 残留后删除 .form-input** — **注**: 实际 AdminIngredients 有 `<select className="form-input">`，故 .form-input 必须保留 — **最终方案**: 10-02 末尾**仅删 .form-label / .form-group / .form-error**，**.form-input 保留**直至 10-03 末尾；将本结论明确写入 success_criteria SC-3）

    迁移清单:
    1. `frontend/src/pages/LoginPage.jsx:47-72` — 2 个 input (username, password via PasswordInput) → `<Input label="用户名" type="text" value={...} onChange={...} placeholder="请输入用户名" required autoComplete="username" />` + `<Input label="密码" type="password" ... />` (PasswordInput 内部迁移)
    2. `frontend/src/pages/ForceChangePasswordPage.jsx` — 2-3 个 input (old/new/confirm password)
    3. `frontend/src/pages/UserProfilePage.jsx` — 4 处 form-input (display name, current password, new password, confirm password)
    4. `frontend/src/pages/OrderPage.jsx:470 区域` — 搜索 input → `<Input type="search" placeholder="搜索菜品..." aria-label="搜索菜品" value={...} onChange={...} />`（无 label 模式，aria-label 兜底）
    5. `frontend/src/pages/ChefDishesPage.jsx` — 4+ 处 form-input（菜品名、价格、描述等）
    6. `frontend/src/pages/AdminDishesPage.jsx` — 6+ 处 form-input（菜品名、分类、价格、描述等）
    7. `frontend/src/pages/AdminIngredientsPage.jsx:480-501` — 5 处 form-input + 1 select + 1 textarea (Description 别名)
    8. `frontend/src/pages/AdminCategoriesPage.jsx` — 1-2 处 form-input
    9. `frontend/src/pages/AdminChefsPage.jsx` — 1-2 处 form-input
    10. `frontend/src/pages/AdminUsersPage.jsx:273-310` — 6+ 处 form-input
    11. `frontend/src/components/WishFormModal.jsx:176-229` — 3 处 input (dish_name, reference_url, note 为 textarea)
    12. `frontend/src/components/WishRejectModal.jsx` — 1 处 textarea (reason, 必填 + 错误态)
    13. `frontend/src/components/PasswordInput.jsx:1-26` — 内部 input → `<Input type={visible ? 'text' : 'password'} value={...} onChange={...} placeholder={...} ...rest />` + 内部 visibility toggle 按钮**保留原样**（不在 10-01 IconButton 范围 — 10-03/12 决定）

    **关键迁移约束**:
    - **`<label className="form-label">X</label>` 旧模式删除**（外置 label 全部下沉到 `<Input label="X" />` 内部 floating label）
    - **`<div className="form-group">` 容器删除**（由 Input 内部 div 接管，margin-bottom: 16px 走 .md-input-wrapper CSS）
    - **`<input required />` 的 native 必填提示保留**（Input 透传 required）
    - **`<input type="number" />` / `type="email"` / `type="url"` / `type="tel"` 全部透传**
    - **`<textarea rows={N}>` → `<Input multiline rows={N} />`**
    - **`<select>` 不迁移**（见上文 D 节）

    === D. 错误态辅助文本迁移 ===
    现有 .form-error 使用模式: `{loginError && <div className="form-error show">{loginError}</div>}`
    迁移: `<Input label="用户名" error={loginError || undefined} ... />` — 错误 prop 触发 .md-input-wrapper--error 类 + supporting text 红色显示；移除外部 `<div className="form-error">`
    适用文件: LoginPage, WishFormModal, WishRejectModal, AdminDishesPage 等含 client-side validation 错误显示

    === E. 删除 styles.css 旧 .form-* 选择器 ===
    - 删 line 229 (.form-group { margin-bottom: 16px; }) — 由 .md-input-wrapper margin-bottom 接管
    - 删 line 230 (.form-label) — 由 .md-input__label 接管
    - 删 line 233-234 (.form-error + .form-error.show) — 由 .md-input__supporting--error 接管
    - **保留 line 231-232 (.form-input + .form-input:focus)** — 兼容 select 与 PasswordInput 兜底；10-03 末尾统一处理（届时确认无 .form-input className 残留）
    - 删 line 233 (`.form-error`) — Input error prop 接管
    - 删除 styles.css line 232 中的 `.form-input:focus { outline: var(--md-focus-ring-outer); outline-offset: 2px; box-shadow: 0 0 0 4px var(--md-focus-ring-inner); }` 中 box-shadow 段（保留 outline — 给 .form-input select 兜底）— 改: `.form-input:focus { outline: var(--md-focus-ring-outer); outline-offset: 2px; }`
    - 注: PasswordInput.jsx 中 `.password-input-field` 是 `.form-input password-input-field` 双 class — 保留 .password-input-field CSS（styles.css 中如有）；本 Task 末尾确认无 .form-input className 引用 input/textarea 时，**.form-input 仅剩 select 使用** — OK 保留

    === F. 验证迁移完整性 ===
    `grep -rE 'className=.*\b(form-label|form-group|form-error)\b' frontend/src/ --include='*.jsx'` 应 0 命中；`grep -rE 'className=.*\bform-input\b' frontend/src/ --include='*.jsx' | grep -vE '\bselect\b' | wc -l` 应 0 命中（select 例外保留）；`grep -rE 'className=.*\bform-input\b' frontend/src/ --include='*.jsx' | grep 'select' | wc -l` ≥ 1（AdminIngredients 1 select 保留）— 10-03 末尾再处理 select 迁移或 .form-input 最终删除

  </action>

  <verify>
    <automated>cd frontend && npm run lint 2>&1 | tail -20; npm run build 2>&1 | tail -10; grep -rE 'className=.*\b(form-label|form-group|form-error)\b' src/ --include='*.jsx' | wc -l; grep -rE 'className=.*\bform-input\b' src/ --include='*.jsx' | wc -l</automated>
  </verify>

  <acceptance_criteria>
    1. `frontend/src/components/primitives/Input.jsx` 存在；`grep -E "md-input-wrapper--(outlined|filled)" frontend/src/components/primitives/Input.jsx` 命中 2 个 variant 字符串；`grep "md-input__(field|label|leading-icon|trailing-icon|supporting)" frontend/src/components/primitives/Input.jsx` 命中 ≥ 4 个子 class；`forwardRef` 包裹存在；`useId` 用于生成 id。
    2. `frontend/src/components/primitives/Input.css` 存在；`grep "var(--md-radius-sm)" frontend/src/components/primitives/Input.css` 命中 12px 圆角；`grep "aspect-ratio" frontend/src/components/primitives/Input.css` 0 命中（Input 不需要 4/3 比例）；`grep ":placeholder-shown" frontend/src/components/primitives/Input.css` 命中浮动条件；`grep -E "^\.md-input" frontend/src/components/primitives/Input.css` 命中 ≥ 8 个 class (wrapper + variant + 2 modifiers + field + label + 2 icon + supporting + 2 supporting modifiers)。
    3. 12 个表单 family 全部迁移到 Input primitive：grep `import Input from '../components/primitives/Input'` 命中 12 个文件；`grep -rE 'className="form-label"' src/ --include='*.jsx' | wc -l` = 0；`grep -rE 'className="form-group"' src/ --include='*.jsx' | wc -l` = 0。
    4. `frontend/src/components/PasswordInput.jsx` 迁移：内部 input → Input primitive；visibility toggle 按钮保留原状。
    5. LoginPage `type="submit"` Button 仍工作（10-01 已迁移）；form submit 零回归。
    6. WishFormModal textarea 迁移为 `<Input multiline rows={4}>`；reference_url 仍支持 `type="url"`；dish_name 仍支持 `required`。
    7. styles.css 删除 `.form-group`、`.form-label`、`.form-error` 旧选择器（`grep -cE "^\.(form-group|form-label|form-error)\s*\{" src/css/styles.css` = 0）；**.form-input 保留**（兼容 select）。
    8. 错误态迁移生效：现有 `<div className="form-error show">` 模式 → `<Input error={...} />`；`grep -rE "form-error" src/ --include='*.jsx' | wc -l` = 0。
    9. `cd frontend && npm run lint` 0 新增 error。
    10. `cd frontend && npm run build` 0 error。
  </acceptance_criteria>

  <done>Input primitive 2 variants + floating label + error 就位；12 form family 全量迁移；styles.css .form-label / .form-group / .form-error 旧选择器删除（.form-input 保留至 10-03 末尾以兼容 select 兜底）；lint/build 通过；零业务逻辑回归；form submit / 受控值 / 错误态 / required / placeholder 全部零回归。</done>
</task>

</tasks>

<verification>
**Plan-level automated verification (run after Task 2 completes):**

```bash
cd /home/temila/family_chef/frontend

# 1. 新 primitive 文件就位
test -f src/components/primitives/Card.jsx
test -f src/components/primitives/Card.css
test -f src/components/primitives/Input.jsx
test -f src/components/primitives/Input.css

# 2. 旧 .card / .dish-card / .wish-card / .form-label / .form-group / .form-error 选择器清除
grep -cE "^\.(card|dish-card|wish-card|card-body|form-label|form-group|form-error)\s*\{" src/css/styles.css
# 期望: 0

# 3. .form-input 仍保留（兼容 select 兜底；10-03 末尾统一处理）
grep -c "^\.form-input" src/css/styles.css
# 期望: ≥ 1

# 4. 旧 className 引用已迁移
grep -rE 'className=.*\b(dish-card|wish-card|form-label|form-group|form-error)\b' src/ --include='*.jsx' | wc -l
# 期望: 0
grep -rE 'className=.*\bform-input\b' src/ --include='*.jsx' | wc -l
# 期望: ≥ 1 (select 兜底)
grep -rE 'className=.*\bform-input\b' src/ --include='*.jsx' | grep -vE '\bselect\b' | wc -l
# 期望: 0 (除 select 外的所有 form-input 迁移完成)

# 5. validation gate
npm run lint  # 0 新增 error
npm run build # 0 error

# 6. Card slot 抽象验证
grep "from './primitives/Card'" src/components/DishCard.jsx
grep "from './primitives/Card'" src/components/WishCard.jsx
grep "from './primitives/Card'" src/components/GuestDishCard.jsx
# 期望: 全部命中

# 7. Input primitive 引用验证
grep -rL "import.*primitives/Input" src/pages src/components --include='*.jsx' | xargs grep -lE '<input[^>]*className="form-input"|<textarea[^>]*className="form-input"' 2>/dev/null
# 期望: 空 (无遗漏迁移)
```

**Human verification (post-execution):**
- 浏览器访问 `/login` → 确认用户名/密码输入框 MD3 浮动 label 工作（聚焦时 label 上移 12sp + 变 primary 色）；错密码时输入框红色 outline + 错误文本
- 浏览器访问 `/admin/dishes` → 确认菜品编辑 modal 6+ 字段全部浮动 label + 错误态正常
- 浏览器访问 `/my-wishes` → 确认 DishCard 渲染（推荐/可点/已售罄 Badge + 跳转菜品详情）；WishCard 渲染（status badge + actions 按钮 + 红点 + 高亮 outline）
- 浏览器访问 `/guest/menu/{token}` → 确认 GuestDishCard 静态渲染（无 hover、cursor default、ripple 禁用）+ 内部 +/- 按钮 + 加号按钮（filled）
- 浏览器访问 `/admin/ingredients` 移动列表 → 确认通用 .card 渲染（elevated 1dp shadow + hover elevation-2）
- 浏览器访问深浅色 → 确认 Card / Input 颜色随主题切换
</verification>

<success_criteria>
**Mapped to ROADMAP Phase 10 success criteria 3-4 + CONTEXT D-11 + D-13:**

- [ ] **SC-1 (ROADMAP SC#3, COMPO-03)**: Card 组件支持 Elevated/Filled/Outlined 三变体；自动继承 Phase 9 的 elevation-1→2 悬浮过渡（Card.elevated hover）；现有 .card / .dish-card / .wish-card 视觉升级、JSX 结构与 props 不破坏 — 由 acceptance_criteria Task 1 #1, #2, #3, #4, #5, #7 验证
- [ ] **SC-2 (CONTEXT D-13)**: DishCard / WishCard / GuestDishCard 重构为薄 slot-based 包装，业务数据通过 props 传入；不再有自包含 className="dish-card" / "wish-card" 逻辑；删除 .dish-card / .wish-card 旧 CSS — 由 acceptance_criteria Task 1 #3, #4, #5, #7 验证
- [ ] **SC-3 (ROADMAP SC#4, COMPO-04)**: Input 组件支持 Outlined/Filled 两种 variant + 错误态辅助文本 + MD3 焦点环 + label 浮动 (CSS-only `:placeholder-shown`)；替换 .form-label + .form-input 模式；表单提交 / onChange / 受控行为零回归 — 由 acceptance_criteria Task 2 #1, #2, #3, #5, #6 验证
- [ ] **SC-4 (CONTEXT D-11)**: Input 组件支持 3 种用法：`<Input label="X" />` 浮动 label、`<Input label="X" placeholder="..." />` 浮动 label + placeholder、`<Input aria-label="X" />` 无 label 受控 input — 由 acceptance_criteria Task 2 #1, #2 验证
- [ ] **SC-5 (CONTEXT D-02)**: 换皮后 styles.css 中 `.card / .dish-card / .wish-card / .card-body / .form-label / .form-group / .form-error` 旧选择器**全部**删除；**.form-input 保留**至 10-03 末尾处理 select 兜底；调用方零残留（grep 验证） — 由 verification #2, #4 验证
- [ ] **SC-6 (LOGIC-01)**: 12 form family 迁移后，所有 onChange / onSubmit / required / placeholder / type=number|email|url|tel|search|file|select / value 受控 / autoComplete / 受控错误态零回归 — 由 verification 浏览器手测确认
- [ ] **SC-7 (LOGIC-02)**: backend/app/* 零改动；本 Plan 仅前端换皮 — 由 git diff 验证
- [ ] **SC-8 (LOGIC-03)**: Card slot API（image/header/children/footer）作为内部结构对外暴露；WishCard actions 行为通过 footer slot 容器 `<div className="wish-card-actions">` 保留；Domain card 不再依赖旧 className — 由 acceptance_criteria Task 1 #4 验证
- [ ] **SC-9 (CONTEXT D-04)**: `npm run lint` 0 新增 error（基线 ≥90 errors 不增加）；`npm run build` 0 error — 由 verification #5 验证

**Distribution of CSS deletion (10-02 plan responsibility):**
- **10-02 删除**: `.card`, `.dish-card`, `.wish-card`, `.card-body`, `.form-label`, `.form-group`, `.form-error`, `.wish-card-highlight`, `.dish-card-actions` 视觉, `.dish-card-wrapper`, `.dish-fav-btn` 强制样式, `.guest-page .dish-card*` 视觉, 多个 focus-visible 旧 class 规则
- **10-02 保留**: `.form-input` (兼容 select — 10-03 末尾处理), `.wish-card-actions` (WishCard footer slot 容器), `.mobile-card-list` (移动列表容器), `.order-card / .login-card / .stat-card / .profile-card / .dietary-warning-card / .preference-section` (domain-specific 视觉 — Phase 12 决定是否抽象)
</success_criteria>

<must_haves>
**Goal-backward truths — by end of 10-02 plan, the following must be TRUE for Phase 10 goal to advance:**

1. **Card primitive exists with 3 variants and slot API** — `<Card variant="elevated" image={...} footer={...}>{body}</Card>` works with image (aspect-ratio 4/3), header, body, footer slots; optional `onClick` enables ripple + state + elevation transition + keyboard activation.
2. **3 domain cards are thin slot-based wrappers** — DishCard passes dish data via image + body + footer props; WishCard passes wish + handlers via body + footer; GuestDishCard passes dish + qty + handlers via image + body + footer; none contain self-managed .dish-card / .wish-card CSS.
3. **20+ generic .card consumers are migrated** — admin mobile lists (AdminIngredients, AdminUsers, AdminCategories, AdminChefs, AdminLogs, ChefDishes, AdminDishes) and OrderDetailPage (3 cards), OrderPage (1 card) all use `<Card variant="elevated">` with proper body/footer composition.
4. **Input primitive exists with 2 variants and floating label** — `<Input label="用户名" type="text" value={...} onChange={...} />` renders MD3 outlined input with floating label (CSS-only :placeholder-shown mechanism); `error` prop triggers red outline + supporting text; Filled variant available via `variant="filled"`.
5. **12 form families are migrated to Input primitive** — LoginPage, ForceChangePasswordPage, UserProfilePage, OrderPage, ChefDishesPage, AdminDishesPage, AdminIngredientsPage, AdminCategoriesPage, AdminChefsPage, AdminUsersPage, WishFormModal, WishRejectModal, PasswordInput all use `<Input>` instead of `<label className="form-label"><input className="form-input">` pattern.
6. **Old .card / .dish-card / .wish-card / .form-label / .form-group / .form-error CSS is removed from styles.css** — grep returns 0 matches; only `.form-input` remains (Phase 10 select compat, deferred to 10-03 cleanup).
7. **`npm run lint` and `npm run build` pass** — no new lint errors; Vite build 0 errors.
8. **Zero business logic regressions** — all onClick / onChange / onSubmit / onCardTap / onClaim / onAdvance / onReject / form validation / controlled values / required / placeholder / error display behavior is identical to pre-migration; backend code is untouched.
</must_haves>

<output>
Create `/home/temila/family_chef/.planning/phases/10-primitive-components/10-02-SUMMARY.md` when done
</output>
