---
phase: 18-custom-editor-seasonal-auto-switch
plan: 09
type: execute
wave: 4
depends_on: []
gap_closure: true
closes_gaps: [test-10]
files_modified:
  - frontend/src/pages/ThemePage.jsx
autonomous: true
requirements:
  - SEAS-02
  - SEAS-04
must_haves:
  truths:
    - "In auto mode, clicking a custom theme card body does NOT navigate to the editor (no-op, same as preset cards) (Test 10)"
    - "In auto mode, the 编辑 button on custom theme cards remains visible and functional — clicking it navigates to the editor"
    - "In manual mode, clicking a custom theme card body applies the theme (unchanged)"
    - "In manual mode, the 编辑 button on custom theme cards remains visible and functional (unchanged)"
  artifacts:
    - path: "frontend/src/pages/ThemePage.jsx"
      provides: "handleThemeClick no longer navigates for auto+custom; auto-mode card-body click is a silent no-op for ALL card kinds"
      contains: "handleThemeClick"
  key_links:
    - from: "frontend/src/pages/ThemePage.jsx handleThemeClick"
      to: "no navigation in auto mode"
      via: "early return after seasonEnabled check"
      pattern: "seasonEnabled.*return"
---

<objective>
Remove the `navigate('/theme/editor?themeId=...')` branch from `handleThemeClick` in `ThemePage.jsx` so that clicking a custom theme card BODY in auto mode is a silent no-op (same as preset cards), instead of navigating to the editor.

Purpose: UAT Test 10 reports that in auto mode, clicking a custom theme card navigates to the editor — but the user wants card-body click to be a no-op (consistent with auto+preset behavior). The 编辑 button is already always rendered on custom cards (`onEdit` is wired for `kind==='custom'` at line 168 regardless of season state), so the user accesses the editor via the button, not via card-body click. The `navigate` branch in `handleThemeClick` (lines 73-75) is the sole source of the unwanted behavior.

Output: Remove 3 lines from `handleThemeClick` and update the docstring at line 15. The edit button wiring is already correct — no changes needed to `onEdit` props or `handleEdit`.

Root cause evidence: `.planning/debug/auto-mode-edit-button.md`.
</objective>

<execution_context>
@/home/temila/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/temila/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-custom-editor-seasonal-auto-switch/18-CONTEXT.md
@.planning/phases/18-custom-editor-seasonal-auto-switch/18-UAT.md
@.planning/debug/auto-mode-edit-button.md
@frontend/src/pages/ThemePage.jsx

<interfaces>
- `handleThemeClick(theme)` at ThemePage.jsx:70-79 — in auto mode: custom → navigate (BUG), preset → no-op. In manual mode: setActiveTheme. This plan removes the custom→navigate branch.
- `handleEdit(theme)` at ThemePage.jsx:82-94 — custom → navigate to editor with themeId; preset fork → navigate with preset query. This is the CORRECT path for opening the editor and is NOT changed.
- `onEdit` prop wiring at ThemePage.jsx:168 — `theme.kind === 'custom' || !seasonEnabled ? () => handleEdit(theme) : undefined`. Custom cards ALWAYS get `onEdit` (both auto and manual modes). `ThemeCard.jsx:78` renders the edit button when `typeof onEdit === 'function'`. So the edit button is always visible on custom cards in ALL modes — no change needed here.
- `setActiveTheme` in theme-context.jsx returns `false` when `seasonEnabled` is true (D-09 mutex, lines 205-209). So even without the navigate removal, clicking a custom card in auto mode wouldn't APPLY the theme — it would just navigate. The fix makes it a clean no-op instead.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove auto+custom navigate branch and update docstring</name>
  <files>frontend/src/pages/ThemePage.jsx</files>
  <read_first>
    <file>frontend/src/pages/ThemePage.jsx</file>
    <file>.planning/debug/auto-mode-edit-button.md</file>
  </read_first>
  <action>
In `frontend/src/pages/ThemePage.jsx`, make two changes:

**Change 1 — Remove the navigate branch in handleThemeClick (lines 70-79):**

The current function reads:

```js
  // D-10 / D-09：单击行为
  const handleThemeClick = (theme) => {
    if (seasonEnabled) {
      // 自动模式下：自定义 → 引导到编辑器（不应用）；预设 → 完全 no-op
      if (theme.kind === 'custom') {
        navigate(`/theme/editor?themeId=${encodeURIComponent(theme.id)}`);
      }
      return;
    }
    setActiveTheme(theme);
  };
```

Remove the `if (theme.kind === 'custom') { navigate(...) }` block (lines 73-75), making auto mode a uniform early return for ALL card kinds. Update the inline comment to reflect the new behavior. The updated function should read:

```js
  // D-10 / D-09：单击行为
  const handleThemeClick = (theme) => {
    if (seasonEnabled) {
      // 自动模式下：所有卡片单击均为 no-op（不应用、不跳转）。
      // 自定义卡片的编辑入口始终通过卡片上的「编辑」按钮（onEdit）。
      return;
    }
    setActiveTheme(theme);
  };
```

**Change 2 — Update the module docstring (line 15):**

The current docstring at line 15 reads:

```
 *   - 自动模式 + 自定义：单击 → 导航到 /theme/editor?themeId=<id>（不应用）
```

Replace it with:

```
 *   - 自动模式 + 自定义：单击 → no-op（不应用、不跳转；编辑通过卡片「编辑」按钮）
```

Do NOT change `handleEdit`, the `onEdit` prop wiring, or any other function. The `navigate` function from `useNavigate()` is still used by `handleNew` (line 62), `handleOpenSettings` (line 66), and `handleEdit` (lines 84, 93) — so the `useNavigate` import and `navigate` variable are still needed.

Gap reason (from debug session auto-mode-edit-button.md): The edit button is already correctly wired for custom cards in both modes (`onEdit={theme.kind === 'custom' || !seasonEnabled ? ...}` at line 168). The navigate branch in handleThemeClick was the ONLY unwanted behavior. Removing it makes auto+custom consistent with auto+preset (both are silent no-ops on card-body click), while preserving editor access via the always-visible edit button.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm run lint -- --quiet</automated>
    <automated>cd frontend &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`handleThemeClick` contains NO `navigate(...)` call inside the `if (seasonEnabled)` block — the function returns early for ALL card kinds in auto mode.</criterion>
    <criterion>The `if (seasonEnabled) { return; }` early-return structure is preserved — auto mode still prevents `setActiveTheme` from being called.</criterion>
    <criterion>The docstring at line 15 reflects the new no-op behavior, not the old navigate behavior.</criterion>
    <criterion>`handleEdit` and the `onEdit` prop wiring (line 168) are unchanged — the edit button still works for custom cards in both modes.</criterion>
    <criterion>`navigate` variable is still referenced by handleNew/handleOpenSettings/handleEdit — no unused-variable lint error.</criterion>
    <criterion>`npm run lint -- --quiet` passes with zero new errors.</criterion>
    <criterion>`npm run build` succeeds.</criterion>
  </acceptance_criteria>
  <done>In auto mode, clicking any theme card body (custom or preset) is a silent no-op. The edit button on custom cards remains visible and functional in all modes. Manual mode behavior is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Verify navigate removal via lint and build</name>
  <files>frontend/src/pages/ThemePage.jsx</files>
  <read_first>
    <file>frontend/src/pages/ThemePage.jsx</file>
  </read_first>
  <action>
After Task 1's branch removal and docstring update, run the verification chain. This task does NOT modify files — it confirms the fix is safe for commit.

1. Run `cd frontend && npm run lint -- --quiet` — must pass with zero new errors. The `navigate` variable is still used by `handleNew` (line 62), `handleOpenSettings` (line 66), and `handleEdit` (lines 84, 93), so removing one usage from `handleThemeClick` does NOT create an unused-variable lint error.

2. Run `cd frontend && npm run build` — must succeed. Removing 3 lines from `handleThemeClick` and updating a comment has no build implications.

3. Verify via grep that `handleThemeClick` does NOT contain `navigate(` — the function should now only have the `if (seasonEnabled) { return; }` guard and the `setActiveTheme(theme)` call. Run a pattern check confirming the navigate call was removed from the auto-mode branch.

4. Verify the `onEdit` prop wiring at line 168 is unchanged: `theme.kind === 'custom' || !seasonEnabled ? () => handleEdit(theme) : undefined`. The edit button must still be rendered on custom cards in all modes.

5. Verify the docstring at line 15 no longer says "导航到 /theme/editor" for auto+custom — it should say "no-op".

Note: The auto-mode no-op behavior (card-body click) requires browser interaction to verify. Human UAT re-testing of Test 10 is required after execution — see the `<verification>` section below.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm run lint -- --quiet &amp;&amp; npm run build</automated>
    <automated>rg -n "navigate" frontend/src/pages/ThemePage.jsx | rg -v "useNavigate|handleNew|handleOpenSettings|handleEdit" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`handleThemeClick` function body contains NO `navigate(` call — verified by grep filtering out other functions.</criterion>
    <criterion>The `onEdit` prop at line 168 is unchanged — `theme.kind === 'custom' || !seasonEnabled` still passes handleEdit for custom cards in all modes.</criterion>
    <criterion>The docstring at line 15 reflects no-op behavior (not navigate).</criterion>
    <criterion>`npm run lint -- --quiet` passes with zero new errors.</criterion>
    <criterion>`npm run build` succeeds.</criterion>
  </acceptance_criteria>
  <done>The navigate removal is committed with lint and build passing. The fix is verified structurally (no navigate in handleThemeClick) and syntactically (lint passes). Human UAT re-testing of Test 10 is the final gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User click → card onClick → handleThemeClick | Card-body click in auto mode is now a no-op (no state change, no navigation). The edit button is a separate click target with stopPropagation isolation (ThemeCard.jsx:81-84). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-09-01 | Tampering | D-09 mutex enforcement | mitigate | Removing the navigate branch strengthens the mutex: auto-mode card-body clicks can no longer trigger ANY side effect (no apply, no navigate). The only intentional auto-mode action path is the explicit edit button, which is a deliberate user action on a separate click target. |
| T-18-09-02 | Elevation of Privilege | Editor access in auto mode | accept | The edit button is intentionally available for custom cards in auto mode (D-10 CONTEXT decision: "点击自定义主题卡片 → 进入编辑器（允许编辑）"). Users can still edit custom themes while auto-switch is on — they just can't APPLY them via card-body click. This is the intended product behavior. |
</threat_model>

<verification>
- `cd frontend && npm run lint -- --quiet` — no new lint errors
- `cd frontend && npm run build` — production build succeeds
- Human UAT: Test 10 from `18-UAT.md` — auto-mode custom card body click is no-op; edit button still navigates to editor
</verification>

<success_criteria>
In auto mode, clicking any theme card body (custom or preset) is a silent no-op — no navigation, no theme application, no toast. The edit button on custom cards remains visible and functional in both auto and manual modes. Manual mode card-body click still applies the theme. UAT Test 10 is closed.
</success_criteria>

<output>
Create `.planning/phases/18-custom-editor-seasonal-auto-switch/18-09-SUMMARY.md` when done.
</output>
