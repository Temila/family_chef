---
phase: 18-custom-editor-seasonal-auto-switch
plan: 08
type: execute
wave: 4
depends_on: []
gap_closure: true
closes_gaps: [test-8]
files_modified:
  - frontend/src/pages/ThemeEditorPage.jsx
autonomous: true
requirements:
  - EDIT-04
must_haves:
  truths:
    - "Saving a theme with a duplicate name shows BOTH an inline Input error AND a top-of-screen error Snackbar popup (Test 8)"
    - "The Snackbar uses the 'error' variant (red bar + error icon, 6s auto-dismiss)"
    - "The Snackbar message is in Chinese: 已存在同名主题：<name>"
  artifacts:
    - path: "frontend/src/pages/ThemeEditorPage.jsx"
      provides: "Duplicate-name catch branch now calls showToast alongside setNameError"
      contains: "showToast"
  key_links:
    - from: "frontend/src/pages/ThemeEditorPage.jsx handleSave catch block"
      to: "frontend/src/contexts/ToastContext.jsx showToast"
      via: "showToast(message, 'error') alongside existing setNameError"
      pattern: "showToast.*已存在同名主题"
---

<objective>
Add a `showToast` call to the duplicate-name catch branch in `ThemeEditorPage.jsx` so that saving a theme with a name that already exists triggers BOTH the inline Input error AND a top-of-screen error Snackbar popup.

Purpose: UAT Test 8 reports that the duplicate-name error is not prominent enough — the user sees only the inline Input error text but wants an additional "popup card" (Snackbar) notification. The root cause is a missing `showToast(...)` call: all other catch branches in the same function (color validation, fallback) already call `showToast(message, 'error')`, but the duplicate-name branch only calls `setNameError(...)`.

Output: A one-line addition to the duplicate-name catch branch. `useToast` is already imported (line 27), `showToast` is already destructured (line 139), `SnackbarProvider` is already mounted in the app tree, and the `'error'` variant is fully supported (red bar + error icon, 6s auto-dismiss).

Root cause evidence: `.planning/debug/duplicate-toast-enhancement.md`.
</objective>

<execution_context>
@/home/temila/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/temila/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-custom-editor-seasonal-auto-switch/18-CONTEXT.md
@.planning/phases/18-custom-editor-seasonal-auto-switch/18-UAT.md
@.planning/debug/duplicate-toast-enhancement.md
@frontend/src/pages/ThemeEditorPage.jsx

<interfaces>
- `handleSave` catch block at ThemeEditorPage.jsx:262-272 — has four branches: duplicate-name (line 264-265), empty-name (266-267), color-validation (268-269), fallback (270-272). Only the duplicate-name branch lacks a `showToast` call.
- `showToast` is destructured from `useToast()` at line 139. The function signature is `showToast(message: string, type: 'success'|'error'|'warn')`.
- `SnackbarProvider` is mounted at the app root (App.jsx). Each `showToast(..., 'error')` call renders a `.md-snackbar--error` card (fixed top-center, z-index 1000, 48dp touch target, 6s auto-dismiss per `ToastContext.jsx:20` `DURATION_BY_TYPE.error = 6000`).
- The backend raises `ValueError(f"已存在同名主题: {name}")` for both POST create (custom_theme_service.py:36) and PUT rename (custom_theme_service.py:86). The frontend regex `/同名|已存在|duplicate/i` at line 264 matches this message.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add showToast call to duplicate-name catch branch</name>
  <files>frontend/src/pages/ThemeEditorPage.jsx</files>
  <read_first>
    <file>frontend/src/pages/ThemeEditorPage.jsx</file>
    <file>.planning/debug/duplicate-toast-enhancement.md</file>
  </read_first>
  <action>
In `frontend/src/pages/ThemeEditorPage.jsx`, locate the `handleSave` function's catch block (lines 262-272). The duplicate-name branch at lines 264-265 currently reads:

```js
      if (/同名|已存在|duplicate/i.test(message)) {
        setNameError(`已存在同名主题：${finalName}`);
      } else if (message.includes('主题名称不能为空') || message.includes('不能为空')) {
```

Add ONE line — `showToast(\`已存在同名主题：${finalName}\`, 'error');` — immediately AFTER the `setNameError` call (after line 265, before the `} else if`). This mirrors the pattern already established in the color-validation branch (line 269: `showToast('颜色值不合法', 'error')`) and the fallback branch (line 271: `showToast(message, 'error')`).

The updated branch should read:

```js
      if (/同名|已存在|duplicate/i.test(message)) {
        setNameError(`已存在同名主题：${finalName}`);
        showToast(`已存在同名主题：${finalName}`, 'error');
      } else if (message.includes('主题名称不能为空') || message.includes('不能为空')) {
```

Do NOT change any other branch, the `setNameError` call, the regex, or any other code in this file. The change is exactly one new line.

Gap reason (from debug session duplicate-toast-enhancement.md): The catch block already has `showToast` wired for all other error types. The duplicate-name branch was the only one missing it. `useToast` is imported (line 27), `showToast` is destructured (line 139), the `'error'` variant is fully supported with 6s duration and red styling. Adding this one line makes the Snackbar and inline Input error appear simultaneously.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm run lint -- --quiet</automated>
    <automated>cd frontend &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    <criterion>The duplicate-name catch branch (matching `/同名|已存在|duplicate/i`) contains BOTH `setNameError(...)` AND `showToast(\`已存在同名主题：${finalName}\`, 'error')`.</criterion>
    <criterion>The showToast call uses the `'error'` type (second argument), not 'success' or 'warn'.</criterion>
    <criterion>The showToast message uses the same Chinese template as setNameError: `已存在同名主题：${finalName}`.</criterion>
    <criterion>No other catch branch is modified — color-validation and fallback branches already have showToast.</criterion>
    <criterion>`npm run lint -- --quiet` passes with zero new errors.</criterion>
    <criterion>`npm run build` succeeds.</criterion>
  </acceptance_criteria>
  <done>Saving a theme with a duplicate name now triggers both an inline Input error and a top-of-screen red error Snackbar popup with the Chinese message "已存在同名主题：<name>".</done>
</task>

<task type="auto">
  <name>Task 2: Verify showToast wiring via lint and build</name>
  <files>frontend/src/pages/ThemeEditorPage.jsx</files>
  <read_first>
    <file>frontend/src/pages/ThemeEditorPage.jsx</file>
  </read_first>
  <action>
After Task 1's one-line addition, run the verification chain. This task does NOT modify files — it confirms the fix is safe for commit.

1. Run `cd frontend && npm run lint -- --quiet` — must pass with zero new errors. `showToast` is already destructured at line 139, so no unused-variable warning. The new line uses the same template literal pattern as the existing `setNameError` call.

2. Run `cd frontend && npm run build` — must succeed. The Snackbar system is already mounted; adding one more `showToast` call has no build implications.

3. Verify via grep that the duplicate-name catch branch now contains BOTH `setNameError` AND `showToast` with the same Chinese template. Run a pattern check confirming `showToast.*已存在同名主题` appears in the file exactly once (the new call) — the existing fallback `showToast(message, 'error')` at line 271 does NOT match this pattern because it uses the generic `message` variable.

4. Verify the showToast call uses the `'error'` variant (second argument), matching the pattern of the color-validation branch (line 269) and fallback branch (line 271).

Note: The Snackbar popup visual cannot be verified in CI (requires browser DOM + interaction). Human UAT re-testing of Test 8 is required after execution — see the `<verification>` section below.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm run lint -- --quiet &amp;&amp; npm run build</automated>
    <automated>rg -c "showToast.*已存在同名主题.*error" frontend/src/pages/ThemeEditorPage.jsx</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`rg -c "showToast.*已存在同名主题.*error" frontend/src/pages/ThemeEditorPage.jsx` returns at least 1 (the new call exists).</criterion>
    <criterion>The duplicate-name catch branch has BOTH `setNameError(...)` AND `showToast(...)` — no branch has one without the other.</criterion>
    <criterion>`npm run lint -- --quiet` passes with zero new errors.</criterion>
    <criterion>`npm run build` succeeds.</criterion>
  </acceptance_criteria>
  <done>The duplicate-name showToast call is committed with lint and build passing. The fix is verified structurally (showToast call present in the branch) and syntactically (lint passes). Human UAT re-testing of Test 8 is the final gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend 400 response → frontend catch block → user notification | Error messages from the backend are surfaced to the user via two channels: inline Input error and Snackbar toast. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-08-01 | Information Disclosure | Error message content | accept | The error message "已存在同名主题：<name>" echoes the user's own input back to the same user. No cross-user data exposure (backend enforces per-user theme isolation per SYNC-04). The Snackbar is a display-only notification with no action buttons. |
| T-18-08-02 | Denial of Service | Repeated duplicate-name attempts | accept | Each attempt triggers one Snackbar (6s auto-dismiss). The backend uq_custom_themes_user_name constraint prevents actual duplicates. No amplification risk. |
</threat_model>

<verification>
- `cd frontend && npm run lint -- --quiet` — no new lint errors
- `cd frontend && npm run build` — production build succeeds
- Human UAT: Test 8 from `18-UAT.md` — duplicate-name save shows both inline Input error AND red error Snackbar popup
</verification>

<success_criteria>
Saving or renaming a custom theme to a name that already exists triggers a visible red error Snackbar popup at the top of the screen (in addition to the existing inline Input error). The Snackbar uses the 'error' variant with Chinese text "已存在同名主题：<name>" and auto-dismisses after 6 seconds. UAT Test 8 is closed.
</success_criteria>

<output>
Create `.planning/phases/18-custom-editor-seasonal-auto-switch/18-08-SUMMARY.md` when done.
</output>
