---
phase: 18-custom-editor-seasonal-auto-switch
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements:
  - EDIT-01
  - SEAS-01
must_haves:
  truths:
    - "The two newly required packages are verified from their canonical registries before an executor installs them."
    - "No package is installed from an unverified or ambiguous source."
  artifacts:
    - path: "https://www.npmjs.com/package/react-colorful"
      provides: "Canonical npm package identity and published metadata for the required React picker"
    - path: "https://pypi.org/project/skyfield/"
      provides: "Canonical PyPI package identity for the dev-time solar-term generator"
  key_links:
    - from: "package legitimacy checkpoint"
      to: "18-03 and 18-04 install/generation tasks"
      via: "blocking approval before npm or uv package resolution"
      pattern: "approved"
---

<objective>
Verify the legitimacy of the two packages that the locked implementation requires before any executor resolves or installs them.

Purpose: The editor must use `react-colorful` per D-12/EDIT-01, and the solar table must be generated with Skyfield per D-02. This blocking gate prevents a package-name typo, substitute dependency, or unreviewed package from entering the build.
Output: Human approval recorded at the execution checkpoint; no repository file changes are made by this plan.
</objective>

<execution_context>
@/home/temila/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/temila/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-custom-editor-seasonal-auto-switch/18-CONTEXT.md
@frontend/package.json
@frontend/package-lock.json
@frontend/vite.config.js
@scripts/
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Approve react-colorful and skyfield package identities</name>
  <files>None — verification checkpoint only; do not modify package manifests before approval.</files>
  <read_first>
    <file>frontend/package.json</file>
    <file>frontend/package-lock.json</file>
    <file>.planning/phases/18-custom-editor-seasonal-auto-switch/18-CONTEXT.md</file>
    <file>.planning/REQUIREMENTS.md</file>
  </read_first>
  <action>Inspect the canonical npm and PyPI records for the exact package names in D-02 and D-12, then pause the phase until the user explicitly approves those identities. Do not modify package manifests or run an install before approval.</action>
  <verify>
    <automated>npm view react-colorful version repository.url --json &amp;&amp; python3 -c "import json, urllib.request; print(json.load(urllib.request.urlopen('https://pypi.org/pypi/skyfield/json'))['info']['version'])"</automated>
  </verify>
  <done>Canonical package metadata has been checked and the executor is paused at the blocking approval gate before any package resolution.</done>
  <how-to-verify>1. Open https://www.npmjs.com/package/react-colorful and confirm the package name is exactly `react-colorful`, the latest published version is 5.8.0, the repository is `omgovich/react-colorful`, and the package has no runtime dependencies. 2. Open https://pypi.org/project/skyfield/ and confirm the package name is exactly `skyfield`, it is the MIT-licensed astronomy library from `brandon-rhodes/python-skyfield`, and it is intended for the generator-only use described in D-02. 3. Confirm no task may substitute another color picker, add a runtime Skyfield import, or install a package with a similar name.</how-to-verify>
  <acceptance_criteria>
    <criterion>Canonical registry pages identify `react-colorful` and `skyfield` with the exact names used by the downstream tasks.</criterion>
    <criterion>`npm view react-colorful version repository.url` returns version `5.8.0` and the `omgovich/react-colorful` repository.</criterion>
    <criterion>PyPI metadata identifies Skyfield as version `1.54` with MIT license and repository `brandon-rhodes/python-skyfield`.</criterion>
    <criterion>The user explicitly resumes with `approved`; any concern stops execution before package installation.</criterion>
  </acceptance_criteria>
  <resume-signal>Type "approved" to allow Plans 18-03 and 18-04 to resolve these exact package names, or describe the package concern.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package registry → local package manager | Registry metadata and tarballs become executable build/generator inputs. |
| human approval → downstream executor | Approval authorizes only the exact package names and uses stated in this phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-04 | Spoofing | npm/PyPI package identity | mitigate | Blocking human verification checks the canonical package URLs, exact names, repository owners, and versions before installation. |
| T-18-05 | Tampering | package tarball/dependency graph | mitigate | Downstream tasks pin `react-colorful@^5.8.0`, use the generated lockfile, run `npm audit`/build checks available in the repository, and use Skyfield only in the generator command. |
| T-18-06 | Information Disclosure | package-manager output | accept | Package metadata contains no application credentials; no `.env` or private registry tokens are read by the checkpoint. |
| T-18-SC | Tampering | npm/pip package installation | mitigate | This blocking gate is never auto-approvable; the executor cannot proceed until the user approves both canonical package records. |
</threat_model>

<verification>
- `npm view react-colorful version repository.url --json`
- `python3 -c "import json, urllib.request; print(json.load(urllib.request.urlopen('https://pypi.org/pypi/skyfield/json'))['info']['version'])"`
- Human approval signal equals `approved` before any install command runs.
</verification>

<success_criteria>
Both package identities are explicitly approved before the editor or solar-term generator can install or resolve them, and the package scope remains exactly the locked React picker plus generator-only Skyfield use.
</success_criteria>

<output>
Create `.planning/phases/18-custom-editor-seasonal-auto-switch/18-02-SUMMARY.md` when done.
</output>
