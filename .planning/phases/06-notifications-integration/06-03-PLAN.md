---
phase: 06-notifications-integration
plan: 03
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - backend/app/integrations/feishu.py
  - backend/app/services/notification_service.py
  - backend/app/services/wish_service.py
  - backend/tests/test_wish_notifications.py
autonomous: true
requirements: [NOTIF-03, NOTIF-05, NOTIF-06]
user_setup: []

must_haves:
  truths:
    - "POST /api/wishes fans out one Feishu card per chef with `feishu_open_id IS NOT NULL`, never to bound non-chefs or unbound chefs"
    - "PUT /api/wishes/{id} on a claimed wish sends one Feishu card to the claiming chef; unclaimed edit sends zero"
    - "DELETE /api/wishes/{id} on a claimed wish sends one Feishu card to the claiming chef; unclaimed cancel sends zero"
    - "Claim, advance, reject, and cancel each write `last_status_change_at = naive_utc_now()` so the submitter's badge reappears after a prior clear"
    - "A content-only `update_wish()` on a claimed wish sends a Feishu card to the claimer but does NOT advance `last_status_change_at` (locked D-H01 rule 2)"
    - "Invalid transitions (concurrent-claim rowcount==0; D-09/D-12 rejection; empty `reject_reason`; second cancel of an already-terminal wish) do NOT advance `last_status_change_at` and do NOT send a Feishu card (locked D-H01 rule 3)"
    - "Every Feishu call is wrapped in its own try/except; one recipient's failure does not prevent delivery to the next, and the wish HTTP request still returns 200/201"
    - "The new `send_wish_notification()` method builds an interactive card with `plain_text` for user content, `lark_md` only for the controlled deep link, and respects the 1000-char note cap"
  artifacts:
    - path: "backend/app/integrations/feishu.py"
      provides: "FeishuClient.send_wish_notification(receive_id, data) method + _truncate_note helper"
      contains: "async def send_wish_notification"
    - path: "backend/app/services/notification_service.py"
      provides: "WishNotificationService singleton with notify_new_wish + notify_claimed_wish_change"
      exports: ["wish_notification_service"]
    - path: "backend/app/services/wish_service.py"
      provides: "5 hook locations wired + submit_wish fan-out + atomic claim timestamp + content-edit boundary (locked D-H01)"
      contains: "wish_notification_service.notify_new_wish"
    - path: "backend/tests/test_wish_notifications.py"
      provides: "Section A fan-out + Section B service payload + Section C card rendering (plain_text/lark_md/optional omission/1000-char truncation) + Section D badge lifecycle (restore + invalid no-op + content-edit no-advance)"
      min_lines: 220
  key_links:
    - from: "backend/app/services/wish_service.py::submit_wish"
      to: "backend/app/services/notification_service.py::notify_new_wish"
      via: "lazy import + await after db.flush()/refresh()"
      pattern: "wish_notification_service\\.notify_new_wish"
    - from: "backend/app/services/wish_service.py::claim_wish"
      to: "Wish.last_status_change_at"
      via: "atomic update(Wish).values(... last_status_change_at=naive_utc_now())"
      pattern: "last_status_change_at=naive_utc_now\\(\\)"
    - from: "backend/app/services/wish_service.py::update_wish + cancel_wish"
      to: "backend/app/services/notification_service.py::notify_claimed_wish_change"
      via: "pre-mutation capture of old_note + claimed_by_chef_id then post-flush notify"
      pattern: "wish_notification_service\\.notify_claimed_wish_change"
    - from: "backend/app/services/wish_service.py::update_wish"
      to: "Wish.last_status_change_at"
      via: "MUST NOT write — content-only edit per locked D-H01 rule 2; absence of `last_status_change_at=` assignment inside update_wish is enforced by test_content_edit_on_claimed_wish_does_not_advance_status_change_at"
      pattern: "(?!.*update_wish.*last_status_change_at=)^.*$"
    - from: "backend/app/integrations/feishu.py::send_wish_notification"
      to: "send_message(receive_id, 'interactive', card)"
      via: "card envelope reused; plain_text elements for user values, lark_md only for deep link"
      pattern: "await self\\.send_message"
    - from: "backend/tests/test_wish_notifications.py Section C"
      to: "feishu_client.send_message"
      via: "patch send_message (NOT send_wish_notification) and inspect the card dict passed as 3rd arg"
      pattern: "patch.*send_message"
---

<objective>
Wire the Feishu delivery layer and the status-change timestamp writes so the badge from Plan 02 actually flips and chefs receive the right card. This plan adds (1) a dedicated `send_wish_notification()` card method, (2) a `WishNotificationService` that resolves recipients and isolates failures, and (3) all 5 Phase 5 hook locations in `wish_service.py` plus the `submit_wish` fan-out and the atomic claim timestamp. NOTIF-05 + NOTIF-06 are fully satisfied; the badge lifecycle from NOTIF-03 becomes observable because every status change now writes `last_status_change_at`.

Purpose: Phase 7 frontend can rely on a stable backend — claim/advance/reject/cancel edits the timestamp, new/claimed-edit/claimed-cancel sends the right Feishu card, and a Feishu failure never bubbles up to the wish HTTP response.

Output: A new `notification_service.py`, edits to `feishu.py` + `wish_service.py`, and a comprehensive test file proving targeting, isolation, and the claim-timestamp regression.
</objective>

<execution_context>
@/home/temila/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/temila/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<execution_preflight>
Before reading or modifying implementation files, preserve the current working tree and require the project branch from AGENTS.md. Record `git status --short`; do not use `git reset`, `git clean`, `git restore`, or any stash command. If the current branch is not `feature/guest_order`, create that local branch from the current HEAD with `git switch -c feature/guest_order` when it does not exist. If it already exists locally, proceed only when `git merge-base --is-ancestor feature/guest_order HEAD` succeeds, then fast-forward the local pointer with `git branch -f feature/guest_order HEAD` and switch to it. If either ancestry or switch fails, stop without altering tracked or untracked artifacts. This deliberately preserves the Phase 5 summaries and Phase 6 planning artifacts instead of switching to the older remote tip.

<automated>current=$(git branch --show-current) && if [ "$current" != "feature/guest_order" ]; then if git show-ref --verify --quiet refs/heads/feature/guest_order; then git merge-base --is-ancestor feature/guest_order HEAD && git branch -f feature/guest_order HEAD && git switch feature/guest_order; else git switch -c feature/guest_order; fi; fi && test "$(git branch --show-current)" = "feature/guest_order"</automated>
</execution_preflight>

<context>
@.planning/phases/06-notifications-integration/06-CONTEXT.md
@.planning/phases/06-notifications-integration/06-RESEARCH.md
@.planning/phases/06-notifications-integration/06-01-PLAN.md
@.planning/phases/06-notifications-integration/06-02-PLAN.md
@backend/app/integrations/feishu.py
@backend/app/services/wish_service.py
@backend/app/services/order_service.py
@backend/app/models/wish.py
@backend/app/models/user.py
@backend/app/utils/datetime_utils.py
@backend/tests/test_wishes.py
@backend/tests/test_feishu.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add send_wish_notification() card method with plain_text + truncation</name>
  <files>
    - backend/app/integrations/feishu.py
  </files>
  <read_first>
    - backend/app/integrations/feishu.py (existing send_order_notification as card-shape template)
    - .planning/phases/06-notifications-integration/06-CONTEXT.md (D-F01 dedicated method, D-F02 NOTIF-05 fields, D-F03 NOTIF-06 fields, D-F04 targets)
    - .planning/phases/06-notifications-integration/06-RESEARCH.md (Pattern 5 plain_text for user content, Pitfall 9 truncation, Pitfall 8 APP_URL normalization, Code Examples "Dedicated card method shape")
    - backend/app/config.py (settings.APP_URL from Plan 01)
  </read_first>
  <behavior>
    - send_wish_notification("open_x", {"notification_type":"new","wish_id":1,"dish_name":"麻婆豆腐","submitter_name":"测试用户","reference_url":None,"note":None}) returns True and sends a card whose header.title is "新愿望通知" + elements include "愿望菜品：麻婆豆腐" + "提交者：测试用户" + a `[查看详情](https://family-chef.app/wishes/1)` lark_md link
    - send_wish_notification("open_x", {"notification_type":"edit","wish_id":2,"dish_name":"红烧肉","change_description":"测试用户 修改了愿望: 红烧肉","old_note":"old","note_changed":True,"new_note":"new"}) includes "通知类型：edit" + "原备注：old" + "新备注：new"
    - send_wish_notification("open_x", {"notification_type":"cancel","wish_id":3,"dish_name":"宫保鸡丁","change_description":"测试用户 撤回了愿望: 宫保鸡丁"}) omits "原备注" and "新备注" sections (no old_note, no note_changed)
    - A 2000-char note gets truncated to 1000 chars in the rendered card (Pitfall 9 A4 cap)
    - Missing reference_url / note in `new` payload causes those field divs to be omitted entirely (D-F02 optional omission)
  </behavior>
  <action>
    Append a new `async def send_wish_notification(self, receive_id: str, data: dict) -> bool` method to `FeishuClient` in `backend/app/integrations/feishu.py`, placed directly after `send_order_notification` (D-F01 separation; do not touch the existing order method).

    Method body per RESEARCH Code Examples "Dedicated card method shape":
    1. Read `notification_type = data.get("notification_type", "new")`, `wish_id = data["wish_id"]`, `dish_name = str(data.get("dish_name") or "")`, then compute `detail_url = f"{settings.APP_URL.rstrip('/')}/wishes/{wish_id}"`.
    2. Branch header:
       - `new` → header title `"新愿望通知"`, fields `[("愿望菜品", dish_name), ("提交者", str(data.get("submitter_name") or "未知用户"))]`, then conditionally append `("参考链接", str(data["reference_url"]))` if truthy, then `("备注", truncate_note(str(data["note"])))` if truthy.
       - else (edit/cancel) → header title `"愿望变更通知"`, fields `[("菜品", dish_name), ("通知类型", notification_type), ("变更说明", str(data.get("change_description") or ""))]`, then `("原备注", truncate_note(str(data["old_note"])))` if truthy, then `("新备注", truncate_note(str(data.get("new_note") or "（已清空）")))` only when `data.get("note_changed")` is truthy (Pitfall 9 cleared-marker per A4).
    3. Build elements list: each field becomes `{"tag":"div","text":{"tag":"plain_text","content":f"{label}：{value}"}}` (Pattern 5 — user content never in `lark_md`).
    4. Append a final `{"tag":"div","text":{"tag":"lark_md","content":f"[查看详情]({detail_url})"}}` element (server-constructed HTTP(S) link only).
    5. Card envelope: `{"config":{"wide_screen_mode":True},"header":{"title":{"tag":"plain_text","content":header_content},"template":"blue"},"elements":elements}`.
    6. Delegate transport: `return await self.send_message(receive_id, "interactive", card_content)`.

    Add a module-level `def _truncate_note(text: str, limit: int = 1000) -> str: return text if len(text) <= limit else text[:limit] + "…"` helper (Pitfall 9 A4 cap; lives next to the method to avoid a new module).

    Do NOT modify `send_order_notification`, `send_message`, `get_tenant_access_token`, or `bind_user`. Do NOT add a new package; do NOT touch `bind_user`'s `TODO`. Do NOT print the dish name, note, or secret values in logs (Pitfall 7/V7).
  </action>
  <verify>
    <automated>cd backend && uv run python -c "
import asyncio
from unittest.mock import AsyncMock, patch
from app.integrations.feishu import feishu_client, _truncate_note
async def main():
    # Sanity: truncation helper
    assert _truncate_note('x'*1500) == 'x'*1000 + '\u2026', 'truncate cap failed'
    # Sanity: new card with all optional fields present
    with patch.object(feishu_client, 'send_message', new_callable=AsyncMock, return_value=True) as m:
        await feishu_client.send_wish_notification('open_x', {'notification_type':'new','wish_id':1,'dish_name':'\u9ebb\u5a46\u8c46\u8150','submitter_name':'\u6d4b\u8bd5','reference_url':'https://x.example','note':'\u8fa3'})
        mtype, content = m.await_args.args[1], m.await_args.args[2]
        assert mtype == 'interactive', f'expected interactive, got {mtype}'
        assert content['header']['title']['content'] == '\u65b0\u613f\u671b\u901a\u77e5', content['header']['title']['content']
        assert any('https://x.example' in e['text']['content'] for e in content['elements']), 'reference link missing'
    # Sanity: new card with optional fields omitted
    with patch.object(feishu_client, 'send_message', new_callable=AsyncMock, return_value=True) as m:
        await feishu_client.send_wish_notification('open_x', {'notification_type':'new','wish_id':2,'dish_name':'d','submitter_name':'s','reference_url':None,'note':None})
        content = m.await_args.args[2]
        flat = ' '.join(e['text']['content'] for e in content['elements'])
        assert '\u53c2\u8003\u94fe\u63a5' not in flat, 'reference link should be omitted'
        assert '\u5907\u6ce8' not in flat, 'note should be omitted'
    # Sanity: edit card with note change
    with patch.object(feishu_client, 'send_message', new_callable=AsyncMock, return_value=True) as m:
        await feishu_client.send_wish_notification('open_x', {'notification_type':'edit','wish_id':3,'dish_name':'d','change_description':'x \u4fee\u6539\u4e86\u613f\u671b: d','old_note':'old','note_changed':True,'new_note':'new'})
        content = m.await_args.args[2]
        assert content['header']['title']['content'] == '\u613f\u671b\u53d8\u66f4\u901a\u77e5'
        flat = ' '.join(e['text']['content'] for e in content['elements'])
        assert '\u901a\u77e5\u7c7b\u578b\uff1aedit' in flat
        assert '\u539f\u5907\u6ce8\uff1aold' in flat
        assert '\u65b0\u5907\u6ce8\uff1anew' in flat
    # Sanity: deep link is lark_md; everything else is plain_text
    with patch.object(feishu_client, 'send_message', new_callable=AsyncMock, return_value=True) as m:
        await feishu_client.send_wish_notification('open_x', {'notification_type':'new','wish_id':7,'dish_name':'d','submitter_name':'s','reference_url':None,'note':None})
        content = m.await_args.args[2]
        link_elems = [e for e in content['elements'] if '\u67e5\u770b\u8be6\u60c5' in e['text']['content']]
        assert len(link_elems) == 1 and link_elems[0]['text']['tag'] == 'lark_md', 'deep link must be the only lark_md'
        other_elems = [e for e in content['elements'] if '\u67e5\u770b\u8be6\u60c5' not in e['text']['content']]
        assert all(e['text']['tag'] == 'plain_text' for e in other_elems), 'user values must be plain_text'
    print('OK')
asyncio.run(main())
"</automated>
  </verify>
  <acceptance_criteria>
    - feishu_client.send_wish_notification is callable and delegates to send_message with msg_type='interactive'
    - 'new' notification_type produces header "新愿望通知"; edit/cancel produce "愿望变更通知"
    - User-controlled strings appear inside `plain_text` tags; the deep link is the only `lark_md` element
    - Missing reference_url / note in `new` payload causes those elements to be omitted (no "None" string rendered)
    - Notes longer than 1000 chars are truncated before insertion into the card (helper verified via assertion)
    - Task 3 Section C test_card_* tests provide deeper structural coverage
  </acceptance_criteria>
  <done>Dedicated wish card method renders the locked content with plain_text + truncated notes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WishNotificationService with fan-out, single-target, and per-recipient failure isolation</name>
  <files>
    - backend/app/services/notification_service.py
  </files>
  <read_first>
    - backend/app/services/order_service.py (notify_order at line 168 — lazy import + try/except pattern)
    - backend/app/integrations/feishu.py (post Task 1: send_wish_notification signature)
    - backend/app/models/user.py (User.role, User.feishu_open_id)
    - backend/app/models/wish.py (Wish.dish_name, Wish.reference_url, Wish.note, Wish.claimer, Wish.user_id, Wish.claimed_by_chef_id)
    - .planning/phases/06-notifications-integration/06-CONTEXT.md (D-F04 targets, D-H03 failure handling, D-A03 separate service to avoid circular imports)
    - .planning/phases/06-notifications-integration/06-RESEARCH.md (Pattern 4 service structure, Pitfall 6 per-recipient try, Pitfall 7 capture-old pre-mutation)
  </read_first>
  <behavior>
    - notify_new_wish with one bound chef and one unbound chef + one bound admin makes exactly one await call (admin not in fan-out; role filter per D-F04)
    - notify_new_wish with three bound chefs makes exactly three await calls (one per chef with non-NULL feishu_open_id)
    - notify_claimed_wish_change with notification_type='edit' and claimed_by_chef_id=<bound chef id> makes exactly one await call
    - notify_claimed_wish_change with claimed_by_chef_id=None (unclaimed) makes zero await calls
    - notify_claimed_wish_change with claimed_by_chef_id=<chef id with NULL feishu_open_id> makes zero await calls (Pitfall 6 silent skip)
    - First recipient raises an exception AND second returns False; both calls attempted (Pitfall 6 isolation); wish HTTP request path is unaffected (this service is invoked post-flush, the request handler continues regardless)
  </behavior>
  <action>
     Create `backend/app/services/notification_service.py` with:

     1. Module docstring in Chinese ("愿望单通知服务 — 封装接收人查询与飞书发送的失败隔离").

     2. Add the required `from typing import Optional` import, then define two `@staticmethod` methods on a `WishNotificationService` class:


       ```python
       @staticmethod
       async def notify_new_wish(db: AsyncSession, wish: Wish, submitter: User) -> None:
           # D-F04: fan out to role='chef' with feishu_open_id IS NOT NULL
           result = await db.execute(
               select(User.feishu_open_id).where(
                   User.role == "chef",
                   User.feishu_open_id.is_not(None),
               )
           )
           receive_ids = list(result.scalars().all())
           payload = {
               "notification_type": "new",
               "wish_id": wish.id,
               "dish_name": wish.dish_name,
               "submitter_name": (submitter.display_name or submitter.username) if submitter else "未知用户",
               "reference_url": wish.reference_url,
               "note": wish.note,
           }
           from app.integrations.feishu import feishu_client
           for receive_id in receive_ids:
               try:
                   sent = await feishu_client.send_wish_notification(receive_id, payload)
                   if not sent:
                       print(f"⚠️ 愿望飞书通知发送失败: wish_id={wish.id}")
               except Exception as exc:
                   print(f"⚠️ 愿望飞书通知发送失败: wish_id={wish.id}, error={exc}")

       @staticmethod
       async def notify_claimed_wish_change(
           db: AsyncSession,
           wish: Wish,
           submitter: User,
           notification_type: str,   # "edit" | "cancel"
           claimed_by_chef_id: int,
           old_note: Optional[str],
       ) -> None:
           # D-F04: exactly the stored claimer (resolve by ID, not by role filter — A5 admin-claimer path)
           result = await db.execute(
               select(User.feishu_open_id).where(User.id == claimed_by_chef_id)
           )
           receive_id = result.scalar_one_or_none()
           if not receive_id:
               return  # chef without binding → silent skip
           submitter_name = (submitter.display_name or submitter.username) if submitter else "未知用户"
           action_verb = "修改了" if notification_type == "edit" else "撤回了"
           payload = {
               "notification_type": notification_type,
               "wish_id": wish.id,
               "dish_name": wish.dish_name,
               "change_description": f"{submitter_name} {action_verb}愿望: {wish.dish_name}",
               "old_note": old_note,
               "note_changed": (old_note != wish.note),
               "new_note": wish.note,
           }
           from app.integrations.feishu import feishu_client
           try:
               sent = await feishu_client.send_wish_notification(receive_id, payload)
               if not sent:
                   print(f"⚠️ 愿望飞书通知发送失败: wish_id={wish.id}")
           except Exception as exc:
               print(f"⚠️ 愿望飞书通知发送失败: wish_id={wish.id}, error={exc}")
       ```

    3. Module-level singleton: `wish_notification_service = WishNotificationService()` (mirrors `wish_service = WishService()` pattern).

    4. Lazy import of `feishu_client` inside each method (matches `order_service.py:171` and avoids the module-load circular-import risk noted in architecture constraints).

    Do NOT print dish_name / note / payload contents in any log line — log only `wish_id` and exception summary (Pitfall 7 / V7). Do NOT add a queue / outbox / retry — the locked synchronous best-effort pattern is the Phase 6 boundary (Architecture Map).
  </action>
  <verify>
    <automated>cd backend && uv run python -c "from app.services.notification_service import WishNotificationService, wish_notification_service; print('ok', WishNotificationService.__name__, wish_notification_service.__class__.__name__)"</automated>
  </verify>
  <acceptance_criteria>
    - wish_notification_service singleton importable from app.services.notification_service
    - notify_new_wish queries User.role == 'chef' AND feishu_open_id IS NOT NULL
    - notify_claimed_wish_change resolves the recipient by claimed_by_chef_id (no role filter — A5 admin-claimer path)
    - Per-recipient try/except is inside the fan-out loop, not around the whole loop (Pitfall 6)
    - No Pydantic / no new package; no edits to order_service or wish_service yet
  </acceptance_criteria>
  <done>Notification service resolves recipients and isolates failures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire 5 hook locations + submit_wish fan-out + atomic claim timestamp + service-payload-vs-card-rendering test split</name>
  <files>
    - backend/app/services/wish_service.py
    - backend/tests/test_wish_notifications.py
  </files>
  <read_first>
    - backend/app/services/wish_service.py (current 5 `# Phase 6 hook:` comments + submit_wish + claim_wish + advance_wish + reject_wish + update_wish + cancel_wish)
    - backend/app/services/notification_service.py (post Task 2)
    - backend/app/models/wish.py (last_status_change_at from Plan 01)
    - backend/app/utils/datetime_utils.py (naive_utc_now from Plan 01)
    - .planning/phases/06-notifications-integration/06-CONTEXT.md (D-H01 hook list with content-edit boundary table, D-H02 submit_wish fire, D-H03 failure handling)
    - .planning/phases/06-notifications-integration/06-RESEARCH.md (Exact Change Surface table, Pitfall 7 capture-old pre-mutation, Atomic claim timestamp example, Pattern 5 plain_text + truncation)
    - backend/tests/test_wishes.py (existing concurrent-claim assertion for DATA-08)
  </read_first>
  <behavior>
    **Service-layer behavior (Feishu + timestamp writes):**
    - POST /api/wishes with one bound chef → `feishu_client.send_wish_notification` awaited exactly once with `notification_type='new'`
    - POST /api/wishes with three bound chefs → three awaits (one per bound chef)
    - POST /api/wishes with zero bound chefs → zero awaits (no failure)
    - PUT /api/wishes/{id} on an unclaimed wish → zero awaits; PUT on a claimed wish → exactly one await with `notification_type='edit'` to the claiming chef's `feishu_open_id`
    - DELETE /api/wishes/{id} on an unclaimed wish → zero awaits; DELETE on a claimed wish → one await with `notification_type='cancel'`
    - After claim, GET /api/wishes as submitter shows `has_unread=true` (because `last_status_change_at` advanced)
    - The atomic claim UPDATE still rejects a concurrent second claimer with `rowcount==0` (DATA-08); winner's `wish.last_status_change_at` is non-NULL after the UPDATE
    - A Feishu send raising does NOT prevent the wish POST/PUT/DELETE/claim/advance/reject HTTP responses from returning their normal 200/201 status

    **Badge lifecycle (NOTIF-03): claim/advance/reject/cancel restore unread after a prior clear; invalid transitions leave timestamps unchanged:**
    - test_claim_restores_unread_after_prior_clear: POST wish → submitter GET detail (clears badge) → chef claims → submitter GET /api/wishes shows `has_unread=true` again
    - test_advance_restores_unread_after_prior_clear: claim → submitter detail clear → chef advance → submitter list `has_unread=true`
    - test_reject_restores_unread_after_prior_clear: claim → submitter detail clear → chef reject → submitter list `has_unread=true`
    - test_cancel_restores_unread_after_prior_clear: submit wish → submitter detail clear → submitter cancel → submitter list `has_unread=true`
    - test_invalid_claim_does_not_change_timestamps: a chef claims a wish; submitter clears the badge; a SECOND chef attempts the same claim (already-claimed → 400); assert both timestamps are still `last_status_change_at > submitter_last_viewed_at` (status still 准备中; no advance on the failed transition)
    - test_invalid_advance_does_not_change_timestamps: claim a wish without a published DishChef; submitter clears the badge; chef calls advance with invalid `related_dish_id` → 400; assert timestamps unchanged
    - test_invalid_reject_does_not_change_timestamps: claim; submitter clears badge; chef calls reject with empty/whitespace `reject_reason` → 400/422; assert timestamps unchanged
    - test_invalid_cancel_does_not_change_timestamps: submit a wish; submitter clears badge; submitter cancels (valid → 200); submitter attempts a second cancel → ValueError; refetch timestamps and confirm no further advance on the failed second cancel
    - test_content_edit_on_claimed_wish_does_not_advance_status_change_at: claim a wish; capture `last_status_change_at`; submitter PUTs note; refetch and assert `last_status_change_at` is UNCHANGED (locked D-H01 rule 2 — content edit must never write the timestamp; `submitter_last_viewed_at` is also unchanged because only the submitter's detail GET can clear)

    **Card rendering (Task 1 split): service payload assertions vs real card rendering:**
    - Service payload tests (Task 3 / `test_wish_notifications.py`): patch `send_wish_notification` with `AsyncMock(return_value=True)`; assert what the SERVICE passes to it (a dict with keys like `notification_type`, `wish_id`, `dish_name`, `submitter_name`, `reference_url`, `note`, `change_description`, `old_note`, `new_note`, `note_changed`). These tests do NOT inspect card structure.
    - Card rendering tests (Task 1 / `test_wish_notifications.py`): patch `feishu_client.send_message` (NOT `send_wish_notification`) with `AsyncMock(return_value=True)`; call `send_wish_notification` directly with various payloads; inspect the card structure passed to `send_message`. These tests assert: header title, plain_text vs lark_md per element, optional field omission, 1000-char truncation, deep-link suffix.
    - This split keeps the two responsibilities testable independently: the service decides what to send; the integration decides how to render. A regression in either layer fails the matching suite only.
  </behavior>
  <action>
    Edit `backend/app/services/wish_service.py`:

    1. Add import at top: `from app.utils.datetime_utils import naive_utc_now`. Do NOT import `feishu_client` at module level — use lazy import inside hook sites (mirrors order_service.py).

    2. **submit_wish** (lines 30-50) — after the existing `await db.refresh(wish)`, add the NOTIF-05 fan-out. Use lazy import inside the method to avoid circular dep:
       ```python
       from app.services.notification_service import wish_notification_service
       await wish_notification_service.notify_new_wish(db, wish, current_user)
       ```
       Do NOT add a timestamp write here — `server_default=func.now()` on the column already populates `last_status_change_at` per D-M01/D-A01 literal formula.

    3. **update_wish** (lines 142-179) — capture `old_note = wish.note`, `was_claimed = wish.status == "准备中" and wish.claimed_by_chef_id is not None`, `claimed_by_chef_id = wish.claimed_by_chef_id` BEFORE the `for field, value in patch.items():` loop (Pitfall 7 — capture pre-mutation). After the existing `await db.refresh(wish)`, if `was_claimed`, lazily import `wish_notification_service` and call `await wish_notification_service.notify_claimed_wish_change(db=db, wish=wish, submitter=current_user, notification_type="edit", claimed_by_chef_id=claimed_by_chef_id, old_note=old_note)`. **Per locked D-H01 rule 2, do NOT touch `last_status_change_at` here** — content-only edit (test `test_content_edit_on_claimed_wish_does_not_advance_status_change_at` enforces this).

    4. **cancel_wish** (lines 182-216) — capture `old_note = wish.note`, `was_claimed = wish.claimed_by_chef_id is not None`, `claimed_by_chef_id = wish.claimed_by_chef_id` BEFORE setting `wish.status = "已撤销"`. After the existing `await db.refresh(wish)`, add `wish.last_status_change_at = naive_utc_now()` then `await db.flush()` (D-B01). Then if `was_claimed`, lazily import and call `wish_notification_service.notify_claimed_wish_change(... notification_type="cancel", claimed_by_chef_id=claimed_by_chef_id, old_note=old_note)`.

    5. **claim_wish** (lines 218-254) — add `last_status_change_at=naive_utc_now()` INTO the existing atomic `.values(...)` call so D-01's single-statement claim winner boundary is preserved (Pitfall + research Atomic claim timestamp). No Feishu call here — submitter badge only (D-H01, NOTIF-03).

    6. **advance_wish** (lines 256-310) — assign `wish.last_status_change_at = naive_utc_now()` together with `wish.status = "已上架"` / `wish.related_dish_id = ...` BEFORE the existing `await db.flush()`. No Feishu (submitter badge only).

    7. **reject_wish** (lines 312-359) — assign `wish.last_status_change_at = naive_utc_now()` together with `wish.status = "已拒绝"` / `wish.reject_reason = ...` BEFORE the existing `await db.flush()`. No Feishu (submitter badge only).

    8. Replace each `# Phase 6 hook:` comment with the actual code or remove the comment once the hook is wired (do not leave stale `# Phase 6 hook:` markers).

    Create `backend/tests/test_wish_notifications.py` with the section header `# ========== 愿望飞书通知 (NOTIF-05/06) ==========` and these tests. The tests are split into THREE sections by responsibility. ALL Feishu calls are mocked via `unittest.mock.AsyncMock` per Pitfall 11.

    **Section A — Feishu fan-out + per-recipient failure isolation (mock `send_wish_notification`):**
    - test_submit_wish_fans_out_to_bound_chefs: seed 2 chefs with `feishu_open_id` + 1 admin with `feishu_open_id`; POST /api/wishes; assert mock.await_count == 2 (admins excluded, role filter).
    - test_submit_wish_skips_unbound_chefs: seed 1 chef with `feishu_open_id` + 1 chef with NULL `feishu_open_id`; assert await_count == 1.
    - test_edit_unclaimed_no_notify: POST wish, PUT wish (no claim); assert await_count == 0.
    - test_edit_claimed_notifies_claimer: POST wish, claim as chef, PUT wish; assert exactly one await with `notification_type='edit'` and `receive_id == chef.feishu_open_id`.
    - test_cancel_unclaimed_no_notify: POST wish, DELETE wish (no claim); assert await_count == 0.
    - test_cancel_claimed_notifies_claimer: POST wish, claim as chef, DELETE wish; assert exactly one await with `notification_type='cancel'`.
    - test_feishu_failure_does_not_break_wish_request: patch `send_wish_notification` with `side_effect=Exception("boom")`; POST wish; assert response.status_code == 201 (failure swallowed, D-H03).
    - test_fan_out_continues_after_one_failure: `side_effect=[Exception("first"), True]`; POST wish with two bound chefs; assert await_count == 2 (Pitfall 6 isolation).
    - test_claim_updates_status_change_timestamp: POST wish, claim; refetch wish; assert `wish.last_status_change_at is not None`.

    **Section B — Service payload assertions (mock `send_wish_notification`; do NOT touch `send_message`):**
    - test_submit_wish_service_payload: patch `send_wish_notification`; POST /api/wishes; capture the first call's positional args; assert the dict passed as the 2nd arg has `notification_type='new'`, `wish_id` is an int, `dish_name` matches the submit payload, `submitter_name` matches the submitter's display_name, `reference_url` and `note` match the optional fields (None when omitted). Do NOT inspect the card or `send_message` here.
    - test_edit_service_payload_includes_old_and_new_note: patch `send_wish_notification`; POST wish with note='old'; claim as chef; PUT wish with note='new'; capture the call's 2nd arg dict; assert `notification_type='edit'`, `old_note='old'`, `new_note='new'`, `note_changed=True`, `change_description` contains the submitter name and '修改了'.
    - test_cancel_service_payload_omits_note_fields: POST wish (no note); claim as chef; DELETE wish; capture the 2nd arg dict; assert `notification_type='cancel'`, `old_note` is None, `note_changed` is False, `change_description` contains the submitter name and '撤回了'. Do NOT inspect the card.

    **Section C — Card rendering assertions (mock `send_message`; do NOT touch `send_wish_notification`):**
    - test_card_new_includes_dish_and_submitter_and_deep_link: patch `feishu_client.send_message` (NOT `send_wish_notification`); call `await feishu_client.send_wish_notification(receive_id, {"notification_type":"new","wish_id":7,"dish_name":"麻婆豆腐","submitter_name":"测试","reference_url":None,"note":None})`; capture the 3rd positional arg (the `content` dict passed to `send_message`); assert `msg_type='interactive'` (2nd arg), `content["header"]["title"]["content"]=="新愿望通知"`, header template is "blue"; find the element div whose text contains "愿望菜品" and assert `text["tag"]=="plain_text"`; find the element div whose text contains "提交者" and assert `plain_text`; find the final element div whose text content contains "查看详情" and assert `text["tag"]=="lark_md"` and `text["content"]` matches the regex `\[查看详情\]\(https://family-chef\.app/wishes/7\)`.
    - test_card_new_omits_missing_optional_fields: same setup with `reference_url=None, note=None`; assert NO element div exists whose `plain_text` content contains "参考链接" and NO element div whose `plain_text` content contains "备注" (D-F02 omission rule).
    - test_card_new_renders_optional_fields_when_present: same setup with `reference_url="https://x.example", note="辣一点"`; assert one element div contains "参考链接：https://x.example" as `plain_text` and one contains "备注：辣一点" as `plain_text`.
    - test_card_edit_includes_change_type_and_notes: patch `send_message`; call `send_wish_notification(receive_id, {"notification_type":"edit","wish_id":3,"dish_name":"红烧肉","change_description":"测试用户 修改了愿望: 红烧肉","old_note":"原","note_changed":True,"new_note":"新"})`; assert header title == "愿望变更通知"; assert one element contains "通知类型：edit" as `plain_text`; assert one contains "变更说明：测试用户 修改了愿望: 红烧肉" as `plain_text`; assert one contains "原备注：原" as `plain_text`; assert one contains "新备注：新" as `plain_text`.
    - test_card_cancel_omits_note_sections_when_no_old_note: patch `send_message`; call `send_wish_notification(receive_id, {"notification_type":"cancel","wish_id":4,"dish_name":"宫保鸡丁","change_description":"测试用户 撤回了愿望: 宫保鸡丁","old_note":None,"note_changed":False})`; assert header title == "愿望变更通知"; assert one element contains "通知类型：cancel"; assert NO element contains "原备注" or "新备注" (D-F03 omission when no old_note).
    - test_card_truncates_notes_longer_than_1000_chars: patch `send_message`; call with `note="x"*1500` (new wish) and `old_note="y"*1500` + `new_note="z"*1500` (edit); assert each rendered `plain_text` value containing the truncated note is `len <= 1000` (Pitfall 9 A4 cap) and the final character is the truncation ellipsis.
    - test_card_user_values_always_plain_text: patch `send_message`; call with all user-controlled fields populated (`dish_name`, `submitter_name`, `note`, `reference_url`, `change_description`, `old_note`, `new_note`); assert every text element whose content embeds a user value has `text["tag"]=="plain_text"`. The deep link is the SOLE `lark_md`.
    - test_card_deep_link_uses_settings_app_url: patch `send_message`; call with `wish_id=42`; assert the `lark_md` element's content ends with `/wishes/42` and starts with `https://family-chef.app/wishes/` (the configured `APP_URL` placeholder from Plan 01).

    **Section D — Badge lifecycle (NOTIF-03) — restore after prior clear and invalid transitions leave timestamps unchanged:**
    - test_claim_restores_unread_after_prior_clear: POST `/api/wishes` as `user_token`; submitter GET `/api/wishes/{wish_id}` (clears badge — `submitter_last_viewed_at` is set); chef POST `/api/wishes/{wish_id}/claim`; submitter GET `/api/wishes` and assert `items[0]["has_unread"] == True`.
    - test_advance_restores_unread_after_prior_clear: same shape but the chef advances the wish via `/api/wishes/{wish_id}/advance` (with a published dish from `published_dish_for_chef` fixture) after a prior clear; assert `has_unread == True`.
    - test_reject_restores_unread_after_prior_clear: chef claims, submitter clears, chef rejects via `/api/wishes/{wish_id}/reject` with non-empty reason; assert `has_unread == True`.
    - test_cancel_restores_unread_after_prior_clear: submitter submits, submitter clears, submitter cancels via DELETE; assert `has_unread == True`.
    - test_invalid_claim_does_not_change_timestamps: chef claims a wish; submitter clears the badge (sets `submitter_last_viewed_at`); `chef2_token` POST `/api/wishes/{wish_id}/claim` → 400; refetch via `GET /api/wishes` and assert `has_unread == True` (the failed second claim must NOT have written `last_status_change_at` to a newer value; the badge state stays as-is after the clear, not re-cleared, not re-advanced).
    - test_invalid_advance_does_not_change_timestamps: claim a wish; submitter clears badge; chef POST `/api/wishes/{wish_id}/advance` with `related_dish_id=999999` (unpublished) → 400; assert `has_unread == True` and refetched wish's `last_status_change_at` equals the value captured right after the original claim.
    - test_invalid_reject_does_not_change_timestamps: claim; submitter clears badge; chef POST `/api/wishes/{wish_id}/reject` with `{"reject_reason": "   "}` (whitespace only) → 400; assert timestamps unchanged.
    - test_invalid_cancel_does_not_change_timestamps: submit wish; submitter clears badge; submitter DELETE → 200 (`has_unread` re-flip true); submitter DELETE again → ValueError (route returns 400); assert no additional timestamp advance on the failed second cancel.
    - test_content_edit_on_claimed_wish_does_not_advance_status_change_at: claim a wish; capture `w.last_status_change_at` from the GET /api/wishes response; submitter PUT wish with a new note; refetch `w.last_status_change_at` and assert it is unchanged (locked D-H01 rule 2); also assert the submitter's `submitter_last_viewed_at` is unchanged (only a detail GET can clear it, not a PUT).
    - test_concurrent_claim_still_atomic: keep the existing Phase 5 atomic-claim regression — `tests/test_wishes.py::test_concurrent_claim_only_one_wins` must still pass; do not duplicate here, but call out in the file's module docstring that this plan must not regress DATA-08.

    Use fixtures from `tests/conftest.py`; to add chefs with `feishu_open_id`, insert extra `User` rows via `test_session_factory` (same pattern as `published_dish_for_chef` in `test_wishes.py`). For Section C card tests, no HTTP request is needed — call `send_wish_notification` directly with constructed payloads.

    Do NOT mock `feishu_client.send_order_notification` in any of these tests (it's irrelevant). Do NOT call real Feishu endpoints. Do NOT edit `tests/test_wishes.py` — its concurrent-claim test is already the DATA-08 anchor.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_wish_notifications.py tests/test_wishes.py tests/test_wish_badge.py -q</automated>
  </verify>
  <acceptance_criteria>
    - All 5 hook locations in wish_service.py are wired (no remaining `# Phase 6 hook:` markers)
    - submit_wish fans out to bound chefs via the notification service
    - update_wish + cancel_wish notify exactly the stored claimer when claimed; zero calls when not claimed
    - claim_wish atomic UPDATE includes last_status_change_at in the .values() (single-statement)
    - advance_wish + reject_wish assign last_status_change_at before flush
    - All 4 test sections (A: fan-out, B: service payload, C: card rendering, D: badge lifecycle) pass
    - Phase 5 test_wishes.py and Phase 6 test_wish_badge.py still pass (no regression)
    - Locked D-H01 rule 2 enforced: test_content_edit_on_claimed_wish_does_not_advance_status_change_at passes
    - Locked D-H01 rule 3 enforced: test_invalid_*_does_not_change_timestamps tests pass
    - Locked D-H01 rule 1 enforced: test_*_restores_unread_after_prior_clear tests pass for claim/advance/reject/cancel
  </acceptance_criteria>
  <done>NOTIF-03 status-change timestamps + NOTIF-05/06 Feishu delivery wired; service payload and card rendering are tested separately; locked D-H01 content-edit boundary enforced; badge lifecycle (restore + invalid-transition no-op) regression-tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Service → Feishu API | Outbound to `open.feishu.cn` — failure is best-effort and must never fail the HTTP response (D-H03 / V7). |
| Service → DB | Notification queries read `User.feishu_open_id`, `Wish.claimed_by_chef_id`, `Wish.note`, `Wish.dish_name`; all user content is treated as untrusted. |
| Wish route → External side effect | The wish mutation must commit even when the Feishu call raises (Pitfall 10 / Architecture Map). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-I | Information Disclosure | send_wish_notification card body | mitigate | All user values (`dish_name`, `submitter_name`, `note`, `reference_url`, `change_description`) rendered as `plain_text`; only the server-constructed deep link uses `lark_md`. Truncate notes to 1000 chars (Pitfall 9). |
| T-06-03-I | Information Disclosure | Feishu fan-out | mitigate | `notify_new_wish` selects only `User.role == 'chef' AND feishu_open_id IS NOT NULL` — admins/non-chefs never receive new-wish cards. `notify_claimed_wish_change` resolves by `claimed_by_chef_id` only (A5 admin-claimer path). |
| T-06-03-D | Denial of Service | Feishu fan-out | mitigate | Per-recipient try/except inside the loop (Pitfall 6). One raising recipient does not skip later recipients. `False` return is logged and ignored separately. |
| T-06-03-D | Denial of Service | 30 KB card size cap | mitigate | `_truncate_note(limit=1000)` per A4; URL is fixed-shape `APP_URL/wishes/{id}`. |
| T-06-03-T | Tampering | claim_wish atomicity | mitigate | `last_status_change_at=naive_utc_now()` is included in the single atomic `.values(...)` so the claim winner boundary stays one statement (D-01 + Research Atomic claim timestamp). Existing DATA-08 test stays green. |
| T-06-03-R | Repudiation | Fan-out audit | accept | Synchronous best-effort; no outbox / event ID. Duplicates on retry accepted as a Phase 6 limitation (Architecture Map). |
| T-06-03-E | Elevation of Privilege | NOTIF-06 targeting | mitigate | `notify_claimed_wish_change` resolves recipient by `claimed_by_chef_id` (already validated by Phase 5 PERM-03/04). Unclaimed wishes send zero notifications. |
| T-06-03-SC | Tampering | Dependency install | mitigate | Zero-install phase (RESEARCH Package Legitimacy Audit = N/A); all imports are existing project modules. |

This plan addresses V4 (targeting), V5 (validation/encoding via plain_text), V7 (error handling around external send), and V8 (data protection — bind-only recipients). V2/V3/V6/V13 are unchanged by this plan.
</threat_model>

<verification>
- `cd backend && uv run pytest tests/test_wish_notifications.py -v` — all 4 sections (A fan-out, B service payload, C card rendering, D badge lifecycle) pass; ~20 tests total.
- `cd backend && uv run pytest tests/test_wishes.py tests/test_wish_badge.py tests/test_wish_notifications.py -q` — combined Phase 5 + Phase 6 suite stays green.
- `cd backend && uv run pytest tests/test_feishu.py -q` — pre-existing 4-failure baseline documented (Pitfall 12) is unchanged.
- `grep -n "# Phase 6 hook" backend/app/services/wish_service.py` returns no output (all hooks wired).
- `grep -n "last_status_change_at" backend/app/services/wish_service.py` shows assignment in claim_wish (inside atomic `.values(...)`), advance_wish, reject_wish, cancel_wish, and submit_wish (server_default only) — but NOT in update_wish (locked D-H01 rule 2).
- Manual Feishu smoke (after deploy): one designated chef test recipient receives a "新愿望通知" card on POST; receives a "愿望变更通知" card on PUT/DELETE of a claimed wish; failure does not fail the wish HTTP response.
</verification>

<success_criteria>
- NOTIF-05: New wish triggers one Feishu card per `role='chef' AND feishu_open_id IS NOT NULL`; no card to admins / unbound chefs.
- NOTIF-06: Submitter edit or cancel of a claimed wish triggers exactly one Feishu card to the stored claimer (D-F04); unclaimed edit/cancel triggers zero.
- NOTIF-03 (locked D-H01 rule 1): After claim, advance, reject, or cancel, the submitter's list `has_unread` flips back to true after a prior clear (covered by Section D restore tests).
- NOTIF-03 (locked D-H01 rule 2): A content-only `update_wish()` on a claimed wish sends NOTIF-06 but does NOT advance `last_status_change_at`; submitter's `has_unread` is unchanged (covered by `test_content_edit_on_claimed_wish_does_not_advance_status_change_at`).
- NOTIF-03 (locked D-H01 rule 3): Failed/invalid transitions (concurrent-claim 400; D-09/D-12 advance 400; whitespace reject 400; second cancel 400) leave both notification timestamps unchanged (covered by Section D invalid-transition tests).
- A Feishu exception or `False` return never bubbles up as a wish HTTP error.
- The atomic-claim regression (DATA-08) is preserved.
- All Feishu network calls in tests are mocked via `AsyncMock`; no live outbound HTTP.
- Card rendering tests verify: `plain_text` for all user-controlled values; `lark_md` only for the deep link; optional fields omitted when absent; 1000-char truncation on long notes; deep link uses `settings.APP_URL/wishes/{id}`.
- Service payload tests verify the dict shape the SERVICE passes to `send_wish_notification`, separately from the card envelope `send_wish_notification` passes to `send_message`.
</success_criteria>

<output>
Create `.planning/phases/06-notifications-integration/06-03-SUMMARY.md` when done
</output>