---
phase: 06-notifications-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/utils/datetime_utils.py
  - backend/app/models/wish.py
  - backend/app/config.py
  - backend/alembic/versions/<new>_add_wish_notification_timestamps.py
autonomous: true
requirements: [NOTIF-03, NOTIF-04]
user_setup: []

must_haves:
  truths:
    - "A new wish is persisted with non-NULL `last_status_change_at` and NULL `submitter_last_viewed_at` after migration"
    - "All existing wish rows survive the migration with their 4 indexes and 3 foreign keys intact"
    - "Every application write to a notification timestamp goes through the same UTC-naive helper (no `datetime.now()` calls in model/service code)"
    - "Settings exposes `APP_URL` with the placeholder default `https://family-chef.app`"
  artifacts:
    - path: "backend/app/utils/datetime_utils.py"
      provides: "naive_utc_now() helper used by all Phase 6 timestamp writes"
      exports: ["naive_utc_now"]
    - path: "backend/app/models/wish.py"
      provides: "Two new nullable DateTime columns on Wish"
      contains: "last_status_change_at"
    - path: "backend/app/config.py"
      provides: "APP_URL setting with placeholder default"
      contains: "APP_URL"
    - path: "backend/alembic/versions/<new>_add_wish_notification_timestamps.py"
      provides: "Batch migration that adds both columns with server default on status column"
      contains: "batch_alter_table"
  key_links:
    - from: "backend/app/models/wish.py"
      to: "backend/alembic/versions/<new>_*.py"
      via: "Column(server_default=func.now()) on last_status_change_at"
      pattern: "server_default=func\\(\\).now\\(\\)"
    - from: "backend/app/utils/datetime_utils.py"
      to: "SQLite CURRENT_TIMESTAMP"
      via: "datetime.now(UTC).replace(tzinfo=None)"
      pattern: "datetime\\.now\\(UTC\\)\\.replace"
---

<objective>
Add the data foundation that Phase 6 hook code will write into: a single UTC-naive clock helper, two new nullable timestamp columns on the `Wish` model, a `Settings.APP_URL` placeholder for Feishu deep links, and an Alembic batch migration that preserves every existing row + index + foreign key while populating `last_status_change_at` for legacy wishes.

Purpose: Every later plan (badge API in 06-02, Feishu hooks in 06-03) reads/writes these columns through one helper. Locking the clock convention here prevents the strict-comparison bug the research identified (local time vs SQLite UTC default).

Output: A new helper, two model columns, one config setting, and a reversible migration that passes a round-trip on a copy of the active DB.
</objective>

<execution_context>
@/home/temila/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/temila/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-notifications-integration/06-CONTEXT.md
@.planning/phases/06-notifications-integration/06-RESEARCH.md
@backend/app/models/wish.py
@backend/app/models/user.py
@backend/alembic/versions/72b56533bb6d_add_wishes_table.py
@backend/app/config.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add naive_utc_now helper, APP_URL setting, and two Wish columns</name>
  <files>
    - backend/app/utils/datetime_utils.py
    - backend/app/models/wish.py
    - backend/app/config.py
  </files>
  <read_first>
    - backend/app/models/wish.py (existing 11 columns, table_args with 4 indexes)
    - backend/app/config.py (Settings class, app/db/jwt/feishu/upload/cors sections)
    - backend/app/database.py (WAL mode + AsyncSession)
    - .planning/phases/06-notifications-integration/06-RESEARCH.md (Pattern 1, D-M01, A3)
  </read_first>
  <behavior>
    - naive_utc_now() returns a tz-aware-now() with tzinfo stripped; calling it twice in a row returns a strictly non-decreasing pair (microsecond resolution retained)
    - Settings(...).APP_URL equals the YAML `app.url` value when present, else exactly "https://family-chef.app"
    - Wish.last_status_change_at has server_default=func.now() and is nullable; Wish.submitter_last_viewed_at is nullable without default
    - Test: `from app.utils.datetime_utils import naive_utc_now; naive_utc_now().tzinfo is None`
  </behavior>
  <action>
    Create `backend/app/utils/datetime_utils.py` with a single function `naive_utc_now() -> datetime` that returns `datetime.now(UTC).replace(tzinfo=None)`. Module docstring in Chinese ("统一的时间时钟辅助（Phase 6）") explaining it matches SQLite `CURRENT_TIMESTAMP` semantics so strict `>` comparisons between application writes and DB defaults stay correct.

    Extend `Settings.__init__` in `backend/app/config.py` so the `app` section is read with a new `url` key:
    - `self.APP_URL: str = app.get("url", "https://family-chef.app")`
    Place it directly after `SECRET_KEY` so the four `app.*` keys stay grouped.

    Edit `backend/app/models/wish.py` to add two columns inside the `Wish` class after `updated_at`:
    - `last_status_change_at = Column(DateTime, nullable=True, server_default=func.now())` — server default ensures D-M01's "set on creation too" behaviour; existing rows backfilled by the migration in Task 2.
    - `submitter_last_viewed_at = Column(DateTime, nullable=True)` — initially NULL; cleared by D-B03.

    Do NOT touch `__table_args__` (D-M01 says no new indexes; status-change scans already hit `ix_wishes_status`).
    Do NOT introduce any third-party package (AGENTS.md zero-install constraint). Do NOT add env-var fallback for APP_URL — single source of truth is `config.yaml`.
  </action>
  <verify>
    <automated>cd backend && uv run python -c "from app.utils.datetime_utils import naive_utc_now; from app.models.wish import Wish; from app.config import Settings; import yaml; s=Settings({'app':{'url':'https://x.example'}}); print(s.APP_URL, hasattr(Wish,'last_status_change_at'), hasattr(Wish,'submitter_last_viewed_at'), naive_utc_now().tzinfo is None)"</automated>
  </verify>
  <acceptance_criteria>
    - naive_utc_now() returns naive datetime with tzinfo is None
    - Settings(...).APP_URL reads YAML `app.url` and defaults to "https://family-chef.app"
    - Wish model exposes last_status_change_at (with server_default=func.now()) and submitter_last_viewed_at (nullable)
    - No import of datetime.now, datetime.utcnow, or pytz appears in app/utils/datetime_utils.py
  </acceptance_criteria>
  <done>Helper + setting + model columns importable and behaviorally correct.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Alembic batch migration + round-trip verification on DB copy</name>
  <files>
    - backend/alembic/versions/<new>_add_wish_notification_timestamps.py
  </files>
  <read_first>
    - backend/alembic/versions/72b56533bb6d_add_wishes_table.py (existing wishes migration as schema reference)
    - backend/alembic.ini (script_location + sqlalchemy.url)
    - .planning/phases/06-notifications-integration/06-RESEARCH.md (Pattern 2 batch recreate, Pitfall 1 SQLite restriction, Migration Round-Trip section)
    - backend/app/models/wish.py (post Task 1 schema)
  </read_first>
  <behavior>
    - `alembic upgrade head` against a copy of the active DB succeeds without `OperationalError`
    - After upgrade: SELECT count(*) FROM wishes equals the pre-upgrade count; PRAGMA table_info('wishes') lists last_status_change_at and submitter_last_viewed_at; PRAGMA foreign_key_check returns 0 rows; PRAGMA index_list('wishes') still contains ix_wishes_user_id, ix_wishes_status, ix_wishes_claimed_by_chef_id, ix_wishes_status_chef
    - Existing rows have non-NULL last_status_change_at and NULL submitter_last_viewed_at
    - `alembic downgrade -1` drops only the two columns; `alembic upgrade head` re-creates them; data still intact
  </behavior>
  <action>
    Generate the migration with `uv run alembic revision -m "add wish notification timestamps"` (chained `down_revision = "72b56533bb6d"`). The filename will be `<rev>_add_wish_notification_timestamps.py`.

    Inside `upgrade()`, replace the auto-generated body with explicit `op.batch_alter_table("wishes", recreate="always")` blocks so SQLite uses move-and-copy instead of raw `ALTER TABLE ... ADD COLUMN ... DEFAULT CURRENT_TIMESTAMP` (the latter fails per SQLite docs and per the verified local probe in 06-RESEARCH.md):
    - Add `last_status_change_at`: `sa.Column("last_status_change_at", sa.DateTime(), nullable=True, server_default=sa.text("CURRENT_TIMESTAMP"))`
    - Add `submitter_last_viewed_at`: `sa.Column("submitter_last_viewed_at", sa.DateTime(), nullable=True)`

    Inside `downgrade()`, mirror with `batch_alter_table(... recreate="always")` dropping `submitter_last_viewed_at` first then `last_status_change_at`.

    Do NOT use `op.execute()` to manually backfill — server_default handles new rows. Do NOT add raw `op.add_column` (Pitfall 1). Do NOT modify the existing migration `72b56533bb6d_add_wishes_table.py`.

    Verification script (run as a one-shot Bash, not committed): copy `backend/data/family_chef.db` to `/tmp/opencode/family_chef_phase6.db`, override `sqlalchemy.url` via Alembic's Python API, run `upgrade head` → `downgrade -1` → `upgrade head` and check row count + `PRAGMA foreign_key_check` + `PRAGMA index_list('wishes')` before and after each step.
  </action>
  <verify>
    <automated>cd backend && cp -n data/family_chef.db /tmp/opencode/fc6.db 2>/dev/null; uv run python -c "
from alembic.config import Config
from alembic import command
cfg = Config('alembic.ini')
cfg.set_main_option('sqlalchemy.url','sqlite+aiosqlite:////tmp/opencode/fc6.db')
command.upgrade(cfg,'head'); command.downgrade(cfg,'-1'); command.upgrade(cfg,'head')
import sqlite3
c = sqlite3.connect('/tmp/opencode/fc6.db')
print('rows', c.execute('select count(*) from wishes').fetchone()[0])
print('cols', [r[1] for r in c.execute(\"PRAGMA table_info('wishes')\").fetchall() if 'status_change' in r[1] or 'viewed' in r[1]])
print('fk', c.execute('PRAGMA foreign_key_check').fetchall())
print('idx', [r[1] for r in c.execute(\"PRAGMA index_list('wishes')\").fetchall()])
"</automated>
  </verify>
  <acceptance_criteria>
    - alembic heads shows exactly one revision whose down_revision is "72b56533bb6d"
    - Migration file uses `op.batch_alter_table("wishes", recreate="always")` for both upgrade and downgrade
    - Round-trip on /tmp/opencode/fc6.db completes with row count preserved and 0 foreign_key_check rows
    - All 4 original wish indexes still present after upgrade
  </acceptance_criteria>
  <done>Migration is reversible, preserves all data + indexes + FKs, and is the only new revision.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | Untrusted user input crosses here; Pydantic still validates all `Wish*` payloads. |
| Service → DB | Application writes timestamps; research Pitfall 2 says mixing UTC+local yields wrong strict-comparison results. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-T | Tampering | Migration runner | mitigate | Use Alembic batch `recreate="always"` (Pitfall 1); run round-trip on a copy of `data/family_chef.db` before touching the active DB (Pitfall 14). |
| T-06-01-R | Repudiation | Migration | mitigate | Reversible: `downgrade()` drops both columns in the correct order; round-trip proves it. |
| T-06-01-I | Information Disclosure | Model columns | accept | `last_status_change_at` is internal status metadata; not exposed in API response schemas yet. `submitter_last_viewed_at` is submitter-private; never exposed via API either. |
| T-06-01-SC | Tampering | Dependency install | mitigate | Zero-install phase (RESEARCH Package Legitimacy Audit); no `pip`/`uv add` step; `requirements` table is `N/A`. |

Note: NOTIF-03/04 surface and Feishu delivery are wired in plans 06-02/06-03; this plan only handles persistence and the clock helper.
</threat_model>

<verification>
- Run Task 1's automated verify command — exits 0 and prints four truthy values plus `None` for tzinfo.
- Run Task 2's automated verify command — `rows` is the pre-migration count; `cols` lists both new columns; `fk` is empty; `idx` includes the four original wish indexes.
- `cd backend && uv run pytest tests/test_wishes.py -q` still passes 25 — confirms existing wish behavior unaffected by adding nullable columns.
</verification>

<success_criteria>
- `naive_utc_now()` is the single write helper for all Phase 6 timestamps (no `datetime.now()` introduced elsewhere in Phase 6 code).
- `Wish` model exposes both columns; existing wish tests continue to pass.
- Alembic head is the single new revision with `down_revision = "72b56533bb6d"`.
- Migration round-trip on the copied DB preserves rows, 4 indexes, and 0 foreign-key violations.
- `Settings.APP_URL` is settable via `app.url` in YAML with `https://family-chef.app` placeholder default.
</success_criteria>

<output>
Create `.planning/phases/06-notifications-integration/06-01-SUMMARY.md` when done
</output>