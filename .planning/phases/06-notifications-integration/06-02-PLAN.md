---
phase: 06-notifications-integration
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - backend/app/schemas/wish.py
  - backend/app/routers/wishes.py
  - backend/tests/test_wish_badge.py
autonomous: true
requirements: [NOTIF-03, NOTIF-04]
user_setup: []

must_haves:
  truths:
    - "GET /api/wishes returns `has_unread: true` for the submitter's own newly-created wish"
    - "GET /api/wishes/{id} as the submitter clears the badge (sets submitter_last_viewed_at to now)"
    - "GET /api/wishes/{id} as the claiming chef (chef MUST have claimed first; visibility per Phase 5) does NOT write submitter_last_viewed_at (Pitfall 4)"
    - "GET /api/wishes/{id} as admin does NOT write submitter_last_viewed_at (Pitfall 4 admin branch)"
    - "GET /api/wishes exposes `has_unread: false` to admin/chef viewers for wishes they did not submit (Pitfall 5)"
    - "After a prior clear, the submitter list item shows `has_unread: true` again once Plan 03 wires claim/advance/reject/cancel"
  artifacts:
    - path: "backend/app/schemas/wish.py"
      provides: "WishListResponse.has_unread bool with default False"
      contains: "has_unread"
    - path: "backend/app/routers/wishes.py"
      provides: "list_wishes per-item has_unread computation + get_wish submitter-only clear side-effect"
      contains: "submitter_last_viewed_at"
    - path: "backend/tests/test_wish_badge.py"
      provides: "Badge lifecycle + clear authorization (submitter + chef-claimer + admin) + viewer masking tests"
      min_lines: 110
  key_links:
    - from: "backend/app/routers/wishes.py::list_wishes"
      to: "Wish.last_status_change_at and Wish.submitter_last_viewed_at"
      via: "compute_has_unread helper invoked per item, masked for non-submitter"
      pattern: "compute_has_unread"
    - from: "backend/app/routers/wishes.py::get_wish"
      to: "Wish.submitter_last_viewed_at"
      via: "exact identity check wish.user_id == current_user.id then naive_utc_now() write + db.flush() (no commit)"
      pattern: "wish\\.user_id == current_user\\.id"
    - from: "backend/tests/test_wish_badge.py::test_chef_detail_does_not_clear_badge"
      to: "Phase 5 PERM-01 visibility gate"
      via: "POST /api/wishes/{id}/claim BEFORE chef detail GET — chef can only read detail when claimed_by_chef_id == chef.id"
      pattern: "claim"
---

<objective>
Surface the unread badge through the wish API: `WishListResponse.has_unread` is computed per item (with submitter-only disclosure), and `GET /api/wishes/{id}` clears it as a side effect only when the caller is exactly the wish's submitter. Plan 03 wires the timestamp writes that drive the badge lifecycle; this plan delivers the read-side contract so Phase 7 frontend can render red dots without further backend changes.

Purpose: NOTIF-03 (status changes surface unread) requires the badge to be observable; NOTIF-04 (detail view clears it) requires the submitter-only clear side-effect. Both are pure additions to existing endpoints — no new routes.

Output: One schema field, two router edits, and a test file proving badge correctness across the submitter, claimer, and admin viewer paths.
</objective>

<execution_context>
@/home/temila/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/temila/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<execution_preflight>
Before reading or modifying implementation files, preserve the current working tree and require the project branch from AGENTS.md. Record `git status --short`; do not use `git reset`, `git clean`, `git restore`, or any stash command. If the current branch is not `feature/guest_order`, create that local branch from the current HEAD with `git switch -c feature/guest_order` when it does not exist. If it already exists locally, proceed only when `git merge-base --is-ancestor feature/guest_order HEAD` succeeds, then fast-forward the local pointer with `git branch -f feature/guest_order HEAD` and switch to it. If either ancestry or switch fails, stop without altering tracked or untracked artifacts. This deliberately preserves the Phase 5 summaries and Phase 6 planning artifacts instead of switching to the older remote tip.

<automated>current=$(git branch --show-current) && if [ "$current" != "feature/guest_order" ]; then if git show-ref --verify --quiet refs/heads/feature/guest_order; then git merge-base --is-ancestor feature/guest_order HEAD && git branch -f feature/guest_order HEAD && git switch feature/guest_order; else git switch -c feature/guest_order; fi; fi && test "$(git branch --show-current)" = "feature/guest_order"</automated>
</execution_preflight>

<context>
@.planning/phases/06-notifications-integration/06-CONTEXT.md
@.planning/phases/06-notifications-integration/06-RESEARCH.md
@.planning/phases/06-notifications-integration/06-01-PLAN.md
@backend/app/schemas/wish.py
@backend/app/routers/wishes.py
@backend/app/services/wish_service.py
@backend/app/models/wish.py
@backend/app/utils/datetime_utils.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add has_unread schema field and wire router compute/clear side-effect</name>
  <files>
    - backend/app/schemas/wish.py
    - backend/app/routers/wishes.py
  </files>
  <read_first>
    - backend/app/schemas/wish.py (WishListResponse currently has submitter_name + claimed_by_chef_name)
    - backend/app/routers/wishes.py (list_wishes lines 46-79, get_wish lines 85-103)
    - backend/app/utils/datetime_utils.py (naive_utc_now helper from Plan 01)
    - .planning/phases/06-notifications-integration/06-CONTEXT.md (D-B01 unread formula, D-B02 list field, D-B03 clear side-effect, D-A01 endpoints)
    - .planning/phases/06-notifications-integration/06-RESEARCH.md (Pattern 3 compute_has_unread, Pitfall 4 clear authorization, Pitfall 5 non-submitter masking)
  </read_first>
  <behavior>
    - After wish creation, GET /api/wishes as the submitter returns has_unread=true (because submitter_last_viewed_at is NULL while last_status_change_at is set by server_default)
    - Submitter GET /api/wishes/{id} once → subsequent GET /api/wishes list shows has_unread=false for that wish
    - Chefer GET /api/wishes/{id} on a submitter's wish does not change submitter_last_viewed_at (a follow-up submitter GET still sees has_unread=true)
    - Admin GET /api/wishes sees has_unread=false for any wish (Pitfall 5)
  </behavior>
  <action>
    Edit `backend/app/schemas/wish.py` — add `has_unread: bool = False` as the last field on `WishListResponse` (after `claimed_by_chef_name`). Do not add the underlying timestamps to `WishResponse` (D-A01 only exposes the computed boolean). `WishDetailResponse` stays unchanged — its submitter-name / claimed-by-chef-name fields are unchanged.

    Edit `backend/app/routers/wishes.py`:

    1. Add `from app.utils.datetime_utils import naive_utc_now` to the imports.

    2. Define a module-level helper (place it directly above `router = APIRouter()`):
       ```python
       def compute_has_unread(wish, current_user) -> bool:
           """D-B01 unread logic with D-A01 + Pitfall 5 masking.
           Returns False for non-submitter viewers; True only when:
             - last_status_change_at is not None, AND
             - (submitter_last_viewed_at is None OR last_status_change_at > submitter_last_viewed_at)
           """
           if wish.user_id != current_user.id:
               return False
           if wish.last_status_change_at is None:
               return False
           if wish.submitter_last_viewed_at is None:
               return True
           return wish.last_status_change_at > wish.submitter_last_viewed_at
       ```
       Module docstring on the helper in Chinese, per convention.

    3. In `list_wishes` (lines ~67-72), replace the per-item mapping with `item.has_unread = compute_has_unread(w, current_user)` so the value is computed only for submitters (the helper handles masking).

    4. In `get_wish` (lines ~85-103), after the `not wish` 404 guard and BEFORE building `resp`, add the clear side-effect:
       ```python
       if wish.user_id == current_user.id:
           wish.submitter_last_viewed_at = naive_utc_now()
           await db.flush()
       ```
       Do NOT call `db.commit()` — `get_db()` commits on successful yield-exit (database.py:39-49). Do NOT branch by role — branch by exact identity (Pitfall 4).

    Do NOT touch `submit_wish`, `update_wish`, `cancel_wish`, `claim_wish`, `advance_wish`, or `reject_wish` — Plan 03 wires the timestamp writes for those.
  </action>
  <verify>
    <automated>cd backend && uv run python -c "from app.schemas.wish import WishListResponse; from app.routers.wishes import compute_has_unread; print('ok', WishListResponse.model_fields['has_unread'].default, compute_has_unread.__name__)"</automated>
  </verify>
  <acceptance_criteria>
    - WishListResponse has has_unread: bool = False
    - compute_has_unread is importable from app.routers.wishes and follows the locked formula + masking
    - get_wish calls naive_utc_now() then db.flush() (no db.commit) only when wish.user_id == current_user.id
    - list_wishes sets item.has_unread per item via the helper
    - No new imports beyond `naive_utc_now`; no schema changes to WishResponse or WishDetailResponse
  </acceptance_criteria>
  <done>Schema + router expose has_unread; submitter-only clear side-effect wired.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Badge lifecycle, clear authorization, and viewer-masking tests</name>
  <files>
    - backend/tests/test_wish_badge.py
  </files>
  <read_first>
    - backend/tests/conftest.py (client/user_token/chef_token/admin_token fixtures + chef2_token/user2_token)
    - backend/tests/test_wishes.py (POST /api/wishes payload + assertion style, user_wish fixture pattern, chef claim flow)
    - backend/app/services/wish_service.py (get_wish_by_id visibility gate — chef requires wish.claimed_by_chef_id == current_user.id)
    - backend/app/routers/wishes.py (post Task 1: list_wishes + get_wish)
    - backend/app/schemas/wish.py (post Task 1: WishListResponse.has_unread)
    - .planning/phases/06-notifications-integration/06-RESEARCH.md (Testing and Verification Guidance — Required Automated Cases)
  </read_first>
  <behavior>
    - test_initial_unread_state: POST /api/wishes → GET /api/wishes as submitter → first item has has_unread=true
    - test_submitter_detail_clears_badge: submitter GET /api/wishes/{id} → subsequent GET /api/wishes has has_unread=false for same wish
    - test_chef_detail_does_not_clear_badge: chef GET /api/wishes/{id} (on a wish the chef has already claimed) does NOT clear; submitter's subsequent list still shows has_unread=true (Pitfall 4 / D-B03). Pre-condition: chef must claim the wish first; the wish's visibility to a chef depends on `claimed_by_chef_id == chef.id` per Phase 5 `get_wish_by_id`.
    - test_admin_detail_does_not_clear_badge: admin GET /api/wishes/{id} (an admin can read any wish) does NOT clear submitter's `submitter_last_viewed_at`; submitter's subsequent list still shows has_unread=true. Covers the admin branch of Pitfall 4 alongside the chef branch.
    - test_admin_list_masks_unread: admin GET /api/wishes shows has_unread=false for any wish regardless of submitter (Pitfall 5)
    - test_chef_list_masks_unread: chef GET /api/wishes (queue + my claims) shows has_unread=false for wishes the chef did not submit
  </behavior>
  <action>
    Create `backend/tests/test_wish_badge.py` with the section header `# ========== 愿望未读红点 (NOTIF-03/04) ==========` and these 6 test cases.

    Reuse existing fixtures from `tests/conftest.py`: `client`, `user_token`, `chef_token`, `chef2_token`, `admin_token`. To make a wish visible to a chef via `GET /api/wishes/{id}`, the chef MUST have claimed it first — Phase 5 `get_wish_by_id` returns None for a chef whose `id != wish.claimed_by_chef_id`, which would surface as 404 not 200. Therefore every "chef sees a wish detail" test must `POST /api/wishes/{id}/claim` with the chef token first.

    Concrete test bodies:

    1. **test_initial_unread_state** — POST `/api/wishes` as `user_token` with `dish_name='宫保鸡丁'`. Then `GET /api/wishes` as `user_token`. Assert `data["items"][0]["has_unread"] == True`.

    2. **test_submitter_detail_clears_badge** — Reuse `user_wish` (or POST then capture `wish_id`). As `user_token` do `GET /api/wishes/{wish_id}` (this MUST be a 200). Then `GET /api/wishes` as `user_token` and assert `has_unread == False` for that item. Asserts the clear side-effect committed (get_db post-yield commit).

    3. **test_chef_detail_does_not_clear_badge** — POST a wish as `user_token`. As `chef_token` `POST /api/wishes/{wish_id}/claim` first (must succeed with 200; this is the precondition for chef visibility). Then as `chef_token` `GET /api/wishes/{wish_id}` (must return 200; chef is now a permitted viewer because `wish.claimed_by_chef_id == chef.id`). Then as `user_token` `GET /api/wishes` and assert the wish's `has_unread == True`. The submitter must NEVER make the GET to the detail endpoint before this assertion — otherwise the badge would already be cleared by the submitter's own call.

    4. **test_admin_detail_does_not_clear_badge** — POST a wish as `user_token`; do NOT let the submitter GET the detail. As `admin_token` `GET /api/wishes/{wish_id}` (admin can read any wish per `get_wish_by_id`'s admin branch). Then as `user_token` `GET /api/wishes` and assert the wish's `has_unread == True`. This proves the admin branch of the clear side-effect is gated by the same exact-identity check (Pitfall 4).

    5. **test_admin_list_masks_unread** — Submit two wishes as `user_token` (do not view detail). Then as `admin_token` `GET /api/wishes` and assert every item has `has_unread == False`. Pitfall 5 + D-B02 masking.

    6. **test_chef_list_masks_unread** — Submit a wish as `user_token`, then `chef_token` claims it, then `chef_token` GETs `/api/wishes` and asserts the wish's `has_unread == False` (chef did not submit it, so the helper returns False even though the wish technically has unread state for its actual submitter).

    Critical assertions to embed across all tests:
    - The 201 response from POST /api/wishes does NOT include `has_unread` (that field only appears on `WishListResponse`); call `/api/wishes` to read it.
    - `response.json()["items"][0]["has_unread"]` is the canonical read.
    - For tests 3 and 4, the submitter's detail GET must come AFTER the chef/admin read; ordering matters.

    Do NOT patch `naive_utc_now` in these tests — the comparison relies on real wall-clock ordering. Do NOT add new fixtures to conftest.py — keep this file self-contained except for the shared fixtures. Do NOT test the Feishu wiring (Plan 03 covers that).
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_wish_badge.py -v</automated>
  </verify>
  <acceptance_criteria>
    - All 6 tests pass under `uv run pytest tests/test_wish_badge.py -v`
    - test_chef_detail_does_not_clear_badge claims the wish BEFORE the chef detail GET (asserts the precondition 200 from claim and 200 from detail)
    - test_admin_detail_does_not_clear_badge covers the admin branch of the clear side-effect
    - Tests assert the locked formula (has_unread=true on fresh wish; false after submitter detail; false for non-submitter viewers; not cleared by chef OR admin)
    - No live Feishu call is made (no AsyncMock needed in this plan)
    - File does not import or mock naive_utc_now (real wall clock is fine for ordering)
  </acceptance_criteria>
  <done>Badge behavior contract is regression-tested across submitter / claimer / admin viewers with correct preconditions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | Untrusted callers can hit GET /api/wishes and /api/wishes/{id}; both must enforce visibility + identity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-E | Elevation of Privilege | GET /api/wishes/{id} clear side-effect | mitigate | Exact identity check `wish.user_id == current_user.id` BEFORE the write (Pitfall 4 / D-B03); chef (after claim) and admin reads must remain no-write. Tests `test_chef_detail_does_not_clear_badge` (chef claims first, then GET detail) and `test_admin_detail_does_not_clear_badge` enforce both branches. |
| T-06-02-I | Information Disclosure | GET /api/wishes has_unread field | mitigate | `compute_has_unread` returns False for any viewer whose `id != wish.user_id` (Pitfall 5 / Security Notes); test `test_admin_list_masks_unread` + `test_chef_list_masks_unread` enforce. |
| T-06-02-T | Tampering | GET /api/wishes/{id} commit boundary | mitigate | `await db.flush()` only; commit happens in `get_db()` post-yield. Test confirms a chef read and an admin read do not persist. |
| T-06-02-R | Repudiation | has_unread field | accept | The field is a derived view of two timestamps; the audit trail comes from the timestamps themselves, not the boolean. |
| T-06-02-SC | Tampering | Dependency install | mitigate | Zero-install phase; no package added in this plan. |

This plan addresses the V4 access-control and V8 data-protection requirements; Plan 03 handles V7 (error handling around external send).
</threat_model>

<verification>
- `cd backend && uv run pytest tests/test_wish_badge.py -v` — all 6 tests pass.
- `cd backend && uv run pytest tests/test_wishes.py tests/test_wish_badge.py -q` — Phase 5 wish tests + Phase 6 badge tests still green.
- Manual: `curl -s http://localhost:8000/api/wishes -H "Authorization: Bearer $TOK"` (with a freshly-submitted wish) returns `has_unread: true` in the first item; re-call after the submitter GETs `/api/wishes/{id}` and the field flips to `false`.
</verification>

<success_criteria>
- `WishListResponse.has_unread` appears in every list response with the locked formula applied only for submitters.
- `GET /api/wishes/{id}` clears the badge for the submitter and only for the submitter (exact-identity check).
- Chef GET /api/wishes/{id} does NOT clear (chef can only read because wish was claimed first).
- Admin GET /api/wishes/{id} does NOT clear (Pitfall 4 admin branch).
- Non-submitter viewers always see `has_unread: false` regardless of timestamp state.
- All 6 badge tests pass; Phase 5 wish tests still pass (no regression).
</success_criteria>

<output>
Create `.planning/phases/06-notifications-integration/06-02-SUMMARY.md` when done
</output>