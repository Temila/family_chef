---
phase: 19-account-bound-theme-preferences
plan: 02
type: execute
wave: 2
depends_on:
  - 19-01
files_modified:
  - frontend/src/api/client.js
  - frontend/src/theme/theme-context.jsx
  - frontend/src/components/composites/Header.jsx
autonomous: true
requirements:
  - D-A1
  - D-A2
  - D-A3
  - D-A4
  - D-A5
  - D-A6
user_setup: []

must_haves:
  truths:
    - "Logged-in users: theme preference setters dual-write to localStorage AND debounced-PUT to /api/users/me/theme-preferences (D-A1, D-A4)"
    - "On login, GET /api/users/me/theme-preferences; if 200, hydrate theme-context state + localStorage from server (D-A4); if 404, upload localStorage values as initial payload (D-A5)"
    - "On logout, removeItem 4 fc_* keys (fc_active_theme, fc_season_enabled, fc_hemisphere, fc_season_theme_map) AND reset theme-context state to DEFAULT_PRESET / false / north / default map; preserve fc_theme + fc_last_season (D-A6)"
    - "Unauthenticated users do NOT see Header light/dark ThemeToggle or Palette IconButton (D-A2, D-A3)"
    - "Pre-existing fc_theme (legacy light/dark) and fc_last_season (rendering cache) survive login/logout cycles untouched (D-A6)"
  artifacts:
    - path: frontend/src/api/client.js
      provides: "ApiClient.getThemePreferences() and updateThemePreferences(payload) methods; error.status exposed on Error instances"
      contains: "getThemePreferences"
    - path: frontend/src/theme/theme-context.jsx
      provides: "Dual-write useEffect with 200ms debounce, login-fetch with 404 to upload fallback, and logout cleanup; new refreshThemePreferences method"
      contains: "refreshThemePreferences"
    - path: frontend/src/components/composites/Header.jsx
      provides: "Header ThemeToggle IconButton plus Palette IconButton wrapped in user guard; avatar dropdown guard already exists"
      contains: "user &&"
  key_links:
    - from: frontend/src/theme/theme-context.jsx
      to: /api/users/me/theme-preferences
      via: "api.getThemePreferences() in refreshThemePreferences; debounced api.updateThemePreferences() in dual-write effect"
      pattern: "api\\.(get|update)ThemePreferences"
    - from: frontend/src/components/composites/Header.jsx
      to: useAuth().user
      via: "user guard conditional rendering around light/dark plus Palette IconButtons"
      pattern: "user\\s*&&\\s*\\("
---

<objective>
Wire the frontend to the new server-side preference persistence (D-A1, D-A4, D-A5, D-A6, D-A2, D-A3). Three minimal additive changes: extend the API client, extend ThemeContext lifecycle for login-fetch plus logout cleanup plus debounced PUT, and wrap two Header IconButtons in a user guard. The rendering pipeline (theme-engine, fouc-bootstrap, presets, ThemeSettingsPage body, color tokens) is NOT touched.
</objective>

<execution_context>
@/home/temila/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/temila/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-account-bound-theme-preferences/19-CONTEXT.md
@frontend/src/api/client.js
@frontend/src/theme/theme-context.jsx
@frontend/src/contexts/AuthContext.jsx
@frontend/src/components/composites/Header.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1 ApiClient methods plus error.status</name>
  <files>frontend/src/api/client.js</files>
  <read_first>frontend/src/api/client.js:16-61 request method error throw at lines 56-58 must be extended to attach .status; frontend/src/api/client.js:271-306 existing getThemes/createTheme pattern mirror the camelCase conversion for sourceColors</read_first>
  <action>Modify frontend/src/api/client.js. Two changes only.

A) Extend the error throw in the request method at lines 56-58 from "throw new Error(data.detail || '请求失败')" to "const err = new Error(data.detail || '请求失败'); err.status = res.status; throw err;". This makes HTTP status available on caught errors without breaking existing catch(err) call sites; new code reads err.status to detect 404.

B) Add two new methods to the ApiClient class after deleteTheme (line 304-306), before the Preferences block. Use literal names getThemePreferences and updateThemePreferences.

getThemePreferences calls this.get('/users/me/theme-preferences') and returns the result. Transform snake_case response to camelCase: { active_theme: { id, name, sourceColors: source_colors, variant, kind }, season_enabled, hemisphere, season_theme_map: transformed_season_map }. The transformed_season_map maps each entry snake_case source_colors to camelCase sourceColors.

updateThemePreferences(payload) calls this.put('/api/users/me/theme-preferences', payload). Inverse transform on payload: convert camelCase sourceColors back to snake_case source_colors. The payload shape from theme-context will be { active_theme: {sourceColors:{primary,secondary,tertiary}}, season_enabled, hemisphere, season_theme_map: {spring: {sourceColors: ...}, ...} }; convert to { active_theme: { source_colors: {primary, secondary, tertiary}, variant, kind, id, name }, season_enabled, hemisphere, season_theme_map: {spring: {source_colors: ...}, ...} }.

Do not modify any existing method. Do not change the Preferences block or the Themes block. Do not touch the 401 redirect logic at lines 30-37.</action>
  <verify>
    <automated>cd /home/temila/family_chef/frontend && grep -n 'getThemePreferences|updateThemePreferences|err.status' src/api/client.js</automated>
  </verify>
  <acceptance_criteria>
    <criterion>frontend/src/api/client.js contains the literal strings getThemePreferences, updateThemePreferences, and err.status assignment.</criterion>
    <criterion>The existing Themes block methods (getThemes, createTheme, updateTheme, deleteTheme) and the auth block are unchanged.</criterion>
    <criterion>The 401 redirect logic at lines 30-37 is unchanged.</criterion>
    <criterion>No new imports needed.</criterion>
  </acceptance_criteria>
  <done>ApiClient exposes getThemePreferences and updateThemePreferences; HTTP errors carry .status so theme-context can detect 404 for first-login migration.</done>
</task>

<task type="auto">
  <name>Task 2 ThemeContext dual-write plus login-fetch plus logout cleanup</name>
  <files>frontend/src/theme/theme-context.jsx</files>
  <read_first>frontend/src/theme/theme-context.jsx:228-244 useState initializers and refs; frontend/src/theme/theme-context.jsx:261-270 injectThemeCss useEffect DO NOT TOUCH; frontend/src/theme/theme-context.jsx:272-305 existing refreshCustomThemes plus user-keyed useEffect extend pattern here; frontend/src/theme/theme-context.jsx:306-347 setSeasonEnabled/setHemisphere/setSeasonTheme internal write logic unchanged; frontend/src/theme/theme-context.jsx:391-394 applyCurrentSeason effect DO NOT TOUCH</read_first>
  <action>Modify frontend/src/theme/theme-context.jsx. Additive changes only. Do NOT touch: injectThemeCss path, buildCssSync call, fouc-bootstrap contract, mutex gate (setActiveTheme/resetToDefault), applyCurrentSeason effect, useMemo value object, justEnabledRef cache bypass, seasonThemeMap merge logic, setSeasonTheme justEnabledRef trigger.

A) Add two refs and a useState at the top of ThemeProvider after line 240 justEnabledRef declaration:
   - preferencesLoadedRef = useRef(false) - whether server sync has completed at least once for the current session
   - skipNextPutRef = useRef(false) - skip the next debounced PUT (set true after a sync from server or first-login upload)
   - debouncedPutTimerRef = useRef(null) - debounce timer handle
   Use refs (not state) for these to avoid triggering re-renders.

B) Replace the existing user-keyed useEffect at lines 301-305 with an extended version:
   - If !user?.id (logout): removeItem 4 fc_* keys (ACTIVE_THEME_KEY=fc_active_theme, SEASON_ENABLED_KEY=fc_season_enabled, HEMISPHERE_KEY=fc_hemisphere, SEASON_THEME_MAP_KEY=fc_season_theme_map). Reset state to DEFAULT_PRESET/false/north/buildDefaultSeasonThemeMap(). Set preferencesLoadedRef.current=false. Clear debouncedPutTimerRef. Return undefined.
   - If user?.id (login): queueMicrotask(() => refreshThemePreferences()); return undefined.

C) Add a new refreshThemePreferences useCallback after the existing refreshCustomThemes around line 305. Logic:
   - try { server = await api.getThemePreferences(); skipNextPutRef.current = true; setActiveThemeState(normalizeTheme(server.active_theme)); setSeasonEnabledState(Boolean(server.season_enabled)); setHemisphereState(normalizeHemisphere(server.hemisphere)); setSeasonThemeMapState(server.season_theme_map || buildDefaultSeasonThemeMap()); writeActiveThemeToStorage(server.active_theme); writeSeasonEnabledToStorage(Boolean(server.season_enabled)); writeHemisphereToStorage(normalizeHemisphere(server.hemisphere)); writeSeasonThemeMapToStorage(server.season_theme_map || buildDefaultSeasonThemeMap()); preferencesLoadedRef.current = true; }
   - catch (err) { if (err.status === 404) { readActiveThemeFromStorage/readSeasonEnabledFromStorage/readHemisphereFromStorage/readSeasonThemeMapFromStorage and PUT as initial payload with skipNextPutRef=true so subsequent state-setter useEffect runs do not echo back; preferencesLoadedRef.current = true; } else { preferencesLoadedRef.current = true; } }

D) Add a new useEffect for the debounced PUT after the injectThemeCss useEffect at lines 261-270. Dependencies: [user, activeTheme, seasonEnabled, hemisphere, seasonThemeMap]. Logic:
   - If !user || !preferencesLoadedRef.current: return
   - If skipNextPutRef.current: skipNextPutRef.current = false; return
   - Clear any pending debouncedPutTimerRef.current
   - debouncedPutTimerRef.current = setTimeout(async () => { try { await api.updateThemePreferences({ active_theme: serialize(activeTheme), season_enabled: seasonEnabled, hemisphere, season_theme_map: serialize(seasonThemeMap) }); } catch { silent; } }, 200)
   - Cleanup: clearTimeout on unmount or dependency change.

E) Add serialize helpers as module-level functions near readActiveThemeFromStorage at lines 70-95. serializeActiveTheme returns { id, name, sourceColors, variant, kind }. serializeSeasonThemeMap maps each entry to { id, name, sourceColors, variant, kind }.

Do not add new toast calls; fetch failures are silent per D-A4 (the existing refreshCustomThemes still shows toasts on failure - leave that alone).</action>
  <verify>
    <automated>cd /home/temila/family_chef/frontend && grep -n 'refreshThemePreferences|preferencesLoadedRef|skipNextPutRef|debouncedPutTimerRef' src/theme/theme-context.jsx</automated>
  </verify>
  <acceptance_criteria>
    <criterion>frontend/src/theme/theme-context.jsx contains all of: refreshThemePreferences, preferencesLoadedRef, skipNextPutRef, debouncedPutTimerRef.</criterion>
    <criterion>The existing localStorage write helpers (writeActiveThemeToStorage, writeSeasonEnabledToStorage, writeHemisphereToStorage, writeSeasonThemeMapToStorage) are called from inside refreshThemePreferences success path.</criterion>
    <criterion>The user-keyed useEffect branches on !user?.id and removes the 4 fc_* keys (ACTIVE_THEME_KEY, SEASON_ENABLED_KEY, HEMISPHERE_KEY, SEASON_THEME_MAP_KEY) but never removes fc_theme (legacy) or LAST_SEASON_KEY (rendering cache).</criterion>
    <criterion>The 404 branch in refreshThemePreferences reads from localStorage and calls api.updateThemePreferences as the initial upload (D-A5).</criterion>
    <criterion>The debounced PUT effect is gated on user && preferencesLoadedRef.current and respects skipNextPutRef (D-A4 silent fetch).</criterion>
    <criterion>The injectThemeCss useEffect (line 261-270), the applyCurrentSeason effect (line 391-394), the useMemo value object (line 396-426), and the mutex-gated setActiveTheme/resetToDefault (line 246-259) are untouched.</criterion>
    <criterion>No new toast calls are added - fetch failures are silent per D-A4.</criterion>
  </acceptance_criteria>
  <done>ThemeContext dual-writes preferences to localStorage (existing) and debounced server (new); login triggers GET with 404 to upload fallback; logout wipes per-user fc_* keys and resets state, preserving fc_theme + fc_last_season.</done>
</task>

<task type="auto">
  <name>Task 3 Header auth guard hide ThemeToggle plus Palette when unauthenticated</name>
  <files>frontend/src/components/composites/Header.jsx</files>
  <read_first>frontend/src/components/composites/Header.jsx:51-54 Header signature plus useAuth destructuring - user is already extracted; frontend/src/components/composites/Header.jsx:97-110 the two IconButtons to wrap; frontend/src/components/composites/Header.jsx:111-167 existing user guard avatar block - mirror its JSX shape; frontend/src/contexts/AuthContext.jsx:46-56 value shape - user is null when not authenticated</read_first>
  <action>Modify frontend/src/components/composites/Header.jsx. No new imports or state - user is already destructured from useAuth() at line 54.

A) Wrap the existing light/dark ThemeToggle IconButton (lines 97-103) in a user guard: open with the literal text "{user && (" before the IconButton JSX and close with ")}" after the IconButton closing tag. Indentation inside the block stays as it was. Preserve all props (icon, ariaLabel, onClick, className="md-header__theme-toggle").

B) Wrap the existing Palette IconButton (lines 105-110) in a user guard identically: open with "{user && (" before the IconButton JSX and close with ")}" after. Preserve all props (icon="palette", ariaLabel="选择主题", onClick={() => navigate('/theme')}, className="md-header__theme-page").

C) Leave the existing "{user && (" avatar dropdown block (line 111-167) unchanged - it is already correctly guarded.

D) Leave all other parts of Header.jsx untouched: logo (80-90), title (94), __right div wrapper (97), click-outside useEffect (64-73), menu rendering (122-166), header-action-bar (171-175), PAGE_TITLES (28-49).

E) Architectural note for downstream: Header is rendered only inside Route element=PcLayout, and every PcLayout route is wrapped in ProtectedRoute requiredRoles={['user','chef','admin']} (App.jsx:296-318). The /guest/:token route uses GuestOrderPage directly outside PcLayout so Header does not render for guests. The /login route uses LoginPage directly. The user guard wraps are defense-in-depth per D-A2 - they cover the brief loading window during AuthProvider's initial authManager.getUser() check (AuthContext.jsx:17-25). The /theme/settings route is already protected by App.jsx:311-318 so no route-level change is needed.</action>
  <verify>
    <automated>cd /home/temila/family_chef/frontend && grep -nB1 -A3 'md-header__theme-toggle|md-header__theme-page' src/components/composites/Header.jsx</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Header.jsx contains the literal text "{user && (" before each of the two IconButtons (light/dark and palette).</criterion>
    <criterion>The existing avatar user guard block at line 111 is unchanged.</criterion>
    <criterion>No new imports, no new state, no new files.</criterion>
    <criterion>PAGE_TITLES dict (28-49) is unchanged.</criterion>
    <criterion>handleToggleTheme function (59-61) is unchanged.</criterion>
  </acceptance_criteria>
  <done>Header light/dark ThemeToggle and Palette IconButton are conditionally rendered only when user is logged in; D-A2 and D-A3 enforced; the existing avatar dropdown guard at line 111 remains unchanged.</done>
</task>

</tasks>

<verification>
  - `cd frontend && npm run lint --quiet` exits 0 (no new lint errors)
  - `cd frontend && npm run build` exits 0 (no Vite build errors)
  - grep "getThemePreferences\|updateThemePreferences\|err.status" frontend/src/api/client.js returns matches
  - grep "refreshThemePreferences\|preferencesLoadedRef\|skipNextPutRef\|debouncedPutTimerRef" frontend/src/theme/theme-context.jsx returns matches
  - grep "user &&" frontend/src/components/composites/Header.jsx returns at least 3 matches (avatar dropdown already had 2; plus 2 new wraps for ThemeToggle and Palette, totaling 4 user guards)
  - Manual end-to-end: login as testuser → existing fc_* keys uploaded to /api/users/me/theme-preferences (GET 200, PUT 200); logout → fc_active_theme, fc_season_enabled, fc_hemisphere, fc_season_theme_map removedItem; fc_theme and fc_last_season preserved
  - Header renders no ThemeToggle/Palette for unauthenticated user
</verification>

<success_criteria>
  - Plan 02 complete when all 6 acceptance checks above pass
  - D-A1: setters dual-write to localStorage AND debounced-PUT 200ms; server LWW semantics
  - D-A2: Header ThemeToggle and Palette buttons hidden when !user
  - D-A3: no anonymous preference write path; only logged-in users have anything in localStorage for the 4 fc_* keys beyond FOUC bootstrap
  - D-A4: login triggers silent GET, brief inconsistency accepted, fetch failure silent
  - D-A5: first-login 404 triggers upload from localStorage as initial payload
  - D-A6: logout removes 4 fc_* keys, resets context state, preserves fc_theme and fc_last_season
  - Rendering pipeline byte-identical for any given fc_* localStorage state
  - No new toast calls; fetch failures are silent per agent discretion
  - PUT debounce 200ms per agent discretion
</success_criteria>

<output>
Create .planning/phases/19-account-bound-theme-preferences/19-02-SUMMARY.md when done
</output>