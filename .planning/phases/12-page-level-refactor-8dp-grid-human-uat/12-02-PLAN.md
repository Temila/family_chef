---
phase: 12-page-level-refactor-8dp-grid-human-uat
plan: 02
type: execute
wave: 3
depends_on:
  - 12-01
files_modified:
  - frontend/src/css/styles.css
  - frontend/src/pages/AdminDishesPage.jsx
  - frontend/src/pages/AdminCategoriesPage.jsx
  - frontend/src/pages/AdminUsersPage.jsx
  - frontend/src/pages/AdminIngredientsPage.jsx
  - frontend/src/pages/ChefDishesPage.jsx
  - frontend/src/pages/OrderPage.jsx
  - frontend/scripts/audit-md3-compliance.mjs
  - frontend/package.json
  - .planning/phases/12-page-level-refactor-8dp-grid-human-uat/12-UAT-REPORT.md
autonomous: false
requirements:
  - UX-01
  - UX-02
  - TOKEN-13
  - LOGIC-01
  - LOGIC-02
  - LOGIC-03
must_haves:
  truths:
    - "D-AUDIT-01: Forbidden Phase 10/11 legacy classes have zero JSX consumers and zero CSS definitions, with only explicitly documented semantic exceptions."
    - "D-UAT-02: Browser automation validates native Ripple clicks, one Header, sampled 4dp alignment, and existing touch targets."
    - "D-UAT-01: All six business flows complete in a real browser without business-logic regression."
    - "D-UAT-03: Lint baseline, stylelint, MD3 grep, build, Playwright, DOM inspection, and browser Console acceptance evidence are recorded."
    - "D-UAT-04: The UAT report records screenshots, issues, fixes, and PASS/FAIL for every flow."
    - "LOGIC-02: Backend remains unchanged."
  artifacts:
    - path: "frontend/scripts/audit-md3-compliance.mjs"
      provides: "Browser-level Ripple/Header/grid compliance audit"
    - path: ".planning/phases/12-page-level-refactor-8dp-grid-human-uat/12-UAT-REPORT.md"
      provides: "Final six-flow human acceptance evidence"
      contains: "Flow 6"
  key_links:
    - from: "frontend/package.json"
      to: "frontend/scripts/audit-md3-compliance.mjs"
      via: "audit:md3 npm script"
      pattern: "audit:md3"
    - from: "frontend/scripts/audit-md3-compliance.mjs"
      to: "frontend/md3-compliance-results.json"
      via: "JSON report output"
      pattern: "md3-compliance-results.json"
    - from: ".planning/phases/12-page-level-refactor-8dp-grid-human-uat/12-UAT-REPORT.md"
      to: "six browser flows"
      via: "per-flow screenshot and PASS/FAIL records"
      pattern: "Flow [1-6]"
---

<objective>
Prove Phase 12 complete through a zero-residue component audit, browser-level MD3 regression automation, the full quality-gate stack, and a human-approved six-flow UAT report.

Purpose: Close UX-01/02, TOKEN-13, and LOGIC-01..03 with source, runtime, and human evidence per D-AUDIT-01, D-UAT-01..04, and locked sequencing D-PLAN-01.
Output: Clean legacy-class surface, `audit-md3-compliance.mjs`, compliance JSON, and final `12-UAT-REPORT.md`.
</objective>

<execution_context>
@/home/temila/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/temila/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-page-level-refactor-8dp-grid-human-uat/12-CONTEXT.md
@.planning/phases/12-page-level-refactor-8dp-grid-human-uat/12-RESEARCH.md
@.planning/phases/12-page-level-refactor-8dp-grid-human-uat/12-UI-SPEC.md
@.planning/phases/12-page-level-refactor-8dp-grid-human-uat/12-PATTERNS.md
@.planning/phases/12-page-level-refactor-8dp-grid-human-uat/12-00-BUGFIX-SUMMARY.md
@.planning/phases/12-page-level-refactor-8dp-grid-human-uat/12-01-SUMMARY.md
@.planning/phases/09-motion-state-layers/09-02-SUMMARY.md
@.planning/phases/11-composite-navigation-components/11-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete the two-sided legacy class audit</name>
  <files>frontend/src/css/styles.css, frontend/src/pages/AdminDishesPage.jsx, frontend/src/pages/AdminCategoriesPage.jsx, frontend/src/pages/AdminUsersPage.jsx, frontend/src/pages/AdminIngredientsPage.jsx, frontend/src/pages/ChefDishesPage.jsx, frontend/src/pages/OrderPage.jsx</files>
  <read_first>
    - `.planning/phases/12-page-level-refactor-8dp-grid-human-uat/12-CONTEXT.md` — D-AUDIT-01 and D-PLAN-01
    - `.planning/phases/12-page-level-refactor-8dp-grid-human-uat/12-RESEARCH.md` — §7 definitive residue inventory and intentional exceptions
    - `.planning/phases/12-page-level-refactor-8dp-grid-human-uat/12-UI-SPEC.md` — Component Adoption Contract and Zero-Residue Classes
    - `.planning/phases/12-page-level-refactor-8dp-grid-human-uat/12-PATTERNS.md` — Cross-Cutting Constraint Notes 4
    - `frontend/src/css/styles.css` — legacy selector definitions, `.form-input`, `.fab`, `.btn-search`
    - `frontend/src/pages/AdminDishesPage.jsx` — select-only `.form-input` sites
    - `frontend/src/pages/ChefDishesPage.jsx` — select-only `.form-input` sites
    - `frontend/src/pages/OrderPage.jsx` — select-only `.form-input` sites
    - `frontend/src/pages/AdminIngredientsPage.jsx` — select-only `.form-input` sites
    - `frontend/src/pages/AdminCategoriesPage.jsx` — select-only `.form-input` site
    - `frontend/src/pages/AdminUsersPage.jsx` — select-only `.form-input` sites
    - `frontend/src/pages/UserWishesPage.jsx` — intentional `.fab` placement use
    - `frontend/src/components/primitives/FAB.jsx` — visual primitive proving `.fab` is placement-only
  </read_first>
  <action>Run D-AUDIT-01 as a two-sided audit over JSX className consumers and CSS selector definitions. The forbidden set is `btn-primary|btn-secondary|btn-outline|btn-icon|btn-sm|btn-lg|card` root|`dish-card*|wish-card*|guest-dish-card*|badge-(warn|danger|success|info|accent|gold|muted|count)|filter-chip|modal-(overlay|content|header|body|footer|close)|pc-sidebar*|bottom-bar|tab-(item|icon|label)|list-item*|toast*|slideDown`. Fix every true forbidden hit by consuming the existing Phase 10/11 primitive/composite and deleting its obsolete definition; preserve original props, callbacks, roles, API calls, and state. Treat `.btn-search` as an allowed utility only after Wave 2 tokenized its padding. Treat `.fab` as allowed only on the existing `<FAB ... className="fab">` placement site and ensure its CSS contains only position/bottom/right/z-index placement. For `.form-input`, follow the approved UI-SPEC deviation: it may remain only on native `<select>` elements because Select was not a v1.2 primitive; verify every occurrence's tag is `select`, its CSS is tokenized, and record exact occurrence count/files for UAT. Any `.form-input` on input/div/button is a failure and must migrate to Input or `.field-trigger`. Do not build a new Select primitive and do not report the documented select/fab exceptions as zero-residue violations. Save the exact audit commands/counts for Task 3's report.</action>
  <acceptance_criteria>
    - Forbidden JSX class audit returns 0 after excluding the exact `.form-input` native-select and `.fab` placement exceptions.
    - Forbidden CSS selector audit returns 0 after excluding only `.form-input`, `.fab`, and `.btn-search` documented semantic utilities.
    - Every `.form-input` className occurrence is on a native `<select>`; no input/div/button consumes it.
    - `.form-input` CSS uses MD3 spacing/radius/motion/color tokens and contains no numeric corner.
    - `.fab` has exactly one JSX consumer on the FAB primitive and its CSS declaration is placement-only.
    - `.btn-search` remains only on compact search actions and uses `var(--md-spacing-1) var(--md-spacing-2)`.
    - `npm run check:md3` and `npm run build` exit 0 after fixes.
  </acceptance_criteria>
  <verify>
    <automated>npm run check:md3 &amp;&amp; npm run build</automated>
  </verify>
  <done>All forbidden legacy visual classes are absent on both source sides; only the explicitly approved native-select, FAB-placement, and search utilities remain and are token-compliant.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add browser-level MD3 compliance automation and run final gates</name>
  <files>frontend/scripts/audit-md3-compliance.mjs, frontend/package.json</files>
  <read_first>
    - `.planning/phases/12-page-level-refactor-8dp-grid-human-uat/12-CONTEXT.md` — D-UAT-02, D-UAT-03, D-PLAN-01
    - `.planning/phases/12-page-level-refactor-8dp-grid-human-uat/12-RESEARCH.md` — §11 exact three-family audit design and page inventory
    - `.planning/phases/12-page-level-refactor-8dp-grid-human-uat/12-UI-SPEC.md` — Touch Target and Playwright Compliance contracts
    - `.planning/phases/12-page-level-refactor-8dp-grid-human-uat/12-PATTERNS.md` — Pattern 14 exact JWT/report structure
    - `frontend/scripts/audit-touch-targets.mjs` — existing JWT injection, routes, mobile viewport, JSON output
    - `frontend/tests/phase12-bugfix.spec.js` — Wave 1 real mouse regression selectors and assertions
    - `frontend/playwright.config.js` — browser test config
    - `frontend/package.json` — scripts and final gate commands
    - `frontend/src/App.jsx` — routes/Header shell
    - `frontend/src/components/primitives/Ripple.jsx` — self/wrap modes
    - `frontend/src/css/tokens.css` — spacing and nav-height values
  </read_first>
  <behavior>
    - A real Playwright `page.click` on a representative Button/IconButton/FAB or deterministic fixture changes application state; DOM `.click()` alone is insufficient.
    - Every audited authenticated page with a page Header has exactly one `<header>`; public Login/Guest routes use explicit expected header counts rather than a blanket false failure.
    - Computed padding/margin/gap samples are 0 or multiples of 4, with explicit exclusions for borders, focus offsets, spinner borders, transforms, and nav-height context.
    - Existing touch-target audit remains runnable and reports no new Phase 12 regressions.
    - Missing auth/guest test data is reported SKIPPED and causes the final authenticated UAT gate to remain incomplete, not a false PASS.
  </behavior>
  <action>Create `frontend/scripts/audit-md3-compliance.mjs` by reusing `audit-touch-targets.mjs`'s `AUDIT_BASE_URL`, `--token`/`FC_TEST_TOKEN`, localStorage injection, chromium lifecycle, route inventory, and JSON writing. Do not hardcode credentials or invitation tokens. Add three required test families per D-UAT-02: (1) native Ripple click regression using Playwright pointer input and state/navigation/dismiss assertions, reusing deterministic selectors from `phase12-bugfix.spec.js`; (2) Header count expectations for each applicable route, asserting authenticated page routes equal 1 and explicitly defining public routes that legitimately differ; (3) deterministic 4dp grid sampling of at least 10 visible representative layout/interactive elements per audited page, parsing each side of padding/margin plus rowGap/columnGap and rejecting nonzero values not divisible by 4, while excluding border/outline/spinner properties rather than excluding arbitrary elements. Include existing touch-target measurements or invoke/aggregate `audit-touch-targets.mjs`, and extend guest coverage only when a real token is supplied through `FC_GUEST_TOKEN`/CLI; no known token in source. Output `frontend/md3-compliance-results.json` with auditDate, route statuses, ripple/header/grid/touch violations, skips, and overall `PASS|FAIL|INCOMPLETE`; exit nonzero for violations, and treat missing credentials as INCOMPLETE for final acceptance. Add `audit:md3` to package.json as `node scripts/audit-md3-compliance.mjs`. Run the complete D-UAT-03 automated stack: capture ESLint baseline count and prove no increase from STATE's known ≥90 baseline rather than falsely requiring zero; require `lint:css`, `check:md3`, build, existing Playwright suite, Phase 12 bugfix spec, touch audit, and md3 audit to pass with authenticated credentials when available.</action>
  <acceptance_criteria>
    - `frontend/scripts/audit-md3-compliance.mjs` exists and accepts `AUDIT_BASE_URL`, `--token`/`FC_TEST_TOKEN`, and optional `FC_GUEST_TOKEN` without source-embedded credentials.
    - The script contains distinct Ripple, Header, grid, and touch result families and writes `frontend/md3-compliance-results.json`.
    - Ripple verification uses Playwright mouse/pointer action plus observable state change, not only `HTMLElement.click()` or event-listener injection.
    - Header expectations distinguish authenticated page routes from public routes and assert 1 where D-BUG-02 applies.
    - Grid parsing checks individual computed padding/margin sides and row/column gaps for at least 10 visible elements per audited route.
    - Missing required auth data yields `INCOMPLETE`/SKIPPED evidence rather than PASS; any actual violation exits nonzero.
    - `frontend/package.json` contains `audit:md3`.
    - Stylelint, check:md3, build, existing Playwright, Phase 12 bugfix, touch audit, and authenticated md3 audit outputs are captured for the UAT report.
  </acceptance_criteria>
  <verify>
    <automated>npm run lint:css &amp;&amp; npm run check:md3 &amp;&amp; npm run build &amp;&amp; npm exec playwright test -- --reporter=line &amp;&amp; npm run audit:md3</automated>
  </verify>
  <done>Automated compliance evidence covers the two regressions, 4dp grid, touch targets, source gates, and production build, with honest credential-dependent status.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Complete six-flow HUMAN-UAT and approve the final report</name>
  <files>.planning/phases/12-page-level-refactor-8dp-grid-human-uat/12-UAT-REPORT.md</files>
  <read_first>
    - `.planning/phases/12-page-level-refactor-8dp-grid-human-uat/12-CONTEXT.md` — D-UAT-01, D-UAT-02, D-UAT-03, D-UAT-04, D-PLAN-01
    - `.planning/phases/12-page-level-refactor-8dp-grid-human-uat/12-UI-SPEC.md` — HUMAN-UAT six flow touchpoints and six visual dimensions
    - `.planning/phases/12-page-level-refactor-8dp-grid-human-uat/12-RESEARCH.md` — §11 E2E coverage map
    - `frontend/md3-compliance-results.json` — Task 2 automation evidence
    - `frontend/touch-audit-results.json` — touch-target evidence
    - `frontend/src/pages/LoginPage.jsx` — Flow 1 source anchor
    - `frontend/src/pages/AdminDishesPage.jsx` — Flow 2 source anchor
    - `frontend/src/pages/ChefDishesPage.jsx` — Flow 2 chef publish anchor
    - `frontend/src/pages/OrderPage.jsx` — Flow 3 source anchor
    - `frontend/src/pages/OrderDetailPage.jsx` — Flow 3 status anchor
    - `frontend/src/pages/UserWishesPage.jsx` — Flow 4 source anchor
    - `frontend/src/pages/ChefWishesPage.jsx` — Flow 4 lifecycle anchor
    - `frontend/src/components/InvitationsSection.jsx` — Flow 5 invite anchor
    - `frontend/src/pages/GuestOrderPage.jsx` — Flow 5 mobile guest anchor
    - `frontend/src/pages/PreferencesPage.jsx` — Flow 6 source anchor
  </read_first>
  <action>Prepare `.planning/phases/12-page-level-refactor-8dp-grid-human-uat/12-UAT-REPORT.md` per D-UAT-04 before pausing. Include date, branch `feature/ui-rebuild`, commit, tester, environment, viewport, automated command/exit-code table, md3/touch JSON summaries, exact D-AUDIT-01 counts, approved deviations (`.form-input` native selects, `.fab` placement, `.btn-search` utility), and six flow sections. Each flow must contain numbered steps, expected functional result, the six visual dimensions (motion/state-layer/focus ring/touch/typography/spacing), Header count, Ripple mouse click result, Console warning/error count, screenshot path(s), issue list with fix status, and explicit `PASS|FAIL|BLOCKED`. Do not pre-mark human observations PASS. Then instruct the user to run all six real flows in Chrome DevTools: F1 register/login and force-password when applicable; F2 admin create/edit/delete plus chef publish/unpublish; F3 user order creation/chef split/detail/status update; F4 wish submit/claim/advance/reject/cancel; F5 invite creation/mobile guest browse/order/confirmation and one-time token behavior; F6 add/remove preferences. Require screenshots for every flow and Console 0 warning/error. Update the report from the user's observations; any FAIL requires implementation repair and rerun before final approval. This is the final acceptance gate; no backend modification is allowed.</action>
  <acceptance_criteria>
    - `12-UAT-REPORT.md` exists at the exact D-UAT-04 path and identifies `feature/ui-rebuild` plus tested commit/environment.
    - The report contains six distinct flow sections matching D-UAT-01 and each has steps, screenshot paths, visual issues, functional issues, fix status, Console count, and `PASS|FAIL|BLOCKED`.
    - Flow 5 records a mobile viewport ≤420px and one-time invitation-token behavior.
    - The report records Ripple mouse clicks, keyboard focus, Header DOM counts, grid/radius/Icon checks, and all six MD3 visual dimensions.
    - Automated gate table records ESLint baseline/no-increase, lint:css, check:md3, build, Playwright, touch audit, and MD3 audit evidence.
    - Approved `.form-input`/`.fab`/`.btn-search` deviations and exact counts are documented rather than hidden.
    - Final report status is PASS only when all six flow statuses are PASS, Console warning/error count is 0, and automated gates contain no FAIL/INCOMPLETE.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/12-page-level-refactor-8dp-grid-human-uat/12-UAT-REPORT.md &amp;&amp; test "$(rg -c '^## Flow [1-6]' .planning/phases/12-page-level-refactor-8dp-grid-human-uat/12-UAT-REPORT.md)" = "6" &amp;&amp; test "$(rg -c 'Status: PASS' .planning/phases/12-page-level-refactor-8dp-grid-human-uat/12-UAT-REPORT.md)" -ge "6"</automated>
    <human-check>In Chrome DevTools, complete the six listed flows using desktop and the ≤420px guest viewport; attach screenshots, confirm Console has zero warnings/errors, confirm one Header on authenticated pages, native mouse Ripple clicks, visible keyboard focus, and report every issue. Type `approved` only after the report shows six PASS statuses and all automated gates are green.</human-check>
  </verify>
  <done>The user has personally approved all six browser flows, screenshots and issues are recorded, all four acceptance layers are green, and the report's final status is PASS.</done>
  <what-built>Phase 12 token/component migration, regression fixes, source gates, and browser compliance automation are complete and ready for final HUMAN-UAT.</what-built>
  <how-to-verify>
    1. Open the running app in Chrome with DevTools Console and Network panels visible.
    2. Follow Flow 1 through Flow 6 exactly as written in `12-UAT-REPORT.md`.
    3. For each flow, capture the required screenshots and record functional/visual issues plus Console warning/error count.
    4. On authenticated routes, run `document.querySelectorAll('header').length` and confirm `1`.
    5. Click representative Button/IconButton/FAB controls with the mouse and confirm actions fire once with visible Ripple; Tab through controls and confirm focus rings/Enter activation.
    6. Use a ≤420px viewport for the guest flow and complete invitation creation, guest ordering, confirmation, and reuse rejection.
    7. Mark a flow PASS only if its business outcome and all six MD3 dimensions pass; otherwise describe the issue for repair.
  </how-to-verify>
  <resume-signal>Type `approved` when all 6 flows and the final report are PASS, or provide the failed flow/step and screenshot details.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source audit regex → acceptance decision | Incomplete or over-broad filters could conceal legacy consumers. |
| Playwright credentials → authenticated browser context | JWT/guest tokens grant test-session access and must not enter source or reports. |
| Automated checks → human release decision | Automated DOM/style samples cannot substitute for full business-flow observation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02-01 | Tampering | Legacy class grep | mitigate | Audit JSX and CSS independently; allow only exact semantic exceptions with tag/content assertions and report counts. |
| T-12-02-02 | Information Disclosure | Playwright JWT/guest tokens | mitigate | Accept only CLI/env credentials, never write tokens to source, JSON, screenshots, or UAT report. |
| T-12-02-03 | Repudiation | HUMAN-UAT report | mitigate | Record commit, environment, command exits, screenshots, per-flow status, issues, fixes, and human approval signal. |
| T-12-02-04 | Denial of Service | Browser interaction regressions | mitigate | Real pointer Ripple tests, touch audit, six end-to-end flows, and Console-zero gate. |
| T-12-02-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no new package; it reuses committed Playwright/stylelint dependencies. |
</threat_model>

<verification>
- D-AUDIT-01 forbidden consumer and selector scans return zero after exact documented exceptions.
- From `frontend/`: `npm run lint:css`, `npm run check:md3`, and `npm run build` exit 0.
- Existing and Phase 12 Playwright suites pass; authenticated `npm run audit:md3` reports PASS, not INCOMPLETE.
- Touch audit reports no new Phase 12 violations.
- `12-UAT-REPORT.md` contains six PASS flows, screenshots, Console 0, automation evidence, deviations, and final human approval.
- `git diff --name-only -- backend` returns no paths.
</verification>

<success_criteria>
- Zero forbidden old-class consumers/definitions, with only explicit token-compliant select/placement/search exceptions documented.
- Ripple, one-Header, 4dp grid, and touch-target browser checks pass on applicable routes.
- All automated quality gates pass without concealing the pre-existing ESLint baseline.
- The user completes and approves all six real business flows with zero Console warnings/errors.
- Final UAT report contains complete evidence and overall PASS; backend and business logic are unchanged.
</success_criteria>

<output>
Create `.planning/phases/12-page-level-refactor-8dp-grid-human-uat/12-02-SUMMARY.md` when done.
</output>
