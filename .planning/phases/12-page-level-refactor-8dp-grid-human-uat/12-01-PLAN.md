---
phase: 12-page-level-refactor-8dp-grid-human-uat
plan: 01
type: execute
wave: 2
depends_on:
  - 12-00-BUGFIX
files_modified:
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/.stylelintrc.json
  - frontend/scripts/check-m3-tokens.sh
  - frontend/src/css/styles.css
  - frontend/src/components/composites/Sidebar.css
  - frontend/src/components/composites/BottomBar.css
  - frontend/src/components/composites/Header.css
  - frontend/src/components/composites/ListItem.css
  - frontend/src/components/composites/Modal.css
  - frontend/src/components/primitives/Button.css
  - frontend/src/components/primitives/FAB.css
  - frontend/src/components/primitives/Ripple.jsx
  - frontend/src/components/primitives/Icon.jsx
  - frontend/src/components/EmptyState.jsx
  - frontend/src/utils/index.js
  - frontend/src/contexts/ToastContext.jsx
  - frontend/src/pages/AdminDishesPage.jsx
  - frontend/src/pages/AdminCategoriesPage.jsx
  - frontend/src/pages/AdminChefsPage.jsx
  - frontend/src/pages/AdminHomePage.jsx
  - frontend/src/pages/AdminIngredientsPage.jsx
  - frontend/src/pages/AdminStatsPage.jsx
  - frontend/src/pages/ChefDishesPage.jsx
  - frontend/src/pages/ChefOrdersPage.jsx
  - frontend/src/pages/DishDetailPage.jsx
  - frontend/src/pages/ForceChangePasswordPage.jsx
  - frontend/src/pages/GuestOrderPage.jsx
  - frontend/src/pages/LoginPage.jsx
  - frontend/src/pages/OrderDetailPage.jsx
  - frontend/src/pages/OrderPage.jsx
  - frontend/src/pages/PreferencesPage.jsx
  - frontend/src/pages/UserFavoritesPage.jsx
  - frontend/src/pages/UserHomePage.jsx
  - frontend/src/pages/UserOrdersPage.jsx
  - frontend/src/pages/UserProfilePage.jsx
  - frontend/src/pages/UserWishesPage.jsx
  - frontend/src/components/ChefSelectModal.jsx
  - frontend/src/components/DishCard.jsx
  - frontend/src/components/GuestDishCard.jsx
  - frontend/src/components/InvitationsModal.jsx
  - frontend/src/components/InvitationsSection.jsx
  - frontend/src/components/PasswordInput.jsx
  - frontend/src/components/WishAdvanceModal.jsx
  - frontend/src/components/WishCard.jsx
autonomous: true
requirements:
  - UX-01
  - UX-02
  - TOKEN-13
  - LOGIC-01
  - LOGIC-02
  - LOGIC-03
must_haves:
  truths:
    - "D-GRID-01..03: Page and component spacing is expressed through the declared --md-spacing-1..8 scale, with documented non-spacing exceptions only."
    - "D-RADIUS-01/D-FILE-01: Hard-coded numeric border-radius declarations are absent and stylelint blocks their return."
    - "D-EMOJI-01: Page/component pictographic emoji are represented by the Icon registry; structural glyphs remain text."
    - "D-MOTION-01: Design motion consumers use --md-motion duration/easing tokens, while documented spinner/reduced-motion/stagger exceptions remain intentional."
    - "D-SNACK-01: showToast supports the object action form and every legacy string call remains compatible."
    - "D-FILE-02: npm run check:md3 deterministically rejects spacing, radius, motion, and emoji regressions."
    - "LOGIC-01..03: Production build passes with unchanged API requests, state machines, auth, and backend."
  artifacts:
    - path: "frontend/.stylelintrc.json"
      provides: "Numeric border-radius policy"
      contains: "declaration-property-value-allowed-list"
    - path: "frontend/scripts/check-m3-tokens.sh"
      provides: "MD3 grep regression gate"
    - path: "frontend/src/components/primitives/Icon.jsx"
      provides: "Expanded Material Symbols registry"
      contains: "new-label"
    - path: "frontend/src/contexts/ToastContext.jsx"
      provides: "Backward-compatible actionable Snackbar API"
      contains: "action"
  key_links:
    - from: "frontend/package.json"
      to: "frontend/.stylelintrc.json"
      via: "lint:css script"
      pattern: "lint:css"
    - from: "frontend/package.json"
      to: "frontend/scripts/check-m3-tokens.sh"
      via: "check:md3 script"
      pattern: "check:md3"
    - from: "frontend/src/components/EmptyState.jsx"
      to: "frontend/src/components/primitives/Icon.jsx"
      via: "string icon-name rendering"
      pattern: "Icon name=\{icon\}"
    - from: "frontend/src/contexts/ToastContext.jsx"
      to: "legacy showToast callers"
      via: "string-or-options overload"
      pattern: "typeof options === 'string'"
---

<objective>
Converge the complete frontend consumption layer onto MD3 spacing, radius, motion, and Icon contracts; add long-lived stylelint/grep enforcement; and enable actionable Snackbars without regressing existing call sites.

Purpose: Satisfy the Phase 12 visual/token gates before the final old-class and HUMAN-UAT audit, per D-GRID-01..03, D-RADIUS-01, D-EMOJI-01, D-MOTION-01, D-SNACK-01, D-FILE-01..02, and D-PLAN-01.
Output: Tokenized frontend sources, Icon/EmptyState/Snackbar API updates, package/config changes, and a deterministic `check:md3` gate.
</objective>

<execution_context>
@/home/temila/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/temila/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-page-level-refactor-8dp-grid-human-uat/12-CONTEXT.md
@.planning/phases/12-page-level-refactor-8dp-grid-human-uat/12-RESEARCH.md
@.planning/phases/12-page-level-refactor-8dp-grid-human-uat/12-UI-SPEC.md
@.planning/phases/12-page-level-refactor-8dp-grid-human-uat/12-PATTERNS.md
@.planning/phases/12-page-level-refactor-8dp-grid-human-uat/12-00-BUGFIX-SUMMARY.md
@.planning/phases/08-md3-design-token-foundation/08-CONTEXT.md
@.planning/phases/09-motion-state-layers/09-CONTEXT.md
@.planning/phases/10-primitive-components/10-CONTEXT.md
@.planning/phases/11-composite-navigation-components/11-CONTEXT.md
@.planning/phases/11-composite-navigation-components/11-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install and configure MD3 radius enforcement</name>
  <files>frontend/package.json, frontend/package-lock.json, frontend/.stylelintrc.json, frontend/src/components/composites/Sidebar.css, frontend/src/components/composites/BottomBar.css</files>
  <read_first>
    - `.planning/phases/12-page-level-refactor-8dp-grid-human-uat/12-CONTEXT.md` — D-RADIUS-01, D-FILE-01, D-PLAN-01
    - `.planning/phases/12-page-level-refactor-8dp-grid-human-uat/12-RESEARCH.md` — §4 and §8 exact residues/config
    - `.planning/phases/12-page-level-refactor-8dp-grid-human-uat/12-UI-SPEC.md` — Shape Contract and stylelint gate
    - `frontend/package.json` — current scripts and dependency groups
    - `frontend/package-lock.json` — lockfile format
    - `frontend/src/css/tokens.css` — allowed radius token names/values
    - `frontend/src/components/composites/Sidebar.css` — hard-coded 16px active pill
    - `frontend/src/components/composites/BottomBar.css` — hard-coded 16px active pill
    - `frontend/src/pages/LoginPage.jsx` — verify reported input residue against current Input use
    - `frontend/src/components/primitives/Input.css` — source of Login input radius
  </read_first>
  <action>Implement D-RADIUS-01 and D-FILE-01. Before installation, verify the exact current registry versions with `npm view stylelint version` and `npm view stylelint-config-standard version`; use the compatible stylelint 16/stylelint-config-standard 36 line from RESEARCH and update both package.json and package-lock.json through `npm install --save-dev`, not manual lockfile editing. Create `frontend/.stylelintrc.json` extending `stylelint-config-standard`; disable `selector-class-pattern` and `custom-property-pattern`; configure `declaration-property-value-allowed-list` so `border-radius` and directional corner-radius properties allow only `var(--md-radius-...)`, `0`, `50%`, `9999px`, `inherit`, or `unset`. Scope `ignoreFiles` only to node_modules/dist; do not exempt production CSS. Add scripts `lint:css` as `stylelint "src/**/*.css"`, retain `check:tokens`, and leave `check:md3`/`check:all` for Task 3. Replace the two measured active-pill declarations in Sidebar.css and BottomBar.css from `16px` to `var(--md-radius-md)`. Verify LoginPage already consumes Input and Input.css uses `var(--md-radius-sm)`; do not fabricate a LoginPage change if the reported stale 4px residue is absent. Do not expand stylelint to width/padding/font-size because that is deferred.</action>
  <acceptance_criteria>
    - `frontend/package.json` and lockfile include compatible `stylelint` 16.x and `stylelint-config-standard` 36.x devDependencies based on registry output.
    - `frontend/package.json` contains exact script key `lint:css` targeting `src/**/*.css` from the frontend working directory.
    - `frontend/.stylelintrc.json` exists, extends `stylelint-config-standard`, and restricts `border-radius` plus directional corner radii to token/0/50%/9999px/inherit/unset values.
    - `.stylelintrc.json` does not ignore Sidebar.css, BottomBar.css, Modal.css, Header.css, or any other production source file.
    - Sidebar.css and BottomBar.css active indicator pills use `var(--md-radius-md)` and contain no `border-radius: 16px`.
    - `npm run lint:css` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run lint:css</automated>
  </verify>
  <done>All current CSS passes a permanent numeric-radius rule, and both known 16px residues are tokenized without unnecessary LoginPage edits.</done>
</task>

<task type="auto">
  <name>Task 2: Complete the spacing, motion, Icon, EmptyState, and Snackbar consumption sweep</name>
  <files>frontend/src/css/styles.css, frontend/src/components/composites/Header.css, frontend/src/components/composites/ListItem.css, frontend/src/components/composites/Modal.css, frontend/src/components/primitives/Button.css, frontend/src/components/primitives/FAB.css, frontend/src/components/primitives/Ripple.jsx, frontend/src/components/primitives/Icon.jsx, frontend/src/components/EmptyState.jsx, frontend/src/utils/index.js, frontend/src/contexts/ToastContext.jsx, frontend/src/pages/*.jsx, frontend/src/components/ChefSelectModal.jsx, frontend/src/components/DishCard.jsx, frontend/src/components/GuestDishCard.jsx, frontend/src/components/InvitationsModal.jsx, frontend/src/components/InvitationsSection.jsx, frontend/src/components/PasswordInput.jsx, frontend/src/components/WishAdvanceModal.jsx, frontend/src/components/WishCard.jsx</files>
  <read_first>
    - `.planning/phases/12-page-level-refactor-8dp-grid-human-uat/12-CONTEXT.md` — D-GRID-01, D-GRID-02, D-EMOJI-01, D-MOTION-01, D-SNACK-01, D-PLAN-01
    - `.planning/phases/12-page-level-refactor-8dp-grid-human-uat/12-RESEARCH.md` — §3 file/count table and rounding map, §5 five motion targets, §6 106-emoji map, §10 exact showToast overload
    - `.planning/phases/12-page-level-refactor-8dp-grid-human-uat/12-UI-SPEC.md` — Spacing, Motion, Iconography, Snackbar and documented exceptions
    - `.planning/phases/12-page-level-refactor-8dp-grid-human-uat/12-PATTERNS.md` — Patterns 5-10, 15-16, and S1/S4/S5
    - `frontend/src/css/tokens.css` — exact spacing/radius/motion source of truth
    - `frontend/src/css/styles.css` — 123 spacing declarations and motion consumers
    - `frontend/src/components/composites/Header.css` — 11 spacing declarations
    - `frontend/src/components/composites/ListItem.css` — 10 spacing declarations
    - `frontend/src/components/composites/Modal.css` — 7 spacing declarations
    - `frontend/src/components/primitives/Button.css` — spinner and spacing declarations
    - `frontend/src/components/primitives/FAB.css` — spacing declarations
    - `frontend/src/components/primitives/Ripple.jsx` — self-mode result and motion values from Wave 1
    - `frontend/src/components/primitives/Icon.jsx` — registry imports and ICONS map
    - `frontend/src/components/EmptyState.jsx` — current emoji API
    - `frontend/src/utils/index.js` — emptyState default and theme/status utilities
    - `frontend/src/contexts/ToastContext.jsx` — current queue/timers/showToast/render contract
    - `frontend/src/pages/AdminDishesPage.jsx` — 8 spacing hits and emoji call sites
    - `frontend/src/pages/ChefDishesPage.jsx` — 8 spacing hits and emoji call sites
    - `frontend/src/pages/AdminIngredientsPage.jsx` — 7 spacing hits and emoji call sites
    - `frontend/src/pages/OrderPage.jsx` — 8 spacing hits and Snackbar action candidate
    - `frontend/src/pages/GuestOrderPage.jsx` — 5 spacing hits and 7 emoji clusters
    - `frontend/src/pages/UserHomePage.jsx` — menu-entry emoji/name rendering pattern
    - `frontend/src/pages/DishDetailPage.jsx` — 11 emoji clusters
    - `frontend/src/pages/AdminHomePage.jsx` — 9 emoji clusters
    - `frontend/src/pages/AdminStatsPage.jsx` — 9 emoji clusters
    - `frontend/src/pages/UserWishesPage.jsx` — EmptyState/FAB and actionable Snackbar candidate
    - `frontend/src/components/InvitationsSection.jsx` — actionable copy Snackbar candidate
  </read_first>
  <action>Perform the D-GRID-02 grep-driven manual sweep, not an automated rewrite. Enumerate every hard-coded padding/margin/gap/row-gap/column-gap in all frontend `*.css` and JSX inline styles, then replace actual spacing values with the exact D-GRID-01 map: 4→spacing-1, 6→spacing-1, 8/9/10→spacing-2, 12→spacing-3, 14/16/18→spacing-4, 20/24→spacing-5, 32/36→spacing-6, 40→spacing-7, 56/60→spacing-8. Convert multi-value declarations component-wise. Preserve 1px borders, 2/4px focus offsets, 3px spinner borders, and use `var(--md-nav-height)` or token arithmetic for the 80px navigation safe-area rather than treating it as ordinary spacing; search-icon inset 44px becomes an explicit token composition such as spacing-5 plus icon/gap, not a blind 24px truncation. Prioritize styles.css (123), Header.css (11), ListItem.css (10), Modal.css (7), AdminDishesPage (8), ChefDishesPage (8), OrderPage (8), AdminIngredientsPage (7), GuestOrderPage (5), then exhaust the remaining 28-file inventory. Preserve every callback, API call, state variable, condition, route, form field, and auth check per LOGIC-01/03.

Complete D-MOTION-01 at the five measured consumers: Ripple transform/fade already uses long/short plus emphasized/standard after Wave 1; Snackbar enter uses `var(--md-motion-duration-medium) var(--md-motion-easing-standard)`; styles.css fadeInUp uses `var(--md-motion-duration-long) var(--md-motion-easing-emphasized)`; both `spin 0.8s linear infinite` consumers become `spin var(--md-motion-duration-long) linear infinite` to keep the mechanically correct linear easing while tokenizing duration. Preserve stagger delays, 0.01ms reduced-motion sentinels, and Button.css 6s reduced-motion fallback with explanatory comments.

Implement D-EMOJI-01 across the measured 106 clusters/31 files. Extend Icon.jsx with verified package exports and stable keys for at least `inventory-2`, `mail`, `new-label`, `ramen-dining`, `circle`, `shopping-cart`, `mood-bad`, `set-meal`, `lock`, `bolt`, `send`, and `trending-up` when absent; keep existing 0.13.0 aliases. Apply the RESEARCH §6 mapping: dish/food clusters→set-meal or ramen-dining, 📋/📦→inventory-2, chef→chef, 📭→mail, vegetables/fruit→eco, cart→shopping-cart, warning/error/status/action clusters→their named icons. Change EmptyState to `icon = 'mail'`, render string values through `<Icon name={icon} size={48} />`, render ReactNode values unchanged, and update every caller plus `emptyState()` default to Icon-name strings. Replace inline emoji renderers with `<Icon>` and preserve `✕`, `▼`, `▲`, `›`, `·`, `•`, and `…` as structural text. Remove emoji-returning `theme.getThemeIcon()` only if it has no consumer; otherwise return Icon names, never emoji.

Implement D-SNACK-01 in ToastContext with exact overload `showToast(message, options = 'success')`, where a string is legacy tone and an object is `{ type = 'success', duration, action: { label, onClick } }`. Normalize unknown tones, use nullish/default duration rather than discarding valid explicit values, store optional action on each item, preserve MAX_VISIBLE queue and pause/resume cleanup, and render the action between message and close as a 48dp `button.md-snackbar__action.md-interactive` colored `--md-color-inverse-primary`; invoking it must await/call `onClick` and dismiss that item without disturbing other timers. Wire real action examples without inventing undo semantics: InvitationsSection's created invitation can expose `复制` using its existing copy helper/data; OrderPage can expose `查看详情` only when the created order id and existing navigate path are available. For UserWishesPage, add `撤销` only if createWish returns the new wish id and the existing cancelWish API semantically reverses the operation; otherwise exercise the API in Playwright and do not attach an invalid action. All existing string-form showToast callers remain unchanged and build-compatible.</action>
  <acceptance_criteria>
    - A repository scan finds no bare CSS/JSX padding, margin, or gap values except documented 1-4px non-spacing effects and nav-height token usage; all 242 candidates were manually classified.
    - `.btn-search` padding is exactly `var(--md-spacing-1) var(--md-spacing-2)` and `.pc-main` safe area uses `var(--md-nav-height)` or token arithmetic.
    - Sidebar.css and BottomBar.css radius values remain tokenized from Task 1.
    - Ripple, Snackbar enter, fadeInUp, global loading spinner, and Button spinner consume `--md-motion-duration-*`; spinner easing remains `linear`.
    - The only raw motion literals left are documented stagger delays, reduced-motion `0.01ms`, and the 6s reduced-motion fallback.
    - `Icon.jsx` maps `new-label`, `ramen-dining`, `circle`, `mail`, `inventory-2`, `shopping-cart`, `mood-bad`, and all names used by migrated callers without unknown-name warnings.
    - `EmptyState.jsx` defaults to `mail`, supports string icon names and ReactNode, and contains no emoji default; `emptyState()` defaults to `mail`.
    - Extended_Pictographic scan across pages/components returns no files, excluding only approved structural non-emoji glyphs.
    - `ToastContext.jsx` contains `showToast(message, options = 'success')`, a `typeof options === 'string'` branch, optional `action`, custom `duration`, `.md-snackbar__action`, and action-before-close rendering.
    - Existing showToast string-form call sites remain source-compatible; production build exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run build &amp;&amp; npm run lint:css</automated>
  </verify>
  <done>The complete frontend visual consumption layer uses MD3 spacing/radius/motion/Icon contracts and actionable Snackbars while preserving all business/auth/data behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Add the deterministic MD3 CI grep gate</name>
  <files>frontend/scripts/check-m3-tokens.sh, frontend/package.json</files>
  <read_first>
    - `.planning/phases/12-page-level-refactor-8dp-grid-human-uat/12-CONTEXT.md` — D-GRID-03, D-FILE-02, D-RADIUS-01, D-EMOJI-01, D-MOTION-01, D-PLAN-01
    - `.planning/phases/12-page-level-refactor-8dp-grid-human-uat/12-RESEARCH.md` — §9 checks #8-10 and source-script path notes
    - `.planning/phases/12-page-level-refactor-8dp-grid-human-uat/12-UI-SPEC.md` — Enforcement Layer and allowed exceptions
    - `scripts/check-tokens.sh` — exact Phase 8 checks #1-7 and path-independent helper pattern
    - `frontend/package.json` — scripts after Tasks 1-2
    - `frontend/src/css/tokens.css` — completeness checks
    - `frontend/src/css/styles.css` — cleaned source that must pass
    - `frontend/src/components/primitives/Ripple.jsx` — motion-token consumer
    - `frontend/src/components/EmptyState.jsx` — emoji-free contract
  </read_first>
  <action>Implement D-GRID-03 and D-FILE-02 by creating executable `frontend/scripts/check-m3-tokens.sh`. Port checks #1-7 and the `fail/check/TOTAL/FAILURES` exit-code behavior from root `scripts/check-tokens.sh`, but resolve `FRONTEND_DIR` from the new script's own parent so it works from project root or frontend cwd. Expand radius checks to every CSS and JSX file and allow only token/0/50%/9999px exceptions. Add Check #8 for hard-coded padding/margin/gap/row-gap/column-gap and inline JSX equivalents, filtering documented border/focus/spinner/nav-height exceptions by explicit property/context rather than broad numeric ignores. Add Check #9 for hard-coded transition/animation duration/easing while explicitly allowing commented/filtered stagger delays, 0.01ms reduced-motion, 6s reduced-motion fallback, and linear spinner easing. Add Check #10 using ripgrep PCRE2 Extended_Pictographic matching across pages and components, with no EmptyState exclusion after its migration; structural glyphs do not match Extended_Pictographic. Use output variables with `|| true` so zero matches do not terminate under strict mode, and avoid self-invalidating grep gates by scanning only source files, not prose/comments. Add `check:md3` as `bash scripts/check-m3-tokens.sh` and `check:all` as `npm run lint:css &amp;&amp; npm run check:md3 &amp;&amp; npm run build`; retain `check:tokens` as a compatibility alias unless all consumers are proven migrated. Mark the script executable. The new npm dependency was already legitimacy-audited by the researcher; if registry verification marks either package suspicious or inconsistent, stop before install rather than bypassing the gate.</action>
  <acceptance_criteria>
    - `frontend/scripts/check-m3-tokens.sh` exists, is executable, and runs successfully from both repository root and `frontend/`.
    - The script preserves checks #1-7 and adds labeled checks for all-source radius, spacing, motion, and emoji.
    - Radius scanning covers `src/css`, `src/components`, and `src/pages`, including JSX `borderRadius` strings/numbers.
    - Spacing scanning covers CSS declarations and JSX inline style literals and does not use a blanket ignore that permits arbitrary 1/2/3/4px padding or gap.
    - Emoji scan has no exclusion for EmptyState or primitives and returns zero migrated-source files.
    - Motion scan allows only the documented stagger/reduced-motion/spinner cases and rejects ordinary raw transition/animation durations.
    - `frontend/package.json` contains `check:md3` and `check:all`; `npm run check:md3` exits 0.
    - `npm run lint:css`, `npm run check:md3`, and `npm run build` all exit 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run check:all</automated>
  </verify>
  <done>Stylelint, deterministic source greps, and build form an executable MD3 regression gate for Wave 3 and future CI use.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source changes → production CSS/JS bundle | High-volume token and Icon edits can alter layout or event wiring if not mechanically bounded. |
| Developer contribution → lint/grep gates | New source can attempt to bypass or accidentally evade policy regexes. |
| Snackbar action callback → application behavior | UI feedback invokes an existing business callback supplied by a caller. |
| npm registry → lockfile | New stylelint packages cross a software-supply-chain boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01-01 | Tampering | Radius/spacing source | mitigate | stylelint plus all-source check:md3 rejects hard-coded regressions; manual D-GRID-02 classification avoids blind replacement. |
| T-12-01-02 | Denial of Service | Snackbar action/timers | mitigate | Preserve queue timer cleanup, dismiss only selected item, and retain legacy overload/build compatibility. |
| T-12-01-03 | Tampering | `check-m3-tokens.sh` | mitigate | Path-independent script, explicit scans/exceptions, deterministic nonzero exit, and no broad exclusion of production directories. |
| T-12-01-04 | Elevation of Privilege | Snackbar caller callback | accept | Action invokes only an existing caller-provided UI/API capability; this phase creates no new permission or auth path. |
| T-12-01-SC | Tampering | npm installs | mitigate | Use only stylelint/stylelint-config-standard entries audited in RESEARCH and verify exact registry versions before lockfile update; stop on suspicious mismatch. |
</threat_model>

<verification>
- From `frontend/`: `npm run lint:css` exits 0.
- From `frontend/`: `npm run check:md3` exits 0 with all checks passing.
- From `frontend/`: `npm run build` exits 0.
- `rg -n 'border-radius:\s*[0-9]+px' frontend/src --glob '*.css'` has no output except allowed 0/9999px declarations filtered by an explicit follow-up.
- Extended_Pictographic scan over `frontend/src/pages` and `frontend/src/components` returns zero source files.
- `git diff --name-only -- backend` returns no paths.
</verification>

<success_criteria>
- All page/component spacing candidates use the MD3 grid or documented non-spacing exceptions.
- Numeric corner residues are zero and stylelint prevents recurrence.
- All page/component pictographic emoji use Icon; EmptyState accepts string names or ReactNode.
- All design motion consumers use MD3 tokens with only documented mechanical/accessibility exceptions.
- Legacy and object-form showToast calls both build; actionable Snackbar UI is usable and timer-safe.
- `lint:css`, `check:md3`, and build are green, with no backend changes.
</success_criteria>

<output>
Create `.planning/phases/12-page-level-refactor-8dp-grid-human-uat/12-01-SUMMARY.md` when done.
</output>
