---
phase: 11-composite-navigation-components
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - frontend/src/components/composites/Sidebar.jsx
  - frontend/src/components/composites/Sidebar.css
  - frontend/src/components/composites/BottomBar.jsx
  - frontend/src/components/composites/BottomBar.css
  - frontend/src/components/composites/Header.jsx
  - frontend/src/components/composites/Header.css
  - frontend/src/components/Sidebar.jsx
  - frontend/src/components/BottomBar.jsx
  - frontend/src/components/Header.jsx
  - frontend/src/App.jsx
  - frontend/src/css/styles.css
autonomous: true
requirements:
  - COMPO-09
  - LOGIC-01
  - LOGIC-02
  - LOGIC-03
must_haves:
  truths:
    - "PC Sidebar renders at 80dp width with icon-only nav items and active indicator pill (primary-container 56x32)"
    - "Mobile BottomBar renders at 80dp height with labels visible and active indicator pill (secondary-container 64x32)"
    - "Sidecar Header shows on PC (>=1024px) only, with logo+brand+subtitle left, page title center, avatar+menu right"
    - "Header user menu dropdown shows display_name + role + theme toggle + logout"
    - "styles.css contains zero .pc-sidebar* / .bottom-bar / .tab-item / .tab-icon / .tab-label / .header* selectors"
  artifacts:
    - path: "frontend/src/components/composites/Sidebar.jsx"
      provides: "80dp navigation rail with role-based navItems, active pill, Ripple wrappers, Icon primitives, Badge for order count"
      min_lines: 100
    - path: "frontend/src/components/composites/Sidebar.css"
      provides: "80dp wide, fixed left, active pill ::before (56x32, primary-container), token-only CSS"
      min_lines: 60
    - path: "frontend/src/components/composites/BottomBar.jsx"
      provides: "80dp nav bar with role-based tabs, active pill, labels visible, Ripple wrappers"
      min_lines: 70
    - path: "frontend/src/components/composites/BottomBar.css"
      provides: "80dp height via --md-nav-height, active pill ::before (64x32, secondary-container), safe-area bottom padding"
      min_lines: 70
    - path: "frontend/src/components/composites/Header.jsx"
      provides: "Sidecar header with logo+brand+subtitle left, page title center via useLocation, avatar+dropdown right"
      min_lines: 100
    - path: "frontend/src/components/composites/Header.css"
      provides: "64dp sticky top, 3-column flex, backdrop-filter blur, user menu dropdown, hidden <1024px"
      min_lines: 80
  key_links:
    - from: "Sidebar.jsx (new)"
      to: "composites/Sidebar.css"
      via: "import './Sidebar.css'"
    - from: "BottomBar.jsx (new)"
      to: "composites/BottomBar.css"
      via: "import './BottomBar.css'"
    - from: "Header.jsx (new)"
      to: "composites/Header.css"
      via: "import './Header.css'"
    - from: "App.jsx"
      to: "composites/Header"
      via: "import Header from './components/composites/Header'"
    - from: "old Sidebar.jsx"
      to: "composites/Sidebar.jsx"
      via: "re-export (or delete + update imports)"
---

<objective>
创建 MD3 化 Navigation Rail（PC 侧栏 80dp）+ Navigation Bar（手机底部栏 80dp）+ Sidecar Header 组件，并集成到 App shell 中。

Purpose：PC 端 Sidebar 从 240px 缩窄到 80dp，仅渲染 Icon（label 移到 Sidecar Header），active state 用 56×32 primary-container pill。手机 BottomBar 保持 80dp 高度，新增 64×32 secondary-container active pill，label 始终可见。Header 重写为 3 列式 sidecar：左 logo+品牌+副标题、中页面标题（useLocation 推断）、右头像+下拉菜单（含主题切换+退出）。现有路由激活、角色显示、usePendingOrderCount Badge 零回归。
Output：6 个新文件（Sidebar/Sidebar.css, BottomBar/BottomBar.css, Header/Header.css）+ 4 个旧文件修改（Sidebar.jsx/BottomBar.jsx/Header.jsx 变成 composites/ 的 re-export + App.jsx 装配）+ styles.css 旧选择器删除
</objective>

<execution_context>
@/home/temila/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/temila/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-composite-navigation-components/11-CONTEXT.md
@.planning/phases/11-composite-navigation-components/11-PATTERNS.md

@frontend/src/components/Sidebar.jsx
@frontend/src/components/BottomBar.jsx
@frontend/src/components/Header.jsx
@frontend/src/App.jsx
@frontend/src/components/primitives/Icon.jsx
@frontend/src/components/primitives/Ripple.jsx
@frontend/src/components/primitives/Badge.jsx
@frontend/src/components/primitives/IconButton.jsx
@frontend/src/components/ThemeToggle.jsx
@frontend/src/contexts/AuthContext.jsx
@frontend/src/hooks/usePendingOrderCount.js
@frontend/src/css/styles.css
@frontend/src/css/tokens.css
@frontend/src/components/primitives/base.css
@frontend/src/utils/index.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Sidebar 80dp + BottomBar MD3 components with co-located CSS</name>
  <read_first>
    @frontend/src/components/Sidebar.jsx (current 240px — navItems, role logic, Ripple, Badge usage to preserve)
    @frontend/src/components/BottomBar.jsx (current 80dp — tabs, isActive logic, Badge usage to preserve)
    @frontend/src/components/primitives/Icon.jsx (Phase 10 Icon primitive — icon name mapping)
    @frontend/src/components/primitives/Ripple.jsx (wrapper pattern)
    @frontend/src/components/primitives/Badge.jsx (pending count badge)
    @frontend/src/hooks/usePendingOrderCount.js
    @frontend/src/css/styles.css:64-76 (.bottom-bar, .tab-item, .tab-icon, .tab-label current styles)
    @frontend/src/css/styles.css:319-345 (.pc-sidebar* current styles, to be replaced)
    @frontend/src/css/styles.css:396-400 (media query hiding sidebar on mobile)
  </read_first>
  <action>
    Create `frontend/src/components/composites/Sidebar.jsx`:
    - Import: `useNavigate` + `useLocation` from react-router-dom; `useAuth` from `../../contexts/AuthContext`; `usePendingOrderCount` from `../../hooks/usePendingOrderCount`; `Icon` from `../primitives/Icon`; `Ripple` from `../primitives/Ripple`; `Badge` from `../primitives/Badge`; `./Sidebar.css`
    - Export default function Sidebar() (no props — same as current)
    - If `!user` return null
    - **navItems**: Copy verbatim from current Sidebar.jsx (lines 17-50), but change `icon` from emoji to Icon name mapping per Phase 10 D-07 30-icon set:
      - '📊' → 'dashboard', '🍽️' → 'restaurant', '🥬' → 'eco', '📂' → 'folder', '👨‍🍳' → 'chef' (or 'group' as closest), '👥' → 'group', '📈' → 'bar-chart' (or 'analytics'), '📝' → 'description', '🍽' (line 28, 点菜预览) → 'restaurant', '💡' → 'lightbulb', '🏠' → 'home', '🍽️' → 'restaurant', '👅' → 'spa' (or 'favorite'), '👤' → 'person'
      - Keep `label` field unchanged (not rendered visually, but used for aria-label and title)
    - Logo: Replace `<div className="pc-sidebar-logo">🍲 家味</div>` with `<Icon name="restaurant" size={28} />` (per the agent's Discretion — using restaurant icon for MD3 consistency over emoji)
    - Render structure:
      ```jsx
      <aside className="md-sidebar">
        <div className="md-sidebar__logo">
          <Icon name="restaurant" size={28} />
        </div>
        <nav className="md-sidebar__nav">
          {navItems.map((item) => (
            <Ripple key={item.path} style={{ width: '100%' }}>
              <button
                className={`md-sidebar__item md-interactive ${location.pathname === item.path ? 'md-sidebar__item--active' : ''}`}
                onClick={() => navigate(item.path)}
                aria-label={item.label}
                title={item.label}
              >
                <span className="md-sidebar__item-icon">
                  <Icon name={item.icon} size={24} />
                  {item.path === '/chef/orders' && <Badge count={pendingCount} />}
                </span>
              </button>
            </Ripple>
          ))}
        </nav>
        <div className="md-sidebar__footer">
          <Ripple style={{ width: '100%' }}>
            <button
              className="md-sidebar__item md-interactive"
              onClick={() => { logout(); navigate('/login'); }}
              aria-label="退出"
              title="退出"
            >
              <span className="md-sidebar__item-icon">
                <Icon name="logout" size={24} />
              </span>
            </button>
          </Ripple>
        </div>
      </aside>
      ```

    Create `frontend/src/components/composites/Sidebar.css` per the PATTERNS.md Sidebar.css skeleton (lines 517-606):
    - `@import '../primitives/base.css'`
    - `.md-sidebar`: `display:none; width:80px; background:var(--md-color-surface-container-lowest); flex-direction:column; position:fixed; top:0; left:0; bottom:0; z-index:100; overflow-y:auto; border-right:1px solid var(--md-color-outline-variant)`
    - `.md-sidebar__logo`: `display:flex; align-items:center; justify-content:center; padding:24px 0 16px; color:var(--md-color-primary)`
    - `.md-sidebar__nav`: `flex:1; padding:8px 0; display:flex; flex-direction:column; gap:0; align-items:stretch`
    - `.md-sidebar__footer`: `padding:8px 0; border-top:1px solid var(--md-color-outline-variant)`
    - `.md-sidebar__item`: `display:flex; align-items:center; justify-content:center; width:100%; height:80px; border:none; background:transparent; cursor:pointer; position:relative; color:var(--md-color-on-surface-variant); transition:color var(--md-motion-duration-short) var(--md-motion-easing-standard)`
    - `.md-sidebar__item--active`: `color:var(--md-color-on-primary-container)`
    - **Active pill (D-11)** via `::before`: `.md-sidebar__item--active::before { content:''; position:absolute; top:50%; left:50%; transform:translate(-50%,-50%); width:56px; height:32px; border-radius:16px; background:var(--md-color-primary-container); z-index:0; transition:background var(--md-motion-duration-short) var(--md-motion-easing-standard) }`
    - `.md-sidebar__item > :not(.md-ripple-layer)`: `position:relative; z-index:2` (content above pill)
    - `.md-sidebar__item-icon`: `position:relative; display:inline-flex; align-items:center; justify-content:center`
    - `@media (min-width:1024px)`: `.md-sidebar { display:flex }` (show only on PC)

    Create `frontend/src/components/composites/BottomBar.jsx`:
    - Import: as Sidebar patterns + `./BottomBar.css`
    - Export default function BottomBar() (no props — same as current)
    - If `!user` return null
    - **tabs**: Copy verbatim from current BottomBar.jsx (lines 17-40) with emoji→Icon mapping:
      - '📊' → 'dashboard', '🍽' → 'restaurant', '💡' → 'lightbulb', '👥' → 'group', '🚪' → 'logout', '👨‍🍳' → 'chef' (or 'group'), '🏠' → 'home', '👤' → 'person'
    - Keep `isActive` function exactly as-is (lines 42-45)
    - Render:
      ```jsx
      <nav className="md-bottom-bar">
        {tabs.map((tab) => (
          <Ripple key={tab.id} style={{ flex: 1 }}>
            <button
              className={`md-tab md-interactive ${isActive(tab.path) ? 'md-tab--active' : ''}`}
              onClick={() => { if (tab.action === 'logout') { logout(); navigate('/login'); } else { navigate(tab.path); } }}
              aria-label={tab.label}
            >
              <span className="md-tab__icon">
                <Icon name={tab.icon} size={24} />
                {tab.id === 'chef-orders' && <Badge count={pendingCount} />}
              </span>
              <span className="md-tab__label">{tab.label}</span>
            </button>
          </Ripple>
        ))}
      </nav>
      ```

    Create `frontend/src/components/composites/BottomBar.css` per the PATTERNS.md BottomBar.css skeleton (lines 701-774):
    - `.md-bottom-bar`: `position:fixed; bottom:0; left:50%; transform:translateX(-50%); width:100%; max-width:420px; height:var(--md-nav-height); background:var(--md-color-surface-container); border-top:1px solid var(--md-color-outline-variant); padding:12px 0 calc(12px + env(safe-area-inset-bottom, 0px)); z-index:200; display:flex; justify-content:space-around; align-items:center`
    - `@media (min-width:640px)`: max-width 768px; `@media (min-width:1024px)`: max-width 1200px
    - `.md-tab`: `flex:1; display:flex; flex-direction:column; align-items:center; gap:4px; padding:8px 0; border:none; background:transparent; color:var(--md-color-on-surface-variant); cursor:pointer; position:relative; transition:color var(--md-motion-duration-short) var(--md-motion-easing-standard)`
    - **Active pill (D-12)** via `::before`: `.md-tab--active { color:var(--md-color-on-secondary-container) }` + `.md-tab--active::before { content:''; position:absolute; top:4px; left:50%; transform:translateX(-50%); width:64px; height:32px; border-radius:16px; background:var(--md-color-secondary-container); z-index:0; transition:background var(--md-motion-duration-short) var(--md-motion-easing-standard) }`
    - `.md-tab > :not(.md-ripple-layer)`: `position:relative; z-index:2`
    - `.md-tab__icon`: `position:relative; display:inline-flex; align-items:center; justify-content:center`
    - `.md-tab__label`: `font-size:0.75rem; font-weight:500; line-height:1`
    - `.md-tab--active .md-tab__label`: `font-weight:600`
    - All values via `var(--md-*)` — zero hardcoded colors

    After creating composite files, **update old Sidebar.jsx** at `frontend/src/components/Sidebar.jsx`:
    - Replace entire content with a re-export: `export { default } from './composites/Sidebar';`
    - Same for **BottomBar.jsx**: `export { default } from './composites/BottomBar';`
    - This preserves all existing import paths across the codebase (e.g., `import Sidebar from './components/Sidebar'` in App.jsx)
  </action>
  <verify>
    <automated>npm run build --prefix frontend 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    1. Sidebar.jsx at `frontend/src/components/composites/Sidebar.jsx` renders 80dp width icon-only navigation with active pill
    2. Sidebar.css uses `width:80px` with `.md-sidebar__item--active::before` as 56x32 primary-container pill
    3. Old Sidebar.jsx re-exports from composites/Sidebar
    4. BottomBar.jsx at `frontend/src/components/composites/BottomBar.jsx` renders with active pill and labels visible
    5. BottomBar.css has `.md-tab--active::before` as 64x32 secondary-container pill, uses `--md-nav-height` for height
    6. Old BottomBar.jsx re-exports from composites/BottomBar
    7. Vite build completes with 0 errors
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create Sidecar Header + App.jsx integration</name>
  <read_first>
    @frontend/src/components/Header.jsx (current header — showBack, title, actions props, ThemeToggle usage)
    @frontend/src/App.jsx:74-84 (PcLayout — Sidebar + Outlet wiring)
    @frontend/src/App.jsx:106 (ToastProvider line — will also be changed in 11-03, but for now just add Header)
    @frontend/src/components/ThemeToggle.jsx (being absorbed into Header dropdown)
    @frontend/src/components/primitives/IconButton.jsx (arrow-back usage)
    @frontend/src/utils/index.js (theme utility — `theme.toggleTheme()`, `theme.getTheme()`)
  </read_first>
  <action>
    Create `frontend/src/components/composites/Header.jsx` per the PATTERNS.md Header.jsx skeleton (lines 791-903):
    - Import: `useState` + `useRef` + `useEffect` from react; `useNavigate` + `useLocation` from react-router-dom; `useAuth` from `../../contexts/AuthContext`; `theme` from `../../utils`; `Icon` from `../primitives/Icon`; `IconButton` from `../primitives/IconButton`; `./Header.css`
    - **PAGE_TITLES** map (useLocation-based title resolution):
      ```js
      const PAGE_TITLES = {
        '/home': '首页', '/order': '点菜', '/preferences': '口味偏好',
        '/my-wishes': '我的愿望', '/profile': '我的',
        '/chef/orders': '订单管理', '/chef/dishes': '菜品管理', '/chef/wishes': '愿望管理',
        '/admin': '管理后台', '/admin/dishes': '菜品管理', '/admin/wishes': '愿望总览',
        '/admin/users': '用户管理', '/admin/categories': '分类管理',
        '/admin/chefs': '厨师管理', '/admin/stats': '数据统计', '/admin/logs': '系统日志',
        '/ingredients': '食材管理',
      }
      ```
    - Export default function Header({ title, showBack=false, actions }) (same props as current for backward compat)
    - State: `const [menuOpen, setMenuOpen] = useState(false)`
    - Ref: `const menuRef = useRef(null)`
    - **Outside-click close**: useEffect on `[menuOpen]` — add `mousedown` listener checking `!menuRef.current?.contains(e.target)`, cleanup remove
    - `resolvedTitle = title || PAGE_TITLES[location.pathname] || '家味'`
    - Render:
      ```jsx
      <header className="md-header">
        <div className="md-header__left">
          {showBack ? (
            <IconButton icon="arrow-back" ariaLabel="返回" onClick={() => navigate(-1)} />
          ) : (
            <div className="md-header__logo">
              <Icon name="restaurant" size={20} />
              <span className="md-header__brand">家味</span>
              <span className="md-header__subtitle">Family Chef</span>
            </div>
          )}
        </div>
        <h1 className="md-header__title">{resolvedTitle}</h1>
        <div className="md-header__right" ref={menuRef}>
          {actions}
          {user && (
            <>
              <button type="button" className="md-header__avatar" onClick={() => setMenuOpen(o => !o)} aria-label="用户菜单" aria-expanded={menuOpen}>
                {(user.display_name || user.username || '?').charAt(0).toUpperCase()}
              </button>
              {menuOpen && (
                <div className="md-header__menu" role="menu">
                  <div className="md-header__menu-info">
                    <div className="md-header__menu-name">{user.display_name || user.username}</div>
                    <div className="md-header__menu-role">{user.role === 'admin' ? '管理员' : user.role === 'chef' ? '厨师' : '用户'}</div>
                  </div>
                  <button type="button" className="md-header__menu-item" role="menuitem"
                    onClick={() => { theme.toggleTheme(); setMenuOpen(false); }}>
                    <Icon name={theme.getTheme() === 'dark' ? 'light-mode' : 'dark-mode'} size={18} />
                    <span>切换主题</span>
                  </button>
                  <button type="button" className="md-header__menu-item" role="menuitem"
                    onClick={() => { logout(); navigate('/login'); }}>
                    <Icon name="logout" size={18} />
                    <span>退出</span>
                  </button>
                </div>
              )}
            </>
          )}
        </div>
      </header>
      ```

    Create `frontend/src/components/composites/Header.css` per patterns (lines 921-1052):
    - `.md-header`: `display:none; position:sticky; top:0; z-index:100; height:64px; background:var(--md-color-surface-container-low); backdrop-filter:blur(20px); -webkit-backdrop-filter:blur(20px); border-bottom:1px solid var(--md-color-outline-variant); padding:0 24px; display:flex; align-items:center; justify-content:space-between; gap:24px`
    - `@media (min-width:1024px)`: `.md-header { display:flex }` (PC only)
    - `.md-header__left`: `display:flex; align-items:center; gap:12px; flex-shrink:0`
    - `.md-header__logo`: `display:flex; align-items:center; gap:8px; color:var(--md-color-primary)`
    - `.md-header__brand`: `font-family:var(--md-font-display); font-size:1.1rem; font-weight:700; color:var(--md-color-primary)`
    - `.md-header__subtitle`: `font-size:0.75rem; color:var(--md-color-on-surface-variant)`
    - `.md-header__title`: `flex:1; text-align:center; font-family:var(--md-font-display); font-size:1.125rem; font-weight:600; color:var(--md-color-on-surface); margin:0; white-space:nowrap; overflow:hidden; text-overflow:ellipsis`
    - `.md-header__right`: `display:flex; align-items:center; gap:8px; flex-shrink:0; position:relative`
    - `.md-header__avatar`: `width:40px; height:40px; border-radius:var(--md-radius-full); background:var(--md-color-primary); color:var(--md-color-on-primary); border:none; font-weight:600; font-size:0.9rem; display:flex; align-items:center; justify-content:center; cursor:pointer; transition:box-shadow var(--md-motion-duration-short) var(--md-motion-easing-standard)` (+ hover: elevation-1)
    - `.md-header__menu`: `position:absolute; top:calc(100% + 8px); right:0; min-width:200px; background:var(--md-color-surface-container-lowest); border:1px solid var(--md-color-outline-variant); border-radius:var(--md-radius-md); box-shadow:var(--md-elevation-3); padding:8px 0; z-index:200`
    - `.md-header__menu-info`: `padding:8px 16px 12px; border-bottom:1px solid var(--md-color-outline-variant); margin-bottom:8px`
    - `.md-header__menu-name`: `font-size:0.9rem; font-weight:600; color:var(--md-color-on-surface)`
    - `.md-header__menu-role`: `font-size:0.75rem; color:var(--md-color-on-surface-variant); margin-top:2px`
    - `.md-header__menu-item`: `display:flex; align-items:center; gap:12px; width:100%; padding:10px 16px; border:none; background:transparent; color:var(--md-color-on-surface); font-size:0.9rem; cursor:pointer; text-align:left; transition:background var(--md-motion-duration-short) var(--md-motion-easing-standard)` + hover: `background:var(--md-color-surface-container)`
    - All values via `var(--md-*)` — zero hardcoded colors

    Then update old `frontend/src/components/Header.jsx` to re-export: `export { default } from './composites/Header';`

    **Update** `frontend/src/App.jsx`:
    - Add import: `import Header from './components/Header';` (already imports Sidebar the same way)
    - In `PcLayout()` function (lines 74-84): Insert `<Header />` between `<Sidebar />` and `<main>`:
      ```jsx
      function PcLayout() {
        const location = useLocation();
        return (
          <div className="pc-layout">
            <Sidebar />
            <Header />
            <main className="pc-main" key={location.pathname}>
              <Outlet />
            </main>
          </div>
        );
      }
      ```
    - Also add `<Header />` import at line 5: `import Header from './components/Header';`
  </action>
  <verify>
    <automated>npm run build --prefix frontend 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    1. Header.jsx at `frontend/src/components/composites/Header.jsx` renders 3-column layout with logo, title, and user menu
    2. Header.css has `.md-header { display:none }` with `@media (min-width:1024px) { display:flex }`
    3. Old Header.jsx re-exports from composites/Header
    4. App.jsx `PcLayout` renders `<Sidebar />` then `<Header />` then `<main>`
    5. User menu shows display_name + role + theme toggle + logout
    6. Vite build completes with 0 errors
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Delete legacy navigation CSS from styles.css + grep verification</name>
  <read_first>
    @frontend/src/css/styles.css:54-62 (.header/.header-title/.header-back/.header-actions)
    @frontend/src/css/styles.css:64-76 (.bottom-bar and .tab-item* chain)
    @frontend/src/css/styles.css:319-345 (.pc-sidebar* chain)
    @frontend/src/css/styles.css:396-398 (@media (max-width:1023px) .pc-sidebar)
    @frontend/src/css/styles.css:537 (.tab-item:focus-visible)
    @frontend/src/css/styles.css:539 (.pc-sidebar-item:focus-visible)
    @frontend/src/css/styles.css:572-580 (global 48dp rule — remove .tab-item, .pc-sidebar-item, .header-back from this list)
    @frontend/src/css/styles.css:602-606 (.header-back 48dp min-size rule)
    @frontend/src/css/styles.css:49-52 (`.pc-sidebar-item.active::before` state-layer override — keep for now, handled by composite CSS)
  </read_first>
  <action>
    Delete the following selectors from `frontend/src/css/styles.css`:

    1. **Entire header block** (lines 54-62): Delete from `/* Header */` through `.header-actions { ... }` — `.header`, `.header-title`, `.header-back`, `.header-back::before`, `.header-back:hover::before`, `.header-back:active::before`, `.header-back:focus-visible::before`, `.header-actions`

    2. **Entire bottom-bar block** (lines 64-76): Delete from `/* Bottom Tab Bar */` through `.tab-item.active .tab-label { ... }` — `.bottom-bar`, `@media (min-width:640px) .bottom-bar`, `@media (min-width:1024px) .bottom-bar`, `.tab-item`, `.tab-item .tab-icon`, `.tab-item .tab-label`, `.tab-item::before`, `.tab-item:hover::before`, `.tab-item:active::before`, `.tab-item:hover`, `.tab-item.active`, `.tab-item.active .tab-label`

    3. **`.tab-item:focus-visible`** (line 538): Delete line `.tab-item:focus-visible { outline: var(--md-focus-ring-outer); outline-offset: 2px; }`

    4. **Entire pc-sidebar block** (lines 319-345): Delete from `.pc-sidebar { ... }` through `.pc-sidebar-footer-actions .theme-toggle { ... }` — `.pc-sidebar`, `.pc-sidebar-header`, `.pc-sidebar-logo`, `.pc-sidebar-subtitle`, `.pc-sidebar-nav`, `.pc-sidebar-item`, `.pc-sidebar-item::before`, `.pc-sidebar-item:hover::before`, `.pc-sidebar-item:active::before`, `.pc-sidebar-item:hover`, `.pc-sidebar-item.active`, `.pc-sidebar-icon`, `.pc-sidebar-footer`, `.pc-sidebar-user`, `.pc-sidebar-user-info`, `.pc-sidebar-user-name`, `.pc-sidebar-user-role`, `.pc-sidebar-footer-actions`, `.pc-sidebar-footer-actions .theme-toggle`

    5. **`.pc-sidebar-item:focus-visible`** (line 539): Delete line `.pc-sidebar-item:focus-visible { outline: var(--md-focus-ring-outer); outline-offset: 2px; }`

    6. **Mobile sidebar hiding** (media query at line 396-400): Delete the entire `@media (max-width: 1023px)` block containing `display: none !important` for `.pc-sidebar` and `.pc-main { padding-left: 0; width: 100%; }` — replaced by `.md-sidebar` media query

    7. **`.pc-sidebar-item.active::before` state-layer override** (line 49-52): Delete lines `.pc-sidebar-item.active::before,` and `.wish-picker-item.active::before { background-color: var(--md-state-layer-on-surface); }` — this override is for old active state, now handled by composite CSS pill

    8. **`.header-back` from global 48dp rule** (lines 570-580): Remove `.header-back` from the selector list

    9. **`.tab-item`, `.pc-sidebar-item` from global 48dp rule** (lines 570-580): Remove `.tab-item, .pc-sidebar-item,` from the selector list

    10. **`.header-back` 48dp padding compensation** (lines 602-606): Delete entire block:
        ```css
        /* 3. header-back: 原 36x36 → 48x48 hit area */
        .header-back {
          min-width: 48px;
          min-height: 48px;
        }
        ```

    11. **`.pc-layout` margin-left** reference (line 365): Change `padding-left: 240px` to `padding-left: 80px` (the `.pc-main` rule at `@media (min-width:1024px)`)
        ```css
        .pc-main { margin-left: 0; padding-left: 80px; width: 100vw; min-height: 100vh; }
        ```

    Keep **`.pc-layout`** (line 318), **`.pc-main`** (line 347) — these are structural, not sidebar visual.

    Verify with:
    ```bash
    grep -rn 'className="pc-sidebar\|className="bottom-bar\|className="tab-item\|className="tab-icon\|className="tab-label\|className="header\b' frontend/src/ --include='*.jsx' | grep -v '^[^:]*:[^:]*//'
    ```
    Expected: 0 results (old Sidebar.jsx/BottomBar.jsx/Header.jsx are re-exports, no className usage). The only matches should be in comments.

    Also verify that the `.pc-layout` structural CSS (`.pc-layout { display: flex }` at line 318, `.pc-main { flex: 1; min-width: 0 }` at line 347) remains intact — these layout rules are needed for the app shell.
  </action>
  <verify>
    <automated>grep -rn 'className="pc-sidebar\|className="bottom-bar\|className="tab-item\|className="tab-label\|className="header[^s-]' frontend/src/ --include='*.jsx' 2>&1; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    1. All `.pc-sidebar*`, `.bottom-bar`, `.tab-item*`, `.header*` CSS selectors removed from styles.css
    2. `.pc-sidebar-item.active::before` state-layer override removed
    3. `.pc-main` media query padding changed from 240px to 80px
    4. `.tab-item`, `.pc-sidebar-item`, `.header-back` removed from global 48dp selector list
    5. `.tab-item:focus-visible`, `.pc-sidebar-item:focus-visible` removed
    6. `.header-back` 48dp min-size block deleted
    7. grep for old className usage returns 0 results across all JSX
    8. Vite build completes with 0 errors
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Header user menu | DOM event outside-click handler — document-level mousedown listener |
| Sidebar nav items | Navigation — role/item logic from AuthContext (trusted) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04 | XSS | Header user name display | mitigate | `user.display_name` renders via JSX `{}`, React auto-escapes. No dangerouslySetInnerHTML. |
| T-11-10 | Information Disclosure | CSS legacy selector cleanup missed | mitigate | grep verify 0 residues — `grep -rnE '(pc-sidebar|bottom-bar|tab-item|header[^-])' frontend/src/ --include='*.css' \| grep -v '10-02-MIGRATION'` ensures complete cleanup |
| T-11-SC | Tampering | npm install (none) | accept | Phase 11 introduces zero new npm dependencies. |
</threat_model>

<verification>
- [ ] `npm run build --prefix frontend` — Vite 0 error
- [ ] `grep -rn 'className="pc-sidebar' frontend/src/ --include='*.jsx'` — 0 results
- [ ] `grep -rn 'className="bottom-bar' frontend/src/ --include='*.jsx'` — 0 results
- [ ] `grep -rn 'className="header ' frontend/src/ --include='*.jsx'` — 0 results (the old Header.jsx class)
- [ ] Manual PC: Sidebar shows 80dp, active pill shows, Header shows logo+title+avatar dropdown
- [ ] Manual mobile (<1024px): Sidebar hidden, BottomBar shows with active pill
</verification>

<success_criteria>
1. Sidebar/BottomBar/Header composite components created under `composites/` with co-located CSS
2. Old Sidebar.jsx/BottomBar.jsx/Header.jsx re-export from composites/
3. App.jsx PcLayout inserts `<Header />` between Sidebar and Outlet
4. styles.css all legacy .pc-sidebar* / .bottom-bar / .tab-item* / .header* selectors deleted
5. `.pc-main` padding-left updated from 240px to 80px
6. Vite build passes (0 errors)
</success_criteria>

<output>
Create `.planning/phases/11-composite-navigation-components/11-02-SUMMARY.md` when done
</output>
