---
phase: 11-composite-navigation-components
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/composites/Modal.jsx
  - frontend/src/components/composites/Modal.css
  - frontend/src/components/ConfirmModal.jsx
  - frontend/src/components/WishFormModal.jsx
  - frontend/src/components/WishRejectModal.jsx
  - frontend/src/components/WishAdvanceModal.jsx
  - frontend/src/components/CreateLinkModal.jsx
  - frontend/src/components/InvitationsModal.jsx
  - frontend/src/components/ChefSelectModal.jsx
  - frontend/src/pages/AdminDishesPage.jsx
  - frontend/src/pages/ChefDishesPage.jsx
  - frontend/src/pages/AdminIngredientsPage.jsx
  - frontend/src/pages/OrderPage.jsx
  - frontend/src/pages/AdminChefsPage.jsx
  - frontend/src/pages/AdminCategoriesPage.jsx
  - frontend/src/pages/AdminUsersPage.jsx
  - frontend/src/pages/UserProfilePage.jsx
  - frontend/src/css/styles.css
autonomous: true
requirements:
  - COMPO-08
  - LOGIC-01
  - LOGIC-02
  - LOGIC-03
must_haves:
  truths:
    - "Existing ConfirmModal opens/closes with same props, focus trap, ESC, scroll lock via <Modal>"
    - "Existing WishFormModal renders form as children with title visible and closeIcon=false"
    - "Existing InvitationsModal renders full-screen variant at 100vw×100vh with header pinned top"
    - "Modal focus traps within active element, ESC fires onClose, scroll is locked, focus returns on close"
    - "styles.css contains zero .modal-overlay / .modal-content / .modal-header / .modal-body / .modal-footer / .modal-close selectors"
  artifacts:
    - path: "frontend/src/components/composites/Modal.jsx"
      provides: "Unified Modal component with basic/full-screen variants, slots, focus trap, ESC, scroll lock"
      min_lines: 140
    - path: "frontend/src/components/composites/Modal.css"
      provides: "MD3 scrim, elevation-3, --md-radius-lg 24dp, flex-shrink:0 header/footer, token-only values"
      min_lines: 80
    - path: "frontend/src/components/ConfirmModal.jsx"
      provides: "Thin wrapper around <Modal> with unchanged ConfirmModal props, actions={[tonal cancel, filled confirm]}"
    - path: "frontend/src/components/WishFormModal.jsx"
      provides: "Thin wrapper around <Modal> with closeIcon=false, form as children, exported props preserved"
    - path: "frontend/src/components/InvitationsModal.jsx"
      provides: "Thin wrapper around <Modal variant='full-screen'> with list body, full-viewport height"
  key_links:
    - from: "Modal.jsx"
      to: "utils/index.js"
      via: "trapFocusWithin"
      pattern: "trapFocusWithin"
    - from: "Modal.jsx"
      to: "Modal.css"
      via: "import './Modal.css'"
    - from: "ConfirmModal.jsx"
      to: "composites/Modal"
      via: "import Modal from '../composites/Modal'"
---

<objective>
创建统一 `<Modal>` composite 组件（Basic + Full-screen 双变体），替换全部 22 个现有 Modal 站点（7 个 wrapper 组件 + 15 个 inline `<div className='modal-overlay'>`），并删除 styles.css 中全部旧 modal-* CSS 选择器。

Purpose：全平台 Modal 视觉统一为 MD3 24dp 圆角 + elevation-3 阴影 + scrim + focus trap + ESC 关闭 + 滚动锁定。7 个现有 Modal wrapper 组件（ConfirmModal / WishFormModal / WishRejectModal / WishAdvanceModal / CreateLinkModal / InvitationsModal / ChefSelectModal）变成 thin wrapper，props 与事件绑定零回归。15 个 inline modal-overlay 站点（在 AdminDishesPage / ChefDishesPage / AdminIngredientsPage / OrderPage / AdminChefsPage / AdminCategoriesPage / AdminUsersPage / UserProfilePage 中）改写为 `<Modal>` 调用。
Output：`composites/Modal.jsx` + `Modal.css` + 21 个文件修改（7 个 wrapper + 15 个 inline）+ styles.css 旧选择器删除
</objective>

<execution_context>
@/home/temila/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/temila/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-composite-navigation-components/11-CONTEXT.md
@.planning/phases/11-composite-navigation-components/11-PATTERNS.md

@frontend/src/components/ConfirmModal.jsx
@frontend/src/components/WishFormModal.jsx
@frontend/src/components/WishRejectModal.jsx
@frontend/src/components/WishAdvanceModal.jsx
@frontend/src/components/CreateLinkModal.jsx
@frontend/src/components/InvitationsModal.jsx
@frontend/src/components/ChefSelectModal.jsx
@frontend/src/components/primitives/Button.jsx
@frontend/src/utils/index.js
@frontend/src/css/styles.css
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Modal composite component + Modal.css</name>
  <read_first>
    @frontend/src/components/ConfirmModal.jsx (focus trap + ESC + scroll lock pattern)
    @frontend/src/components/WishFormModal.jsx (form-as-children + closeIcon=false pattern)
    @frontend/src/components/InvitationsModal.jsx (full-screen variant pattern)
    @frontend/src/components/primitives/Card.jsx (slot-based prop pattern reference)
    @frontend/src/components/primitives/base.css (shared state-layer + hit-box patterns)
    @frontend/src/css/styles.css:402-412 (current modal-* CSS to replace)
    @frontend/src/utils/index.js:91-121 (trapFocusWithin utility)
    @frontend/src/css/tokens.css (--md-radius-lg, --md-elevation-3, --md-color-scrim, etc.)
  </read_first>
  <action>
    Create `frontend/src/components/composites/` directory.

    Create `Modal.jsx` with the following structure per D-01/D-02/D-03:
    - Import: `useEffect`, `useRef` from react; `trapFocusWithin` from `../../utils`; `./Modal.css`
    - Export default function Modal with props: `variant='basic'`, `open=true`, `onClose`, `title`, `closeIcon=true`, `header` (ReactNode slot), `footer` (ReactNode slot), `actions` (ReactNode array), `children`, `closeOnBackdrop=true`, `labelledBy`, `describedBy`, `initialFocusRef`, `className=''`, `style`
    - When `!open` return null
    - **Scroll lock + focus restore effect** (from ConfirmModal.jsx:26-41 pattern): useEffect on `[open, initialFocusRef]` — save `document.activeElement`, set `document.body.style.overflow='hidden'`, focus `initialFocusRef?.current || closeRef.current`, return cleanup restoring overflow and refocusing saved element if still in DOM
    - **ESC close effect**: useEffect on `[open, onClose]` — keydown listener `e.key==='Escape'` fires `onClose?.()`, cleanup removeEventListener
    - **Header slot logic** (D-03): `header !== undefined` renders custom slot; else render default with `<h3>` title + closeIcon button (ref=closeRef, onClick=onClose, aria-label=`关闭${title||'窗口'}`)
    - **Footer slot logic** (D-03): `footer !== undefined` renders custom slot; else if `actions` exists, wrap in `<div className='md-modal__actions'>` — accept single ReactNode or array, map over array
    - **Full-screen variant** (D-02): When `variant==='full-screen'`, overlay padding=0, align-items=stretch; modal border-radius=0, max-width=100%, max/height=100vh
    - **A11y**: `role='dialog'`, `aria-modal='true'`, `aria-labelledby={labelledBy}`, `aria-describedby={describedBy}`, `tabIndex={-1}` on modal-content div
    - **Backdrop click**: `handleBackdropClick` checks `closeOnBackdrop && e.target===e.currentTarget` then fires `onClose?.()`
    - **Focus trap**: `onKeyDown={(e)=>trapFocusWithin(e, dialogRef.current)}` on modal-content div
    - CSS class names: overlay = `md-modal-overlay` + `md-modal--${variant}`; modal = `md-modal`; header = `md-modal__header`; title = `md-modal__title`; close = `md-modal__close`; body = `md-modal__body`; footer = `md-modal__footer`; actions = `md-modal__actions`

    Create `Modal.css` with per the PATTERNS.md Modal.css skeleton (lines 201-299):
    - `@import '../primitives/base.css'` at top
    - `.md-modal-overlay`: `position:fixed; inset:0; background:var(--md-color-scrim); z-index:500; display:flex; align-items:center; justify-content:center; padding:16px`
    - `.md-modal--full-screen`: `padding:0; align-items:stretch`
    - `.md-modal`: `background:var(--md-color-surface-container-lowest); border-radius:var(--md-radius-lg); width:100%; max-width:480px; max-height:90vh; overflow-y:auto; box-shadow:var(--md-elevation-3); display:flex; flex-direction:column; position:relative`
    - Full-screen child override: `.md-modal--full-screen .md-modal` — `max-width:100%; max-height:100vh; height:100vh; border-radius:0; max-width:none`
    - `.md-modal__header`: `display:flex; align-items:center; justify-content:space-between; padding:16px 20px; border-bottom:1px solid var(--md-color-outline-variant); flex-shrink:0`
    - `.md-modal__title`: `font-size:1rem; font-weight:600; color:var(--md-color-on-surface); margin:0`
    - `.md-modal__close`: `border:none; background:transparent; border-radius:var(--md-radius-full); font-size:1rem; color:var(--md-color-on-surface-variant); width:40px; height:40px; display:flex; align-items:center; justify-content:center; cursor:pointer` (48dp hit box via base.css md-interactive min-width/min-height applies automatically if base.css classes used; the close button itself does NOT use md-interactive — its 48dp hit area comes from the global styles.css button rule, will be removed by styles.css cleanup in Task 3; ensure the close button has `min-width:48px; min-height:48px` via CSS or padding, matching `.modal-close` current 48dp rule at styles.css:609-613)
    - `.md-modal__body`: `padding:20px; flex:1; overflow-y:auto`
    - `.md-modal__footer`: `display:flex; gap:12px; justify-content:flex-end; padding:16px 20px; border-top:1px solid var(--md-color-outline-variant); flex-shrink:0`
    - `.md-modal__actions`: `display:flex; gap:8px; align-items:center`
    - `@media (prefers-reduced-motion:reduce)`: `.md-modal{transition:none}`
    - All values via `var(--md-*)` tokens — zero hardcoded hex/px (except 0 values and structural ones like `inset:0`)

    Add `.md-modal__close` hit area: per D-02 Phase 10 pattern — `min-width:48px; min-height:48px; padding:4px;` to ensure 48dp touch target (compensates for 40px visual size). This replaces the removed `.modal-close` rule at styles.css:609-613.

    **Do NOT** import `Ripple` — Modal close button is a simple button, uses CSS state-layer from base.css's `.md-interactive` class. Add `className='md-interactive'` to the close button for state-layer hover/pressed/focused feedback.
  </action>
  <behavior>
    - Test 1: Modal renders with correct structure when open=true, returns null when open=false
    - Test 2: ESC keydown fires onClose callback
    - Test 3: Backdrop click fires onClose (when closeOnBackdrop=true)
    - Test 4: Full-screen variant has no border-radius and full viewport height
    - Test 5: header slot overrides default title+close rendering
    - Test 6: actions array renders wrapped in md-modal__actions div
  </behavior>
  <verify>
    <automated>npm run build --prefix frontend 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    1. Modal.jsx exists at `frontend/src/components/composites/Modal.jsx` with 140+ lines implementing all props listed above
    2. Modal.css exists at `frontend/src/components/composites/Modal.css` with 80+ lines and zero hardcoded hex/rgb values
    3. Modal exports default function component with variant, open, onClose, title, closeIcon, header, footer, actions, children, closeOnBackdrop, labelledBy, describedBy, initialFocusRef, className, style props
    4. ESC keydown calls onClose, backdrop click calls onClose when closeOnBackdrop=true, body scroll locked while open
    5. Vite build completes with 0 errors
    6. Close button has `className="md-modal__close md-interactive"` and `min-width:48px; min-height:48px` for touch target
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Migrate 7 wrapper Modal components + 15 inline Modal sites to &lt;Modal&gt;</name>
  <read_first>
    @frontend/src/components/composites/Modal.jsx (the new component being consumed)
    @frontend/src/components/ConfirmModal.jsx (current implementation to rewrite)
    @frontend/src/components/WishFormModal.jsx (current implementation to rewrite)
    @frontend/src/components/WishRejectModal.jsx (current implementation to rewrite)
    @frontend/src/components/WishAdvanceModal.jsx (current implementation to rewrite)
    @frontend/src/components/CreateLinkModal.jsx (current implementation to rewrite)
    @frontend/src/components/InvitationsModal.jsx (current implementation to rewrite)
    @frontend/src/components/ChefSelectModal.jsx (current implementation to rewrite)
    @frontend/src/pages/AdminDishesPage.jsx (search for all `className="modal-overlay"` occurrences)
    @frontend/src/pages/ChefDishesPage.jsx (search for all `className="modal-overlay"` occurrences)
    @frontend/src/pages/AdminIngredientsPage.jsx (search for all `className="modal-overlay"` occurrences)
    @frontend/src/pages/OrderPage.jsx (search for all `className="modal-overlay"` occurrences)
    @frontend/src/pages/AdminChefsPage.jsx (search for all `className="modal-overlay"` occurrences)
    @frontend/src/pages/AdminCategoriesPage.jsx (search for all `className="modal-overlay"` occurrences)
    @frontend/src/pages/AdminUsersPage.jsx (search for all `className="modal-overlay"` occurrences)
    @frontend/src/pages/UserProfilePage.jsx (search for all `className="modal-overlay"` occurrences)
  </read_first>
  <action>
    Migrate ALL 22 Modal sites. Strategy per D-01: each site's old `className="modal-overlay"` div (with modal-content, modal-header, modal-body, modal-footer, modal-close sub-elements) gets replaced by `<Modal>` component with appropriate props. The modal-overlay === `<Modal>` at top level. Modal-content's `maxWidth` style becomes `<Modal style={{maxWidth:N}}>` prop.

    **7 Wrapper components** — rewrite each as thin wrapper that imports `<Modal>` and renders its existing children/content inside `<Modal>` slots:

    1. **ConfirmModal.jsx**: Import `Modal` from `../composites/Modal`. Replace entire outer `<div className="modal-overlay">`..`</div>` with `<Modal title={title} open onClose={onCancel} closeIcon style={{maxWidth:360}} labelledBy="confirm-modal-title" describedBy="confirm-modal-body">`. Move existing `<div className="modal-body" id="confirm-modal-body">` content as children. Replace `<div className="modal-footer">` with `actions={[<Button variant="tonal" onClick={onCancel} disabled={confirming}>{cancelText}</Button>, <Button ref={confirmRef} variant={danger?'outlined':'filled'} style={danger?{borderColor:'var(--md-color-error)',color:'var(--md-color-error)'}:undefined} onClick={onConfirm} disabled={confirming}>{confirmText}</Button>]}`. Remove the old modal-close button and modal-header div — Modal renders them from props. **Delete** the manual useEffect scroll-lock/focus-trap/ESC handlers (lines 26-50) — Modal handles these. **Preserve** the `danger` prop, `confirming` guard, `confirmRef`, dialogRef only needed for trapFocusWithin (now internal), `previouslyFocused` is internal to Modal. ConfirmModal still receives `onCancel` as Modal's `onClose`. The `confirming` guard for ESC: Modal's ESC always fires `onClose(onCancel)`, but ConfirmModal's `onCancel` already checks `!confirming` — preserved.

    2. **WishFormModal.jsx**: Import `Modal` from `../composites/Modal`. Replace outer structure with `<Modal title={isEdit?'编辑愿望':'新建愿望'} open onClose={onClose} closeIcon={false} labelledBy="wish-form-title" describedBy="wish-form-description" style={{maxWidth:480}}>`. Keep existing `<form className="modal-body">` as children (remove the outer `<div className="modal-overlay">`..wrapper divs). Remove `dialogRef`, `previouslyFocused`, ESC handler, scroll lock — all in Modal now. **Keep** `submitting` state and `handleKey` ESC guard (`!submitting`) — Modal ESC fires `onClose` which caller controls; but WishFormModal's own `useEffect` ESC handler (line 82-88) already guards `!submitting`, however since Modal already handles ESC globally, change the guard: WishFormModal should NOT have its own ESC listener — Modal handles it. Instead, pass an `onClose` that checks `!submitting`. **Remove** the duplicate useEffect (lines 64-79 and 82-88). Move `closeLabel`, `submitLabel` to `footer` slot or use `actions` prop. Best approach: use `footer` slot for the form's button row — `<Modal title={...} closeIcon={false} footer={<div className="modal-footer"><Button variant="tonal" ...>{closeLabel}</Button><Button type="submit" ...>{submitLabel}</Button></div>}>`. Keep form's `onSubmit={handleSubmit}` on `<form>`. The form's `handleSubmit` handles the submit action — Modal is just a container.

    3. **WishRejectModal.jsx**: Import `Modal`. Replace outer structure with `<Modal title="拒绝愿望" open onClose={onClose} style={{maxWidth:480}} actions={[<Button variant="tonal" onClick={onClose}>暂不拒绝</Button>, <Button variant="filled" style={{background:'var(--md-color-error)'}} onClick={handleConfirm} disabled={rejecting}>确认拒绝</Button>]}>{children}</Modal>`. Keep form as children. Remove old useEffect handlers (dialogRef, scroll lock, ESC).

    4. **WishAdvanceModal.jsx**: Import `Modal`. Replace outer structure with `<Modal title={title} open onClose={onClose} style={{maxWidth:560}}>{existing body content}</Modal>`. Remove old useEffect handlers.

    5. **CreateLinkModal.jsx**: Import `Modal`. Replace outer structure with `<Modal title="邀请链接已创建" open onClose={onClose} style={{maxWidth:480}} actions={[<Button variant="filled" onClick={handleCopy}>复制链接</Button>, <Button variant="tonal" onClick={handleShare} disabled={!canShare}>分享</Button>]}>{body content}</Modal>`. Remove old useEffect handlers.

    6. **InvitationsModal.jsx**: Import `Modal`. Replace outer structure with `<Modal variant="full-screen" title="邀请记录" open onClose={onClose}>{invitations list content}</Modal>`. Remove the old `useEffect` scroll lock (line 22-27) — Modal handles it. Keep the inline `style={{alignItems:'flex-start', paddingTop:0}}` on old modal-overlay becomes unnecessary — full-screen variant handles this via `.md-modal--full-screen { padding:0; align-items:stretch }`. Remove the old inline `style={{maxWidth:'100%', height:'100vh', borderRadius:0}}` — full-screen handles this. Keep Badge, Loading, EmptyState imports as-is.

    7. **ChefSelectModal.jsx**: Import `Modal`. Replace outer structure with `<Modal title="选择厨师" open onClose={onClose} style={{maxWidth:480}}>{chef list content}</Modal>`. Remove old useEffect handlers.

    **15 Inline Modal sites** — Grep all `className="modal-overlay"` in these files and replace each instance with `<Modal>`. Pattern per site:
    - Find: `<div className="modal-overlay" onClick={handler}>` wrapping modal-content, modal-header, modal-body, modal-footer
    - Replace: `<Modal title="..." open onClose={handler} style={{maxWidth:...}}>{body children}</Modal>`
    - The title text comes from the `<h3>` in modal-header div
    - Actions/buttons in modal-footer become `actions={[<Button>...]}` prop
    - If the modal has a close button, that's handled by Modal's closeIcon (default true)
    - If the footer has cancel/submit buttons, those go to `actions` prop
    - Each site's custom content (forms, lists, etc.) becomes Modal's children

    **All 15 inline sites** are in:
    - AdminDishesPage.jsx (3 sites: dish form at ~675, extract at ~918, add-ingredient at ~1013)
    - ChefDishesPage.jsx (3 sites: dish form at ~693, extract at ~941, add-ingredient at ~1038)
    - AdminIngredientsPage.jsx (2 sites: modal at ~478, parse at ~523)
    - OrderPage.jsx (2 sites: chef-picker at ~569, confirm at ~611)
    - AdminChefsPage.jsx (1 site: bind at ~168)
    - AdminCategoriesPage.jsx (1 site: modal at ~209)
    - AdminUsersPage.jsx (1 site: modal at ~270)
    - UserProfilePage.jsx (1 site: edit at ~194)

    **IMPORTANT**: For each inline site, add `import Modal from '../components/composites/Modal'` (add to top import block, grouped after component imports). Keep all existing business logic, event handlers, state — only the wrapping modal-overlay structure is replaced.

    **Common transformation for inline sites:**
    - `<div className="modal-overlay" onClick={handler}>` → `<Modal title="..." open onClose={handler} style={{maxWidth:NNN}}>`
    - `</div>` closing modal-overlay → `</Modal>`
    - Inner `<div className="modal-content/body/header/footer">` → remove wrapper divs, content is direct children of `<Modal>`
    - `<div className="modal-header"><h3>TITLE</h3><button className="modal-close">✕</button></div>` → Modal's `title` prop (close handled internally)
    - `<div className="modal-body">` → keep content, remove this wrapper div
    - `<div className="modal-footer">` with action buttons → `actions={[...]}` prop

    Import path: `../components/composites/Modal` from pages (one level up from pages), `../../components/composites/Modal` from components (two levels up). Use relative path that resolves correctly.
  </action>
  <verify>
    <automated>npm run build --prefix frontend 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    1. All 7 wrapper Modal components (ConfirmModal, WishFormModal, WishRejectModal, WishAdvanceModal, CreateLinkModal, InvitationsModal, ChefSelectModal) import `<Modal>` and are rewritten as thin wrappers
    2. All 15 inline `<div className="modal-overlay">` sites replaced with `<Modal>` components
    3. No file contains `className="modal-overlay"` — grep returns 0 hits (excluding comments)
    4. No file references `modal-content`, `modal-header`, `modal-body`, `modal-footer`, `modal-close` as classNames in JSX (may appear in import statements or comments — verify className usage only)
    5. Vite build completes with 0 errors
    6. Each wrapper component's exported props are unchanged (same props interface)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Delete legacy modal CSS from styles.css + grep verification</name>
  <read_first>
    @frontend/src/css/styles.css:402-412 (current modal-* selectors)
    @frontend/src/css/styles.css:529 (.list-item:focus-visible — keep for now, deleted in 11-03)
    @frontend/src/css/styles.css:541 (.modal-close:focus-visible — delete this)
    @frontend/src/css/styles.css:572-580 (global 48dp rule targeting .list-item, .tab-item, .pc-sidebar-item, .modal-close — delete just .modal-close from this selector)
    @frontend/src/css/styles.css:608-613 (.modal-close 48dp min-size rule — delete this entire block)
  </read_first>
  <action>
    Delete the following selectors from `frontend/src/css/styles.css`:

    1. **Entire modal block** (lines 402-412): Delete from `/* ═══ Modal ══ */` through and including `.modal-footer { ... }` — that's `.modal-overlay`, `.modal-content`, `.modal-header`, `.modal-header h3`, `.modal-close`, `.modal-close::before`, `.modal-close:hover::before`, `.modal-close:hover`, `.modal-body`, `.modal-footer`

    2. **`.modal-close:focus-visible`** (line 541): Delete the line `.modal-close:focus-visible { outline: var(--md-focus-ring-outer); outline-offset: 2px; }`

    3. **`.modal-close` from global 48dp rule** (lines 570-580): The selector list `button, a, input, select, textarea, [role="button"], [tabindex]:not([tabindex="-1"]), .list-item, .tab-item, .pc-sidebar-item, .header-back, .theme-toggle, .modal-close, .qty-stepper button, .menu-item, .chef-select-item, .wish-picker-item, .guest-add-btn, .preference-tag button, .quick-action` — remove `.modal-close` from this list

    4. **`.modal-close` 48dp padding compensation** (lines 608-613): Delete the entire block:
       ```css
       /* 4. modal-close: 原 32x32 → 48x48 hit area, padding:8px 维持视觉 */
       .modal-close {
         min-width: 48px;
         min-height: 48px;
         padding: 8px;
       }
       ```

    After deletions, verify with:
    ```bash
    grep -rn 'class="modal-overlay\|class="modal-content\|class="modal-header\|class="modal-body\|class="modal-footer\|class="modal-close' frontend/src/ --include='*.jsx' | grep -v '^[^:]*:[^:]*//'
    ```
    The grep should return **0 results** for modal className usage in JSX (excluding import statements/comments).

    **Do NOT** delete `.list-item`, `.tab-item`, `.pc-sidebar-item` from styles.css yet — those are cleaned up in 11-02 and 11-03 plans respectively. Only delete modal-related CSS in this task.
  </action>
  <verify>
    <automated>grep -rn 'className="modal-overlay\|className="modal-content\|className="modal-header\|className="modal-body\|className="modal-footer\|className="modal-close" frontend/src/ --include='*.jsx' 2>&1; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    1. `.modal-overlay`, `.modal-content`, `.modal-header`, `.modal-header h3`, `.modal-close`, `.modal-close::before`, `.modal-close:hover::before`, `.modal-close:hover`, `.modal-body`, `.modal-footer` no longer exist in styles.css
    2. `.modal-close:focus-visible` no longer exists in styles.css
    3. `.modal-close` removed from the global 48dp selector list (lines 570-580)
    4. `.modal-close` 48dp padding compensation block deleted
    5. `className="modal-overlay"` grep returns 0 results across all JSX
    6. Vite build completes with 0 errors
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Modal overlay → rendered content | Uncontrolled children render inside modal-body |
| ESC handler | Document-level keydown listener |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | XSS | Modal title/header slot | mitigate | Both title string and header slot content render via JSX `{}` expression, not dangerouslySetInnerHTML. React auto-escapes during reconciliation. |
| T-11-03 | Denial of Service | ESC + backdrop close | mitigate | ESC always fires onClose; caller responsible for `submitting` guard before destructive action. Pattern documented in ConfirmModal.jsx:46 (ESC blocked when confirming). |
| T-11-09 | Information Disclosure | Focus trap with no focusable | mitigate | Modal content div gets `tabIndex={-1}` fallback — focus lands on non-interactive container rather than escaping to background page. trapFocusWithin returns early if 0 focusable. |
| T-11-SC | Tampering | npm install (none) | accept | Phase 11 introduces zero new npm dependencies. All packages inherited from Phase 8/9/10. |
</threat_model>

<verification>
- [ ] `npm run build --prefix frontend` — Vite 0 error
- [ ] `grep -rn 'className="modal-overlay"' frontend/src/ --include='*.jsx'` — 0 results
- [ ] `grep -rn 'className="modal-content"' frontend/src/ --include='*.jsx'` — 0 results
- [ ] Manual: open app → ConfirmModal opens/closes with focus trap + ESC + scroll lock
</verification>

<success_criteria>
1. Modal.jsx + Modal.css created with all required props — variant, open, onClose, title, closeIcon, header, footer, actions, children, focus trap, ESC, scroll lock
2. All 22 Modal sites (7 wrapper + 15 inline) migrated — zero className="modal-overlay" references remain
3. styles.css contains zero modal-* selectors — deleted and verified via grep
4. Vite build passes (0 errors)
</success_criteria>

<output>
Create `.planning/phases/11-composite-navigation-components/11-01-SUMMARY.md` when done
</output>
