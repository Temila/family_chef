---
phase: 11-composite-navigation-components
plan: 03
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - frontend/src/contexts/ToastContext.jsx
  - frontend/src/App.jsx
  - frontend/src/components/composites/ListItem.jsx
  - frontend/src/components/composites/ListItem.css
  - frontend/src/components/composites/Divider.jsx
  - frontend/src/components/composites/Divider.css
  - frontend/src/pages/AdminHomePage.jsx
  - frontend/src/components/InvitationsModal.jsx
  - frontend/src/components/InvitationsSection.jsx
  - frontend/src/css/styles.css
autonomous: true
requirements:
  - COMPO-10
  - COMPO-11
  - COMPO-12
  - LOGIC-01
  - LOGIC-02
  - LOGIC-03
must_haves:
  truths:
    - "Snackbar shows as MD3 inverse-surface card with left 4dp color bar + icon + message + close button"
    - "Up to 3 Snackbars visible simultaneously; new pushes to top, old ones auto-dismiss with timer"
    - "Success/Info auto-dismiss at 4s, Warn/Error at 6s; hover pauses timer"
    - "ListItem renders 1/2/3-line variants with Leading/Content/Headline/Supporting/Trailing compound slots"
    - "ListItem trailing button click does NOT trigger row onClick (auto-stopPropagation)"
    - "Divider renders as 1px outline-variant hr with optional 56dp inset"
    - "3 existing list-item sites (AdminHomePage, InvitationsModal, InvitationsSection) use <ListItem>"
    - "styles.css contains zero .list-item* / .toast / .toast-success / .toast-error / @keyframes slideDown selectors"
  artifacts:
    - path: "frontend/src/contexts/ToastContext.jsx"
      provides: "Rewritten SnackbarContext with queue-of-3, 4s/6s by tone, timer cleanup, dismiss, useToast alias preserved"
      min_lines: 100
    - path: "frontend/src/components/composites/ListItem.jsx"
      provides: "Slot-based compound component with 5 sub-components, clickable opt-in, trailing stopPropagation, disabled"
      min_lines: 100
    - path: "frontend/src/components/composites/ListItem.css"
      provides: "1/2/3-line min-heights (48/64/88dp), 16dp gap, token-only, md-interactive state-layer"
      min_lines: 80
    - path: "frontend/src/components/composites/Divider.jsx"
      provides: "<Divider /> full-width and <Divider inset /> with 56dp left indent"
      min_lines: 20
    - path: "frontend/src/components/composites/Divider.css"
      provides: "1px solid --md-color-outline-variant, inset 56dp margin-left"
      min_lines: 10
  key_links:
    - from: "ToastContext.jsx (rewritten)"
      to: "App.jsx"
      via: "<SnackbarProvider> replaces <ToastProvider>"
    - from: "ListItem.jsx"
      to: "ListItem.css"
      via: "import './ListItem.css'"
    - from: "Divider.jsx"
      to: "Divider.css"
      via: "import './Divider.css'"
    - from: "AdminHomePage.jsx:168"
      to: "composites/ListItem"
      via: "uses <ListItem variant='2-line'> with Content/Headline/Supporting"
---

<objective>
重写 ToastContext 为 MD3 SnackbarContext（Rich tone + 队列）+ 创建 ListItem（1/2/3-line slot-based compound）+ Divider 组件，并迁移 3 个 list-item 消费站点的旧 className 用法。

Purpose：Snackbar 升级为 MD3 inverse-surface 卡片，4 个 tone（success/primary、warn/tertiary、error/error、info/secondary），左侧 4dp 色条。队列最多 3 条，4s/6s 自动消失。ListItem 改为 slot-based compound 组件，支持 1/2/3-line + Leading/Content/Headline/Supporting/Trailing 子组件 + 整行 onClick + trailing auto-stopPropagation。Divider 独立组件。3 个现有 .list-item 站点（AdminHomePage、InvitationsModal、InvitationsSection）迁移到 `<ListItem>`。
Output：ToastContext.jsx 重写 + ListItem.jsx/CSS + Divider.jsx/CSS + 3 个调用站点迁移 + styles.css 旧选择器删除
</objective>

<execution_context>
@/home/temila/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/temila/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-composite-navigation-components/11-CONTEXT.md
@.planning/phases/11-composite-navigation-components/11-PATTERNS.md

@frontend/src/contexts/ToastContext.jsx
@frontend/src/contexts/AuthContext.jsx
@frontend/src/App.jsx
@frontend/src/components/primitives/Card.jsx
@frontend/src/components/primitives/Ripple.jsx
@frontend/src/components/primitives/Badge.jsx
@frontend/src/components/primitives/IconButton.jsx
@frontend/src/components/primitives/Button.jsx
@frontend/src/components/primitives/base.css
@frontend/src/components/InvitationsModal.jsx
@frontend/src/components/InvitationsSection.jsx
@frontend/src/pages/AdminHomePage.jsx
@frontend/src/css/styles.css
@frontend/src/css/tokens.css
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite ToastContext.jsx as SnackbarContext with queue + timer + Rich tone visual</name>
  <read_first>
    @frontend/src/contexts/ToastContext.jsx (current single-toast, 3s timer — entire file is 50 lines)
    @frontend/src/contexts/AuthContext.jsx:7-66 (provider + hook pattern to replicate)
  </read_first>
  <action>
    **Rewrite** `frontend/src/contexts/ToastContext.jsx` in-place (D-08 — keep filename to minimize import changes).

    **Provider structure** — modeled on `AuthContext.jsx` provider pattern:
    - `import { createContext, useCallback, useContext, useRef, useState } from 'react';`
    - Change internal name from `ToastContext` to `SnackbarContext` (D-08 points this)
    - Change `ToastProvider` to `SnackbarProvider`
    - Keep `useToast` hook name as alias for backward compat (213 callsites)
    - Default export `SnackbarContext` (for compatibility)

    **Queue logic:**
    ```js
    const DURATION_BY_TYPE = { success: 4000, info: 4000, warn: 6000, error: 6000 };
    const MAX_VISIBLE = 3;
    let nextId = 0;
    ```

    **State:**
    - `const [items, setItems] = useState([])` — array of `{id, message, type, createdAt}`
    - `const timersRef = useRef(new Map())` — `id → timeoutId` mapping

    **dismiss(id):** `useCallback` — `setItems(prev => prev.filter(it => it.id !== id))`, clear/delete timer from ref map

    **showToast(message, type='success'):** `useCallback` — create `{ id: ++nextId, message, type, createdAt: Date.now() }`, `setItems(prev => { const next = [...prev, item]; return next.slice(-MAX_VISIBLE); })`, set `setTimeout(() => dismiss(id), DURATION_BY_TYPE[type] || 4000)`, store timer in ref map

    **value:** `{ showToast, dismiss }`

    **Render** — inside `<SnackbarContext.Provider value={value}>`:
    - `{children}`
    - If `items.length > 0`:
      ```jsx
      <div className="md-snackbar-stack" role="status" aria-live="polite">
        {[...items].reverse().map((it) => (
          <div key={it.id} className={`md-snackbar md-snackbar--${it.type}`}>
            <span className={`md-snackbar__bar md-snackbar__bar--${it.type}`} aria-hidden="true" />
            <span className="md-snackbar__message">{it.message}</span>
            <button
              type="button"
              className="md-snackbar__close md-interactive"
              onClick={() => dismiss(it.id)}
              aria-label="关闭通知"
            >
              ✕
            </button>
          </div>
        ))}
      </div>
      ```

    **Snackbar CSS** — add a `<style>` block within the file itself (minimizes file count). Since D-19 says Snackbar can be co-located CSS or separate, and it's `frontend/src/contexts/` not `composites/`, inline `<style>` keeps the context self-contained. Write the CSS as a template literal string and inject via `<style>{CSS_STRING}</style>` at the top of the render.

    **Snackbar CSS content:**
    - `.md-snackbar-stack`: `position:fixed; top:calc(env(safe-area-inset-top, 0px) + 80px); left:50%; transform:translateX(-50%); z-index:1000; display:flex; flex-direction:column; gap:8px; width:auto; max-width:480px; pointer-events:none`
    - `.md-snackbar`: `display:flex; align-items:center; gap:12px; background:var(--md-color-inverse-surface); color:var(--md-color-inverse-on-surface); padding:0; border-radius:var(--md-radius-sm); box-shadow:var(--md-elevation-3); min-height:48px; pointer-events:auto; overflow:hidden; animation:md-snackbar-in 0.3s var(--md-motion-easing-standard)`
    - `.md-snackbar__bar`: `width:4px; align-self:stretch; flex-shrink:0`
    - `.md-snackbar__bar--success`: `background:var(--md-color-primary)`
    - `.md-snackbar__bar--warn`: `background:var(--md-color-tertiary)`
    - `.md-snackbar__bar--error`: `background:var(--md-color-error)`
    - `.md-snackbar__bar--info`: `background:var(--md-color-secondary)`
    - `.md-snackbar__message`: `flex:1; padding:12px 0; font-size:0.875rem; line-height:1.4`
    - `.md-snackbar__close`: `border:none; background:transparent; color:var(--md-color-inverse-on-surface); cursor:pointer; border-radius:var(--md-radius-full); width:40px; height:40px; min-width:48px; min-height:48px; display:flex; align-items:center; justify-content:center; font-size:0.9rem; flex-shrink:0; margin-right:4px`
    - `.md-snackbar__close:hover`: `background:var(--md-color-inverse-on-surface)26` (16% opacity overlay on inverse-on-surface)
    - `@keyframes md-snackbar-in`: `from { opacity:0; transform:translateY(-10px) } to { opacity:1; transform:translateY(0) }`
    - `@media (prefers-reduced-motion:reduce)`: `.md-snackbar { animation:none }`
    - Mobile offset: on mobile, `top:calc(env(safe-area-inset-top, 0px) + 16px)` — can implement via a class added to body or media query. Simpler: keep `top:80px` and the existing `.toast` position — SnackbarStack uses fixed, top 80px matches old .toast position per D-06. Mobile safe-area via: add to `.md-snackbar-stack` — `@media (max-width:639px) { .md-snackbar-stack { top: calc(env(safe-area-inset-top, 0px) + 16px); } }`

    **Export changes:**
    - `export const useToast = () => { const ctx = useContext(SnackbarContext); if (!ctx) throw new Error('useToast must be used within a SnackbarProvider'); return ctx; };`
    - `export default SnackbarContext;`

    **Update** `frontend/src/App.jsx`:
    - Line 3: change `import { ToastProvider } from './contexts/ToastContext'` to `import { SnackbarProvider } from './contexts/ToastContext'`
    - Line 106: change `<ToastProvider>` to `<SnackbarProvider>`
    - Line 294: change `</ToastProvider>` to `</SnackbarProvider>`

    **Verify**: All 8 `import { useToast } from '../contexts/ToastContext'` call sites are unchanged by this rewrite (preserved filename + export name).

    **Do NOT** delete CSS selectors from styles.css yet — that happens in Task 3.
  </action>
  <behavior>
    - Test 1: showToast('test', 'success') creates a snackbar item with 4s auto-dismiss
    - Test 2: showToast called 5 times in sequence shows at most 3 items (MAX_VISIBLE=3 enforcement)
    - Test 3: dismiss(id) removes the item immediately
    - Test 4: warn/error type uses 6s duration
    - Test 5: useToast returns { showToast, dismiss } with same signature as before
  </behavior>
  <verify>
    <automated>npm run build --prefix frontend 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    1. ToastContext.jsx rewritten — SnackbarContext internal name, SnackbarProvider export, SnackbarContext default export
    2. useToast hook preserved as alias returning { showToast, dismiss }
    3. Queue enforces MAX_VISIBLE=3, new items added, oldest dropped when queue full
    4. Timer: success/info 4s, warn/error 6s; dismiss clears timer
    5. Snackbar renders MD3 inverse-surface card with left 4dp color bar per tone
    6. App.jsx uses `<SnackbarProvider>` instead of `<ToastProvider>`
    7. All 213 showToast callsites unchanged (same import path, same signature)
    8. Vite build completes with 0 errors
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create ListItem + Divider composite components with co-located CSS</name>
  <read_first>
    @frontend/src/components/primitives/Card.jsx (slot-based compound reference — forwardRef, isClickable, Ripple, role/tabIndex/keyboard)
    @frontend/src/components/primitives/Ripple.jsx (wrapper pattern for clickable items)
    @frontend/src/components/primitives/base.css (md-interactive state-layer shared pattern)
    @frontend/src/css/styles.css:147-154 (current .list-item* visual to replace)
  </read_first>
  <action>
    Create `frontend/src/components/composites/ListItem.jsx` per PATTERNS.md lines 1064-1174:
    - Import: `forwardRef` from react; `Ripple` from `../primitives/Ripple`; `./ListItem.css`
    - `const ListItem = forwardRef(function ListItem({ variant='1-line', onClick, disabled=false, as: As='div', className='', children, ...rest }, ref) {`
    - `const isClickable = Boolean(onClick) && !disabled`
    - **Classes**: `['md-list-item', \`md-list-item--${variant}\`, isClickable && 'md-list-item--clickable', disabled && 'md-list-item--disabled', className].filter(Boolean).join(' ')`
    - **Keyboard activation**: `handleKeyDown` — if `isClickable && (e.key==='Enter' || e.key===' ')` then `e.preventDefault(); onClick(e)`
    - **Render**: `<Ripple disabled={!isClickable}><As ref={ref} className={classes} onClick={isClickable?onClick:undefined} role={isClickable?'button':undefined} tabIndex={isClickable?0:undefined} aria-disabled={disabled||undefined} onKeyDown={handleKeyDown} {...rest}>{children}</As></Ripple>`
    - **Compound sub-components** (D-15):
      - `ListItem.Leading = ({ children, className='' }) => <div className={\`md-list-item__leading ${className}\`}>{children}</div>`
      - `ListItem.Content = ({ children, className='' }) => <div className={\`md-list-item__content ${className}\`}>{children}</div>`
      - `ListItem.Headline = ({ children, className='' }) => <div className={\`md-list-item__headline ${className}\`}>{children}</div>`
      - `ListItem.Supporting = ({ children, className='' }) => <div className={\`md-list-item__supporting ${className}\`}>{children}</div>`
      - `ListItem.Trailing = ({ children, onClick, className='', as: TAs='div', ...rest }) => { const handleClick = useCallback((e) => { e.stopPropagation(); onClick?.(e); }, [onClick]); return <TAs className={\`md-list-item__trailing ${className}\`} onClick={handleClick} {...rest}>{children}</TAs>; }`
    - Export default ListItem

    Create `frontend/src/components/composites/ListItem.css` per PATTERNS.md lines 1181-1265:
    - `@import '../primitives/base.css'`
    - `.md-list-item`: `display:flex; align-items:center; gap:16px; min-height:48px; padding:8px 16px; background:var(--md-color-surface); border-bottom:1px solid var(--md-color-outline-variant); position:relative; color:var(--md-color-on-surface); transition:background var(--md-motion-duration-short) var(--md-motion-easing-standard)`
    - `.md-list-item:last-child`: `border-bottom:none`
    - `.md-list-item--clickable`: `cursor:pointer`
    - `.md-list-item--clickable:focus-visible`: `outline:var(--md-focus-ring-outer); outline-offset:-2px`
    - `.md-list-item--disabled`: `opacity:var(--md-state-layer-disabled, 0.38); cursor:not-allowed; pointer-events:none`
    - **Variants** (D-15): 
      - `.md-list-item--1-line`: `min-height:48px; padding-top:8px; padding-bottom:8px`
      - `.md-list-item--2-line`: `min-height:64px; padding-top:12px; padding-bottom:12px`
      - `.md-list-item--3-line`: `min-height:88px; padding-top:16px; padding-bottom:16px`
    - **Slots**:
      - `.md-list-item__leading`: `width:40px; height:40px; border-radius:var(--md-radius-sm); display:flex; align-items:center; justify-content:center; flex-shrink:0; color:var(--md-color-on-surface-variant)`
      - `.md-list-item__content`: `flex:1; min-width:0; display:flex; flex-direction:column; gap:2px`
      - `.md-list-item__headline`: `font-size:0.95rem; font-weight:500; color:var(--md-color-on-surface); line-height:1.4; overflow:hidden; text-overflow:ellipsis; white-space:nowrap`
      - 2/3-line headline: `.md-list-item--2-line .md-list-item__headline, .md-list-item--3-line .md-list-item__headline { white-space:normal; display:-webkit-box; -webkit-line-clamp:2; -webkit-box-orient:vertical }`
      - `.md-list-item__supporting`: `font-size:0.8rem; color:var(--md-color-on-surface-variant); line-height:1.4`
      - `.md-list-item__trailing`: `display:flex; align-items:center; gap:4px; flex-shrink:0`

    Create `frontend/src/components/composites/Divider.jsx`:
    - Import: `./Divider.css`
    - Export default function Divider({ inset=false, className='', ...rest }):
      ```jsx
      const classes = ['md-divider', inset && 'md-divider--inset', className].filter(Boolean).join(' ');
      return <hr className={classes} role="separator" {...rest} />;
      ```

    Create `frontend/src/components/composites/Divider.css`:
    - `.md-divider`: `border:none; border-top:1px solid var(--md-color-outline-variant); margin:0; height:0`
    - `.md-divider--inset`: `margin-left:56px`
  </action>
  <behavior>
    - Test 1: `<ListItem variant='1-line'><ListItem.Content><ListItem.Headline>Title</ListItem.Headline></ListItem.Content></ListItem>` renders with 48dp min-height, single-line truncated headline
    - Test 2: `<ListItem variant='2-line' onClick={fn}>` renders as 64dp with cursor:pointer and role='button'
    - Test 3: Trailing button click does NOT trigger row onClick (stopPropagation)
    - Test 4: disabled ListItem has opacity 0.38 and pointer-events:none
    - Test 5: `<Divider />` renders as hr with border-top; `<Divider inset />` adds 56px margin-left
  </behavior>
  <verify>
    <automated>npm run build --prefix frontend 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    1. ListItem.jsx exists with 5 compound sub-components (Leading, Content, Headline, Supporting, Trailing)
    2. ListItem supports 3 line variants (1/2/3-line) with min-heights 48/64/88dp
    3. Trailing component auto-stopPropagation on click (D-16)
    4. Divider.jsx exists with full-width and inset (56dp) variants
    5. All CSS uses `var(--md-*)` tokens only — zero hardcoded colors
    6. Vite build completes with 0 errors
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Migrate 3 list-item call sites + delete legacy CSS and toast CSS from styles.css</name>
  <read_first>
    @frontend/src/pages/AdminHomePage.jsx:168-173 (uses .list-item, .list-item-name, .list-item-meta)
    @frontend/src/components/InvitationsModal.jsx:52-124 (uses .list-item with inline styles, Badge, IconButton, Button)
    @frontend/src/components/InvitationsSection.jsx:158-218 (uses .list-item with same pattern as InvitationsModal)
    @frontend/src/css/styles.css:146-154 (.list-item* selectors — full chain to delete)
    @frontend/src/css/styles.css:290-294 (.toast / .toast-success / .toast-error / @keyframes slideDown — delete)
    @frontend/src/css/styles.css:529 (.list-item:focus-visible — delete this line)
    @frontend/src/css/styles.css:572 (.list-item in global 48dp selector — delete this reference)
  </read_first>
  <action>
    **Part A: Migrate 3 list-item call sites to use `<ListItem>`**

    1. **AdminHomePage.jsx** (line ~168):
       - Add import: `import ListItem from '../components/composites/ListItem'` (at top, grouped with other imports)
       - Replace `<div key={index} className="list-item" style={{ cursor: 'default' }}>`...`</div>` block:
         ```jsx
         <ListItem key={index} variant="2-line" className="md-list-item--static">
           <ListItem.Content>
             <ListItem.Headline>{activity.action} - {activity.target_type}</ListItem.Headline>
             <ListItem.Supporting>{formatDate(activity.created_at)}</ListItem.Supporting>
           </ListItem.Content>
         </ListItem>
         ```
       - Note: `style={{ cursor: 'default' }}` indicates non-clickable — no `onClick` prop means `isClickable=false` by default, which gives default cursor. Remove the inline style.

    2. **InvitationsModal.jsx** (line ~52-124):
       - Add import: `import ListItem from '../../components/composites/ListItem'` (from components/)
       - Replace `<div key={inv.id} className="list-item" style={{ cursor: 'default' }}>`...`</div>` block:
         ```jsx
         <ListItem key={inv.id} variant="3-line">
           <ListItem.Leading>
             <Badge text={inv.status === 'active' ? '活跃' : undefined}
                    status={inv.status === 'active' ? undefined : inv.status}
                    type={inv.status === 'active' ? 'success' : undefined} />
           </ListItem.Leading>
           <ListItem.Content>
             <ListItem.Headline>{formatDate(inv.created_at)}</ListItem.Headline>
             <ListItem.Supporting>
               {user?.role !== 'chef' && inv.chef_name ? inv.chef_name : ''}
             </ListItem.Supporting>
           </ListItem.Content>
           <ListItem.Trailing>
             {inv.status === 'active' && (
               <>
                 <IconButton icon="content-copy" ariaLabel="复制链接" onClick={() => onCopyLink(inv.token)} />
                 <Button variant="outlined" size="sm"
                   style={{ borderColor: 'var(--md-color-error)', color: 'var(--md-color-error)' }}
                   onClick={() => { onRevoke(inv); onClose(); }}>
                   撤销
                 </Button>
               </>
             )}
           </ListItem.Trailing>
         </ListItem>
         ```
       - Remove the old inline structural divs (flex containers, gap, minWidth wrappers) — all handled by ListItem slots
       - Keep the same Badge Badge import (already imported from `../components/primitives/Badge` — change to `../../components/primitives/Badge` since composite files are in components/composites/ but the InvitationsModal is in components/, so import path becomes `../components/primitives/Badge`... wait. Let me verify. InvitationsModal.jsx is at `frontend/src/components/`. The ListItem import: `import ListItem from '../composites/ListItem'` — correct, it goes up from components/ to components/composites/.
       - Badge import: currently `import Badge from '../components/primitives/Badge'` — this works as-is since InvitationsModal.jsx is at `frontend/src/components/` and Badge is at `frontend/src/components/primitives/Badge` — wait, that import looks wrong already. From components/InvitationsModal.jsx, `../components/primitives/Badge` would resolve to parent of components/ then back into components/primitives/Badge. That would be `../../primitives/Badge`. This is a pre-existing pattern — just leave it as-is.

    3. **InvitationsSection.jsx** (line ~158-218):
       - Add import: `import ListItem from '../composites/ListItem'` (InvitationsSection is in components/)
       - Same transformation as InvitationsModal — replace `<div className="list-item">` block with `<ListItem variant="3-line">` using Leader (Badge), Content (Headline date, Supporting chef), Trailing (IconButton copy + Button revoke)
       - `onCopyLink` is `handleCopyLink(inv.token)` and `onRevoke` is `setRevokeTarget(inv)` — preserve these specific handlers

    **Part B: Delete legacy CSS from styles.css**

    1. **List-item block** (lines 146-154): Delete from `.list-item { ... }` through `.list-item-meta { ... }` — all 8 lines (146-154)
    2. **`.list-item:focus-visible`** (line 529): Delete line
    3. **Toast block** (lines 290-294): Delete `.toast`, `.toast-success`, `.toast-error`, `@keyframes slideDown`
    4. **`.list-item` in global 48dp rule** (line 572): Remove `.list-item,` from the selector list (so it reads `.tab-item, .pc-sidebar-item, ...` — but these will also be removed in 11-02. For now just remove `.list-item,`)

    Verify:
    ```bash
    grep -rn 'className="list-item' frontend/src/ --include='*.jsx' 2>&1
    grep -rn 'className="toast' frontend/src/ --include='*.jsx' 2>&1
    ```
    Both should return 0 results.
  </action>
  <verify>
    <automated>grep -rn 'className="list-item' frontend/src/ --include='*.jsx' 2>&1; echo "---"; grep -rn 'className="toast' frontend/src/ --include='*.jsx' 2>&1; echo "---"; npm run build --prefix frontend 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    1. AdminHomePage.jsx uses `<ListItem variant="2-line">` with Content/Headline/Supporting for activity items
    2. InvitationsModal.jsx uses `<ListItem variant="3-line">` with Leading (Badge), Content (Headline/Supporting), Trailing (IconButton/Button)
    3. InvitationsSection.jsx uses `<ListItem variant="3-line">` with identical slot pattern
    4. No `.list-item`, `.list-item-img`, `.list-item-info`, `.list-item-name`, `.list-item-meta` selectors remain in styles.css
    5. No `.toast`, `.toast-success`, `.toast-error`, `@keyframes slideDown` selectors remain in styles.css
    6. `.list-item` removed from global 48dp selector list in styles.css
    7. `.list-item:focus-visible` removed from styles.css
    8. grep for `className="list-item"` in JSX returns 0 results
    9. grep for `className="toast"` in JSX returns 0 results
    10. Vite build completes with 0 errors
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Snackbar message rendering | User-provided message string rendered via JSX {} |
| Snackbar timer | Timer manages auto-dismiss lifecycle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04 | XSS | Snackbar message | mitigate | React auto-escapes via `{it.message}` — no dangerouslySetInnerHTML. |
| T-11-07 | Tampering | ListItem Trailing + row onClick | mitigate | Trailing's `onClick` handler calls `e.stopPropagation()` before running own handler, preventing row-level onClick from firing. |
| T-11-08 | Denial of Service | Snackbar timer leak | mitigate | MAX_VISIBLE=3 prevents unbounded queue growth; every dismiss clears its timer via `timersRef.current.delete(id)`; provider unmount clears all timers if needed (add useEffect cleanup in SnackbarProvider). |
| T-11-10 | Information Disclosure | CSS legacy selector residue | mitigate | grep verify 0 residues (see verification section). |
| T-11-SC | Tampering | npm install (none) | accept | Phase 11 introduces zero new npm dependencies. |
</threat_model>

<verification>
- [ ] `npm run build --prefix frontend` — Vite 0 error
- [ ] `grep -rn 'className="list-item' frontend/src/ --include='*.jsx'` — 0 results
- [ ] `grep -rn 'className="toast' frontend/src/ --include='*.jsx'` — 0 results
- [ ] `grep -rn '\.list-item[^-]' frontend/src/css/styles.css` — 0 results (no list-item base selectors)
- [ ] Manual: Snackbar (success/warn/error/info) stacks + auto-dismiss + manual close
- [ ] Manual: ListItem with trailing button click doesn't trigger row onClick
</verification>

<success_criteria>
1. ToastContext.jsx rewritten as SnackbarContext with queue-of-3 + rich tone + 4s/6s timer
2. App.jsx uses `<SnackbarProvider>` (1 line change)
3. All 213 showToast callsites preserved unchanged
4. ListItem.jsx + Divider.jsx created with co-located CSS
5. 3 list-item call sites migrated to `<ListItem>` compound component
6. All legacy .list-item* / .toast* CSS deleted from styles.css
7. Vite build passes (0 errors)
</success_criteria>

<output>
Create `.planning/phases/11-composite-navigation-components/11-03-SUMMARY.md` when done
</output>
