---
phase: 03-frontend-authenticated
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - frontend/src/pages/UserHomePage.jsx
  - frontend/src/components/InvitationsSection.jsx
  - frontend/src/components/InvitationsModal.jsx
  - frontend/src/components/ChefSelectModal.jsx
  - frontend/src/components/CreateLinkModal.jsx
  - frontend/src/components/ConfirmModal.jsx
autonomous: true
requirements: [INV-04, INV-05, INV-06, INV-07]

must_haves:
  truths:
    - "用户首页在快速操作网格下方显示'邀请访客'区块"
    - "Chef 点击'创建邀请'一键生成 → 弹窗显示链接+复制+分享+2h提示"
    - "User 点击'创建邀请'→ 厨师选择弹窗 → 选定后 → 同一弹窗显示链接+复制+分享"
    - "创建链接弹窗内有复制按钮和 Web Share 按钮并排显示"
    - "复制到剪贴板后 Toast 提示'链接已复制到剪贴板'"
    - "Web Share 不可用时 Toast 提示降级到复制"
    - "邀请区块展示最近5条，每条含状态 Badge、创建时间、过期时间、厨师名(User角色)、复制图标、撤销按钮"
    - "仅活跃状态的邀请显示复制图标和撤销按钮（D-16, D-21）"
    - "撤销需要确认弹窗（非 window.confirm），确认后乐观更新状态为已撤销"
    - "撤销失败时回滚到之前状态并显示错误 Toast"
    - "'查看全部'打开全屏 Modal 展示所有邀请，Modal 内无创建按钮"
  artifacts:
    - path: "frontend/src/components/InvitationsSection.jsx"
      provides: "邀请管理区块组件 — 嵌入 UserHomePage，包含列表显示+创建/撤销流程"
      min_lines: 150
    - path: "frontend/src/components/InvitationsModal.jsx"
      provides: "全屏 Modal 展示所有邀请记录"
      min_lines: 40
    - path: "frontend/src/components/ChefSelectModal.jsx"
      provides: "User 角色选择厨师的 Modal"
      min_lines: 60
    - path: "frontend/src/components/CreateLinkModal.jsx"
      provides: "创建后显示链接+复制+分享+2h 提示的 Modal"
      min_lines: 70
    - path: "frontend/src/components/ConfirmModal.jsx"
      provides: "可复用的确认弹窗（撤销使用）"
      min_lines: 35
    - path: "frontend/src/pages/UserHomePage.jsx"
      provides: "嵌入 InvitationsSection"
      contains: "InvitationsSection"
  key_links:
    - from: "InvitationsSection"
      to: "src/api/client.js"
      via: "api.getInvitations, api.createInvitation, api.revokeInvitation"
      pattern: "api\\.(getInvitations|createInvitation|revokeInvitation)"
    - from: "InvitationsSection"
      to: "CreateLinkModal"
      via: "setNewLink → setShowCreateLink"
      pattern: "showCreateLink"
    - from: "InvitationsSection"
      to: "ChefSelectModal"
      via: "setShowChefSelect (user role)"
      pattern: "ChefSelectModal"
    - from: "InvitationsSection"
      to: "ConfirmModal"
      via: "revokeTarget state → setRevokeTarget"
      pattern: "revokeTarget"
    - from: "InvitationsSection"
      to: "InvitationsModal"
      via: "setShowFullList"
      pattern: "showFullList"
---

<objective>
构建完整的邀请管理 UI — 用户首页的邀请区块、创建邀请（含 Chef/User 双角色流程）、链接分享（复制 + Web Share）、邀请列表、撤销功能。

Purpose: 注册用户可以通过应用 UI 直观地创建、分享、查看和管理邀请链接
Output: 5 个新组件 + 1 个页面嵌入，覆盖 INV-04/05/06/07 全部需求
</objective>

<execution_context>
@/home/temila/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/temila/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-frontend-authenticated/03-CONTEXT.md (D-01 through D-21 — all invitation UI decisions)
@.planning/phases/03-frontend-authenticated/03-RESEARCH.md (Pattern 1-4, Code Examples 1-6, Anti-Patterns, Pitfalls)
@.planning/phases/03-frontend-authenticated/03-PATTERNS.md (all analog patterns — InvitationsSection, InvitationsModal, ChefSelectModal, CreateLinkModal, ConfirmModal)
@.planning/phases/03-frontend-authenticated/03-UI-SPEC.md (Component Inventory, Copywriting Contract, Interaction States, Color, Spacing)
@frontend/src/pages/UserHomePage.jsx (target page — embed InvitationsSection after menu grid)
@frontend/src/components/Badge.jsx (supports text+type props for invitation badges)
@frontend/src/api/client.js (has getInvitations/createInvitation/revokeInvitation from Plan 01)
@frontend/src/utils/index.js (statusBadge map has used/expired/revoked entries from Plan 01; formatDate available)
@frontend/src/css/styles.css (modal CSS at 376-384, chef-select-item at 409-411, badge classes at 111-117)
@frontend/src/contexts/ToastContext.jsx (showToast for copy/revoke/create feedback)
@frontend/src/components/Loading.jsx (reusable loading spinner)
@frontend/src/components/EmptyState.jsx (reusable empty state display)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create InvitationsSection + InvitationsModal + embed in UserHomePage</name>
  <files>
    frontend/src/components/InvitationsSection.jsx (NEW)
    frontend/src/components/InvitationsModal.jsx (NEW)
    frontend/src/pages/UserHomePage.jsx (MODIFY)
  </files>
  <read_first>
    @frontend/src/pages/ChefOrdersPage.jsx (data loading + list rendering pattern — useState/useEffect/loading/error)
    @frontend/src/pages/UserHomePage.jsx (existing structure — header, menu grid, bottom bar; add InvitationsSection after line 51, before BottomBar)
    @frontend/src/css/styles.css (section class at 136-137, list-item at 146-152, badge classes at 111-117, filter-chips at 122-126, modal at 376-384)
    @frontend/src/components/Badge.jsx (behavior: text+type props for invitation statuses)
    .planning/phases/03-frontend-authenticated/03-PATTERNS.md (InvitationsSection pattern at 674-805, InvitationsModal pattern at 809-840, section header at 749-770, row layout at 774-804)
    .planning/phases/03-frontend-authenticated/03-UI-SPEC.md (Empty state copy: "还没有邀请记录", Interaction states)
    .planning/phases/03-frontend-authenticated/03-RESEARCH.md (Pattern 1: Invitation Section with Modal Layering at 304-355, Pattern 3: Invitation Row Layout at 387-399)
  </read_first>
  <action>
    Create two new component files and modify one existing page.

    FILE 1 — frontend/src/components/InvitationsSection.jsx (NEW)
    This is the main component. It owns invitation data state and controls all modal visibility.

    Imports:
    - useState, useEffect from 'react'
    - useAuth from '../contexts/AuthContext'
    - useToast from '../contexts/ToastContext'
    - api from '../api/client'
    - Badge from '../components/Badge'
    - Loading from '../components/Loading'
    - EmptyState from '../components/EmptyState'
    - formatDate from '../utils'
    - InvitationsModal from './InvitationsModal' (created in this task)
    - ChefSelectModal from './ChefSelectModal' (created in Task 2)
    - CreateLinkModal from './CreateLinkModal' (created in Task 2)
    - ConfirmModal from './ConfirmModal' (created in Task 2)

    State variables:
    - invitations: array — loaded from API
    - loading: bool — initial loading state
    - showFullList: bool — controls InvitationsModal
    - showChefSelect: bool — controls ChefSelectModal (User role)
    - showCreateLink: bool — controls CreateLinkModal
    - newLink: string|null — the created link URL
    - revokeTarget: object|null — invitation being revoked, controls ConfirmModal

    loadInvitations function:
    - Set loading true → try api.getInvitations({ page: 1, page_size: 50 }) → setInvitations(res.items || [])
    - Catch: showToast('加载邀请列表失败', 'error') → finally: setLoading false
    - Called from useEffect on mount (empty deps).

    handleCreateClick function:
    - If user.role === 'chef': call handleCreateInvitation() immediately (no chef_id param)
    - If user.role === 'user': setShowChefSelect(true)

    handleCreateInvitation async (chefId?):
    - try: const res = await api.createInvitation(chefId)
    - setNewLink(window.location.origin + '/guest/' + res.token)
    - setShowChefSelect(false), setShowCreateLink(true)
    - Show toast: '邀请链接已创建'
    - await loadInvitations() — refresh list to include new invitation
    - catch: showToast('创建邀请失败，请稍后重试', 'error')

    handleCopyLink async (token):
    - const url = window.location.origin + '/guest/' + token
    - try: await navigator.clipboard.writeText(url); showToast('链接已复制到剪贴板')
    - catch: showToast('复制失败，请手动复制', 'error')

    handleRevoke async (invitationId):
    - const prevInvitations = [...invitations] (save for rollback)
    - Optimistic update: setInvitations(prev => prev.map(inv => inv.id === invitationId ? { ...inv, status: 'revoked' } : inv))
    - Close confirm modal: setRevokeTarget(null)
    - try: await api.revokeInvitation(invitationId); showToast('邀请已撤销')
    - catch: setInvitations(prevInvitations); showToast('撤销失败，请稍后重试', 'error')

    handleCloseCreateLink:
    - setShowCreateLink(false), setNewLink(null)
    - Call loadInvitations() to refresh the list

    Component render structure:
    ```
    <section className="section">
      <div className="section-title" style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center' }}>
        <span>邀请访客</span>
        <div style={{ display: 'flex', gap: 8, alignItems: 'center' }}>
          <button className="btn btn-primary btn-sm" onClick={handleCreateClick}>创建邀请</button>
          {invitations.length > 0 && (
            <button onClick={() => setShowFullList(true)} style={{ fontSize: '0.85rem', color: 'var(--accent)', background: 'none', border: 'none', cursor: 'pointer' }}>
              查看全部 ›
            </button>
          )}
        </div>
      </div>

      {loading ? <Loading /> : invitations.length === 0 ? (
        <EmptyState icon="📭" text="还没有邀请记录" subtext="创建邀请链接，让来做客的朋友提前点好想吃的菜" />
      ) : (
        invitations.slice(0, 5).map(inv => (
          <div key={inv.id} className="list-item" style={{ cursor: 'default' }}>
            <div style={{ display: 'flex', alignItems: 'center', gap: 8, flex: 1, minWidth: 0 }}>
              <Badge text={inv.status === 'active' ? '活跃' : undefined} status={inv.status === 'active' ? undefined : inv.status} type={inv.status === 'active' ? 'success' : undefined} />
              <div style={{ fontSize: '0.75rem', color: 'var(--text-muted)', whiteSpace: 'nowrap' }}>
                {formatDate(inv.created_at)}
              </div>
              {user?.role !== 'chef' && inv.chef_name && (
                <div style={{ fontSize: '0.75rem', color: 'var(--text-secondary)' }}>
                  {inv.chef_name}
                </div>
              )}
            </div>
            <div style={{ display: 'flex', alignItems: 'center', gap: 4, flexShrink: 0 }}>
              {inv.status === 'active' && (
                <>
                  <button className="btn-icon" onClick={() => handleCopyLink(inv.token)} title="复制链接" style={{ cursor: 'pointer' }}>📋</button>
                  <button className="btn btn-outline btn-sm" style={{ borderColor: 'var(--danger)', color: 'var(--danger)' }} onClick={() => setRevokeTarget(inv)}>撤销</button>
                </>
              )}
            </div>
          </div>
        ))
      )}
    </section>

    {/* Modals */}
    {showFullList && (
      <InvitationsModal
        invitations={invitations}
        loading={loading}
        onClose={() => setShowFullList(false)}
        onRevoke={(inv) => setRevokeTarget(inv)}
        onCopyLink={handleCopyLink}
        user={user}
      />
    )}
    {showChefSelect && (
      <ChefSelectModal
        onSelect={(chef) => handleCreateInvitation(chef.id)}
        onClose={() => setShowChefSelect(false)}
      />
    )}
    {showCreateLink && newLink && (
      <CreateLinkModal linkUrl={newLink} onClose={handleCloseCreateLink} />
    )}
    {revokeTarget && (
      <ConfirmModal
        title="撤销邀请"
        message="确定要撤销这条邀请链接吗？撤销后邀请将立即失效，访客无法继续使用此链接点菜。"
        confirmText="确定撤销"
        danger
        onConfirm={() => handleRevoke(revokeTarget.id)}
        onCancel={() => setRevokeTarget(null)}
      />
    )}
    ```

    Note for Badge usage:
    - For 'active' status: use `<Badge text="活跃" type="success" />` because the statusBadge map's 'active' entry is used by AdminChefsPage for chef active/inactive display (the map entry shows "启用" not "活跃").
    - For 'used', 'expired', 'revoked': use `<Badge status={inv.status} />` which maps to entries added in Plan 01.

    FILE 2 — frontend/src/components/InvitationsModal.jsx (NEW)
    Full-screen modal showing all invitations. Props: invitations, loading, onClose, onRevoke, onCopyLink, user.

    Imports:
    - useEffect from 'react'
    - Badge, Loading, EmptyState, formatDate (same as section)

    Component:
    - Prevent body scroll with useEffect: document.body.style.overflow = 'hidden'; cleanup: restore
    - Render full-screen modal:
      `<div className="modal-overlay" onClick={onClose} style={{ alignItems: 'flex-start', paddingTop: 0 }}>
        <div className="modal-content" style={{ maxWidth: '100%', height: '100vh', borderRadius: 0 }} onClick={e => e.stopPropagation()}>
          <div className="modal-header">
            <h3>邀请记录</h3>
            <button className="modal-close" onClick={onClose}>✕</button>
          </div>
          <div className="modal-body" style={{ padding: 0 }}>
            {loading ? <Loading /> : invitations.length === 0 ? (
              <EmptyState icon="📭" text="还没有邀请记录" />
            ) : (
              invitations.map(inv => (
                // Same row layout as InvitationsSection — render all invitations
                <div key={inv.id} className="list-item" style={{ cursor: 'default' }}>
                  <div style={{ display: 'flex', alignItems: 'center', gap: 8, flex: 1, minWidth: 0 }}>
                    <Badge text={inv.status === 'active' ? '活跃' : undefined} status={inv.status === 'active' ? undefined : inv.status} type={inv.status === 'active' ? 'success' : undefined} />
                    <div style={{ fontSize: '0.75rem', color: 'var(--text-muted)' }}>
                      {formatDate(inv.created_at)}
                    </div>
                    {user?.role !== 'chef' && inv.chef_name && (
                      <div style={{ fontSize: '0.75rem', color: 'var(--text-secondary)' }}>
                        {inv.chef_name}
                      </div>
                    )}
                  </div>
                  <div style={{ display: 'flex', alignItems: 'center', gap: 4, flexShrink: 0 }}>
                    {inv.status === 'active' && (
                      <>
                        <button className="btn-icon" onClick={() => onCopyLink(inv.token)} title="复制链接" style={{ cursor: 'pointer' }}>📋</button>
                        <button className="btn btn-outline btn-sm" style={{ borderColor: 'var(--danger)', color: 'var(--danger)' }} onClick={() => { onRevoke(inv); onClose(); }}>撤销</button>
                      </>
                    )}
                  </div>
                </div>
              ))
            )}
          </div>
        </div>
      </div>`
    - IMPORTANT: there is NO "创建邀请" button inside this modal (per D-04)

    FILE 3 — frontend/src/pages/UserHomePage.jsx (MODIFY)
    Two changes:
    a) Add import at top: `import InvitationsSection from '../components/InvitationsSection';` (after existing imports)
    b) Add `<InvitationsSection />` between the menu grid div (line 51 close) and `<BottomBar />` (line 53). Insert:
      ```
      <InvitationsSection />
      ```
      After the closing `</div>` of the menu grid (line 51) and before `<BottomBar />` (line 53).
  </action>
  <verify>
    <automated>
      cd /home/temila/family_chef && node -e "
const fs = require('fs');
const section = fs.readFileSync('frontend/src/components/InvitationsSection.jsx','utf8');
const modal = fs.readFileSync('frontend/src/components/InvitationsModal.jsx','utf8');
const homePage = fs.readFileSync('frontend/src/pages/UserHomePage.jsx','utf8');

// InvitationsSection
assert(section.includes('export default function InvitationsSection'), 'Missing section export');
assert(section.includes('showFullList'), 'Missing full list state');
assert(section.includes('ChefSelectModal'), 'Missing ChefSelectModal import');
assert(section.includes('CreateLinkModal'), 'Missing CreateLinkModal import');
assert(section.includes('ConfirmModal'), 'Missing ConfirmModal import');
assert(section.includes('handleCreateClick'), 'Missing create handler');
assert(section.includes('handleCopyLink'), 'Missing copy handler');
assert(section.includes('handleRevoke'), 'Missing revoke handler');
assert(section.includes('optimistic'), 'Missing optimistic update');
assert(section.includes('还没有邀请记录'), 'Missing empty state text');

// InvitationsModal
assert(modal.includes('export default function InvitationsModal'), 'Missing modal export');
assert(modal.includes('document.body.style.overflow'), 'Missing body scroll lock');
assert(!modal.includes('创建邀请'), 'Modal should NOT have create button (D-04)');

// UserHomePage
assert(homePage.includes('InvitationsSection'), 'Missing InvitationsSection import/usage');

console.log('All assertions passed');
function assert(cond, msg) { if (!cond) { console.error('FAIL: ' + msg); process.exit(1); } }
"</automated>
  </verify>
  <acceptance_criteria>
    - InvitationsSection.jsx 导出函数组件，管理邀请列表和所有 Modal 状态
    - InvitationsSection 包含 loadInvitations、handleCreateClick、handleCreateInvitation、handleCopyLink、handleRevoke 处理函数
    - InvitationsModal.jsx 导出全屏 Modal 组件，无创建按钮
    - UserHomePage.jsx 导入并嵌入 InvitationsSection
    - 所有条件渲染的 Modal 导入引用在文件中
    - 空状态使用 "还没有邀请记录" 文案
  </acceptance_criteria>
  <done>用户首页展示邀请区块，可查看最近5条邀请，查看全部跳转全屏 Modal</done>
</task>

<task type="auto">
  <name>Task 2: Create ChefSelectModal, CreateLinkModal, and ConfirmModal components</name>
  <files>
    frontend/src/components/ChefSelectModal.jsx (NEW)
    frontend/src/components/CreateLinkModal.jsx (NEW)
    frontend/src/components/ConfirmModal.jsx (NEW)
  </files>
  <read_first>
    @frontend/src/css/styles.css (modal patterns at 376-384 — modal-overlay, modal-content, modal-header, modal-close, modal-body, modal-footer; chef-select-item at 409-411, btn classes at 79-91, flex-1 utility)
    @frontend/src/components/Badge.jsx (pattern for component structure)
    @frontend/src/contexts/ToastContext.jsx (showToast for copy and share feedback)
    @frontend/src/api/client.js (getChefs available at line 250-252)
    .planning/phases/03-frontend-authenticated/03-PATTERNS.md (ChefSelectModal at 849-899, CreateLinkModal at 909-970, ConfirmModal at 979-1006)
    .planning/phases/03-frontend-authenticated/03-RESEARCH.md (Code Example 5: Clipboard Copy + Web Share at 575-610)
    .planning/phases/03-frontend-authenticated/03-UI-SPEC.md (Copywriting Contract for all strings)
  </read_first>
  <action>
    Create three modal component files. Each follows the existing modal CSS pattern.

    FILE 1 — frontend/src/components/ChefSelectModal.jsx (NEW)
    Props: onSelect(chef), onClose

    Imports: useState, useEffect from 'react'; api from '../api/client'; useToast; Loading; EmptyState.

    Component logic:
    - State: chefs array, loading bool
    - useEffect on mount: load chefs via api.getChefs()
      - Response handling: const list = Array.isArray(res) ? res : (res.items || [])
      - On error: showToast('加载厨师列表失败', 'error')
    - Render modal overlay + content:
      - Header: "选择厨师" + close button (✕)
      - Body:
        - Loading state: show loading spinner
        - Empty state: <EmptyState icon="👨‍🍳" text="暂无可用厨师" />
        - Chef list: map over chefs with .chef-select-item layout
          Each item: avatar (first char of display_name in accent gradient circle) + chef display_name/username
          On click: onSelect(chef) — auto-proceeds to create invitation
      - No footer needed (selection auto-proceeds)

    FILE 2 — frontend/src/components/CreateLinkModal.jsx (NEW)
    Props: linkUrl (string), onClose

    Imports: useToast from ToastContext.

    Component logic:
    - handleCopy: async → navigator.clipboard.writeText(linkUrl) → showToast('链接已复制到剪贴板')
      → catch → showToast('复制失败，请手动复制', 'error')
    - handleShare: async → if typeof navigator.share === 'function':
      try: navigator.share({ title: '家味 · Family Chef', text: '来点菜吧！', url: linkUrl })
      catch: (user cancelled — no feedback)
      else: showToast('当前浏览器不支持分享功能，请使用复制链接', 'error')

    Render:
    - Header: "邀请链接已创建" + close button
    - Body:
      - Link URL display: monospace font, wordBreak: 'break-all', bg-elevated background, 12px padding
      - "2小时内有效" muted text
      - Two side-by-side buttons (flex, gap 12):
        - 📋 复制链接 (btn-primary, flex-1)
        - 🚀 分享 (btn-outline, flex-1)
    - Footer: "完成" button (btn-secondary) → calls onClose

    IMPORTANT: Copied toast D-14 says show toast only (no button state change). Web Share + copy buttons side by side per D-15.

    FILE 3 — frontend/src/components/ConfirmModal.jsx (NEW)
    Props: title (string), message (string), confirmText (string, default "确定"), cancelText (string, default "取消"), onConfirm, onCancel, danger (boolean, default false)

    Render:
    - Modal overlay (onClick=onCancel)
    - Modal content (maxWidth 360, stopPropagation)
    - Header: title + close button (close calls onCancel)
    - Body: message in text-secondary, line-height 1.6
    - Footer: cancel button (btn-secondary) + confirm button
      - If danger: confirm button uses btn-outline with borderColor/style set to var(--danger) and color var(--danger)
      - If not danger: confirm button uses btn-primary
    - No useEffect for body scroll needed (parent InvitationsSection handles modal orchestration)
  </action>
  <verify>
    <automated>
      cd /home/temila/family_chef && node -e "
const fs = require('fs');
const chefModal = fs.readFileSync('frontend/src/components/ChefSelectModal.jsx','utf8');
const createModal = fs.readFileSync('frontend/src/components/CreateLinkModal.jsx','utf8');
const confirmModal = fs.readFileSync('frontend/src/components/ConfirmModal.jsx','utf8');

// ChefSelectModal
assert(chefModal.includes('ChefSelectModal'), 'Missing ChefSelectModal export');
assert(chefModal.includes('选择厨师'), 'Missing header title');
assert(chefModal.includes('暂无可用厨师'), 'Missing empty state');

// CreateLinkModal
assert(createModal.includes('CreateLinkModal'), 'Missing CreateLinkModal export');
assert(createModal.includes('邀请链接已创建'), 'Missing header');
assert(createModal.includes('复制链接'), 'Missing copy button');
assert(createModal.includes('分享'), 'Missing share button');
assert(createModal.includes('2小时内有效'), 'Missing expiry hint');
assert(createModal.includes('navigator.share'), 'Missing Web Share API');

// ConfirmModal
assert(confirmModal.includes('ConfirmModal'), 'Missing ConfirmModal export');
assert(confirmModal.includes('danger'), 'Missing danger prop handling');
assert(confirmModal.includes('确定'), 'Missing default confirm text');

console.log('All modal assertions passed');
function assert(cond, msg) { if (!cond) { console.error('FAIL: ' + msg); process.exit(1); } }
"</automated>
  </verify>
  <acceptance_criteria>
    - ChefSelectModal: 加载厨师列表，点击厨师后调用 onSelect，有加载和空状态
    - CreateLinkModal: 显示链接、复制按钮(Clipboard API)、分享按钮(Web Share API)、2小时提示
    - CreateLinkModal: 复制成功后 toast "链接已复制到剪贴板"（无按钮状态变化），分享不支持时 toast 降级提示
    - ConfirmModal: 通用确认弹窗，支持 danger 模式（红色按钮），接收 title/message/confirmText/cancelText
    - 所有 Modal 遵循现有 CSS 模式（modal-overlay → modal-content → modal-header/body/footer）
  </accomplished>
  <done>所有四个 Modal 组件完成，可供 InvitationsSection 导入使用</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Clipboard API | Copy-to-clipboard — user gesture required, no security risk |
| Browser → Web Share API | OS-level share sheet — user selects target, no data exfiltration risk |
| Browser → API (JWT) | Invitation creation/listing/revocation via JWT-authenticated endpoints |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Information Disclosure | Clipboard paste | accept | Link URLs contain UUID4 tokens — even if leaked, tokens are one-time use and expire in 2h. Risk of unauthorized order placement is mitigated by the one-time + expiry guards from Phase 2. |
| T-03-06 | Spoofing | Web Share API | accept | Web Share is a browser-native API. The URL is user-facing and shared intentionally. No injection risk — link is constructed client-side from `window.location.origin + token`, never from user input. |
| T-03-07 | Information Disclosure | Optimistic revoke rollback | mitigate | On revoke failure, the optimistic update is reverted by restoring `prevInvitations` array. The user sees the correct pre-revoke state. The API endpoint also enforces ownership per T-03-02. |
| T-03-SC | Tampering | npm packages | mitigate | No new packages required — all components use existing React 19, no npm install needed. |
</threat_model>

<verification>
- [ ] InvitationsSection 导入并嵌入 UserHomePage，在 menu grid 和 BottomBar 之间
- [ ] InvitationsSection 中的 loadInvitations 用 API 加载邀请列表
- [ ] Chef / User 角色创建流程分支正确（Chef 一键创建，User 先选择厨师）
- [ ] CreateLinkModal 中复制和分享按钮正常工作
- [ ] 乐观撤销：立即更新 UI，失败时回滚
- [ ] 全屏 InvitationsModal 无创建按钮
- [ ] 所有文案符合 Copywriting Contract
</verification>

<success_criteria>
- 用户首页展示邀请区块，包含最近5条邀请记录
- "创建邀请"一键生成或选择厨师后生成链接弹窗
- 链接弹窗包含复制 + Web Share + 2h提示 + 完成按钮
- 每行活跃邀请显示复制图标和撤销按钮
- 撤销显示确认弹窗，确认后乐观更新
- "查看全部"打开全屏邀请列表 Modal（无创建按钮）
- 列表倒序排列（最新在前）
</success_criteria>

<output>
Create `.planning/phases/03-frontend-authenticated/03-02-SUMMARY.md` when done
</output>
