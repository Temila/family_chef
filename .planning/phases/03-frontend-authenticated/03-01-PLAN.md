---
phase: 03-frontend-authenticated
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/guest_service.py
  - backend/app/schemas/guest.py
  - backend/app/routers/guest.py
  - backend/app/schemas/order.py
  - backend/app/routers/orders.py
  - frontend/src/api/client.js
  - frontend/src/utils/index.js
  - frontend/src/css/styles.css
  - frontend/src/pages/AdminChefsPage.jsx
  - frontend/src/pages/ChefOrdersPage.jsx
  - frontend/src/pages/OrderDetailPage.jsx
autonomous: true
requirements: [NOTIF-02]

must_haves:
  truths:
    - "GuestService 支持列出创建者自己的邀请列表和撤销邀请"
    - "GET /api/guest/invitations 返回分页的邀请列表，每条包含厨师名称"
    - "PUT /api/guest/invitations/{id}/revoke 验证所有权后才更新 status 为 revoked"
    - "订单列表和订单详情的 API 响应包含 is_guest: bool 字段"
    - "厨师在订单列表的卡片头部 Order ID 旁看到橙色'访客订单' Badge"
    - "厨师的订单筛选栏有'访客订单'筛选芯片"
    - "订单详情页的订单标题旁显示橙色'访客订单' Badge"
    - "前端 API 客户端支持 getInvitations / createInvitation / revokeInvitation"
    - "statusBadge 映射包含 invitation 状态: active(活跃) / used(已使用) / expired(已过期) / revoked(已撤销)"
    - "AdminChefsPage 显示厨师状态的 Badge 不受影响（启用/停用）"
  artifacts:
    - path: "backend/app/services/guest_service.py"
      provides: "list_invitations 和 revoke_invitation 方法"
      contains: "list_invitations.*def|revoke_invitation.*def"
    - path: "backend/app/schemas/guest.py"
      provides: "GuestInvitationListResponse 含 chef_name"
      contains: "chef_name"
    - path: "backend/app/routers/guest.py"
      provides: "GET /invitations 和 PUT /{id}/revoke 端点"
      contains: "@router.get.*invitations|@router.put.*revoke"
    - path: "backend/app/schemas/order.py"
      provides: "is_guest 字段在 OrderListResponse 和 OrderDetailResponse"
      contains: "is_guest.*bool"
    - path: "backend/app/routers/orders.py"
      provides: "is_guest=order.guest_invitation_id is not None 的赋值"
      contains: "is_guest"
    - path: "frontend/src/api/client.js"
      provides: "getInvitations、createInvitation、revokeInvitation 方法"
      contains: "getInvitations|createInvitation|revokeInvitation"
    - path: "frontend/src/utils/index.js"
      provides: "扩展的 statusBadge 含 active/used/expired/revoked"
      contains: "used.*已使用"
    - path: "frontend/src/css/styles.css"
      provides: ".badge-muted CSS class"
      contains: "badge-muted"
    - path: "frontend/src/pages/ChefOrdersPage.jsx"
      provides: "访客订单 Badge + 筛选芯片"
      contains: "is_guest.*访客订单"
    - path: "frontend/src/pages/OrderDetailPage.jsx"
      provides: "订单详情页的访客订单 Badge"
      contains: "is_guest.*访客订单"
  key_links:
    - from: "backend/app/routers/guest.py"
      to: "backend/app/services/guest_service.py"
      via: "guest_service.list_invitations / guest_service.revoke_invitation"
      pattern: "guest_service\\.(list_invitations|revoke_invitation)"
    - from: "backend/app/routers/orders.py"
      to: "backend/app/models/order.py"
      via: "order.guest_invitation_id is not None"
      pattern: "guest_invitation_id"
    - from: "frontend/src/pages/ChefOrdersPage.jsx"
      to: "frontend/src/api/client.js"
      via: "api.getOrders() returns order.is_guest"
      pattern: "is_guest"
---

<objective>
建立 Phase 3 的后端支持: 邀请列表查询、邀请撤销、订单访客标识字段，以及前端的 API 集成和访客订单可视化。

Purpose: 为邀请管理 UI 和访客订单标识提供完整的后端支撑和前端基础设施
Output: 后端两个新端点（列表/撤销）+ 订单 is_guest 字段 + 前端 API 客户端扩展 + 访客订单 Badge 集成到厨师视图
</objective>

<execution_context>
@/home/temila/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/temila/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/PROJECT.md
@.planning/phases/03-frontend-authenticated/03-CONTEXT.md
@.planning/phases/03-frontend-authenticated/03-RESEARCH.md
@.planning/phases/03-frontend-authenticated/03-PATTERNS.md
@.planning/phases/03-frontend-authenticated/03-UI-SPEC.md
@.planning/phases/01-data-foundation/01-01-SUMMARY.md
@.planning/phases/02-backend-core/02-01-SUMMARY.md
@.planning/phases/02-backend-core/02-02-SUMMARY.md
@backend/app/services/guest_service.py
@backend/app/schemas/guest.py
@backend/app/routers/guest.py
@backend/app/schemas/order.py
@backend/app/routers/orders.py
@backend/app/models/guest_invitation.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: GuestService + Schemas + Router — invitation list & revocation APIs</name>
  <files>
    backend/app/services/guest_service.py
    backend/app/schemas/guest.py
    backend/app/routers/guest.py
  </files>
  <read_first>
    @backend/app/services/guest_service.py (existing create_invitation, validate_invitation patterns — mimic @staticmethod + ValueError error handling)
    @backend/app/schemas/guest.py (existing GuestInvitationResponse schema — extends with chef_name for list response)
    @backend/app/routers/guest.py (existing POST /invitations and /{token}/dishes patterns — mimic require_role + Dependency + try/except ValueError)
    @backend/app/models/guest_invitation.py (GuestInvitation.columns: id, token, inviter_id, chef_id, status, expires_at, created_at; relationships: inviter, chef)
    .planning/phases/02-backend-core/02-01-SUMMARY.md (existing GuestService patterns, lazy expiry, ownership validation)
    .planning/phases/02-backend-core/02-02-SUMMARY.md (existing GuestService atomic patterns, error handling)
  </read_first>
  <action>
    Create three changes spreading across three files:

    FILE 1 — backend/app/schemas/guest.py
    Add GuestInvitationListResponse schema (not modify existing GuestInvitationResponse). This new schema extends the fields of the existing response with chef_name. Fields: id (int), token (str), inviter_id (int), chef_id (int), chef_name (Optional[str], default None), status (str), expires_at (datetime), created_at (datetime), Config.from_attributes = True. The chef_name is populated via eager-loading the GuestInvitation.chef relationship and reading chef.display_name.

    FILE 2 — backend/app/services/guest_service.py
    Add two new @staticmethod methods to GuestService class (before the module-level guest_service = GuestService() line):

    Method 1: list_invitations(db: AsyncSession, inviter_id: int, params: PaginationParams) -> tuple[list[GuestInvitation], int]
    - Import func from sqlalchemy for count
    - Count query: select(func.count()).select_from(GuestInvitation).where(GuestInvitation.inviter_id == inviter_id)
    - Data query: select(GuestInvitation).where(GuestInvitation.inviter_id == inviter_id)
      .options(selectinload(GuestInvitation.chef)) — eager load chef for chef_name
      .order_by(GuestInvitation.created_at.desc())
      .offset(params.offset).limit(params.limit)
    - Return: list(result.scalars().all()), total

    Method 2: revoke_invitation(db: AsyncSession, invitation_id: int, current_user_id: int) -> GuestInvitation
    - Query: select(GuestInvitation).where(GuestInvitation.id == invitation_id)
    - If not invitation: raise ValueError("邀请不存在")
    - If invitation.inviter_id != current_user_id: raise ValueError("无权撤销此邀请")
    - If invitation.status != "active": raise ValueError("仅活跃状态的邀请可撤销")
    - Lazy expiry check: if invitation.expires_at < datetime.now(): invitation.status = "expired"; await db.flush(); raise ValueError("邀请已过期，无法撤销")
    - invitation.status = "revoked"; await db.flush(); await db.refresh(invitation)
    - Return invitation

    FILE 3 — backend/app/routers/guest.py
    Add two new endpoint functions (import GuestInvitationListResponse from app.schemas.guest in the function body to avoid circular import):

    Endpoint 1: GET /invitations
    - @router.get("/invitations", response_model=PageResponse)
    - Args: page: int = Query(1, ge=1), page_size: int = Query(20, ge=1, le=50)
    - Dependencies: current_user = Depends(require_role("chef", "user")), db = Depends(get_db)
    - Body: params = PaginationParams(page=page, page_size=page_size)
    - try: invitations, total = await guest_service.list_invitations(db, current_user.id, params)
    - except ValueError as e: raise HTTPException(400, detail=str(e))
    - items = [GuestInvitationListResponse.model_validate(inv) for inv in invitations]
      where each inv.chef_name comes from inv.chef.display_name (the eager-loaded relationship)
    - Return PageResponse[GuestInvitationListResponse](total=total, page=page, page_size=page_size, items=items)

    Endpoint 2: PUT /invitations/{invitation_id}/revoke
    - @router.put("/invitations/{invitation_id}/revoke")
    - Args: invitation_id: int
    - Dependencies: current_user = Depends(require_role("chef", "user")), db = Depends(get_db)
    - Body: try: invitation = await guest_service.revoke_invitation(db, invitation_id, current_user.id)
    - except ValueError as e: raise HTTPException(400, detail=str(e))
    - await db.commit()
    - Return GuestInvitationListResponse.model_validate(invitation) — includes chef_name via eager load from service
  </action>
  <verify>
    <automated>
      cd /home/temila/family_chef && python -c "
import asyncio; from backend.app.schemas.guest import GuestInvitationListResponse
# Verify schema has chef_name
import pydantic
assert 'chef_name' in GuestInvitationListResponse.model_fields, 'Missing chef_name'
assert GuestInvitationListResponse.model_fields['chef_name'].annotation == str | None, 'chef_name should be Optional[str]'
print('Schema OK')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - GuestInvitationListResponse 包含 chef_name: Optional[str] 字段
    - GuestService.list_invitations 方法存在，接受 (db, inviter_id, params) → tuple
    - GuestService.revoke_invitation 方法存在，验证所有权和 status='active'
    - GET /api/guest/invitations 端点存在，使用 require_role("chef", "user") 保护
    - PUT /api/guest/invitations/{invitation_id}/revoke 端点存在，验证所有权
    - 模块导入无错误，无语法错误
  </acceptance_criteria>
  <done>GuestService 拥有 list_invitations 和 revoke_invitation 方法，路由拥有两个新端点，schema 包含 chef_name</done>
</task>

<task type="auto">
  <name>Task 2: Order schemas + router — add is_guest field</name>
  <files>
    backend/app/schemas/order.py
    backend/app/routers/orders.py
  </files>
  <read_first>
    @backend/app/schemas/order.py (OrderListResponse and OrderDetailResponse schemas — add is_guest field)
    @backend/app/routers/orders.py (build_order_detail at line 73-86 and list_orders at line 156-164 — add is_guest assignment)
    .planning/phases/02-backend-core/02-02-SUMMARY.md (Phase 2 added guest_invitation_id FK on Order model)
  </read_first>
  <action>
    Make four targeted changes:

    FILE 1 — backend/app/schemas/order.py
    Change 1: In OrderListResponse (line 44-55), add `is_guest: bool = False` after `status: str` field.
    Change 2: In OrderDetailResponse (line 57-73), add `is_guest: bool = False` after `status: str` field. Also add `guest_invitation_id: Optional[int] = None` after `user_id: int`.

    FILE 2 — backend/app/routers/orders.py
    Change 3: In build_order_detail (line 73-86), add parameter to OrderDetailResponse: `is_guest=order.guest_invitation_id is not None` and `guest_invitation_id=order.guest_invitation_id`.
    Change 4: In list_orders (line 156-164), add parameter to OrderListResponse: `is_guest=o.guest_invitation_id is not None`.
  </action>
  <verify>
    <automated>
      cd /home/temila/family_chef && python -c "
import asyncio; from backend.app.schemas.order import OrderListResponse, OrderDetailResponse
assert 'is_guest' in OrderListResponse.model_fields, 'OrderListResponse missing is_guest'
assert 'is_guest' in OrderDetailResponse.model_fields, 'OrderDetailResponse missing is_guest'
assert OrderDetailResponse.model_fields['is_guest'].default is False, 'is_guest should default False'
print('Order schemas OK')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - OrderListResponse 新增 is_guest: bool = False 字段
    - OrderDetailResponse 新增 is_guest: bool = False 和 guest_invitation_id: Optional[int] = None 字段
    - build_order_detail 中 is_guest=order.guest_invitation_id is not None
    - list_orders 的循环中 is_guest=o.guest_invitation_id is not None
    - 模块导入无错误
  </acceptance_criteria>
  <done>订单 API 返回 is_guest 和 guest_invitation_id 字段，前端可据此显示访客标识</done>
</task>

<task type="auto">
  <name>Task 3: Frontend — API client + Badge/utils/CSS + ChefOrdersPage + OrderDetailPage guest badge integration</name>
  <files>
    frontend/src/api/client.js
    frontend/src/utils/index.js
    frontend/src/css/styles.css
    frontend/src/pages/AdminChefsPage.jsx
    frontend/src/pages/ChefOrdersPage.jsx
    frontend/src/pages/OrderDetailPage.jsx
  </files>
  <read_first>
    @frontend/src/api/client.js (existing method patterns — line 120-148 for orders, line 249-263 for chefs)
    @frontend/src/utils/index.js (statusBadge map at lines 52-69 — will modify lines 64-65 and add new entries)
    @frontend/src/css/styles.css (badge classes at lines 111-117, filter-chips at lines 122-126, modal overlay at 376-384, chef-select-item at 409-411)
    @frontend/src/components/Badge.jsx (behavior: if `text` prop exists, uses `badge-${type||'info'}`; else uses statusBadge(status))
    @frontend/src/pages/AdminChefsPage.jsx (lines 120, 146 — change from status-based to text-based Badge)
    @frontend/src/pages/ChefOrdersPage.jsx (filter-chips at 77-102, order card header at 112-115, filteredOrders at 67-70, imports at 1-15)
    @frontend/src/pages/OrderDetailPage.jsx (card header at 64-67)
    .planning/phases/03-frontend-authenticated/03-PATTERNS.md (exact code excerpts for all modifications)
    .planning/phases/03-frontend-authenticated/03-RESEARCH.md (API client additions code example at lines 514-534, badge extension at 536-545, CSS at 548-554, guest badge in ChefOrdersPage at 555-569, OrderDetailPage at 555-569)
  </read_first>
  <action>
    Make changes across 6 files. Each change is small and targeted.

    FILE 1 — frontend/src/api/client.js
    After the "Tools" section (line 297 comment), before "Core HTTP Methods" (line 297 actual), add a new section:
    // ─── Guest Invitations ─────────────────────────────────────
    Three methods following the existing pattern (see getOrders at lines 126-132, getFavorites at 207-212):
    - async getInvitations(params = {}) — builds URLSearchParams for page/page_size, returns this.get(`/guest/invitations?${qs}`)
    - async createInvitation(chefId) — const body = chefId ? { chef_id: chefId } : undefined; returns this.post('/guest/invitations', body)
    - async revokeInvitation(id) — returns this.put(`/guest/invitations/${id}/revoke`)

    FILE 2 — frontend/src/utils/index.js
    Three changes in the statusBadge map (lines 53-68):
    - Change line 64: `active: { text: '启用', cls: 'badge-success' },` → `active: { text: '活跃', cls: 'badge-success' },`
    - Change line 65: `inactive: { text: '停用', cls: 'badge-danger' },` → keep this (no change required)
    - After line 65 (after inactive entry), add three new lines:
        used: { text: '已使用', cls: 'badge-muted' },
        expired: { text: '已过期', cls: 'badge-warn' },
        revoked: { text: '已撤销', cls: 'badge-danger' },

    FILE 3 — frontend/src/css/styles.css
    After line 117 (after .badge-gold), add one new CSS rule:
    .badge-muted { background: var(--bg-elevated); color: var(--text-muted); }

    FILE 4 — frontend/src/pages/AdminChefsPage.jsx
    Two identical changes (lines 120 and 146):
    Change from: `<Badge status={chef.is_active ? 'active' : 'inactive'} />`
    Change to: `<Badge text={chef.is_active ? '启用' : '停用'} type={chef.is_active ? 'success' : 'danger'} />`
    This frees up the 'active' key in statusBadge map for invitation status, while preserving exact visual output (badge-success green for active, badge-danger red for inactive).

    FILE 5 — frontend/src/pages/ChefOrdersPage.jsx
    Four changes:
    a) Imports — add `Badge` import (already present at line 12, verify)
    b) Filtered orders (line 67-70): change from:
         const filteredOrders = orders.filter(order => {
           if (filterStatus === 'all') return true;
           return order.status === filterStatus;
         });
       To:
         const filteredOrders = orders.filter(order => {
           if (filterStatus === 'all') return true;
           if (filterStatus === 'guest') return order.is_guest;
           return order.status === filterStatus;
         });
    c) Filter chips (add after the "已完成" chip, after line 101):
         <button
           className={`filter-chip ${filterStatus === 'guest' ? 'active' : ''}`}
           onClick={() => setFilterStatus('guest')}
         >
           访客订单 ({orders.filter(o => o.is_guest).length})
         </button>
    d) Order header (line 112-115): change from:
         <div className="order-header">
           <span className="order-no">#{order.id}</span>
           <span className="order-date">{formatDate(order.created_at)}</span>
         </div>
       To:
         <div className="order-header">
           <span className="order-no">
             #{order.id}
             {order.is_guest && <span className="badge badge-warn" style={{ marginLeft: 8 }}>访客订单</span>}
           </span>
           <span className="order-date">{formatDate(order.created_at)}</span>
         </div>

    FILE 6 — frontend/src/pages/OrderDetailPage.jsx
    One change in the card header (line 64-66). Change from:
        <h3 style={{ margin: 0 }}>订单 #{order.id}</h3>
    To:
        <h3 style={{ margin: 0 }}>
          订单 #{order.id}
          {order.is_guest && (
            <span className="badge badge-warn" style={{ marginLeft: 8, verticalAlign: 'middle' }}>
              访客订单
            </span>
          )}
        </h3>
  </action>
  <verify>
    <automated>node -e "
const fs = require('fs');
const client = fs.readFileSync('frontend/src/api/client.js','utf8');
const utils = fs.readFileSync('frontend/src/utils/index.js','utf8');
const css = fs.readFileSync('frontend/src/css/styles.css','utf8');
const chefsPage = fs.readFileSync('frontend/src/pages/AdminChefsPage.jsx','utf8');
const chefOrders = fs.readFileSync('frontend/src/pages/ChefOrdersPage.jsx','utf8');
const orderDetail = fs.readFileSync('frontend/src/pages/OrderDetailPage.jsx','utf8');

// API client
assert(client.includes('getInvitations'), 'Missing getInvitations');
assert(client.includes('createInvitation'), 'Missing createInvitation');
assert(client.includes('revokeInvitation'), 'Missing revokeInvitation');

// Utils
assert(utils.includes(\"used: { text: '已使用'\"), 'Missing used status');
assert(utils.includes(\"revoked: { text: '已撤销'\"), 'Missing revoked status');

// CSS
assert(css.includes('.badge-muted'), 'Missing .badge-muted');

// AdminChefsPage — updated to use text+type
assert(!chefsPage.includes(\"status={chef.is_active ? 'active' : 'inactive'}\"), 'AdminChefsPage still uses status-based Badge');
assert(chefsPage.includes(\"type={chef.is_active ? 'success' : 'danger'}\"), 'AdminChefsPage missing type-based Badge');

// ChefOrdersPage
assert(chefOrders.includes(\"filterStatus === 'guest'\"), 'ChefOrdersPage missing guest filter');
assert(chefOrders.includes('访客订单'), 'ChefOrdersPage missing guest order badge');

// OrderDetailPage
assert(orderDetail.includes('访客订单'), 'OrderDetailPage missing guest order badge');

console.log('All frontend assertions passed');
function assert(cond, msg) { if (!cond) { console.error('FAIL: ' + msg); process.exit(1); } }
"</automated>
  </verify>
  <acceptance_criteria>
    - client.js 新增 getInvitations, createInvitation, revokeInvitation 方法
    - statusBadge 映射新增 active(活跃)/used(已使用)/expired(已过期)/revoked(已撤销)
    - CSS 新增 .badge-muted class
    - AdminChefsPage 使用 Badge text+type 方式（不受 active 键变化影响）
    - ChefOrdersPage 橙色访客订单 Badge + 筛选芯片可用
    - OrderDetailPage 橙色访客订单 Badge 可用
    - Node 断言检查全部通过
  </acceptance_criteria>
  <done>前端 API 客户端、UI 工具函数、样式和三个页面的访客订单标识全部就绪</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → API (JWT) | Authenticated user requests for invitation list/revoke — all require valid JWT via require_role |
| Browser → API (internal) | Guest order list and detail requests — users can only see their own (user role) or chef-bound (chef role) orders per existing authorization |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | GET /api/guest/invitations | mitigate | GuestService.list_invitations filters by inviter_id=current_user.id. Router uses require_role("chef", "user"). Users cannot enumerate other users' invitations. |
| T-03-02 | Tampering | PUT /api/guest/invitations/{id}/revoke | mitigate | GuestService.revoke_invitation verifies invitation.inviter_id == current_user_id before performing any status change. Returns 400 on mismatch. |
| T-03-03 | Tampering | PUT /api/guest/invitations/{id}/revoke | mitigate | Only 'active' status invitations can be revoked. Pre-condition check: if status != "active", raise ValueError. Lazy expiry check also applied before revocation. |
| T-03-04 | Information Disclosure | Order API is_guest | accept | is_guest is derived from guest_invitation_id which is already visible to authorized order viewers (order owner or chef). No additional information is exposed. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages required — all dependencies are existing. Plan 01 uses only existing project dependencies. |
</threat_model>

<verification>
- [ ] Python import check: GuestInvitationListResponse imports without error
- [ ] Python import check: OrderListResponse and OrderDetailResponse have is_guest field
- [ ] Node assertions: all 6 frontend files modified as specified
- [ ] Backend API: quick start `uvicorn app.main:app` imports without error
</verification>

<success_criteria>
- GuestService 新增 list_invitations 和 revoke_invitation 方法
- 路由新增 GET /api/guest/invitations 和 PUT /api/guest/invitations/{id}/revoke
- 订单 API 返回 is_guest 字段
- 前端 API 客户端有 3 个邀请相关方法
- 厨师订单列表显示访客订单橙色 Badge 和筛选芯片
- 订单详情页显示访客订单橙色 Badge
- statusBadge 映射包含 4 个邀请状态
</success_criteria>

<output>
Create `.planning/phases/03-frontend-authenticated/03-01-SUMMARY.md` when done
</output>
