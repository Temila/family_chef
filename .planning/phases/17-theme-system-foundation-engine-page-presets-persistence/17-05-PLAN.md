---
phase: 17
plan: 05
type: execute
wave: 3
depends_on:
  - 17-04
files_modified:
  - frontend/src/components/primitives/Icon.jsx
  - frontend/src/components/composites/Header.jsx
  - frontend/src/components/composites/Header.css
  - frontend/src/pages/ThemePage.jsx
  - frontend/src/components/theme/ThemeCard.jsx
  - frontend/src/components/theme/ThemePreview.jsx
  - frontend/src/components/theme/ThemeCard.css
  - frontend/src/css/theme-page.css
  - frontend/src/index.css
autonomous: true
requirements:
  - TPAGE-01
  - TPAGE-02
  - TPAGE-03
  - TPAGE-04
  - TPAGE-05
  - TPAGE-06
  - TPAGE-07
key_decisions:
  - D-01
  - D-02
  - D-03
  - D-05
  - D-06
  - D-07
  - D-18
  - D-19
  - D-20
  - D-24
  - D-25
must_haves:
  truths:
    - "Header has a new Palette IconButton between theme-toggle and avatar with ariaLabel='选择主题' that navigates to /theme"
    - "PAGE_TITLES includes '/theme': '主题'"
    - "/theme route renders ThemePage with page-container, Header, card grid, and BottomBar"
    - "ThemePage renders ALL themes in a single grid with a deterministic total order: presets first in presets.js array order (默认/春/夏/秋/冬), then customThemes in API-returned updated_at DESC order — no client-side re-sort, no preset/custom grouping separation"
    - "Each ThemeCard wraps a <div data-fc-theme-scope={themeId}> with a scoped <style> overriding --md-color-* for that subtree; inside renders real <Card>/<Button>/<Chip> primitives + 4-step surface ramp"
    - "Click a ThemeCard → setActiveTheme(theme); card with active theme shows '已选中' indicator"
    - "Top-right reset IconButton (RestartAlt) single-click calls resetToDefault() + showToast('已还原默认主题', 'success') — no confirmation dialog, no two-click confirm (UI-SPEC § Copywriting Contract line 646-647)"
    - "Icon registry exposes 'palette' and 'restart-alt' glyphs"
    - "Header.css line 5 comment updated to reflect D-19 (full-device visibility)"
  artifacts:
    - path: frontend/src/components/primitives/Icon.jsx
      provides: "Palette and RestartAlt glyphs added to ICONS map"
    - path: frontend/src/components/composites/Header.jsx
      provides: "PAGE_TITLES['/theme'] = '主题' + Palette IconButton in __right cluster"
    - path: frontend/src/components/composites/Header.css
      provides: "Updated line 5 comment to reflect D-19 (no CSS rule changes)"
    - path: frontend/src/pages/ThemePage.jsx
      provides: "Page shell: topbar (title + reset), card grid, EmptyState for zero custom, BottomBar"
    - path: frontend/src/components/theme/ThemeCard.jsx
      provides: "Card-as-preview with scoped data-fc-theme-scope wrapper + click handler + active indicator"
    - path: frontend/src/components/theme/ThemePreview.jsx
      provides: "Mini-UI: elevated Card + filled Button + tonal Button + filter Chip + 4-step surface ramp"
    - path: frontend/src/components/theme/ThemeCard.css
      provides: "BEM layout for .theme-card + active indicator"
    - path: frontend/src/css/theme-page.css
      provides: "Page-level layout: .theme-page__topbar, .theme-page__title, .theme-card-grid (auto-fill responsive)"
  key_links:
    - from: frontend/src/pages/ThemePage.jsx
      to: frontend/src/theme/theme-context.jsx
      via: "const { activeTheme, setActiveTheme, customThemes, resetToDefault } = useTheme()"
      pattern: "useTheme\\(\\)"
    - from: frontend/src/pages/ThemePage.jsx
      to: frontend/src/theme/presets.js
      via: "const allThemes = [...PRESETS, ...customThemes] — deterministic total order: presets first in presets.js array order, then customs in API updated_at DESC order; NO client-side sort (BLOCKER B4)"
      pattern: "\\[\\.\\.\\.PRESETS, \\.\\.\\.customThemes\\]"
    - from: frontend/src/components/composites/Header.jsx
      to: react-router-dom navigate
      via: "onClick={() => navigate('/theme')}"
      pattern: "navigate\\('/theme'\\)"
    - from: frontend/src/components/theme/ThemeCard.jsx
      to: frontend/src/theme/theme-engine.js
      via: "buildCssSync(theme.sourceColors, theme.variant) → scoped <style>"
      pattern: "buildCssSync\\("
    - from: frontend/src/components/theme/ThemeCard.jsx
      to: frontend/src/components/primitives/Card.jsx
      via: "renders <Card> inside scoped wrapper"
      pattern: "<Card "
---

<objective>
Build the user-facing /theme page surface: Palette entry button in Header + page card grid + ThemeCard with scoped CSS-var preview + ThemePreview mini-UI + reset button + Icon registry extensions + CSS files for both card and page.

Purpose: TPAGE-01 (Header entry button), TPAGE-02 (card grid mobile-first responsive), TPAGE-03 (card-as-preview via scoped CSS-var boundary + real primitives), TPAGE-04 (5 presets rendered), TPAGE-05 (click card → apply + persist localStorage — persist handled by ThemeContext in plan 17-04; click handler here), TPAGE-06 (active indicator on selected card), TPAGE-07 (presets editable not deletable — Phase 17 has no editor, so this is enforced by absence of edit/delete UI per D-24).

Output: 8 new/modified frontend files, Vite build succeeds, /theme route renders the full page with all 5 presets + any custom themes from DB.
</objective>

<execution_context>
@/home/temila/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/temila/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/17-theme-system-foundation-engine-page-presets-persistence/17-CONTEXT.md
@.planning/phases/17-theme-system-foundation-engine-page-presets-persistence/17-PATTERNS.md
@frontend/src/components/composites/Header.jsx:28-48 (PAGE_TITLES), :96-160 (__right cluster)
@frontend/src/components/composites/Header.css:1-10 (line 5 comment to update)
@frontend/src/components/primitives/Icon.jsx:13-82 (import block), :85-149 (ICONS map)
@frontend/src/components/primitives/Card.jsx (Card primitive to render inside scope)
@frontend/src/components/primitives/Button.jsx (Button primitive for preview)
@frontend/src/components/primitives/Chip.jsx (Chip primitive for preview)
@frontend/src/components/primitives/IconButton.jsx (used in Header)
@frontend/src/pages/UserWishesPage.jsx:292-380 (page shell with Header + BottomBar pattern)
@frontend/src/css/styles.css:152-156 (.dish-grid responsive pattern)
@frontend/src/theme/theme-context.jsx (created in plan 17-04 — useTheme() returns the shape)
@frontend/src/theme/presets.js (PRESETS array from plan 17-02)
@frontend/src/theme/theme-engine.js (buildCssSync from plan 17-02)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Icon registry with palette + restart-alt + add Header Palette IconButton + PAGE_TITLES + Header.css comment</name>
  <files>frontend/src/components/primitives/Icon.jsx, frontend/src/components/composites/Header.jsx, frontend/src/components/composites/Header.css</files>
  <read_first>
    - frontend/src/components/primitives/Icon.jsx:13-82 (existing import block — alphabetized grouping; add Palette + RestartAlt in the 'Phase 10 necessary extensions' or new 'Phase 17 necessary extensions' group)
    - frontend/src/components/primitives/Icon.jsx:85-149 (existing ICONS map)
    - frontend/node_modules/@material-symbols-svg/react/dist/index.d.ts (confirmed Palette + RestartAlt exports per PATTERNS line 1113-1114)
    - frontend/src/components/composites/Header.jsx:28-48 (PAGE_TITLES dict)
    - frontend/src/components/composites/Header.jsx:96-160 (__right cluster — theme-toggle IconButton is the insertion anchor per D-18)
    - frontend/src/components/composites/Header.css:1-10 (file header comment block)
  </read_first>
  <action>
    Per D-18 + D-19 + D-20:
    - Edit frontend/src/components/primitives/Icon.jsx:
      1. Add 2 new imports after the existing Phase 12 emoji→Icon block (after line 81 `Send as SendIcon,`): `Palette as PaletteIcon, RestartAlt as RestartAltIcon,` — in a new Phase 17 comment block.
      2. Add 2 new entries to ICONS map after the Phase 12 block (after line 149): `palette: PaletteIcon, 'restart-alt': RestartAltIcon,`.
    - Edit frontend/src/components/composites/Header.jsx:
      1. Add `'/theme': '主题',` to PAGE_TITLES (after the `'/ingredients': '食材管理'` line at line 47 — alphabetic by route path).
      2. Insert a new <IconButton icon="palette" ariaLabel="选择主题" onClick={() => navigate('/theme')} className="md-header__theme-page" /> BETWEEN the existing theme-toggle IconButton (lines 97-102) AND the user avatar block (line 103 `{user && (...)}`). No other changes to Header.jsx — the IconButton primitive provides 48dp hit box natively.
    - Edit frontend/src/components/composites/Header.css line 5 comment: change `'仅 PC (≥1024px) 显示，移动端隐藏。'` to `'全设备可见 (Phase 17 D-19) — /theme 入口需在 PC+mobile 均可访问。'`. NO CSS rule changes (UI-SPEC audit confirmed there is no `@media (max-width: 1023px) { .md-header { display: none } }` rule today).
    - Run cd frontend && npm run lint and verify no regressions.
  </action>
  <verify>
    <automated>cd frontend && grep -E "Palette|RestartAlt" src/components/primitives/Icon.jsx | head -5 && cd frontend && grep -E "/theme|选择主题|palette" src/components/composites/Header.jsx | head -5 && cd frontend && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/primitives/Icon.jsx imports include `Palette as PaletteIcon` and `RestartAlt as RestartAltIcon` (verified via grep).
    - frontend/src/components/primitives/Icon.jsx ICONS map contains `palette: PaletteIcon` and `'restart-alt': RestartAltIcon` entries.
    - frontend/src/components/composites/Header.jsx PAGE_TITLES contains `'/theme': '主题',` line.
    - frontend/src/components/composites/Header.jsx __right cluster contains a new `<IconButton icon="palette" ariaLabel="选择主题" onClick={() => navigate('/theme')} className="md-header__theme-page" />` between the theme-toggle and the user avatar block.
    - frontend/src/components/composites/Header.css line 5 comment updated to reflect D-19 (full-device visibility). No CSS rules added/removed.
    - cd frontend && npm run lint exits 0.
  </acceptance_criteria>
  <done>Icon registry exposes palette + restart-alt; Header has the Palette entry button; PAGE_TITLES has /theme mapping; Header.css comment is accurate.</done>
</task>

<task type="auto">
  <name>Task 2: Create ThemePage.jsx + theme-page.css (page topbar + reset button + card grid + empty state)</name>
  <files>frontend/src/pages/ThemePage.jsx, frontend/src/css/theme-page.css</files>
  <read_first>
    - frontend/src/pages/UserWishesPage.jsx:292-380 (page shell with Header + section + BottomBar — mirror the shape)
    - frontend/src/theme/theme-context.jsx (useTheme() shape: activeTheme, setActiveTheme, customThemes, refreshCustomThemes, resetToDefault, PRESETS — created in plan 17-04)
    - frontend/src/components/composites/Header.jsx (Header component accepts title prop)
    - frontend/src/css/styles.css:152-156 (.dish-grid responsive pattern — repeat(auto-fill, minmax(280px, 1fr)))
    - frontend/src/components/EmptyState.jsx (EmptyState primitive for the zero-custom case)
    - frontend/src/components/primitives/IconButton.jsx (for the reset button)
  </read_first>
  <action>
    Per D-25 (all themes in one grid, deterministic order) + BLOCKER B4 + UI-SPEC § Copywriting Contract (W1):
    - Create frontend/src/css/theme-page.css with BEM selectors:
      - `.theme-page__topbar { display: flex; align-items: center; justify-content: space-between; padding: var(--md-spacing-5) 0 var(--md-spacing-4); padding-left: var(--md-spacing-4); padding-right: var(--md-spacing-4); }`
      - `.theme-page__title { font-family: var(--md-font-display); font-size: 1.25rem; font-weight: 600; color: var(--md-color-on-surface); margin: 0; }`
      - `.theme-page__subtitle { font-size: 0.875rem; font-weight: 400; color: var(--md-color-on-surface-variant); margin-top: var(--md-spacing-1); }`
      - `.theme-card-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(280px, 1fr)); gap: var(--md-spacing-4); padding: 0 var(--md-spacing-4) var(--md-spacing-6); }`
      - `.theme-page__empty { text-align: center; padding: var(--md-spacing-7) var(--md-spacing-4); color: var(--md-color-on-surface-variant); }`
      - `.theme-page__empty-sub { font-size: 0.875rem; font-weight: 400; color: var(--md-color-on-surface-variant); margin-top: var(--md-spacing-1); }`
      - All spacing via `var(--md-spacing-*)` — NO colors (UI-SPEC § Color says no new color tokens).
    - Create frontend/src/pages/ThemePage.jsx (default export `ThemePage`):
      - Imports: `import Header from '../components/Header';`, `import BottomBar from '../components/BottomBar';`, `import EmptyState from '../components/EmptyState';`, `import IconButton from '../components/primitives/IconButton';`, `import { useToast } from '../contexts/ToastContext';`, `import { useTheme } from '../theme';`, `import { PRESETS } from '../theme';` (or `useTheme().PRESETS`), `import ThemeCard from '../components/theme/ThemeCard';`. (No `useState` needed — the reset flow is a single click per UI-SPEC.)
      - Inside component: `const { activeTheme, setActiveTheme, customThemes, resetToDefault } = useTheme();` and `const { showToast } = useToast();`
      - Compute merged list with a DETERMINISTIC total order (BLOCKER B4): `const allThemes = [...PRESETS, ...customThemes];` — NO client-side sort. Presets come first in their fixed `presets.js` array order (默认, 春, 夏, 秋, 冬); custom themes follow in the order returned by GET /api/themes, which the backend guarantees is `updated_at DESC` (plan 17-01 `list_themes` uses `.order_by(CustomTheme.updated_at.desc())`). Per D-25 there is no preset/custom grouping separation — both kinds are cards in the same grid, distinguished only by the kind chip. This total order is deterministic and needs no re-sort code.
      - Page JSX (copy strings per UI-SPEC § Copywriting Contract — page title `主题`, subtitle `选择一个喜欢的色调，应用即生效`, reset aria-label `还原默认主题` + tooltip `重置默认`, reset confirmation snackbar `已还原默认主题`, empty-state heading `还没有自定义主题` + subtext `从 5 个预设中选一个开始，或保存你喜欢的颜色搭配`):
        ```jsx
        <div className="page-container">
          <Header title="主题" />
          <div className="theme-page__topbar">
            <div>
              <h1 className="theme-page__title">主题</h1>
              <div className="theme-page__subtitle">选择一个喜欢的色调，应用即生效</div>
            </div>
            <IconButton
              icon="restart-alt"
              ariaLabel="还原默认主题"
              title="重置默认"
              onClick={() => {
                resetToDefault();
                showToast('已还原默认主题', 'success');
              }}
            />
          </div>
          {customThemes.length === 0 && (
            <div className="theme-page__empty">
              <EmptyState icon="palette" text="还没有自定义主题" />
              <div className="theme-page__empty-sub">从 5 个预设中选一个开始，或保存你喜欢的颜色搭配</div>
            </div>
          )}
          <div className="theme-card-grid">
            {allThemes.map(theme => (
              <ThemeCard
                key={theme.id}
                theme={theme}
                isActive={activeTheme.id === theme.id}
                onClick={() => setActiveTheme(theme)}
              />
            ))}
          </div>
          <BottomBar />
        </div>
        ```
      - Reset interaction per UI-SPEC: NO confirmation dialog — a single IconButton click immediately calls resetToDefault() and fires the `已还原默认主题` success snackbar (4s default duration). No two-click confirm state, no window.confirm.
      - Empty state is a BANNER above the grid, only when customThemes.length === 0 (presets always render — the grid is never empty since PRESETS has 5 entries).
    - Add `<link>` reference for theme-page.css if needed: actually the project uses `frontend/src/index.css` as consolidated entry (per Phase 9). Add `@import './css/theme-page.css';` to `frontend/src/index.css` after the existing `@import './css/styles.css';`. (Verify the existing index.css has the styles.css import — if yes, add theme-page.css right after. Do NOT use the JS-side `import '../css/theme-page.css'` alternative — commit to the index.css @import path so files_modified stays accurate.)
  </action>
  <verify>
    <automated>cd frontend && grep -E "theme-page__|theme-card-grid" src/css/theme-page.css && cd frontend && grep -E "useTheme|PRESETS|setActiveTheme|还原默认主题|还没有自定义主题" src/pages/ThemePage.jsx | head -8 && cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/css/theme-page.css exists with .theme-page__topbar, .theme-page__title, .theme-page__subtitle, .theme-card-grid, .theme-page__empty, .theme-page__empty-sub selectors; all spacing via var(--md-spacing-*); zero hex literals.
    - frontend/src/pages/ThemePage.jsx exists with default export ThemePage.
    - ThemePage imports useTheme from ../theme and PRESETS from ../theme.
    - ThemePage renders <Header title="主题" />, and .theme-page__topbar contains `<h1 className="theme-page__title">主题</h1>` + subtitle `选择一个喜欢的色调，应用即生效` + reset IconButton with ariaLabel `还原默认主题` / title `重置默认` (UI-SPEC copy contract).
    - Reset is a single click → resetToDefault() + showToast('已还原默认主题', 'success') — no confirmation dialog (UI-SPEC § Copywriting Contract).
    - Deterministic ordering: ThemePage computes `allThemes = [...PRESETS, ...customThemes]` with NO client-side re-sort — presets render first in presets.js array order, then customThemes in API-returned (updated_at DESC) order (BLOCKER B4).
    - Empty state: when customThemes.length === 0, a banner with EmptyState text `还没有自定义主题` + subtext `从 5 个预设中选一个开始，或保存你喜欢的颜色搭配` renders ABOVE the grid; the grid always renders (presets never empty).
    - ThemePage includes <BottomBar />.
    - frontend/src/index.css contains `@import './css/theme-page.css';` (committed path — the index.css @import, not the JS-side import alternative).
    - cd frontend && npm run build exits 0.
  </acceptance_criteria>
  <done>ThemePage renders the full /theme experience per UI-SPEC copy: header, topbar (title `主题`, subtitle, single-click reset with snackbar), deterministic card grid (presets array order → customs updated_at DESC), custom-empty banner above the always-rendered grid, BottomBar.</done>
</task>

<task type="auto">
  <name>Task 3: Create ThemeCard.jsx + ThemePreview.jsx + ThemeCard.css (card-as-preview with scoped CSS-var boundary + 4-step surface ramp + active indicator)</name>
  <files>frontend/src/components/theme/ThemeCard.jsx, frontend/src/components/theme/ThemePreview.jsx, frontend/src/components/theme/ThemeCard.css</files>
  <read_first>
    - frontend/src/components/primitives/Card.jsx:17-65 (Card primitive shape — accepts image/header/footer/onClick/className; role='button' when clickable)
    - frontend/src/components/primitives/Button.jsx (Button primitive — variant='filled' or 'tonal')
    - frontend/src/components/primitives/Chip.jsx (Chip primitive — filter variant)
    - frontend/src/components/primitives/Badge.jsx (optional: chip for kind label "预设"/"自定义")
    - frontend/src/theme/theme-engine.js (buildCssSync function)
    - frontend/src/theme/presets.js (PRESETS shape)
    - frontend/src/css/styles.css:152-156 (.dish-grid responsive grid — spacing tokens only)
    - frontend/src/components/DishCard.jsx (existing composite card pattern — for slot-based composition)
  </read_first>
  <action>
    Per D-05/D-06/D-07 (scoped CSS-var boundary + real primitives + 4-step ramp + static preview) + UI-SPEC § Card Anatomy:
    - Create frontend/src/components/theme/ThemeCard.css with BEM selectors:
      - `.theme-card { display: flex; flex-direction: column; gap: var(--md-spacing-3); padding: var(--md-spacing-4); }` (card body padding)
      - `.theme-card__header { display: flex; align-items: center; justify-content: space-between; gap: var(--md-spacing-2); padding: var(--md-spacing-3) var(--md-spacing-4) 0; }` (chip flush with preview)
      - `.theme-card__chip { font-size: 0.75rem; font-weight: 500; padding: 0 var(--md-spacing-2); height: 24px; display: inline-flex; align-items: center; border-radius: var(--md-radius-xs); background: var(--md-color-secondary-container); color: var(--md-color-on-secondary-container); }` (preset chip). Custom chip overrides to `--md-color-tertiary-container` / `--md-color-on-tertiary-container` (per UI-SPEC).
      - `.theme-card__name { font-size: 0.875rem; font-weight: 500; color: var(--md-color-on-surface); }`
      - `.theme-card__preview { padding: var(--md-spacing-3); border-radius: var(--md-radius-sm); background: var(--md-color-surface-container); }` (preview shell)
      - `.theme-card__active-indicator { display: inline-flex; align-items: center; gap: var(--md-spacing-1); font-size: 0.75rem; font-weight: 500; padding: var(--md-spacing-1) var(--md-spacing-2); border-radius: var(--md-radius-xs); background: var(--md-color-primary-container); color: var(--md-color-on-primary-container); }`
      - `.theme-card[data-kind='custom'] .theme-card__chip { background: var(--md-color-tertiary-container); color: var(--md-color-on-tertiary-container); }` (custom chip color override via data-kind attribute)
      - `.theme-card--active { outline: 2px solid var(--md-color-primary); outline-offset: -2px; }`
      - All spacing via `var(--md-spacing-*)`, all colors via `var(--md-color-*)`. NO hex literals.
    - Create frontend/src/components/theme/ThemePreview.jsx (default export `ThemePreview`, no props — consumes scoped vars from wrapper):
      - Imports: `import Card from '../primitives/Card';`, `import Button from '../primitives/Button';`, `import Chip from '../primitives/Chip';`.
      - Renders inside its parent scope (which has --md-color-* overrides):
        ```jsx
        <div style={{ display: 'flex', flexDirection: 'column', gap: 'var(--md-spacing-2)' }}>
          <Card variant="elevated" style={{ padding: 'var(--md-spacing-2)', marginBottom: 'var(--md-spacing-2)' }}>
            <div style={{ fontSize: '10px', color: 'var(--md-color-on-surface)', fontWeight: 600 }}>菜品示例</div>
            <div style={{ fontSize: '9px', color: 'var(--md-color-on-surface-variant)', marginTop: '2px' }}>麻婆豆腐 · 川菜</div>
          </Card>
          <div style={{ display: 'flex', gap: 'var(--md-spacing-2)' }}>
            <Button variant="filled" size="small" style={{ fontSize: '10px', padding: '4px 8px' }}>主操作</Button>
            <Button variant="tonal" size="small" style={{ fontSize: '10px', padding: '4px 8px' }}>次操作</Button>
          </div>
          <Chip variant="filter" selected style={{ alignSelf: 'flex-start', fontSize: '10px' }}>已选标签</Chip>
          {/* 4-step surface ramp */}
          <div style={{ display: 'flex', height: '16px', borderRadius: 'var(--md-radius-xs)', overflow: 'hidden', marginTop: 'var(--md-spacing-1)' }}>
            <div style={{ flex: 1, background: 'var(--md-color-surface-container-lowest)' }} />
            <div style={{ flex: 1, background: 'var(--md-color-surface-container)' }} />
            <div style={{ flex: 1, background: 'var(--md-color-surface-container-high)' }} />
            <div style={{ flex: 1, background: 'var(--md-color-surface-container-highest)' }} />
          </div>
        </div>
        ```
      - All inline styles use `var(--md-*)` tokens — no hex literals.
    - Create frontend/src/components/theme/ThemeCard.jsx (default export `ThemeCard`, props `{ theme, isActive, onClick }`):
      - Imports: `import { useMemo } from 'react';`, `import Card from '../primitives/Card';`, `import { buildCssSync } from '../../theme/theme-engine';`, `import ThemePreview from './ThemePreview';`, `import './ThemeCard.css';`.
      - Compute scoped CSS: `const scopedCss = useMemo(() => { try { return buildCssSync(theme.sourceColors, theme.variant || 'TonalSpot'); } catch { return ''; } }, [theme.id, JSON.stringify(theme.sourceColors), theme.variant]);` — wraps in try/catch per T-17-16.
      - JSX (final shape — the <Card> primitive wraps the entire interactive surface and provides ripple + 48dp touch target + role='button'):
        ```jsx
        <Card
          variant="elevated"
          onClick={onClick}
          aria-label={theme.name}
          className={`theme-card ${isActive ? 'theme-card--active' : ''}`}
          data-kind={theme.kind === 'custom' || theme.user_id ? 'custom' : 'preset'}
        >
          <style>{scopedCss.replace(/:root\s*\{/g, `[data-fc-theme-scope="${theme.id}"] {`).replace(/\[data-theme="dark"\]\s*\{/g, `[data-fc-theme-scope="${theme.id}"][data-theme="dark"] {`)}</style>
          <div data-fc-theme-scope={theme.id} aria-hidden="true">
            <div className="theme-card__header">
              <span className="theme-card__name">{theme.name}</span>
              <span className="theme-card__chip">{theme.kind === 'custom' || theme.user_id ? '自定义' : '预设'}</span>
            </div>
            <div className="theme-card__preview">
              <ThemePreview />
            </div>
          </div>
          {isActive && (
            <div className="theme-card__active-indicator" aria-live="polite">✓ 已选中</div>
          )}
        </Card>
        ```
      - ARIA per UI-SPEC § ARIA: `aria-label={theme.name}` (UI-SPEC line 847-848 — the theme name is announced on focus, NOT a composed '应用主题 X' string), the preview scope div `<div data-fc-theme-scope={theme.id}>` gets `aria-hidden="true"` (line 856-858 — preview mini-UI must not be double-announced), the active-indicator wrapper gets `aria-live="polite"` (line 850-851 — screen reader announces active-theme change after click). role='button' comes from the Card.jsx onClick path (Card.jsx:51).
      - The scoped <style> is rendered as a child of <Card> (React allows this; the style element applies globally but its selectors are scoped by the [data-fc-theme-scope] attribute). The 4-step ramp + primitives read scoped vars naturally. The `.theme-card` className on <Card> adds the BEM padding via the ThemeCard.css rule.
    - Run cd frontend && npm run build and confirm exit 0.
  </action>
  <verify>
    <automated>cd frontend && npm run build && cd frontend && grep -E "data-fc-theme-scope|buildCssSync" src/components/theme/ThemeCard.jsx | head -5 && cd frontend && npm run check:tokens</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/theme/ThemeCard.css exists with .theme-card, .theme-card__header, .theme-card__chip, .theme-card__name, .theme-card__preview, .theme-card__active-indicator, .theme-card[data-kind='custom'] .theme-card__chip, .theme-card--active selectors; all spacing via var(--md-spacing-*); zero hex literals.
    - frontend/src/components/theme/ThemePreview.jsx exists with default export ThemePreview; renders 1 Card + 2 Buttons + 1 Chip + 4-step surface ramp; all inline styles use var(--md-*) tokens.
    - frontend/src/components/theme/ThemeCard.jsx exists with default export ThemeCard accepting `{ theme, isActive, onClick }` props.
    - ThemeCard uses `useMemo` to compute `scopedCss` from buildCssSync(theme.sourceColors, theme.variant) wrapped in try/catch.
    - ThemeCard JSX renders a <style> element with selectors rewritten to `[data-fc-theme-scope="${theme.id}"]` and `[data-fc-theme-scope="${theme.id}"][data-theme="dark"]` (replacing `:root` and `[data-theme="dark"]` respectively).
    - ThemeCard JSX renders a `<div data-fc-theme-scope={theme.id} aria-hidden="true">` containing the kind chip, theme name, and <ThemePreview /> (aria-hidden per UI-SPEC line 856-858 — preview mini-UI must not be double-announced).
    - The <Card> wrapper carries `aria-label={theme.name}` (UI-SPEC line 847-848 — theme name only), `onClick={onClick}`, and className `theme-card` (+ `theme-card--active` when active).
    - The active indicator "✓ 已选中" renders only when isActive is true, inside a wrapper with `aria-live="polite"` (UI-SPEC line 850-851).
    - cd frontend && npm run build exits 0.
    - cd frontend && npm run check:tokens exits 0 (Check #8 still passes — no hex literals in new ThemeCard.jsx / ThemePreview.jsx / ThemeCard.css).
  </acceptance_criteria>
  <done>Card-as-preview with scoped CSS-var boundary is wired; click handler applies theme via setActiveTheme; active indicator visible on selected card; 5 presets + customs all render with faithful mini-UI previews.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ThemeCard click → setActiveTheme | UI handler; user input triggers state mutation; theme object shape validated upstream in ThemeContext |
| Reset button click → resetToDefault | Single-click per UI-SPEC (line 646-647); accidental reset is cheap to recover: 4s success snackbar + one-click re-selection of any theme card in the grid. No confirm dialog in Phase 17 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-21 | Tampering | theme.sourceColors from custom theme DB row | mitigate | ThemeCard's scopedCss computation is wrapped in try/catch; on engine throw, returns '' and the scoped <style> element renders empty (no override applied → falls back to page's active theme inside the card). User sees no preview but the click still works (setActiveTheme normalizes shape) |
| T-17-22 | Spoofing | ThemeCard aria-label injection via theme.name | mitigate | React's JSX text content escaping prevents XSS in aria-label attribute; theme.name comes from authenticated backend (custom) or hardcoded PRESETS constants |
| T-17-23 | Denial of Service | Excessive custom themes per user | accept | Backend SYNC-04 + page pagination is not implemented (per D-14 — unlimited count). With typical family app usage (< 20 themes per user), the auto-fill grid handles it gracefully |
| T-17-24 | Repudiation | Click on preset that gets re-edited later | accept | Phase 17 has no edit UI (per D-24); preset card click only applies; cannot be re-edited. Phase 18 EDIT-05 will add edit flow |
| T-17-25 | Elevation of Privilege | /theme route guard | mitigate | ProtectedRoute requiredRoles=['user','chef','admin'] from plan 17-04 + backend JWT on POST/PUT/DELETE (plan 17-01). Read-only display (Phase 17) means no mutation attacks |
| T-17-26 | Tampering | Hex literal in new ThemeCard.jsx / ThemePreview.jsx / ThemeCard.css | mitigate | Check #8 (plan 17-03) covers JSX hex scan; verification runs `npm run check:tokens` which gates on 0 violations |
</threat_model>

<verification>
- [ ] cd frontend && npm run build exits 0
- [ ] cd frontend && npm run lint exits 0
- [ ] cd frontend && npm run check:tokens exits 0 (8/8 PASS including Check #8 JSX hex-lint)
- [ ] frontend/src/pages/ThemePage.jsx renders Header + theme-card-grid + ThemeCard map + BottomBar
- [ ] ThemePage computes allThemes as [...PRESETS, ...customThemes] with NO client-side sort (BLOCKER B4 — deterministic presets-first order)
- [ ] Reset single-click path: resetToDefault() + showToast('已还原默认主题', 'success') — no confirm dialog, no two-click state (UI-SPEC line 646-647)
- [ ] frontend/src/components/theme/ThemeCard.jsx uses data-fc-theme-scope + scoped <style> with selector rewrite
- [ ] ThemeCard aria: aria-label={theme.name}, preview div aria-hidden="true", active indicator aria-live="polite"
- [ ] Header has Palette IconButton between theme-toggle and avatar
- [ ] PAGE_TITLES contains '/theme': '主题'
</verification>

<success_criteria>
TPAGE-01 through TPAGE-07 all delivered: Header entry button, mobile-first card grid, card-as-preview via scoped CSS-var boundary with real primitives + 4-step surface ramp, 5 presets rendered, click-to-apply (delegated to ThemeContext's setActiveTheme from plan 17-04), active indicator, presets rendered as presets (no delete UI per D-24).
</success_criteria>

<output>
Create .planning/phases/17-theme-system-foundation-engine-page-presets-persistence/17-05-SUMMARY.md when done.
</output>