---
phase: 17
plan: 06
type: execute
wave: 4
depends_on:
  - 17-01
  - 17-02
  - 17-03
  - 17-04
  - 17-05
files_modified: []
notes: "VERIFICATION-ONLY plan — files_modified is intentionally empty. This plan creates no source files; its only artifact is 17-06-SUMMARY.md. All checks run against the 32 file actions created by plans 17-01..17-05 (authoritative inventory listed in Task 1; see key_links for the cross-plan connections being exercised)."
autonomous: true
requirements:
  - FND-04
  - FND-07
  - SYNC-03
key_decisions:
  - D-15
  - D-16
  - D-17
must_haves:
  truths:
    - "All 7 ROADMAP Phase 17 success criteria are testable from the code on disk (no missing files, no broken imports)"
    - "Full backend test suite green (including the 12 new test_themes.py cases from plan 17-01)"
    - "Full frontend lint + Vite build + npm run check:tokens + npm run check:md3 all green"
    - "End-to-end smoke test: create theme via curl → verify appears in GET → delete via curl → verify 404 on subsequent GET — confirms SYNC-03 wiring"
    - "ThemeContext mount-fetch is wired and surfaced to the user as a console-visible fetch (via dev console or backend log inspection)"
  artifacts:
    - path: .planning/phases/17-theme-system-foundation-engine-page-presets-persistence/17-06-SUMMARY.md
      provides: "Verification report mapping each ROADMAP success criterion to the file/behavior that satisfies it"
  key_links:
    - from: frontend/src/theme/theme-context.jsx
      to: backend /api/themes
      via: "api.getThemes() in refreshCustomThemes — verified by curl smoke test"
      pattern: "api\\.getThemes\\(\\)"
    - from: scripts/check-tokens.sh
      to: frontend/src/components/theme/*.jsx
      via: "Check #8 rg --glob '*.jsx' scan"
      pattern: "Check #8"
    - from: frontend/src/theme/fouc-bootstrap.js
      to: frontend/src/theme/theme-engine.js
      via: "inline classic <script> emitted by inline-theme-bootstrap plugin (BLOCKER B2 fix: Vite strips inline module scripts; esbuild IIFE bundle is used instead) — verified in built dist/index.html"
      pattern: "fc_active_theme"
---

<objective>
End-to-end verification of Phase 17 deliverable: confirm all 7 ROADMAP success criteria are testable from the code on disk, run full backend test suite + frontend lint + build + hex-lint + MD3 audit, and execute a curl-based smoke test that proves SYNC-03 (DB is source of truth + mount-fetch reconciliation) end-to-end.

Purpose: No new files created in this plan — it is a verification gate that the previous 5 plans collectively deliver a working /theme system ready for human UAT. The Phase 16 TD-09 fix (backend test suite 107 → 0 failures) is referenced to confirm zero regressions.

Output: 17-06-SUMMARY.md mapping each ROADMAP criterion to the file/behavior that satisfies it, plus a JSON-shaped verification report with pass/fail per check.
</objective>

<execution_context>
@/home/temila/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/temila/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/17-theme-system-foundation-engine-page-presets-persistence/17-CONTEXT.md
@.planning/phases/17-theme-system-foundation-engine-page-presets-persistence/17-RESEARCH.md
@.planning/ROADMAP.md (Phase 17 Success Criteria — lines 77-84)
@.planning/phases/17-theme-system-foundation-engine-page-presets-persistence/17-01-PLAN.md
@.planning/phases/17-theme-system-foundation-engine-page-presets-persistence/17-02-PLAN.md
@.planning/phases/17-theme-system-foundation-engine-page-presets-persistence/17-03-PLAN.md
@.planning/phases/17-theme-system-foundation-engine-page-presets-persistence/17-04-PLAN.md
@.planning/phases/17-theme-system-foundation-engine-page-presets-persistence/17-05-PLAN.md
@backend/app/main.py (AUTO_MIGRATE=1 default — startup auto-runs alembic upgrade head per TD-06)
@backend/app/routers/themes.py (created in plan 17-01 — endpoint signatures for smoke test)
@frontend/src/theme/theme-context.jsx (created in plan 17-04 — mount-fetch wiring)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inventory all 32 file actions from the five plans' files_modified frontmatter and confirm they exist on disk</name>
  <files>None (verification only — verifies existence of files created by plans 17-01 through 17-05)</files>
  <read_first>
    - .planning/phases/17-theme-system-foundation-engine-page-presets-persistence/17-PATTERNS.md §1 (the file tables — NOTE: its totals line is stale; see below)
    - .planning/ROADMAP.md §Phase 17 Success Criteria (7 criteria — the items that map to file existence + behavior)
  </read_first>
  <action>
    Per Quality Gate Checklist — "All files from PATTERNS.md accounted for":
    - AUTHORITATIVE INVENTORY (W4 + I1 fix): PATTERNS.md §1 tables list 25 rows but its totals line claims 28 (double-counting package-lock.json + main.py and claiming a second tooling file). The BLOCKER B2 revision (plan 17-02) added fouc-bootstrap.js + inline-theme-bootstrap.js + vite.config.js, B3 revision (plan 17-04) added App.jsx, and plan 17-01 adds theme-engine.test.mjs + conftest.py. Round-3 I1 fix added frontend/src/index.css (theme-page.css @import anchor, MODIFIED). The canonical count is therefore **32 distinct file actions**, taken from the files_modified frontmatter of plans 17-01..17-05:
      Frontend (22): frontend/index.html (placeholder); frontend/src/theme/theme-engine.js (NEW); frontend/src/theme/presets.js (NEW); frontend/src/theme/theme-context.jsx (NEW); frontend/src/theme/index.js (NEW); frontend/src/theme/fouc-bootstrap.js (NEW, B2); frontend/src/theme/theme-engine.test.mjs (NEW, TDD); frontend/plugins/inline-theme-bootstrap.js (NEW, B2); frontend/vite.config.js (MODIFIED, B2); frontend/src/pages/ThemePage.jsx (NEW); frontend/src/components/theme/ThemeCard.jsx (NEW); frontend/src/components/theme/ThemePreview.jsx (NEW); frontend/src/components/theme/ThemeCard.css (NEW); frontend/src/css/theme-page.css (NEW); frontend/src/index.css (MODIFIED, @import anchor for theme-page.css, I1); frontend/src/components/composites/Header.jsx (MODIFIED); frontend/src/components/composites/Header.css (MODIFIED); frontend/src/components/primitives/Icon.jsx (MODIFIED); frontend/src/api/client.js (MODIFIED); frontend/src/App.jsx (MODIFIED, B3); frontend/package.json (MODIFIED); frontend/package-lock.json (REGENERATED).
      Backend (9): backend/app/models/custom_theme.py (NEW); backend/app/models/__init__.py (MODIFIED); backend/app/schemas/theme.py (NEW); backend/app/services/custom_theme_service.py (NEW); backend/app/routers/themes.py (NEW); backend/app/main.py (MODIFIED); backend/alembic/versions/<hash>_add_custom_themes_table.py (NEW); backend/tests/test_themes.py (NEW); backend/tests/conftest.py (MODIFIED).
      Tooling (1): scripts/check-tokens.sh (MODIFIED).
    - The Alembic migration filename uses a generated hash → the check uses a glob pattern (`backend/alembic/versions/*add_custom_themes_table.py`).
    - Build a verification report: `for path in "${paths[@]}"; do test -f "$path" && echo "OK $path" || echo "MISSING $path"; done`. Expected output: all paths print OK; missing any prints MISSING (gate failure).
    - Map each ROADMAP Phase 17 success criterion to the file(s) that satisfy it:
      1. "Cold-load with active custom theme, no FOUC under DevTools 4× CPU throttle" → frontend/src/theme/fouc-bootstrap.js (sync buildCssSync IIFE bundle, BLOCKER B2) + frontend/plugins/inline-theme-bootstrap.js (build-time inline emission) + frontend/index.html (<!-- fc-bootstrap --> placeholder) + frontend/src/theme/theme-context.jsx (initial state from localStorage)
      2. "Custom theme active, flip light/dark → instant repaint, zero JS re-apply" → frontend/src/theme/theme-engine.js (light + dark blocks in single <style>) + frontend/src/utils/index.js (existing data-theme toggle, unmodified)
      3. "Dark mode elevation + surface-tint follow custom colors" → frontend/src/theme/theme-engine.js (SPECIAL_PALETTE_ROLES.surfaceTint in both blocks + buildElevationCss() emitting --md-elevation-0..5 in the dark block only, per BLOCKER B1 + UI-SPEC § Elevation in Dark Mode)
      4. "Click Header entry button → /theme page; cards responsive; each card is faithful mini-UI" → frontend/src/components/composites/Header.jsx (Palette IconButton) + frontend/src/pages/ThemePage.jsx + frontend/src/components/theme/ThemeCard.jsx + frontend/src/css/theme-page.css (responsive grid)
      5. "5 presets rendered; click card → apply + persist localStorage; active indicator; presets editable not deletable" → frontend/src/theme/presets.js (5 constants) + frontend/src/components/theme/ThemeCard.jsx (active indicator + click handler) + frontend/src/theme/theme-context.jsx (persists via applyTheme useEffect); "presets editable not deletable" → enforced by absence of edit/delete UI (per D-24)
      6. "Custom themes saved to backend DB; cross-device sync; toast on failure" → backend/app/models/custom_theme.py + backend/alembic/versions/*add_custom_themes_table.py + backend/app/services/custom_theme_service.py + backend/app/routers/themes.py + frontend/src/api/client.js (4 methods with source_colors ↔ sourceColors mapping) + frontend/src/theme/theme-context.jsx (mount-fetch + D-16 reconciliation + D-17 failure toast)
      7. "CI hex-lint fails when components hardcode hex colors" → scripts/check-tokens.sh Check #8 (added in plan 17-03)
  </action>
  <verify>
    <automated>bash -c 'paths=("frontend/index.html" "frontend/src/theme/theme-engine.js" "frontend/src/theme/presets.js" "frontend/src/theme/theme-context.jsx" "frontend/src/theme/index.js" "frontend/src/theme/fouc-bootstrap.js" "frontend/src/theme/theme-engine.test.mjs" "frontend/plugins/inline-theme-bootstrap.js" "frontend/vite.config.js" "frontend/src/pages/ThemePage.jsx" "frontend/src/components/theme/ThemeCard.jsx" "frontend/src/components/theme/ThemePreview.jsx" "frontend/src/components/theme/ThemeCard.css" "frontend/src/css/theme-page.css" "frontend/src/index.css" "frontend/src/components/composites/Header.jsx" "frontend/src/components/composites/Header.css" "frontend/src/components/primitives/Icon.jsx" "frontend/src/api/client.js" "frontend/src/App.jsx" "frontend/package.json" "frontend/package-lock.json" "backend/app/models/custom_theme.py" "backend/app/models/__init__.py" "backend/app/schemas/theme.py" "backend/app/services/custom_theme_service.py" "backend/app/routers/themes.py" "backend/app/main.py" "backend/tests/test_themes.py" "backend/tests/conftest.py" "scripts/check-tokens.sh"); missing=0; for p in "${paths[@]}"; do test -f "$p" || { echo "MISSING: $p"; missing=$((missing+1)); }; done; alembic_file=$(ls backend/alembic/versions/*add_custom_themes_table.py 2>/dev/null | head -1); test -n "$alembic_file" || { echo "MISSING alembic migration"; missing=$((missing+1)); }; echo "missing=$missing"'</automated>
  </verify>
  <acceptance_criteria>
    - The verification bash command prints `missing=0` AND no MISSING lines (only the final `missing=0` summary).
    - All 31 explicit path files exist (30 prior + frontend/src/index.css per I1 fix); the Alembic migration glob finds exactly one matching file (32 file actions total, per plan frontmatter inventory — W4 + I1 fix).
    - The 17-06-SUMMARY.md (or this plan's verification section) maps each of the 7 ROADMAP Phase 17 success criteria to the file(s) that satisfy it (criterion → file mapping table).
  </acceptance_criteria>
  <done>All 32 file actions from plans 17-01..17-05 exist on disk; 7 success criteria each trace to concrete files.</done>
</task>

<task type="auto">
  <name>Task 2: Run full backend test suite + frontend lint + build + check:tokens + check:md3 + ruff check</name>
  <files>None (verification only)</files>
  <read_first>
    - .planning/STATE.md (Phase 16 TD-09 — backend test suite was 107 → 0 failures; treat any current failure as a regression that needs explanation)
    - backend/tests/conftest.py (setup_database + clean_all_tables — confirm CustomTheme added per plan 17-01)
    - scripts/check-tokens.sh (current 8-check shape after plan 17-03)
    - frontend/package.json (lint + build + check:tokens + check:md3 scripts)
    - backend/pyproject.toml (ruff config — line-length 120, target py311)
  </read_first>
  <action>
    Per Quality Gate Checklist — "Wave structure maximizes parallelism", "Full backend test suite green", "CI hex-lint fails when components hardcode hex colors (0-match invariant preserved)":
    - Backend verification (assumes plan 17-01 has run successfully — re-run as regression check):
      1. `cd backend && uv run alembic current` — confirm the new migration revision is the head.
      2. `cd backend && uv run pytest -q` — full backend suite. Expected: all tests pass including the 12 test_themes.py cases from plan 17-01. If any fail, treat as a Phase 17 regression.
      3. `cd backend && uv run pytest tests/test_themes.py -v` — verbose run of just the theme tests for a focused pass/fail report.
      4. `cd backend && uv run ruff check app/ tests/` — zero lint errors expected.
    - Frontend verification (assumes plans 17-02/03/04/05 have run successfully):
      5. `cd frontend && npm run lint` — zero ESLint errors.
      6. `cd frontend && npm run build` — production build succeeds (validates MCU Vite bundling + ThemeProvider + ThemeCard imports).
      7. `cd frontend && npm run check:tokens` — 8/8 PASS including new Check #8.
      8. `cd frontend && npm run check:md3` — MD3 token compliance unchanged (12/12 or whatever Phase 12-16 baseline is).
    - Capture pass/fail per check. Build a verification matrix.
  </action>
  <verify>
    <automated>cd backend && uv run alembic current && cd backend && uv run pytest -q && cd backend && uv run pytest tests/test_themes.py -v && cd backend && uv run ruff check app/ tests/ && cd frontend && npm run lint && cd frontend && npm run build && cd frontend && npm run check:tokens && cd frontend && npm run check:md3</automated>
  </verify>
  <acceptance_criteria>
    - All 9 verification commands exit 0.
    - `uv run alembic current` prints the new revision hash (from plan 17-01 Task 1).
    - `uv run pytest -q` exits 0 with no FAIL lines (full backend green; Phase 16 TD-09 invariant preserved).
    - `uv run pytest tests/test_themes.py -v` shows 12 tests passing.
    - `uv run ruff check app/ tests/` returns 0 errors.
    - `npm run lint` exits 0 with no errors.
    - `npm run build` exits 0 and produces `frontend/dist/`.
    - `npm run check:tokens` prints `PASS: 8/8 令牌不变量检查通过` (8 of 8 including new Check #8).
    - `npm run check:md3` exits 0 (existing MD3 compliance baseline preserved).
  </acceptance_criteria>
  <done>All 9 automated verification commands green; Phase 16 TD-09 zero-failure invariant preserved; Phase 17 deliverables don't introduce regressions.</done>
</task>

<task type="auto">
  <name>Task 3: End-to-end smoke test of /api/themes (curl-based) + write SUMMARY.md with criterion→file mapping</name>
  <files>.planning/phases/17-theme-system-foundation-engine-page-presets-persistence/17-06-SUMMARY.md</files>
  <read_first>
    - backend/app/routers/themes.py (created in plan 17-01 — endpoint signatures, JWT requirement)
    - backend/app/main.py (AUTO_MIGRATE=1 default — container/local start runs alembic upgrade head before binding port 8000)
    - .planning/phases/17-theme-system-foundation-engine-page-presets-persistence/17-CONTEXT.md D-15/D-16/D-17 (mount-fetch + reconciliation + failure toast — these are wired in plan 17-04)
  </read_first>
  <action>
    Per Quality Gate Checklist — "must_haves map to the 7 ROADMAP success criteria":
    - Bring up the backend in the background (if not already running): `cd backend && uv run uvicorn app.main:app --port 8000 &` and wait ~5s for the startup migration to complete. (If AUTO_MIGRATE=0, explicitly run `cd backend && uv run alembic upgrade head` first.) Note the PID.
    - Run a curl-based smoke test for the 4 endpoints (validates SYNC-03 wiring is reachable from the frontend):
      1. Login as a seeded user (e.g., admin via /api/auth/login with credentials admin/admin123 — Phase 16 seed defaults). Capture JWT.
      2. POST /api/themes with `{name:'smoke', source_colors:{primary:'#34834E',secondary:'#506446',tertiary:'#F5B43C'}, variant:'TonalSpot'}` → expect 201 + body with id.
      3. GET /api/themes → expect array with 1 item (the smoke theme).
      4. PUT /api/themes/{id} with `{name:'smoke-renamed'}` → expect 200 + updated name.
      5. DELETE /api/themes/{id} → expect 204.
      6. GET /api/themes → expect empty array.
      7. Verify cross-user 403: log in as user2 (testuser/testuser123) and try GET /api/themes → expect empty array (admin's theme is gone, user2 has none).
    - Kill the backend process (`kill $PID`).
    - Write `.planning/phases/17-theme-system-foundation-engine-page-presets-persistence/17-06-SUMMARY.md` containing:
      1. Verification matrix (9 rows from Task 2 + 7 ROADMAP success criteria from Task 1)
      2. The curl smoke test transcript (expected status codes)
      3. Any observed regressions or warnings
       4. Sign-off statement: "Phase 17 verified ready for human UAT; all 6 plans executed; all 32 file actions (per plan files_modified inventory — PATTERNS.md §1 tables + B2/B3 revision files) present; all 9 automated checks green; 7 ROADMAP success criteria mapped to deliverables."
  </action>
  <verify>
    <automated>cd backend && uv run uvicorn app.main:app --port 8000 > /tmp/uvicorn.log 2>&1 & PID=$!; sleep 6; curl -s -X POST http://localhost:8000/api/auth/login -H 'Content-Type: application/json' -d '{"username":"admin","password":"admin123"}' | python3 -c "import sys,json; print(json.load(sys.stdin).get('access_token',''))" > /tmp/jwt.txt; JWT=$(cat /tmp/jwt.txt); echo "=== POST theme ==="; curl -s -w "HTTP %{http_code}\n" -X POST http://localhost:8000/api/themes -H "Authorization: Bearer $JWT" -H 'Content-Type: application/json' -d '{"name":"smoke","source_colors":{"primary":"#34834E","secondary":"#506446","tertiary":"#F5B43C"},"variant":"TonalSpot"}'; echo "=== GET themes ==="; curl -s -w "HTTP %{http_code}\n" http://localhost:8000/api/themes -H "Authorization: Bearer $JWT"; echo "=== DELETE theme ==="; THEME_ID=$(curl -s http://localhost:8000/api/themes -H "Authorization: Bearer $JWT" | python3 -c "import sys,json; print(json.load(sys.stdin)[0]['id'])"); curl -s -w "HTTP %{http_code}\n" -X DELETE "http://localhost:8000/api/themes/$THEME_ID" -H "Authorization: Bearer $JWT"; echo "=== GET themes (empty) ==="; curl -s -w "HTTP %{http_code}\n" http://localhost:8000/api/themes -H "Authorization: Bearer $JWT"; kill $PID 2>/dev/null; wait $PID 2>/dev/null; echo "DONE"</automated>
  </verify>
  <acceptance_criteria>
    - The curl smoke test runs against the live backend (uvicorn started on port 8000).
    - POST /api/themes returns HTTP 201 with a JSON body containing `id`, `name`, `variant`, `source_colors`, `created_at`, `updated_at`.
    - GET /api/themes returns HTTP 200 with a JSON array containing the just-created theme.
    - DELETE /api/themes/{id} returns HTTP 204.
    - GET /api/themes after delete returns HTTP 200 with empty array.
    - Backend process is killed cleanly (no orphaned uvicorn).
    - `.planning/phases/17-theme-system-foundation-engine-page-presets-persistence/17-06-SUMMARY.md` exists with verification matrix + criterion→file mapping + sign-off statement.
  </acceptance_criteria>
  <done>End-to-end smoke test passes; SUMMARY.md documents the verification; Phase 17 is ready for human UAT.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verification → live backend | The smoke test must not pollute production data; uses seed credentials and cleanups the created theme |
| Verification → git history | The SUMMARY.md is a planning artifact, not committed code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-27 | Tampering | Smoke test leaves test data in DB | mitigate | The smoke test DELETEs the theme it creates; if DELETE fails, manual cleanup via DELETE call or DB truncate. Acceptable because test environment uses seed data |
| T-17-28 | Denial of Service | Uvicorn process orphaned | mitigate | The verify block uses `kill $PID 2>/dev/null; wait $PID 2>/dev/null` to ensure cleanup |
| T-17-29 | Information Disclosure | Smoke test JWT in command line | accept | JWT is short-lived (24h per backend settings); test uses admin token only in local dev context; production data not affected |
</threat_model>

<verification>
- [ ] All 9 verification commands from Task 2 exit 0
- [ ] All 32 file actions from plans 17-01..17-05 files_modified exist on disk (Task 1 verify prints missing=0)
- [ ] Curl smoke test returns expected HTTP codes (201, 200, 204, 200)
- [ ] 17-06-SUMMARY.md exists with verification matrix + criterion mapping + sign-off
</verification>

<success_criteria>
All 7 ROADMAP Phase 17 success criteria are mapped to concrete files/behaviors; all 9 automated checks are green; the end-to-end smoke test confirms SYNC-03 is wired; the SUMMARY documents the deliverable for human UAT. Phase 17 verification complete.
</success_criteria>

<output>
Create `.planning/phases/17-theme-system-foundation-engine-page-presets-persistence/17-06-SUMMARY.md` when done. This is the verification gate that closes the phase and routes to UAT.
</output>