---
phase: 17
plan: 04
type: execute
wave: 2
depends_on:
  - 17-01
  - 17-02
files_modified:
  - frontend/src/theme/theme-context.jsx
  - frontend/src/theme/index.js
  - frontend/src/api/client.js
  - frontend/src/App.jsx
autonomous: true
requirements:
  - FND-03
  - FND-05
  - FND-06
  - SYNC-03
  - TPAGE-01
key_decisions:
  - D-08
  - D-09
  - D-11
  - D-15
  - D-16
  - D-17
  - D-20
must_haves:
  truths:
    - "useTheme() returns { activeTheme, setActiveTheme, customThemes, refreshCustomThemes, applyTheme, resetToDefault } with a memoized value object (re-renders only when deps change)"
    - "On ThemeProvider mount, apply injects <style id='fc-dynamic-theme'> via theme-engine.injectThemeCss() and persists active theme to localStorage fc_active_theme"
    - "On ThemeProvider mount (after auth resolves), fire-and-forget GET /api/themes via api.getThemes() sets customThemes state; on failure showToast('无法同步主题（请检查网络），已使用上次缓存', 'warn')"
    - "Switching active theme: setActiveTheme({...}) → re-injects style + writes localStorage"
    - "App.jsx provider stack order: AuthProvider → SnackbarProvider → CategoriesProvider → ThemeProvider → Routes — ThemeProvider is INSIDE the snackbar provider (so useToast works per ToastContext.jsx:364-367) and OUTSIDE the routed components that consume useTheme(); registers <Route path='/theme' element={<ProtectedRoute requiredRoles={['user','chef','admin']}><ThemePage /></ProtectedRoute>} /> inside the <Route element={<PcLayout />} /> block"
    - "ApiClient has working getThemes/createTheme/updateTheme/deleteTheme methods (4 new), all auto-attaching JWT via the existing getAuthHeader()"
    - "ApiClient theme methods normalize keys at the boundary: getThemes() maps response `source_colors` → `sourceColors`; createTheme()/updateTheme() map request `sourceColors` → `source_colors` (snake_case on the wire per project convention — client.js:17-110 does raw JSON with no auto key transform)"
  artifacts:
    - path: frontend/src/theme/theme-context.jsx
      provides: "React Provider + useTheme hook with useMemo value + apply side-effect + mount fetch"
    - path: frontend/src/theme/index.js
      provides: "Barrel updated to also re-export ThemeProvider + useTheme from theme-context"
    - path: frontend/src/api/client.js
      provides: "4 new ApiClient methods: getThemes, createTheme, updateTheme, deleteTheme"
    - path: frontend/src/App.jsx
      provides: "ThemeProvider wired into provider stack + /theme route registration"
  key_links:
    - from: frontend/src/theme/theme-context.jsx
      to: frontend/src/theme/theme-engine.js
      via: "import { buildCssSync, injectThemeCss }"
      pattern: "buildCssSync\\(activeTheme\\.sourceColors"
    - from: frontend/src/theme/theme-context.jsx
      to: frontend/src/api/client.js
      via: "api.getThemes() on mount"
      pattern: "api\\.getThemes\\(\\)"
    - from: frontend/src/theme/theme-context.jsx
      to: frontend/src/contexts/ToastContext.jsx
      via: "showToast('无法同步主题...') on mount-fetch failure"
      pattern: "showToast\\('无法同步主题"
    - from: frontend/src/App.jsx
      to: frontend/src/theme/theme-context.jsx
      via: "<ThemeProvider> nested INSIDE <SnackbarProvider> (stack: Auth > Snackbar > Categories > Theme > Routes)"
      pattern: "<ThemeProvider>"
    - from: frontend/src/api/client.js
      to: /api/themes
      via: "this.get/post/put/del('/themes')"
      pattern: "this\\.(get|post|put|del)\\('/themes"
---

<objective>
Wire the React layer for Phase 17: add 4 CRUD methods to the API client, build the memoized ThemeContext Provider + useTheme hook (consuming theme-engine + firing mount-fetch per D-15/D-16/D-17), register /theme route inside PcLayout in App.jsx, and re-export the new symbols from the theme/ barrel. Plan 17-05 (next) adds the actual page + Header entry button + theme cards that consume these primitives.

Purpose: FND-03 (light/dark x custom CSS cascade via existing style mechanism — no ThemeContext JS re-apply needed; just confirm via test), FND-05 (memoized ThemeContext value), FND-06 (elevation/surface-tint tracked via engine emit), SYNC-03 (mount-fetch + last-write-wins reconciliation in ThemeContext), TPAGE-01 partial (route registration; the entry button is in 17-05).

Output: 4 modified/new frontend files, Vite build succeeds, mount-fetch behavior unit-tested via the integration test in plan 17-06.
</objective>

<execution_context>
@/home/temila/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/temila/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/17-theme-system-foundation-engine-page-presets-persistence/17-CONTEXT.md
@.planning/phases/17-theme-system-foundation-engine-page-presets-persistence/17-PATTERNS.md
@frontend/src/contexts/AuthContext.jsx (context provider shape; PATTERNS notes ThemeContext DIVERGES with useMemo)
@frontend/src/contexts/ToastContext.jsx (showToast signature)
@frontend/src/theme/theme-engine.js (created in plan 17-02)
@frontend/src/theme/presets.js (created in plan 17-02 — DEFAULT_PRESET)
@frontend/src/theme/index.js (barrel from plan 17-02)
@frontend/src/api/client.js (ApiClient class; favorites block is the closest shape analog)
@frontend/src/App.jsx (provider stack lines 103-110; PcLayout block lines 121-290; wish route shape at line 266-273)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 4 theme CRUD methods to ApiClient</name>
  <files>frontend/src/api/client.js</files>
  <read_first>
    - frontend/src/api/client.js:254-268 (favorites block — closest analog)
    - frontend/src/api/client.js:371-385 (core HTTP methods: get/post/put/del)
    - frontend/src/api/client.js:11-14 (getAuthHeader — auto-JWT attachment)
  </read_first>
  <action>
    Per D-14 (no pagination on list) + PATTERNS API client pattern + BLOCKER B3a (explicit key normalization at the boundary):
    - KEY-CONVENTION NOTE: the backend wire format is snake_case `source_colors` (plan 17-01 ThemeResponse), while the frontend engine/Context consume camelCase `sourceColors`. `client.js:17-110` passes raw JSON with NO automatic key transformation, so the theme methods MUST map keys themselves. This matches the project's snake_case wire convention (favorites/wishes endpoints send snake_case payloads).
    - Edit frontend/src/api/client.js to insert a new // ─── Themes ─── block immediately AFTER the // ─── Favorites ─── block (after line 268, before the Preferences block at line 270). The 4 methods:
      - getThemes() — `const items = await this.get('/themes'); return (items || []).map(t => { const { source_colors, ...rest } = t; return { ...rest, sourceColors: source_colors }; });` (bare array, no PageResponse wrapper per D-14; rest-spread preserves `id`, `user_id`, `name`, `variant`, `created_at`, `updated_at` — `updated_at` is needed for D-16 reconciliation)
      - createTheme(data) — `this.post('/themes', { name: data.name, source_colors: { primary: data.sourceColors.primary, secondary: data.sourceColors.secondary, tertiary: data.sourceColors.tertiary }, variant: data.variant })`
      - updateTheme(id, data) — builds the payload by mapping `data.sourceColors` → `source_colors` when present: `const body = { ...data }; if (data.sourceColors) { body.source_colors = { primary: data.sourceColors.primary, secondary: data.sourceColors.secondary, tertiary: data.sourceColors.tertiary }; delete body.sourceColors; } return this.put(`/themes/${id}`, body);`
      - deleteTheme(id) — returns this.del(`/themes/${id}`) (null on 204)
    - All 4 inherit JWT via this.request() calling getAuthHeader() — no per-method override needed.
  </action>
  <verify>
    <automated>cd frontend && node -e "import('./src/api/client.js').then(m => { const api = m.api; for (const k of ['getThemes','createTheme','updateTheme','deleteTheme']) if (typeof api[k] !== 'function') throw new Error(k+' missing'); console.log('OK'); })" && cd frontend && grep -n "source_colors" src/api/client.js | head -8 && cd frontend && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/api/client.js contains a comment header // ─── Themes AND 4 method declarations: async getThemes(), async createTheme(data), async updateTheme(id, data), async deleteTheme(id).
    - The 4 methods each call this.get/post/put/del with the appropriate /themes or /themes/${id} URL.
    - The theme methods contain an explicit `source_colors` ↔ `sourceColors` key mapping: getThemes() maps the response's `source_colors` to `sourceColors`; createTheme() and updateTheme() map `sourceColors` → `source_colors` in the request body (verified via grep — `source_colors` appears in the Themes block).
    - cd frontend && node -e "..." confirms all 4 methods exist as functions on the api singleton.
    - cd frontend && npm run lint exits 0 (no ESLint regressions in client.js).
  </acceptance_criteria>
  <done>4 theme CRUD methods available on api singleton with JWT auto-attach and explicit snake_case ↔ camelCase key normalization at the boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Create ThemeContext (Provider + useTheme hook with memoized value + apply side-effect + mount fetch)</name>
  <files>frontend/src/theme/theme-context.jsx, frontend/src/theme/index.js</files>
  <read_first>
    - frontend/src/contexts/AuthContext.jsx:12-60 (current context provider shape — DIVERGES by adding useMemo)
    - frontend/src/contexts/ToastContext.jsx:260-281 (showToast(message, 'warn') string-tone overload)
    - frontend/src/theme/theme-engine.js (buildCssSync + injectThemeCss exports from plan 17-02)
    - frontend/src/theme/presets.js (DEFAULT_PRESET export from plan 17-02)
    - frontend/src/api/client.js (modified in Task 1 — api.getThemes available)
    - PATTERNS Cross-File Patterns React Context pattern (memoized value with useMemo)
  </read_first>
  <action>
    Per D-11 (memoized value + apply side-effect) + D-15/D-16/D-17 (mount-fetch + updatedAt reconciliation + failure toast):
    - Create frontend/src/theme/theme-context.jsx (JSX file, NOT .js — it returns JSX). Module docstring: "主题 Context — Phase 17 (D-11 memoized value + D-15/16/17 sync)". Imports from 'react': createContext, useCallback, useContext, useEffect, useMemo, useRef, useState. Imports from local: '../contexts/ToastContext' (useToast), '../contexts/AuthContext' (useAuth), '../api/client' (api). Imports from './theme-engine': buildCssSync, injectThemeCss. Imports from './presets': DEFAULT_PRESET, PRESETS.
    - Define localStorage key constant ACTIVE_THEME_KEY = 'fc_active_theme'.
    - Define helper readActiveThemeFromStorage() that:
      1. Tries localStorage.getItem(ACTIVE_THEME_KEY).
      2. If null/empty, returns DEFAULT_PRESET.
      3. Tries JSON.parse. If JSON invalid or missing required fields (sourceColors.primary/secondary/tertiary), returns DEFAULT_PRESET.
      4. Otherwise returns the parsed object; determines `kind` from the persisted `kind` field — preset if `kind === 'preset'`, else 'custom' (if `kind` is absent, infer: if the blob's `sourceColors` deep-equals any preset's sourceColors → 'preset', else 'custom'). This prevents presets from reloading as 'custom' (which would wrongly gate the D-16 reconciliation branch).
    - Define helper writeActiveThemeToStorage(theme) that writes JSON.stringify({ sourceColors: theme.sourceColors, variant: theme.variant, kind: theme.kind, id: theme.id }) to localStorage — the `id` and `kind` fields are a superset of the D-10 minimum contract `{sourceColors, variant}` and are required so the active-card indicator (SC5) and the D-16 custom-theme gate work correctly after a reload.
    - Export ThemeProvider component:
      - useState(readActiveThemeFromStorage) for activeTheme
      - useState([]) for customThemes
      - useRef(null) for fetchedAtRef (ISO string of last successful fetch; null means "never fetched"). CRITICAL: a ref, NOT useState — a state-backed fetchedAt causes either an infinite refetch loop (if fetchedAt is in the refreshCustomThemes useCallback deps, every setFetchedAt re-triggers the mount-fetch effect) or a permanently dead guard (if omitted from deps, the closure keeps the initial null forever). Ref mutation does not re-render, so neither failure mode can occur.
      - useEffect keyed on [activeTheme, showToast]: try { buildCssSync + injectThemeCss + writeActiveThemeToStorage } catch { showToast('主题应用失败，已恢复默认', 'error'); setActiveThemeState(DEFAULT_PRESET) }
      - useCallback refreshCustomThemes: gated on user; on success setCustomThemes + set fetchedAtRef.current = new Date().toISOString() + D-16 reconciliation — guard with `if (activeTheme.kind === 'custom' && fetchedAtRef.current !== null && activeTheme.id != null && Date.parse(fresh.updated_at) > Date.parse(fetchedAtRef.current))`: match the active custom theme in the fetched list (by `id` if present, else by sourceColors deep-equality) → `setActiveTheme(matchedFetchedTheme)` (this is the actual cross-device re-apply target — re-injecting the stale activeTheme would make the toast a lie and the sync a no-op) → toast '已同步最新主题', 'success'. The `fetchedAtRef.current !== null` guard prevents the spurious first-mount toast (fetchedAtRef starts null; on the very first successful fetch `fetchedAtRef.current` is null → guard fails → no toast, then the ref is set for subsequent mounts); compare via Date.parse() not string `>` because backend emits second-precision ISO while Date.toISOString() includes milliseconds — lexicographic comparison breaks sub-second timestamps; on failure showToast('无法同步主题（请检查网络），已使用上次缓存', 'warn') per D-17
      - useEffect keyed on [user?.id, refreshCustomThemes]: queueMicrotask → refreshCustomThemes() (D-15 fire-and-forget)
      - useCallback setActiveTheme(theme): normalizes kind field (preset vs custom)
      - useCallback resetToDefault: setActiveThemeState(DEFAULT_PRESET)
      - const value = useMemo(() => ({ activeTheme, setActiveTheme, customThemes, refreshCustomThemes, resetToDefault, PRESETS }), [activeTheme, customThemes, setActiveTheme, refreshCustomThemes, resetToDefault])
      - Returns ThemeContext.Provider with value wrapping children
    - Export useTheme hook: useContext + throw on null
    - Default export ThemeContext
    - Edit frontend/src/theme/index.js (barrel from plan 17-02) to append: export { ThemeProvider, useTheme } from './theme-context'; (after presets re-export)
  </action>
  <verify>
    <automated>cd frontend && node -e "import('./src/theme/index.js').then(m => { if (typeof m.ThemeProvider !== 'function') throw 'no ThemeProvider'; if (typeof m.useTheme !== 'function') throw 'no useTheme'; if (!Array.isArray(m.PRESETS) || m.PRESETS.length !== 5) throw 'PRESETS wrong'; console.log('OK'); })" && cd frontend && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/theme/theme-context.jsx exists with export const ThemeProvider, export const useTheme, and export default ThemeContext.
    - Inside ThemeProvider, the value object is constructed via useMemo(() => ({...}), [deps]) (not a fresh object literal — the load-bearing divergence per D-11).
    - useEffect keyed on [activeTheme, showToast] calls injectThemeCss(buildCssSync(activeTheme.sourceColors, activeTheme.variant)) and writeActiveThemeToStorage(activeTheme).
    - refreshCustomThemes calls api.getThemes(), sets setCustomThemes(...) on success, calls showToast('无法同步主题（请检查网络），已使用上次缓存', 'warn') on failure (D-17).
    - D-16 reconciliation guard: refreshCustomThemes' reconciliation branch requires `fetchedAtRef.current !== null` (no spurious first-mount toast; fetchedAtRef is a useRef, NOT useState — a state-backed fetchedAt either infinite-loops the mount-fetch effect or dead-ends the guard, see Task 2 action), compares timestamps via Date.parse() (backend second-precision ISO vs JS toISOString() milliseconds), matches the active custom theme in the fetched list by `id` (or sourceColors deep-equality), and calls `setActiveTheme(matchedFetchedTheme)` (the re-apply target is the fetched fresh theme, not the stale activeTheme).
    - writeActiveThemeToStorage persists `{ sourceColors, variant, kind, id }` (superset of D-10 minimum contract) so the active-card indicator survives reload.
    - readActiveThemeFromStorage determines `kind` from the persisted `kind` field, falling back to sourceColors deep-equality against presets (presets never reload as 'custom').
    - setActiveTheme normalizes preset vs custom by setting kind field.
    - frontend/src/theme/index.js re-exports ThemeProvider and useTheme (verified via node -e smoke test).
    - cd frontend && npm run lint exits 0.
  </acceptance_criteria>
  <done>ThemeContext provides memoized value, applies theme on mount + change, fetches custom themes on user login, surfaces sync failures via toast.</done>
</task>

<task type="auto">
  <name>Task 3: Wire ThemeProvider into App.jsx provider stack + register /theme route + verify build</name>
  <files>frontend/src/App.jsx</files>
  <read_first>
    - frontend/src/App.jsx:1-32 (imports block)
    - frontend/src/App.jsx:101-110 (provider stack — AuthProvider wraps CategoriesProvider wraps SnackbarProvider)
    - frontend/src/App.jsx:121-290 (PcLayout block with all the Route entries inside it)
    - frontend/src/App.jsx:266-289 (the wish routes — exact shape to mirror for /theme registration)
  </read_first>
  <action>
    Per D-20 (/theme route registration) + BLOCKER B3b (ThemeProvider must be INSIDE the snackbar provider):
    - Edit frontend/src/App.jsx:
      1. Add import after line 4 (import { CategoriesProvider } from './contexts/CategoriesContext';): import { ThemeProvider } from './theme';
      2. Add import after line 29 (import AdminWishesPage from './pages/AdminWishesPage';): import ThemePage from './pages/ThemePage';. NOTE: ThemePage is created by plan 17-05; if it does not yet exist when this plan runs, create a temporary placeholder at frontend/src/pages/ThemePage.jsx with: `export default function ThemePage() { return <div className="page-container"><h1>主题</h1><p>主题页面建设中（Phase 17-05 待实施）</p></div>; }` so Vite build doesn't break. Replace placeholder in plan 17-05.
      3. Modify the provider stack (currently `AuthProvider > CategoriesProvider > SnackbarProvider > Routes` at lines 103-107) to the EXACT order `AuthProvider → SnackbarProvider → CategoriesProvider → ThemeProvider → Routes`:
         - `AuthProvider` stays outermost (ThemeContext calls useAuth()).
         - `SnackbarProvider` (ToastContext's provider) moves up to wrap CategoriesProvider — REQUIRED because ThemeContext's mount-fetch failure calls `showToast` (D-17) and `useToast` throws outside SnackbarProvider (ToastContext.jsx:364-367).
         - `CategoriesProvider` stays inside SnackbarProvider.
         - `ThemeProvider` is inserted inside CategoriesProvider, wrapping `<Routes>` — so it is INSIDE the snackbar provider (useToast works) and OUTSIDE all routed components that consume useTheme().
         - `<Routes>` (and everything routed) stays innermost inside ThemeProvider. BrowserRouter remains outermost of all.
      4. Add <Route path="/theme" element={<ProtectedRoute requiredRoles={['user','chef','admin']}><ThemePage /></ProtectedRoute>} /> inside the <Route element={<PcLayout />}></Route> block (between the wishes routes at lines 282-289 and closing </Route> at line 290).
    - Run cd frontend && npm run build and confirm exit 0.
  </action>
  <verify>
    <automated>cd frontend && npm run build && cd frontend && grep -E "ThemeProvider|SnackbarProvider|/theme" src/App.jsx | head -10</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/App.jsx imports ThemeProvider from ./theme (or relative path).
    - frontend/src/App.jsx imports ThemePage from ./pages/ThemePage.
    - frontend/src/App.jsx provider stack nesting is `AuthProvider > SnackbarProvider > CategoriesProvider > ThemeProvider > Routes` — ThemeProvider is nested INSIDE SnackbarProvider (verified by reading the JSX nesting order; the SnackbarProvider open/close tags wrap the ThemeProvider tags).
    - frontend/src/App.jsx PcLayout block contains <Route path="/theme" element={<ProtectedRoute requiredRoles={['user','chef','admin']}>...<ThemePage />...</ProtectedRoute>} />.
    - cd frontend && npm run build exits 0.
    - The built dist/index.html references the new chunks for the theme module (Vite splits lazy imports).
  </acceptance_criteria>
  <done>ThemeProvider wired inside SnackbarProvider + /theme route registered; build green; ThemeContext ready to consume from plan 17-05's ThemePage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser localStorage → ThemeProvider | JSON in fc_active_theme may be malformed; must fall back to default gracefully |
| Backend /api/themes → customThemes state | Network failures must not crash the app; must toast per D-17 |
| Auth state → mount-fetch | Mount-fetch must only fire after user is authenticated; user.id change re-triggers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-15 | Tampering | localStorage.getItem('fc_active_theme') JSON | mitigate | readActiveThemeFromStorage() validates shape (sourceColors.primary/secondary/tertiary) and falls back to DEFAULT_PRESET on parse error or missing fields |
| T-17-16 | Tampering | Malformed theme.sourceColors from backend | mitigate | buildCssSync (engine) throws on invalid shape; ThemeContext's apply useEffect wraps in try/catch, shows toast '主题应用失败，已恢复默认', resets to DEFAULT_PRESET |
| T-17-17 | Information Disclosure | Backend response leaks other users' themes | accept | Backend SYNC-04 enforcement (plan 17-01 service-layer WHERE user_id == current_user.id) is the authoritative gate; frontend getThemes() trust boundary is implicitly enforced by the JWT-protected endpoint |
| T-17-18 | Denial of Service | Mount-fetch failure loops | mitigate | refreshCustomThemes is wrapped in try/catch; failure only calls showToast (no setState that would re-trigger). useEffect dependency on user?.id only re-fires on login/logout, not on every refresh failure |
| T-17-19 | Repudiation | updatedAt stale data | accept | D-16 last-write-wins: only when activeTheme.kind==='custom' && fetchedAtRef.current !== null && activeTheme.id != null && Date.parse(updated_at) > Date.parse(fetchedAtRef.current) — match fetched theme by id (or sourceColors equality) → setActiveTheme(matched). fetchedAtRef is a useRef (no re-render loop); null-start guard prevents spurious first-mount toast. Race condition (fetch during user edit) accepted; documented as Phase 18 hardening opportunity |
| T-17-20 | Elevation of Privilege | Frontend route guards | mitigate | /theme requires roles ['user','chef','admin'] via ProtectedRoute; backend additionally requires JWT for /api/themes (plan 17-01). Two layers. |
</threat_model>

<verification>
- [ ] cd frontend && npm run build exits 0
- [ ] cd frontend && npm run lint exits 0
- [ ] cd frontend && node -e "import('./src/theme/index.js').then(m => ...)" confirms ThemeProvider + useTheme + PRESETS exported
- [ ] ThemeProvider's value is wrapped in useMemo (search for useMemo in theme-context.jsx)
- [ ] Mount-fetch api.getThemes() is called inside refreshCustomThemes
- [ ] Failure toast string '无法同步主题（请检查网络），已使用上次缓存' exists in theme-context.jsx
- [ ] D-16 reconciliation in refreshCustomThemes guards on `fetchedAtRef.current !== null` (fetchedAtRef is a useRef — grep `useRef` in theme-context.jsx; no useState-backed fetchedAt, which would infinite-loop or dead-end the guard) and calls setActiveTheme with the matched fetched theme (grep `setActiveTheme(fetchedMatch)` or equivalent in theme-context.jsx); timestamps compared via Date.parse()
- [ ] writeActiveThemeToStorage persists kind + id alongside sourceColors + variant (active indicator survives reload)
- [ ] client.js Themes block contains source_colors ↔ sourceColors key mapping (grep source_colors in client.js)
- [ ] App.jsx provider stack: ThemeProvider nested inside SnackbarProvider (Auth > Snackbar > Categories > Theme > Routes)
- [ ] /theme route registered inside PcLayout with requiredRoles={['user','chef','admin']}
</verification>

<success_criteria>
FND-05 (ThemeContext with memoized value), FND-06 (engine emit covers dark-mode elevation + surface-tint — delivered in plan 17-02), SYNC-03 (mount-fetch + last-write-wins reconciliation in ThemeContext), and TPAGE-01 partial (route registered; entry button in plan 17-05) all delivered.
</success_criteria>

<output>
Create .planning/phases/17-theme-system-foundation-engine-page-presets-persistence/17-04-SUMMARY.md when done.
</output>