---
phase: 17
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check-tokens.sh
autonomous: true
requirements:
  - FND-07
key_decisions:
  - D-23
must_haves:
  truths:
    - "scripts/check-tokens.sh contains 8 checks (Check #1 through Check #8)"
    - "Check #8 fails if any .jsx file under frontend/src/components/ or frontend/src/pages/ contains a hardcoded `#RRGGBB` literal"
    - "Check #8 passes when the codebase has zero JSX hex literals (the desired invariant)"
    - "Running `bash scripts/check-tokens.sh` with no violations exits 0; running it after introducing a test violation exits 1"
  artifacts:
    - path: scripts/check-tokens.sh
      provides: "8-check MD3 token regression gate including new Check #8 JSX hex-lint"
  key_links:
    - from: scripts/check-tokens.sh
      to: frontend/src/components/**/*.jsx
      via: "rg --glob '*.jsx' pattern"
      pattern: "rg -n --no-heading \"#[0-9a-fA-F]{6}\\b\" --glob '\\*\\.jsx'"
---

<objective>
Add Check #8 to `scripts/check-tokens.sh` — a JSX hex-lint gate that fails CI when any `.jsx` file under `frontend/src/components/` or `frontend/src/pages/` contains a hardcoded `#RRGGBB` hex literal in a `color:` or `background:` style prop. Preserves the 0-match invariant by scoping the `rg` to `.jsx` only (so `presets.js` and `theme-engine.js` hex emitters are naturally exempt). Verified by introducing + removing a test violation.

Purpose: FND-07 (hex-lint CI gate maintained). The CSS-side Check #3 already covers hex literals in `.css`; this new check covers the JSX style-prop surface where Phase 17's `ThemeCard` / `ThemePreview` / `ThemePage` files live.

Output: 1 modified script, 8 total checks, demonstrated pass/fail behavior.
</objective>

<execution_context>
@/home/temila/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/temila/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/17-theme-system-foundation-engine-page-presets-persistence/17-CONTEXT.md
@.planning/phases/17-theme-system-foundation-engine-page-presets-persistence/17-PATTERNS.md
@scripts/check-tokens.sh (current 7-check shape; Check #3 line 56-61 is the template)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Check #8 (JSX hex-lint) to scripts/check-tokens.sh + update header comment</name>
  <files>scripts/check-tokens.sh</files>
  <read_first>
    - scripts/check-tokens.sh:56-61 (Check #3 hex-in-CSS — pattern template)
    - scripts/check-tokens.sh:63-77 (Check #4-6 RGBA/radius — additional pattern references)
    - scripts/check-tokens.sh:79-95 (Check #7 tokens.css invariant)
    - scripts/check-tokens.sh:1-13 (header comment block listing the checks)
  </read_first>
  <action>
    Per D-23 (extend check-tokens.sh with JSX hex-lint):
    - Edit the file header comment block (lines 1-13) to add a 7th and 8th item:
      ```
      #   6. JSX 中的硬编码 borderRadius: <非零>number
      #   7. tokens.css 存在且包含全部 MD3 令牌族
      #   8. JSX 中的硬编码 #xxxxxx 十六进制色值（Phase 17 D-23 — 排除 .js 后缀的 presets.js / theme-engine.js）
      ```
    - Insert a new Check #8 block after Check #7 (after line 95, before the "最终汇总" section line 97). The block:
      ```bash
      # Check #8 — JSX 中的硬编码 #xxxxxx 十六进制色值（Phase 17 D-23）
      # presets.js / theme-engine.js 因扩展名为 .js 不匹配 --glob '*.jsx'，天然豁免
      JSX_HEX_OUTPUT=$(rg -n --no-heading "#[0-9a-fA-F]{6}\b" \
          --glob '*.jsx' \
          "$FRONTEND_DIR/src/components/" \
          "$FRONTEND_DIR/src/pages/" 2>/dev/null || true)
      check "JSX 中的 #xxxxxx 硬编码色值 (Check #8)" "$JSX_HEX_OUTPUT"
      ```
      - `FRONTEND_DIR` is already defined at line 18 (`$PROJECT_DIR/frontend`), so the variable resolves correctly.
      - The `--glob '*.jsx'` filter excludes `presets.js` and `theme-engine.js` automatically (those have `.js` extension). No explicit `--glob '!*presets*'` needed.
      - The `check()` helper function (lines 28-37) handles the failure increment + display.
    - Save the file. Verify the bash syntax is valid with `bash -n scripts/check-tokens.sh`.
  </action>
  <verify>
    <automated>bash -n scripts/check-tokens.sh && grep -cE "^# Check #8|^JSX_HEX_OUTPUT=|^check \"JSX" scripts/check-tokens.sh</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/check-tokens.sh` header comment lists 8 checks (Check #1 through Check #8).
    - `scripts/check-tokens.sh` contains the `JSX_HEX_OUTPUT=$(rg ... --glob '*.jsx' ...)` block.
    - `scripts/check-tokens.sh` contains `check "JSX 中的 #xxxxxx 硬编码色值 (Check #8)" "$JSX_HEX_OUTPUT"`.
    - `bash -n scripts/check-tokens.sh` exits 0 (no syntax errors).
    - `grep -cE "JSX_HEX_OUTPUT|JSX 中的 #xxxxxx" scripts/check-tokens.sh` returns a count ≥ 2.
  </acceptance_criteria>
  <done>Check #8 block added; file is syntactically valid bash; header comment updated.</done>
</task>

<task type="auto">
  <name>Task 2: Verify Check #8 passes on the current codebase + fails on a synthetic violation</name>
  <files>scripts/check-tokens.sh (verification only)</files>
  <read_first>
    - scripts/check-tokens.sh (modified in Task 1 — the new Check #8 rg pattern)
    - frontend/src/components/primitives/Icon.jsx (existing JSX file — confirm no hex literals present today)
    - frontend/src/pages/UserHomePage.jsx (existing JSX file — confirm no hex literals present today)
  </read_first>
  <action>
    Two-step verification:
    1. Clean baseline (expect pass): Run `bash scripts/check-tokens.sh` from the project root. Expected: script prints `PASS: 8/8 令牌不变量检查通过` and exits 0. (Check #8 should produce zero output because no `.jsx` file under `frontend/src/components/` or `frontend/src/pages/` contains a hardcoded `#RRGGBB` literal — the 0-match invariant.)
    2. Synthetic violation (expect fail): Create a temporary sentinel file at `/tmp/opencode/test-jsx-hex-violation.jsx` (not under the repo, so as not to commit it; the rg scopes to `frontend/src/` so an out-of-tree file would NOT be detected). Instead, perform an in-tree negative test: run `rg -n --no-heading "#[0-9a-fA-F]{6}\b" --glob '*.jsx' frontend/src/components/ frontend/src/pages/ | head -5` — expected output is EMPTY (proving 0 violations on the current codebase). Then, to demonstrate the gate WOULD fail on a violation, run a synthetic test: `echo 'const X = () => <div style={{ color: "#ff0000" }} />' > /tmp/test-violation.jsx && cd frontend && rg -n --no-heading "#[0-9a-fA-F]{6}\b" --glob '*.jsx' /tmp/test-violation.jsx` — expected: the synthetic file is detected with one match. Remove the synthetic file after verification with `rm /tmp/test-violation.jsx`.
    3. Final clean state: Run `bash scripts/check-tokens.sh` once more — exit 0 + `PASS: 8/8`.
  </action>
  <verify>
    <automated>bash scripts/check-tokens.sh && bash -c 'echo "<div style={{color: \"#ff0000\"}} />" > /tmp/_test_hex.jsx && rg -n --no-heading "#[0-9a-fA-F]{6}\b" --glob "*.jsx" /tmp/_test_hex.jsx; rm -f /tmp/_test_hex.jsx'</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/check-tokens.sh` exits 0 and prints `PASS: 8/8 令牌不变量检查通过`.
    - `rg -n --no-heading "#[0-9a-fA-F]{6}\b" --glob '*.jsx' frontend/src/components/ frontend/src/pages/` returns NO output (empty result, confirming 0-match invariant).
    - The synthetic violation test (`echo '<div style={{color: "#ff0000"}} />' > /tmp/_test_hex.jsx && rg ... /tmp/_test_hex.jsx`) returns at least 1 matching line, demonstrating the rg pattern works.
    - `/tmp/_test_hex.jsx` is removed after the test (the temporary file does not persist).
  </acceptance_criteria>
  <done>Check #8 is wired in, passes on the current 0-match codebase, and demonstrably fails on synthetic violation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → repo | The script's exit code is the gate; must be deterministic across environments |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-12 | Tampering | CI script bypass via `set +e` or `set -u` removal | mitigate | Script uses `set -u` (line 14); any future edit that disables it would be caught by code review. The `check()` helper increments `FAILURES` deterministically and exits 1 on any failure. |
| T-17-13 | Spoofing | False negative from glob escape | mitigate | `--glob '*.jsx'` is passed to `rg` as a separate argument; tested with synthetic violation in Task 2. Bash syntax validated with `bash -n`. |
| T-17-14 | Repudiation | Hidden hex literals in JSX style props | mitigate | Check #8 rg pattern catches both `color: '#xxxxxx'` and `background: '#xxxxxx'` style props via the generic `#[0-9a-fA-F]{6}\b` regex (hex literals are the only way to embed raw colors in JSX style objects; preset hex values are in `.js` files naturally excluded). |
</threat_model>

<verification>
- [ ] `bash -n scripts/check-tokens.sh` exits 0
- [ ] `bash scripts/check-tokens.sh` exits 0 with `PASS: 8/8`
- [ ] Synthetic JSX hex violation is detected by the new rg pattern
- [ ] Header comment lists 8 checks
</verification>

<success_criteria>
FND-07 delivered: hex-lint CI gate extended with Check #8 covering JSX style-prop hex literals; current codebase passes (0-match invariant preserved); synthetic violation demonstrates the gate's effectiveness.
</success_criteria>

<output>
Create `.planning/phases/17-theme-system-foundation-engine-page-presets-persistence/17-03-SUMMARY.md` when done.
</output>