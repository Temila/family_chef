---
phase: 08-md3-design-token-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 08-02
files_modified:
  - frontend/src/css/styles.css
  - frontend/src/components/ChefSelectModal.jsx
  - frontend/src/components/ConfirmModal.jsx
  - frontend/src/components/CreateLinkModal.jsx
  - frontend/src/components/EmptyState.jsx
  - frontend/src/components/InvitationsModal.jsx
  - frontend/src/components/InvitationsSection.jsx
  - frontend/src/components/WishAdvanceModal.jsx
  - frontend/src/components/WishCard.jsx
  - frontend/src/components/WishRejectModal.jsx
  - frontend/src/pages/AdminCategoriesPage.jsx
  - frontend/src/pages/AdminChefsPage.jsx
  - frontend/src/pages/AdminDishesPage.jsx
  - frontend/src/pages/AdminHomePage.jsx
  - frontend/src/pages/AdminIngredientsPage.jsx
  - frontend/src/pages/AdminLogsPage.jsx
  - frontend/src/pages/AdminStatsPage.jsx
  - frontend/src/pages/AdminUsersPage.jsx
  - frontend/src/pages/ChefDishesPage.jsx
  - frontend/src/pages/ChefWishesPage.jsx
  - frontend/src/pages/DishDetailPage.jsx
  - frontend/src/pages/GuestOrderPage.jsx
  - frontend/src/pages/OrderDetailPage.jsx
  - frontend/src/pages/OrderPage.jsx
  - frontend/src/pages/PreferencesPage.jsx
  - frontend/src/pages/UserHomePage.jsx
  - frontend/src/pages/UserOrdersPage.jsx
  - frontend/src/pages/UserProfilePage.jsx
  - frontend/src/pages/UserWishesPage.jsx
  - scripts/check-tokens.sh
  - frontend/package.json
autonomous: true
requirements:
  - TOKEN-13
  - MOTION-04
  - MOTION-05
  - UX-02
  - LOGIC-01
  - LOGIC-02
  - LOGIC-03
user_setup: []

must_haves:
  truths:
    - "MD3 focus ring tokens (--md-focus-ring-outer: 2px var(--md-color-on-primary); --md-focus-ring-inner: 2px var(--md-color-surface)) are applied to all interactive elements via :focus-visible"
    - "All hardcoded border-radius values (4px/6px/9px/3px/5px) in foundation files (styles.css + JSX inline styles) are replaced with MD3 radius tokens"
    - "Motion duration/easing consumers added in Wave 2 are extended with additional emphasized-easing applications for elevation transitions and focus ring reveals"
    - "A regression guard script (scripts/check-tokens.sh) prevents reintroduction of old token names, raw hex colors outside tokens.css, and hardcoded border-radius values"
    - "npm run check:tokens passes locally and exits with non-zero on regression"
  artifacts:
    - path: "frontend/src/css/styles.css"
      provides: "Focus ring tokens consumed via :focus-visible; foundation hardcoded radii swept to --md-radius-*"
      contains: ":focus-visible"
    - path: "scripts/check-tokens.sh"
      provides: "Regression guard script — fails on old token names, raw hex outside tokens.css, hardcoded border-radius"
      min_lines: 30
    - path: "frontend/package.json"
      provides: "npm script check:tokens wired to scripts/check-tokens.sh"
      contains: "check:tokens"
  key_links:
    - from: "scripts/check-tokens.sh"
      to: "frontend/src/css/styles.css and frontend/src/{components,pages}/"
      via: "rg grep checks"
      pattern: "rg.*var\\(--(accent|bg-|text-|border|shadow|font|transition|nav|radius|size-unread|space-wish|unread-dot|gold|warn|danger|info|success|primary\\)|warning)"
    - from: "frontend/package.json scripts.check:tokens"
      to: "scripts/check-tokens.sh"
      via: "npm script wrapper"
      pattern: "check:tokens.*check-tokens\\.sh"
---

<objective>
Complete the MD3 design-token foundation with three final layers: (1) apply the MD3 focus ring tokens (`--md-focus-ring-outer` 2px On-Primary + `--md-focus-ring-inner` 2px Surface) via `:focus-visible` to every interactive element — buttons, inputs, filter chips, list items, search inputs; (2) sweep all hardcoded `border-radius: <px>` numeric values (4px/6px/9px/3px/5px) from `frontend/src/css/styles.css` and from inline `style={{ borderRadius: <number> }}` literals across all 28 JSX files, replacing each with the appropriate MD3 radius token (`--md-radius-xs/sm/md/lg/xl/full`); (3) add a regression guard `scripts/check-tokens.sh` (Bash + ripgrep) that fails CI on any new old-name reference, any raw hex outside `tokens.css`, or any numeric `border-radius` declaration, and wire it as `npm run check:tokens` in `frontend/package.json`. This is the closing sweep for the foundation layer — TOKEN-13 closes here, MOTION-04 finalizes the focus ring application, MOTION-05 adds the remaining emphasized-easing uses for elevation transitions, UX-02 ships the focus ring on interactive surfaces.

Purpose: After Wave 3, the frontend cannot regress the foundation layer accidentally — a developer adding `border-radius: 4px` anywhere will fail CI. Phase 9-12 work proceeds against a sealed token surface.

Output: `scripts/check-tokens.sh` exists and passes; `npm run check:tokens` is wired; foundation files have zero hardcoded radii; `:focus-visible` rules apply MD3 focus rings on buttons/inputs/chips/list items; lint/build still green; Wave 3 success criteria #5 from ROADMAP.md (lint防护 + 4px/6px sweep) fully satisfied.
</objective>

<execution_context>
@/home/temila/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/temila/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/phases/08-md3-design-token-foundation/08-CONTEXT.md
@.planning/phases/08-md3-design-token-foundation/08-RESEARCH.md
@.planning/phases/08-md3-design-token-foundation/08-PATTERNS.md
@.planning/phases/08-md3-design-token-foundation/08-01-PLAN.md
@.planning/phases/08-md3-design-token-foundation/08-01-SUMMARY.md
@.planning/phases/08-md3-design-token-foundation/08-02-PLAN.md
@.planning/phases/08-md3-design-token-foundation/08-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply MD3 focus ring tokens via :focus-visible + extended motion easing consumers</name>
  <files>frontend/src/css/styles.css</files>
  <read_first>
    - frontend/src/css/styles.css (post-Wave-2 state — color/shape/elevation/typography/motion tokens fully wired)
    - frontend/src/css/tokens.css (focus ring token definitions: `--md-focus-ring-outer: 2px solid var(--md-color-on-primary)` and `--md-focus-ring-inner: 2px solid var(--md-color-surface)`)
    - .planning/phases/08-md3-design-token-foundation/08-RESEARCH.md (MD3 focus ring pattern)
    - .planning/phases/08-md3-design-token-foundation/08-PATTERNS.md (audit of focus styles currently in styles.css: .search-bar input:focus line 138, .form-input:focus line 206, .btn:focus-visible absent, .filter-chip:focus-visible absent)
    - .planning/phases/08-md3-design-token-foundation/08-CONTEXT.md D-04 (theme extension capability — focus ring must work in both light and dark mode)
  </read_first>
  <action>
    Apply MD3 focus ring tokens across `frontend/src/css/styles.css`. The MD3 focus ring is the standard `2px On-Primary outer ring + 2px Surface inner ring` pattern that reveals when an interactive element receives keyboard focus and stays invisible during mouse interaction. The tokens `--md-focus-ring-outer` and `--md-focus-ring-inner` are already defined in `tokens.css` (Wave 1 generator output).

    Add the following focus rules to `styles.css`. Place them at the END of the file (after all component selectors) so they have the highest specificity override where needed:

    1. Global `:focus-visible` baseline. Add at the bottom of the file:
       - `:focus-visible { outline: 2px solid var(--md-color-primary); outline-offset: 2px; }` — a sensible default that applies to any interactive element (buttons, links, inputs) without explicit focus styling.
       - `:focus:not(:focus-visible) { outline: none; }` — suppress the default outline during mouse interaction so the ring only appears on keyboard focus.

    2. Component-level focus styles. UPDATE the existing focus rules to use the MD3 focus ring pattern instead of the old `box-shadow: 0 0 0 3px var(--accent-light)` approach:
       - `.search-bar input:focus` (currently line 138: `border-color: var(--md-color-primary); box-shadow: 0 0 0 3px var(--md-color-primary-container);`) → change the `box-shadow` declaration to use the focus ring pattern. Specifically: REPLACE the `box-shadow: 0 0 0 3px var(--md-color-primary-container);` with `outline: 2px solid var(--md-color-primary); outline-offset: 2px;` and REMOVE the `border-color` override (the focus ring replaces the border highlight). Add `box-shadow: 0 0 0 4px var(--md-color-primary-container);` for the inner-ring effect.
       - `.form-input:focus` (currently line 206: `border-color: var(--md-color-primary); box-shadow: 0 0 0 3px var(--md-color-primary-container);`) → same pattern: use outline + inner box-shadow ring, remove border-color override.
       - ADD `:focus-visible` rule to `.btn` family: append after `.btn:disabled, .btn[disabled]` (line 96): `.btn:focus-visible { outline: var(--md-focus-ring-outer); outline-offset: 2px; box-shadow: 0 0 0 4px var(--md-focus-ring-inner); }`. The `--md-focus-ring-outer` token resolves to `2px solid var(--md-color-on-primary)` and `--md-focus-ring-inner` resolves to `2px solid var(--md-color-surface)` per tokens.css.
       - ADD `:focus-visible` rule to `.filter-chip` family: append after `.filter-chip.active` (around line 132): `.filter-chip:focus-visible { outline: var(--md-focus-ring-outer); outline-offset: 2px; box-shadow: 0 0 0 4px var(--md-focus-ring-inner); }`.
       - ADD `:focus-visible` rule to `.list-item` (line 152): `.list-item:focus-visible { outline: var(--md-focus-ring-outer); outline-offset: -2px; }` (negative offset to keep the ring inside the row).
       - ADD `:focus-visible` rule to `.dish-card` (line 104): `.dish-card:focus-visible { outline: var(--md-focus-ring-outer); outline-offset: 2px; }`.
       - ADD `:focus-visible` rule to `.card` (line 99): `.card:focus-visible { outline: var(--md-focus-ring-outer); outline-offset: 2px; }`.
       - ADD `:focus-visible` rule to `.order-card` (line 229): `.order-card:focus-visible { outline: var(--md-focus-ring-outer); outline-offset: 2px; }`.

    3. Extended motion easing for elevation transitions. Several components transition their box-shadow between elevation levels (`.card:hover` line 100 changes from elevation-0 to elevation-2 via `border-color` + `box-shadow`). Update those transitions to use the `--md-motion-easing-emphasized` token for a more pronounced MD3 feel:
       - `.card` line 99 currently `transition: all var(--md-motion-duration-medium) var(--md-motion-easing-standard);` → change the easing segment to `var(--md-motion-easing-emphasized)` so card hover feels more "alive" per MD3 spec.
       - `.dish-card` line 104 same pattern.
       - `.quick-action:hover` line 223 currently uses `box-shadow: var(--md-elevation-1)`; add a transition declaration if absent, or update existing transition to use emphasized easing.

    4. Verify completeness. After the edits, run the master verification grep and confirm:
       - `styles.css` contains at least 5 references to `var(--md-focus-ring-outer)` and at least 5 references to `var(--md-focus-ring-inner)` (or `var(--md-focus-ring-inner)` used as a `box-shadow` color for the inner ring effect — substitute with `var(--md-color-surface)` if the design prefers the explicit form).
       - `styles.css` contains at least 2 `:focus-visible` selectors.
       - `styles.css` contains at least 4 references to `var(--md-motion-easing-emphasized)`.

    Do NOT add focus-visible rules that would override `:focus` on the same element — keep `:focus-visible` separate so mouse users do not see the ring.
  </action>
  <verify>
    <automated>cd frontend && bash -c 'set -e; frouter=$(rg -c "var\(--md-focus-ring-outer\)" src/css/styles.css | awk -F: "{s+=\$2} END {print s}"); frinner=$(rg -c "var\(--md-focus-ring-inner\)" src/css/styles.css | awk -F: "{s+=\$2} END {print s}"); test "$frouter" -ge 5; test "$frinner" -ge 3; fv=$(rg -c ":focus-visible" src/css/styles.css | awk -F: "{s+=\$2} END {print s}"); test "$fv" -ge 5; emph=$(rg -c "var\(--md-motion-easing-emphasized\)" src/css/styles.css | awk -F: "{s+=\$2} END {print s}"); test "$emph" -ge 3; npm run build' </automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/css/styles.css` contains ≥5 references to `var(--md-focus-ring-outer)`
    - `frontend/src/css/styles.css` contains ≥3 references to `var(--md-focus-ring-inner)`
    - `frontend/src/css/styles.css` contains ≥5 `:focus-visible` selectors
    - `frontend/src/css/styles.css` contains ≥3 references to `var(--md-motion-easing-emphasized)`
    - `.search-bar input:focus` and `.form-input:focus` no longer use `box-shadow: 0 0 0 3px var(--md-color-primary-container)`; they use the MD3 outline + box-shadow ring pattern
    - Global `:focus-visible` and `:focus:not(:focus-visible)` baseline rules exist at the end of the file
    - `cd frontend && npm run build` exits 0
  </acceptance_criteria>
  <done>
    `frontend/src/css/styles.css` has the MD3 focus ring applied across all interactive surfaces (buttons, inputs, filter chips, list items, cards, order cards). Mouse interaction does not trigger the ring; keyboard tab does. Elevation transitions use emphasized easing for a more pronounced MD3 feel. Build succeeds.
  </done>
</task>

<task type="auto">
  <name>Task 2: Sweep hardcoded border-radius from styles.css and JSX inline styles</name>
  <files>frontend/src/css/styles.css, frontend/src/components/ChefSelectModal.jsx, frontend/src/components/ConfirmModal.jsx, frontend/src/components/CreateLinkModal.jsx, frontend/src/components/EmptyState.jsx, frontend/src/components/InvitationsModal.jsx, frontend/src/components/InvitationsSection.jsx, frontend/src/components/WishAdvanceModal.jsx, frontend/src/components/WishCard.jsx, frontend/src/components/WishRejectModal.jsx, frontend/src/pages/AdminCategoriesPage.jsx, frontend/src/pages/AdminChefsPage.jsx, frontend/src/pages/AdminDishesPage.jsx, frontend/src/pages/AdminHomePage.jsx, frontend/src/pages/AdminIngredientsPage.jsx, frontend/src/pages/AdminLogsPage.jsx, frontend/src/pages/AdminStatsPage.jsx, frontend/src/pages/AdminUsersPage.jsx, frontend/src/pages/ChefDishesPage.jsx, frontend/src/pages/ChefWishesPage.jsx, frontend/src/pages/DishDetailPage.jsx, frontend/src/pages/GuestOrderPage.jsx, frontend/src/pages/OrderDetailPage.jsx, frontend/src/pages/OrderPage.jsx, frontend/src/pages/PreferencesPage.jsx, frontend/src/pages/UserHomePage.jsx, frontend/src/pages/UserOrdersPage.jsx, frontend/src/pages/UserProfilePage.jsx, frontend/src/pages/UserWishesPage.jsx</files>
  <read_first>
    - frontend/src/css/styles.css (post-Wave-2 + post-Task 1 — line numbers may have shifted, locate by content)
    - .planning/phases/08-md3-design-token-foundation/08-PATTERNS.md:596-610 (hardcoded radius audit — full list of known numeric border-radius values)
    - .planning/phases/08-md3-design-token-foundation/08-CONTEXT.md D-08 (FAB radius already swept in Wave 2 to 16px; verify it's not regressed here)
    - frontend/src/css/styles.css (locate `.badge-count` ~line 125, `.markdown-body code` ~line 476, `.fab` ~line 195 — already verified by Task 1 to be 16px)
    - frontend/src/pages/AdminDishesPage.jsx:554, 619 (numeric 4px radii)
    - frontend/src/pages/ChefDishesPage.jsx:576, 640 (numeric 4px radii)
    - frontend/src/pages/DishDetailPage.jsx:151 (numeric 4px)
    - frontend/src/pages/AdminIngredientsPage.jsx:564 (numeric 999)
    - frontend/src/pages/InvitationsModal.jsx:110-111 (intentional `borderRadius: 0` for full-screen modal — DO NOT touch)
  </read_first>
  <action>
    Sweep `frontend/src/css/styles.css` for hardcoded `border-radius: <px>` numeric values. Use `rg -n "border-radius:\s*[0-9]+(px|%)?" frontend/src/css/styles.css` to find every match. For each match, replace with the appropriate MD3 radius token. Special cases to honor:
    - `border-radius: 0` and `border-radius: 50%` are intentional (full-bleed rectangles and circles). Leave them unchanged.
    - `border-radius: 9px` at `.badge-count` (line 125) → `border-radius: var(--md-radius-full)` (the 18px badge-count pill should round fully, not at 9px; MD3 small badge uses --md-radius-full). Verify the visual size still looks pill-shaped.
    - `border-radius: 3px` at `.markdown-body code` (line 476) → `border-radius: var(--md-radius-xs)` (8px is the closest MD3 token; the 3px was Vite residue).
    - `border-radius: var(--md-radius-md)` at `.fab` (line 195, already 16px per Wave 2 D-08) — leave unchanged.
    - Any other numeric `border-radius` (4px, 5px, 6px, 8px, etc.) → map to the closest MD3 token: 4/5/6/8 → `--md-radius-xs` (8px), 12 → `--md-radius-sm`, 16 → `--md-radius-md`, 24 → `--md-radius-lg`, 28 → `--md-radius-xl`. Prefer the larger MD3 token if the original was between two values (e.g., 6px → 8px because MD3 has no 6px tier).

    Sweep all 28 JSX files for inline `style={{ borderRadius: <number> }}` literals. Use `rg -n "borderRadius:\s*[0-9]+" frontend/src/components/ frontend/src/pages/` to find every match. Per file, apply the replacement via the Edit tool. Same mapping rules as above. Special cases:
    - `borderRadius: 0` (intentional, e.g., `InvitationsModal.jsx:110-111` full-screen modal) → leave unchanged.
    - `borderRadius: '50%'` (intentional circle, e.g., avatar with full roundness) → leave unchanged.
    - `borderRadius: 4` → `borderRadius: 'var(--md-radius-xs)'`.
    - `borderRadius: 999` or `borderRadius: 9999` → `borderRadius: 'var(--md-radius-full)'`.
    - `borderRadius: 12` → `borderRadius: 'var(--md-radius-sm)'`.
    - `borderRadius: 16` → `borderRadius: 'var(--md-radius-md)'`.
    - `borderRadius: 24` → `borderRadius: 'var(--md-radius-lg)'`.
    - `borderRadius: 28` → `borderRadius: 'var(--md-radius-xl)'`.

    Preserve all surrounding JSX structure. Do NOT modify onClick, onChange, useState, useEffect, API calls, props, or component composition. Only change the value of the `borderRadius` prop within `style={{ ... }}`.

    Known target sites per PATTERNS.md:596-610:
    - `frontend/src/pages/AdminDishesPage.jsx:554, 619` (numeric 4)
    - `frontend/src/pages/ChefDishesPage.jsx:576, 640` (numeric 4)
    - `frontend/src/pages/DishDetailPage.jsx:151` (numeric 4)
    - `frontend/src/pages/AdminIngredientsPage.jsx:564` (numeric 999)

    After all edits, run `rg -n "borderRadius:\s*[0-9]+" frontend/src/components/ frontend/src/pages/` and `rg -n "border-radius:\s*[0-9]+px" frontend/src/css/styles.css` — both must return zero results (other than the intentional `borderRadius: 0` and `border-radius: 0` exceptions, which use a different regex pattern with no digit-after-zero — they won't match `[0-9]+` if the regex is written strictly as `[0-9]+` against `0` ... actually `0` matches `[0-9]+`, so refine the grep to exclude `0`: `borderRadius:\s*[1-9][0-9]*\b` and `border-radius:\s*[1-9][0-9]*px`).

    Final build verification: `cd frontend && npm run build` exits 0; `cd frontend && npm run lint` ≤ pre-existing baseline + 5 errors.
  </action>
  <verify>
    <automated>cd frontend && bash -c 'set -e; css_left=$(rg -n "border-radius:\s*[1-9][0-9]*px" src/css/styles.css | rg -v "var\(" | wc -l); test "$css_left" -eq 0; jsx_left=$(rg -n "borderRadius:\s*[1-9][0-9]*[,\s}]" src/components/ src/pages/ | rg -v "var\(" | wc -l); test "$jsx_left" -eq 0; npm run build' </automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/css/styles.css` contains zero `border-radius: <nonzero>px` numeric declarations (e.g., `border-radius: 4px`, `border-radius: 6px`, `border-radius: 9px`)
    - `frontend/src/css/styles.css` retains its existing `border-radius: 0` and `border-radius: 50%` (intentional) — these are excluded by the nonzero regex
    - All 28 JSX files contain zero `borderRadius: <nonzero>number` numeric literals in inline `style={{ ... }}` props (excluding `0` and `50%` and `'9999'` if quoted string)
    - `.badge-count` in styles.css uses `var(--md-radius-full)` (was 9px)
    - `.markdown-body code` in styles.css uses `var(--md-radius-xs)` (was 3px)
    - `.fab` in styles.css still uses `var(--md-radius-md)` 16px (D-08 not regressed)
    - `InvitationsModal.jsx` retains its intentional `borderRadius: 0` on the full-screen modal
    - `cd frontend && npm run build` exits 0
  </acceptance_criteria>
  <done>
    All hardcoded `border-radius` numeric values in `styles.css` and 28 JSX inline styles have been replaced with MD3 radius tokens. Intentional `borderRadius: 0` and `border-radius: 50%` exceptions are preserved. Build green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add regression guard script and wire as npm run check:tokens</name>
  <files>scripts/check-tokens.sh, frontend/package.json</files>
  <read_first>
    - .planning/phases/08-md3-design-token-foundation/08-RESEARCH.md (Pattern 3 lint-gated token sweep; Common Pitfalls section)
    - .planning/phases/08-md3-design-token-foundation/08-PATTERNS.md:686-691 (no-close-analog for stylelint vs bash script)
    - .planning/phases/08-md3-design-token-foundation/08-CONTEXT.md D-05 (no-aliases invariant the guard must enforce)
    - frontend/src/css/styles.css (post-Task 2 — must be passing all checks so the guard has a known-good baseline)
    - frontend/package.json (existing scripts structure: dev/build/lint/preview/gen:tokens)
    - scripts/run.sh (existing Bash convention — path resolution from script location)
  </read_first>
  <action>
    Create `scripts/check-tokens.sh` as a Bash regression guard. The script must:

    1. Resolve project paths. Use the same pattern as `scripts/run.sh:1-6`: `SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"`, then `PROJECT_DIR="$(dirname "$SCRIPT_DIR")"`, then `FRONTEND_DIR="$PROJECT_DIR/frontend"`. This makes the script work regardless of cwd.

    2. Define the failure counter. Initialize `FAILURES=0` and a `fail()` helper that echoes `[FAIL] <message>` to stderr and increments the counter.

    3. Check #1 — no old color/bg/text/border/shadow/semantic/ghost/wish token names anywhere under `frontend/src/`. Run:
       `rg -n --no-heading "var\(--(accent|bg-(primary|secondary|card|card-hover|elevated|input|page|hover)|text-(primary|secondary|muted)|border(-medium|-strong)?|shadow-(sm|md|lg|accent)|success(-light)?|warn(-light)?|danger(-light)?|info(-light)?|gold(-light)?|unread-dot|size-unread-dot|space-wish-card-stack|primary\)|warning-bg|warning-text)\b" "$FRONTEND_DIR/src/" || true`
       If any lines are output, fail with message "old token names found in <file>:<line>".

    4. Check #2 — no old shape/typography/motion/nav-height token names anywhere under `frontend/src/`. Run:
       `rg -n --no-heading "var\(--(radius-(sm|md|lg|xl|full)|font-(display|body)|transition-(fast|normal)|nav-height|shadow-)\b" "$FRONTEND_DIR/src/" || true`
       Same fail-on-output behavior.

    5. Check #3 — no raw `#xxxxxx` hex color literals outside `tokens.css`. Run:
       `rg -n --no-heading "#[0-9a-fA-F]{6}\b" "$FRONTEND_DIR/src/css/styles.css" "$FRONTEND_DIR/src/components/" "$FRONTEND_DIR/src/pages/" || true`
       Fail on output. (The regex intentionally does NOT match 3-digit hex `#abc` because those don't appear in the codebase; 6-digit hex only.)

    6. Check #4 — no raw `rgba(...)` color literals outside `tokens.css`. Run:
       `rg -n --no-heading "rgba\(" "$FRONTEND_DIR/src/css/styles.css" || true`
       If output, inspect each match: the only allowed rgba consumers are inside `var(--md-elevation-*)` definitions (which live in `tokens.css`, NOT `styles.css` — `styles.css` only CONSUMES elevation tokens). So any rgba in `styles.css` is a regression. Fail on output.

    7. Check #5 — no hardcoded `border-radius: <nonzero>px` in `styles.css`. Run:
       `rg -n --no-heading "border-radius:\s*[1-9][0-9]*px" "$FRONTEND_DIR/src/css/styles.css" || true`
       Fail on output.

    8. Check #6 — no hardcoded `borderRadius: <nonzero>number` in JSX. Run:
       `rg -n --no-heading "borderRadius:\s*[1-9][0-9]*[,\s}]" "$FRONTEND_DIR/src/components/" "$FRONTEND_DIR/src/pages/" || true`
       Fail on output.

    9. Check #7 — sanity: `tokens.css` exists and contains the required MD3 token families. Run:
       `test -f "$FRONTEND_DIR/src/css/tokens.css" && grep -q "^:root {" "$FRONTEND_DIR/src/css/tokens.css" && grep -q '\[data-theme="dark"\] {' "$FRONTEND_DIR/src/css/tokens.css" && grep -q "^\s*--md-radius-xs:\s*8px" "$FRONTEND_DIR/src/css/tokens.css" && grep -q "^\s*--md-elevation-0:" "$FRONTEND_DIR/src/css/tokens.css" && grep -q "^\s*--md-font-body:" "$FRONTEND_DIR/src/css/tokens.css" && grep -q "^\s*--md-focus-ring-outer:" "$FRONTEND_DIR/src/css/tokens.css" || fail "tokens.css missing or incomplete"`
       Fail if any required token is absent.

    10. Print final summary. After all checks, print:
        - `PASS: <count>/<total> token invariant checks passed` if zero failures
        - `FAIL: <count>/<total> token invariant checks failed` if any failed
        - Exit code 0 on pass, 1 on any failure.

    Make the script executable: `chmod +x scripts/check-tokens.sh`.

    Add the npm script entry `"check:tokens": "bash ../scripts/check-tokens.sh"` to `frontend/package.json` `scripts` block, placed after `build` and before `lint` to keep alphabetical/colonic ordering.

    Run the script once via `bash scripts/check-tokens.sh` to confirm exit 0 (all checks pass). Then INTENTIONALLY introduce a regression by adding `var(--accent)` to a test file, run again to confirm exit 1, then revert the test change.

    Final verification: `cd frontend && npm run check:tokens` exits 0; `cd frontend && npm run build` exits 0; `cd frontend && npm run lint` ≤ pre-existing baseline + 5 errors.
  </action>
  <verify>
    <automated>cd /home/temila/family_chef && chmod +x scripts/check-tokens.sh && bash scripts/check-tokens.sh && cd frontend && npm run check:tokens && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/check-tokens.sh` exists, is executable, is ≥30 lines
    - `scripts/check-tokens.sh` checks for: old color/bg/text/border/shadow/semantic/ghost/wish tokens, old shape/typography/motion/nav-height tokens, raw hex outside tokens.css, raw rgba in styles.css, hardcoded border-radius in styles.css, hardcoded borderRadius in JSX, tokens.css token family presence
    - `scripts/check-tokens.sh` exits 0 when all checks pass
    - `scripts/check-tokens.sh` exits 1 when any check fails (tested by introducing a temporary `var(--accent)` in a test JSX file)
    - `frontend/package.json` contains the literal entry `"check:tokens": "bash ../scripts/check-tokens.sh"`
    - `cd frontend && npm run check:tokens` exits 0
    - `cd frontend && npm run build` exits 0
    - `cd frontend && npm run lint` ≤ pre-existing baseline + 5 errors
  </acceptance_criteria>
  <done>
    `scripts/check-tokens.sh` is in place as a deterministic regression guard against reintroduction of old tokens, raw colors, and hardcoded radii. `npm run check:tokens` is wired and exits 0 on a clean tree. Any developer who adds `var(--accent)`, raw `#xxxxxx`, raw `rgba()`, or numeric `border-radius` anywhere under `frontend/src/` will fail the guard and have to use the MD3 tokens instead. Foundation layer sealed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Wave 3 is foundation sealing — no new boundaries introduced. The check-tokens.sh script is a local-only Bash tool, not a network service.

| Boundary | Description |
|----------|-------------|
| scripts/check-tokens.sh → frontend/src/ | Local ripgrep checks; no network access; no auth surface |
| focus ring application → user keyboard | Browser focus event handling; MD3 spec-compliant keyboard accessibility |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-W3-01 | Tampering | Hardcoded `border-radius: 4px` reintroduced in a future commit | mitigate | `check-tokens.sh` Check #5 (styles.css) and Check #6 (JSX) fail CI on any nonzero numeric border-radius |
| T-08-W3-02 | Tampering | Old `var(--accent)` reintroduced in a future commit | mitigate | `check-tokens.sh` Check #1 fails CI on any old color/bg/text/border/shadow/semantic/ghost/wish token reference |
| T-08-W3-03 | Spoofing | Focus ring disabled via `:focus { outline: none; }` override | mitigate | Global `:focus-visible` baseline rule at end of styles.css ensures every interactive element shows the ring by default; component-level overrides cannot disable it without an explicit `:focus-visible { outline: none }` which would be obvious in code review |
| T-08-W3-04 | Information Disclosure | check-tokens.sh reveals internal token structure | accept | Token structure is intentionally public per MD3 spec; the script's regexes are not sensitive |
| T-08-W3-05 | Denial of Service | check-tokens.sh runs slowly on large codebase | mitigate | `rg` (ripgrep) is fast even on 10k+ files; script completes in < 5 seconds on the current 28-JSX-file codebase; no expensive operations |
| T-08-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages in Wave 3 — all work is against Wave 1's pinned deps |

## Package Legitimacy Audit (from RESEARCH.md)

| Package | slopcheck | Disposition |
|---------|-----------|-------------|
| `@material/material-color-utilities` 0.4.0 | [OK] | Approved (Wave 1) |
| `material-symbols` 0.45.9 | [OK] | Approved (Wave 1) |

No new packages in Wave 3.
</threat_model>

<verification>
1. `bash scripts/check-tokens.sh` exits 0 (clean tree)
2. `cd frontend && npm run check:tokens` exits 0
3. `cd frontend && npm run build` exits 0
4. `cd frontend && npm run lint` ≤ pre-existing baseline + 5 errors
5. Regression test: add `var(--accent)` to any JSX file, run check-tokens.sh, confirm exit 1, revert
6. `:focus-visible` selectors work in browser keyboard tab navigation
7. `getComputedStyle(document.activeElement).outline` on a focused button shows the MD3 focus ring
8. Wave 3 success criteria #5 from ROADMAP.md (TOKEN-13 hardcoded sweep + lint防护) fully satisfied
</verification>

<success_criteria>
1. MD3 focus ring tokens (`--md-focus-ring-outer` 2px On-Primary + `--md-focus-ring-inner` 2px Surface) are applied to all interactive elements via `:focus-visible` rules (buttons, inputs, filter chips, list items, search inputs, cards, order cards)
2. Global `:focus-visible` baseline at end of styles.css ensures keyboard accessibility by default
3. Global `:focus:not(:focus-visible) { outline: none }` suppresses mouse-only focus rings
4. All hardcoded `border-radius: <px>` (4px/6px/9px/3px/5px) in `frontend/src/css/styles.css` are replaced with MD3 radius tokens
5. All hardcoded `borderRadius: <number>` (4/6/999/etc.) in inline `style={{ ... }}` props across all 28 JSX files are replaced with MD3 radius tokens
6. `scripts/check-tokens.sh` exists as a deterministic regression guard with 7+ invariant checks (old color tokens, old shape tokens, raw hex outside tokens.css, raw rgba in styles.css, hardcoded border-radius in styles.css, hardcoded borderRadius in JSX, tokens.css presence/sanity)
7. `npm run check:tokens` is wired and exits 0 on a clean tree
8. Backend untouched (no changes to `backend/`, `alembic/`, or Python deps)
9. JSX business logic, props, event handlers, state hooks, API client calls, and component exports are byte-identical to pre-Wave-3 state
10. Build green, lint within tolerance, guard green
</success_criteria>

<output>
Create `.planning/phases/08-md3-design-token-foundation/08-03-SUMMARY.md` when done.
</output>