---
phase: 08-md3-design-token-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - frontend/src/css/styles.css
  - frontend/src/components/ChefSelectModal.jsx
  - frontend/src/components/ConfirmModal.jsx
  - frontend/src/components/CreateLinkModal.jsx
  - frontend/src/components/EmptyState.jsx
  - frontend/src/components/InvitationsModal.jsx
  - frontend/src/components/InvitationsSection.jsx
  - frontend/src/components/WishAdvanceModal.jsx
  - frontend/src/components/WishCard.jsx
  - frontend/src/components/WishRejectModal.jsx
  - frontend/src/pages/AdminCategoriesPage.jsx
  - frontend/src/pages/AdminChefsPage.jsx
  - frontend/src/pages/AdminDishesPage.jsx
  - frontend/src/pages/AdminHomePage.jsx
  - frontend/src/pages/AdminIngredientsPage.jsx
  - frontend/src/pages/AdminLogsPage.jsx
  - frontend/src/pages/AdminStatsPage.jsx
  - frontend/src/pages/AdminUsersPage.jsx
  - frontend/src/pages/ChefDishesPage.jsx
  - frontend/src/pages/ChefWishesPage.jsx
  - frontend/src/pages/DishDetailPage.jsx
  - frontend/src/pages/GuestOrderPage.jsx
  - frontend/src/pages/OrderDetailPage.jsx
  - frontend/src/pages/OrderPage.jsx
  - frontend/src/pages/PreferencesPage.jsx
  - frontend/src/pages/UserHomePage.jsx
  - frontend/src/pages/UserOrdersPage.jsx
  - frontend/src/pages/UserProfilePage.jsx
  - frontend/src/pages/UserWishesPage.jsx
autonomous: true
requirements:
  - TOKEN-01
  - TOKEN-02
  - TOKEN-03
  - TOKEN-04
  - TOKEN-05
  - TOKEN-10
  - TOKEN-12
  - UX-05
  - LOGIC-01
  - LOGIC-02
  - LOGIC-03
user_setup: []

must_haves:
  truths:
    - "5 level radius tokens (--md-radius-xs/sm/md/lg/xl + --md-radius-full) are used across all 28 JSX inline styles and styles.css component rules"
    - "5 level elevation tokens (--md-elevation-0..5) replace all shadow consumers; FAB uses 16px (--md-radius-md) per D-08"
    - "8 spacing tokens (--md-spacing-1..8) are available in tokens.css and the var() form is referenceable for any future declaration"
    - "Chinese-English font stacks (--md-font-display, --md-font-body) are wired to the application and visible in browser"
    - "No remaining references to old shape/spacing/elevation/typography/nav-height/motion tokens in styles.css or any JSX file"
  artifacts:
    - path: "frontend/src/css/styles.css"
      provides: "Component stylesheet consuming --md-radius-*, --md-spacing-*, --md-elevation-*, --md-font-*, --md-nav-height, --md-motion-duration-*, --md-motion-easing-*"
      contains: "var(--md-radius-"
    - path: "frontend/src/css/tokens.css"
      provides: "Source of truth for all MD3 tokens including radius, spacing, elevation, motion, typography"
      contains: "--md-radius-xs:8px"
  key_links:
    - from: "frontend/src/css/styles.css"
      to: "frontend/src/css/tokens.css"
      via: "var(--md-*) references"
      pattern: "var\\(--md-(radius|spacing|elevation|font|motion|nav-height)"
    - from: "frontend/src/components/Sidebar.jsx and BottomBar.jsx"
      to: "var(--md-nav-height)"
      via: "CSS height references in styles.css"
      pattern: "var\\(--md-nav-height\\)"
---

<objective>
Apply the MD3 shape, spacing, elevation, typography, and motion-family (duration/easing) tokens across `frontend/src/css/styles.css` and all 28 JSX files. The tokens themselves are already defined in `frontend/src/css/tokens.css` (Wave 1); this plan is the consumer-side rewrite that activates them. Specifically: 5 level radius system (`--md-radius-xs` 8px / `--md-radius-sm` 12px / `--md-radius-md` 16px / `--md-radius-lg` 24px / `--md-radius-xl` 28px / `--md-radius-full` 9999px), 5 level elevation (`--md-elevation-0..5`), 8dp spacing grid (`--md-spacing-1..8`), Chinese-English font stacks (`--md-font-display`, `--md-font-body`), navigation height (`--md-nav-height`), and motion duration/easing (`--md-motion-duration-{short,medium,long}` + `--md-motion-easing-{standard,emphasized}`). The FAB shape (line 195 of styles.css) is moved to MD3 standard 16px per D-08.

Purpose: With color tokens applied (Wave 1) and shape/elevation/spacing/typography tokens applied (Wave 2), every component CSS class in the codebase references only MD3 names — the foundation layer is complete, and Phase 10/11 primitive + composite components can drop in without any further token introduction.

Output: All 28 JSX files and `styles.css` use `--md-radius-*`, `--md-spacing-*`, `--md-elevation-*`, `--md-font-*`, `--md-nav-height`, `--md-motion-duration-*`, `--md-motion-easing-*`; zero old shape/spacing/elevation/typography/motion/nav-height tokens remain; build succeeds; FAB renders as MD3 standard 16px corner.
</objective>

<execution_context>
@/home/temila/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/temila/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/phases/08-md3-design-token-foundation/08-CONTEXT.md
@.planning/phases/08-md3-design-token-foundation/08-RESEARCH.md
@.planning/phases/08-md3-design-token-foundation/08-PATTERNS.md
@.planning/phases/08-md3-design-token-foundation/08-01-PLAN.md (parent plan)
@.planning/phases/08-md3-design-token-foundation/08-01-SUMMARY.md (parent plan SUMMARY)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite styles.css shape, spacing, elevation, typography, motion, nav-height tokens</name>
  <files>frontend/src/css/styles.css</files>
  <read_first>
    - frontend/src/css/styles.css (entire file in its post-Wave-1 state — color tokens already migrated to MD3, but shape/spacing/elevation/typography/motion/nav-height still reference old names)
    - frontend/src/css/tokens.css (Wave 1 output — confirm exact MD3 token names)
    - .planning/phases/08-md3-design-token-foundation/08-PATTERNS.md (mapping table at lines 540-578)
    - .planning/phases/08-md3-design-token-foundation/08-CONTEXT.md D-08 (FAB radius override: 56dp/Extended FAB use 16px = --md-radius-md; the original ROADMAP locked 28px is now overridden)
  </read_first>
  <action>
    Apply the shape/spacing/elevation/typography/motion/nav-height token rewrite to `frontend/src/css/styles.css` end-to-end. After Wave 1, this file's `:root { ... }` and `[data-theme="dark"] { ... }` blocks have been removed, so no declarations remain to delete — the work here is purely consumer-side replacement.

    Apply these replacements verbatim (case-sensitive, no partial-token matches):
    - `var(--radius-sm)` (old 8px) → `var(--md-radius-xs)` (8px). The 8px value is unchanged; the name moves from radius-sm to radius-xs in the MD3 hierarchy.
    - `var(--radius-md)` (old 12px) → `var(--md-radius-sm)` (12px). Value unchanged; name moves down one tier.
    - `var(--radius-lg)` (old 16px) → `var(--md-radius-md)` (16px). Value unchanged; name moves down one tier.
    - `var(--radius-xl)` (old 24px) → `var(--md-radius-lg)` (24px). Value unchanged; name moves down one tier.
    - `var(--radius-full)` (9999px) → `var(--md-radius-full)` (9999px). Same value, same semantic.
    - `--md-radius-xl` (28px) is defined in tokens.css but has zero current consumers in styles.css; do NOT introduce fake consumers in Wave 2 — Phase 10/11 components will adopt it.
    - `var(--nav-height)` → `var(--md-nav-height)`. Value remains 64px; name moves to MD3 namespace. Consumers: `.page-container` line 59, `.bottom-bar` line 72, `.order-bar` line 251, `.cart-bar` line 256, `.cart-detail-panel` line 261, `.fab` line 195.
    - `var(--font-display)` → `var(--md-font-display)`. Consumers: `.header-title` line 66, `.dish-card-name` line 111, `.section-title` line 143, `.login-logo` line 201, `.stat-value` line 218, `.guest-confirm-title` line 500, `.guest-error-title` line 509, plus any markdown-body selectors.
    - `var(--font-body)` → `var(--md-font-body)`. Consumers: `body` line 52, `button` line 55, `input/textarea/select` line 56.
    - `var(--transition-fast)` → split into `var(--md-motion-duration-short) var(--md-motion-easing-standard)` inside the `transition:` property. This is a value-level replacement, not a token-name rename: each consumer currently reads `transition: <prop> var(--transition-fast);` and must become `transition: <prop> var(--md-motion-duration-short) var(--md-motion-easing-standard);`. Common sites: `.header-back` line 67, `.tab-item` line 75, `.btn` line 83, `.btn-icon` line 90, `.search-bar input` line 136, `.list-item` line 152, `.theme-toggle` line 166, `.password-toggle-btn` line 213, `.quick-action` line 222, `.qty-stepper button` line 190, `.fab` line 195.
    - `var(--transition-normal)` → split into `var(--md-motion-duration-medium) var(--md-motion-easing-standard)`. Consumers: `.card` line 99, `.dish-card` line 104, plus any `.wish-card` if present.
    - FAB radius (D-08 override): at line 195, the `.fab` rule's `border-radius` declaration → `border-radius: var(--md-radius-md)` (16px per D-08). Add a Chinese inline comment above the rule explaining the override.
    - Elevation tokens (`--md-elevation-0..5`) were wired in Wave 1 Task 2. Verify completeness in this task: every `var(--shadow-*)` should already be gone; if a survivor exists, fix it.

    The `--md-spacing-1..8` tokens are defined in tokens.css but NOT retroactively applied to existing `padding: 16px` / `margin-bottom: 8px` / `gap: 8px` declarations in Wave 2 — those are page-level sweeps that belong to Phase 12. Wave 2 only ensures the tokens are available; Phase 12 will sweep consumers.

    The 5 line `@import url('https://fonts.googleapis.com/css2?family=Noto+...')` at line 7 (top of styles.css) remains unchanged — the Google Fonts CDN import is the runtime font delivery mechanism; the `--md-font-*` tokens select FROM the loaded fonts. Do not modify line 7.

    After all replacements, run a verification grep and fix any old token reference that survives. The grep must cover: `var(--radius-`, `var(--font-`, `var(--transition-`, `var(--nav-height)`, `var(--shadow-`, `var(--size-unread-dot)`, `var(--space-wish-card-stack)`. Confirm FAB at line 195 uses `var(--md-radius-md)`.
  </action>
  <verify>
    <automated>cd frontend && bash -c 'set -e; ! rg -q "var\(--radius-" src/css/styles.css; ! rg -q "var\(--font-" src/css/styles.css; ! rg -q "var\(--transition-" src/css/styles.css; ! rg -q "var\(--nav-height\)" src/css/styles.css; ! rg -q "var\(--shadow-" src/css/styles.css; ! rg -q "var\(--size-unread-dot\)" src/css/styles.css; ! rg -q "var\(--space-wish-card-stack\)" src/css/styles.css; rg -q "var\(--md-radius-md\)" src/css/styles.css; rc=$(rg -c "var\(--md-radius-" src/css/styles.css | awk -F: "{s+=\$2} END {print s}"); test "$rc" -ge 15; rc2=$(rg -c "var\(--md-font-" src/css/styles.css | awk -F: "{s+=\$2} END {print s}"); test "$rc2" -ge 2; rc3=$(rg -c "var\(--md-motion-" src/css/styles.css | awk -F: "{s+=\$2} END {print s}"); test "$rc3" -ge 8' && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/css/styles.css` contains zero references to `var(--radius-sm|md|lg|xl|full)`, `var(--font-display)`, `var(--font-body)`, `var(--transition-fast)`, `var(--transition-normal)`, `var(--nav-height)`, `var(--shadow-sm|md|lg|accent)`, `var(--size-unread-dot)`, `var(--space-wish-card-stack)`
    - `frontend/src/css/styles.css` contains ≥15 references to `var(--md-radius-*)` (radius tokens actively consumed)
    - `frontend/src/css/styles.css` contains ≥2 references to `var(--md-font-*)` (display + body)
    - `frontend/src/css/styles.css` contains ≥8 references to `var(--md-motion-*)` (duration + easing combined across transition consumers)
    - `frontend/src/css/styles.css` contains ≥1 reference to `var(--md-nav-height)` (consumed by .bottom-bar and similar)
    - The `.fab` rule at line 195 has `border-radius: var(--md-radius-md)` (16px per D-08) and a Chinese inline comment explaining the override
    - The `@import url('https://fonts.googleapis.com/css2?family=Noto+...')` import at line 7 is preserved unchanged
    - `cd frontend && npm run build` exits 0
  </acceptance_criteria>
  <done>
    `frontend/src/css/styles.css` is fully migrated to MD3 shape/spacing/elevation/typography/motion/nav-height tokens. Zero old names survive. FAB uses 16px per D-08. All motion consumers now reference `--md-motion-duration-*` and `--md-motion-easing-*` (separated per MD3 spec). Build succeeds. JSX inline styles still reference old names — that is Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Sweep shape, typography, nav-height tokens across 28 JSX files</name>
  <files>frontend/src/components/ChefSelectModal.jsx, frontend/src/components/ConfirmModal.jsx, frontend/src/components/CreateLinkModal.jsx, frontend/src/components/EmptyState.jsx, frontend/src/components/InvitationsModal.jsx, frontend/src/components/InvitationsSection.jsx, frontend/src/components/WishAdvanceModal.jsx, frontend/src/components/WishCard.jsx, frontend/src/components/WishRejectModal.jsx, frontend/src/pages/AdminCategoriesPage.jsx, frontend/src/pages/AdminChefsPage.jsx, frontend/src/pages/AdminDishesPage.jsx, frontend/src/pages/AdminHomePage.jsx, frontend/src/pages/AdminIngredientsPage.jsx, frontend/src/pages/AdminLogsPage.jsx, frontend/src/pages/AdminStatsPage.jsx, frontend/src/pages/AdminUsersPage.jsx, frontend/src/pages/ChefDishesPage.jsx, frontend/src/pages/ChefWishesPage.jsx, frontend/src/pages/DishDetailPage.jsx, frontend/src/pages/GuestOrderPage.jsx, frontend/src/pages/OrderDetailPage.jsx, frontend/src/pages/OrderPage.jsx, frontend/src/pages/PreferencesPage.jsx, frontend/src/pages/UserHomePage.jsx, frontend/src/pages/UserOrdersPage.jsx, frontend/src/pages/UserProfilePage.jsx, frontend/src/pages/UserWishesPage.jsx</files>
  <read_first>
    - frontend/src/css/styles.css (post-Task 1 — canonical reference for every MD3 shape/font/motion/nav-height token)
    - .planning/phases/08-md3-design-token-foundation/08-PATTERNS.md (mapping table at lines 540-578)
    - .planning/phases/08-md3-design-token-foundation/08-PATTERNS.md (per-file anchor table at lines 486-520)
    - frontend/src/pages/AdminDishesPage.jsx (representative sweep target — locate inline `borderRadius`, `fontFamily`, `fontSize` literals)
    - frontend/src/pages/AdminIngredientsPage.jsx (lines 272, 317, 334-458, 533-577 — numeric borderRadius consumers)
    - frontend/src/pages/UserOrdersPage.jsx (lines 114-153 — borderRadius/text tokens)
  </read_first>
  <action>
    For each of the 28 JSX files, sweep for old shape/typography/nav-height token references and replace with MD3 equivalents. The mapping is mechanical and idempotent. Per file:

    Open the file and use `rg -n` to locate every old reference. For each match, apply the replacement using the Edit tool. Preserve all surrounding JSX — do NOT modify onClick, onChange, onSubmit, useState, useEffect, useCallback, useRef, useMemo, useContext, useReducer, custom hook calls, API client calls, prop signatures, default exports, named exports, JSX className strings, or component composition. Only modify the value strings inside `style={{ ... }}` and string-template literals.

    Replacement patterns (read the file's exact text before editing):
    - `style={{ borderRadius: 'var(--radius-sm)' }}` → `style={{ borderRadius: 'var(--md-radius-xs)' }}`
    - `style={{ borderRadius: 'var(--radius-md)' }}` → `style={{ borderRadius: 'var(--md-radius-sm)' }}`
    - `style={{ borderRadius: 'var(--radius-lg)' }}` → `style={{ borderRadius: 'var(--md-radius-md)' }}`
    - `style={{ borderRadius: 'var(--radius-xl)' }}` → `style={{ borderRadius: 'var(--md-radius-lg)' }}`
    - `style={{ borderRadius: 'var(--radius-full)' }}` → `style={{ borderRadius: 'var(--md-radius-full)' }}`
    - `style={{ fontFamily: 'var(--font-display)' }}` → `style={{ fontFamily: 'var(--md-font-display)' }}`
    - `style={{ fontFamily: 'var(--font-body)' }}` → `style={{ fontFamily: 'var(--md-font-body)' }}`

    For FAB-related JSX: any inline style that overrides FAB `borderRadius` → `'var(--md-radius-md)'` (16px per D-08). Search for `fab` className consumers in JSX; typically FAB is rendered via className only, so no JSX change is needed — Task 1's styles.css change propagates.

    Leave the following alone (not in Wave 2 scope):
    - Numeric `borderRadius: <number>` literals (Wave 3 hardcoded radius sweep handles these)
    - Numeric `padding`, `margin`, `gap` literals (Phase 12 page-level sweep handles these)
    - String literal font names like `'Noto Sans SC'` (these reference the Google Fonts CDN import at styles.css line 7, not a CSS variable; MD3 font tokens only apply when `var(--font-*)` was the original reference)
    - `'50%'` borderRadius (intentional circles)
    - `borderRadius: 0` (intentional full-bleed rectangles like the InvitationsModal full-screen modal)

    After processing all 28 files, run the master verification grep to confirm zero old shape/typography/nav-height references remain in `frontend/src/components/` and `frontend/src/pages/`.

    Final build verification: `cd frontend && npm run build` exits 0; `cd frontend && npm run lint` ≤ pre-existing baseline + 5 errors.
  </action>
  <verify>
    <automated>cd frontend && bash -c 'set -e; ! rg -q "var\(--(radius-|font-(display|body)|nav-height|transition-|size-unread-dot|space-wish-card-stack)" src/components/ src/pages/; rad=$(rg -c "var\(--md-radius-" src/components/ src/pages/ | awk -F: "{s+=\$2} END {print s}"); test "$rad" -ge 10; fnt=$(rg -c "var\(--md-font-" src/components/ src/pages/ | awk -F: "{s+=\$2} END {print s}"); echo "font refs: $fnt"' && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - All 28 JSX files have zero references to old shape/typography/nav-height/motion tokens (`var(--radius-sm|md|lg|xl|full)`, `var(--font-display)`, `var(--font-body)`, `var(--nav-height)`, `var(--transition-*)`, `var(--size-unread-dot)`, `var(--space-wish-card-stack)`)
    - Across all 28 files, the combined count of `var(--md-radius-*)` references is ≥10
    - Across all 28 files, the combined count of `var(--md-font-*)` references is ≥2 (or zero, if no JSX file happens to reference font tokens directly — styles.css covers all font consumers)
    - No JSX business logic is modified — onClick/onChange/onSubmit/useState/useEffect/api.* calls unchanged
    - No component prop signature or default export name is modified
    - Numeric `borderRadius` literals (4, 999, 6, etc.) remain unchanged — Wave 3 sweep
    - `cd frontend && npm run build` exits 0
  </acceptance_criteria>
  <done>
    All 28 JSX files are migrated to MD3 shape/typography/nav-height tokens. JSX business logic unchanged. Build succeeds. Numeric borderRadius literals are preserved for Wave 3 hardcoded sweep.
  </done>
</task>

<task type="auto">
  <name>Task 3: Build verification + light/dark visual parity check + lint baseline comparison</name>
  <files>(verification only, no file changes)</files>
  <read_first>
    - frontend/src/css/styles.css (post-Task 1 — verify shape tokens are wired)
    - .planning/STATE.md (documented lint baseline of ≥90 errors as deferred technical debt)
    - .planning/phases/08-md3-design-token-foundation/08-CONTEXT.md D-08 (FAB 16px requirement)
    - .planning/phases/08-md3-design-token-foundation/08-CONTEXT.md D-04 (theme extension capability — `[data-theme="xxx"]` structure)
  </read_first>
  <action>
    No file changes in this task. Run end-to-end verification:

    1. Run `cd frontend && npm run build` and confirm exit code 0. If build fails, inspect Vite output for missing-token warnings or unresolved import errors; fix and rerun.

    2. Run `cd frontend && npm run lint` and capture the error count. Compare against the documented pre-existing baseline of ≥90 errors per STATE.md Deferred Items ("前端全量 lint 基线红（≥90 errors）"). Acceptable regression: pre-existing + 5 errors. If the lint count exceeds baseline + 5, the rename sweep likely introduced new ESLint complaints — investigate which rules fired (most likely unused-vars from inline style token references) and fix only critical new errors.

    3. Open `frontend/dist/index.html` (the build output) in a browser via `cd frontend && npm run preview` and confirm both modes render correctly:
       - Default mode (no `fc_theme` in localStorage): light theme via `prefers-color-scheme` if system is dark, otherwise light
       - Toggle dark mode via `ThemeToggle` button: `data-theme="dark"` attribute set on `<html>`; verify the surface tier system swaps (light surfaces become dark)
       - FAB renders as 16px corner (D-08): `getComputedStyle(document.querySelector('.fab')).borderRadius` should equal `16px` (or the rounded equivalent of `var(--md-radius-md)`)
       - All buttons/cards/labels render with Chinese font stack (PingFang SC, Noto Sans SC)
       - No `console.warn` about undefined CSS variables

    4. Spot-check that old token names produce no console errors. Open browser DevTools, navigate to a couple of pages (e.g., `/home`, `/chef/orders`, `/admin/dishes`), and verify the Network panel shows all CSS/JS bundles loaded with no 404s.

    5. Final dry-run of `npm run gen:tokens` to confirm the generator is still idempotent after Wave 2 work. `md5sum frontend/src/css/tokens.css` before and after a second run must be identical.

    Capture the verification output (build status, lint delta, dark mode confirmation) for inclusion in the Wave 2 SUMMARY. If anything fails, document the failure mode in the SUMMARY's "Issues" section and decide whether to fix in this plan or defer to a follow-up.
  </action>
  <verify>
    <automated>cd frontend && bash -c 'set -e; npm run build; lint_before=$(npm run lint 2>&1 | grep -oE "[0-9]+ errors?" | head -1 | awk "{print \$1}"); echo "lint errors: ${lint_before:-unknown}"; npm run gen:tokens && md5sum -c <(echo "$(cat src/css/tokens.css | md5sum)  src/css/tokens.css") || (npm run gen:tokens && md5sum src/css/tokens.css)'</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm run build` exits 0
    - `cd frontend && npm run lint` error count is ≤ pre-existing baseline + 5
    - `npm run gen:tokens` is idempotent — `md5sum frontend/src/css/tokens.css` identical before and after second invocation
    - `frontend/src/css/styles.css` contains no surviving old token references from the Wave 2 sweep target set
    - All 28 JSX files contain no surviving old token references from the Wave 2 sweep target set
    - Browser hard-reload of `npm run preview` shows both light and dark modes with no `console.warn` for undefined CSS variables
    - `getComputedStyle(.fab).borderRadius` equals `16px` (the MD3 standard per D-08)
  </acceptance_criteria>
  <done>
    Wave 2 verification complete: build green, lint within tolerance, generator idempotent, both modes render correctly, FAB shows MD3 standard 16px corner. The shape/spacing/elevation/typography/motion/nav-height foundation is fully applied; Phase 10/11 components can build on this layer without introducing new tokens.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Wave 2 is consumer-side rename work — no new dependencies, no new network boundaries, no auth surface touched. All work is in `frontend/src/` against tokens that were already verified in Wave 1.

| Boundary | Description |
|----------|-------------|
| styles.css consumers → tokens.css | CSS variable resolution at runtime; Vite bundles both files into a single stylesheet |
| JSX inline styles → tokens.css | CSS variable resolution in computed style attributes; same bundle as above |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-W2-01 | Tampering | `--transition-fast` rename accidentally drops a transition consumer | mitigate | Each replacement is a literal token substitution; CSS parser will report syntax errors via build verification (npm run build exits 0) |
| T-08-W2-02 | Spoofing | FAB rendered with non-16px corner | mitigate | Acceptance criteria verifies `getComputedStyle(.fab).borderRadius === '16px'`; if D-08 is violated, browser check fails |
| T-08-W2-03 | Repudiation | Rename silently leaves old name in a corner file | mitigate | Master verification grep covers all 28 JSX files + styles.css; zero survivors required |
| T-08-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages in Wave 2 — all work is against Wave 1's pinned @material/material-color-utilities@0.4.0 and material-symbols@0.45.9 |
</threat_model>

<verification>
1. `cd frontend && npm run build` exits 0
2. `cd frontend && npm run lint` ≤ pre-existing baseline + 5 errors
3. `frontend/src/css/styles.css` and all 28 JSX files have zero old shape/typography/nav-height/motion token references
4. `getComputedStyle(document.querySelector('.fab')).borderRadius` equals `16px`
5. `npm run gen:tokens` produces byte-identical output on rerun
6. Browser hard-reload shows both light and dark modes rendering correctly
</verification>

<success_criteria>
1. 5 level radius system (`--md-radius-xs` 8px / `--md-radius-sm` 12px / `--md-radius-md` 16px / `--md-radius-lg` 24px / `--md-radius-xl` 28px / `--md-radius-full` 9999px) is the only radius reference in `styles.css` and JSX
2. 5 level elevation tokens (`--md-elevation-0..5`) replace all shadow consumers (already wired in Wave 1, verified here)
3. 8 spacing tokens (`--md-spacing-1..8`) are available in `tokens.css` for any future page-level consumer (Phase 12 sweep will adopt them)
4. Chinese-English font stacks (`--md-font-display`, `--md-font-body`) are wired and visible in browser
5. FAB renders at 16px corner per D-08, overriding the original ROADMAP 28px lock
6. Motion duration/easing tokens (`--md-motion-duration-{short,medium,long}` + `--md-motion-easing-{standard,emphasized}`) are consumed in all `transition:` declarations
7. Navigation height (`--md-nav-height`) replaces `--nav-height` everywhere
8. Backend untouched (no changes to `backend/`, `alembic/`, or Python deps)
9. JSX business logic, props, event handlers, state hooks, API client calls, and component exports are byte-identical to pre-Wave-2 state
10. Build green, lint within tolerance, dark mode works, generator idempotent
</success_criteria>

<output>
Create `.planning/phases/08-md3-design-token-foundation/08-02-SUMMARY.md` when done.
</output>