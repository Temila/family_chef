---
phase: 15-navigation-restructure-test-data
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - frontend/tests/phase12-bugfix.spec.js
  - frontend/tests/md3-compliance.spec.js
autonomous: true
requirements:
  - NAV-03

must_haves:
  truths:
    - "phase12-bugfix.spec.js no longer asserts Sidebar footer has theme + logout buttons"
    - "phase12-bugfix.spec.js asserts Sidebar footer contains a version text node and zero buttons"
    - "phase12-bugfix.spec.js asserts Header contains a theme IconButton that toggles document.documentElement.dataset.theme"
    - "md3-compliance.spec.js no longer counts Sidebar footer buttons as touch targets"
    - "md3-compliance.spec.js asserts the new Sidebar footer version text matches /^v\d+\.\d+\.\d+$/"
    - "Both test files are syntactically valid Playwright specs"
  artifacts:
    - path: "frontend/tests/phase12-bugfix.spec.js"
      provides: "Replaces D-BUG-02 sidebar footer button assertions with NAV-03 version-text + zero-button assertions + new Header theme-toggle coverage"
      contains: "md-sidebar__version"
    - path: "frontend/tests/md3-compliance.spec.js"
      provides: "Replaces sidebar footer button count invariant with version text regex invariant"
      contains: "md-sidebar__version"
  key_links:
    - from: "phase12-bugfix.spec.js"
      to: "Sidebar footer"
      via: "locator('.md-sidebar__version') + count('.md-sidebar__footer button') === 0"
      pattern: "md-sidebar__version"
    - from: "md3-compliance.spec.js"
      to: "Sidebar footer"
      via: "textContent match against /^v\d+\.\d+\.\d+$/"
      pattern: "v\d+\.\d+\.\d+"
---

<objective>
Replace stale Sidebar-footer-button assertions in Phase 12 and MD3-compliance Playwright specs with NAV-03-aligned invariants (version text present, zero footer buttons, Header theme-toggle now lives in main row). This is Wave 0 — must complete BEFORE any feature code so when Wave 1 deletes the Sidebar footer buttons the migrated tests assert the new contract, not the old one.

Purpose: Sidebar and Header behavior change in NAV-03 (footer buttons removed) and D-NAV03-03 (theme toggle moves from Sidebar footer to Header main row). The existing Phase 12 / Phase 9 regression tests assert the removed controls. Without Wave 0 migration, the test suite would falsely fail when Wave 1 lands.

Output: 2 modified test files. No new code paths, no new dependencies.
</objective>

<execution_context>
@/home/temila/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/temila/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/15-navigation-restructure-test-data/15-CONTEXT.md
@.planning/phases/15-navigation-restructure-test-data/15-RESEARCH.md
@.planning/phases/15-navigation-restructure-test-data/15-PATTERNS.md
@.planning/phases/15-navigation-restructure-test-data/15-UI-SPEC.md

# Source files to read first
@frontend/tests/phase12-bugfix.spec.js
@frontend/tests/md3-compliance.spec.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate phase12-bugfix.spec.js Sidebar footer assertions</name>
  <files>frontend/tests/phase12-bugfix.spec.js</files>
  <read_first>
    - frontend/tests/phase12-bugfix.spec.js (current D-BUG-02 assertions at lines 88-110 — Sidebar footer button count + theme toggle + logout button tests)
    - frontend/src/components/composites/Sidebar.jsx (current footer markup — Sidebar.jsx:103-133 has 2 buttons: theme toggle + logout)
    - frontend/src/components/composites/Header.jsx (current Header structure — avatar menu still contains a 切换主题 menuitem at lines 116-130)
    - .planning/phases/15-navigation-restructure-test-data/15-PATTERNS.md "phase12-bugfix.spec.js (modify — replace Sidebar footer assertions)" section
  </read_first>
  <action>
    In frontend/tests/phase12-bugfix.spec.js:

    1. REPLACE the test block at lines ~88-110 that currently asserts Sidebar footer contains 2 buttons (theme + logout). The new contract per NAV-03 / D-NAV03-01 / D-NAV03-05: footer has 0 buttons, footer has a .md-sidebar__version text node, and theme toggle now lives in .md-header__theme-toggle inside Header.

       Replace with two tests:

       - test('Sidebar footer 仅显示版本号，无按钮', ...): call page.locator('.md-sidebar__footer').waitFor(); assert page.locator('.md-sidebar__version') is visible; assert page.locator('.md-sidebar__footer button') has count 0.

       - test('Header 主题按钮切换 data-theme（替代原 Sidebar footer 测试）', ...): call page.locator('.md-header').waitFor(); locate page.locator('.md-header__theme-toggle'); capture document.documentElement.dataset.theme via page.evaluate; click the toggle; recapture data-theme; expect before !== after.

    2. KEEP all other tests in phase12-bugfix.spec.js (D-BUG-01, D-BUG-02 th alignment, D-BUG-03, D-BUG-04 dropdown, D-BUG-05 footer pinning, etc.) unchanged.

    3. The replacement uses Playwright assertions that reference selectors that WILL exist after Wave 1 (15-02) lands. Running these tests against the CURRENT pre-Phase-15 code will fail because .md-header__theme-toggle does not exist yet and .md-sidebar__footer button count is 2. That is expected — this plan's success criterion is that the test file is CORRECT against the locked Phase 15 contract, not that the current code passes. Do not add bridging logic. Do not gate tests with environment flags.

    4. Do NOT delete imports or change the surrounding describe/test scaffolding.
  </action>
  <acceptance_criteria>
    - frontend/tests/phase12-bugfix.spec.js contains a test that asserts page.locator('.md-sidebar__footer button') count is 0 AND page.locator('.md-sidebar__version') is visible
    - Same file contains a test asserting .md-header__theme-toggle is present in the Header and toggles document.documentElement.dataset.theme on click (before/after values not equal)
    - No remaining test in phase12-bugfix.spec.js asserts that .md-sidebar__footer button count is positive
    - File still imports the same Playwright fixtures as before (no removed imports)
    - File passes `node --check frontend/tests/phase12-bugfix.spec.js` (syntax valid JS)
  </acceptance_criteria>
  <verify>
    <automated>node --check frontend/tests/phase12-bugfix.spec.js && grep -c 'md-sidebar__version' frontend/tests/phase12-bugfix.spec.js | awk '{ if ($1 >= 1) print "version-selector PRESENT"; else { print "version-selector MISSING"; exit 1 } }' && grep -c 'md-header__theme-toggle' frontend/tests/phase12-bugfix.spec.js | awk '{ if ($1 >= 1) print "header-toggle-selector PRESENT"; else { print "header-toggle-selector MISSING"; exit 1 } }'</automated>
  </verify>
  <done>
    - phase12-bugfix.spec.js contains NAV-03-aligned Sidebar footer test (version visible, 0 buttons) and Header theme-toggle test
    - File is syntactically valid (`node --check` passes)
    - All other Phase 12 tests unchanged
    - Existing imports preserved
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate md3-compliance.spec.js footer button invariant</name>
  <files>frontend/tests/md3-compliance.spec.js</files>
  <read_first>
    - frontend/tests/md3-compliance.spec.js (current footer button count assertion at lines 104-128)
    - frontend/src/components/composites/Sidebar.jsx (current footer — confirms 2 buttons to be removed)
    - .planning/phases/15-navigation-restructure-test-data/15-PATTERNS.md "frontend/tests/md3-compliance.spec.js (modify — footer invariant)" section
  </read_first>
  <action>
    In frontend/tests/md3-compliance.spec.js:

    1. REPLACE the assertion at lines ~104-128 that currently does `const footerButtons = await page.locator('.md-sidebar__footer button').count(); expect(footerButtons).toBeGreaterThanOrEqual(2)` (or equivalent footer-button-count invariant).

       Replace with version-text assertion:
       ```
       const versionText = await page.locator('.md-sidebar__version').textContent();
       expect(versionText).toMatch(/^v\d+\.\d+\.\d+$/);
       ```

    2. KEEP all other touch-target audit assertions in md3-compliance.spec.js (Header, BottomBar, Sheet, Card, etc.) unchanged. The footer is a non-interactive text node and is exempt from the 48dp touch-target rule per Phase 14 SC7; this exemption does not need a new test.

    3. Add a brief comment above the new assertion: `// NAV-03: Sidebar footer is now a version text node (non-interactive), exempt from 48dp touch target`.

    4. Do NOT delete imports or change the surrounding describe/test scaffolding.
  </action>
  <acceptance_criteria>
    - frontend/tests/md3-compliance.spec.js no longer contains any assertion that counts .md-sidebar__footer button as a touch target
    - Same file contains an assertion that page.locator('.md-sidebar__version') textContent matches /^v\d+\.\d+\.\d+$/
    - Comment `// NAV-03` (or Chinese equivalent) is present near the new assertion
    - File passes `node --check frontend/tests/md3-compliance.spec.js` (syntax valid JS)
    - All other touch-target audit assertions retained (Header, BottomBar, Sheet, etc.)
  </acceptance_criteria>
  <verify>
    <automated>node --check frontend/tests/md3-compliance.spec.js && grep -c 'md-sidebar__version' frontend/tests/md3-compliance.spec.js | awk '{ if ($1 >= 1) print "version-selector PRESENT"; else { print "version-selector MISSING"; exit 1 } }' && grep -c 'v\\d\+\\.\\d\+\\.\\d\+' frontend/tests/md3-compliance.spec.js | awk '{ if ($1 >= 1) print "version-regex PRESENT"; else { print "version-regex MISSING"; exit 1 } }'</automated>
  </verify>
  <done>
    - md3-compliance.spec.js Sidebar footer invariant asserts version text regex match instead of button count
    - File retains all other touch-target audit assertions (Header, BottomBar, Sheet, etc.)
    - Comment explaining NAV-03 change present
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- None new. This plan modifies only frontend Playwright test files. No new HTTP endpoints, no new data shape, no auth/session interaction.
- Tests run against an already-authenticated session (existing fixtures). No new authentication flow is introduced.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01-S | Spoofing | — | n/a | Test files assert UI selectors; no identity surface |
| T-15-02-T | Tampering | — | n/a | No DOM injection path; static selector strings |
| T-15-03-R | Repudiation | — | n/a | No audit-relevant events |
| T-15-04-I | Information Disclosure | — | n/a | No data flow changes |
| T-15-05-D | Denial of Service | — | n/a | No backend surface |
| T-15-06-E | Elevation of Privilege | — | n/a | No role-check bypass opportunity; tests inherit existing fixtures |
| T-15-SC | Supply Chain | npm/pip/cargo installs | n/a | Plan 01 introduces zero new dependencies |

Net: zero new threat surface. Test migration is a documentation/assertion update — no security-relevant behavior changes.
</threat_model>

<verification>
- frontend/tests/phase12-bugfix.spec.js references .md-sidebar__version (new) and .md-header__theme-toggle (new); no longer asserts .md-sidebar__footer button count is positive
- frontend/tests/md3-compliance.spec.js references .md-sidebar__version textContent regex; no longer asserts .md-sidebar__footer button count is positive
- Both test files pass `node --check` syntax validation
- Test files retain their existing fixtures and imports
</verification>

<success_criteria>
- SC for NAV-03 partial: tests assert the new Sidebar footer contract (version text + zero buttons) BEFORE the feature code lands
- Test files commit-ready
</success_criteria>

<output>
Create `.planning/phases/15-navigation-restructure-test-data/15-01-SUMMARY.md` when done
</output>