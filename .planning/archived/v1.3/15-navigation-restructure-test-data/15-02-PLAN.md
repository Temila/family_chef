---
phase: 15-navigation-restructure-test-data
plan: 02
type: execute
wave: 1
depends_on:
  - "15-01"
files_modified:
  - frontend/src/components/composites/Header.jsx
  - frontend/src/components/composites/Header.css
  - frontend/src/components/composites/Sidebar.jsx
  - frontend/src/components/composites/Sidebar.css
  - frontend/vite.config.js
autonomous: true
requirements:
  - NAV-01
  - NAV-02
  - NAV-03

must_haves:
  truths:
    - "Header main row shows only avatar + theme IconButton on the right; no other buttons"
    - "Header renders .header-action-bar div BELOW the main row when actions prop is provided"
    - "Header does NOT render any .header-action-bar div when actions prop is null/undefined"
    - "Avatar dropdown menu contains exactly 2 menuitems: 编辑资料 + 退出登录, separated by <Divider />"
    - "Avatar dropdown retains the display_name + role info section"
    - "Theme toggle is in Header main row (.md-header__theme-toggle) immediately before avatar; clicking it changes document.documentElement.dataset.theme"
    - "Sidebar footer contains only a .md-sidebar__version text node; no theme button, no logout button"
    - "Sidebar retains its logo, navItems, usePendingOrderCount, and Ripple wrapper (only footer changes)"
  artifacts:
    - path: "frontend/src/components/composites/Header.jsx"
      provides: "Header with action-bar wrapper, theme IconButton in main row, 2-item avatar menu with Divider"
      exports: ["default Header"]
      contains: "header-action-bar"
    - path: "frontend/src/components/composites/Header.css"
      provides: ".header-action-bar styling + .md-header__menu-item--danger color rule"
      contains: "header-action-bar"
    - path: "frontend/src/components/composites/Sidebar.jsx"
      provides: "Sidebar with version text in footer, no theme/logout buttons; removed currentTheme state and logout destructuring"
      exports: ["default Sidebar"]
      contains: "md-sidebar__version"
    - path: "frontend/src/components/composites/Sidebar.css"
      provides: ".md-sidebar__version styling (0.75rem / 400 / 1.3 / on-surface-variant)"
      contains: "md-sidebar__version"
  key_links:
    - from: "Header.jsx (main row)"
      to: "Header.css .header-action-bar"
      via: "JSX renders <div className=\"header-action-bar\">{actions}</div> below <header className=\"md-header\">"
      pattern: "header-action-bar"
    - from: "Header.jsx (theme IconButton)"
      to: "utils theme.toggleTheme()"
      via: "Header-local useState currentTheme set from theme.toggleTheme() return value"
      pattern: "theme.toggleTheme"
    - from: "Header.jsx (avatar menu)"
      to: "Divider composite"
      via: "import Divider from './Divider'; render between 编辑资料 menuitem and 退出登录 menuitem"
      pattern: "import Divider"
    - from: "Sidebar.jsx (footer)"
      to: "build-time VITE_APP_VERSION"
      via: "const APP_VERSION = import.meta.env.VITE_APP_VERSION || '0.0.0'"
      pattern: "VITE_APP_VERSION"
---

<objective>
Restructure Header and Sidebar composites to lock the Phase 15 navigation shell. This is the Wave 1 foundation that Wave 2's caller-wrap plans and Wave 4's tests depend on.

Specifically:
- Header: keep the `actions` prop but render the action node BELOW the main row inside `.header-action-bar` instead of inside `.md-header__right`; move the theme toggle into the Header main row immediately before the avatar; reduce the avatar menu to exactly 2 menuitems (编辑资料 + 退出登录) with a `<Divider />` between them; retain the display_name + role info section.
- Sidebar: remove the two footer buttons (theme + logout); replace with a version text node `.md-sidebar__version`; remove `currentTheme` state, `theme` import, `handleToggleTheme`, and `logout` destructuring from Sidebar.

Purpose: Wave 2 plans wrap 7 `<Header actions={...}>` callsites with `<div className="header-action-bar">`. Wave 3 NAV-04 (UserHomePage) and NAV-05 (BottomBar) plans depend on the Header not consuming `actions` in its main row. The new Sidebar footer is the only place the version text appears; if Sidebar still imports `logout` after this plan, the logout path would be unreachable from the desktop layout.

Output: 2 component files + 2 CSS files modified. No new components, no new dependencies, no new tokens.
</objective>

<execution_context>
@/home/temila/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/temila/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/15-navigation-restructure-test-data/15-CONTEXT.md
@.planning/phases/15-navigation-restructure-test-data/15-RESEARCH.md
@.planning/phases/15-navigation-restructure-test-data/15-PATTERNS.md
@.planning/phases/15-navigation-restructure-test-data/15-UI-SPEC.md

# Source files to read first
@frontend/src/components/composites/Header.jsx
@frontend/src/components/composites/Header.css
@frontend/src/components/composites/Sidebar.jsx
@frontend/src/components/composites/Sidebar.css
@frontend/src/components/composites/Divider.jsx
@frontend/src/utils/index.js
@frontend/src/components/Header.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure Header.jsx — action-bar wrapper, theme IconButton, 2-item avatar menu</name>
  <files>
    frontend/src/components/composites/Header.jsx
    frontend/src/components/composites/Header.css
  </files>
  <read_first>
    - frontend/src/components/composites/Header.jsx (full file — current main row at lines 70-149, avatar menu at lines 102-149 with 切换主题 menuitem at 116-130, logout menuitem at 131-142; current actions rendering at lines 88-90 inside .md-header__right)
    - frontend/src/components/composites/Header.css (full file — current .md-header__right, .md-header__menu, .md-header__menu-item rules; identify the spot after line 147 to insert .header-action-bar and .md-header__menu-item--danger rules)
    - frontend/src/components/composites/Divider.jsx (full file — default export, semantic hr[role="separator"])
    - frontend/src/utils/index.js (theme.toggleTheme() returns the new theme string; theme.getTheme() reads current value)
    - frontend/src/components/Header.jsx (re-export wrapper at lines 1-6; do not modify)
    - .planning/phases/15-navigation-restructure-test-data/15-PATTERNS.md "Header.jsx (composite, request-response)" section
  </read_first>
  <action>
    In frontend/src/components/composites/Header.jsx:

    1. ADD a new import at the top of the existing import block (after IconButton import):
       `import Divider from './Divider';`

    2. ADD Header-local theme state immediately after the existing useState/useRef declarations (mirror Sidebar.jsx:28-33 pattern):
       ```
       const [currentTheme, setCurrentTheme] = useState(() => theme.getTheme());
       const handleToggleTheme = () => {
         setCurrentTheme(theme.toggleTheme());
       };
       ```
       This is required because theme.toggleTheme() does NOT broadcast a DOM event — without Header-local state, the new IconButton will not re-render its icon after a click.

    3. DELETE the existing 切换主题 menuitem block at Header.jsx:116-130 (the entire `<button className="md-header__menu-item" onClick={() => { theme.toggleTheme(); setMenuOpen(false); }}>...</button>` element). Theme toggle moves to the main row per D-NAV03-03.

    4. ENSURE the 编辑资料 menuitem exists with:
       `onClick={() => { setMenuOpen(false); navigate('/profile'); }}`
       Label: `编辑资料` with Icon name="edit" size={18}.

    5. INSERT `<Divider />` between the 编辑资料 menuitem and the 退出登录 menuitem.

    6. EDIT the 退出登录 menuitem: change label from `退出` to `退出登录`; add `className="md-header__menu-item md-header__menu-item--danger"`; Icon name="logout" size={18}; onClick stays as `() => { logout(); navigate('/login'); }` (preserves existing logout behavior).

    7. KEEP the menu-info section (display_name + role) unchanged.

    8. RESTRUCTURE the main row: REMOVE the `{actions}` rendering at lines 88-90 from inside `.md-header__right`. INSERT a new `<IconButton icon={currentTheme === 'dark' ? 'light-mode' : 'dark-mode'} ariaLabel={currentTheme === 'dark' ? '切换浅色' : '切换深色'} onClick={handleToggleTheme} className="md-header__theme-toggle" />` immediately BEFORE the avatar button inside `.md-header__right`.

    9. CHANGE the root return to a fragment wrapping the main `<header>` AND a conditional `.header-action-bar` div below it:
       ```
       return (
         <>
           <header className="md-header">
             {/* left / center / right columns — actions removed from .md-header__right */}
             ...
           </header>
           {actions && (
             <div className="header-action-bar">
               {actions}
             </div>
           )}
         </>
       );
       ```
       When `actions` is null/undefined, NO `.header-action-bar` div is rendered.

    10. Do NOT modify the re-export at frontend/src/components/Header.jsx (it forwards Header.jsx as default).

    In frontend/src/components/composites/Header.css:

    1. INSERT a new rule block after the existing `.md-header__menu-item:focus-visible` selector (after line 147 area):
       ```
       /* D-NAV01-03: Header 下方 action-bar */
       .header-action-bar {
         display: flex;
         align-items: center;
         justify-content: flex-end;
         gap: var(--md-spacing-2);
         height: 56px;
         padding: 0 var(--md-spacing-4);
         background: var(--md-color-surface);
         border-top: 1px solid var(--md-color-outline-variant);
       }
       ```

    2. INSERT after that rule:
       ```
       /* D-NAV02-01: 退出登录 menuitem 用 error 颜色 */
       .md-header__menu-item--danger {
         color: var(--md-color-error);
       }
       .md-header__menu-item--danger:hover {
         background: color-mix(in srgb, var(--md-color-error-container), var(--md-color-on-surface) var(--md-state-layer-hover));
       }
       ```

    3. Do NOT modify any other Header.css rules. Do NOT add new tokens.
  </action>
  <acceptance_criteria>
    - frontend/src/components/composites/Header.jsx contains the string `header-action-bar` exactly once (in the JSX)
    - Same file contains `<Divider />` between the 编辑资料 menuitem and the 退出登录 menuitem
    - Same file contains NO `切换主题` string (deleted menuitem)
    - Same file contains the string `退出登录` (not just `退出`)
    - Same file contains `md-header__theme-toggle` className on the new IconButton
    - Same file contains `const [currentTheme, setCurrentTheme] = useState(...)` and `handleToggleTheme` defined
    - frontend/src/components/composites/Header.css contains the selector `.header-action-bar {` with `height: 56px`
    - Same CSS file contains `.md-header__menu-item--danger { color: var(--md-color-error);`
    - File passes `node --check frontend/src/components/composites/Header.jsx`
  </acceptance_criteria>
  <verify>
    <automated>node --check frontend/src/components/composites/Header.jsx && grep -c 'header-action-bar' frontend/src/components/composites/Header.jsx | awk '{ if ($1 == 1) print "action-bar PASS"; else { print "action-bar FAIL count=" $1; exit 1 } }' && grep -c 'md-header__theme-toggle' frontend/src/components/composites/Header.jsx | awk '{ if ($1 >= 1) print "theme-toggle PASS"; else { print "theme-toggle FAIL"; exit 1 } }' && grep -c '退出登录' frontend/src/components/composites/Header.jsx | awk '{ if ($1 >= 1) print "logout-label PASS"; else { print "logout-label FAIL"; exit 1 } }' && (grep -c '切换主题' frontend/src/components/composites/Header.jsx | awk '{ if ($1 == 0) print "old-theme REMOVED"; else { print "old-theme STILL PRESENT"; exit 1 } }') && grep -c '<Divider' frontend/src/components/composites/Header.jsx | awk '{ if ($1 >= 1) print "Divider-imported PASS"; else { print "Divider-imported FAIL"; exit 1 } }' && grep -c '.header-action-bar {' frontend/src/components/composites/Header.css | awk '{ if ($1 >= 1) print "css-rule PASS"; else { print "css-rule FAIL"; exit 1 } }'</automated>
  </verify>
  <done>
    - Header.jsx restructure complete (action-bar wrapper + theme IconButton in main row + 2-item avatar menu with Divider)
    - Header.css gains .header-action-bar and .md-header__menu-item--danger rules
    - File is syntactically valid (node --check passes)
    - All other Header.jsx/Header.css behavior preserved (logo, back button, title, click-outside handler)
  </done>
</task>

<task type="auto">
  <name>Task 2: Restructure Sidebar.jsx + Sidebar.css — remove footer buttons, add version text</name>
  <files>
    frontend/src/components/composites/Sidebar.jsx
    frontend/src/components/composites/Sidebar.css
  </files>
  <read_first>
    - frontend/src/components/composites/Sidebar.jsx (full file — current footer markup at lines 103-133 with 2 buttons: theme toggle + logout; useState theme state at lines 28-33; useAuth destructuring at line 27)
    - frontend/src/components/composites/Sidebar.css (full file — current footer block at lines 45-60 with grid 2-row layout; identify the spot to replace with .md-sidebar__version rule)
    - .planning/phases/15-navigation-restructure-test-data/15-PATTERNS.md "Sidebar.jsx (composite, request-response)" section
  </read_first>
  <action>
    In frontend/src/components/composites/Sidebar.jsx:

    1. DELETE the `import { useState } from 'react';` import (D-NAV03-04 — no more currentTheme state).
       If useState is used elsewhere in Sidebar.jsx (e.g. for menu open state), preserve the import and only delete the useState usage in Task 2.4 below.

    2. DELETE `import { theme } from '../../utils';` (no more theme import).
       If `theme` is used elsewhere, leave the import and only delete the usage in Task 2.4.

    3. CHANGE `const { user, logout } = useAuth();` to `const { user } = useAuth();` (D-NAV03-05 — logout no longer invoked from Sidebar).

    4. DELETE the theme state declarations:
       - `const [currentTheme, setCurrentTheme] = useState(theme.getTheme());`
       - `const handleToggleTheme = () => { setCurrentTheme(theme.toggleTheme()); };`

    5. REPLACE the entire footer block at Sidebar.jsx:103-133 with:
       ```
       <div className="md-sidebar__footer">
         <div className="md-sidebar__version" aria-label="应用版本">
           v{APP_VERSION}
         </div>
       </div>
       ```

    6. ADD at the top of the file (after imports, before any component logic):
       `const APP_VERSION = import.meta.env.VITE_APP_VERSION || '0.0.0';`
       (Per agent discretion in CONTEXT.md — choose ONE source: **Vite build-time injection** via `define: { 'import.meta.env.VITE_APP_VERSION': JSON.stringify(process.env.npm_package_version) }` in `vite.config.js` so the displayed version matches `frontend/package.json`. The fallback `'0.0.0'` is only for `import.meta.env` being undefined in non-Vite/test environments.)

    7. KEEP untouched: logo (lines 76-78), navItems arrays (lines 39-72), usePendingOrderCount() for chef badge (line 27), Ripple wrapper around nav items (lines 84-100), isActive / navigate logic.

    In frontend/vite.config.js (companion edit):

    1. ADD a `define` block to the Vite config so `import.meta.env.VITE_APP_VERSION` is sourced from `frontend/package.json` at build/dev-server time. Locate the existing config object (root level under `defineConfig({...})` or via the `vite.config.js` top-level export). Insert or extend the `define` field:
       ```js
       define: {
         ...(existing define keys if any),
         'import.meta.env.VITE_APP_VERSION': JSON.stringify(process.env.npm_package_version),
       },
       ```

    2. KEEP untouched: any existing `define` keys (e.g., for `process.env.NODE_ENV`); only ADD the VITE_APP_VERSION key. Do NOT remove or rename other fields in vite.config.js.

    3. The acceptance test for this Step 7 is: after `npm run build`, the Sidebar footer textContent equals `v<frontend/package.json["version"]>`, OR when running `npm run dev` and inspecting the rendered DOM, `document.querySelector('.md-sidebar__version').textContent` matches `/^v\d+\.\d+\.\d+$/` AND equal to `frontend/package.json["version"]` (e.g., `1.4.2` → renders `v1.4.2`).

    In frontend/src/components/composites/Sidebar.css:

    1. REPLACE the footer grid block at Sidebar.css:45-60 with:
       ```
       /* Footer: 单行版本号（Phase 15 NAV-03）
          原 grid 2 行布局删除（主题切换 + 退出按钮已迁出），
          改为居中 0.75rem / 400 / 1.3 文字节点。 */
       .md-sidebar__footer {
         display: flex;
         align-items: center;
         justify-content: center;
         border-top: 1px solid var(--md-color-outline-variant);
         flex-shrink: 0;
       }
       .md-sidebar__version {
         padding: var(--md-spacing-3) 0;
         font-size: 0.75rem;
         font-weight: 400;
         line-height: 1.3;
         color: var(--md-color-on-surface-variant);
         text-align: center;
       }
       ```

    2. KEEP all other Sidebar.css rules unchanged (logo, nav-item, active-pill, etc.).
  </action>
  <acceptance_criteria>
    - frontend/src/components/composites/Sidebar.jsx contains the string `md-sidebar__version` exactly once (in the JSX)
    - Same file contains `v{APP_VERSION}` in the footer
    - Same file does NOT contain any `<button>` element with className containing `md-sidebar__footer`
    - Same file does NOT contain `theme.toggleTheme` (theme import deleted)
    - Same file does NOT contain `const { user, logout } = useAuth()` (logout removed from destructure)
    - Same file contains `import.meta.env.VITE_APP_VERSION` for APP_VERSION source
    - frontend/vite.config.js contains `define` field with key `'import.meta.env.VITE_APP_VERSION': JSON.stringify(process.env.npm_package_version),`
    - After `npm run build` or `npm run dev`, the rendered `.md-sidebar__version` textContent matches `/^v\d+\.\d+\.\d+$/` AND equals `v` + `frontend/package.json["version"]` (proves Vite define directive binding works)
    - frontend/src/components/composites/Sidebar.css contains the selector `.md-sidebar__version {` with `font-size: 0.75rem`
    - File passes `node --check frontend/src/components/composites/Sidebar.jsx`
    - Sidebar retains `usePendingOrderCount` import + `pendingCount` variable (chef badge)
    - Sidebar retains `navItems` arrays (not deleted)
  </acceptance_criteria>
  <verify>
    <automated>node --check frontend/src/components/composites/Sidebar.jsx && grep -c 'md-sidebar__version' frontend/src/components/composites/Sidebar.jsx | awk '{ if ($1 >= 1) print "version-text PASS"; else { print "version-text FAIL"; exit 1 } }' && grep -c 'VITE_APP_VERSION' frontend/src/components/composites/Sidebar.jsx | awk '{ if ($1 >= 1) print "version-source PASS"; else { print "version-source FAIL"; exit 1 } }' && (grep -c 'theme.toggleTheme' frontend/src/components/composites/Sidebar.jsx | awk '{ if ($1 == 0) print "theme-call REMOVED"; else { print "theme-call STILL PRESENT"; exit 1 } }') && (grep -c 'const { user, logout }' frontend/src/components/composites/Sidebar.jsx | awk '{ if ($1 == 0) print "logout-destructure REMOVED"; else { print "logout-destructure STILL PRESENT"; exit 1 } }') && grep -c 'usePendingOrderCount' frontend/src/components/composites/Sidebar.jsx | awk '{ if ($1 >= 1) print "chef-badge KEPT"; else { print "chef-badge REMOVED (regression)"; exit 1 } }' && grep -c '.md-sidebar__version {' frontend/src/components/composites/Sidebar.css | awk '{ if ($1 >= 1) print "css-rule PASS"; else { print "css-rule FAIL"; exit 1 } }'</automated>
  </verify>
  <done>
    - Sidebar.jsx footer renders only `<div className="md-sidebar__version">v{APP_VERSION}</div>`
    - Sidebar.jsx has no theme state, no logout destructure, no theme.toggleTheme references
    - Sidebar.css `.md-sidebar__version` rule uses 0.75rem / 400 / 1.3 / on-surface-variant
    - APP_VERSION is sourced from import.meta.env.VITE_APP_VERSION with '0.0.0' fallback
    - All other Sidebar behavior preserved (logo, navItems, usePendingOrderCount, Ripple wrapper)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- This plan modifies React components that render user identity (avatar menu) and theme controls. No new HTTP endpoints, no new data shape.
- The avatar menu still calls `logout()` and `navigate('/login')` — same behavior as pre-Phase-15 (Header.jsx:131-142). The logout redirect surface is unchanged.
- The theme toggle still calls `theme.toggleTheme()` — same behavior as pre-Phase-15 (Sidebar.jsx:111). The only change is the icon button is now in the Header instead of the Sidebar footer.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02-S | Spoofing | Header avatar menu | accept | No identity surface change; same display_name/role info section preserved |
| T-15-02-T | Tampering | Header.jsx / Sidebar.jsx JSX | accept | Static JSX; no DOM injection path; no user input flows into the class name or theme value |
| T-15-02-R | Repudiation | Header.jsx logout() | accept | Logout already audited in Phase 14; same onClick handler retained |
| T-15-02-I | Information Disclosure | Header.jsx avatar menu | accept | User identity already exposed via display_name + role; no new data |
| T-15-02-D | Denial of Service | — | n/a | No backend surface |
| T-15-02-E | Elevation of Privilege | Sidebar navItems | accept | NavItems still role-gated; ProtectedRoute in App.jsx unchanged |
| T-15-02-SC | Supply Chain | npm/pip/cargo installs | n/a | Plan 02 introduces zero new dependencies |

Net: zero new threat surface. Header/Sidebar restructure is purely UI refactor; the only externally observable change is visual layout + 1 menu item moved between regions.
</threat_model>

<verification>
- frontend/src/components/composites/Header.jsx contains `header-action-bar`, `<Divider />`, `md-header__theme-toggle`, `退出登录`, NO `切换主题`
- frontend/src/components/composites/Header.css contains `.header-action-bar {` and `.md-header__menu-item--danger {`
- frontend/src/components/composites/Sidebar.jsx footer renders only version text; NO theme button, NO logout button, NO currentTheme state
- frontend/src/components/composites/Sidebar.css contains `.md-sidebar__version {` with `font-size: 0.75rem`
- All 4 files pass `node --check` (JSX) or basic CSS brace-balance
- Re-export at frontend/src/components/Header.jsx unchanged
</verification>

<success_criteria>
- SC for NAV-01: Header main row has theme + avatar only; actions render in `.header-action-bar` below; null actions = no bar div
- SC for NAV-02: Avatar menu has identity info + exactly 2 menuitems (编辑资料, 退出登录) with Divider between them
- SC for NAV-03: Sidebar footer has version text only; logo + navItems + usePendingOrderCount preserved
- File commit-ready
</success_criteria>

<output>
Create `.planning/phases/15-navigation-restructure-test-data/15-02-SUMMARY.md` when done
</output>