---
phase: 14-ui-bugfix-filter-popup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/composites/Modal.jsx
  - frontend/src/components/composites/Modal.css
  - frontend/src/components/composites/Sheet.jsx
  - frontend/src/components/composites/Sheet.css
autonomous: true
requirements:
  - UI-02
  - UI-03
  - BUG-07

must_haves:
  truths:
    - "Admin can open advanced filter as centered modal on desktop (≥1024px) and bottom sheet on mobile (<1024px)"
    - "Sheet inherits all Modal MD3 behavior — ESC close, backdrop click close, scroll lock, focus trap, focus return"
    - "Modal border visible in dark mode (--md-color-outline-variant) without altering light-mode visual"
    - "Bottom-sheet variant animates in from bottom on mobile and renders centered/static on desktop"
  artifacts:
    - path: "frontend/src/components/composites/Sheet.jsx"
      provides: "Bottom-sheet modal wrapper composite (delegates to Modal)"
      exports: ["Sheet"]
      contains: "import Modal from './Modal'"
    - path: "frontend/src/components/composites/Sheet.css"
      provides: "md-modal--bottom-sheet variant styling + md-sheet-in keyframes + desktop fallback"
      contains: "md-modal--bottom-sheet"
    - path: "frontend/src/components/composites/Modal.css"
      provides: "D-11 outline-variant border on .md-modal base + border:none override for sheet/full-screen"
      contains: "border: 1px solid var(--md-color-outline-variant)"
    - path: "frontend/src/components/composites/Modal.jsx"
      provides: "Variant prop accepts 'bottom-sheet' alongside 'basic'/'full-screen' (docblock)"
      contains: "bottom-sheet"
  key_links:
    - from: "Sheet.jsx"
      to: "Modal.jsx"
      via: "renders <Modal variant='bottom-sheet' {...props}>"
      pattern: "variant=\"bottom-sheet\""
    - from: "Sheet.css"
      to: "tokens.css"
      via: "var(--md-radius-lg), var(--md-motion-duration-medium), var(--md-motion-easing-emphasized)"
      pattern: "md-motion-easing-emphasized"
    - from: "Modal.css"
      to: "tokens.css"
      via: "var(--md-color-outline-variant) for D-11 dark-mode contrast border"
      pattern: "md-color-outline-variant"
---

<objective>
Add the Sheet composite (responsive centered-modal/bottom-sheet) and apply the D-11 dark-mode contrast border to Modal. These are the foundational primitives for Wave 3 filter migration (UI-02) and dark-mode contrast (BUG-07 / UI-03).

Purpose: Phase 14 admin pages need a responsive filter surface (Sheet) plus visible modal edges in dark mode (D-11 border). Wave 3 migration is gated on Sheet existing; Wave 3 user-facing dark-mode visual is gated on D-11.

Output: 2 new files (Sheet.jsx, Sheet.css) + 2 modified files (Modal.css adds border, Modal.jsx docblock update). No new token, no new dependency, no JSX consumer yet (Wave 3 wires consumers).
</objective>

<execution_context>
@/home/temila/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/temila/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-ui-bugfix-filter-popup/14-CONTEXT.md
@.planning/phases/14-ui-bugfix-filter-popup/14-PATTERNS.md

# Source files to read first (per deep_work_rules)
@frontend/src/components/composites/Modal.jsx
@frontend/src/components/composites/Modal.css
@frontend/src/css/tokens.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Sheet composite (Sheet.jsx + Sheet.css)</name>
  <files>
    frontend/src/components/composites/Sheet.jsx
    frontend/src/components/composites/Sheet.css
  </files>
  <read_first>
    - frontend/src/components/composites/Modal.jsx (Modal prop API — Sheet.jsx delegates to it; lines 31-47 props, lines 80-145 body)
    - frontend/src/components/composites/Modal.css (existing md-modal / md-modal--full-screen / md-modal-overlay rules)
    - frontend/src/css/tokens.css (--md-radius-lg=24px, --md-motion-duration-medium=250ms, --md-motion-easing-emphasized=cubic-bezier(0.2,0,0,1), --md-color-outline-variant dark=#414941)
    - .planning/phases/14-ui-bugfix-filter-popup/14-CONTEXT.md D-01..D-04 + D-11 (PC centered + mobile sheet, z-index hierarchy note)
    - .planning/phases/14-ui-bugfix-filter-popup/14-PATTERNS.md "Sheet.jsx (NEW composite)" + "Sheet.css (NEW CSS)" sections
  </read_first>
  <action>
    Create `frontend/src/components/composites/Sheet.jsx` as a thin pass-through wrapper. Per D-04 and PATTERNS.md Path A recommendation: Sheet wraps `<Modal variant="bottom-sheet" {...props} />` to inherit all MD3 modal behavior (focus trap, ESC, scroll lock, focus return) without duplicating logic. Drop the `variant` prop from Sheet's public API so callers cannot accidentally override the responsive behavior.

    Add a `@file`/module docblock (Chinese) following the Modal.jsx pattern: import `Modal from './Modal'`, import `./Sheet.css`. No `useState`/`useRef`/`useEffect` in Sheet — Modal handles all interaction state. Body destructures Modal's props (open, onClose, title, closeIcon, header, footer, actions, children, closeOnBackdrop, labelledBy, describedBy, initialFocusRef, className, style) and forwards them. The component's functional body is approximately 25-30 lines including docblock.

    Create `frontend/src/components/composites/Sheet.css` with the responsive bottom-sheet variant:

    - `.md-modal--bottom-sheet` overlay (mobile default): `padding: 0; align-items: flex-end; justify-content: center;`
    - `.md-modal--bottom-sheet .md-modal` (mobile default): `max-width: 100%; width: 100%; max-height: 90vh; height: auto; border-radius: var(--md-radius-lg) var(--md-radius-lg) 0 0; border-bottom: none;` with enter animation `animation: md-sheet-in var(--md-motion-duration-medium) var(--md-motion-easing-emphasized);`
    - `@media (min-width: 1024px)`: override `.md-modal--bottom-sheet` to `padding: var(--md-spacing-4); align-items: center;` and override `.md-modal--bottom-sheet .md-modal` to `max-width: 480px; border-radius: var(--md-radius-lg); border-bottom: 1px solid var(--md-color-outline-variant); animation: none;` (this border-bottom is added by Task 2 — both tasks must coexist; Sheet.css does not add it itself)
    - `@keyframes md-sheet-in { from { transform: translateY(100%); } to { transform: translateY(0); } }`
    - `@media (prefers-reduced-motion: reduce) { .md-modal--bottom-sheet .md-modal { animation: none; } }`

    Use only `var(--md-*)` tokens. Do NOT introduce new tokens. Do NOT add the imports block (Sheet.css does not need `@import './base.css'` — Modal.css already imports it and the sheet styles override Modal's rules).

    Do not add new variants, slots, or props beyond what D-04 specifies. Do not migrate any existing page to Sheet in this task (Wave 3 does that).
  </action>
  <behavior>
    - Sheet accepts the same prop surface as Modal (sans `variant`)
    - Sheet renders centered modal on desktop (≥1024px) and bottom-anchored sheet on mobile (<1024px)
    - Sheet inheriting Modal ESC/scroll-lock/focus-trap means ESC closes, backdrop click closes, scroll is locked, focus returns to trigger
  </behavior>
  <verify>
    <automated>node --check frontend/src/components/composites/Sheet.jsx && node -e "const fs = require('fs'); const css = fs.readFileSync('frontend/src/components/composites/Sheet.css', 'utf8'); if (!css.includes('md-modal--bottom-sheet')) throw new Error('missing bottom-sheet variant selector'); if (!css.includes('md-sheet-in')) throw new Error('missing keyframe'); if (!css.includes('@media (min-width: 1024px)')) throw new Error('missing desktop fallback'); if (!css.includes('prefers-reduced-motion')) throw new Error('missing reduced-motion fallback'); console.log('OK');"</automated>
  </verify>
  <done>
    - `Sheet.jsx` exists at `frontend/src/components/composites/Sheet.jsx`, imports `Modal from './Modal'`, default exports a function that forwards all Modal props with hardcoded `variant="bottom-sheet"`, no useState/useEffect/useRef
    - `Sheet.css` exists at `frontend/src/components/composites/Sheet.css`, contains all 5 selector blocks (mobile overlay + mobile modal + desktop override + keyframes + reduced-motion) per the variant spec
    - Desktop override includes `border-bottom: 1px solid var(--md-color-outline-variant);` to coexist with Modal.css D-11 task
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply D-11 dark-mode contrast border to Modal.css + extend Modal.jsx variant API</name>
  <files>
    frontend/src/components/composites/Modal.css
    frontend/src/components/composites/Modal.jsx
  </files>
  <read_first>
    - frontend/src/components/composites/Modal.css (full file — lines 27-39 base `.md-modal`, lines 41-47 full-screen override, lines 109-112 reduced-motion rule)
    - frontend/src/components/composites/Modal.jsx (lines 7-25 docblock — variant prop docstring; lines 86-91 overlayClasses construction)
    - frontend/src/css/tokens.css (--md-color-outline-variant light=#c1c9bf, dark=#414941 at lines 38 + 223)
    - .planning/phases/14-ui-bugfix-filter-popup/14-CONTEXT.md D-11 (theme border for dark-mode contrast, NOT primary; exclude full-screen and bottom-sheet)
  </read_first>
  <action>
    In `frontend/src/components/composites/Modal.css`:
    1. Add `border: 1px solid var(--md-color-outline-variant);` to the existing `.md-modal` rule (after `position: relative;` on line 38). This implements D-11 dark-mode edge contrast — the token value differs between light/dark mode so light mode is barely visible while dark mode gains visible edges against scrim.
    2. Add a new selector block immediately after the existing `.md-modal--full-screen .md-modal` override (after line 47) that resets the border for both full-bleed variants: `.md-modal--full-screen .md-modal, .md-modal--bottom-sheet .md-modal { border: none; }`. This satisfies D-11's exclusion clause ("full-bleed variants don't need edges").

    In `frontend/src/components/composites/Modal.jsx`:
    1. Extend the docblock to document that `variant` now accepts `'basic' | 'full-screen' | 'bottom-sheet'`. Update the line `variant        'basic' | 'full-screen'（caller 显式 opt-in）` to add `'bottom-sheet'` to the union. No runtime logic changes needed — the existing `md-modal--${variant}` class construction (line 89) handles `'bottom-sheet'` via CSS lookup automatically.
    2. Do NOT change any other code in Modal.jsx. The variant string comparison `variant === 'full-screen'` (line 86) correctly returns false for `'bottom-sheet'`, so the `style` prop still applies — correct behavior for the desktop centered-modal fallback.

    Use only existing tokens — do not introduce new CSS variables. Do NOT touch any of the other 14 lines of Modal.css (header, body, footer, close button). The border is purely additive at the base `.md-modal` selector; full-screen/sheet exclusions are additive overrides.
  </action>
  <behavior>
    - In dark mode, every Modal (basic variant) has a 1px outline-variant edge that contrasts against the scrim background
    - In light mode, the same border is barely visible (the token `#c1c9bf` blends with light surfaces)
    - Full-screen and bottom-sheet variants have NO border — they fill the viewport and don't need edge contrast
    - ESC, backdrop, scroll lock continue to work identically (no behavioral change)
  </behavior>
  <verify>
    <automated>grep -n 'border: 1px solid var(--md-color-outline-variant)' frontend/src/components/composites/Modal.css | grep -v '^#' | head -3 && grep -n 'md-modal--bottom-sheet .md-modal { border: none' frontend/src/components/composites/Modal.css | head -1 && grep -n 'bottom-sheet' frontend/src/components/composites/Modal.jsx | head -1 && (echo "Modal.css changes: PASS"; node -e "const fs=require('fs');const c=fs.readFileSync('frontend/src/components/composites/Modal.css','utf8');const baseHas=c.match(/\.md-modal\s*\{[^}]*border:\s*1px solid var\(--md-color-outline-variant\)/m);const exclHas=c.match(/\.md-modal--full-screen \.md-modal,\s*\.md-modal--bottom-sheet \.md-modal\s*\{\s*border:\s*none/m);if(!baseHas)throw new Error('base rule missing border');if(!exclHas)throw new Error('exclusion rule missing');console.log('all checks passed')")</automated>
  </verify>
  <done>
    - `Modal.css` line ~38 area: `.md-modal` rule contains `border: 1px solid var(--md-color-outline-variant);` as a new declaration
    - `Modal.css` line ~48 area: new selector `.md-modal--full-screen .md-modal, .md-modal--bottom-sheet .md-modal { border: none; }` exists after the full-screen override block
    - `Modal.jsx` line ~10: variant prop docblock lists `'basic' | 'full-screen' | 'bottom-sheet'`
    - No other Modal.css or Modal.jsx logic changed
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- None new. Phase 14 touches only frontend CSS and JSX components. No new HTTP endpoints, no new data shape, no auth/session interaction. Modal's existing ESC handler (Modal.jsx:71-78), scroll lock (Modal.jsx:52-68), and focus trap (`trapFocusWithin` from `../../utils`, Modal.jsx:100) are preserved verbatim via delegation — Sheet.jsx contains zero interaction logic.
- The only behavioral surface change is visual (sheet animation + D-11 border). No new injection vector: Sheet.css uses static CSS rules referenced from a CSS class string built by Modal.jsx (no user input flows into the class name).

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01-S | Spoofing | Sheet.jsx | accept | No identity surface in Sheet composite — it never holds user data |
| T-14-02-T | Tampering | Modal.css D-11 border | accept | Static CSS using existing tokens; no DOM injection path |
| T-14-03-R | Repudiation | — | n/a | No audit-relevant events |
| T-14-04-I | Information Disclosure | — | n/a | No data flow changes |
| T-14-05-D | Denial of Service | — | n/a | No backend surface |
| T-14-06-E | Elevation of Privilege | Sheet.jsx | accept | No role-check bypass opportunity; UI-only composite |
| T-14-SC | Supply Chain | npm/pip/cargo installs | n/a | Plan 01 introduces zero new dependencies (Sheet.jsx uses only existing React + Modal) |

Net: no new threat surface. Modal's existing escape/scroll-lock/focus-trap handlers (audited and shipped in Phase 11) are reused unchanged via component composition.
</threat_model>

<verification>
- `frontend/src/components/composites/Sheet.jsx` exists, does not contain `useState`/`useEffect`/`useRef` (delegates all state to Modal), imports Modal, default-exports a function component
- `frontend/src/components/composites/Sheet.css` contains the 5 selector blocks: mobile overlay, mobile modal, desktop override, keyframes, reduced-motion
- `frontend/src/components/composites/Modal.css` has D-11 border in `.md-modal` base, plus exclusion in `.md-modal--full-screen .md-modal, .md-modal--bottom-sheet .md-modal`
- `frontend/src/components/composites/Modal.jsx` docblock lists `bottom-sheet` as valid variant (text change only)
</verification>

<success_criteria>
- SC6 (BUG-07): dark-mode modal has themed outline-variant border; light-mode retains existing visual
- SC7 (UI-02): Sheet foundation exists (consumed by Wave 3 Plan 03 for filter migration; full visual verification at 360px + 1280px viewports happens after Wave 3)
</success_criteria>

<output>
Create `.planning/phases/14-ui-bugfix-filter-popup/14-01-SUMMARY.md` when done
</output>
