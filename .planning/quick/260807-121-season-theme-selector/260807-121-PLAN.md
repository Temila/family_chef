---
phase: quick-260807-121
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/theme/presets.js
  - frontend/src/theme/presets.test.mjs
  - frontend/src/theme/theme-context.jsx
  - frontend/src/theme/fouc-bootstrap.js
  - frontend/src/pages/ThemeSettingsPage.jsx
  - frontend/src/css/theme-settings.css
autonomous: true
requirements: [QUICK-260807-121]
must_haves:
  truths:
    - "季节自动切换开启时，主题设置页显示春/夏/秋/冬四个主题选择器"
    - "每个选择器可从所有预设 + 用户自定义主题中挑选"
    - "默认映射 = 当前行为（春→春预设，夏→夏预设，秋→秋预设，冬→冬预设）"
    - "更改当前季节的主题后立即生效（无需刷新）"
    - "选择会持久化，刷新或重启后仍然生效（含首帧 FOUC 引导）"
    - "开关关闭时不显示季节主题选择器区域"
  artifacts:
    - path: "frontend/src/theme/presets.js"
      provides: "buildDefaultSeasonThemeMap 纯函数，导出默认四季映射"
      contains: "export function buildDefaultSeasonThemeMap"
    - path: "frontend/src/theme/presets.test.mjs"
      provides: "buildDefaultSeasonThemeMap 单元测试"
    - path: "frontend/src/theme/theme-context.jsx"
      provides: "seasonThemeMap 状态 + setSeasonTheme setter + applyCurrentSeason 改读 map"
      contains: "SEASON_THEME_MAP_KEY"
    - path: "frontend/src/theme/fouc-bootstrap.js"
      provides: "首帧引导读取 fc_season_theme_map 应用所选主题"
      contains: "fc_season_theme_map"
    - path: "frontend/src/pages/ThemeSettingsPage.jsx"
      provides: "季节主题选择器 section（仅 seasonEnabled 时渲染）"
    - path: "frontend/src/css/theme-settings.css"
      provides: "季节主题选择器样式（纯 MD3 令牌）"
      contains: "theme-settings__season-map"
  key_links:
    - from: "frontend/src/pages/ThemeSettingsPage.jsx"
      to: "theme-context.jsx setSeasonTheme"
      via: "useTheme() 解构 setSeasonTheme + seasonThemeMap"
      pattern: "setSeasonTheme"
    - from: "theme-context.jsx applyCurrentSeason"
      to: "seasonThemeMap[season]"
      via: "直接读取映射中的完整主题对象"
      pattern: "seasonThemeMap\\[season\\]"
    - from: "fouc-bootstrap.js"
      to: "localStorage fc_season_theme_map"
      via: "JSON.parse 后按当前季节取主题"
      pattern: "fc_season_theme_map"
---

<objective>
为主题设置页增加"季节主题选择器"：当季节自动切换开关开启时，允许用户为春/夏/秋/冬各选一个主题（预设或自定义），默认沿用当前四季预设映射。

Purpose: 当前季节自动切换固定绑定四个内置预设；用户想用自定义主题或重新分配季节主题时无能为力。本计划让用户完全掌控每个季节的视觉基调，同时保持既有的互斥模型、缓存闸门语义与首帧 FOUC 引导无网络约束。

Output: presets.js 新增纯函数 + 测试；theme-context.jsx 新增 seasonThemeMap 状态与 setter 并改写 applyCurrentSeason/缓存闸门；fouc-bootstrap.js 读 map；ThemeSettingsPage.jsx 新增选择器 section；theme-settings.css 新增令牌化样式。
</objective>

<execution_context>
@/home/temila/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/temila/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@frontend/src/theme/presets.js
@frontend/src/theme/season.js
@frontend/src/theme/theme-context.jsx
@frontend/src/theme/fouc-bootstrap.js
@frontend/src/pages/ThemeSettingsPage.jsx
@frontend/src/css/theme-settings.css
@frontend/src/api/client.js
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: presets.js 新增 buildDefaultSeasonThemeMap 纯函数（含测试）</name>
  <files>frontend/src/theme/presets.js, frontend/src/theme/presets.test.mjs</files>
  <behavior>
    - buildDefaultSeasonThemeMap() 返回对象，键为 'spring'/'summer'/'autumn'/'winter'
    - 每个键的值 = PRESETS 中对应 id 的完整对象（spring→id:'spring' 的预设，依此类推）
    - 返回值结构与 PRESETS 元素完全一致：含 id/name/kind:'preset'/sourceColors{primary,secondary,tertiary}/variant
    - 返回的四个对象与 PRESETS 中的源对象引用相等（同一引用，非拷贝）以保证一致性
    - 返回对象是不可变快照：调用方修改不应影响 PRESETS（用 Object.freeze 包裹外层即可，元素保持引用）
    - 仅导入 PRESETS 与 season.js 的 SEASONS；不导入 React
  </behavior>
  <action>
先创建测试文件 RED，再实现 GREEN：

1. 新建 frontend/src/theme/presets.test.mjs，沿用 season.test.mjs 的 node:test + node:assert/strict 范式（参考 frontend/src/theme/season.test.mjs:11-13 的 import 风格）。测试组：
   - 'buildDefaultSeasonThemeMap 返回四季键'：断言键集合 = {spring,summer,autumn,winter}
   - 'buildDefaultSeasonThemeMap 每季映射到匹配的预设'：对每个 season，在 PRESETS 里 find id===season，断言结果与 map[season] 引用相等（assert.equal）
   - 'buildDefaultSeasonThemeMap 映射值含完整预设字段'：任取一季断言含 id/name/kind/sourceColors{primary,secondary,tertiary}/variant
   - 'buildDefaultSeasonThemeMap 结果被冻结'：assert.ok(Object.isFrozen(result))

2. 在 frontend/src/theme/presets.js 末尾（DEFAULT_PRESET 导出之后）新增：
   - 顶部 import 增加 `import { SEASONS } from './season.js';`（注意：season.js 仅 import SOLAR_TERMS，无循环依赖风险——season.js 不 import presets.js）
   - 导出 `buildDefaultSeasonThemeMap()`：遍历 SEASONS，对每个 season 在 PRESETS 中 find(p => p.id === season)，构建 { [season]: preset } 映射；用 Object.freeze 包裹返回对象。注释用中文说明"默认季节→主题映射，与开启季节自动切换的历史行为等价"。

注意：PRESETS 的四个季节预设 id 本就是 'spring'/'summer'/'autumn'/'winter'（见 presets.js:15-41），find 必命中；若未来预设缺失，该季返回 undefined——这是可接受的失败语义（上游 applyCurrentSeason 有兜底）。
  </action>
  <verify>
    <automated>cd frontend && node --test --test-reporter=spec src/theme/presets.test.mjs</automated>
  </verify>
  <done>presets.test.mjs 全部通过；buildDefaultSeasonThemeMap 导出且返回冻结的四季→预设映射对象。</done>
</task>

<task type="auto">
  <name>Task 2: theme-context 与 fouc-bootstrap 接入 fc_season_theme_map 状态机</name>
  <files>frontend/src/theme/theme-context.jsx, frontend/src/theme/fouc-bootstrap.js</files>
  <action>
**theme-context.jsx 改造（核心）：**

1. 新增常量（紧邻现有 ACTIVE_THEME_KEY 等定义，约 theme-context.jsx:42-45 处）：
   `const SEASON_THEME_MAP_KEY = 'fc_season_theme_map';`
   并在顶部 import presets：`import { DEFAULT_PRESET, PRESETS, buildDefaultSeasonThemeMap } from './presets.js';`（替换现有 DEFAULT_PRESET/PRESETS import 行）。

2. 新增模块级读写校验辅助函数（放在 writeLastSeasonToStorage 之后，shouldApplySeasonalPreset 之前，沿用现有 try/catch 静默失败范式）：
   - `readSeasonThemeMapFromStorage()`：try 读取 localStorage.getItem(SEASON_THEME_MAP_KEY)；若为空返回 buildDefaultSeasonThemeMap()；JSON.parse 后校验是对象；对 SEASONS（用现有 VALID_SEASONS）中每个季节，若 parsed[season] 存在且 isValidSourceColors(parsed[season].sourceColors) 为真则采用，否则用默认 map 的对应项；返回合并后的完整四季映射。catch 返回 buildDefaultSeasonThemeMap()。
   - `writeSeasonThemeMapToStorage(map)`：try localStorage.setItem(SEASON_THEME_MAP_KEY, JSON.stringify(map))，catch 静默。

3. 改写 shouldApplySeasonalPreset 缓存闸门签名，增加 seasonThemeMap 参数：
   将 `const activeMatchesSeasonal = Boolean(activeTheme && activeTheme.id === season);`
   改为 `const mappedId = seasonThemeMap?.[season]?.id;`
   `const activeMatchesSeasonal = Boolean(activeTheme && mappedId != null && activeTheme.id === mappedId);`
   函数签名变为 `shouldApplySeasonalPreset(season, hemisphere, cachedKey, activeTheme, seasonThemeMap)`。

4. ThemeProvider 内新增状态（紧邻现有 hemisphere state，约 theme-context.jsx:194 之后）：
   `const [seasonThemeMap, setSeasonThemeMapState] = useState(readSeasonThemeMapFromStorage);`

5. 新增公开 setter（放在 setHemisphere 之后，约 theme-context.jsx:288 处）：
   `const setSeasonTheme = useCallback((season, theme) => { ... }, [])`
   内部：校验 season 在 VALID_SEASONS 中且 theme 与 isValidSourceColors(theme.sourceColors) 为真；setSeasonThemeMapState(prev => { const next = { ...prev, [season]: theme }; writeSeasonThemeMapToStorage(next); return next; }); 然后设 justEnabledRef.current = true（镜像 setSeasonEnabled 的即时生效范式 theme-context.jsx:274-277），强制下一次 applyCurrentSeason 重应用。
   无效入参直接 return 不动状态。

6. 改写 applyCurrentSeason（theme-context.jsx:302-323）：删除 getSeasonPresetId + PRESETS.find 两步；改为：
   `const themed = seasonThemeMap[season];`
   `if (!themed || !isValidSourceColors(themed.sourceColors)) return false;`
   （保留 season null 早退与 force/justEnabledRef/shouldApply 闸门逻辑不变，仅把 shouldApplySeasonalPreset 调用补上第 5 个参数 seasonThemeMap）
   `applySeasonalPresetDirect(themed);` 替换原 `applySeasonalPresetDirect(preset);`
   useCallback 依赖数组增加 seasonThemeMap。

7. 暴露到 context value（theme-context.jsx:337-362）：在 value 对象加 `seasonThemeMap, setSeasonTheme,`；useMemo 依赖数组同步加这两项。

**fouc-bootstrap.js 改造（首帧无网络）：**

在现有 `if (seasonEnabled)` 块（fouc-bootstrap.js:35-45）内，presetId 命中后、`var match` 查找之前，插入 map 读取逻辑：
- `var seasonMapRaw = localStorage.getItem('fc_season_theme_map');`
- `var seasonMap = null; try { seasonMap = seasonMapRaw ? JSON.parse(seasonMapRaw) : null; } catch (e) { seasonMap = null; }`
- 若 `seasonMap && seasonMap[season] && seasonMap[season].sourceColors && seasonMap[season].sourceColors.primary`，则 `seasonPreset = seasonMap[season];`（直接用完整主题对象）。
- 否则继续走现有 PRESETS find 逻辑作为兜底（保留对历史/损坏 map 的健壮性）。

注意：fouc-bootstrap.js 不能 import buildDefaultSeasonThemeMap（会引入 SEASONS→solar-terms 链路膨胀首帧 bundle），仅用 map[season] 字段校验，缺失即回退 PRESETS。
  </action>
  <verify>
    <automated>cd frontend && node --test --test-reporter=spec src/theme/presets.test.mjs src/theme/season.test.mjs src/theme/theme-engine.test.mjs && npm run lint -- src/theme/theme-context.jsx src/theme/fouc-bootstrap.js 2>&1 | tail -20 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>theme-context 暴露 seasonThemeMap + setSeasonTheme；applyCurrentSeason 从 map 读主题而非 PRESETS.find；缓存闸门对比 map[season].id；fouc-bootstrap 首帧读 fc_season_theme_map 优先于 PRESETS；全部既有测试通过；lint 无新错；build 成功。</done>
</task>

<task type="auto">
  <name>Task 3: ThemeSettingsPage 季节主题选择器 UI + 令牌化样式</name>
  <files>frontend/src/pages/ThemeSettingsPage.jsx, frontend/src/css/theme-settings.css</files>
  <action>
**ThemeSettingsPage.jsx 改造：**

1. 从 useTheme() 解构增加 seasonThemeMap、setSeasonTheme、customThemes（替换现有解构 theme-context.jsx:33-38）。
   现有解构为 `{ seasonEnabled, hemisphere, setSeasonEnabled, setHemisphere }`，新增 `seasonThemeMap, setSeasonTheme, customThemes,`。

2. 在半球 section（ThemeSettingsPage.jsx:111-170）之后、底部 actions div（:172）之前，新增一个 section，仅当 `seasonEnabled` 为真时渲染：

   section 结构：
   - section className="theme-settings__section"
   - section-title 文本"季节主题"
   - 对 SEASONS（['spring','summer','autumn','winter'] — 直接内联数组常量，与 season.js SEASONS 同源）映射 4 行；每行一个 div className="theme-settings__season-row"，内含：
     - label className="theme-settings__season-label"，文本为中文季节名（春/夏/秋/冬，用 const SEASON_LABELS = { spring:'春', summer:'夏', autumn:'秋', winter:'冬' } 内联映射）htmlFor={季节+ '-season-select'}
     - 一个原生 `<select>`（移动端友好、无障碍），className="theme-settings__season-select"，id 同 htmlFor，value={seasonThemeMap[season]?.id ?? ''}，onChange 调用 handleChange(season, e)
     - onChange handler：从 `[...PRESETS, ...customThemes]` 中 find id === e.target.value 的主题对象；命中则 setSeasonTheme(season, matched) 并 showToast(`已更新${SEASON_LABELS[season]}主题：${matched.name}`, 'success')；未命中静默 return。
   - select 的 options：遍历 `[...PRESETS, ...customThemes]` 生成 `<option key={t.id} value={t.id}>{t.name}</option>`（预设名是中文如"春"/"默认"，自定义主题用后端返回的 name）。
   - section-hint 文本"为每个季节选择主题；更改当前季节的主题会立即生效。"
   - 需从 useTheme 同时取 PRESETS（已在 context value 中暴露，见 theme-context.jsx:344）。

3. 更新互斥提醒横幅文案（ThemeSettingsPage.jsx:79-81）以反映新能力：将"开启后仅使用四季主题，手动应用失效"改为"开启后按所选季节主题自动切换，手动应用失效"。仅改文案字符串，DOM 结构不变。

**theme-settings.css 改造：**

在现有 hemisphere 样式块（theme-settings.css:188-228）之后、actions 样式（:230）之前新增以下类，严格使用 var(--md-*) 令牌，禁止 hex：
- `.theme-settings__season-row`：display:flex; align-items:center; justify-content:space-between; gap:var(--md-spacing-3); min-height:48px;
- `.theme-settings__season-label`：font-family:var(--md-font-body); font-size:0.95rem; font-weight:500; color:var(--md-color-on-surface); flex:1 1 auto;
- `.theme-settings__season-select`：min-width:140px; min-height:48px; padding:var(--md-spacing-2) var(--md-spacing-3); border-radius:var(--md-radius-md); border:1px solid var(--md-color-outline-variant); background:var(--md-color-surface); color:var(--md-color-on-surface); font-family:var(--md-font-body); font-size:0.95rem; cursor:pointer;（focus-visible 加 outline:var(--md-focus-ring-outer); outline-offset:2px;）

在 420px 媒体查询（theme-settings.css:240-248）内补一条：`.theme-settings__season-row { flex-direction:column; align-items:stretch; }` 让窄屏选择器占满宽度。

注意：保留现有 `.theme-settings__section` 容器样式复用，新 section 自动继承卡片背景与间距。
  </action>
  <verify>
    <automated>cd frontend && npm run lint:css && npm run check:md3 2>&1 | tail -15 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>季节开关开启时页面渲染四个季节主题选择器（春/夏/秋/冬），每项可选预设+自定义主题；更改当前季节主题后 UI 与视觉立即更新并持久化；开关关闭时该 section 不渲染；CSS 通过 stylelint + MD3 令牌检查；build 成功。</done>
</task>

</tasks>

<verification>
- `node --test` 全部既有 + 新增 presets 测试通过
- `npm run lint:css` 无新错（季节选择器样式无 hex literal）
- `npm run check:md3` 通过（令牌合规）
- `npm run build` 成功（无 import/语法错误）
- 手动：浏览器开启季节开关 → 出现四个选择器 → 改当前季节主题立即生效 → 刷新后保持 → 首帧无闪烁（FOUC bootstrap 读 map）
</verification>

<success_criteria>
- 季节自动切换开启时，主题设置页显示四季节主题选择器，默认值为当前四季预设
- 选择器选项含所有预设 + 当前用户自定义主题
- 修改当前季节主题后即时生效（justEnabledRef 强制重应用路径）
- 映射持久化至 fc_season_theme_map，刷新与首帧 FOUC 引导均读 map
- 互斥模型与缓存闸门语义未被破坏（setActiveTheme 在 seasonEnabled 时仍返回 false）
- 全部测试通过，CSS 令牌合规，build 成功
</success_criteria>

<output>
Create `.planning/quick/260807-121-season-theme-selector/260807-121-SUMMARY.md` when done
</output>
