---
quick_id: "260727-i2g"
slug: sync-dev-with-main-split-wishlist-commit
description: >
  Sync dev with main, split wishlist commits (89) to feature/wishlist with
  squash-merge PR + GitHub release v1.1, split UI rebuild commits (39) to
  feature/UI-rebuild with squash-merge PR + GitHub release v1.2, delete
  feature/guest_order.
date: 2026-07-27
mode: quick
---

# Quick Task 260727-i2g: Branch Restructure + Releases

## Context

- `origin/main` = `fd0ec25` (latest, 7 bugfix PRs ahead of dev)
- `origin/dev` = `aa7a79c` (stale, behind main by 7 commits)
- `feature/guest_order` (local) = `98ab338` — contains ALL work:
  - **89 wishlist commits** (`a572e04`→`d454aec`, v1.1 milestone)
  - **39 UI rebuild commits** (`4221dd3`→`98ab338`, v1.2 MD3 refactor)
- `v1.1` tag exists on `d454aec`; no GitHub releases exist.

## Tasks

### Task 1: Sync dev with main
- **action:** Reset local dev to origin/main, force push.
- **verify:** `git log --oneline origin/main..origin/dev` is empty.
- **done:** dev matches main on remote.

### Task 2: Create feature/wishlist + PR + release v1.1
- **action:**
  1. `git checkout -b feature/wishlist d454aec`
  2. `git merge origin/main` (resolve conflicts)
  3. Remove `.planning/` from branch (keep main clean)
  4. Push branch
  5. `gh pr create --base main --head feature/wishlist`
  6. `gh pr merge --squash --delete-branch`
  7. Move v1.1 tag to squash commit, `gh release create v1.1`
- **verify:** main has wishlist code, no .planning; v1.1 release exists on GitHub.
- **done:** PR merged, release published.

### Task 3: Create feature/UI-rebuild + PR + release v1.2
- **action:**
  1. `git rebase --onto origin/main d454aec 98ab338` (replay 39 UI commits)
  2. `git checkout -b feature/UI-rebuild`
  3. Remove `.planning/`
  4. Push branch
  5. `gh pr create --base main --head feature/UI-rebuild`
  6. `gh pr merge --squash --delete-branch`
  7. `git tag v1.2`, `gh release create v1.2`
- **verify:** main has UI rebuild code; v1.2 release exists.
- **done:** PR merged, release published.

### Task 4: Cleanup
- **action:** Delete feature/guest_order (local + remote).
- **verify:** Branch gone.
- **done:** Repo has clean branch structure.

## must_haves
- truths:
  - origin/dev == origin/main after sync
  - feature/wishlist squash-merged to main with only wishlist code
  - feature/UI-rebuild squash-merged to main with only UI rebuild code
  - v1.1 and v1.2 GitHub releases exist
  - feature/guest_order deleted
- artifacts:
  - GitHub PRs (wishlist + UI-rebuild)
  - GitHub releases v1.1 + v1.2
- key_links: []
