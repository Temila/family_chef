---
phase: quick
quick_id: 260730-m6k
slug: wrap-left-filters
date: 2026-07-30
status: complete
description: 撤销260730-m1p的非对称padding，将高级筛选+已关联+未关联包装到同一div（gap=spacing-2）
---

# Quick Task 260730-m6k: 包装左侧筛选按钮到同一 div

## 目标
撤销 260730-m1p（左 flush padding），恢复对称 48px padding；
把高级筛选 + 已关联 + 未关联 三个按钮包装到同一 div，gap=var(--md-spacing-2)。

## 任务
1. CSS: `.filter-action-row` padding 回退到 `0 48px var(--md-spacing-2)`
2. CSS: 新增 `.filter-action-row__filters { display:flex; gap:var(--md-spacing-2); }`
3. JSX: 把三个左按钮包到 `<div className="filter-action-row__filters">`
