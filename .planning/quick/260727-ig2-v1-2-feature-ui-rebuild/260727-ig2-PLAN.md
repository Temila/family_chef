---
quick_id: "260727-ig2"
slug: v1-2-feature-ui-rebuild
description: "撤销v1.2的发布，重建feature/ui-rebuild分支"
date: 2026-07-27
mode: quick
---

# Quick Task 260727-ig2 — Plan

## Goal
撤销 v1.2（Material Design 3 UI 重构）的发布，将 main/dev 回退到 v1.1，删除 v1.2 的 tag 与 GitHub Release，并把 UI 重构成果保留在重建的 `feature/ui-rebuild` 分支上（指向 squash 提交 `7419be0`）。

## Locked Decisions (from CONTEXT.md)
- 撤销方式：`reset --hard v1.1` + force-push（不使用 revert）
- 分支内容：feature/ui-rebuild → `7419be0`（squash 提交）
- 发布范围：删除 GitHub Release + tag（本地+远程）；PR #23 无法 unmerge，保留

## Pre-conditions
- [x] main = dev = origin/main = origin/dev = `7419be0`
- [x] v1.1 = `14ed5fd`, v1.2 = `7419be0`
- [x] gh 已认证 (Temila)
- [x] 无本地 feature/* 分支

## Tasks

### Task 1: 回退 main/dev 到 v1.1 并强制推送
**action:**
```bash
git switch main
git reset --hard v1.1          # main → 14ed5fd
git push --force-with-lease origin main

git switch dev
git reset --hard v1.1          # dev → 14ed5fd
git push --force-with-lease origin dev
```
**verify:**
- `git rev-parse main` == `14ed5fd...`
- `git rev-parse dev` == `14ed5fd...`
- `git rev-parse origin/main` == `14ed5fd...`
- `git rev-parse origin/dev` == `14ed5fd...`
**done:** main、dev、origin/main、origin/dev 均指向 v1.1 (14ed5fd)

### Task 2: 删除 v1.2 tag 与 GitHub Release
**action:**
```bash
gh release delete v1.2 --yes               # 删除 GitHub Release
git tag -d v1.2                             # 删除本地 tag
git push --delete origin v1.2              # 删除远程 tag
```
**verify:**
- `git tag -l v1.2` 输出为空
- `git ls-remote --tags origin v1.2` 输出为空
- `gh release view v1.2` 返回 not-found 错误
**done:** v1.2 tag（本地+远程）与 GitHub Release 均已删除

### Task 3: 重建 feature/ui-rebuild 分支指向 7419be0
**action:**
```bash
git branch feature/ui-rebuild 7419be0
git push -u origin feature/ui-rebuild
```
**verify:**
- `git rev-parse feature/ui-rebuild` == `7419be0...`
- `git rev-parse origin/feature/ui-rebuild` == `7419be0...`
**done:** feature/ui-rebuild 本地+远程均指向 7419be0，UI 重构成果得以保留

### Task 4: 最终状态校验
**action:** 汇总检查所有 ref 与 GitHub 资源状态
**verify:**
- main/dev = v1.1
- feature/ui-rebuild = 7419be0
- v1.2 tag/release 已删除
- v1.0, v1.1 tag 仍在
**done:** 全部符合预期

## must_haves
- truths:
  - main 与 dev（含远程）回退到 v1.1 (14ed5fd)
  - v1.2 的本地 tag、远程 tag、GitHub Release 全部删除
  - feature/ui-rebuild（含远程）指向 7419be0
  - v1.0、v1.1 tag 与 release 不受影响
- artifacts:
  - feature/ui-rebuild 分支（本地+远程）
- key_links:
  - `.planning/quick/260727-i2g-sync-dev-with-main-split-wishlist-commit/260727-i2g-SUMMARY.md`（上一个任务，本任务是其逆向）

## Notes
- 本任务无代码改动，纯属 git/GitHub 操作；.planning/ 在本仓库刻意不被跟踪，故最终无 commit 产物（操作本身即工作成果）
- 使用 `--force-with-lease` 而非 `--force`，更安全（若远程被他人推动会拒绝）
- PR #23 已 merged，GitHub 不支持 unmerge，只能通过回退 main 来"撤销"其内容影响
