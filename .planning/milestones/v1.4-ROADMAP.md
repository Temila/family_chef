# v1.4 Tech Debt Cleanup — Roadmap Archive

**Milestone:** v1.4
**Shipped:** 2026-07-30
**Phase Count:** 1
**Plan Count:** 4

## Phase 16: Tech Debt Cleanup

**Goal:** Fix all 10 accumulated tech debt items (TD-01 ~ TD-10)

**Requirements:** TD-01 ~ TD-10
**Success Criteria:**

  1. `::before` 字面值 override 已确认关闭（operator 确认 th/tbody 不再需要 ::before 伪元素）
  2. CORS 收窄 + fallback 加固（无 `["*"]` 残留）
  3. `app.url` 在 config.yaml / config.example.yaml 显式配置
  4. `alembic env.py` 含 `render_as_batch=True`，migration 8a258d50ee87 用 batch_alter_table
  5. 启动自动 alembic upgrade head（env-gated by AUTO_MIGRATE + PYTEST_CURRENT_TEST）
  6. Sidebar 版本号读后端 `/api/version` 而非 Vite define
  7. `WishDeepLinkRedirect` 补 `encodeURIComponent(id)`
  8. ChefWishesPage actingId / advanceTarget / rejectTarget 在 finally 中重置
  9. 后端测试套件 108 fail → 0
  10. 前端 lint 101 errors → 0

**Plans:** 4 plans (Wave 1: 16-01 → 16-02; Wave 2: 16-03 + 16-04)

## Tech Debt Closed

- TD-01: `::before` override signed off
- TD-02: Version from config.yaml via `/api/version`
- TD-03: CORS fallback hardened (no `["*"]`)
- TD-04: `app.url` in both YAML configs
- TD-05: Migration batch fix verified
- TD-06: Auto-migrate on startup (env-gated)
- TD-07: Deep-link URL encoding
- TD-08: actingId stale state fix
- TD-09: Backend test suite: 108 → 0 failures
- TD-10: Frontend lint: 101 → 0 errors

## Bonus Work

- ChefDishesPage layout aligned with AdminDishesPage (filter-action-row + Sheet)
- Fixed duplicate `loadIngredients` declaration in AdminIngredientsPage.jsx

## UAT Results (16-UAT.md)

- 10/10 tests run
- 9 passed
- 1 issue (version bump 0.1.0 → 1.3.0) — fixed in this milestone

## Stats

- Commits: 7+ atomic commits
- Files changed: 30+ (frontend + backend + config + migrations)
- Lines: 400+ removed, 500+ added
- Backend tests: 350 passed, 0 failed (was 108 failed)
- Frontend lint: 0 errors, 0 warnings (was 101 errors)
