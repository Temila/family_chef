# v1.4 Tech Debt Cleanup — Requirements Archive

**Milestone:** v1.4 Tech Debt Cleanup
**Shipped:** 2026-07-30

## v1.4 Requirements — Tech Debt Cleanup

| ID | Description | Phase | Final Status |
|----|-------------|-------|--------------|
| TD-01 | `::before` 字面值 override 确认关闭 | Phase 16 | ✓ Complete (operator signed off, no ::before needed) |
| TD-02 | 版本号读 config.yaml 而非 package.json | Phase 16 | ✓ Complete (`/api/version` endpoint + Sidebar runtime fetch) |
| TD-03 | CORS `allow_origins` 确认已收窄 | Phase 16 | ✓ Complete (config.yaml explicit + fallback hardened) |
| TD-04 | `config.yaml` 添加 `app.url` 显式配置 | Phase 16 | ✓ Complete (added to both YAML files) |
| TD-05 | `alembic env.py` 添加 `render_as_batch=True` | Phase 16 | ✓ Complete (verified with fresh-DB upgrade) |
| TD-06 | 启动时自动 `alembic upgrade head` | Phase 16 | ✓ Complete (env-gated AUTO_MIGRATE) |
| TD-07 | `WishDeepLinkRedirect` 补 `encodeURIComponent(id)` | Phase 16 | ✓ Complete |
| TD-08 | `actingId` 跨卡片点击残留修复 | Phase 16 | ✓ Complete (finally-block reset) |
| TD-09 | 后端测试套件 107 fail → 0 fail | Phase 16 | ✓ Complete (350 passed, 0 failed) |
| TD-10 | 前端 lint 基线 101 errors → 0 errors | Phase 16 | ✓ Complete (0 errors, 0 warnings) |

## Coverage

- v1.4 requirements: 10 total
- Mapped to Phase 16: 10
- Completed: 10/10 (100%)

## Notes

- TD-05 was originally mis-diagnosed in REQUIREMENTS.md (render_as_batch alone wasn't sufficient); corrected during research to also include migration 8a258d50ee87 batch_alter_table refactor
- TD-09 root cause: trailing-slash mismatch between test calls (`/api/dishes/`) and router registration (`@router.post("")`) + SPA catch-all shadowing
- TD-10 root cause: react-hooks v7 compiler-era rules (immutability, set-state-in-effect) — 24+5 of 101 errors
